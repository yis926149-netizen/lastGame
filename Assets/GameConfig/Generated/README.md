# Assets/GameConfig/Generated —— 自动生成的运行期 SO（禁止手改）

本目录下的 `.asset` 由 `Tools/游戏配置/导入并校验` 从 `Config/Generated/game-config.json` 生成，**不允许人工修改**。

| 资产 | 说明 |
| --- | --- |
| `UnitBalanceDatabase.asset` | 单位平衡数据库（阶段1 示例表，自动生成、只读） |

需要调整数值时，请编辑 `Config/Excel/游戏数值配置.xlsx` → 运行导出器 → 重新导入，不要直接改本目录资产。
