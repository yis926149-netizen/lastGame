# 延期优化清单（全模块汇总）

本文档汇总模块 04 ~ 16 的延期改进项。所有项目都应先用 Unity Profiler 建立基线，再决定优先级。

---

# 模块 04 - 地图数据与程序化生成

本文记录地图数据、程序化生成与河流资源模块中暂不处理的边界保护、算法和性能事项。

## 极小或全海地图保护

### 当前情况

- `MapGenerationConfigSO` 没有最小地图尺寸和最少可出生陆地数量约束。
- 极小地图可能使 AI 随机出生位置的上界非法。
- 全海地图或可用陆地不足时，玩家出生逻辑可能持续查找合法地块，无法进入玩家回合。
- 当前正式资产使用 11x11 地图，正常配置下未发现该问题。

### 后续方向

- 明确玩家、AI、城市和初始单位所需的最小出生容量。
- 地图生成完成后一次性收集合法出生地块，不使用无界随机重试。
- 候选数量不足时给出明确配置错误，并安全终止启动流程。
- 增加 1x1、全海、仅一个陆地格及出生地块不足的测试。

## 地图服务生成期快照

### 当前情况

- `RiverGenerator` 通过公共 `IMapDataService` 查询邻居，因此 `MapGenerator` 会先发布基础地块数据，河流、地貌和资源完成后再发布完整数据。
- 当前生成和启动流程是同步的，现有调用顺序下未发现实际运行错误。
- 服务直接暴露部分内部可变集合，未来引入异步生成、生成事件或新的消费者后可能看到或修改半成品。

### 后续方向

- 为地图生成阶段建立私有生成上下文，向河流算法提供只读地块和邻接查询。
- 全部生成阶段成功后，再向 `IMapDataService` 原子发布完整快照。
- 对外返回只读集合或副本，避免消费者修改服务内部索引。
- 补充初始化前访问、生成失败保留旧快照及外部集合不可修改测试。

## 坐标查询性能

### 当前情况

- `WorldToHexCoordinate` 会线性扫描全部地块中心，时间复杂度为 O(n)。
- 默认 11x11 地图规模较小，暂未发现明显卡顿。

### 后续方向

- 使用世界坐标到立方坐标的解析式换算和 cube rounding，将查询降为 O(1)。
- 保留地图成员检查和六边形边界判断，避免地图外输入吸附到边缘地块。
- 使用 Profiler 对比默认地图和大地图上的调用耗时与 GC。

## 生成与渲染性能

### 待测指标

- 默认 11x11、较大地图和极端合法配置的总生成耗时。
- 地形、河流、地貌、资源和网格各阶段耗时。
- 峰值 GC、场景对象数量、Mesh 数量、顶点数和 Draw Call。
- 连续生成及重复渲染后的对象数量和内存变化。

### 潜在优化方向

- 减少地图集合的重复复制和临时 List 分配。
- 避免为每个地块复制整图网格数据，评估共享 Mesh、合批或实例化方案。
- 重建地图时统一管理运行时地图根节点，避免河水、湖海、网格、地貌和资源对象累加。
- 优化寻路中的成员查询、优先队列重复入队和过期节点处理。

## 规则说明

- 河流允许在内陆结束，内陆河是合法生成结果。
- 后续算法调整不得强制所有河流连接海洋，除非玩法规则再次变更。

---

# 模块 06 - 单位、地块、移动与战斗

本文记录本轮为控制风险而没有实施的性能、算法和结构优化。

## 寻路算法

- 将 `CalculateMinMovementCostBetweenTwoHexes` 和 `GetAllReachableHexesFromStartHex` 合并为一个共享的 Dijkstra 核心，避免两份规则继续漂移。
- 把 `allPoints.Contains` 从 `List<Vector3>` 改为 `HashSet<Vector3>`，避免每个邻接点进行线性查询。
- 在目标节点从优先队列出队后提前结束最短路搜索，不必耗尽整个候选队列。
- 只在距离真正改善时入队；当前实现会把未改善的邻接点重复入队。
- 评估使用整数六边形坐标代替 `Vector3` 字典键，明确坐标相等规则并减少浮点契约风险。
- 对相同起点、移动力和地图通行版本缓存本帧的可达范围，供高亮、预览和 AI 重用。

## 移动任务

- 将移动任务、目标预留和完成状态封装成显式命令对象，替代多个公开布尔字段。
- 支持取消时停在"最后已结算地块"，而不是统一回到起点；需要先明确产品规则和逐格占用模型。
- 用事件或完成句柄替代 AI 每帧轮询 `IsBusy`。
- 场景切换时集中取消所有移动和攻击命令，并增加 PlayMode 生命周期测试。

## 战斗规则

- 把玩家输入、信息面板和 AI 的攻击合法性统一到独立战斗服务，集中处理阵营、射程、目标存活、伤害、反击和行动消耗。
- 用单位数据中的能力字段代替 ID 3、5、9 判断远程单位。
- 明确并实现反击规则；当前项目没有足够设计依据，本轮不擅自增加玩法。
- 将动画表现与伤害结算时点解耦，用一次性战斗结果驱动表现，避免 Animator 状态成为领域状态来源。

## 仓库与生命周期

- 为仓库增加 `GameObject -> owner` 唯一索引，避免每次跨 AI 组扫描。
- 将 `UnitRemovalService` 的幂等标记迁移到实体生命周期状态，避免 `_removedUnits` 长期强引用所有历史对象。
- 为建筑建立与单位一致的仓库和幂等移除服务，统一城市、攻防建筑、祭坛和科文建筑索引。

## 性能验证

- 在默认地图和较大地图记录玩家范围高亮、路径预览、完整 AI 回合的 CPU、GC Alloc 和候选队列峰值。
- 使用固定地图和固定单位布局对比优化前后的寻路结果，确保性能改动不改变规则。
- 长局批量生成/移除单位后使用 Memory Profiler 检查 GameObject、移动任务和仓库引用是否回落。

---

# 模块 07 - 玩家输入、选择状态与镜头

本文记录玩家输入、选择状态与镜头模块中暂不实施的性能、算法和结构优化。

## 射线与输入分配

- 将 `Physics.RaycastAll` 替换为复用缓冲区的 `Physics.RaycastNonAlloc`，并对命中结果进行原地排序或单次最优目标筛选，减少连续点击时的数组分配。
- 复用 EventSystem 的 `PointerEventData` 和 `RaycastResult` 列表，避免检测卡牌拖拽时重复创建临时对象。
- 将卡牌拖拽状态通过明确事件从 `CardController` 传给地图高亮系统，删除 `PlayerInputHandler` 对 EventSystem 命中结果的二次推断。
- 若后续统一单位和建筑 Layer，改用窄 LayerMask 限制物理候选集；迁移时需批量验证所有动态生成 Prefab 的根节点和 Collider 子节点。

## 路径与高亮

- 缓存当前选中单位的起点、剩余移动力和地图占用版本，避免鼠标悬停时重复构造全部六边形坐标列表。
- 复用移动路径的顶点、UV、三角形列表和 Mesh，避免每次悬停新地块时产生集合与 Mesh 数据分配。
- 为地格高亮建立所有者或通道模型，区分移动范围、攻击范围、卡牌落点和远程攻击，避免多个系统通过 `SetActive` 相互覆盖。
- 用事件更新敌方指示器，替代每次选择时 `FindGameObjectsWithTag` 后全部销毁重建。

## 场景引用

- 将 `UIModel`、`Movement_Indicator`、单位/建筑父节点和敌方指示器父节点改为 Installer 显式引用，减少运行时 `GameObject.Find` 并让场景契约可验证。
- 为所有 Build Settings 场景增加统一的 Missing Script、空序列化引用、Tag、Layer 和 Collider 完整性 Editor 测试。

## 镜头验证

- 用 PlayMode 参数化测试记录 30、60、120 FPS 下相同键盘时长和滚轮刻度的最终位置差异。
- 在不同地图尺寸、宽高比和旋转角度下验证边界约束；如产品需要镜头视锥完全留在地图内，再由当前"相机点边界"升级为视锥投影边界算法。
- 使用 Profiler 记录持续平移、缩放和旋转时的 CPU、GC Alloc 与 SmoothDamp 收敛时间，再决定是否合并位置和旋转平滑状态。

---

# 模块 08 - 卡牌、手牌、解锁与部署

本文记录卡牌、手牌、解锁与部署模块的性能和算法改进。

## 性能

- `CardController.ClearHighlights()` 当前在每次取消或完成拖拽时遍历全图。可由高亮系统记录本次实际修改的地格，只清理该集合，将复杂度从 O(全地图格数) 降为 O(高亮格数)。
- `PlayerInputHandler` 的卡牌命中检测可复用 `PointerEventData` 和 `RaycastResult` 列表，减少拖拽期间的逐帧临时分配和 GC 压力。
- `CardPresenter` 与 AI 生成流程中的 `GameObject.Find` 可替换为 installer 注入的 `PlayerUnit`、`EnemyUnit`、`PlayerBuilding`、`EnemyBuilding` 根节点，降低运行时查找成本并强化场景配置校验。
- `CardUnlockRuleProvider` 当前每次抽牌都会校验数据库、创建集合并返回新列表。可在启动时完成一次数据库校验，并按科技/文化等级缓存只读解锁集合。

## 算法与结构

- 玩家和 AI 的单位/建筑部署仍有大量重复逻辑。后续可抽取统一部署服务，集中处理合法性校验、Prefab 初始化、仓库登记、地格占用、势力/视野更新及失败回滚，避免两条路径继续分化。
- AI 每回合会复制势力范围、执行 LINQ 过滤并随机选点。地图扩大或增加多个 AI 时，可维护随占用和归属变化增量更新的合法部署格集合。
- 卡牌 ID 目前通过"单位数量 + 建筑数据库 ID"编码。后续可引入显式卡牌定义记录，直接关联显示图、Prefab、解锁节点和部署类型，减少平行数组及下标偏移契约。
- `IUnitDataProvider.GetUnitIconCount()` 应返回 `int` 而不是浮点类型，避免反复强制转换并使卡牌 ID 边界更明确。

---

# 模块 09 - 科技、文化、成长与回合结算

本文记录科技、文化、成长与回合结算模块中暂不实施的性能、算法和结构优化。

## 进度与升级算法

- 当前进度使用 `0..1` 归一化值并通过 `Mathf.Min` 截断，超过当前等级阈值的点数会丢失。后续应改为保存绝对点数，逐级扣除各等级成本，明确一次结算能否连续升级，并为玩家和 AI 使用同一套计算函数。
- 当前 `SwitchOptionsTurn` 将每种进度限制为每回合最多升级一次。需要先确认这是玩法规则还是动画节流；若只是动画限制，应将领域升级与 UI 动画队列拆开，避免表现层决定升级结果。
- 满级应采用显式状态，不再累计无效进度或反复播放填满动画。最大等级应由统一的科技/文化节点定义决定，而不是分别依赖图标、名称、描述和成本列表长度。
- 玩家和 AI 当前各自实现科技文化累计逻辑。后续应抽取无 Unity UI 依赖的进度模型，统一阈值、溢出、满级和多级升级规则，并用参数化测试覆盖从 0 级到满级的边界。

## 动画与分配

- `ProgressBarAniInterp` 每次动画都会按当前帧率创建 `List<float>`。可改为记录起点、终点、开始时间和时长，在 `Update` 中直接按时间插值，消除列表和逐元素分配。
- 科技与文化的动画、节点切换和领域数据目前都由 `Update` 轮询。可在点数或等级变化时发布事件，仅在动画活跃期间更新 UI。

## 成长效果与结算结构

- 文化等级效果按单位 ID 和建筑类型写在大型 `switch` 中。后续可将目标、属性和数值配置为只读升级定义，由统一应用器处理现有实体和新生成实体，减少两条路径规则漂移。
- 结算阶段目前多次遍历全部玩家单位并重复查询所在格。可在一次遍历中取得地格后依次计算地貌和建筑恢复；地图或单位规模扩大后，再根据 Profiler 结果考虑缓存已提交地格。
- 科技文化建筑以增减固定产量维护总值。后续可从当前有效建筑集合派生产量，或把增减封装成幂等的领域登记，避免新增销毁路径时遗漏回滚。

---

# 模块 10 - AI 决策、行动队列与容错

本文记录 AI 决策、行动队列与容错模块中暂不实施的性能、算法和结构优化。

## 决策与寻路

- `HandleSingleUnitTurn` 会为每个 AI 单位复制全地图坐标，并分别对每个玩家单位和建筑执行完整最短路径计算。地图或单位规模扩大后，应通过一次多目标搜索、距离场或按世界版本失效的路径缓存减少重复计算，并以 Profiler 数据决定实现。
- 当前远程单位只在目标已处于射程时攻击，否则随机移动，不会主动移动到合适射击位置。后续应评估所有可达格的射程、地形和风险后选择站位。
- 近战目标选择以路径成本最小为主，随机移动也不保留战略方向。后续可建立可解释的效用评分，综合目标价值、预计伤害、城市防御和资源收益。
- 开局相邻陆地组合目前会完整收集后随机选择。超大地图可改为蓄水池采样，在保持均匀随机的同时避免保存全部组合；当前地图规模下无需提前优化。

## 可复现性与容错

- AI 同时使用 `System.Random` 和 `UnityEngine.Random`，且种子不可注入，无法稳定复现决策。后续应统一随机源并由对局种子派生 AI 独立种子，将种子写入诊断日志和存档。
- 移动与攻击等待依赖控制器最终清除忙碌标志，尚无动作级超时或取消令牌。后续可为行动定义明确完成结果与超时策略，超时时取消预留和动作状态，而不是仅依赖协程轮询。
- AI 异常当前按"记录并结束该阶段"降级。后续可增加结构化上下文，包括回合号、AI 索引、单位 ID、起点、目标和卡牌状态，便于复现场景。

## 多 AI 与状态边界

- `AIIndex = 1`、单个 `AIPlayerState` 和单组科技文化数据决定了当前架构只支持一个 AI。若需要多 AI，应以 AI 标识索引独立保存卡池、科技文化、城市、单位和随机源，并让回合阶段逐个调度。
- 科技文化进度、卡牌规则和玩家实现仍有重复，可能随玩法迭代发生规则漂移。应抽取无 UI 依赖的共享领域逻辑，但需先统一溢出、多级升级和满级规则。

## 行动队列遗留代码

- `CommandQueue` 和 `MoveCommand` 没有发现命令提交方，且 `MoveCommand.DequeueValidate` 的代码、注释与 `isMoved` 语义不一致。后续应先通过场景序列化和引用扫描确认完全未使用，再删除；若决定恢复使用，则应补齐空对象、目标变化、队列清理和重复执行测试。
- 静态全局队列没有场景重载清理和异常隔离。若该模式重新启用，应改为有明确生命周期的实例服务，并保证单条命令失败不会阻断后续命令。

---

# 模块 11 - 城市、建筑、势力归属与胜负

本文记录城市、建筑、势力归属与胜负模块的性能、算法和架构改进。

## 性能

- `EndGame.Update` 当前每帧汇总全部 AI 城市数。后续可在城市创建、易主和销毁时发布城市数量变化事件，由结束判定按事件运行一次。
- 城市移除或易主时会遍历该阵营全部单城范围重建总势力。当前地图和城市规模下优先保证正确；规模扩大后可为每个地块维护同阵营城市覆盖计数，把移除成本限制在变化范围内。
- 城市接管会逐格清理附属建筑和多个玩家建筑字典。后续可建立统一建筑仓库，以建筑对象直接定位类型、属主、地块和产量贡献，避免扫描多个索引。

## 算法与模型

- 当前重叠地块只能保存一个 `Player_City_Index`，单城范围却允许多个城市同时包含该地块。后续若扩张、补给或附属建筑规则依赖"所有覆盖城市"，应显式存储覆盖城市集合，而不是依赖字典遍历顺序选择一个城市 ID。
- 城市易主目前按旧城市的独占范围整体转移，并销毁该范围内原属主附属建筑。建议把"范围转移、建筑处理、产量回滚、视觉更新"封装为一个可回滚的领域事务，并为保留、摧毁或一并接管附属建筑建立明确配置。
- 玩家和 AI 建城逻辑仍分散在 `UIController` 与 `IAIManager`。后续可抽取共享建城服务，统一校验、ID 分配、Prefab 初始化、单位消耗、势力扩张和事件发布，减少两条入口再次漂移。
- 胜负 UI 目前只有一套结束表现，虽然领域结果已区分胜利、失败和平局，但尚未展示不同文案或动画。后续应为三种结果配置明确表现，并决定"玩家从未建城"是否存在失败回合上限。

---

# 模块 12 - 游戏 UI、开始场景与交互状态

本文记录游戏 UI、开始场景与交互状态模块的性能、算法和架构改进。

## 性能

- 远程攻击范围当前遍历全部地块并计算六边形距离。后续可由地图服务按中心和半径枚举环形范围，避免随全地图规模线性扫描。
- 网格高亮仍通过 `renderer.material` 访问颜色，会为 Renderer 创建材质实例。后续应统一改用 `MaterialPropertyBlock` 或独立 overlay，并由单一高亮服务维护层级。
- `PlayerInputHandler` 与 `UIController` 各自创建敌方单位指示器。后续应合并为一个状态驱动的指示器管理器，并复用对象或使用对象池。
- 开始场景仍有较多 `GameObject.Find` 和按名称查找层级。后续可缓存序列化引用，尤其是按键弹窗和页面切换路径。
- 设置页在 `Awake` 中一次实例化所有页面控件。设置系统正式实现后，可按首次打开页面延迟创建。

## 算法与状态模型

- 移动、攻击、卡牌放置和全局网格目前共享同一批 `GridMesh` 的激活状态与材质。建议建立有优先级的高亮通道模型，由基础网格、移动范围、攻击范围、悬停和卡牌放置分别提交状态，统一合成最终表现。
- 远程技能模式现已由全局输入入口执行命令，但展示状态仍位于 `UIController`。后续可建立单一交互状态机，使选择、目标模式、取消、阶段变化和命令提交由同一所有者管理。
- 开始菜单命令仍依赖动态列表的固定下标。建议为按钮配置显式命令枚举或序列化事件，并在启动时校验"可见、可交互、已绑定"三者一致。
- 选项与按键绑定需要独立配置模型，明确默认值、应用时机、持久化格式和运行时消费者。实现前不应重新开放当前控件。
- `UIManager` 等组件仍依赖固定 `GetChild` 层级。后续可改为序列化组件引用或小型 View 组件，降低 Prefab 调整造成运行时越界的风险。

---

# 模块 13 - 音频、动画、特效与结束表现

本文记录音频、动画、特效与结束表现模块的性能、算法和架构改进。

## 性能

- 高频 SFX 当前都由单个 `AudioSource.PlayOneShot` 混播。后续可按同时发声上限建立 SFX AudioSource 池，并为攻击、UI、环境等类别设置优先级，避免密集战斗时无上限叠加。
- 音频 Clip 全部由 `ProjectContext` 常驻引用，会让全部 BGM/SFX 随全局上下文常驻内存。后续可按场景或音频组异步加载与释放，并结合实际导入设置选择 Decompress On Load、Compressed In Memory 或 Streaming。
- `EndGame.Update()` 每帧遍历 AI 城市计数字典求和。后续可由城市所有权服务维护总数或发布城市数量变化事件，仅在状态变化时评估胜负。
- 卡牌和菜单动画会分别创建多个独立 Tween。后续可使用 `Sequence` 统一所有权和完成回调，并对频繁复用的 UI 项启用可复用 Tween，减少短生命周期分配。
- 攻击命中特效通过固定子节点激活。若后续改为实例化粒子 Prefab，应使用对象池并按粒子实际停止事件回收，避免战斗期间反复 Instantiate/Destroy。

## 算法与状态模型

- BGM、SFX 音量目前只是运行时字段，未连接选项持久化。设置系统实现时应引入 `AudioMixer` 分组，用对数映射控制通道音量和静音，并在全局音频服务创建时恢复配置。
- 胜负表现使用 1.5 秒和 6.5 秒固定延时，与视频真实长度和播放失败状态无关。后续应监听 `VideoPlayer.prepareCompleted`、`loopPointReached` 和错误事件，使 UI 切换由媒体状态驱动并保留超时兜底。
- 攻击动画、伤害、音效和粒子目前由脚本固定延时及 Animator bool 混合驱动。后续可定义一次攻击表现序列，由动画事件或统一时间轴发出命中和结束信号，但核心伤害提交仍应有独立兜底，不能依赖表现事件才能完成回合。
- 结束界面目前只共用一套视频和 UI，`EndGameResult` 没有驱动胜利、失败、平局的差异化内容。后续可用结果配置表映射视频、文本、音效和可用按钮，避免分支散落在场景脚本中。
- 音频调用使用字符串名称，重命名资源只能在运行时发现。后续可改为受控 ID/枚举或 ScriptableObject 音频目录，并在 Editor 测试中校验所有调用 ID 都存在。

---

# 模块 15 - 包体、资源与性能基线

本文记录包体、性能和资源内存优化。本轮不实施，后续必须先做引用扫描、备份基线和构建对比，不能直接批量删除或改导入设置。

## P1 包体：拆分 `Assets/Resources/Import`

- 当前 Windows Development Build 为 1.7 GB，其中用户资产 1.6 GB。
- 纹理占 1.4 GB（85.8%），网格占 190.1 MB（11.5%）。
- Build Report 显示第三方 Demo、PSD、TerrainData、4K 贴图等均从 `Assets/Resources/Import` 进入 Player；前三个 PSD 各占未压缩 64 MB。
- 后续方案：扫描 `Resources.Load`、场景、Prefab、材质和 ScriptableObject 的 GUID 引用；将实际业务资产移出总 `Resources`，删除或隔离未使用 Demo/Documentation/Welcome Screen；重新构建并对比 Build Report 前 20 项。
- 验收：两套发布场景无缺失资源、无粉色材质，关键特效和 HighlightPlus 正常，包体变化有前后数据。

## P1 内存：关闭不必要的模型 Read/Write

- 静态扫描发现 `Resources` 内约 591 个启用 Read/Write 的资产，其中绝大多数为 FBX；业务角色 FBX 也存在同类设置。
- 后续方案：先搜索 `mesh.vertices`、`mesh.triangles`、`MeshCollider` 动态写入和运行时合批需求，再分批关闭无读取需求模型的 Read/Write。
- 验收：Memory Profiler 对比 Mesh 内存，地图生成、动画、碰撞和特效无回归。

## P2 视频与 RenderTexture

- `WinGame.renderTexture` 为 1080x1080 且带深度模板，视频导入尺寸为 752x416。
- 后续方案：按实际 UI 显示尺寸评估降低 RenderTexture 分辨率并移除不需要的深度缓冲。
- 验收：胜负视频无明显锯齿、拉伸或闪烁，并记录显存变化。

## P2 音频加载

- 两段较长 BGM 当前使用 Decompress On Load，源 WAV 约 12 MB 和 3.6 MB。
- 后续方案：对 BGM 评估 Streaming 或 Compressed In Memory；短音效保留适合低延迟的加载方式。
- 验收：对比启动时间、音频内存、循环接缝和播放延迟。

## P2 质量档位与帧预算

- Standalone 默认使用 Ultra，阴影距离为 200，粒子射线预算为 4096。
- 后续方案：在明确最低硬件后建立 Low/Medium/High 基线，按目标帧率调整阴影距离、抗锯齿、粒子和后处理。
- 验收：记录硬件、分辨率、画质档位，以及默认地图和 20 回合流程的 FPS、主线程峰值、GC Alloc、内存和 Draw Call。

## P3 未使用包与重复资源

- Ads、Purchasing、Development Feature 和测试依赖会扩大依赖面；业务未发现 Ads/Purchasing 调用。
- 存在内容相同的字体和多套重复 Stage FBX，但 Unity 最终是否去重需由构建结果确认。
- 后续方案：逐包核对程序集和业务引用后移除未使用包；使用内容哈希、GUID 引用和 Build Report 三方证据处理重复资源。
- 验收：干净 Library 可恢复依赖并成功构建，测试程序集仍可运行，发布流程无缺包。

---

# 模块 16 - 地图查询、AI 搜索与移动预览

本文记录模块 16 中发现的性能与算法优化机会。所有项目都应先用 Unity Profiler 建立默认地图和连续回合基线，再决定优先级。

## P1 地图查询与寻路输入分配

- `HexMapService.GetAllCells()` 和 `GetAllHexCoordinates()` 每次调用都会通过 `ToList()` 复制全图集合，移动、路径预览和 AI 行动会重复产生分配。
- `HexMapService.GetCellByWorldPosition()` 线性扫描全部地块中心点，频繁选择、移动和战斗时成本随地图规模增长。
- 后续方案：为地图拓扑提供生命周期明确的只读视图或缓存；世界坐标查询改用六边形坐标取整并保留边界校验。
- 验收：对比默认及最大地图上的单次查询耗时、每回合 GC Alloc，并覆盖边缘地块和浮点误差测试。

## P1 AI 目标搜索

- `IAIManager` 会为多个单位遍历玩家单位和场景建筑，并对候选目标重复执行最短路搜索。
- 后续方案：缓存本回合目标集合，先按六边形距离筛选少量候选；地图较大时评估多源 Dijkstra 或共享最短路径树。
- 验收：记录不同单位数、城市数下 AIPhase 总耗时，确保目标选择和合法移动行为不变。

## P2 移动预览与网格材质

- `PlayerInputHandler` 在悬停目标变化时重复寻路并重建预览 Mesh。
- 高亮流程访问 `Renderer.material` 会产生材质实例，长时间选择和取消可能增加内存与批次。
- 后续方案：选中单位时缓存可达范围/最短路径树，复用 Mesh 缓冲；颜色覆盖改用 `MaterialPropertyBlock`。
- 验收：快速移动鼠标 60 秒，对比主线程、GC Alloc、材质实例数和 Draw Call。

## P2 出生点选择

- `GameFlowManager` 的出生点选择循环会重复获取全地图列表；没有合法地块时缺少明确失败出口。
- 后续方案：生成后一次性筛选合法候选，随机抽取并在空集合时返回可诊断错误。
- 验收：覆盖正常地图、极小地图和无合法陆地配置，确认不会无限等待。

## P3 无行为组件开销

- `MapRenderer` 为大量地貌和资源实例添加 `ModelController`，该组件当前只在 `Start()` 查找摄像机，之后没有运行行为。
- 后续方案：确认没有 Prefab/动画事件依赖后移除动态附加，并比较地图生成时间及组件数量。
- 验收：地图视觉、点击和动画无回归，启动帧耗时有前后数据。
