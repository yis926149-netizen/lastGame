# 本项目转 H5 小游戏难点分析报告

> 分析对象：`E:\BaiduNetdiskDownload\毕设\My project - new`
> 分析方式：仅静态阅读源码与工程配置，**未改动任何代码/资源**
> 结论口径：H5 小游戏 = **Unity 官方 WebGL 目标**构建产物，跑在浏览器（桌面 Chrome/Edge、手机微信内置浏览器、iOS Safari、Android Chrome）里，无需微信小游戏适配器（`minigame-unity-webgl-transform`），也不受微信 4MB/20MB 代码包限制。
> 与同目录 `微信小游戏改造难点分析报告.md` 的关系：微信小游戏是"WebGL + 微信适配层 + 微信容器"；本报告聚焦"**裸 WebGL 进浏览器**"这条路径下**新增/变重**的难点，微信适配器特有的 SDK 项不再重复展开。

---

## 0. 一句话结论

本项目是一个 **Unity 2022.3.62f3c1 的六边形地块实时策略游戏**，业务代码本身相当干净（无文件 IO、无线程、无网络、无 `dynamic`、无反射 Emit），但从工程形态看它是**面向桌面（鼠标/键盘 + 内置渲染管线 + 全量常驻资源）**写的。转 H5 的第一道坎不是"能不能编译出 WebGL 包"，而是：**4.4 GB 资源让浏览器首屏加载变成灾难、内置管线的迷雾/后处理链路在移动浏览器上又重又脆、纯桌面输入在触屏上残缺、IL2CPP+AOT 会在 DI 反射处炸**。**它无法"一键转 H5"，必须经过资源瘦身与远程化、渲染链路收敛、输入触摸化、AOT 加固四轮结构性改造。**

---

## 1. 项目技术画像（据实提取）

| 维度 | 实际情况 | 证据 |
|---|---|---|
| 引擎版本 | Unity **2022.3.62f3c1** | `ProjectSettings/ProjectVersion.txt` |
| 游戏类型 | 六边形地块策略（探索/占地/卡牌/战斗/公共建筑/迷雾/山脉/后勤） | `Assets/Scripts/` 目录 |
| DI 框架 | **Zenject**（MonoInstaller + 场景强绑定） | `GameInstaller.cs` |
| 响应式 | **UniRx** | `Assets/Plugins/UniRx` |
| 动画/缓动 | **DOTween** | `Assets/DOTween_1_2_765` |
| 文本 | **TextMesh Pro 3.0.9** | `Packages/manifest.json` |
| 几何 | **LibTessDotNet**（多边形三角化） | `Assets/Plugins/LibTessDotNet` |
| 渲染 | **内置管线为主**（QualitySettings 六档全部内置管线，仅 Ultra 档挂 URP）；同时装有 URP14(ShaderGraph) + Post-Processing Stack v2 | 见 §3.2 |
| 平台插件 | Unity IAP / Ads / Analytics / **Coplay** | `Packages/manifest.json` L3-5 |
| 资源总量 | **Assets 4,424.6 MB（14,662 文件）**，其中 `Assets/Resources` 独占 **3,669.9 MB（7,058 文件，几乎全在 `Resources/Import`）** | 本地实测 |
| 主程序集 | `Assets/Scripts/MainGame.asmdef` + `Assets/GameConfig/Runtime/GameConfig.Runtime.asmdef` | `*.asmdef` |
| 无 StreamingAssets | 项目内**没有** `StreamingAssets` 目录（仅构建产物 `AuditReports/.../StreamingAssets` 下有一份残留） | 目录检索 |

关键 WebGL 侧 PlayerSettings 现状（`ProjectSettings/ProjectSettings.asset`）：

| 项 | 值 | H5 影响 |
|---|---|---|
| `m_ActiveColorSpace` | **1（Linear）** | WebGL 需 WebGL2/GLES3 + 一次颜色空间转换，增加带宽与开销（L50） |
| `webGLMemorySize / InitialMemorySize` | 16 / 32（MB） | 初始 WASM 堆偏小，几乎必涨（L730/L746） |
| `webGLMaximumMemorySize` | 2048（MB） | 上限，但受浏览器/设备实际内存约束（L747） |
| `webGLMemoryGrowthMode` | 2（几何增长） | 默认，可行（L748） |
| `webGLThreadsSupport` | **0（单线程）** | 无 Web Worker 线程，程序化 Mesh 全在主线程（L744） |
| `webGLCompressionFormat` | 1（Gzip） | 未开 Brotli，压缩率可再提高（L741） |
| `webGLLinkerTarget` | 1（WebAssembly） | 正常（L743） |
| `webGLDataCaching` | 1 | 启用 IndexedDB 缓存，利好（L734） |
| `managedStrippingLevel WebGL` | **1** | 中等裁剪 + 无主程序 `link.xml`，AOT 风险（L792） |
| `scriptingDefineSymbols WebGL` | `DOTWEEN;UNITY_POST_PROCESSING_STACK_V2` | PPv2 宏在 WebGL 生效（L771） |
| `m_BuildTargetGraphicsAPIs WebGL` | 自动 | 未显式锁 WebGL2/GLES3（L450-452） |
| WebGL 默认画质 | **High**（`WebGL: 3`） | 该档 `customRenderPipeline: {fileID: 0}` = 内置管线 |
| `defaultScreenWidth/HeightWeb` | 960×600 | 桌面横屏默认尺寸（L47-48） |

---

## 2. H5 与微信小游戏的关键差异（决定难点权重不同）

在逐条剖析前，先明确"转 H5"相对"转微信小游戏"**哪里更松、哪里更紧**，否则会把两件事混为一谈：

| 维度 | 微信小游戏 | H5（浏览器 WebGL） |
|---|---|---|
| 硬性代码包限制 | 主包 ≤ 4MB、总分包 ≤ 20MB | **无硬性数字限制**，但有"首屏加载时长"这道软性生死线 |
| 资源加载 | 可用微信 CDN/分包预下载，用户点开前已在加载 | 用户点链接**当场**开始下载，4.4GB 直接判死 |
| 运行容器 | 微信客户端 WebView（WebGL2/GLES3 可控） | 各家浏览器 + 各版本 Safari，**兼容面更宽更不可控** |
| 内存上限 | 微信容器有平台内存监控 | 浏览器内存**更不透明**，iOS Safari 尤其严 |
| 音频自动播放 | 微信侧规则 | **iOS Safari 强制要求用户手势解锁 AudioContext** |
| 存档 | `wx.setStorage`/云存档 | `PlayerPrefs`→IndexedDB（有配额）+ 需自建云存档 |
| 登录/支付/分享 | 微信 SDK | 需自有后端或微信 JSAPI 网页支付，且**要 ICP 备案域名 + HTTPS** |
| 适配层 | `minigame-unity-webgl-transform` | **无需适配层**，但也没有适配层帮你擦屁股 |

一句话：**H5 省掉了微信 SDK/适配器这套对接，但把"包体与首屏加载""浏览器兼容""iOS Safari 限制"这三座山抬得更高了。**

---

## 3. 难点分级与逐条剖析

按"能否不改架构直接过去"分四级：**致命 / 高 / 中 / 低**。

---

### 3.1 【致命】4.4 GB 资源 vs 浏览器首屏加载（H5 第一杀手）

微信小游戏有 4MB/20MB 硬限制逼着你做分包；H5 没有这个数字，但后果更糟——**浏览器没有"预下载"，用户点开链接的那一刻开始下载，等 3 秒流失一半，等 10 秒流失九成**。本项目实测：

```
Assets              4,424.6 MB   14,662 文件
Assets/Resources    3,669.9 MB    7,058 文件   ← 全部位于 Assets/Resources/Import
Assets/Scenes          54.6 MB      227 文件
Assets/Model           31.5 MB      245 文件
Assets/Texture         13.5 MB       18 文件
Assets/Particles        7.0 MB       31 文件
Assets/Materials       10.4 MB       46 文件
Assets/Scripts          1.9 MB      645 文件
```

`Assets/Resources/Import` 扩展名拆解：

```
.png  1,760.9 MB (446)
.tga    938.2 MB ( 54)   ← 设计源格式
.exr    411.3 MB ( 18)   ← HDR 源图，直接进包是灾难
.fbx    306.1 MB (1085)
.psd     76.8 MB (  6)   ← 设计源格式
```

对 H5 的三重打击：

1. **`Resources` 是"必打包"语义**。Unity 把 `Resources` 下所有资源打进构建的 `.data` 文件，且**无法被引用裁剪剥离、无法方便地按需远程化**。3.67 GB 的 `Resources/Import/*` 绝大多数是 demo 素材包（Toon RTS、RPG Monster、unity-chan、Toon Crystals、Cartoon FX Remaster/CFXR、Kino Bloom、HighlightPlus、Hyper Casual FX 等），几乎未被场景/代码引用，却因为躺在 `Resources` 里**全部进入构建**。这就是首屏加载爆炸的根因。

2. **源格式资产直接进包**：`.exr`(411MB) / `.tga`(938MB) / `.psd`(77MB) 是设计源图；WebGL 会把这些转换成纹理上传到 GPU，既不压缩（未配置 ASTC/ETC2，`ProjectSettings.asset` 无 `m_TextureMipmapLimitGroupNames` 配置）又白白膨胀下载量。

3. **引用链与"远程加载"天然冲突**：`GameInstaller.cs` L9-51 用 `[SerializeField]` 直接序列化了 **30+ 个 ScriptableObject 与场景对象引用**（`_unitDatabaseSO`、`_buildingDatabaseSO`、`_uiConfigSO`、`_mapGenerationConfigSO`…），这些 SO 再各自引用 Prefab/材质/纹理。这条"场景内直接拖引用"的链子，与"H5 用 AssetBundle/Addressables + CDN 远程加载"的结构直接冲突——要远程化必须把整条引用链改成可寻址加载。

> **H5 特有风险**：微信小游戏可以靠 CDN + 分包预下载缓解；H5 的 `Resources` 全量打包意味着 `.data` 动辄几 GB，CDN 下载 + 浏览器解压（Gzip，未开 Brotli）+ WASM 初始化会让用户"看白屏看到关页面"。
>
> 改造代价：**引入 Addressables/AssetBundle + CDN、资源去重/降级/压缩、源格式转码**，属"重做资源管线"级别，是工作量最大的一步。

---

### 3.2 【致命】渲染链路：表面着色器 + `finalcolor` + 内置管线 + PPv2 并存，迷雾深度依赖内置图像特效

这是**最隐蔽的雷**。三套渲染相关的东西同时存在且互相矛盾：

1. **URP 包已装但没真正用**：`manifest.json` 有 `com.unity.shadergraph: 14.1.0`（URP14），`GraphicsSettings.asset` L81 `m_SRPDefaultSettings` 指向一个 URP 资产；
2. **实际跑的是内置管线**：`GraphicsSettings.asset` L56 `m_CustomRenderPipeline: {fileID: 0}`；`QualitySettings.asset` 六档里只有 Ultra 档挂了 URP（L292），其余全内置；而 **WebGL 默认画质是 High 档**（L316 `WebGL: 3`），High 档 `customRenderPipeline: {fileID: 0}` = 内置管线；
3. **PPv2 宏已对 WebGL 生效**：`scriptingDefineSymbols.WebGL = DOTWEEN;UNITY_POST_PROCESSING_STACK_V2`（L771），而 `com.unity.postprocessing: 3.4.0` 是**内置管线的后处理方案**（`OnRenderImage` 系），与 URP 完全不兼容。

**本项目所有自定义 Shader 都是"内置管线表面着色器"，且迷雾强绑 `finalcolor` 修饰器**（全文检索 `Assets/Shader/*.shader`）：

```
Fog.shader                       #pragma surface surf Lambert noforwardshadows
TerrainBase_Fog.shader           #pragma surface surf Standard ... finalcolor:fogFinal
ThreeMaterialBlend_Land.shader   #pragma surface surf Standard ... finalcolor:fogFinal
RealMaterialMaskBlend.shader     #pragma surface surf Standard ... finalcolor:fogFinal
River.shader                     #pragma surface surf Standard alpha ... finalcolor:fogFinal
MountainLowPoly_Fog.shader       #pragma surface surf Standard ... finalcolor:fogFinal
ExplorationPillar.shader         #pragma surface surf Standard ...
（全部 #pragma target 3.0）
```

`finalcolor:fogFinal` + `#pragma surface` 是**内置管线专属**：URP 下 Surface Shader 不会被自动转换，`finalcolor` 直接不执行。这意味着：

- **切到 URP**：所有地形/河流/山脉 Shader 需用 HLSL/ShaderGraph 重写，迷雾"末色处理"要用 `RendererFeature`/`RenderPass` 重做；
- **留在内置管线**：迷雾核心 `FogEnvironmentSelectiveEffect.cs` 用了一整套内置管线图像特效 API（见下），在移动浏览器上又重又脆。

**迷雾系统写死内置管线图像特效 API**（`FogEnvironmentSelectiveEffect.cs`）：

- L114 `OnRenderImage(RenderTexture, RenderTexture)` + L118/L126 `Graphics.Blit(...)`——内置管线全屏后处理入口，URP 下不被调用；
- L283-286 创建**整屏 RenderTexture**（`ARGBHalf` + `RenderTextureReadWrite.Linear`）存对象遮罩；
- L303-351 `CommandBuffer` + `CameraEvent.BeforeImageEffects` 每帧把"地貌/资源/建筑/单位/单位UI"用 5 套 Shader（`Hidden/FogEnvironmentObjectMask*`、`Hidden/FogEnvironmentUnitErase`、`Hidden/FogEnvironmentUnitUIErase`）**重绘一遍**到遮罩 RT，再与场景色合成。

对 H5 的三重问题：

- **URP 兼容**：`OnRenderImage`/`Graphics.Blit`/`CameraEvent.BeforeImageEffects` 全部失效，切 URP 必重写；
- **内置管线 + WebGL 的开销**：`OnRenderImage` 本身在 WebGL 内置管线**可用**，但"全屏 `ARGBHalf` + `Linear` RT + 每帧对所有 Renderer 多一遍 drawcall + 深度纹理"在手机上带宽与显存开销极高；
- **`ARGBHalf` + Linear 在 WebGL 的兼容门槛**：半浮点渲染纹理在 WebGL1 需要 `OES_texture_half_float` 扩展，`RenderTextureReadWrite.Linear` + HDR 在 WebGL2/GLES3 才稳；低端机型/旧 Safari 可能直接不达标。`GraphicsSettings.asset` L29-52 还常驻包含 **20 个 `m_AlwaysIncludedShaders`**，进一步抬高 shader 编译与体积。

> 改造代价：**先拍板"最终用哪条管线"，再把迷雾/后处理重写到该管线**（内置管线要降级遮罩 RT，URP 要用 RendererFeature 重写）。否则每次构建都在两套管线打架的状态里。

---

### 3.3 【致命】IL2CPP + AOT：Zenject/UniRx 反射 + 无主程序 `link.xml`

Unity WebGL **只支持 IL2CPP**（无 Mono），而 IL2CPP 是 AOT 编译，对运行时反射/泛型动态实例化敏感。本项目风险点：

- **Zenject**：依赖反射做注入解析。`Assets/Plugins/Zenject/OptionalExtras/ReflectionBaking` 可在编辑器期把反射"烘焙"成静态代码，**必须启用**，否则构造注入/`FromComponentInHierarchy` 解析会因 AOT 缺失而崩；
- **UniRx**：泛型 `IObservable<T>` 的操作符在 IL2CPP 下需要 AOT 注册/裁剪白名单；
- **主程序没有 `link.xml`**：全文检索仅命中 `Assets/Plugins/Zenject/Source/Usage/link.xml`、`Assets/Plugins/Sirenix/Assemblies/link.xml`、`Library/PackageCache/...newtonsoft-json.../link.xml` 三处。主程序大量依赖"接口 + 具体实现"的 DI 绑定（`GameInstaller.cs` 里 `IMapDataService → HexMapService` 等），`managedStrippingLevel WebGL = 1` 下极可能被裁剪。

**有利面**：主程序游戏代码全文检索**没有 `dynamic`、没有 `MakeGenericType`、没有实际 `System.Reflection` 调用、没有 `System.IO`/`Thread`/`Task`/`async`/网络**，AOT 风险高度集中在第三方插件内部。

> 改造代价：中——启用 Zenject ReflectionBaking、补主程序 `link.xml`、调整裁剪级别并真机回归。

---

### 3.4 【致命】平台插件必须剥离（同微信，但 H5 更直接）

`Packages/manifest.json` 挂了与 H5 不兼容的依赖：

| 包 | 问题 | 处理 |
|---|---|---|
| `com.coplaydev.coplay`（Git 依赖） | 开发期 AI 辅助插件，与运行无关 | 发布前移除 |
| `com.unity.purchasing: 4.11.0` | Unity IAP 在浏览器无商店，H5 支付需微信 JSAPI/支付宝/自建 | 移除或换 H5 支付 |
| `com.unity.ads: 4.4.2` | Unity Ads 无 H5 直连 | 移除或换 |
| `com.unity.analytics: 3.8.1` | 上报改走自有埋点 | 移除或换 |

未实际使用的 `com.unity.visualscripting` / `timeline` / `collab-proxy` 等应在 H5 构建中裁剪，减少 IL2CPP 包体与 AOT 面。

> 改造代价：中低，但属硬前置。

---

### 3.5 【高】输入纯鼠标/键盘，H5 触屏残缺

H5 在手机上跑，**没有滚轮、没有 WASD、没有 Ctrl、没有右键**。本项目输入层是纯桌面式：

`Assets/Scripts/Core/Services/InputService.cs`（唯一输入抽象）
- L17 `MousePosition => Input.mousePosition`
- L19-25 `GetMouseButton*/GetKey*` → `Input.GetMouseButton*/GetKey*`
- L27 `GetAxis` → `Input.GetAxis`
- L28 `MouseScrollDelta => Input.mouseScrollDelta.y`  ← 滚轮缩放

`Assets/Scripts/Controllers/CameraController.cs`
- L231-250 用 `GetAxis("Horizontal"/"Vertical")` + `LeftControl` 做**键盘平移**；
- L252-268 用 `MouseScrollDelta` 做**滚轮缩放**；
- L271-335 用 `Ctrl + A/D` 做**键盘旋转**；
- L405-433 用鼠标左键拖拽平移。

`Assets/Scripts/UI/CardController.cs`
- L306/L312 直接 `Input.mousePosition` 做卡牌落点射线。

全文检索**没有任何 `Input.touchCount` / `Input.GetTouch`**。WebGL 只把单指触摸模拟成鼠标，因此：

- 单指点按/拖拽"勉强能跑"；
- **双指捏合缩放、双指旋转、边缘拖屏完全缺失**，键盘平移在手机上不可用。

`Assets/Scripts/Scenes/StartScene/UIControl.cs` L523 `Application.Quit()` 在浏览器是 **no-op**（无法关闭网页），需改提示逻辑。

> 改造代价：中高——好在有 `IInputService` 抽象层，触摸改造可收敛在 `InputService` + `CameraController` + `CardController` 三处，不必散改全工程。

---

### 3.6 【高】程序化 Mesh + MeshCollider cooking，WebGL 单线程

本项目地图**全程序化生成**，几何密集：

- `ChunkMapRenderer.cs`（3,333 行）：8×8 分块，每块 Terrain/Water/River 的 active/staging **双缓冲 Mesh + `MeshCollider`**（L8、L30、L870 `AddComponent<MeshCollider>`、L3279 `TerrainCollider`）；
- `MeshDataGenerator.cs`（4,000+ 行）、`Triangulator.cs`、`UVGenerator.cs`、`WallMeshDeformer.cs`、`RiverGenerator.cs`、`RidgeGenerator.cs`、`MountainGeometryBuilder.cs` 等运行时 CPU 建顶点/法线/UV/三角面；
- 高度扰动依赖运行时读纹理：`HexMetrics.cs` L26 与 `TerrainGenerator.cs` L423 用 `Texture2D.GetPixelBilinear` 采样噪声/色板，**要求纹理 `isReadable`，WebGL 上既占 CPU 内存又是性能坑**。

WebGL 是**单线程**（`webGLThreadsSupport: 0`，且 `m_BuildTargetGraphicsJobs WebGL = 0`），`MeshCollider` cooking 与程序化建网格全在主线程串行，加上动态地图脏区重建（`MapSlicedCommitExecutor` 分帧提交）与 CPU 顶点动画（`SetChunkAnimationProgress` 每帧写 `mesh.vertices`），在手机上会造成明显卡顿与首屏耗时。

> 改造代价：中高——已有分块 + 分帧提交方向正确；需调低地形细分、简化/近似 `MeshCollider`、把可读纹理改预计算噪声，并真机 Profiling。

---

### 3.7 【高】内存预算 + iOS Safari 音频自动播放（H5 特有）

**内存**：
- WASM 堆 `webGLMaximumMemorySize: 2048` MB，但 WebGL 的纹理/缓冲也吃同一块浏览器内存池，iOS Safari 对 WebGL 进程内存上限远低于桌面；
- 吃内存大户：**整屏 `ARGBHalf` 遮罩 RT（`FogEnvironmentSelectiveEffect.cs` L283-286，视网膜屏下约 20MB+）+ 480 格双缓冲 Mesh + 未压缩大纹理 + 音频常驻**。

**音频自动播放（H5 独有硬伤）**：
`AudioManager.cs` 把 `bgmClips/sfxClips` 全部序列化常驻（L12-13），且 `Start()` 直接 `PlayBGM("Place_Village_Loop")`（L46-55）。**iOS Safari / 多数移动浏览器 WebGL 的 AudioContext 必须由用户手势解锁**，`Start()` 里自动播放会被静默拦截，BGM 不响；需要加"点击开始"手势解锁层。

> 改造代价：中——与 §3.2/§3.6 强耦合：遮罩 RT 降级、纹理压缩、音频转码 + 按需加载 + 手势解锁是必须动作。

---

### 3.8 【中】自定义 Shader 的 WebGL/GLES 兼容面

所有自定义 Shader 均 `#pragma target 3.0`（见 §3.2 清单），而：

- `#pragma target 3.0` 对应 GLES3，**要求 WebGL2**；WebGL1（GLES2）下这些 Shader 会编译失败或降级报错，需要锁定 WebGL2/GLES3 目标（`ProjectSettings.asset` L450-452 当前是"自动"）；
- `FogEnvironmentSelective.shader` L45/L63 在片元里用 `tex2Dlod`（`FogBlend.cginc` 亦然），WebGL1 片元 `tex2Dlod` 需要扩展，WebGL2 才稳；
- 内置管线 Shader 在 URP 下不兼容（见 §3.2），两套管线并存让"到底按哪套编译"难以验证。

> 改造代价：中——锁定 WebGL2/GLES3 + 按最终管线做 Shader 兼容回归；老 iOS 设备（WebGL1）需明确放弃或降级。

---

### 3.9 【中】中文字体渲染 + UI 适配

- TMP 3.0.9 + EmojiOne；UI 文案硬编码中文（`UIControl.cs`、`gameOptionController.cs` 等大量中文）。中文若走 TMP 动态图集，**字形量大、动态字体图集在手机上非常大**，需预生成静态中文字体资产并裁剪字符集；
- 屏幕方向：`defaultScreenOrientation: 4`、`allowedAutorotateToPortrait/Landscape` 全开，而 `CameraController.cs` L122-131 按 `Screen.width/height` 竖屏放大 FOV、UI 用 `Screen.height * 比例` 做偏移——H5 在手机竖屏/横屏切换下需要实测适配，浏览器地址栏/安全区还会挤压可视区。

> 改造代价：中——字体预生成 + 视口/安全区适配。

---

### 3.10 【中】场景与"远程资源加载"结构

- `EditorBuildSettings.asset` 仅两个场景：`StartScene.unity`、`GameScene.unity`；
- `GameInstaller` 用 Zenject **强绑定场景对象**（`FromComponentInHierarchy` 的 `MapGenerator`/`PlayerModelManager`/`EnemyModelManager`/`AIManager`/`CameraController`/`EndGame` 等，L392-401 缺任一直接 `throw ZenjectException`）；
- H5 要走"小首包 + 资源远程化"，必须让 Zenject 的强绑定适配 Addressables/AB 的异步加载时序（当前是同步加载全部）。

> 改造代价：中——取决于资源远程化诉求。

---

### 3.11 【低/观察项】

- **存档**：主程序无 `PlayerPrefs`/`System.IO`。H5 的 `PlayerPrefs` 映射到 IndexedDB（有配额），要云存档需自建后端；
- **网络**：主程序无 `Thread/Task/async/UnityWebRequest/Socket`（"Network" 仅是 `AnnexationService.AdjacentToNetwork` 连通性方法名），当前无联机需求；但 H5 一旦要远程加载资源，**必须引入 `UnityWebRequest` + 正确处理 CORS/HTTPS**，这是新增依赖；
- **确定性**：`SeedService` 用 `System.Random(derivedSeed)`，IL2CPP/WebGL 行为一致，利于后续回放/校验——有利；
- **退出/全屏**：`Application.Quit()` no-op；`Application.OpenURL` 未使用；浏览器全屏/返回键行为需单独处理。

---

## 4. 难点总览矩阵

| # | 难点 | 级别 | 根因（具体代码/配置） | 是否硬前置 |
|---|---|---|---|---|
| 3.1 | 4.4GB 资源 vs 浏览器首屏 | 致命 | `Assets/Resources` 3.67GB + `GameInstaller` 强引用链 | 是 |
| 3.2 | 渲染链路：surface+finalcolor+内置管线+PPv2 并存，迷雾依赖图像特效 | 致命 | `FogEnvironmentSelectiveEffect` 的 `OnRenderImage/Blit/CommandBuffer/ARGBHalf RT`；`QualitySettings` 双管线 | 是 |
| 3.3 | IL2CPP AOT / 反射裁剪 | 致命 | Zenject/UniRx 反射 + 无主程序 `link.xml` | 是 |
| 3.4 | 平台插件 IAP/Ads/Analytics/Coplay | 致命 | `Packages/manifest.json` | 是 |
| 3.5 | 输入纯鼠标/键盘 | 高 | `InputService`/`CameraController`/`CardController` | 是（触摸必需） |
| 3.6 | 程序化 Mesh + MeshCollider | 高 | `ChunkMapRenderer` 双缓冲 + `MeshCollider`；WebGL 单线程 | 否（优化项） |
| 3.7 | 内存预算 + iOS 音频自动播放 | 高 | `ARGBHalf` RT + 大纹理 + 音频常驻；`AudioManager.Start` 直接 Play | 是（音频必需） |
| 3.8 | Shader 的 WebGL2/GLES3 兼容 | 中 | 全部 `#pragma target 3.0`；WebGL 目标未锁 WebGL2 | 视机型而定 |
| 3.9 | 中文字体 + 屏幕适配 | 中 | TMP 动态图集；`CameraController` FOV/UI 比例 | 否 |
| 3.10 | 场景/远程资源加载结构 | 中 | 两场景 + Zenject 强绑定同步加载 | 视远程化而定 |
| 3.11 | 存档/退出/全屏 | 低 | 主程序干净，仅 `Application.Quit` no-op | 否 |

---

## 5. 有利因素（降低整体难度）

1. **主程序无文件 IO / 无线程 / 无网络 / 无 `dynamic` / 无反射 Emit / 无 `ComputeShader` / 无 `VideoPlayer`**：WebGL 最常见的硬坑，本项目一个都没踩（全文检索未命中）。
2. **地图规模小**（600 格量级）：性能压力有限。
3. **已有抽象收敛点**：`IInputService`（输入）、`IMapRenderBackend`（渲染后端，`ChunkMapRenderer` 是唯一实现）、`IMapRaycastService`（拾取）——替换/裁剪有入口。
4. **确定性种子**（`SeedService`）：利于回放/校验。
5. **已有分块 + 分帧提交**（`ChunkMapRenderer` + `MapSlicedCommitExecutor`）：作者已有"为性能分帧"意识，路径可继续沿用。
6. **无 StreamingAssets 脏包袱**：资源问题集中在 `Resources/Import`，可集中治理。
7. **H5 无微信适配器依赖**：少了 `minigame-unity-webgl-transform` 这一层的版本/兼容风险。

---

## 6. 建议的改造路线（仅建议，未执行）

按依赖顺序分六阶段：

- **阶段 0 — 定渲染管线**：在内置管线与 URP 之间二选一冻结；删除 `com.unity.postprocessing` 与 `UNITY_POST_PROCESSING_STACK_V2` 宏；按最终管线重写迷雾（内置管线降级遮罩 RT / URP 用 RendererFeature）。同时**锁定 WebGL2/GLES3 目标**。
- **阶段 1 — 资源瘦身 + 远程化（工作量最大）**：把 `Assets/Resources/Import` 未引用 demo 包移出 `Resources`（或删）；建 Addressables/AssetBundle + CDN；`.exr/.psd/.tga` 转码、纹理统一 ASTC/ETC2、音频转 ogg/mp3；开 Brotli 压缩。
- **阶段 2 — 插件清理**：移除 Coplay/IAP/Ads/Analytics，裁剪 Visual Scripting/Timeline/Collab 等未用包。
- **阶段 3 — 输入触摸化 + 音频手势解锁**：`InputService` 增加 `Input.GetTouch` 分支，`CameraController` 加双指缩放/平移、边缘拖屏，卡牌拖拽改触摸；音频加"点击开始"解锁。
- **阶段 4 — IL2CPP AOT**：启用 Zenject ReflectionBaking、补主程序 `link.xml`、调整裁剪级别并真机回归。
- **阶段 5 — H5 接入**：CDN + CORS + HTTPS + 备案域名（微信 H5 分发必需）、存档接 IndexedDB/云存档、首屏进度条与断点续传。
- **阶段 6 — 性能**：迷雾 RT 降级、MeshCollider 简化、音频按需加载、可读噪声纹理改预计算、真机 Profiling。

---

## 7. 关键证据索引

- 引擎版本：`ProjectSettings/ProjectVersion.txt`
- 资源体积：实测 `Assets` 4,424.6 MB / `Assets/Resources` 3,669.9 MB（`.png` 1.76GB / `.tga` 0.94GB / `.exr` 0.41GB / `.fbx` 0.31GB）
- 包依赖：`Packages/manifest.json`（shadergraph=URP14、postprocessing=PPv2、purchasing/ads/analytics/coplay）
- 渲染管线：`ProjectSettings/GraphicsSettings.asset` L56/L81；`ProjectSettings/QualitySettings.asset` L292/L316
- 迷雾后处理：`Assets/Scripts/Managers/FogEnvironmentSelectiveEffect.cs` L114/L118/L126/L283-286/L303/L350-351
- 自定义 Shader：`Assets/Shader/*.shader`（`#pragma surface ... finalcolor:fogFinal`、`#pragma target 3.0`）；`FogBlend.cginc` L121/L133 `tex2Dlod`
- 输入：`Assets/Scripts/Core/Services/InputService.cs` L17-28；`CameraController.cs` L231-268/L271-335/L405-433；`CardController.cs` L306/L312
- 程序化网格：`ChunkMapRenderer.cs` L8/L30/L870/L3279；`HexMetrics.cs` L26；`TerrainGenerator.cs` L423
- DI 强绑定：`GameInstaller.cs` L9-51/L392-401
- WebGL 设置：`ProjectSettings.asset` L47-48/L50/L730/L741/L743-748/L771/L792
- 音频常驻与自动播放：`AudioManager.cs` L7-13/L46-55
- 退出 no-op：`Scenes/StartScene/UIControl.cs` L523
- 无主程序 link.xml：仅 `Zenject`/`Sirenix`/`Newtonsoft` 三处 `link.xml`

---

*本报告基于对源码与工程配置的静态阅读得出，未改动任何文件；资源体积为本地文件系统实测。*
