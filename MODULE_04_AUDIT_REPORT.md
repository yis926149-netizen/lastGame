# 04. 地图数据、程序化生成与河流资源审计报告

## 结论

- 审查日期：2026-07-17
- 审查对象：`MapGenerator`、`TerrainGenerator`、`RiverGenerator`、`HexMapService`、地图配置、六边形邻接与相关测试。
- 结果：确认并修复 7 类数据生成缺陷；当前未发现影响默认地图正常流程的明显 bug，本模块通过审计。
- 编译验证：`MainGame.csproj` 与 `Tests.csproj` 均构建成功，0 error；仅有既有 Unity/Zenject 注入字段告警。
- 运行验证限制：未运行 Unity EditMode/PlayMode 和 Profiler，场景对象重建、生成耗时与 GC 尚未实测。
- 后续事项：极小/全海地图保护、地图服务生成期快照和性能优化已移至 `MODULE_04_DEFERRED_IMPROVEMENTS.md`，不阻塞当前模块通过。

## 已修复

### [P1] 连续生成因旧集合未清空而抛重复键异常

- 根因：`Generate()` 复用四个公开集合，第二次生成直接对旧字典调用 `Add`。
- 修复：每轮生成前清空世界坐标、世界坐标映射、地块映射和生成顺序映射。
- 结果：数据层重复生成不再累加旧地块；渲染对象重建仍归模块 05 验证。

### [P1] 非方形地图的高度与地块坐标错位

- 根因：高度数组按 `x -> z` 展平，地块按 `z -> x` 消费；方形地图表现为转置，非方形地图会分配到错误地块。
- 修复：高度统一按 `z` 外层、`x` 内层展平，与 `GenerateOrder` 一致。

### [P1] 河流候选源头可被前一条河占用后再次覆盖

- 根因：所有源头预先抽取，实际生成前不重新检查当前地块的河流状态。
- 修复：每条河开始前重新验证源头未被占用且不是海面，失效候选直接跳过。

### [P2] 地形、河流、地貌和资源无法通过种子完整复现

- 根因：各阶段分别创建无种子的 `System.Random`，Perlin Noise 也没有种子偏移。
- 修复：配置新增 `randomSeed`；生成入口创建单一随机源并传给河流、地貌和资源，地形噪声使用由同一种子派生的坐标偏移。
- 测试：新增同种子地形结果一致的回归用例。

### [P2] 地貌或资源可与河道生成在同一地块

- 根因：生成资格只排除高度不大于 0 的地块。
- 修复：地貌与资源生成同时排除 `hasRiver` 地块，并保留原有地貌/资源互斥规则。

### [P2] 地形平滑使用矩形 8 邻域并在平票时偏向高度 0

- 根因：算法直接遍历二维数组周围 8 格，不符合项目六方向坐标规则；平票按 `0 -> 1 -> 2` 顺序覆盖。
- 修复：改用奇偶行对应的 6 个六边形邻居；平票时保留原高度。
- 测试：补充六邻域和平票回归用例。

### [P2] 地图外世界坐标被吸附为边缘地块

- 根因：世界坐标转换无条件返回全图最近中心，未初始化还返回合法的 `(0,0,0)`。
- 修复：新增 `TryWorldToHexCoordinate`，按六边形外接圆半径拒绝地图外输入；`GetCellByWorldPosition` 对地图外返回 `null`。旧接口保留给现有调用方。
- 测试：使用生产坐标公式覆盖中心附近、Y 偏移、地图外、中心六邻居和角落邻居唯一性。

## 规则确认

### 河流允许在内陆结束

- 河流达到目标长度或无合法方向时可以形成内陆河，不要求终点连接海洋。
- 当前行为符合项目规则，不作为 bug 或待修复风险。

## 延后事项

- 极小或全海地图的启动保护、地图服务生成期半成品发布、坐标查询和生成性能优化均已记录在根目录 `MODULE_04_DEFERRED_IMPROVEMENTS.md`。
- 这些事项不影响当前默认 11x11 配置的审计结论，后续按实际需求处理。

## 验证结果

- `dotnet restore MainGame.csproj`：成功。
- `dotnet restore Tests.csproj`：成功。
- `dotnet build MainGame.csproj --no-restore`：成功，0 error。
- `dotnet build Tests.csproj --no-restore`：成功，0 error。
- `git diff --check`：无空白错误，仅工作区既有 CRLF 提示。
