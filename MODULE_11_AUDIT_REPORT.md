# 11. 城市、建筑、势力归属与胜负审计报告

## 结论

本轮发现并修复 3 组明显正确性问题：城市 ID 冲突、重叠势力范围错误转移，以及初始化期间误判胜利/缺少失败边界。建城、建筑部署、资源收割和城市易主主链路经静态复核后未发现其他明显阻断性问题。

## 已修复

### [P1] 城市销毁后复用数量作为 ID，覆盖仍存城市

- 玩家和 AI 原先都用当前 `CityCount` 作为新城市 ID。任意非末尾城市被攻占后计数减一，下一次建城会复用仍存城市的 ID，造成单城范围字典冲突或串城。
- 玩家和每个 AI 现在分别使用单调递增 ID 分配器，活动城市计数只用于数量和胜负判断。

### [P1] 多城市范围重叠时，攻占一城会错误移除或转移另一城领土

- 原逻辑从总势力字典直接删除被攻占城市的全部地块，没有检查这些地块是否也属于同阵营其他城市。
- 现在先移除目标单城范围，再从剩余城市重建总势力及地块归属；只有不再受原阵营城市覆盖的地块会转移。附属建筑清理也只作用于实际转移范围。
- `ExpandTheSphereOfInfluence` 现在同步设置中心格归属，避免中心格只进入字典却保留旧阵营或无主状态。

### [P1] 开局可误判胜利，且玩家最后一城丢失没有失败结果

- `EndGame.Update` 原先从首帧检查 AI 城市数，早于 `GameFlowManager.Initialize` 时可能以 0 城直接触发胜利；它也完全不检查玩家城市。
- 现在由游戏初始化流程显式开放胜负判定，只触发一次，并区分胜利、失败和平局。玩家从未建城时不会仅因 0 城失败；拥有过城市后失去最后一城会失败，同时归零为平局。

## 已核对

- 玩家与 AI 建城都拒绝海洋、已有建筑和敌方领土，并允许移民占据目标格；建城后移民通过统一移除服务清理。
- 普通建筑卡只能部署在本阵营城市范围及空建筑格；建筑落地后同步地块占用、所属城市和势力扩张。
- 城市易主同步城市 Tag、父对象、血条颜色、控制器城市键、地块归属、单城/总势力字典和城市计数；旧范围内实际转移的附属建筑会清理。
- 玩家资源收割先把数据状态置为 `None`，随后销毁并清空资源模型；重复点击不会重复结算。AI 宝箱收割采用相同的数据先行清理顺序。

## 验证

- `dotnet build MainGame.csproj --no-restore`：成功，0 error；现有 Zenject 注入字段静态告警保留。
- 新增领域回归测试：城市 ID 不复用、剩余城市保留重叠地块、初始化/胜利/失败/平局边界。
- 仍需在 Unity PlayMode 手工验证：连续建立至少 3 城后攻占中间城市、两城范围重叠后攻占其一、三种结束结果的实际动画和按钮流程。
