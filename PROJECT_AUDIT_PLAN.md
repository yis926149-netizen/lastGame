# 项目逐模块检查计划

## 1. 目标与范围

本计划用于对当前 Unity 2022.3 六边形回合制策略游戏进行一次完整、可追踪的工程审查。检查以发现真实缺陷、行为回归、数据不一致、场景配置错误、性能风险和测试空白为主，不以统一代码风格或大规模重构为目标。

检查范围包括：

- `Assets/Scripts/` 中的全部业务代码。
- `Assets/Scenes/`、业务 Prefab、ScriptableObject、材质、动画、音频等运行时资产。
- `Assets/Tests/` 中的现有测试及缺失测试。
- `Assets/Resources/`、`Packages/`、`ProjectSettings/` 中会影响编译、构建、运行和包体的配置。
- `README.md`、`Docs/` 与实际实现的一致性。
- `StartScene -> GameScene -> 完整回合 -> 胜负结束` 的端到端流程。

第三方插件源码不逐行审查，但必须检查其版本、许可证、项目接入方式、运行时引用和构建影响。主要第三方目录包括 `Assets/Plugins/`、`Assets/DOTween_1_2_765/` 及各类导入资源目录。

## 2. 审查原则

1. **先复现，后判断**：能运行验证的问题不得只凭静态推测定性。
2. **先正确性，后质量**：优先检查崩溃、卡死、状态损坏、规则错误和构建失败，再检查性能、可维护性和命名。
3. **检查契约，不只检查单文件**：重点验证服务接口、场景对象、Prefab、ScriptableObject 和调用方之间是否一致。
4. **不顺手改代码**：检查 agent 默认只提交报告，不实施修复。确需修复时另开任务，避免审查结果与代码变化混在一起。
5. **不把历史写法本身当缺陷**：只有造成错误、明显风险、重复实现冲突或无法验证的设计才记录为问题。
6. **保护现有工作区**：不得重置、覆盖或清理其他人的未提交修改；不得删除疑似未使用资产，除非完成依赖和构建验证且另获授权。
7. **每项结论必须有证据**：至少包含文件与行号、调用链、测试结果、Console 日志、截图或 Profiler / Build Report 数据之一。

## 3. 统一通过标准

所有模块均须满足以下基础标准，模块章节另有更具体的检查项。

### 3.1 正确性

- 正常路径、边界输入和失败路径的行为符合游戏规则。
- 状态更新具有唯一来源，不出现服务数据、地块数据、GameObject 和 UI 各自不一致。
- 重复调用、快速点击、切换阶段、对象销毁和场景切换不会产生重复执行或悬空引用。
- 集合、数组、ID、坐标和阶段切换不存在越界、空引用或无限循环。
- 随机逻辑存在终止条件；需要复现的问题可以固定种子或记录随机输入。

### 3.2 Unity 生命周期与资产

- `Awake/Start/OnEnable/OnDisable/OnDestroy` 中的初始化、注册和退订成对且顺序可靠。
- Zenject 绑定唯一、依赖可解析，直接启动 `GameScene` 与从 `StartScene` 进入时行为明确。
- 场景、Prefab、ScriptableObject 无 Missing Script、断 GUID、缺失必填引用和错误 Tag/Layer。
- 运行时创建和销毁对象不会泄漏事件监听、协程、Tween、材质实例或网格。
- Editor API 不进入 Player 程序集；目标平台 Player 构建无编译错误。

### 3.3 性能与资源

- 高频 `Update/Tick`、寻路、射线、网格重建和 UI 刷新无明显重复调用或无界分配。
- 不在热路径反复执行全场景查找、全地图线性扫描、同步加载或重复实例化材质。
- 使用 Profiler 数据说明 CPU、GC、内存和渲染风险；没有数据时只能标为“待验证”，不能直接判定性能缺陷。
- `Resources` 内容、未使用包和第三方样例资源的包体贡献可由 Build Report 追踪。

### 3.4 测试与文档

- 高风险规则至少有可重复的 EditMode 或 PlayMode 验证方案。
- 修复建议应指出需要新增或调整的测试，不要求检查 agent 当场编写。
- README、Docs、Inspector 配置和实际行为一致；无法从代码判断的设计规则列为待确认项。

## 4. 缺陷分级与报告规则

| 级别 | 定义 | 示例 |
| --- | --- | --- |
| `P0 阻断` | 无法编译、无法构建、主流程无法启动、稳定崩溃或数据整体损坏 | Player Build 因 Editor API 失败；进入游戏必定报错 |
| `P1 严重` | 核心玩法错误、回合卡死、胜负错误、单位或城市状态损坏，且常规操作可触发 | AI 阶段不结束；死亡单位仍占用地块 |
| `P2 一般` | 非核心路径错误、明显性能风险、配置脆弱或体验问题，有规避方法 | 特定地图配置越界；切场景重复音频对象 |
| `P3 建议` | 可维护性、测试、文档或低概率风险，不直接证明当前行为错误 | 死代码、文档偏差、缺少回归测试 |

每个发现必须使用以下格式：

```markdown
### [P1] 简短且可定位的问题标题

- 位置：`Assets/.../File.cs:123`
- 现象：用户或系统实际看到的错误行为。
- 触发条件：最小复现步骤、输入或场景状态。
- 根因：对应代码、配置或资产契约为何不成立。
- 影响：受影响的玩法、平台和数据范围。
- 证据：日志、测试、截图、调用链或性能数据。
- 修复方向：最小修复思路，不在审查任务中直接改代码。
- 测试建议：可防止复发的具体测试。
```

如果没有发现问题，也必须写明：检查过的文件和场景、执行过的验证、尚未覆盖的风险。不得仅回复“未发现问题”。

## 5. 执行顺序与依赖

建议按以下批次推进。批次内可由多个 agent 并行，后续批次使用前序结论作为输入。

| 批次 | 模块 | 前置关系 |
| --- | --- | --- |
| A | 01 工程基线、02 组合根与启动、03 数据与配置资产 | 最先执行，确定项目能否可靠打开和运行 |
| B | 04 地图数据与生成、05 地图渲染与视觉、06 单位移动与战斗 | 依赖 A 中的配置和依赖注入结论，可并行 |
| C | 07 输入与镜头、08 卡牌与部署、09 科技文化与结算、10 AI、11 城市与胜负 | 依赖地图、单位和回合基础结论，可并行但需互相引用 |
| D | 12 UI 与开始场景、13 音频与表现、14 测试体系、15 资源包体与发布、16 文档与架构 | 核心行为稳定后执行 |
| E | 17 端到端与跨模块一致性 | 汇总全部模块，最后执行 |

## 6. 分模块检查清单

### 01. 工程、编译与构建基线

**范围**

- `Packages/manifest.json`、`Packages/packages-lock.json`
- `ProjectSettings/`
- `Assets/Scripts/MainGame.asmdef`、`Assets/Tests/Tests.asmdef`
- `Assets/Plugins/`、DOTween、UniRx、NSubstitute 等依赖

**检查标准**

- 使用 `2022.3.62f3c1` 打开项目后资源导入和脚本编译无错误。
- EditMode 测试可以被发现并执行，结果和失败日志完整记录。
- 至少完成一次目标平台 Development Build；未确定目标平台时先以 Windows Standalone 为基线，并把平台选择列为待确认项。
- Player 程序集不引用 `UnityEditor`；特别检查开始场景脚本和无平台限制的 asmdef。
- 构建场景顺序为 `StartScene`、`GameScene`，场景 GUID 有效。
- 包锁文件可在干净缓存环境恢复；直接依赖有实际用途，Registry 不可用时错误可识别。
- 记录基线：编译警告数、测试通过/失败数、构建结果、包体大小和 Build Report 路径。

**通过条件**

- Editor 编译、EditMode 测试发现和基线 Player Build 均可执行；任何失败均已作为 P0/P1 记录并能复现。

### 02. 依赖注入、启动与场景生命周期

**范围**

- `Assets/Scripts/Infrastructure/Installers/`
- `Assets/Scripts/Managers/GameFlowManager.cs`
- `Assets/Resources/ProjectContext.prefab`
- `Assets/Scenes/StartScene.unity`、`Assets/Scenes/GameScene.unity`

**检查标准**

- 核对每项 Zenject 接口与实现的生命周期：`AsSingle/AsCached/Transient` 是否符合实际所有权。
- 安装器序列化字段缺失时能明确失败，不因提前 `return` 留下半初始化容器。
- 分别验证从 `StartScene` 进入和直接运行 `GameScene`；记录两条路径的行为差异。
- 场景切换后全局服务、事件对象和音频管理器数量正确，不重复常驻。
- `GameFlowManager` 的地图生成、渲染、AI、玩家、迷雾和状态机初始化顺序满足调用前置条件。
- 启动期间 AI 城市数等临时状态不会提前触发结束游戏。

**通过条件**

- 两种启动路径无 Zenject resolve error、空引用或重复全局对象，且进入首个玩家回合时所有核心服务已完成初始化。

### 03. 领域模型、ScriptableObject 与配置资产

**范围**

- `Assets/Scripts/Core/Models/`
- `Assets/Scripts/Data/`
- `Assets/Scripts/ScriptableObjects/` 及其 `.asset`
- `Assets/Scripts/Core/Services/DataProviders/`
- `Assets/Scripts/Utilities/Enums.cs`、`HexMetrics.cs`

**检查标准**

- 单位、建筑、卡牌、科技、文化、地形、资源 ID 唯一，索引契约明确且与枚举一致。
- 所有并行数组长度一致或有清楚的不同长度设计；特别核对单位/建筑模型、属性、卡图和图标数组。
- 数值范围合法：生命、攻击、移动、射程、成本、概率、地图尺寸、河流长度等无负值或反向区间。
- 所有运行时必需 Prefab、Sprite、Material、Animator、AudioClip 引用非空且 GUID 可解析。
- 区分模板数据和运行时状态，确认文化/科技升级不会意外永久修改共享 ScriptableObject 或共享数据实例。
- `HexCellData` 中地块规则、单位/建筑占用、探索状态与场景对象保持一致。

**通过条件**

- 可通过自动化资产检查或完整人工清单证明 ID、数组、引用和数值契约成立；运行时状态不会污染模板资产。

### 04. 地图数据、程序化生成与河流资源

**范围**

- `Assets/Scripts/Managers/MapGenerator.cs`
- `TerrainGenerator.cs`、`RiverGenerator.cs`
- `Assets/Scripts/Core/Services/HexMapService.cs`
- `Assets/Scripts/Utilities/` 中六边形与队列算法

**检查标准**

- 六边形坐标与世界坐标双向转换在中心、边缘和地图外输入下行为正确。
- 邻接查询无重复、越界或遗漏；距离、范围和可达性符合六边形规则。
- 生成前清空旧集合和场景对象，连续生成两次不会累加旧数据或事件状态。
- 地图服务只在完整数据可用时对外提供一致视图；重点检查生成中途初始化和最终初始化是否冲突。
- 河流起点、长度、方向、终点和海洋连接合法，不越界、不形成损坏网格。
- 资源、地貌和地形生成符合互斥/依赖规则，无非法组合。
- 固定种子可复现；若当前不支持固定种子，记录为测试性风险。
- 至少验证最小合法地图、默认地图、较大地图和极端合法配置，并记录生成耗时和 GC。

**通过条件**

- 所有地块坐标唯一且邻接正确，无越界河流或非法资源；重复生成结果独立；默认规模性能无明显阻塞。

### 05. 地图渲染、迷雾、网格与势力边界

**范围**

- `Assets/Scripts/Managers/MapRenderer.cs`
- `FogManager.cs`、`SphereOfInfluenceRenderer.cs`
- `Assets/Scripts/Core/Services/MeshDataGenerator.cs`
- `Assets/Scripts/Utilities/MeshGenerator.cs`、`UVGenerator.cs`、`Triangulator.cs`
- `MapVisualEventSO` 及材质、Shader

**检查标准**

- 地形、海岸、水面、河流、网格、迷雾和势力边界的 Mesh 顶点、索引、UV、法线和 Bounds 有效。
- 探索、单位移动、建城、城市易主后仅触发必要的视觉重建，显示与逻辑状态同步。
- ScriptableObject 事件订阅与退订成对，重进场景不重复响应。
- 网格开关不会改变地块交互状态或造成材质实例泄漏。
- Built-in Render Pipeline 下业务材质无粉色 Shader；第三方 URP 资产不会被误用于主场景。
- 在默认地图和较大地图记录网格生成时间、帧尖峰、Draw Call、顶点数和 GC；确认没有每帧全图重建。

**通过条件**

- 所有地图层正确显示且与状态同步，无 Mesh 错误、重复事件或持续内存增长；性能数据已记录。

### 06. 单位仓库、移动、寻路与战斗

**范围**

- `IUnitRepository`、`UnitRepository.cs`
- `IUnitService`、`UnitService.cs`
- `IUnitMovement`、`UnitMovementSystem.cs`
- `UnitMovementController.cs`、`ModelController.cs`、`BuildingController.cs`

**检查标准**

- 仓库增删查、阵营过滤和空集合行为正确；重复注册和销毁后查询有明确结果。
- Dijkstra / 优先队列对不可通行、未探索、敌方占用、友方占用、建筑、海洋和地图边缘处理一致。
- 统一检查不可通行成本的表示和过滤规则，避免 `-1`、`float.MaxValue` 等约定冲突。
- 移动范围、预览路径、实际路径和移动力扣除使用同一规则。
- 移动开始、途中、结束及取消时，原地块和目标地块占用状态始终一致。
- 近战会停在合法攻击位置；远程攻击不会错误移动；射程、伤害、反击和行动消耗符合设计。
- 单位/建筑死亡只结算一次，并清除仓库、地块引用、事件、UI 选择和 GameObject。
- 协程和 Tween 在对象销毁、切阶段、切场景时能安全终止。

**通过条件**

- 玩家和 AI 对同类移动/战斗规则得出一致结果；不存在穿越阻挡、负移动力、重复伤害或死亡后仍占格。

### 07. 玩家输入、选择状态与镜头

**范围**

- `PlayerInputHandler.cs`、`InputService.cs`
- `CameraController.cs`
- EventSystem、Camera、Physics Raycaster、Tag 和 Layer 配置

**检查标准**

- 左键选择、移动，右键攻击，卡牌拖拽，网格开关和镜头操作不会互相抢占输入。
- 指针位于 UI 上时不会穿透操作地图；拖拽取消、拖出窗口和快速重复点击可恢复稳定状态。
- 只允许在玩家阶段操作己方对象；AI/结算阶段输入被正确屏蔽。
- 选择对象被销毁、移动或切回合时，高亮、路径、信息面板和引用被清理。
- 检查 `CameraController` 是否同时由 Zenject `ITickable` 和其他组件手动 Tick，确保每帧仅更新一次。
- WASD、方向键、滚轮和旋转在地图边界、不同帧率及 Time Scale 下行为稳定。
- 所有射线依赖的 Tag、Layer、Collider 和 Camera 引用与 Prefab/场景一致。

**通过条件**

- 各阶段输入权限正确，无 UI 穿透、重复 Tick、卡死拖拽或悬空选择；镜头在边界内稳定运行。

### 08. 卡牌、手牌、解锁与部署

**范围**

- `CardService.cs`、`CardPresenter.cs`
- `CardController.cs`、`ICardView.cs`
- `CardUnlockRuleProvider.cs`、`AllCardsData.cs`
- 卡牌 Prefab、单位/建筑数据库与部署流程

**检查标准**

- 初始手牌、固定首牌、五槽位、预告牌、抽牌和满手牌规则一致。
- 卡牌 ID、显示内容、Prefab 和实际生成对象一一对应。
- 玩家与 AI 的科技文化解锁规则一致；若设计上不同，必须有明确依据。
- 特别核对随机发牌是否绕过 `CardUnlockRuleProvider`，以及 README 所述解锁能力是否真实生效。
- 合法落点检查覆盖地形、探索、势力范围、占用、城市前置条件和阵营。
- 非法放置、取消拖拽和生成失败不会消耗卡牌或留下高亮/临时对象。
- 成功部署后手牌、仓库、地块、视野、势力范围和 UI 原子性更新。
- 动态实例所需 Zenject 注入和 MonoBehaviour 生命周期均执行完整。

**通过条件**

- 卡牌来源、解锁、显示、消耗和生成闭环一致；任何失败路径均不损坏手牌或地图状态。

### 09. 科技、文化、成长与回合结算

**范围**

- `ITechCultureService` 及实现
- `Tech_CultureTreeController.cs`
- `TechData.cs`、`CultureData.cs`
- `SettlementPhase.cs`

**检查标准**

- 点数增加、等级阈值、溢出、满级和多次升级行为符合设计且无数组越界。
- 每个结算阶段只执行一次；回合数、科技点、文化点和产出显示同步。
- 升级对现有单位、建筑和后续生成对象采用一致规则，不重复叠加共享模板。
- 生命恢复不超过上限；攻击、生命、移动、产出等加成精确且可追踪。
- 检查文化效果是否在 `Update` 中重复修改数据，以及建筑升级获取组件/数据的方式是否有效。
- 科技文化解锁能够真实影响玩家和 AI 卡池，而非仅改变 UI。
- 动画中断、快速点击和切换面板不会重复扣点或升级。

**通过条件**

- 从 0 级到满级的边界均可验证；结算和升级只发生一次；运行时与新生成对象属性一致且不污染配置资产。

### 10. AI 决策、行动队列与容错

**范围**

- `Assets/Scripts/Core/Interfaces/IAIManager.cs`
- `AIPlayerState.cs`
- `AIPhase.cs`
- AI 使用的卡牌、寻路、战斗、建城和科技文化接口

**检查标准**

- 明确 `IAIManager.cs` 的实际类型和职责，不能因目录名假设其为接口。
- AI 出生点和城市选址随机循环有上限；无合法位置时可结束并报告，不会卡死。
- 多 AI 的状态、索引、单位、城市、卡池和科技文化数据互不串用。
- AI 在无单位、无卡、无路径、目标已销毁、城市已易主等情况下仍能完成阶段。
- 每个单位每回合行动次数、移动力和攻击次数正确；协程异常不会阻止 `AIPhase` 完成。
- AI 使用与玩家一致的地形、占用、射程、卡牌和升级规则。
- 目标选择不会引用已销毁对象；行动途中世界状态变化后会重新验证前置条件。
- 固定种子或构造场景可复现关键决策；记录至少 20 个连续回合的稳定性结果。

**通过条件**

- AI 在正常和资源耗尽/无路/目标消失等退化状态下均可结束回合，不作弊、不重复行动、不损坏共享状态。

### 11. 城市、建筑、势力归属与胜负

**范围**

- `PlayerModelManager.cs`、`EnemyModelManager.cs`、`MachineModelManager.cs`
- `BuildingController.cs`
- 建城、势力圈、资源收割、城市易主和 `EndGame.cs`

**检查标准**

- 玩家和 AI 建城前置条件相同且明确，城市编号/ID 不因销毁产生冲突。
- 建城后中心、势力范围、所属阵营、地块占用和 UI 立即同步。
- 多城市范围重叠时，归属、扩张和移除一座城市后的剩余范围正确。
- 建筑只能部署到合法城市范围；城市被摧毁或易主时附属建筑按规则处理。
- 城市易主后 Tag、父对象、血条、控制器、地块归属、玩家/AI 列表和计数全部更新。
- 资源收割只能结算一次，资源对象与数据状态一致。
- 胜利/失败仅在初始化完成且条件真实成立时触发一次；继续游戏或返回菜单流程明确。
- 最后一座城市、同时死亡、无城市开局等边界有确定结果。

**通过条件**

- 建城、扩张、建筑部署、城市受击、易主和最终胜负形成一致闭环，无残留归属或误判结束。

### 12. 游戏 UI、开始场景与交互状态

**范围**

- `Assets/Scripts/UI/`
- `UIManagerPresenter.cs`
- `Assets/Scripts/Scenes/StartScene/`
- Canvas、EventSystem、UI Prefab、动画与分辨率配置

**检查标准**

- 单位、城市、科技文化、卡牌、回合和结束界面显示与领域状态同步。
- 事件注册不会因启用/禁用或重进场景而重复；视图销毁后 Presenter 不再调用。
- 检查固定 `GetChild`、对象名和按钮下标依赖，确认所有目标场景层级均满足契约并有越界保护。
- 开始、新游戏、选项、退出、异常提示及返回流程均可完成；动画期间交互状态明确。
- 选项值若可编辑，必须实际持久化并影响游戏；未实现的控件不得伪装为有效功能。
- 两套远程攻击入口如同时存在，其射程、目标、消耗和反馈必须一致。
- 在至少 1920×1080、1366×768、16:10 和常见窄屏比例验证无重叠、截断、越界和不可点击。
- 键鼠焦点、拖拽、连续点击、Esc/返回等状态切换不遗留遮罩或锁定。

**通过条件**

- 所有主要界面路径可完成，显示与操作一致，在目标分辨率下无阻断性交互和层级错误。

### 13. 音频、动画、特效与结束表现

**范围**

- `AudioManager.cs`
- 场景 AudioSource / AudioListener
- DOTween、Animator、Timeline、VFX 与胜负动画

**检查标准**

- 运行时 AudioManager 和 AudioListener 数量符合设计，切场景不重复。
- BGM 与 SFX 通道独立；连续 SFX 不打断 BGM，缺失名称有可诊断日志。
- 音量设置、静音和场景切换后的播放状态符合选项设置。
- Tween、动画事件、协程和特效在对象销毁时清理，不操作失效引用。
- 攻击、受伤、死亡、建造、升级和胜负表现只播放一次，且不提前改变/延迟关键逻辑状态。
- 缺失 Animator 参数、Clip、材质或特效 Prefab 时不阻断核心流程。

**通过条件**

- 音频可并发且生命周期正确；动画和特效与逻辑状态同步，无因表现层造成的卡回合或异常。

### 14. 自动化测试体系与可测试性

**范围**

- `Assets/Tests/`
- Unity Test Framework、NUnit、NSubstitute 配置
- 核心接口与服务的可测试边界

**检查标准**

- 逐个核对现有测试是否真正断言行为，是否存在占位断言、过度 Mock 或与当前实现不一致。
- 现有覆盖至少包括：卡牌服务、状态机、地图服务、科技文化、单位移动和仓库；列出每项未覆盖分支。
- 设计资产完整性 Editor 测试：Missing Script、断引用、数据库数组/ID、构建场景、Tag/Layer 契约。
- 设计 PlayMode 冒烟测试：两种启动路径、完整回合、部署与战斗、AI 阶段结束、城市易主和胜负。
- 随机模块支持固定种子；时间、输入、场景和协程相关测试无不稳定等待。
- 测试程序集不进入 Player，测试依赖版本固定且可恢复。
- 建立建议覆盖矩阵，不以单一行覆盖率代替行为覆盖。

**通过条件**

- 现有测试可信且可重复；每个 P0/P1 核心流程至少有自动化方案或明确记录无法自动化的原因。

### 15. 资源、包体、性能与发布配置

**范围**

- `Assets/Resources/`、业务模型/贴图/材质/音频/视频
- 第三方导入资源与示例
- `ProjectSettings/QualitySettings.asset`、`GraphicsSettings.asset`、`ProjectSettings.asset`
- Build Report、Profiler、Frame Debugger

**检查标准**

- 使用 Build Report 列出包体前 20 项，特别确认 `Assets/Resources/Import/` 是否把未使用第三方样例整体打包。
- 区分业务资产、第三方运行时依赖和可移除示例；任何清理建议必须附引用扫描与构建验证。
- 检查纹理尺寸/压缩、模型 Read/Write、网格、音频加载方式、视频和 RenderTexture 的内存成本。
- 确认目标渲染管线，Built-in 与导入的 URP/Shader Graph 资产不冲突。
- 在默认地图和 20 回合流程记录 FPS、主线程尖峰、GC Alloc、内存、Draw Call；硬件和画质档位必须写入报告。
- 发布前确认产品名、公司名、包名、版本、图标、目标平台和最低系统要求。
- 核对 Ads、Analytics、Purchasing 等包是否实际使用，以及隐私、联网和许可证要求。

**通过条件**

- 包体构成和性能瓶颈有量化证据；无意外 Resources 打包、粉色材质或明显持续泄漏；发布配置待办完整。

### 16. 文档、架构边界与遗留代码

**范围**

- `README.md`、`Docs/`、DocFX 配置
- `Core/Interfaces`、`Core/Services`、`Turn/` 与旧式控制器
- 未引用类、重复流程和命名不符职责的实现

**检查标准**

- README 中启动、操作、回合、服务、测试和场景描述与当前实现一致。
- 核实卡牌解锁、AI、城市易主、资源收割、音频和胜负等实际能力是否被正确描述。
- DocFX 输入路径、filter、链接和输出配置有效，`docfx build` 无错误和断链。
- 找出重复或疑似遗留流程，例如 `PhaseMachine`、`CommandQueue`、`MoveCommand`，通过引用和场景序列化证明是否仍使用。
- 目录名和类型职责不一致时记录认知风险，例如具体 `MonoBehaviour` 位于 `Interfaces` 目录。
- 当前 `MainGame.asmdef` 为单程序集，评估循环依赖和 Editor API 混入风险；没有实际收益时不建议为“分层”强拆程序集。
- 公共接口文档说明前置条件、状态变化、失败行为和所有权，而不只重复方法名称。

**通过条件**

- 文档可按步骤复现项目；遗留和重复实现均有引用证据及保留/清理建议；架构问题关联到具体风险。

### 17. 端到端流程与跨模块一致性

**范围**

- 全项目，使用前 16 个模块报告作为输入。

**必测流程**

1. 打开 `StartScene`，完成菜单和选项操作后进入游戏。
2. 地图生成完成，玩家与 AI 正确出生，迷雾、资源和 UI 状态一致。
3. 玩家选择单位、移动、近战、远程攻击，并验证非法操作。
4. 使用单位卡和建筑卡，验证合法/非法部署、卡牌消耗和视野/势力更新。
5. 建立城市、扩张势力、收割资源、推进科技文化并跨过升级阈值。
6. 结束回合，完整执行 `PlayerPhase -> AIPhase -> SettlementPhase`，连续运行至少 20 回合或直至胜负。
7. 击杀单位、摧毁建筑、攻占城市，验证归属、仓库、地块、UI 和表现层同步。
8. 触发胜利/失败，验证动画、音频、继续/返回流程和重复触发保护。
9. 返回菜单后再次开局，确认无重复服务、事件、音频、静态状态或旧地图残留。

**跨模块不变量**

- 一个单位只属于一个阵营、一个仓库和至多一个地块。
- 一个地块的逻辑占用与场景对象一致。
- 卡牌只有在对象成功部署后才消耗。
- 每个阶段每回合只进入和退出一次，结算只执行一次。
- 玩家与 AI 共享的规则不因入口不同而产生差异。
- 城市和建筑归属在管理器、地块、Tag、父节点和 UI 中一致。
- 死亡/销毁对象不再被寻路、AI、UI、事件或协程引用。
- 重新开局后的运行时状态等价于首次开局，不携带上一局数据。

**通过条件**

- 必测流程均有结果记录；所有跨模块不变量成立；阻断端到端流程的缺陷已分级并关联到责任模块。

## 7. Agent 分工模板

每个 agent 原则上只负责一个模块。复制以下模板并替换占位内容：

```text
你负责执行 PROJECT_AUDIT_PLAN.md 的“模块 XX：模块名称”。

任务要求：
1. 先阅读该模块范围内的全部业务代码、相关接口、调用方、场景/Prefab/SO 配置和现有测试。
2. 按模块检查标准逐项验证。能运行 Unity/Test Runner/Build/Profiler 时保留结果；不能运行时明确写“未验证”，不得把推测当事实。
3. 默认只审查，不修改代码、场景、Prefab、资产或配置，不清理文件。
4. 发现按 P0-P3 排序，每条必须给出文件行号、触发条件、影响、证据、最小修复方向和测试建议。
5. 同时检查与相邻模块的契约，但不要重复审查其他模块；跨模块问题标注双方模块编号。
6. 无发现时仍需列出已检查范围、执行的验证和剩余风险。
7. 将结果写入 `AuditReports/XX-module-name.md`。若目录或同名报告不存在可新建；若已存在，不覆盖其他 agent 的内容，先反馈冲突。

报告末尾必须包含：
- 结论：通过 / 有条件通过 / 不通过 / 因环境未完成
- 发现统计：P0、P1、P2、P3 各多少项
- 已执行验证及结果
- 未执行验证及原因
- 建议后续负责的模块编号
```

## 8. 单模块报告模板

```markdown
# 模块 XX 检查报告：模块名称

## 结论

- 状态：通过 / 有条件通过 / 不通过 / 因环境未完成
- 检查日期：YYYY-MM-DD
- 检查者：agent 名称或会话 ID
- 基准提交：Git commit SHA；工作区非干净时同时记录相关文件状态

## 检查范围

- 已检查文件/资产：
- 相关调用链：
- 排除范围及原因：

## 发现

按 P0 -> P3 排列；使用计划中的统一缺陷格式。

## 已执行验证

| 验证 | 环境/输入 | 结果 | 证据位置 |
| --- | --- | --- | --- |

## 检查清单结果

| 检查项 | 通过/失败/未验证/不适用 | 说明 |
| --- | --- | --- |

## 测试缺口

- 缺失的自动化测试与优先级。

## 剩余风险

- 因工具、场景、设计信息或时间限制未能排除的风险。

## 汇总

- P0：0
- P1：0
- P2：0
- P3：0
- 建议后续模块：XX
```

## 9. 最终汇总与验收

全部模块完成后，由一个独立汇总 agent 执行以下工作：

1. 合并重复发现，以根因而非表象计数，并保留所有证据来源。
2. 检查互相矛盾的结论，必要时回派原模块复核。
3. 建立 `缺陷 -> 责任模块 -> 影响流程 -> 修复顺序 -> 回归测试` 矩阵。
4. P0/P1 按依赖关系排序，不按文件数量或修复难度排序。
5. 对 P2/P3 区分“毕业项目交付前必须处理”和“可延期优化”。
6. 生成一次独立的端到端复测报告，不以各模块静态审查代替集成验证。
7. 只有在以下条件均满足时，项目整体可判定“通过”：
   - 无未处置的 P0。
   - P1 均已修复或有明确、可接受的风险处置结论。
   - Editor 编译、EditMode 测试和目标平台构建通过。
   - 主流程和跨模块不变量通过端到端验证。
   - 未验证项目、发布配置和性能基线已明确记录，不被隐含当作通过。

建议最终输出文件：`AuditReports/00-FINAL-AUDIT-SUMMARY.md`。

## 10. 当前已知审查提示

以下内容仅用于指导优先级，仍须由对应 agent 独立复核后才能计入正式发现：

- 开始场景脚本可能在运行时程序集引用 `UnityEditor`，应优先进行 Player Build 验证。
- `CameraController` 可能同时由 Zenject Tick 和输入处理器手动 Tick，需要确认是否重复更新。
- 玩家卡牌生成与 AI 卡牌生成可能使用不同的科技文化解锁规则。
- `PhaseMachine`、`CommandQueue`、`MoveCommand` 可能是未接入或不完整的遗留流程。
- 单位寻路中不可通行成本可能存在多种表示约定，需要检查调用链一致性。
- 科技文化升级可能修改共享单位数据，建筑升级的数据/组件访问方式需要运行验证。
- 城市易主涉及多个管理器、地块、GameObject 和 UI，是高风险跨模块流程。
- `Assets/Resources/Import/` 含大量第三方资源，可能显著增加 Player 包体。
- 当前测试仅有 Editor/EditMode 程序集，场景集成、资产完整性和 PlayMode 覆盖明显不足。
- 项目主要业务脚本位于单一 `MainGame.asmdef`，目录分层并不构成程序集依赖隔离。
