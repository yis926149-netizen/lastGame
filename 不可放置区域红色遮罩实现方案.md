# 不可放置区域红色遮罩实现方案

> 目标：卡牌「提起」时，把**不可放置的地块区域作为一个整体**，用一层半透明红色遮罩圈出来。
> 遮罩要覆盖地块主体及其之间的**矩形过渡区域、三角过渡区域**，而不是只圈住每个地块的中心点。

---

## 1. 总体思路

采用**屏幕空间 UI 遮罩**方案，而不是继续复刻地形 mesh：

```text
不可放置地块中心点
  → 过滤有效山格（MountainCellRule.IsEffectiveMountainCell，对齐现有高亮门禁）
  → 按六边形邻接关系分组（区分不连续区域，方向索引 0..5 不含 None）
  → 每个区域取地块中心点世界坐标
  → 经 Camera.main.WorldToScreenPoint 映射到屏幕空间（剔除 z<0 背面点）
  → 对中心点做「地块半径外扩」
  → 求该区域的外轮廓（凹包 / 邻接边界）
  → 对轮廓做曲线平滑（Catmull-Rom / Bezier）
  → 去自交 / 去退化边
  → 用 Ear Clipping 对轮廓内部三角化
  → 在独立 UI Canvas 中绘制半透明红色多边形
```

这样既能自然覆盖地块之间的矩形、三角过渡区域，又能保证遮罩是一个整体，不会出现「每个地块中心各一块红色」的割裂感。

---

## 2. 数据来源（复用现有逻辑，不重写判定）

- 直接读取已保留的只读出口（`Assets/Scripts/Core/Services/PlayerInputHandler.cs:207-210`）：

```text
PlayerInputHandler.RaisedUnplaceableCells   // 不可放置格
PlayerInputHandler.RaisedPlaceableCells     // 可放置格（可选，用于后续绿色区域）
```

- 每个 `HexCellData` 通过 `RealCenterWorldCoordinate` 取得地块中心世界坐标。

> 因此本方案**不再重新判断「能否放置」**，只负责把已经算好的不可放置集合表达成遮罩。

### 2.1 红区口径（已定）

- **红区 = 不可放置地块**（`RaisedUnplaceableCells` 的全图口径），不做「只取可放置格周边一圈」的裁剪。
- ⚠️ 注意：`RaisedUnplaceableCells` 由 `GetAllCells` 全图遍历得出（`PlayerInputHandler.cs:241-246`），
  未探索、水面、山脉、归属外等格都在其中。因此**通常红区是地图主体、绿区是孤岛**，
  §3.2 的连通分组要按此现实处理（可能只有一个巨大红区 + 少数被绿区挖出的凹陷/洞）。

### 2.2 有效山格过滤（已定）

- 现有 `HexHighlightRenderer` 对玩家可见通道会过滤有效山格
  （`HexHighlightRenderer.IsBlockedByMountainGate` → `MountainCellRule.IsEffectiveMountainCell`，
  `HexHighlightRenderer.cs:106-110`）。
- 但 `RaisedUnplaceableCells` **没有做山格过滤**，直接使用会把整片山脉圈红。
- **本方案在收集阶段先过滤有效山格**，与现有高亮系统口径一致：

```csharp
// 收集不可放置中心点前先剔除有效山格
if (MountainCellRule.IsEffectiveMountainCell(cell)) continue;
```

> `IsEffectiveMountainCell` 语义 = 山脉标记 && 未被永久清除 && 非水域
> （`MountainCellRule.cs:26-29`）；水淹/清除后自动放行，与几何/通行口径统一。

---

## 3. 实现流程

### 3.1 收集不可放置地块中心点

- 遍历 `RaisedUnplaceableCells`，**先剔除有效山格**（`MountainCellRule.IsEffectiveMountainCell`，见 §2.2），
  再取每格的 `RealCenterWorldCoordinate`。
- 得到世界坐标点集 `P0, P1, P2, ...`。

> ⚠️ **动画期陷阱**：`RealCenterWorldCoordinate` 在地图突变/波浪动画期间是**逻辑值而非显示值**
> （`FogManager.cs:13`、`MapVisualTransitionService.cs:28` 已明确禁止直接当视觉高度用）。
> 卡牌提起若恰逢地图动画，需改读动画中的显示高度（参考 `_visualTransition.GetAnimatedWorldY`），
> 否则遮罩会与地形错位。初版若不覆盖动画期，可先接受此偏差并在待确定项标注。

### 3.2 按六方向邻接关系分组

- 不可放置地块可能被可放置地块隔成多个不连续区域（但按 §2.1，现实更可能是「一个巨大红区 + 若干绿洞」）。
- 使用 `IMapDataService.GetNeighbor(cell, direction)` 对**已过滤山格后**的不可放置集合做连通性分组。
- ⚠️ **方向遍历只取索引 0..5**：`Enums.HexDirection` 定义为 `{ NE, E, SE, SW, W, NW, None }`
  （`Enums.cs:13-16`），`None` 是第 7 个值（索引 6）。遍历必须 `for (int i = 0; i < 6; i++)`，
  严禁 `i <= 6` 或 `i < 7`（会走到 `None` → `GetNeighbor` 的 `default: return null`，`HexMapService.cs:168`）。
- 每个连通组单独生成一个遮罩轮廓，避免把中间的可放置区域误包进来。

结果结构：

```text
List<UnplaceableRegion>
  每个区域包含：
    - 该区域的不可放置地块列表
    - 该区域的中心点集合
    - 该区域的屏幕空间外轮廓
    - 该区域对应的 UI mesh
```

### 3.3 中心点映射到屏幕空间

- 相机为**透视相机**（已定；项目统一走 `Camera.main`，实际由 `CameraController` 驱动，
  `Assets/Scripts/Controllers/CameraController.cs`）。
- 使用当前游戏摄像机：

```text
Vector3 screenPos = Camera.main.WorldToScreenPoint(worldPos);
```

- ⚠️ **剔除背面点**：透视相机下，位于相机背后的世界点会返回 `screenPos.z < 0`
  （项目已有先例：`IncrementTrailFlyPresenter.cs:77` 注释）。投影后必须过滤 `z < 0` 的点，
  否则会产生翻转/错误的屏幕坐标，污染轮廓。
- ⚠️ 因为是透视相机，「缩放」通常是相机沿视线**推拉（改变距离）**而非改 `orthographicSize`；
  刷新检测应监听 `CameraController` 的变换（position/rotation）变化，而非正交尺寸（见 §5）。

- 依据 Canvas 类型做二次换算：

| Canvas 类型 | 处理方式 |
| --- | --- |
| Screen Space - Overlay | 屏幕坐标直接换算到 Canvas 本地坐标 |
| Screen Space - Camera | 传入对应 UI Canvas 的 worldCamera |
| World Space Canvas | 使用 `RectTransformUtility.ScreenPointToLocalPointInRectangle` |

- 遮罩放在独立 UI Canvas / UI 层，渲染顺序：**地图之上、卡牌之下**。

### 3.4 中心点外扩（覆盖矩形 / 三角过渡的关键）

只连中心点会让遮罩偏小、盖不住过渡区域，因此做一次**屏幕空间外扩**：

- 以每个不可放置中心点为圆心，向外扩展约 **半个地块尺寸**；
- 或更贴近拓扑：在六方向偏移点基础上求外轮廓。

扩展半径参考值：

```text
屏幕空间中相邻六边形中心距离的一半（可再加少量 padding）
```

效果：

- 覆盖不可放置地块主体；
- 覆盖地块之间的矩形过渡区；
- 覆盖三地块交界处的三角区域；
- 消除红区边缘的连接缝隙。

### 3.5 求外轮廓

三种可选算法：

| 算法 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- |
| 凸包 | 实现简单、稳定 | 过度覆盖，凹区会被填红，分离区域会被连起来 | 不建议直接用于最终版 |
| 凹包 / Alpha Shape | 保留凹凸形状 | 实现复杂，Alpha 参数需调，过小会碎、过大会退化成凸包 | 可选 |
| 六边形邻接边界 | 贴合地图拓扑，天然识别凹形与不连续区域 | 需要依赖邻居关系 | **推荐** |

**推荐的六边形邻接边界做法：**

1. 遍历每个不可放置地块；
2. 检查它的六个邻居；
3. 若某方向邻居不是不可放置格，则该侧属于红区边界；
4. 收集所有边界方向点；
5. 按顺序连接成一个或多个轮廓（已按连通组分开）；
6. 对轮廓做外扩与平滑。

### 3.6 曲线平滑

- 对轮廓顶点使用 **Catmull-Rom** 或 **Bezier** 平滑；
- 删除过短边，避免尖刺；
- 重新采样成较密曲线点，视觉上是一块连续柔和的红色区域，而不是一串六边形。

### 3.7 内部三角化

- 曲线只是边界，内部需要填充。
- 凸轮廓可用扇形三角化，但凹轮廓必须用 **Ear Clipping（耳切法）**。
- ⚠️ **平滑与耳切之间需去自交**：§3.4 的外扩 + §3.6 的重采样加密叠加后，凹角处
  轮廓易产生**自相交**，朴素 Ear Clipping 会失败或产生翻面三角。三角化前应先做
  轮廓自交检测 / 相邻重复点去重 / 退化边清理，必要时对自交段做裁剪。
- 若后续需要支持洞、复杂形状，再升级为带洞多边形三角化（按 §2.1，绿洞在红区中间的情形是常态，
  见待确定项「是否支持洞」）。

---

## 4. UI 遮罩实现

- **不复用** `HexHighlightRenderer`（它是世界空间地块高亮，与屏幕空间曲线遮罩层次不同）。
- 新建独立组件，例如：

```text
PlacementRangeMaskUI
```

职责：

1. 接收 `RaisedUnplaceableCells`；
2. 获取当前相机；
3. 计算屏幕点；
4. 生成区域轮廓；
5. 构建 UI mesh；
6. 设置半透明红材质；
7. 卡牌落下 / 拖拽 / 销毁时清除。

### 4.1 表现结构

```text
Canvas
└── PlacementRangeMaskRoot
    ├── RegionMask_0
    ├── RegionMask_1
    └── RegionMask_2
```

- 每个 `RegionMask` 是一张红色半透明 UI 多边形。
- 可用继承自 `Graphic` 的 UGUI 组件，在 `OnPopulateMesh(VertexHelper vh)` 中生成多边形。

### 4.2 材质

- 红色，Alpha 约 `0.2 ~ 0.35`；
- 关闭描边，或使用柔和边缘；
- 可选：红色面 + 一圈更低透明度羽化边，增强柔和感。

---

## 5. 相机移动 / 缩放时的刷新策略

屏幕空间遮罩会随相机移动而偏移，需要决定刷新时机。相机为**透视相机**（`CameraController` 驱动）。

| 方案 | 说明 | 结论 |
| --- | --- | --- |
| 提起时只生成一次 | 性能最好，但相机移动后遮罩会与地图脱节 | 不推荐 |
| 变化时刷新 | 相机变换（位置/旋转/推拉距离）、提起卡牌变化时重建 | **推荐** |

推荐刷新时机：

```text
卡牌提起时              → 生成一次
相机变换（位移/旋转/推拉）→ 刷新
卡牌落下/拖拽           → 清除
```

> 透视相机的「缩放」是沿视线推拉（改变相机到地图的距离），不是正交 `orthographicSize`；
> 因此刷新检测应监听 `CameraController` 的 `transform`（position + rotation）变化，
> 而非正交尺寸。可缓存上一帧相机 position/rotation，超过阈值才重建，避免每帧重投影。

---

## 6. 推荐的最终方案

```text
1. 读取现有 RaisedUnplaceableCells（不重写放置判定）；
2. 收集阶段先过滤有效山格（MountainCellRule.IsEffectiveMountainCell），口径对齐现有高亮门禁；
3. 按六方向邻居（索引 0..5，不含 None）把不可放置地块分成连通区域（现实常态：一个大红区 + 若干绿洞）；
4. 每个区域取地块中心世界坐标（注意动画期 RealCenterWorldCoordinate 是逻辑值）；
5. 经 Camera.main.WorldToScreenPoint 映射到屏幕空间，剔除 z<0 背面点；
6. 对中心点做「地块半径外扩」；
7. 用六边形邻接边界求区域外轮廓（凹形、不连续区域都正确）；
8. 对轮廓做 Catmull-Rom / Bezier 平滑；
9. 平滑后去自交/去退化边，再用 Ear Clipping 三角化；
10. 在独立 UI Canvas 中绘制半透明红色遮罩；
11. 相机变换（位移/旋转/推拉）或提起状态变化时刷新；
12. 卡牌落下、拖拽或销毁时清除。
```

---

## 7. 待确定项

1. **轮廓贴合程度**：曲线要贴近不可放置地块边界，还是允许一定平滑外扩？（建议：中心点外扩半个地块尺寸 + 轻度平滑）
2. **是否只做红色不可放置区**，还是同时用绿色表达可放置区？
3. **遮罩透明度**：初步取 `0.25 ~ 0.35`，待真机标定。
4. **是否支持洞**：按 §2.1，红区是地图主体、绿区是孤岛，**绿洞在红区中间是常态**。
   是否需要在红色面中挖洞（否则被绿区包围的区域也会被填红）？（初版可先不支持，后续用带洞三角化补上）
5. **刷新粒度**：相机移动是「变化时重建」还是「逐帧微调」？（建议先做变化时重建 + 位移/旋转阈值门限）
6. **动画期高度**：卡牌提起若逢地图突变/波浪动画，是否改读 `GetAnimatedWorldY` 显示高度，
   还是初版接受与逻辑高度的偏差？（建议初版接受，动画期极短）
