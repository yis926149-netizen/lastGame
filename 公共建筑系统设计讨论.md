# 公共建筑系统——设计讨论

>状态:技术方案讨论中,未开始编码。讨论确认项可直接开发,未确认项需继续讨论后补齐。

---

## 一、系统概述

在地图中立区域按规律随机生成的**公共建筑**,开局归属中立,有自己的血量。玩家/AI 双方争夺——谁先击破其初始血量就归属谁,归属后防守血量回满。归属后再被敌方击破则**直接易主**(不回中立),如此反复。

每个公共建筑有不同的效果(具体效果制空),占地可能不止一格(最多4格),归属后向周围扩展势力范围。

---

## 二、已确认决策

| # | 类别 | 决策 |
|---|------|------|
| 1 | 阵营表示 | **不新增枚举**，公共建筑视为特殊AI势力：占用独立 PlayerIndex，只有建筑+势力范围，不能操作 |
| 2 | 两阶段HP | 分开:`captureHp`(中立夺取HP) ≠ `defenseHp`(归属后防守HP) |
| 3 | 继承结构 | 抽 `BuildingBase`,现有 `BuildingController` + 公共建筑都继承它 |
| 4 | 多格形状 | 固定形状:中心根格 + 3 个方向邻格(共4格) |
| 5 | 多格HP | 一份 HP,由根格 Controller 统一管理 |
| 6 | 多格攻击 | 攻击任意占位格都算(单位到达任意4格中任一格的邻格即可攻击) |
| 7 | 势力范围扩展 | 归属后**每个占位格各自向外扩展一环** |
| 8 | 势力范围逻辑 | 公共建筑地块归属复用城市那套 `SphereOfInfluence` 逻辑 |
| 9 | 易主逻辑 | 直接易主给攻击方,不回中立态 |
| 10 | 可见性 | 需单位走近才能发现(按战争迷雾规则,不预先探索)；当前迷雾**实际为两态**(可见=已探索/不可见=未探索)，公共建筑随所在格 `IsExplored` 显隐 |
| 11 | 生成方式 | 代码控制,按一定规律随机生成 |
| 12 | 单位攻击 | 单位自动攻击公共建筑,AI 主动争夺(具体自动化逻辑制空) |
| 13 | 血量规则 | 首次夺取用 `captureHp`,之后每次易主用 `defenseHp`,每次重置为满血 |
| 14 | 效果接口 | 基类不留效果钩子,仅搭结构架子,不做实机验证 |
| 15 | GameLoop接入 | 公共建筑的死亡检测与周期性计时纳入 `GameLoop` 统一管理 |
| 16 | BuildingBase职责 | 上提：血量、血条、Faction归属、受击入口、伤害公式(可覆写)、死亡标记；死亡触发改为GameLoop驱动而非`Update()`轮询；势力范围/销毁逻辑由子类实现 |
| 17 | BuildingController | 继承BuildingBase，保留现有逻辑，城市易主/普通建筑销毁不动 |
| 18 | PublicBuildingBase | 继承BuildingBase，新增：两阶段HP、多格坐标管理、攻击转发、势力范围扩展；死亡=易主而非销毁 |
| 19 | 攻击路径(多格) | 方案A：`HexCellData`上新增字段存根格Controller引用；子格被攻击时查此引用转发到根格 |
| 20 | 势力范围重叠 | 公共建筑优先覆盖：易主时其4格+各占位格外1环**强制**划归新主人，不论原先属于谁 |
| 21 | 数据分层 | `BuildingData`拆分：基类保留`hp/currentHp/Faction/controller`；`PublicBuildingData`子类新增`captureHp/defenseHp/rootHex/subHexOffsets` |
| 22 | PlayerIndex分配 | 开局后动态分配：真AI优先（1,2...），公共建筑后续递增（N+1, N+2...），GameFlowManager 统一分配并记录边界 |
| 23 | 真/伪AI标记 | 管理器分类中新增标记区分"真AI"（有行为逻辑）和"公共建筑伪AI"（无操作） |
| 24 | 血条颜色判断 | 根据管理器标记判断是否为公共建筑，初始显示中立色（白/灰），易主后按归属显示 |
| 25 | 战争迷雾 | FieldOfViewService/AIFogService 跳过公共建筑 PlayerIndex，不计算其视野 |
| 26 | GameLoop接入方式 | GameLoop 新增 `List<PublicBuildingBase> _publicBuildings`，Tick时单独遍历，不改UnitBrainBase |
| 27 | PlayerIndex分配器 | `GameFlowManager`中实现分配器：`AllocatePlayerIndex()`递增分配；玩家→真AI→公共建筑依次调用 |
| 28 | 公共建筑生成时机 | 地图生成后立即建立（地图加载的一部分），此时势力范围为空，无冲突；之后游戏才正式开始 |
| 29 | HexCellData新字段 | 新增 `PublicBuildingBase publicBuildingRoot`，子格存根格引用；保留现有`BulidingTypeOnHex_Building`字段不动 |
| 30 | 标记存储位置 | `EnemyModelManager` 中新增 `HashSet<int> PublicBuildingPlayerIndexes`，提供 `IsPublicBuilding(int)` 查询方法 |
| 31 | BuildingData血量表示 | 方案A：基类`hp/currentHp`动态切换；初始`hp=captureHp`，首次夺取后切换为`hp=defenseHp`；易主时`currentHp=hp`回满 |
| 32 | 易主流程 | `PublicBuildingBase.OnCaptured()`：移除旧势力范围→切换playerIndex→切换血量阶段(首次时hp切换)→回满→扩展新势力范围→更新视觉；记录`initialNeutralPlayerIndex`判断首次 |
| 33 | 势力范围去重 | 4格各外圈一环用`HashSet`去重后统一覆盖，避免重复添加到`SphereOfInfluence` |
| 34 | 子格偏移表示 | 固定形状用`HexDirection[]`存储(如`{NE,E,SE}`)，生成时通过`HexMetrics.GetNeighbor`转为绝对坐标 |
| 35 | 配置数据 | 新建`PublicBuildingSO`专用配置(prefab/captureHp/defenseHp/cardSprite)，新建`IPublicBuildingDataProvider`接口 |
| 36 | 攻击目标优先级 | 暂定"先遇到先打"，不区分公共建筑与普通建筑/单位；后续测试看效果再调整 |
| 37 | 多格攻击转发位置 | 在`CombatResolver`中新增辅助方法`GetBuildingTarget(HexCellData)`，优先检查`publicBuildingRoot`字段，无则取普通建筑 |
| 38 | 生成流程插入点 | 插在`GenerateResources()`之后、`InitializePlayer()`之前；此时地形固定、势力范围为空，无冲突 |
| 39 | 可见性（两态迷雾） | 公共建筑随所在格`IsExplored`显隐，无记忆区概念；归属颜色仅在可见时展示 |
| 40 | UI中立态文本 | 归属未定时显示"中立" |
| 41 | GameLoop管理范围 | 只将`PublicBuildingBase`纳入GameLoop；普通建筑(`BuildingController`)继续保留`Update()`轮询，不动现有代码 |
| 42 | 公共建筑及周围探索 | 公共建筑生成时，其4个占位格**加上每个占位格外一环**的所有地块初始`IsExplored = false`，即周围区域同样不可探索，防止玩家通过"未探索区域有空洞"推断建筑位置 |

---

## 三、待确认项

### 3.1 多格形状的旋转(需确认→已推迟到生成规则讨论)

固定方向 vs 可旋转:推迟至与 3.2 一起讨论,属于生成规律范畴。

>状态:推迟,后续生成规则讨论中统一确认。

---

### 3.2 随机生成的规律(推迟)

包括且不限于:形状旋转策略、数量、位置约束(可建地块\避开出生点\最小间距)、生成时机。

>状态:推迟,后续专项讨论。

---

### 3.3 具体公共建筑类型(制空)

具体效果/名称制空。基类不留效果钩子(如 `OnCaptured`/`ApplyPassiveEffect` 等),仅搭 Multi-Hex 结构骨架,不做实机运行验证。

>状态:制空。

---

### 3.4 自动化攻击与 AI 逻辑(制空)

- 单位自动攻击公共建筑的具体优先级
- AI 主动争夺公共建筑的决策逻辑
- 多个单位同时围攻时的"最后一刀"归属判定

>状态:制空,后续讨论。

---

## 四、架构对接点

### 4.1 需改动的现有文件

| 文件 | 改动点 |
|------|--------|
| `Enums.cs` | 新增 `Faction` 枚举;`BulidingType` 可能需要新增公共建筑类型 |
| `BuildingData.cs` | 新增 `captureHp`、`defenseHp`、`faction` 等字段 |
| `BuildingController.cs` | 拆出 `BuildingBase`,现有逻辑保留在子类 |
| `CombatResolver.cs` | `ResolveBuilding` 需适配多格建筑逻辑 |
| `HexCellData.cs` | 多格建筑所属地块标记方式 |
| `GameFlowManager.cs` | 地图生成流程中加入公共建筑生成步骤 |
| 单位Brain(PlayerUnitBrain/AIUnitBrain) | 攻击目标选择逻辑中加入中立公共建筑 |

### 4.2 需新增的文件(规划)

| 文件 | 职责 |
|------|------|
| `BuildingBase.cs` | 建筑基类,含血量管理、归属、基础攻击逻辑 |
| `PublicBuildingBase.cs` | 公共建筑基类,多格管理、两阶段HP、势力范围扩展 |
| `PublicBuilding_X.cs` | 具体公共建筑子类(制空,各实现不同效果) |
| `PublicBuildingGenerator.cs` | 公共建筑随机生成器 |
| `NeutralBuildingSO.cs` | 公共建筑 ScriptableObject 配置(HP/模型/卡面等) |

### 4.3 多格建筑在六边形上的数据结构

```
PublicBuildingBase
├── rootHex       // 根格(中心格,持有 HP 和 Controller)
├── subHexes[3]   // 3 个附属格(相对根格的 hex 偏移)
└── occupiedHexes // rootHex + subHexes,共4格

每个 HexCellData 上需标记:
├── PublicBuildingOwner  // 哪个公共建筑实例占用了此格(nullable)
└── IsPublicBuildingSubHex  // 是否为附属格(区分根格)
```

---

## 五、讨论记录

- **2026-07-24 第一轮**：确立基础12项决策（阵营、两阶段HP、继承结构、多格设计、生成方式等）
- **2026-07-24 第二轮**：确认血量规则(#13)、效果制空(#14)、GameLoop接入(#15~#21)
- **2026-07-24 第三轮**：改为"公共建筑=特殊AI势力"方案，确认PlayerIndex动态分配、管理器标记等(#22~#30)
- **2026-07-24 第四轮**：确认血量表示、易主流程、势力范围去重、子格偏移表示、SO配置(#31~#35)
- **2026-07-24 第五轮**：确认攻击优先级、CombatResolver辅助方法、生成时机、迷雾两态现状、UI文本、GameLoop管理范围(#36~#41)；核实当前迷雾**实际为两态**（`FieldOfViewService.Recompute()`中`IsVisible=IsExplored`短路，三态基础设施存在但未启用）
- **2026-07-26 第六轮**：确认公共建筑及周围探索规则(#42)：生成时4占位格+各格周围一环均初始`IsExplored=false`，防止玩家通过迷雾空洞推断建筑位置

---

## 六、参考

- 现有建筑系统：无继承体系，`BuildingController` + `BuildingData` + `Enums.BulidingType` 枚举驱动
- 城市易主流程：参考 `BuildingController.CityDestroyed()` 和 `CombatResolver.ResolveBuilding()`
- 势力范围逻辑：参考 `PlayerModelManager` 的 `SphereOfInfluence` / `RebuildSphereOfInfluence`
- 战争迷雾：当前**实际两态**（可见=已探索）；`FieldOfViewService.cs:52` 的 `IsVisible=IsExplored` 是短路点；`FogBlend.cginc` 的 `visibility` 参数已传入但被丢弃；三态所需基础设施完备但未激活
- 游戏循环：参考 `GameLoop.cs`；公共建筑死亡检测将注册进此循环
- PlayerIndex分配：玩家固定0，真AI从1递增，公共建筑紧随其后；`EnemyModelManager.IsPublicBuilding(int)` 作为全局判断入口
