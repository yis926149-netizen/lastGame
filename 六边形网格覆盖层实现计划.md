# 六边形网格覆盖层实现计划

> 目标：在地图上覆盖一层**规则六边形半透明细线**，大致指示每个逻辑地块的范围。
> 核心语义：网格不是实心区域的真实边界，也不跟随边界扰动；相邻地块的共享线位于两格过渡区域的中间。
> 视觉目标：类似规则蜂窝纸，只显示连续边框线，不显示或强调过渡区域本身，不整格填充。
> 遮挡关系：网格线被单位、建筑和山体正确遮挡（`ZTest LEqual` + 深度偏移）。
> 范围：仅 Chunked 渲染后端（`ChunkMapRenderer`，当前唯一活跃后端）。

---

## 1. 需求边界与现状修正

### 1.1 本次网格的正确含义

- 每个六边形以 `HexCellData.CenterWorldCoordinate` 为逻辑中心。
- XZ 轮廓使用配置中的完整规则六边形尺寸：外接圆半径为 `MapGenerationConfigSO.OuterRadius`，内切圆半径为 `InnerRadius`。
- 相邻格中心距为 `2 * InnerRadius`，因此共享边位于两中心连线的中点，正好是过渡带的中线。
- 网格轮廓不读取 `ctx.Solid[1..6]`，不使用 `SolidAreaRatio`，也不经过 `HexMetrics.Perturb`。地形过渡带可以继续存在，但网格只以一条规则细线从其中间穿过。
- “大致指示”是指 XZ 拓扑保持规则；Y 方向仍需贴合可见地表，避免线条埋入地形或悬空。

### 1.2 可复用资产

| 资产 | 位置 | 本次用途 |
| --- | --- | --- |
| 旧六边形环带几何 | `MeshDataGenerator.BuildGridVertices / BuildGridUV / BuildGridDrawOrder`（`MeshDataGenerator.cs:4276`） | 只参考“细带由三角形组成”的拓扑；**不能直接复用顶点位置**，因为它沿扰动后的实心区边缘生成 |
| 地块尺寸 | `MapGenerationConfigSO.OuterRadius / InnerRadius` | 生成未扰动、完整尺寸的规则六边形轮廓 |
| 过渡区几何 | `ChunkMapRenderer` 的 rectangle / triangle transition 构建数据 | 为网格顶点提供地表 Y；不用于决定网格的 XZ 形状 |
| 半透明网格 Shader | `Assets/Shader/GridLine.shader` | 可参考混合设置；现有 `ZTest Always` 不符合遮挡要求 |
| 迷雾混合工具 | `Assets/Shader/FogBlend.cginc` | 复用世界对齐迷雾采样，使未探索区网格淡出 |
| Chunk 重建入口 | `BuildChunkStaging / CommitChunkStaging / CommitChunkGeometry` | 网格与脏 Chunk 一起重建和提交 |

**结论**：需要新增“逻辑规则六边形”几何，而不是把现有 `BuildGridVertices` 线宽调细。旧函数仍服务于 `HexHighlightRenderer`，本次不修改其既有语义。

---

## 2. 方案选型

采用 **Chunk 级独立规则网格覆盖层（overlay mesh）**：

- 网格 XZ 坐标只由逻辑中心和 `OuterRadius` 决定，与实心区域、过渡区宽度和噪声扰动解耦。
- 独立 mesh 不触碰地形主 mesh 的 submesh、材质混合、山体槽和碰撞数据。
- 每 Chunk 合并为一个 mesh，无逐格 GameObject 或 Collider。
- 独立材质可单独控制透明度、迷雾、渲染队列和深度测试。

否决方案：

- **沿 `ctx.Solid` 描边**：得到的是实心区真实边缘，线会落在过渡带内侧且随扰动弯曲，不符合示意网格需求。
- **直接画半径为 `OuterRadius` 的整格水平环**：XZ 正确，但高差过渡处会埋入或悬空，斜坡上还可能断线。
- **Shader 内 SDF 网格**：难以可靠获得复杂地表的世界空间贴合和遮挡，并需侵入多套地形 Shader。
- **逐格 GameObject**：对象数和管理成本没有必要。

---

## 3. 详细设计

### 3.1 XZ 几何：规则蜂窝线

- 新增独立几何接口，例如 `BuildLogicalGridEdge(...)` 或 `BuildLogicalGridVertices(...)`，不要改变旧 `BuildGridVertices`。
- 六个规则角点由逻辑中心直接计算。角度顺序必须与项目当前尖顶六边形坐标系一致：
  - 南北角使用 `center.z +/- OuterRadius`；
  - 其余四角使用 `center.x +/- InnerRadius`、`center.z +/- 0.5 * OuterRadius`。
- 不乘 `SolidAreaRatio`，不调用 `HexMetrics.Perturb/PerturbY2`，不从 `_solidVertices` 拷贝 XZ。
- 共享边的中心到格中心距离为 `InnerRadius`，即相邻中心连线中点。这样线自然位于两个实心区域之间的过渡带中间，而不会把过渡带画成一个可见区域。
- 线宽采用世界空间窄带，建议默认 `OuterRadius * 0.015~0.025`，最终以主镜头视觉验收为准。配置应表达为 `gridLineWidth`，不要继续使用语义模糊的旧 `widthRatio`。

### 3.2 地表贴合：只跟随 Y，不改变规则 XZ

网格不能简单使用整格中心高度。高差地块的共享边位于过渡斜面中间，需要单独求表面 Y：

- 对每条待绘边，先生成规则边的 XZ 起点、终点；窄带内外侧顶点也保持规则直线。
- 根据相邻两格的最终地表高度和现有过渡 profile，在共享边位置计算 Y。优先复用 rectangle transition 的中线插值结果；若该方向无邻居，则使用当前格边缘对应的地表高度。
- 一条边至少采样两个端点。若相邻格高差较大或现有 transition 有细分，则沿边同步增加分段，并对各采样点求地表 Y，避免长边穿入斜面。
- Y 计算可以读取扰动后地表/profile，但**只取高度**；采样点 XZ 不被吸附到扰动后的真实边界。
- 最终沿表面法线或世界 Y 增加小偏移，建议 `0.02~0.05` 世界单位。首版可用世界 Y 偏移；若陡坡侧出现埋线，再升级为法线偏移。
- 河道切口属于地表局部细节。网格保持规则直线语义，可被河岸/地貌深度遮挡，不要求沿河槽下凹重塑。

### 3.3 边去重与 Chunk 归属

半透明共享边必须只提交一次，否则 alpha 叠加会导致内部线深浅不一：

- 每格仅拥有 `NE / E / SE` 三条正向边。
- 对 `SW / W / NW`，仅当该方向不存在邻居时补画地图外边界。
- 规则：方向 `d` 需要绘制，当且仅当 `d` 属于 `{NE, E, SE}`，或 `neighbor(d) == null`。
- 跨 Chunk 的共享边仍归正向边所在格的 Chunk，避免 Chunk 边界重复。
- 每条边独立生成窄带顶点和索引，比“每格先生成完整 12 点环、再选索引”更适合高度采样和可变细分。

### 3.4 水域口径

- 保持原需求：水域格不显示网格。
- 仅当共享边两侧都属于可显示地块时绘制该共享边；若一侧为水域，则默认不补画陆水分界线，避免网格退化成海岸描边。
- 地图外边界仅为非水域格补画。
- 若后续希望水面也显示完整蜂窝，应作为单独开关 `showGridOnWater`，不能与本次默认口径混在一起。

### 3.5 Chunk 宿主与双缓冲

- `ChunkRenderData` 增加 `GridHost`、`GridFilter`、`GridRenderer`、`ActiveGrid`、`StagingGrid`。
- `GetOrCreateChunk` 创建 `GridHost`，挂在 `chunk.Root` 下并使用 `Map` 层，不添加 Collider。
- 全部 Chunk 共享同一个网格材质，不逐格或逐 Chunk实例化材质。
- 无顶点时隐藏 `GridHost`，有顶点时显示。

### 3.6 构建与提交

- 在地形 transition profile 已准备完毕后执行 `BuildChunkGrid(chunkCells)`，保证 Y 采样能复用同一轮地形数据。
- 遍历 `chunkCells`，按水域规则和边归属规则决定待绘边。
- 对每条边生成规则 XZ、采样地表 Y、加表面偏移，再合并至 `GridGeometry` 的顶点/索引/UV 列表。
- `BuildChunkStaging` 将 `GridGeometry` 与 Terrain、River、Water 一起返回。
- `CommitChunkStaging / CommitChunkGeometry` 清空并回填活动网格，更新 bounds、材质和显隐。
- 动态地图最终态变化后随脏 Chunk 重建刷新。动画期间首版允许网格直接使用目标地表 Y；`FinalizeChunkAnimation` 后必须与最终地形一致。逐帧跟随动画列为 P2。

### 3.7 Shader：遮挡、迷雾与颜色

新建 `Assets/Shader/HexGridOverlay.shader`：

- `Tags { "Queue"="Transparent" "RenderType"="Transparent" }`。
- `Blend SrcAlpha OneMinusSrcAlpha`、`ZWrite Off`、`Cull Off`。
- 使用 `ZTest LEqual`，确保单位、建筑和山体能够遮挡网格。
- 使用适度 `Offset -1, -1`，并配合几何 Y/法线偏移抑制 Z-fighting。
- `_Color` 包含整体颜色和 alpha。线宽由 CPU 几何控制，不在首版 Shader 中伪造 `_LineWidth`。
- 接入 `FogBlend.cginc` 和现有全局迷雾参数，使用 explored 掩码调制 alpha，使未探索区不显示网格信息。

### 3.8 配置项

在 `MapGenerationConfigSO` 增加：

- `gridMaterial`
- `showHexGrid`
- `gridColor`
- `gridAlpha`
- `gridLineWidth`
- `gridSurfaceOffset`

`OuterRadius / InnerRadius` 直接决定网格尺寸，不新增容易与地图拓扑失配的 `gridRadius`。旧 WholeMap 的 G 键 `gridGameObject` 属于死路径，不在本次处理。

---

## 4. 实施步骤

1. **规则几何辅助**：新增基于逻辑中心、`OuterRadius / InnerRadius` 的单边窄带构建，不修改旧 `BuildGridVertices`。
2. **地表高度采样**：为规则边端点/细分点复用 transition profile 求 Y，并验证 XZ 始终保持未扰动规则坐标。
3. **边归属与水域过滤**：实现正向三边去重、地图边界补边和陆水共享边过滤。
4. **Shader**：新增 `HexGridOverlay.shader`，实现 `ZTest LEqual`、深度偏移、透明混合和迷雾淡出。
5. **Chunk 宿主**：在 `ChunkRenderData / GetOrCreateChunk` 增加网格宿主和双缓冲 Mesh。
6. **构建提交**：把 `GridGeometry` 接入 `BuildChunkStaging`、`CommitChunkStaging` 和最终提交路径。
7. **配置接线**：加入开关、颜色、透明度、世界空间线宽和表面偏移。
8. **动态地图回归**：验证升降、波浪、竞技场清湖海后网格随脏 Chunk 更新，无重复边和残留。

---

## 5. 验收标准

- [ ] 俯视时呈现连续、规则的蜂窝网格，整体效果接近参考图。
- [ ] 网格线位于相邻逻辑地块中心的垂直平分线上，即过渡区域中线，而非实心区域边缘。
- [ ] 调整 `SolidAreaRatio` 或地形 XZ 扰动后，网格的规则 XZ 轮廓不发生变化。
- [ ] 网格不展示过渡带宽度、扰动形状或材质分界，只保留一条细线。
- [ ] 高差过渡处线条贴合地表，无明显埋线、悬空或断裂；允许 Y 随地形变化。
- [ ] 内部共享边和地图外边界深浅一致，无双绘；跨 Chunk 边界无接缝。
- [ ] 单位、建筑和山体正确遮挡网格，无穿模和 Z-fighting 闪烁。
- [ ] 未探索区网格淡出，探索推进后与地形同步显现。
- [ ] 水域不显示网格，陆水交界不额外形成海岸描边。
- [ ] 动态地图最终态更新后网格位置正确，无残留或 Mesh 泄漏。
- [ ] 无逐格 GameObject / Collider；每 Chunk 仅一个网格 mesh；关闭开关后全部隐藏。

---

## 6. 风险与备注

- **高度采样是主要风险**：规则 XZ 边位于 transition 中线，不能直接取 `ctx.Solid[0].y`。必须先确认现有 rectangle profile 在该位置的高度插值口径，避免网格在高差地块上消失。
- **角点三格交汇**：规则六边形角点同时接触三格，Y 值应采用与 terrain triangle transition 一致的结果；否则相邻边端点可能出现细小裂口。实现时应缓存同一逻辑角点的 Y，供相接边复用。
- **陡坡贴合**：只有端点采样时，长边中段可能穿过非线性表面。优先沿用地形 transition 的细分级别，而不是固定过度细分全部网格。
- **半透明排序**：共享边去重后大部分排序问题消失；仍应保证单 Chunk 网格 mesh 内没有重叠窄带。
- **斜视角可见性**：世界空间扁平窄带在低俯仰镜头下会视觉变细，最终线宽需在主镜头下验收。
- **动画期间高度错位**：首版允许短暂错位，但最终提交必须恢复贴合；严格逐帧同步列为 P2。
- **迷雾参数一致性**：必须复用地形现有 `_FogMaskTex / _FogMapOrigin / _FogMapSize` 绑定源。
- **文档范围**：本文仅修改实现计划，不包含代码改动。
