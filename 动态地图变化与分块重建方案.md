# 动态地图变化与分块重建方案

> 状态：方案已确认（2026-08-03 动画设计；2026-08-04 代码审计修订；2026-08-04 实施决策锁定；**2026-08-04 最终一致性修订**；**2026-08-04 真实状态修订**；**2026-08-04 Chunked 验证通过修订**）。**阶段一（MeshGenerator 无状态化）已实施完成（2026-08-04），可用**；**阶段二（基础地图变化服务 + 竞技场玩法全流程）代码已实施（2026-08-04），GUI 冒烟验证中**；**阶段三 Chunked 后端于 2026-08-04 实机验证通过（`mapRenderMode=Chunked` + `enableExperimentalChunkRenderer=true` 均正常显示），此前的"散点/缺面"几何损坏实为阶段四动画 Shader 代码所致（§十九-21），修复后 Chunked 恢复可用**；**阶段四 Shader 顶点动画实机发现破坏稳定渲染（详见 §十九-21），动画代码已从三套稳定 Shader 回退，动画路径仍不可用**；**阶段五依赖阶段四动画的分帧/并行动画能力暂不可用，其余部分随阶段三可用**。已修复水域 profile 顺序、海岸三角数据源、脏 Chunk 遍历、河流末端零顶点连接，并加入 staging 硬校验。
> **当前已验证能力**：① `WholeMap` + `Duration=0` 同步重建；② `Chunked` + 脏 Chunk 局部重建（`SupportsChunkedRebuild` 路径）——两后端在稳定 Shader（无动画通道）下均正常；**③ 动态重建实测：竞技场 37 格突起仅重建 4/9 个 Chunk（86ms），脏 Chunk 收敛与脏位正确（2026-08-04 实机，详见阶段三实测数据）**。动画过渡（`Duration>0`）仍不可用。
> 目标：**每个地块都能动态变化**（高度/地形/河流/地貌/资源/通行/迷雾/归属），竞技场动态突起为首个使用者，为洪水/地震/可破坏地形/造桥等未来玩法预留通用管线。
> 关联文档：`游戏系统/竞技场与中央宝箱-玩法机制设计.md`、`游戏系统/竞技场与中央宝箱-实现方式讨论.md`。
> 审计：2026-08-04 对照现有代码逐项核对，发现并修订 12 类问题（事件访问限制、卡牌射线、水域拓扑、材质生命周期、法线接缝、迷雾范围元数据等），见 §十九。

---

## 一、背景与目标拆解

目标拆成两个层面：

1. **逻辑层**：每个地块都能独立修改（`HexCellData` 已支持字段级修改 ✅）。
2. **渲染层**：修改一个地块时**不必重建整张地图**（当前 `MapRenderer` 为整图合并 mesh，只能全量重建 ❌）。

> 结论：建设**通用地块变化服务（MapMutationService）+ 分块 mesh 重建**，而非"一格一个 GameObject"。

---

## 二、总体架构

```
竞技场 / 技能 / 天灾 / 地形编辑器
              ↓
     MapMutationService          IMapInteractionGate（交互锁）
   （通用地块变化服务）            （动画/事务期间锁定受影响格）
              ↓
   修改 HexCellData + 计算脏范围
              ↓
   MapChunkRenderService      MapVisualTransitionService
  （按脏区块重建 mesh）      （动画进度/错峰/单位视觉高度）
              ↓
   MapChangedEvent（CommitId/ChangedCells/DirtyFlags/Options/Phase）
              ↓
 寻路 / 单位 / 模型 / 迷雾 / 标签联动
```

- 竞技场只是该系统的第一个调用方，动态地图逻辑不写死在 `ArenaEventManager` 中。
- **事件协议分阶段落地**：现有 `MapVisualEventSO` 为无参广播（`MapVisualEventSO.cs:7-35`），订阅者只能全图扫描。阶段二先新增最小强类型 `MapChangedEvent(CommitId, ChangedCells, DirtyFlags, Phase=Committed)`；阶段四扩展 `TransitionStarted/Finalized/Cancelled` 与 TransitionOptions；阶段五再补诊断/批处理能力。旧 `Raise()` 固定在 Committed 后兼容调用一次，不等动画 Finalize。
- **交互锁**：现有移动/探索/AI/卡牌各入口无统一锁（见 §12.6），新增 `IMapInteractionGate.IsLocked(cell, type)` 统一门控。

---

## 三、为什么不做"一格一个 Mesh"

| 问题 | 说明 |
|---|---|
| Draw Call 激增 | 上千个 `MeshRenderer`，性能不可控 |
| 对象数量过多 | 层级爆炸，管理成本高 |
| 过渡面多格依赖 | 相邻过渡/三角过渡/湖海海岸/河流连接仍涉及多格，单格无法独立更新 |

因此"每格都能变化"是对**调用方**而言每格独立，不是渲染对象一格一个。

---

## 四、通用地块变化接口（HexCellPatch + 事务）

各系统**不直接写字段**，统一经服务提交：

```csharp
mapMutationService.BeginTransaction();

mapMutationService.Apply(cell, new HexCellPatch
{
    Height = arenaFloorHeight,
    HexType = Enums.HexType.NoRiver,
    ClearRiver = true,
    ClearLandForm = true,
    ClearResource = true,
    MovementCost = 1f,
    IsUnexplorable = true
});

mapMutationService.Commit(new MapTransitionOptions { ... });
```

`HexCellPatch` 只填写需要修改的字段，未来可扩展：

```
Height | HexType | MovementCost | IsUnexplorable
LandForm | Resource | RiverIncoming/Outgoing | TerrainType
Owner | Building | VisualDecoration
```

**约束（2026-08-04 修订）：**

- **归属 Patch 不能直接写 `Player_City_Index`**——归属是多个系统的事实源，必须经 `TerritoryService` 的领域入口（探索、后勤重算、领地字典重建、事件通知，`TerritoryService.cs:94-165`）。
- **临时可见性不属于 `HexCellPatch`/`HexCellData`**：统一由 `TemporaryVisibilityService` 返回 `VisibilityLease`（来源 token）管理；多个事件同时点亮同一格时，只释放自己的 lease，不影响其他来源。

未来可直接支持：单格升高/降低、洪水淹没、河流改道、火山生成、冰面融化、炸毁桥梁、地块腐化、临时不可通行、地形恢复、运行时地形编辑器。

---

## 五、为什么要事务

竞技场一次修改 37 格——逐格提交会连续重建 37 次。批量事务在 `Commit()` 时统一执行：校验 → 写数据 → 计算脏范围 → 单位/路径处理 → 重建脏区块 → 模型/迷雾/标签更新 → 广播事件。

---

## 六、渲染层：分块 mesh

把整张地图划分为固定大小渲染区块（Chunk）：

- **推荐第一版：8×8 格一个 Chunk**（实际尺寸按地图规模与 Profiler 数据定，见 §十六）
- 每个 Chunk 持有：Terrain / Water / River / Grid 的 **active/staging Mesh 双缓冲** + MeshCollider + **cell→顶点范围映射** + **材质缓存**

```
Map
├─ Chunk_0_0  (Terrain + Water + River + Grid)
├─ Chunk_1_0
├─ Chunk_2_0
└─ ...
```

**修订要点（2026-08-04）：**

1. **迷雾顶点范围元数据必须归 Chunk 持有**：`HexCellData` 目前只存全局索引（`MeshSolidAreaVertexStartIndex` / `MeshTransitionVertexRanges` / `MeshWaterVertexRanges` / `MeshRiverVertexRanges`，`HexCellData.cs:70-77`），`MapRenderer` 只持一套 mesh 与颜色数组（`MapRenderer.cs:25-37`）——Chunk 化后各 Chunk 顶点偏移独立、局部重建会改变偏移，继续存 `HexCellData` 会把迷雾顶点色写到错误的 mesh/索引。改为 Chunk 持有 `Dictionary<HexCoordinate, CellVertexRanges>`。
2. **Mesh/Material 生命周期与原子交换**：`MapController.CreatMesh` 每次新建大量独立材质（基础副本 + 双/三材质过渡实例，`MapController.cs:252-298`、`:332-422`）→ 每 Chunk 至少持有 `activeMesh`/`stagingMesh`。Prepare 只 `Clear()` 并填充 staging；Commit 依次交换 `MeshFilter.sharedMesh` 与 `MeshCollider.sharedMesh`；旧 active 若承担动画快照，Finalize 后才回到 staging 池。材质按材质键缓存共享，独占运行时材质显式 `Destroy`，禁止依赖"引擎自动回收"。
3. **Grid Mesh 与单格高亮冲突**：现有系统直接操作 `HexCellData.GridMesh` 开关/着色（拖拽高亮 `PlayerInputHandler.cs:93-124`、可达高亮 `UIController.cs:369-400`）。合并为 Chunk Grid Mesh 后没有"单格 GameObject" → 必须先定义替代方案：**独立 `HexHighlightRenderer`（按需高亮的格生成小型动态 mesh）或 Chunk Grid 顶点色/纹理 mask**；替代接口落地前不删除 `HexCellData.GridMesh`。

---

## 七、脏范围规则（改一格 ≠ 只重建一格）

当前 mesh 的邻接依赖：

- 实心区域：只依赖自身
- 矩形过渡：依赖自身 + 相邻格
- 三角过渡：依赖 3 个相邻地块（**含邻格拥有的矩形 profile**，见 §九）
- 湖海海岸：依赖周围是否为水（**注意现有代码用渲染缓存判断，见 §八**）
- 河流连接：依赖上下游方向
- 网格线高度：依赖自身

安全规则：

> **地块几何变化 → 标记该格及其一环邻居为脏 → 收集这些格所属 Chunk → 每 Chunk 只重建一次（含 halo 依赖预生成，见 §九）**

```
修改中心格
    ↓
收集中心格 + 一环（7 格）
    ↓
加入所属 Chunk（可能 2~4 个）
    ↓
按 Chunk 去重重建
```

---

## 八、脏标记类型（位标记）

不是所有变化都要重建 mesh：

```csharp
[Flags]
public enum MapDirtyFlags
{
    None       = 0,
    Terrain    = 1 << 0,   // 高度/地形 → 地形 mesh
    Water      = 1 << 1,
    River      = 1 << 2,
    Grid       = 1 << 3,
    Objects    = 1 << 4,   // 地貌/资源模型
    Fog        = 1 << 5,   // 永久/临时可见性变化（由可见性服务触发）
    Navigation = 1 << 6,   // 寻路/移动
    Labels     = 1 << 7,   // 探索费用标签
    Territory  = 1 << 8,   // 归属（2026-08-04 补）
    Logistics  = 1 << 9    // 后勤重算（2026-08-04 补）
}
```

| 修改字段 | 需要处理 |
|---|---|
| `Height` | Terrain + Water + River + Grid + Objects + Navigation |
| `HexType` | Terrain + Water + River + Navigation |
| 河流字段 | Terrain + River |
| `landForm/resource` | Objects（含 LandFormMarkerManager 浮标） |
| `movementCost` | Navigation |
| `IsUnexplorable` | Labels + 探索规则 |
| `TemporaryVisibilityChanged` | Fog（由 `TemporaryVisibilityService` 发布 changedCells） |
| 归属 | **Territory + Logistics**（经 `TerritoryService` 领域入口，不直接写字段）+ Fog |
| 装饰模型 | Objects |

**水域拓扑判断修订（2026-08-04）**：现有湖海连接判断直接检查**渲染缓存** `neighbor.lakeOrSeaVertices.Count != 0`（`MapRenderer.cs:492`、`:527-530`、`:998-1017`）——动态变化后旧水格缓存未清会被误判为水、只清本 Chunk 邻格未重建会误判为非水 → **一律改为逻辑数据判断**（`WaterLevelConfig.IsWater` / `HexType`），并显式处理"从水变陆地"（重置 `HexType`/`isCoast`/`waterLevel`/`movementCost`/湖海顶点缓存）。

> 宝箱摧毁后的恢复只改 `movementCost`/`IsUnexplorable`，并释放 Arena 的 `VisibilityLease` → 不触发任何 mesh 重建 ✅。

---

## 九、Chunk 几何归属规则（防重复/漏面）

继续沿用当前 `MapRenderer` 的所有者规则，**所有几何放入"所有者地块所属 Chunk"**：

- 每格生成自己的实心区域
- 每格只负责 NE / E / SE 三个方向的矩形过渡
- 每格只负责现有两组三角过渡
- 水面/海岸/河流的连接面由明确的所有者格生成

保证：一条边只生成一次，跨 Chunk 边界不重复、不漏面、无 Z-fighting。

**修订要点（2026-08-04）：**

1. **跨 Chunk 三角过渡依赖邻格 profile**：NE-E 三角读取 `neighborNE.SE`（`MapRenderer.cs:788-795`）、E-SE 三角读取 `neighborSE.NE`（`:797-804`），profile 缺失直接抛 `InvalidOperationException`（`:810-819`）。→ **Chunk 构建必须两阶段**：
   - 阶段 1：为目标 Chunk + 一环 halo **预生成矩形 profile**（只供依赖，不输出几何）
   - 阶段 2：生成目标 Chunk 自有矩形与三角几何
2. **跨 Chunk 平滑法线/切线**：现有法线平滑在整图单 mesh 内按世界位置聚合（`MapRenderer.cs:1535-1573`），拆 Chunk 后不同 mesh 无法互相平均 → **halo 法线贡献合并或按世界位置全局累积后回写边界顶点**；除 `FlatAll` 外的 `ShadingStyle` 必须专项验证（§十六）。

---

## 十、顶点列表累积问题的解决

`MeshGeneratorService` 当前大量 `AddRange` 写入 `HexCellData` 顶点列表（如 `SolidAreaVertices.AddRange(...)`，MeshDataGenerator.cs:206），**无 Clear**。

**比"44 → 88"更严重（2026-08-04 修订）**：多数生成函数追加后仍读取**固定索引**（`[0..18]` / `[0..43]`，`MeshDataGenerator.cs:97-117`、`:1339-1406`、`:3468-3492`）——二次重建会继续使用**第一次生成的旧高度顶点**，导致轮廓/过渡错乱。

**推荐改法：MeshGenerator 无状态化**——不再把渲染缓存写回 `HexCellData`：

```csharp
CellMeshData BuildCellMesh(CellBuildInput input, IReadOnlyMapView view);

public sealed class CellMeshData
{
    public List<Vector3> TerrainVertices;
    public List<Vector2> TerrainUVs;
    public List<int> TerrainIndices;
    public List<Vector3> WaterVertices;
    public List<Vector3> RiverVertices;
}
```

Chunk 收集各格临时结果后合并。落地顺序（2026-08-04 修订）：

1. **第一版即推进无状态化（`CellMeshData`）**——审计结论：不能长期依赖"重建前统一清空"作为底层基础（清理协议必须覆盖脏 Chunk + halo 涉及格的全部几何缓存，且要先重建 halo 实心轮廓再建跨格过渡，极易漏）
2. 若第一版必须过渡兼容：统一清空范围 = 脏 Chunk + halo 涉及格，且按"先轮廓后过渡"顺序重建

---

## 十一、碰撞体策略

- 地形高度变化后必须同步 `MeshCollider.sharedMesh`
- **每个 Chunk 一个 MeshCollider**，重建 Terrain Mesh 后同步替换
- Water / River / Grid 不挂碰撞体
- 边界不可通行仍由 `movementCost` 控制，不依赖物理碰撞

**修订要点（2026-08-04）——卡牌射线兼容层：**

现有卡牌落点要求射线命中对象**恰好等于地图根**：

```csharp
hit.transform.gameObject == _mapDataService.MapGameObject   // CardController.cs:197
```

Chunk Collider 在子对象上会使该判断失效，**所有卡牌拖放失败**。必须：

1. Chunk 根节点挂 `MapChunkView` 标记组件
2. 卡牌落点改为 `hit.transform.GetComponentInParent<MapChunkView>() != null`（或统一 Map Layer 判断）
3. 所有地图射线入口（卡牌 `CardController.cs:199`、拖拽高亮 `PlayerInputHandler.cs:103` 等）统一收敛到 `IMapRaycastService`，落点一律经 `GetCellByWorldPosition(hit.point)` 反查

---

## 十二、事务协议：Prepare / Commit 两阶段提交 + Finalize 收尾

> 术语修订：不是“Prepare / Commit / Finalize 两阶段”；准确表述为 **Prepare / Commit 两阶段提交 + Finalize 动画收尾**。

### 12.1 Prepare（无正式副作用）

1. 校验 Patch、目标格和当前事务状态
2. 创建不可变目标快照（`IReadOnlyMapView`）和旧值快照
3. 计算脏格、脏 Chunk 与 halo 依赖集
4. 用目标视图生成 staging Mesh/material descriptors/目标 `RealCenterWorldCoordinate`
5. 收集待移除对象的 `RemovedVisualHandle`，**不清正式引用、不移动单位、不失效路径**
6. 全部成功后产出 `PreparedMapMutation`；任一步失败则释放 staging 资源，正式地图、单位、模型和事件均不变化

交互锁可在事务进入执行队列时获取，但必须由事务 guard 在**所有失败/取消路径**通过 `try/finally` 释放，不能作为“正式地图未变化”的例外。

### 12.2 Commit（同一逻辑帧原子提交）

1. 写入正式 `HexCellData`（归属类变化第一版不支持；后续必须经 `TerritoryService`）
2. Commit 原子地清除资源/地貌逻辑引用，并把旧 GameObject 所有权转交给 `RemovedVisualHandle`
3. 处理单位：`CancelMovesIntersecting(dirtyCells)` + 不可通行格弹射（见 §12.4）
4. 依次交换 Chunk 的 `MeshFilter.sharedMesh` 和 `MeshCollider.sharedMesh`（active ↔ staging）
5. 同步单位/建筑/费用标签到新 `RealCenterWorldCoordinate`
6. 失效 Brain 路径（`GameLoop.InvalidateAllBrainPaths`）
7. 更新费用标签（增删 + 重显刷新 `_labelWorldPositions`）
8. 申请/释放 `VisibilityLease`，更新迷雾目标
9. 广播最小强类型 `MapChangedEvent(CommitId, ChangedCells, DirtyFlags, Phase=Committed)`；随后兼容性 `MapVisualEventSO.Raise()` **只调用一次**

Commit 完成后观察者读取到的必须全是新状态。订阅者异常不得阻断事务收尾；事件调用与后续 Finalize/解锁放在 `try/finally` 保护下。

### 12.3 Finalize（动画/资源收尾）

- `Duration=0`：Commit 后同一调用内同步 Finalize，发布 `Finalized`，销毁/回收旧视觉，释放锁，事务出队
- `Duration>0`：动画结束/取消/对局结束时统一 Kill tween，销毁 `RemovedVisualHandle`，将旧 active Mesh 放回 staging 池，清动画参数，发布 `Finalized` 或 `Cancelled`，释放锁
- 同一个 `CommitId` 只能 Finalize 一次；重复完成/释放为幂等操作

### 12.4 单位移动 API（2026-08-04 修订）

`UnitMovementSystem` 目前只有 `CancelMove(unit)`（`UnitMovementSystem.cs:145-158`），移动任务及路径为私有结构（`:753-764`），且移动中不会重新检查 `movementCost`（`:344-366`）——地块变 MaxValue 后未取消的单位仍会穿过去。**需要新增**：

```csharp
CancelMovesIntersecting(IReadOnlySet<Vector3> dirtyCells);
LockCells(...); UnlockCells(...);
```

同时**原子迁移**四套占用状态：`HaveUnit` / `Occupant` / `AttackerSlots` / `_reservedDestinations`（`HexCellData.cs:243-293`）——弹射单位后预占目的地必须释放、攻击槽必须清理，防止"格内双单位"或"弹走仍占槽"。

### 12.5 RealCenterWorldCoordinate 与动画高度（2026-08-04 修订）

- 逻辑中心在几何生成时即到最终高度（移动系统每步实时读 `RealCenterWorldCoordinate`，`UnitMovementSystem.cs:344`）
- **动画视觉偏移必须与逻辑坐标分离**：`MapVisualTransitionService` 只对单位**视觉模型**施加临时 y 偏移，动画结束归零；禁止在逻辑 transform 上叠加偏移，否则与移动系统追高产生双重高度/跳跃

### 12.6 交互锁（2026-08-04 新增）

现有入口分散且无锁：移动 `UnitMovementSystem.RequestMove`（`:39`）、探索 `ExplorationService.TryExplore`（`:47`）、AI 探索 `AIAutoExplorer`（`:87`）、卡牌拖放 `CardController`。**不得用 `IsUnexplorable` 兼作动画演出锁**（它是永久玩法状态）→ 新增 `IMapInteractionGate.IsLocked(cell, MapInteractionType)`，动画/事务期间锁定，结束后统一解锁。

---

## 十三、地图变化的动画表现（2026-08-03 确认，08-04 修订）

### 13.1 核心原则：逻辑原子切换，视觉独立动画

数据与玩法状态**原子切换**（事务 `Commit` 后寻路/通行/迷雾已为新状态）；**不逐帧修改 `Height` 并重建 Chunk**——每帧重建 mesh/Collider 开销大、湖海/河流等拓扑变化无法简单插值、单位与判定容易不同步。

> **玩法状态一次性提交，视觉表现独立播放动画。**

### 13.2 竞技场突起四阶段时序

1. **Prepare（无正式副作用）**：保存受影响 Chunk 旧渲染快照；通过只读目标视图计算 37 格新数据并生成 staging Mesh；收集待移除模型句柄；不作废路径、不弹射单位、不清正式逻辑引用
2. **LogicalCommit**（同一逻辑帧）：边界格 `MaxValue`、内部平地、清河流/湖海/地貌/资源；申请 Arena 的 `VisibilityLease`；生成 CentralChest；处理单位与路径；交换目标 Chunk mesh/碰撞体——单位不会在动画中穿墙
3. **VisualTransition**（0.8~1.5 秒）：内 2 环升至平台高度、边界环升至墙高（可中心向外错峰）、旧湖海/河流淡出、地貌/资源模型下沉或溶解、符文亮起后淡出、石墙升起、宝箱模型升起、迷雾快速消散
4. **Finalize**：删除旧视觉快照/RemovedVisualHandle、回收旧 active Mesh 到 staging 池、移除动画材质参数、经 `IMapInteractionGate` 解锁、刷新单位站立高度、播放全图播报

### 13.3 Shader 动画的代码事实（2026-08-04 修订）

- **必须同时修改三套地形 Shader**：`TerrainBase_Fog`（基础地形）、`RealMaterialMaskBlend`（双材质过渡）、`ThreeMaterialBlend_Land`（三材质过渡）——否则实心区域升起而矩形/三角过渡留在原地。
- **动画数据通道必须是每顶点语义**：`CreatMesh` 目前只写 `uv`/`colors`（`MapController.cs:207-213`）；顶点色 R/G 已被迷雾占用（`TerrainBase_Fog.shader:38-51`）→ 写入 **UV2.x=`startVertexY`、UV2.y=`targetVertexY`、UV3.x=该顶点来源格的 `staggerDelay`、UV3.y=`participatesInTransition`**。不能用每格统一高度，否则坡面/阶梯/竖面会塌平。
- **跨格过渡 delay 规则**：矩形/三角过渡顶点按其几何端点来源格写 delay，内部插值点对端点 delay 插值；不同错峰下共享边顶点必须逐帧保持完全重合。
- **并行动画**：单个全局 `_ArenaProgress` 无法独立驱动多个同时/排队 Chunk 动画 → 每 Chunk 用 `MaterialPropertyBlock` 或事件索引缓冲。
- 三套 Shader 都在**顶点变形后**调用 `FogBlend_vert`，保证迷雾世界坐标跟随动画位置；ShadowCaster 也必须执行同一 vertex modifier。

### 13.4 三种视觉动画方案

| 方案 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| A **Shader 顶点动画（推荐）** | 目标 Chunk mesh 顶点记录起始高度/目标高度/延迟（UV2/UV3），Shader 内按进度插值 | 每帧只更新少量参数；不重建 mesh；性能最好；支持每格错峰 | 需改三套地形 Shader；点击/碰撞单独处理；湖海/河流拓扑变化需配合淡出 |
| B 临时动画 mesh | 为 37 格生成临时表现层（Floor/Boundary/RiverFade/WaterFade/Decoration），动画完销毁露出底层 | 不改 Shader；演出易控 | 与正式 mesh 防穿插/Z-fighting；偏竞技场专用，通用性弱 |
| C 旧/新 Chunk 交叉淡化 | 旧快照淡出/下沉 + 新 mesh 升起/显现 | 适合拓扑变化（湖海/河流/地貌） | 双倍绘制；透明排序复杂；重叠闪烁；作辅助方案 |

**推荐组合**：

| 变化类型 | 动画方式 |
|---|---|
| 纯高度变化 | Shader 顶点插值（方案 A） |
| 湖海出现/消失 | 旧水面淡出 + 新地形升起（方案 C） |
| 河流出现/消失 | 河流 mesh 溶解/淡出 |
| 地貌/资源清除 | 模型下沉、缩放或溶解 |
| `movementCost` 改变 | 无视觉动画，逻辑立即生效 |
| `VisibilityLease` | 申请时迷雾快速消散、释放时重新聚拢（复用 FogAlpha 过渡） |
| 装饰模型 | Transform 升起/下沉 |
| 归属变化 | 原有领地视觉过渡 |

### 13.5 逻辑切换点：动画开始时切换（推荐）

t=0 逻辑切换（新寻路/新通行/宝箱激活），t=0~1s 只播放动画——保持"突起与宝箱激活同帧"决策，单位不穿墙。动画期间：经 `IMapInteractionGate` 锁定竞技场范围、单位视觉 y 跟随动画高度、Collider 提前切换为最终状态。

> ⚠️ 已知短暂不一致（2026-08-04 补充）：t=0 起射线/物理表面已是最终高度，而画面地面仍在低处——需场景验证可接受性（§十六）。

- 备选 1（动画结束后切换）：所见即所得，但需锁定整个竞技场区域，宝箱激活晚于玩法设定
- 备选 2（动画中点切换）：视觉自然但时序最难维护，**不推荐**

### 13.6 单位与碰撞体处理

逻辑切换后新 Chunk 的 `MeshCollider` 立即安装；动画期间：

- 经 `IMapInteractionGate` 锁定受影响单位的新移动请求
- 边界格单位先弹出
- 内部单位每帧经 `MapVisualTransitionService.GetAnimatedWorldPosition(cell)` 获取当前动画高度，**仅视觉模型** `transform.y` 跟随（逻辑坐标不变，见 §12.5）
- 动画结束偏移归零，单位吸附到最终 `RealCenterWorldCoordinate`

### 13.7 通用动画接口

```csharp
public sealed class MapTransitionOptions
{
    public float Duration = 1f;
    public AnimationCurve Easing;
    public MapTransitionStagger Stagger;
    public bool LockAffectedCells = true;
}

mapMutationService.Commit(new MapTransitionOptions
{
    Duration = 1.2f,
    Easing = arenaRiseCurve,
    Stagger = MapTransitionStagger.CenterToOuter
});
```

错峰模式：`Simultaneous`（同时）/ `CenterToOuter`（中心向外）/ `OuterToCenter`（外环向内）/ `Random`（随机）/ `Directional`（方向推进）/ `Wave`（波浪）。

同一套系统未来可用于：地震波、洪水推进、地面塌陷、冰面逐格融化、腐化扩散、桥梁升起、城墙生成、地块复原。

### 13.8 动画状态机

Chunk/地块状态：`Stable → Preparing → Animating → Finalizing`。

- 动画期间同一 Chunk **不接受第二次几何变更**（排队或合并）
- 纯迷雾/归属变化可与动画合并
- 新高度变化等待当前动画结束
- 对局结束时强制完成动画并清理临时资源

### 13.9 宝箱摧毁后的动画（低成本）

高度不回落 → 无地形下降动画：`movementCost`/`IsUnexplorable` 立即恢复；释放 Arena 的 `VisibilityLease` 走 FogAlpha 过渡（迷雾重新聚拢）；边界石墙下沉/溶解；宝箱破碎消失；**无需重建 Chunk mesh**。

### 13.10 第一版范围

竞技场突起：**中心向外错峰升起，1.2 秒，Commit 固定在动画起始帧**，Shader 顶点动画 + 湖海/河流淡出 + 模型溶解；动画管线后续扩展到任意单格/多格动态事件。`MapLogicalCommitPoint` 不再作为可配置项，避免产生中点/末尾提交的未支持分支。

---

## 十四、竞技场改造为薄层

`ArenaEventManager` 只负责生成 Patch：

| 阶段 | 动作 |
|---|---|
| Reserved | 批量标记 37 格 `IsUnexplorable` |
| Activated | 批量提交 37 格地形/河流/资源/通行变化 + 申请 Arena `VisibilityLease` + 实例化 CentralChest。阶段二无动画能力时强制 `Duration=0`；阶段四检测 `SupportsAnimatedTransition` 后启用 1.2s 中心向外错峰 |
| Destroyed | 批量恢复 `movementCost`/`IsUnexplorable` + 释放 Arena `VisibilityLease` |

它不再负责：如何重建 mesh、刷新迷雾、清除模型、失效路径、移动单位——全部由 `MapMutationService` 统一处理。未来其他地图事件直接复用。

---

## 十五、迁移与兼容策略（2026-08-04 修订）

> 执行顺序以 **§二十一 分步实施计划** 为准，本节为技术路线概要。

1. **MeshGenerator 无状态化（`CellMeshData`）**——第一版即推进，审计结论：不能长期依赖"统一清空"（清理协议覆盖 halo 且顺序敏感，易漏）
2. 新建 `MapMutationService`（单格 Patch + Prepare/Commit + Finalize 收尾 + 最小强类型 `MapChangedEvent`）+ `IMapInteractionGate` + `TemporaryVisibilityService`
3. 先拆运行时安全 WholeMap 后端（Initialize/Prepare/Commit/RefreshCellObjects），再将 `MapRenderer` 扩展为固定大小 Chunk 后端；运行时事务禁止再次调用完整 `MapRender()`
4. **Chunk 射线兼容层**（`MapChunkView` + `IMapRaycastService`，修 `CardController.cs:197`）+ **单格高亮替代方案**（`HexHighlightRenderer`，落定前保留 `HexCellData.GridMesh`）
5. 迷雾顶点范围元数据迁移到 Chunk（cell→range 映射）；水域拓扑判断改为逻辑数据（去 `lakeOrSeaVertices.Count` 依赖）
6. 单位移动 API 扩展（`CancelMovesIntersecting`/锁）+ 原子迁移四套占用状态
7. 新增 `MapVisualTransitionService`（动画进度/错峰/单位视觉高度）+ 三套地形 Shader 动态高度通道（UV2/UV3，方案 A 依赖）
8. 竞技场作为首个使用者验证整条动态地图管线（含动画）

---

## 十六、待确认

> 2026-08-04 更新：大部分未决项已在 §二十 锁定为实施决策，此处仅保留需实现过程中实测/评估的项。

- 性能目标基线（突起帧 30~50ms / 动画帧 <5ms）待 Profiler 实测后定；**当前无自动性能测试设施**，`get_worst_cpu_frames`/`get_worst_gc_frames` 先人工采样
- 8×8 Chunk 已锁定为第一版（§二十-1），最终尺寸按实测调整
- 三套地形 Shader 动态高度通道的改造量待实现时评估
- 非 `FlatAll` 的 ShadingStyle 跨 Chunk 法线无缝为**后续增强**（§二十-11），第一版不保证

---

## 十七、参考代码位置

- 整图渲染入口：`Assets/Scripts/Managers/MapRenderer.cs`（`MapRender()` / `MainMapMeshCreat` / `RiverMeshCreat` / `LakeOrSeaMeshCreat` / `GridMeshCreat`）
- 顶点生成（AddRange 累积 + 固定索引读取旧高度）：`Assets/Scripts/Core/Services/MeshDataGenerator.cs:97-117`、`:206`、`:1339-1406`、`:3468-3492`
- 地块数据（顶点缓存/全局范围/占用状态）：`Assets/Scripts/Core/Models/HexCellData.cs:60-293`
- 合并 mesh 创建（材质实例泄漏点）：`Assets/Scripts/Utilities/MapController.cs`（`CreatMesh` `:200-321`、材质构造 `:252-422`）
- 过渡面依赖相邻高度差 + 跨 Chunk profile 依赖：`MapRenderer.GetGenericRectangleMesh` / `GetSubdivision` / `:783-819`
- 水域拓扑错误依赖渲染缓存：`MapRenderer.cs:492`、`:527-530`、`:998-1017`
- 法线平滑（整图聚合）：`MapRenderer.cs:1535-1573`
- 卡牌射线根对象判断：`Assets/Scripts/UI/CardController.cs:197`
- 单格网格线高亮操作：`Assets/Scripts/Core/Services/PlayerInputHandler.cs:93-124`、`Assets/Scripts/UI/UIController.cs:369-400`
- 迷雾目标计算/限频刷新：`MapRenderer.cs:1218-1241`、`:1160-1169`；`FogTransitionManager.cs:10-12`
- 血条可见性：`Assets/Scripts/Controllers/BuildingBase.cs:56-68`
- 归属领域入口：`Assets/Scripts/Core/Services/Territory/TerritoryService.cs:94-165`
- 移动任务私有结构：`Assets/Scripts/Core/Services/UnitMovementSystem.cs:145-158`、`:344-366`、`:753-764`
- 费用标签动态创建/位置不刷新：`Assets/Scripts/Managers/CostLabelRenderer.cs:150-239`（`:167-177`）
- 地貌浮标仅全量创建：`Assets/Scripts/Managers/LandFormMarkerManager.cs:35-97`
- 事件（无参广播）：`Assets/Scripts/ScriptableObjects/MapVisualEventSO.cs`
- 地形 Shader（方案 A 需加动态高度通道，三套一致）：`Assets/Shader/TerrainBase_Fog.shader`、`Assets/Shader/RealMaterialMaskBlend.shader`、`Assets/Shader/ThreeMaterialBlend_Land.shader`

---

## 十八、验证方案（2026-08-04 确认 + 修订）

分层验证：**功能正确性 → 视觉正确性 → 性能 → 回归**。

### 18.1 单元测试层（数据/逻辑，无渲染）

| 测试 | 验证点 |
|---|---|
| **几何一致性（核心，2026-08-04 修订）** | Chunk 化后**不能直接"顶点/索引序列逐项相等"**（局部索引重排、遍历顺序与材质分组顺序改变）→ 改为：按**所有者格、几何类型、材质键**归一化，三角形转世界坐标并按顶点排序归一化，**浮点容差**比较；单独验证**边界边出现次数**（每条边恰好一次）、法线/切线一致、Collider 命中格 |
| 顶点不累积 | 同一 Chunk 重建两次，顶点数不变（无状态化后自然成立） |
| 脏范围计算 | 改中心格 → 一环邻居 + 所属 Chunk 集合正确；Chunk 边界格 → 邻居 Chunk 也被标记；**枚举中心在 Chunk 内的全部余数位置**（最坏 Chunk 数） |
| 脏位标记 | `Height` 改 → Terrain\|Water\|River\|Grid\|Objects\|Navigation；`movementCost` 改 → 仅 Navigation；归属 → Territory\|Logistics |
| 事务原子性 | 37 格批量提交只触发一次 Commit 流程，不逐格重建 |
| Chunk 归属与 halo | 每格所属 Chunk 索引；跨 Chunk 三角依赖 profile 预生成（不抛 `InvalidOperationException`） |
| 水域双向变化 | 水→陆地：`HexType`/`isCoast`/`waterLevel`/`movementCost` 全重置；陆地→水：反向；连接面不残留/不漏面（去渲染缓存依赖） |
| 单位原子迁移 | 弹射后 `HaveUnit`/`Occupant`/`AttackerSlots`/`_reservedDestinations` 一致，无格内双单位/占槽残留 |
| 交互锁 | 动画/事务期间移动/探索/AI/卡牌入口全部被 `IMapInteractionGate` 拦截 |
| 寻路联动 | `movementCost` 变化后 Dijkstra 结果即时更新（扩展现有 `UnitMovementSystemTests`） |
| Prepare 故障注入 | 几何生成任意步骤抛异常后：正式 HexCellData、单位位置/占用、模型引用、可见性 lease、锁、事件均未变化 |
| Commit/事件异常 | 任一订阅者抛异常后仍能进入 Finalize/Cancelled，锁和队列必定释放；同一 CommitId 只完成一次 |
| `Duration=0` | 同一调用内按固定顺序发布 Committed → Finalized，返回时无活动事务/锁/旧视觉句柄 |
| 临时可见性多来源 | Arena+Spell 重叠、重复释放、异常取消、对局结束强制释放；释放一个来源不影响另一个 |
| Mesh 双缓冲 | Prepare 只改 staging；Commit 前 active 内容不变；Finalize 后旧 active 回池；连续重建 Mesh/材质数不增长 |
| Shader 共享边逐帧一致 | 坡/阶梯/矩形/三角过渡在不同 stagger delay 下逐帧比较共享边顶点，始终完全重合 |

### 18.2 集成验证（Editor 运行时）

- **全流程冒烟**：固定种子地图，`ArenaActivateTime` 测试调短（如 10s）→ 预留区 → 突起 → 单位弹射 → 宝箱出现 → 摧毁 → 恢复，全链路走一遍
- **A/B 截屏对比**：同一种子下"全图重建 vs 分块重建"截屏逐格对比（`capture_scene_object`），找裂缝/接缝/Z-fighting
- **动画验证**：逐帧/录屏检查错峰升起、动画期间单位 y 跟随（无浮空/穿模）、湖海淡出无闪烁；t=0 射线/物理在最终高度 vs 画面在低处的不一致观感
- **迷雾时序**：突起帧 37 格瞬间点亮（Snap 后立即刷新视觉，突破 20fps 限频）、摧毁后重新遮盖
- **卡牌/点击回归**：Chunk Collider 下卡牌拖放、拖拽高亮、可达高亮全部正常
- **边界场景**：竞技场中心落在湖海上/地图边缘/公共建筑附近；入口格朝向；移动中单位恰好途经边界环

### 18.3 性能验证（2026-08-04 修订）

| 指标 | 方法 | 目标基线（待实测） |
|---|---|---|
| 突起帧耗时 | Profiler + `get_worst_cpu_frames`（手工采样） | 重建脏 Chunk ≤ 30~50ms |
| GC 分配 | `get_worst_gc_frames` | 重建一次性分配可接受；动画帧无分配 |
| 动画帧开销 | 每帧只更新少量 Shader 参数/PropertyBlock | < 5ms |
| Draw Call / SetPass | Chunk 化前后记录对比（**允许上升**——每 Chunk 重复材质组合，见 §十九-8） | 增量在预算内 |
| 材质实例数 / Mesh 数 | 重建前后统计（缓存共享后应不增长） | 无泄漏 |
| 渲染对象总数 | 网格线合并 + 高亮独立层后对比 | 显著下降 |

### 18.4 回归验证

- 跑通现有 `Assets/Tests`（移动/探索/过渡几何等，现有测试含 `TriangleTransitionMeshTests`、`MapControllerMeshTests`、`UnitMovementSystemTests`、`HexMapServiceTests`、`DomainInvariantTests`），确保 `MapRenderer` 重构不动初始渲染行为（几何一致性测试兜底）
- **竞技场关闭态**（`ArenaActivateTime` 设极大或关闭）：整局行为与现状完全一致

### 18.5 需补充的测试设施

1. **几何对比测试**（18.1 第一条，按归一化比较法）——投入最大但价值最高
2. **调试命令**：`ArenaEventManager.ActivateNow()`（execute_script 可调，跳过等待）+ 动画 `Duration = 0` 快进开关（逻辑测试用）
3. **调试可视化**：脏 Chunk 高亮色、halo 依赖集、弹射目标格高亮
4. 性能测试：当前无 `Unity.PerformanceTesting` 基准设施——若需要可回归的性能指标，后续引入或先以手工采样存档对比
5. **故障注入钩子**：Prepare 几何构建、Commit 交换、事件订阅者分别可控抛异常，用于验证 guard/Finalize/资源回收

### 18.6 现有工具复用

MCP 工具链：`capture_scene_object`（截屏对比）、`get_unity_logs`、`get_worst_cpu_frames` / `get_worst_gc_frames`（性能，手工采样）、`execute_script`（调试命令注入）、`play_game` / `stop_game`（流程冒烟）；`useFixedSeed = true` 保证地图可复现。

---

## 十九、审计修订记录（2026-08-04）

对照现有代码逐项核对后的修订对照表：

| # | 审计发现 | 严重度 | 修订位置 |
|---|---|---|---|
| 1 | `CentralChest` 子类无法直接触发 `OnPublicBuildingCaptured`（事件仅声明类型内部 Invoke，`PublicBuildingBase.cs:29-30`）；`CurrentDiscoveryState` 为 `private set`（`:53`）；`TickDiscovery` 非 virtual（`:121`） | 严重 | 实现文档 §7.2/§7.3：基类加 protected 入口 |
| 2 | 每 Chunk 一个 MeshCollider 会破坏卡牌落点判断（`CardController.cs:197` 要求命中对象 == 地图根） | 严重 | 本方案 §11：MapChunkView + IMapRaycastService |
| 3 | 水域拓扑用渲染缓存 `lakeOrSeaVertices.Count != 0` 判断（`MapRenderer.cs:492` 等），动态变化会误判 | 严重 | §8：改逻辑数据判断 + 双向重置 |
| 4 | 顶点缓存累积 + **固定索引读取旧高度**（`MeshDataGenerator.cs:97-117` 等），二次重建轮廓错乱 | 严重 | §10：第一版即无状态化 |
| 5 | `MapController.CreatMesh` 每次新建大量独立材质，重复重建泄漏（`MapController.cs:252-422`）；"引擎自动回收"表述不可靠 | 高 | §6、§11；实现文档 §六 |
| 6 | 跨 Chunk 三角过渡依赖邻格 profile，缺失抛异常（`MapRenderer.cs:783-819`） | 高 | §9：halo 两阶段构建 |
| 7 | 跨 Chunk 平滑法线/切线产生光照接缝（`MapRenderer.cs:1535-1573` 整图聚合） | 高 | §9、§16 |
| 8 | Draw Call "只降不升"指标错误：整图 1 mesh 含多 submesh，Chunk 化会重复材质组合 | 高 | §18.3、§16 |
| 9 | `HexCellData` 全局顶点范围元数据在 Chunk 化后失效（迷雾顶点色写错 mesh） | 中高 | §6：Chunk 持有 cell→range 映射 |
| 10 | `MapDirtyFlags` 无 Territory/Logistics 位但表格引用；归属不能直接写 `Player_City_Index` | 中高 | §8：补位 + 领域入口 |
| 11 | `MapVisualEventSO` 无参广播无法承载事务协议（脏格/旧新值/进度/CommitId） | 中高 | §2：新增 MapChangedEvent |
| 12 | `UnitMovementSystem` 无按格取消/锁定 API，移动中不重查 movementCost；四套占用状态需原子迁移 | 高 | §12.4 |
| 13 | 无统一交互锁（移动/探索/AI/卡牌入口分散），`IsUnexplorable` 不可兼作演出锁 | 高 | §12.6：IMapInteractionGate |
| 14 | `RealCenterWorldCoordinate` 唯一赋值在几何生成内（`MeshDataGenerator.cs:207`），文档步骤顺序（先重建后更新）不可分离 | 高 | §12：先算目标几何再提交 mesh |
| 15 | 动画需同时改三套 Shader；顶点色 R/G 被迷雾占用，需 UV2/UV3 通道；全局 `_ArenaProgress` 无法驱动并行动画 | 中 | §13.3 |
| 16 | 费用标签"动态补建"已支持（`CostLabelRenderer.cs:150-239`），缺的是重显时刷新位置（`:167-177`） | 中 | 实现文档 §四-8、§六 |
| 17 | `LandFormMarkerManager` 无按格移除接口（`LandFormMarkerManager.cs:35-97`） | 中 | §12-10：RefreshClusters |
| 18 | 几何一致性测试"顶点/索引逐项相等"不可直接成立（局部索引重排/分组顺序变化） | 中 | §18.1：归一化比较 |
| 19 | "37 格 → 4~6 Chunk"取决于中心余数位置，不能直接确认 | 低 | §16 更正 |
| 20 | `FlatAll` 法线缝低风险，其他 ShadingStyle 需专项验证 | 低 | §16 |
| 21 | **三套稳定地形 Shader 被并入阶段四 UV2/UV3 顶点动画代码**（`vert` 无条件读 `texcoord1/texcoord2`，`participates>0.5` 即改 `v.vertex.y`）→ 实机"整个地图都乱"；实测换普通 Shader 正常（确认非网格问题）；仅加 `Cull Off` 仍乱；**撤除动画代码后恢复正常**。**附带结论：阶段三 Chunked 此前的"散点/缺面"几何损坏同样由该动画 Shader 代码造成——修复后 `Chunked` + `enableExperimentalChunkRenderer=true` 实机验证正常** | 严重 | 三套 Shader（`TerrainBase_Fog`/`RealMaterialMaskBlend`/`ThreeMaterialBlend_Land`）保留 `Cull Off`、撤除 `_ChunkProgress`/UV2/UV3 顶点变形（2026-08-04 已回退，实机恢复）；动画 Shader 后续必须独立命名、独立材质，待验证后再接入（§十三/§二十-10 修订） |

---

## 二十、实施决策锁定（2026-08-04）

以下 12 项为编码前锁定的实施决策，后续如无新事实不再更改：

| # | 决策项 | 锁定方案 | 要点 |
|---|---|---|---|
| 1 | **Chunk 划分** | **8×8 offset-grid Chunk**：`row = GenerateOrder / xNumber`、`column = GenerateOrder % xNumber`、`chunkX = column / 8`、`chunkZ = row / 8` | 不依赖 cube 坐标奇偶/负值，Chunk 分配只用生成网格索引；`HexCoordinate` 继续负责邻接/距离；实现后枚举所有竞技场中心余数位置，实测 37 格影响的最坏 Chunk 数（不预设 4~6） |
| 2 | **渲染后端** | 保留 **WholeMap / Chunked 双后端**（配置 `MapRenderMode` 切换），共用无状态 `CellMeshData` 生成器 | 同种子 A/B 截图、几何归一化对比、快速回退；Chunk 路径稳定后再删旧后端 |
| 3 | **事务原子性** | **Prepare / Commit 两阶段提交 + Finalize 动画收尾**：Prepare 用只读目标地图视图（`IReadOnlyMapView`）生成 staging mesh，成功后才写正式数据 | Prepare 严禁路径/单位/模型等正式副作用；Commit 交换 mesh/Collider + 更新单位/模型/迷雾/标签 + 广播 Committed；Finalize 动画完成/同步路径下清理旧视觉 + 解锁 |
| 4 | **单格高亮** | 独立 **`HexHighlightRenderer`**：按需高亮的格实时生成/更新小型 mesh，多通道（CardPlacement / Reachable / AttackRange / Selection / DebugDirtyChunk） | `PlayerInputHandler`/`UIController` 改为提交高亮格集合，不再直接操作 `cell.GridMesh`；替代接口落地后删除 `HexCellData.GridMesh` |
| 5 | **动画期间交互** | 动画期间受影响格**禁止地图点击/探索/部署/新移动**（`IMapInteractionGate`，玩家与 AI 统一查询）；Collider 起始帧切最终状态 | 解决"物理面在最终高度、画面仍在低处"的不一致——区域不可操作即无错误命中；动画完成统一解锁 |
| 6 | **事务并发** | 第一版**几何事务全局串行队列**：一次只允许一个几何事务进入 Preparing/Animating，后续排队；纯 Fog/Labels/Territory 变化允许合并；对局结束强制完成当前动画并清队 | 后续有性能需求再支持不相交 Chunk 并行 |
| 7 | **临时可见性** | **`TemporaryVisibilityService` + `VisibilityLease`**（Arena / Spell / Tutorial / Debug）：申请 lease / 按 lease 释放 / `IsTemporarilyVisible` | 不用单 bool；可见性不进入 HexCellPatch/HexCellData；ArenaEventManager 持有 lease 并在摧毁、取消、异常、对局结束释放；MapRenderer/BuildingBase 统一查询 `IMapVisibilityResolver` |
| 8 | **地形清除资源** | 独立管线 **`RemoveResourceByMutation` / `RemoveLandFormByMutation`**，**不发奖励** | Commit 清逻辑事实源并把旧对象转交 `RemovedVisualHandle`；Finalize/取消/异常统一销毁；Duration=0 同步清理；`LandFormMarkerManager` 第一版全量重建标记 |
| 9 | **CentralChest 发现机制** | `PublicBuildingBase.TickDiscovery()` 改 **virtual**，宝箱覆写为空；基类新增 `protected MarkRevealedWithoutExploration()` + `protected RaiseCapturedEvent(int factionId)` | 宝箱仍注册 `RegisterPublicBuilding` 复用死亡检测，不建第二套 Tick 注册表 |
| 10 | **Shader 动画通道** | 锁定**每顶点**通道：UV2.x=`startVertexY`、UV2.y=`targetVertexY`、UV3.x=来源格/插值后的错峰延迟、UV3.y=参与标记；每 Chunk 用 `MaterialPropertyBlock` | 过渡顶点 delay 按端点来源/插值规则生成；三套 Shader 同一顶点函数且变形后计算迷雾坐标；不用 StructuredBuffer |
| 11 | **法线支持范围** | 第一版**只保证 `FlatAll` 无 Chunk 光照缝**；其他 ShadingStyle 运行时警告、不保证无缝 | 跨 Chunk 法线/切线回写（halo 贡献）降为后续增强，不再作为第一版阻断项 |
| 12 | **归属 Patch** | `HexCellPatch.Owner` 保留字段设计，但第一版**设置即报"不支持"**，不写入正式数据 | 归属继续走 `TerritoryService` 领域入口；等事务管线稳定后再接入归属 Patch（竞技场本身不需要动态归属） |

---

## 二十一、分步实施计划（2026-08-04 确认）

**决策：分步实现**（阶段一~五），不一次全量实现。

理由：改动面覆盖渲染器、地形生成、单位移动、Shader、UI、建筑基类等 6+ 子系统；一次全量实现时任何一处失败都难以定位，且周期内看不到可运行的竞技场。分步后**每阶段独立可验证、可回退**（阶段三双后端即回退手段）。

### 阶段一：MeshGenerator 无状态化（§二十-3 前置）—— **已完成（2026-08-04）**

| 项 | 内容 |
|---|---|
| 做法 | 抽出显式只读接口 `CellMeshData BuildCellMesh(CellBuildInput input, IReadOnlyMapView view)`，输出不得写回正式 `HexCellData`；现有 WholeMapRenderer 改用它，**视觉输出不变** |
| 覆盖决策 | §二十-2（双后端共用无状态生成器）、§二十-3（IReadOnlyMapView 前置） |
| 验证 | 同种子截图与原版一致；现有 `Assets/Tests` 全绿（`TriangleTransitionMeshTests` / `MapControllerMeshTests` / `HexMapServiceTests` 等） |
| 风险 | 最低——纯重构，失败立即可见，不影响任何游戏逻辑 |

**实施记录（2026-08-04）**：新增 `IReadOnlyMapView`（只读邻居/格子查询 + `MapDataReadOnlyView` 适配器）与 `CellMeshData`（`SolidAreaMeshData` + `CellBuildContext` 构建输入上下文）；`IMeshGenerator` 几何方法全部改为无状态签名（`CellBuildContext ctx` + 显式参数），实心区域/矩形/三角/河流/湖海/海岸/网格线不再写回 `HexCellData` 渲染缓存；`MapRenderer` 维护 `_solidVertices`/`_lakeOrSeaVertices`/`_rectVerticesByCell` 格级注册表并显式同步 `RealCenterWorldCoordinate`；湖海水域判断从渲染缓存（`lakeOrSeaVertices.Count`）改为注册表键判断（行为等价，顺带落实 §8 精神）；`GetFogVertices`/`GetFogConnectorBoundaries` 改为内部即时重建 44 点；删除无调用方的 `GetTriStep4UV_Old` 与旧三角分发函数。验证：Unity 编译零错误；EditMode 测试中网格相关测试（`MapControllerMeshTests`/`TriangleTransitionMeshTests`/`HexMapServiceTests`）全绿，其余 28 个失败均为预存在项（Zenject GameLoop 容器绑定、资源经济数值、edit-mode Destroy 限制、`-nographics` 环境），与本次重构无关；批处理无状态化验证（44 顶点/幂等/不回写/绘制顺序长度）通过。同种子 A/B 截屏对比待编辑器 GUI 可用后补做（当前环境 Unity 以管理员权限拒绝 GUI 启动）。

### 阶段二：基础地图变化服务 + 竞技场玩法全流程（里程碑）

**仍用 WholeMap 后端重建**，先让玩法跑通；但禁止运行时再次调用当前一次性 `MapRender()`。

| 项 | 内容 |
|---|---|
| 做法 | 拆出运行时安全 WholeMap 后端：`InitializeWholeMapView()`（一次性）/ `PrepareWholeMapGeometry(targetView)` / `CommitWholeMapGeometry(staging)` / `RefreshCellObjects(changedCells)`；`MapMutationService`（Prepare/Commit + 同步 Finalize）；阶段二强制 `Duration=0`；最小强类型 `MapChangedEvent`（CommitId/ChangedCells/DirtyFlags/Committed）；`TemporaryVisibilityService` + `IMapVisibilityResolver`；`IMapInteractionGate`；`UnitMovementSystem` 扩展；`PublicBuildingBase` 基类改动；`CentralChest`；`ArenaEventManager`；费用标签位置刷新 |
| 覆盖决策 | §二十-3/5/7/8/9 |
| 验证 | 固定种子 + `ArenaActivateTime=10s` 全流程冒烟；海克斯触发方/迷雾/交互锁正确；**提交返回后无活动事务、无锁定格、队列为空**；连续 `Activate → Destroy → Activate` 后对象数、订阅次数、Mesh/材质数不增长；现有 Tests 全绿 |
| 里程碑 | **竞技场玩法可完整游戏测试**——此后可先行试玩验证玩法设计；若发现问题，在阶段三前调整成本最低 |

**实施记录（2026-08-04，已实施，待编辑器 GUI 复编译/冒烟验证）**：

- **渲染后端**：`MapRenderer` 实现新接口 `IMapRenderBackend`（`PrepareWholeMapGeometry` / `CommitWholeMapGeometry` / `RefreshCellObjects` / `ForceRefreshFogVisuals`），四个 `*MeshCreat` 拆为"纯数据构建（TerrainGeometry/RiverGeometry/WaterGeometry）→ 应用"两段：运行时重建复用既有 Mesh（`mesh.Clear()` 后重建，Collider 同引用自动生效），材质按基础材质组合键缓存共享（`_rectMaterialCache`/`_triMaterialCache`，基础 3 材质只建一次）；河流/湖海/网格线宿主 GO 持久复用，无水面时显式销毁旧宿主。`MapController` 的 `ConfigureBlendMaterial`/`SanitizeNormalsAndTangents`/`CreateTerrainFogMaterial`/`GetOrCreateBarycentricMask` 改 public 供缓存复用。**阶段二 Prepare 直接读已写入目标数据的真实地块**（非克隆目标视图），与 Commit 同一同步调用内完成——Duration=0 下无观察窗口，等价原子（true 两阶段留给阶段三 Chunk 后端）。`RefreshCellObjects` 处理变化格：清空模型转交 `RemovedVisualHandle`（同步 Finalize 销毁）、保留模型归位、网格线按格重建/补建/销毁。
- **地图变化服务**：新增 `MapMutationService`（BeginTransaction/Apply/Commit/Rollback，实现 §12 事务协议 + 同步 Finalize）、`HexCellPatch`（字段级补丁；`Owner` 设置即抛 NotSupported，§二十-12）、`MapDirtyFlags`/`MapChangedEvent`（阶段二仅 Committed）、`MapTransitionOptions`（Duration>0 警告并按 0 处理）、`MapCommitResult`、`RemovedVisualHandle`。Commit 顺序：写数据（含水→陆/陆→水双向重置，§8）→ `CancelMovesIntersecting` + `EjectUnitsFromImpassableCells`（决策 B）→ Prepare/Commit 几何 → `RefreshStandingUnitPositions` → `RefreshCellObjects` → `LandFormMarkerManager.CreateAllMarkers`（§二十-8 全量重建）→ `InvalidateAllBrainPaths` → `MapVisualEventSO.Raise()`（兼容，仅一次）→ `MapChanged(Committed)` → 句柄销毁；finally 释放锁/事务。**交互锁拆为独立叶子类 `MapInteractionGate`**（`LockCells/UnlockAll/IsLocked`，避免 MapMutationService ↔ UnitMovementSystem 循环构造依赖）；入口接入：`UnitMovementSystem.RequestMove`（Move）、`ExplorationService.TryExplore`（Explore，AI 经统一入口天然覆盖）、`CardPresenter.IsReleaseValid`（Deploy）。阶段二 Duration=0，锁仅同步持有。
- **临时可见性**：`TemporaryVisibilityService`（来源式 `VisibilityLease`：Acquire/Release 幂等/ReleaseAll；`IMapVisibilityResolver.IsVisibleToFaction` = 临时 lease || 后勤永久可见性）；`MapRenderer.UpdateFogTransitionTargets` 与 `BuildingBase.RefreshHealthBarVisibility` 统一改查 `IMapVisibilityResolver`（宝箱血条随 Arena lease 显示）。`ForceRefreshFogVisuals` 突破 20fps 限频，突起/恢复瞬间生效。
- **单位移动**：`CancelMovesIntersecting(blockedCells)`（路径途经 MaxValue 格即取消，回落起点）、`EjectUnitsFromImpassableCells`（6 向 BFS 最近可通行且无占用格，原子迁移 HaveUnit/Occupant/攻击槽/位置，纯位置迁移不触发占领）、`RefreshStandingUnitPositions`（站立单位吸附新 `RealCenterWorldCoordinate`，移动中单位跳过）；`HexCellData.ReleaseAttackerSlots(unit)` 新增。
- **宝箱**：`PublicBuildingBase` 新增 `protected RaiseCapturedEvent(int)`（海克斯触发入口，事件仅声明类型内部 Invoke）+ `MarkRevealedWithoutExploration()`（直接置 Revealed，不写探索位）；`TickDiscovery` 改 virtual。`CentralChest`：1 格/单段 500HP/始终中立（PlayerIndex=-1）/生成即激活/覆写 `OnDeath`（触发海克斯 + `ChestDestroyed` 通知）/覆写 `UpdateVisual`（NeutralBuilding 双方可攻击 + 金色血条）/覆写 `TickDiscovery` 为空。宝箱血条画布运行时构造（World Space Canvas + child0 Slider，WireBuildingCanvas 约定）。索敌链第二优先级：`UnitBrainBase.FindNearestChest()` + `MeleeStrategy`/`RangedStrategy` 接入（敌方单位 > 宝箱 > 敌方建筑，玩法文档 §4.2）；箭塔天然忽略（只索敌单位）。
- **竞技场**：`ArenaEventManager`（状态机 Inactive→Reserved→Activated→Destroyed；`ArenaActivateTime` 用 GameLoop.GameTime 自算，0=开局即突起，暂停冻结；37 格=中心+半径3，入口 E/W 对称留 2 格；预留区开局 `IsUnexplorable=true`；突起：内 2 环平台高+清河流/地貌/资源、外环墙高+非入口 MaxValue、入口按普通地块；Arena `VisibilityLease` 点亮 37 格；宝箱生成即激活；摧毁恢复：全 37 格解除 IsUnexplorable + 外环恢复 movementCost=1 + 释放 lease + 迷雾重盖 + 中心格确定性清格；`Shutdown()` 由 `EndGame.EndThisGame` 调用兜底）。配置：`MapGenerationConfigSO` 新增 `ArenaActivateTime`(90)/`ArenaFloorHeight`(3)/`ArenaWallHeight`(4)/`centralChestModel`（引用 `Assets/Model/ResourceModel/Chest.prefab`），资产 YAML 已更新。出生排除：`GameFlowManager.PlayerInit` 排除预留区外 2 环、`PublicBuildingGenerator` 候选排除外 1 环（玩法文档 §7.3）。
- **标签/浮标**：`CostLabelRenderer` 重显时刷新 `_labelWorldPositions` 到新 `RealCenterWorldCoordinate`（修复高度变化后标签停留旧 Y）。
- **DI**：GameInstaller 绑定 `TemporaryVisibilityService`（+IMapVisibilityResolver）、`MapInteractionGate`（+IMapInteractionGate）、`MapMutationService`、`ArenaEventManager`（ITickable）。
- **测试**（EditMode 新增 3 文件）：`MapMutationServiceTests`（13 例：补丁写入/水陆双向重置/河流清空/脏位矩阵/事件单次 Committed/批量重建一次/提交后无事务无锁/Owner 拒绝/无事务 Commit/Rollback/Duration>0 兼容）、`TemporaryVisibilityServiceTests`（9 例：多来源/释放互不影响/幂等/ReleaseAll/永久回落/Null 兜底）、`UnitMovementEjectionTests`（6 例：弹射最近格/跳过被占格/目的地变不可通行取消/无关格不取消/站立吸附/移动中跳过）。`MainGame.csproj`/`Tests.csproj` 为编辑器生成文件，本次为离线编译验证临时加入新文件条目（编辑器刷新后自动重生成）。
- **已知偏差（阶段二内接受）**：① Prepare 读真实地块而非克隆目标视图（上文说明，Duration=0 等价原子）；② 预留区符文/石墙视觉标记未做（设计标注"可选"）；③ 全图播报/特效未接（阶段四动画一并处理）；④ 同种子 A/B 截屏与全流程冒烟待编辑器 GUI 可用后补做。

### 阶段三：Chunk 渲染器 —— **已实施（2026-08-04），实机验证通过：`mapRenderMode=Chunked` + `enableExperimentalChunkRenderer=true` 均正常显示**

| 项 | 内容 |
|---|---|
| 做法 | `ChunkMapRenderer`（8×8 offset-grid，§二十-1）；WholeMap / Chunked 双后端并存（配置切换）；Chunk 持有 cell→range 映射（迷雾顶点色回写）；`mesh.Clear()` 复用 + 材质缓存；`IMapRaycastService` + 卡牌射线兼容（`CardController.cs:197`）；`HexHighlightRenderer` 替代 `cell.GridMesh`；几何归一化 A/B 对比 |
| 覆盖决策 | §二十-1/2/4 |
| 验证 | A/B 截图无接缝/Z-fighting；卡牌拖放/拖拽高亮/可达高亮回归；突起只重建脏 Chunk 而非全图；Profiler 确认性能基线；枚举全部中心余数位置实测最坏 Chunk 数 |

**实施记录（2026-08-04）**：

- **Chunk 划分（§二十-1）**：`ChunkIndex`（X/Z 只读结构 + 相等/哈希/`ToString`）；`ChunkIndex.Of(cell, xNumber)` 实现锁定公式（`row=GenerateOrder/xNumber`、`column=GenerateOrder%xNumber`、`chunkX=column/8`、`chunkZ=row/8`）；`ChunkMapRenderer.ChunkSize=8`。
- **ChunkMapRenderer**（`Assets/Scripts/Managers/ChunkMapRenderer.cs`，IMapRenderBackend 实现）：每 Chunk 独立 GO（根挂 `MapChunkView` 标记 + Map Layer）+ Terrain（MeshFilter/MeshRenderer/MeshCollider）+ Water + River 宿主；active/staging 双缓冲 Mesh（`mesh.Clear()` 复用）；材质按基础组合键缓存共享（`_terrainBaseMaterial0/1/2` 单建 + `_rectMaterialCache`/`_triMaterialCache`）；`ChunkRenderData` 持有 cell→`CellVertexRanges` 映射（§6-1：迷雾顶点色按 Chunk 局部索引回写，不再依赖 `HexCellData.MeshSolidAreaVertexStartIndex` 等全局索引）。
- **两阶段构建（§九）**：`PreBuildRectProfiles` 阶段 1 对（目标 Chunk + 二环）预生成实心 44 点，并对（目标 Chunk + 一环 halo）预生成湖海 25 点/矩形 profile（halo 格构建矩形 profile 还会读取自身邻居 solid，因此 solid 依赖必须多扩一环）；`BuildChunkTerrain/River/Water` 阶段 2 只输出目标 Chunk 自有几何，三角过渡经 `GetGenericTriangleMesh` 读 halo profile；缺失依赖现在抛含 owner/direction/neighbor 的明确 `InvalidOperationException`，不再以模糊 `NullReferenceException` 失败；水域拓扑一律逻辑数据判断（`WaterLevelConfig.IsWater`，§8）。
- **脏 Chunk 重建（§七）**：`PrepareChunkGeometry(changedCells)` 改格 + 一环邻居 → 所属 Chunk 去重 → 只重建脏 Chunk staging；`CommitChunkGeometry` 交换双缓冲；`PrepareWholeMapGeometry`/`CommitWholeMapGeometry` 在 Chunked 后端退化为全 Chunk 重建。`IMapRenderBackend` 扩展 `SupportsChunkedRebuild` + Chunk 方法，`MapMutationService.Commit` 按后端能力分派（WholeMap 后端照旧全量）。
- **射线兼容层（§11）**：`MapChunkView` 标记组件 + `IMapRaycastService`/`MapRaycastService`（`IsMapObject` = 地图根 || `GetComponentInParent<MapChunkView>`）；`CardController.cs:197` 改为经 `IMapRaycastService.RaycastMap` + `GetCellByWorldPosition(hit.point)`（注入失败回退原物理射线判断）。
- **单格高亮（§二十-4）**：`HexHighlightRenderer`（多通道 `HexHighlightChannel`：CardPlacement/Reachable/AttackRange/Selection/DebugDirtyChunk），按格生成小型动态 mesh（`BuildGridVertices` 外圈 6 点 + 中心上表面），`SetHighlightedCells`/`ClearChannel`/`ClearAll`；`PlayerInputHandler` 拖拽高亮与 `UIController` 可达高亮接入（注入 Optional，未注入时保持 `cell.GridMesh` 旧路径——替代接口落地前不删 `HexCellData.GridMesh`，§6-3）。
- **配置与绑定**：`MapGenerationConfigSO.mapRenderMode`（`Enums.MapRenderMode`：WholeMap/Chunked，默认 WholeMap）；`GameInstaller` 按配置绑定 `IMapRenderBackend`（Chunked → `ChunkMapRenderer` FromNewComponentOnNewGameObject；WholeMap → `MapRenderer` FromResolve）+ `IMapRaycastService` + `HexHighlightRenderer`（FromNewComponentOnNewGameObject，代码创建不依赖场景组件）；`MapRenderer.MapRender` 内按模式分派网格构建（Chunked → `_chunkMapRenderer.ChunkMapRender`），地貌/资源/环境雾效/费用标签/迷雾全局仍由 MapRenderer 统一处理；`ArenaEventManager` 依赖从 `MapRenderer` 改为 `IMapRenderBackend`（`ForceRefreshFogVisuals` 兼容双后端）。
- **已知偏差（阶段三内接受）**：① 非 `FlatAll` 的 ShadingStyle 未做跨 Chunk 法线合并（§二十-11：第一版只保证 FlatAll，运行时其他风格不保证无缝）；② Chunk 后端网格线（`cell.GridMesh`）未合并为 Chunk 级 Grid mesh，高亮走 `HexHighlightRenderer`、原网格线显示逻辑保留（§6-3 替代接口落地前不删除）；③ 跨 Chunk 平滑法线/切线（§9-2）列为后续增强。
- **实机验证（2026-08-04，补充）**：`mapRenderMode=Chunked` + `enableExperimentalChunkRenderer=true` 实机运行**正常显示**，Chunk 网格无散点/缺面——此前记录的"严重几何损坏"实为阶段四动画 Shader 代码所致（§十九-21），修复后 Chunked 恢复可用。严格几何归一化 A/B 对比与竞技场全流程冒烟仍按 §18 补做。
- **动态重建实测数据（2026-08-04，补充）**：经 `MapMutationDebugHotkeys`（§18.5-2 调试命令的运行时替代：F8 提交日志 / F9 脏 Chunk 高亮 / F10 立即突起 / F11 资源计数）验证竞技场突起：
  - `Commit#1 完成：补丁 37、脏格 37、脏 Chunk 4、脏位 Terrain,Water,River,Grid,Objects,Navigation,Labels、耗时 86ms`——全图 576 格 = 24×24 → 8×8 Chunk 共 **9 个**，37 格竞技场只重建 **4 个（≈44%）**，脏 Chunk 收敛正确（中心恰在 Chunk 边界附近呈 2×2）；86ms 为 Commit 全程（写数据+单位处理+4 Chunk 几何+对象/浮标+路径失效+广播），后续可用 `CommitSliced` 分帧摊薄；
  - 资源基线（突起后 F11 快照）：Mesh 总数 228（运行时实例 89）、Material 318、活动 MeshRenderer 2585、活动 MeshCollider 12（9 Chunk Terrain + 其他）——双缓冲 + 材质缓存预期无泄漏，**连续 Activate→Destroy→Activate 后对比 Mesh/Collider 数不增长** 待补测。
- **测试**（`Assets/Tests/ChunkRendererTests.cs`）：`ChunkIndexTests`（4 例：offset-grid 公式/Chunk 边界列/首格/相等性）、`CellVertexRangesTests`（2 例：初始状态/局部偏移记录）、`DirtyChunkComputationTests`（3 例：中心格 + 一环最坏 ≤4 Chunk/同 Chunk 去重/Chunk 边界格集合）。离线编译：`MainGame.csproj`/`Tests.csproj` 0 error（新增文件条目已临时加入，编辑器刷新后自动重生成）。

### 阶段四：Shader 顶点动画 —— **实验代码已实施（2026-08-04），实机发现破坏稳定渲染，动画代码已回退，动画路径当前不可用**

| 项 | 内容 |
|---|---|
| 做法 | 阶段二事务协议扩展 `TransitionStarted/Finalized/Cancelled`；三套 Shader 加每顶点 UV2/UV3 通道；每 Chunk `MaterialPropertyBlock`；`MapVisualTransitionService`（进度/错峰/单位视觉偏移）；动画状态机；检测 `SupportsAnimatedTransition` 后启用 1.2s 中心向外错峰 |
| 覆盖决策 | §二十-5/10 |
| 验证 | 动画流畅、无穿模、单位 y 跟随、湖海/地貌淡出正确；动画期间交互锁定生效；t=0 物理面与画面短暂不一致不可被玩家操作感知 |

**实施记录（2026-08-04）**：

- **事务协议扩展**：`MapChangedPhase` 新增 `TransitionStarted/Finalized/Cancelled` 阶段；`MapTransitionOptions` 增加 `Easing`（AnimationCurve，null=默认 smoothstep）、`Stagger`（`MapTransitionStagger` 枚举：Simultaneous/CenterToOuter 已实现，其余保留后续接入并降级警告）、`StaggerCenter`（中心向外错峰基准格）；`RemovedVisualHandle` 暴露 `Objects` 只读列表（动画期间模型下沉溶解用，§13.4）。
- **MapVisualTransitionService**（`Core/Services/MapMutation/MapVisualTransitionService.cs`，ITickable）：`ComputeStaggerDelays`（Simultaneous 全 0；CenterToOuter 按 cube 距离归一化 ×0.6 错峰上限，§13.2）；`BeginTransition`（Duration≤0 同步完成；已有活动动画先强制完成旧动画——全局串行 §20-6）；`Tick(deltaTime)` 逐帧推进 eased 进度 → 每 Chunk `SetChunkAnimationProgress`；单位视觉高度跟随（站立且非移动中单位，§13.6）；视觉跟随物（宝箱随格升起，§13.2）；待移除模型下沉+缩小溶解（§13.4）；`ForceComplete`（对局结束，发布 Cancelled）；`GetAnimatedWorldY(cell)`（动画期格子当前世界 Y）。完成/取消统一走 `Complete`（幂等）：进度定格 1 → 后端收尾 → 单位吸附最终高度 → 销毁句柄 → 解锁 → 发布 Finalized/Cancelled。
- **IMapRenderBackend 扩展**：`SupportsAnimatedTransition`（Chunked=true / WholeMap=false）；`PrepareAnimatedChunkGeometry(changedCells, oldHeights, staggerDelays)`（同脏 Chunk 计算 + 带 UV2/UV3 动画通道的 staging）；`CommitAnimatedChunkGeometry`（交换 mesh + MPB 进度置 0）；`SetChunkAnimationProgress(index, progress)`（MPB `_ChunkProgress`）；`FinalizeChunkAnimation(index)`（进度定格 1 + 销毁湖海/河流淡出幽灵）。WholeMap 后端全部抛 NotSupportedException（MapMutationService 检测后降级同步）。
- **每顶点动画通道（§20-10）**：`TerrainGeometry` 增加 `UV2s/UV3s`（`Vector2[]`，普通重建为 null）；`FillMeshData` 仅在非 null 时写 `mesh.uv2/uv3`。`BuildChunkTerrain` 动画模式：实心区域 startY=v.y−delta(cell)、过渡顶点按几何端点来源格插值——矩形过渡用 UV.v（profile 进度 0=self→1=neighbor）插值两端格 delta/delay；三角过渡用重心 UV（self 权重 1−u−v）三端插值（§13.3 共享边逐帧重合）；河流矩形按 owner 格近似（竞技场清河后实际不出现）。UV3.y=participates=1，UV3.x=错峰延迟。
- **三套地形 Shader**（`TerrainBase_Fog` / `RealMaterialMaskBlend` / `ThreeMaterialBlend_Land`）：~~Properties 加 `_ChunkProgress`；`vert` 读 `texcoord1`(uv2)/`texcoord2`(uv3)，participates>0.5 时按 `saturate((_ChunkProgress−delay)/(1−delay))` + smoothstep 插值 `v.vertex.y`，顶点变形后调用 `FogBlend_vert`；addshadow 复用同一 vertex 函数~~——**2026-08-04 实机发现该改动破坏稳定地图渲染（换普通 Shader 正常），已全部回退**；当前三套 Shader 仅保留 `Cull Off`，动画通道待独立动画 Shader（`*_Transition` 命名 + 独立材质）方案验证后再接入（§十九-21）。
- **湖海/河流淡出（方案C 简化）**：`CommitAnimatedChunkGeometry` 提交前若"旧水面/河流有顶点而新 staging 无"则克隆旧 mesh 为淡出幽灵（共享材质 + MPB `_FadeAlpha`=1−progress），Finalize 销毁；`LakeorSea.shader`/`River.shader` 增加 `_FadeAlpha` 通道（§13.4）。
- **MapMutationService 动画路径**：`Commit` 检测 `Duration>0 && SupportsAnimatedTransition && 服务注入`；写数据前快照 `oldHeights`（Height 级差）与 `oldCenterWorldY`（世界 Y）；动画路径不解锁、不销毁句柄（锁保持到 Finalize，§20-5）；先强制完成旧动画再锁新格（避免旧 Finalize 清锁）；`BeginTransition` 抛异常时 finally 仍解锁（flag 后置）。`Commit_DurationGreaterThanZero` 降级警告逻辑保留。
- **竞技场**：`ArenaEventManager.Activate()` 检测 `_renderBackend.SupportsAnimatedTransition` → `Duration=1.2f`（`ArenaRiseDurationSeconds`）+ `CenterToOuter` + `StaggerCenter=_centerCell`；宝箱生成后注册为视觉跟随物随地形升起（§13.2）；`OnChestDestroyed`/`Shutdown` 注销跟随物并 `ForceComplete`。
- **DI**：GameInstaller 绑定 `MapVisualTransitionService`（ITickable）；`UnitMovementSystem.IsUnitMoving` 公开（§12.5 移动中单位跳过）。
- **测试**（`Assets/Tests/MapVisualTransitionServiceTests.cs` 14 例：错峰计算/同步完成/生命周期/进度驱动/幂等/ForceComplete/单位跟随/宝箱跟随/模型溶解；`MapMutationServiceTests` 增 3 例：动画路径几何分派+锁保持到 Finalize、ForceComplete 解锁、不支持动画降级）。离线编译：`MainGame.csproj`/`Tests.csproj` 0 error。
- **已知偏差（阶段四内接受）**：① 湖海/河流淡出仅覆盖"整 Chunk 水面/河流移除"场景（部分移除仍瞬时消失），方案C 完整交叉淡化留待后续；② 动画期间单位视觉跟随作用于逻辑 transform.y（本代码库无独立视觉子模型），依赖受影响格锁定+移动取消保证安全（§12.5 精神）；③ 河流矩形动画 UV 按 owner 格近似（竞技场清河后不触发）；④ t=0 物理面在最终高度而画面在低处的短暂不一致依赖交互锁不可感知（§13.5/§20-5）。

### 阶段五：通用化与后续增强 —— **代码已实施（2026-08-04）；依赖阶段三的部分（分帧提交、并行动画调度）随 Chunked 验证通过而可用，依赖动画的视觉增强仍不可用**

> 阶段五把阶段二~四为竞技场打磨的管线从"竞技场专用"泛化为"任意地图事件可接入"的通用底座，并补齐几项性能/视觉增强。`MapMutationService` + `HexCellPatch` 本身已经是通用入口，竞技场只是第一个调用方。

| 项 | 内容 | 优先级 |
|---|---|---|
| 归属 Patch 接入（§二十-12） | `HexCellPatch.Owner` 当前设置即抛 NotSupported；接入后可经 `TerritoryService` 领域入口动态改归属 | 低（竞技场不涉及动态归属） |
| 诊断/批处理扩展 | MapChangedEvent 补充脏范围可视化、批量提交日志等开发辅助能力 | 低（调试时可手动） |
| 非 `FlatAll` 法线/切线同步 | 跨 Chunk 边界光照接缝修复（§二十-11），当前默认 FlatAll 不受影响 | 低（shadingStyle=FlatAll） |
| 不相交 Chunk 并行动画（§二十-6） | 多个互不相邻的动画同时播放；当前全局串行已够用 | 低（单竞技场无并发需求） |
| 分帧提交 | 大范围变化拆多帧防卡顿；竞技场 37 格重建帧耗时在预算内 | 低 |

| 项 | 内容 |
|---|---|
| 覆盖决策 | §二十-6/11/12 |
| 验证 | 管线通用性验证（以任意一个新地图变化事件接入同一条管线即达标） |

**实施记录（2026-08-04）**：

- **归属 Patch 接入（§二十-12）**：`MapMutationService` 构造新增可选 `ILogisticsService`（`[InjectOptional]`）；`Apply(Owner)` 不再直接抛 NotSupported，而是收集 `_pendingOwners`，`Commit` 写数据后经领域入口应用（`Owner >= 0` → `SetOwner`；`< 0` → `ClearOwner`），并置 `Territory|Logistics|Fog` 脏位；未注入 `ILogisticsService` 时保持抛 NotSupported（向后兼容旧测试）。`HexCellPatch.Owner` 注释同步更新。
- **诊断扩展**：`MapChangedEvent` / `MapCommitResult` 新增可选 `AffectedChunks`（脏 Chunk 索引，Chunked 后端有值）；新增静态开关 `MapMutationDiagnostics`（`EnableCommitLogging` / `EnableDirtyChunkHighlight` / `HighlightDurationSeconds` / 高亮颜色）；`MapMutationService` 用 `Stopwatch` 计时并在开关开启时打印提交摘要（CommitId/补丁数/脏格数/脏 Chunk 数/耗时/脏位）；可选注入 `HexHighlightRenderer`，开关开启时用 `DebugDirtyChunk` 通道高亮脏格，提供 `ClearDirtyChunkHighlight()` 手动清除。
- **非 FlatAll 跨 Chunk 法线合并（§二十-11）**：`ChunkMapRenderer` 新增 `MergeChunkBoundaryNormals`——`CommitChunkGeometry` 与首帧全量 `ChunkMapRender` 提交后，对本次脏 Chunk 的 Terrain mesh 按世界位置（量化 1e-4）聚合顶点法线，边界顶点（出现在 ≥2 个 mesh）写回归一化平均并 `RecalculateTangentsSafe`；`FlatAll` 跳过（面法线无接缝）。初始全量构建同样执行。混合风格（`FlatRect_SmoothTri` 等）矩形/三角过渡为扁平法线（重复顶点），跨 Chunk 合并可能轻微影响扁平外观，运行时仍按原逻辑警告，待专项验证。
- **不相交 Chunk 并行动画（§二十-6）**：`MapVisualTransitionService` 单 `_active` 改为 `List<ActiveMapTransition> _actives`：`BeginTransition` 只强制完成与新动画 Chunk 相交的旧动画（`CompleteConflicting`），不相交动画并行推进；`ForceCompleteConflicting(chunks)` 供提交前锁顺序处理（chunks 为 null 时保守完成全部，等价旧全局串行）；`Complete` 只 `UnlockCells` 自己的格（`MapInteractionGate` 新增 `UnlockCells`），互不清锁；跟随物（宝箱）移入 `ActiveMapTransition.Followers` 各动画独立持有；`GetAnimatedWorldY` 按含该格的活动动画查询。`MapMutationService.Commit` 动画路径：锁前先经 `TryComputeDirtyChunkIndices`（新接口方法，WholeMap 抛 NotSupported 时回退 null）`ForceCompleteConflicting`；非动画路径 finally 由 `UnlockAll` 改为 `UnlockCells(changedSet)`（避免清掉并行动画的锁）；`Rollback` 同样只释放本事务格。
- **分帧提交**：`IMapRenderBackend` 新增 `ComputeDirtyChunkIndices(changedCells)` 与 `PrepareChunkGeometrySlice(chunkIndices)`（Chunked 实现；WholeMap 抛 NotSupported）；`MapMutationService.CommitSliced(options, maxChunksPerFrame)`：数据写入/归属/单位处理同步原子完成，脏 Chunk 入队后由 `TickSlicedCommit()` 每帧构建 ≤maxChunksPerFrame 个 Chunk staging，全部完成才 `CommitChunkGeometry` + 对象/浮标/路径/广播/解锁（`Committed` 只发一次）；首帧立即推进一批；锁从开始保持到全部完成（等价动画锁语义）；不支持 Chunked 时警告降级同步 `Commit`；分帧进行中禁止新事务（§20-6 串行）；`ForceCompleteSliced()` 供对局结束兜底（`ArenaEventManager.Shutdown` 调用）；新 `MapSlicedCommitExecutor : ITickable` 每帧转发 `TickSlicedCommit`（GameInstaller 绑定）。
- **管线通用性验证**：新增 `MapMutationDemoCommands`（execute_script 可调）三个演示事件——洪水（整环压至 seaLevel 变水）、地震抬升（整环抬升 + 可选外环不可通行）、分帧洪水（CommitSliced 版），全部经同一条 `MapMutationService` 管线，证明任意新事件接入即达标。`ChunkStagingGeometry`/`PreparedChunkGeometry.Chunks` 由 internal 改 public（纯 DTO，供分帧提交与测试构造）。
- **DI**：GameInstaller 绑定 `MapSlicedCommitExecutor`（ITickable）；`MapMutationService` 可选注入 `ILogisticsService`/`HexHighlightRenderer`（均已绑定，无需新增）。
- **测试**（`Assets/Tests/MapMutationStage5Tests.cs` 新增 18 例）：归属 Patch（SetOwner/ClearOwner 经领域入口、脏位 Territory|Logistics|Fog、未注入仍抛）、AffectedChunks 事件字段、日志/高亮开关不抛、并行动画（不相交并行/相交取消旧动画/ForceCompleteConflicting 只完成冲突者/并行完成只解锁自己的格）、分帧提交（3 Chunk 每帧 1 个 → 3 次构建 + 1 次提交 + Committed 只发一次 + 锁保持到完成/WholeMap 降级同步/ForceCompleteSliced/无事务返回 null/分帧中新事务抛错）、管线通用性（洪水风格新事件 + 动画版新事件全生命周期）。
- **已知偏差（阶段五内接受）**：① 混合 ShadingStyle（`FlatRect_SmoothTri`/`SmoothRect_FlatTri`/`ForceUpNormals`/`ExaggeratedNormals`）跨 Chunk 法线合并对扁平过渡部分的视觉效果待专项验证（§二十-11 后续增强）；② 分帧提交第一版不支持动画组合（Duration>0 警告降级）；③ 分帧提交的 `AffectedChunks` 在 `CommitSliced` 返回值中为 null（事件中的有值）；④ EditMode 测试运行与全流程冒烟待 MCP 桥接可用后补做（当前编辑器实例未挂 MCP 桥接；离线编译 `MainGame.csproj`/`Tests.csproj` 0 error，编辑器 GUI 后台复编译 `LogAssemblyErrors 0ms` 通过）。

### 阶段间关系

- 阶段一+二代码量约占整体 40%，贡献 100% 的可测试玩法价值——**先跑通玩法，再优化渲染**。
- 每阶段结束保持可运行状态；阶段三双后端并存提供回退能力。
- 阶段二结束若发现玩法设计问题（节奏/数值/索敌），调整成本最小；阶段三以后改动集中在渲染层，玩法层冻结。
