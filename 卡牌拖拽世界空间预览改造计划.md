# 卡牌拖拽世界空间预览-实现计划

> 目标：拖拽普通卡时，在世界空间实例化一次该卡对应的单位/建筑模型，由主相机渲染、跟随指针；松手后**同一对象**就地接上运行时组件，成为真实单位/建筑，并播放一段落位补间。成功路径不重复实例化、不销毁模型；模型以实心呈现。

---

## 一、总体设计

- 拖拽时实例化的模型**就是**最终落地模型（一次实例化，落地即升级）。
- 模型由主相机直接渲染，不引入独立预览相机或离屏渲染；旧的 RenderTexture 预览方案整体下线（见 §八）。
- 模型**保持 prefab 自身的 root scale / rotation，不做任何覆盖**。
  注意：单位 prefab 根缩放为 1，建筑不是——`arrow_tower_b_blue` / `barracks_blue` 为 `2.5`，`building_GoldMine` 为 `(1.5, 2, 1.5)`。
  实现中禁止写 `localScale = Vector3.one`，否则建筑会被缩小 2.5 倍。
- 普通卡走本方案；战术卡维持单阶段缩放手感，无 3D 预览。
- 卡牌本身回退为**单阶段**缩放/淡出（原「卡牌阶段 → 模型阶段」的两阶段进度失去意义，见 §六）。

---

## 二、交互流程

| 阶段 | 行为 |
|---|---|
| 拎起 | 解析模型 prefab，实例化一次到世界空间的拖拽根节点，`PrepareForDrag` 停用运行时组件，跟随指针悬浮；卡牌缩小+淡出。 |
| 拖动 | **每帧**（不只在指针移动时）射线命中地形，实例吸附到命中点上方固定高度；姿态保持 prefab 原值。 |
| 松手·有效 | `RestoreForDeployment` → **同步**复用实例完成单位/建筑接线（扣费、腾槽、补牌）→ 从释放位置向最终位置播放纯视觉落位补间。不销毁实例。 |
| 松手·取消/无效 | 销毁实例；卡牌复位。 |

落点判定、部署资格、高亮复用统一规则（见 §4.3 关于屏幕坐标的强制约定）。

---

## 三、模块划分

| 文件 | 职责 |
|---|---|
| `Assets\Scripts\UI\CardDragPreview\CardDragWorldPreviewController.cs` | **新增**。持握控制器：逐帧跟随、落位补间、取消销毁 |
| `Assets\Scripts\UI\CardDragPreview\CardDragPreviewUtils.cs` | 改造：`StripToVisual` → `PrepareForDrag` / `RestoreForDeployment`（成对可逆） |
| `Assets\Scripts\UI\CardDragPreview\CardDragPreviewController.cs` | **删除**（RT / 预览相机 / RawImage 旧方案） |
| `Assets\Scripts\Core\Services\CardPresenter.cs` | 编排：实例化、准备、复用升级、扣费腾槽 |
| `Assets\Scripts\UI\CardController.cs` | 两阶段进度回退为单阶段；转发新的逐帧回调 |
| `Assets\Scripts\Core\Interfaces\ICardView.cs` | `ICardDragVisualHandler.OnCardDragUpdate` 签名精简 |
| `Assets\Scripts\Infrastructure\Installers\GameInstaller.cs` | 替换控制器绑定（去掉 `WithArguments(_targetUICanvas)`） |
| `Assets\Scripts\Core\Services\FeelConfigProvider.cs` | 新增 3 个属性；移除 7 个不再使用的属性 |
| 配置链（Excel / schema / Data / Importer / csv / json / asset） | 表现数值唯一主源，详见 §七 |

---

## 四、实现要点

### 4.1 提交时序：同步提交，异步只做视觉

**这是与初版计划最大的差异，必须严格遵守。**

`ICardDropHandler.HandleCardDragEnd` 返回 `bool`，`CardController.OnEndDrag`（`CardController.cs:442-454`）靠它二选一：

- `true` → 只 `EndDragVisual()` 后 return；
- `false` → `OnCardDragCancel(this)` + `ResetToOrigin()`。

因此**不能**把生成推迟到落位动画的回调里：回调触发时 `OnEndDrag` 早已返回，「失败则复位卡牌」在物理上无法实现。

正确时序（全部在 `HandleCardDragEnd` 内同步完成）：

1. 暂停校验、`IsReleaseValid` 校验 → 失败 `return false`（实例仍由控制器持有，走取消路径销毁）。
2. `RestoreForDeployment(instance)`。
3. **先让控制器释放实例所有权**（`ReleaseOwnership`），再调 `TrySpawnCard(config, targetCell, instance)`。
   顺序不可颠倒：`ConsumePlayedCard` 会 `SetActive(false)` 卡牌并触发 `CardController.OnDisable` → `EndDragVisual`，若此时控制器仍持有实例，任何走到 `Cancel` 的分支都会把刚落地的真实单位销毁。
4. 失败（`false` 或异常且 `IsDeploymentCommitted` 为 false）→ 销毁实例、`return false`，卡牌由 `OnEndDrag` 复位。
5. 成功 → `ConsumePlayedCard(view)` → 启动落位补间 → `return true`。

落位补间是**纯视觉**的：`TrySpawnCard` 内部已把对象放到最终位置（单位还会被 `TryClaimStandingUnit` 二次吸附到站位槽），因此补间的做法是：
记录 `endPos = instance.transform.position`，把 `instance.transform.position` 拨回松手瞬间的悬停位置，再补间回 `endPos`。
逻辑状态从第 0 帧起就是「已落地」，只有画面在飞。

**单位补间期的冲突**：单位落地即 `AddComponent<PlayerUnitBrain>()` 并 `_gameLoop.Register(brain)`，下一帧就可能决策并由 `UnitMovementController` 改写 `transform.position`，与补间打架。
补间期间置 `brain.IsPaused = true`，补间结束恢复为 `_gameLoop.IsPaused`（不能无条件写 `false`，否则游戏处于暂停时会把单位解冻）。建筑无 brain，不需要处理。

### 4.2 模型实例复用

`SpawnUnit` / `SpawnBuilding` 各增加可选参数：

```csharp
private GameObject SpawnUnit(int unitID, Vector3 position, GameObject instance = null);
private bool SpawnBuilding(int buildingID, Vector3 position, GameObject instance = null);
```

- `instance` 非空时跳过 `Object.Instantiate`，直接对传入对象做后续接线：父节点、tag、控制器、注入、血条、领域提交、注册、音效。
- `instance` 为空时行为不变。
  **仅 `ExplorationRewardSystem` 走这条路**（经 `IPlayerUnitSpawnService` / `IPlayerBuildingSpawnService`）；AI 用 `AIEntityFactory.GenerateUnit/GenerateBuilding`，兵营用 `BarracksSpawner.SpawnPlayerUnit(Vector3)`，两者都不经过这两个方法。
- `TrySpawnCard` 透传该实例。

三处与现有代码的冲突必须处理：

| 冲突 | 现状 | 处理 |
|---|---|---|
| 双重注入 | 控制器用 `_container.InstantiatePrefab`（已注入），`SpawnUnit:437` 又调 `_container.InjectGameObject(g)` | 复用路径跳过 `InjectGameObject`；或统一改用 `Object.Instantiate` + 由 `SpawnUnit` 单点注入（推荐后者，改动更小） |
| UI 重复接线 | `SpawnUIWiring.WireUnitCanvas/WireBuildingCanvas` 无条件 `AddComponent<UIController>()` | 复用路径下 `PrepareForDrag` 不得触碰 Canvas 层级；接线仍只执行一次（在 `SpawnUnit` 内），无需改动，但需在验收中确认无重复 `UIController` |
| 预检查读 prefab | `hasUnitUi`（`CardPresenter.cs:415-419`）/ `hasBuildingUi`（`535-539`）均从 prefab 推导 | 判据不变，仍走 prefab；实例路径下 prefab 依然可从 `_unitData`/`_buildingData` 取到 |

### 4.3 持握控制器

```csharp
public class CardDragWorldPreviewController : Zenject.ITickable, System.IDisposable
{
    void Begin(GameObject instance, object token);
    void Follow(Vector2 pointerPosition, object token);   // 传入原始触点，内部换算
    GameObject ReleaseOwnership(object token);            // 交出实例，不销毁
    void PlayLanding(GameObject instance, Vector3 fromPos, System.Action onComplete);
    void Cancel(object token);
    void Dispose();
}
```

- 注入 `IMapRaycastService`、`GameLoop`。构造参数不再需要 `Canvas`。
- **屏幕坐标必须统一**：内部一律用 `CardController.GetCardDragLogicPosition(pointerPosition)`（触点 + `Screen.height * 0.065f`）做射线。
  高亮（`PlayerInputHandler.cs:112`）与落点判定（`CardController.cs:409`）都用这个偏移过的坐标，卡牌视觉才用原始触点。若模型沿用原始触点，模型悬停的格子会和高亮/实际落地的格子**差一格**。
- **射线距离必须显式指定**：`IMapRaycastService.RaycastMap` 默认 `maxDistance = 100f` 不够。
  相机俯角 67°、`maxZoomDistance = 75`、横屏 FOV 60 时，屏幕上缘射线到地面约 125 世界单位；竖屏 FOV 78 时约 160。传 `300f`。
  否则最大缩放下屏幕上半部分射线失效，表现为模型卡住不动。
- **必须每帧驱动**：`Follow` 只在 `CardController.OnDrag`（指针移动）时被调用，而拖拽期间相机仍可用键盘/边缘平移移动（`IsAnyCardDragging` 只挡住了 `CameraController.HandleMouseDrag:263`）。
  控制器实现 `ITickable`，缓存最近一次触点，每帧重算射线并更新位置。
- 命中时 `position = hit.point + Vector3.up * hoverHeight`；未命中保持上一位置。
- `Cancel` / `Dispose`：销毁尚未交出所有权的实例。所有方法按 token 校验，拒绝上一张卡的迟到回调。

### 4.4 拖拽期停用 / 落地恢复

`PrepareForDrag(instance)` —— 沿用现有 `StripToVisual` 的既有结论（那份清单是踩坑踩出来的），但**不改 Layer、不改 Renderer**：

- 全部 `MonoBehaviour`（与旧实现一致；`Awake` 在实例化时已执行，禁用只挡 `Update`/`OnEnable`）
- `Collider`、`Rigidbody`（转运动学）、`NavMeshAgent` / `NavMeshObstacle`
- `AudioSource`、`ParticleSystem` + `ParticleSystemRenderer`、`TrailRenderer`、`Light`
- 世界空间 `Canvas`（血条）、`GraphicRaycaster`、`EventTrigger`
- `Animator` 保留启用：`applyRootMotion = false`（**不设会被根运动带偏悬停位置**）、`cullingMode = AlwaysAnimate`
- `SkinnedMeshRenderer.updateWhenOffscreen = true`

`RestoreForDeployment(instance)`：按 `PrepareForDrag` 记录的原始状态**逐一恢复**（不是无条件置 true——prefab 上本就禁用的组件必须保持禁用）。

### 4.5 CardPresenter 编排

- `OnCardDragBegin`：解析 prefab → 实例化到拖拽根节点 → `PrepareForDrag` → `Begin`。
- `OnCardDragUpdate`：`Follow`。
- `HandleCardDragEnd`：按 §4.1 时序同步完成，返回真实成败。
- `OnCardDragCancel`：`Cancel`（销毁未交出的实例）。
- `OnCardDragEnd`：释放控制器对本次 token 的引用，**幂等且不销毁**（成功路径下实例所有权已在 `HandleCardDragEnd` 内交出）。

`ICardDragVisualHandler.OnCardDragUpdate` 精简为：

```csharp
void OnCardDragUpdate(ICardView view, Vector2 pointerPosition);
```

`upwardDistance` / `cardProgress` / `modelProgress` 三个参数随两阶段方案一起移除。该接口只有 `CardPresenter` 实现（战术卡不实现），改动面可控。

### 4.6 部署顺序不变量

- 确认前先校验部署资格（`IsReleaseValid`，与高亮同一规则）。
- 确认后同步生成/提交实体。
- 成功后先隐藏卡牌，再扣费、腾槽、补牌（`ConsumePlayedCard` 内既有顺序不变）。
- 提交失败时销毁实例并返回 `false`，由 `CardController.OnEndDrag` 复位卡牌。

---

## 五、暂停语义

项目**全局不使用 `Time.timeScale`**，暂停靠 `GameLoop.IsPaused` / `GameLoop.GameTime`（暂停时不累加）。裸 DOTween 在暂停期间照常播放。

落位补间必须走项目既有的暂停安全写法：`DOTween.To(...).SetLoops(-1).OnUpdate(...)`，进度由 `_gameLoop.GameTime` 的增量驱动（参照 `CardPresenter.InsertCardAtFrontWithFly`）。

控制器 `ITickable` 的逐帧跟随**不受暂停影响**（与摄像机、卡牌拖拽 UI 交互一致，见 `GameLoop` 顶部注释）。

---

## 六、卡牌手感回退为单阶段

模型在拎起瞬间即出现，不再有「卡牌阶段 → 模型阶段」的交接，因此 `CardController.OnDragUpdate` 中：

- 删除 `d2` / `_modelProgress` 与对 `FeelConfigProvider.CardDragStage2Ratio` 的读取（`CardController.cs:282,285`）。
- 保留 `_cardProgress`（由 `cardDragStage1Ratio` 驱动）用于卡牌缩放与淡出。
- 删除 `CardPresenter.OnCardDragUpdate` 中对 `CardDragModelFadeIn` 的读取（`CardPresenter.cs:352`）。

---

## 七、表现配置

### 7.1 字段增删

新增 3 列：

| 字段 | 含义 | 默认 | 约束 |
|---|---|---|---|
| `cardDragPreviewHoverHeight` | 持握悬停高度（世界单位） | `1.0` | `min: 0` |
| `cardDragPreviewSnapDuration` | 落位补间时长（秒） | `0.2` | `min: 0` |
| `cardDragPreviewAppearDuration` | 拎起 scale-in 时长（秒，0=不做） | `0.08` | `min: 0` |

保留并继续使用：`cardDragStage1Ratio`(0.13)、`cardDragCardMinScale`(0.1)、`cardDragCardFadeStart`(0.6)。

停止使用的 7 个字段：`cardDragStage2Ratio`、`cardDragModelMinScale`、`cardDragModelFadeIn`、`cardDragPreviewRTSize`、`cardDragPreviewWindowSize`、`cardDragPreviewCameraDistance`、`cardDragPreviewPadding`。

**处理方式：只删代码侧的 `FeelConfigProvider` 属性（`FeelConfigProvider.cs:38,41-46`），Excel 列 / schema 列定义 / `FeelConfigData` 字段 / Importer 赋值一律保留并标注 deprecated。**
理由：从 xlsx 删列要做二进制外科手术并重排列序与 `dimension`/`spans`，风险远大于收益；保留的死列对导出与运行时无副作用。
（若先删 Provider 属性而不改 §六 的两处读取点，`CardController.cs:282` 与 `CardPresenter.cs:352` 会编译失败——两者必须同一次改动完成。）

### 7.2 配置链改动清单（顺序不可乱）

`Config/Excel/游戏数值配置.xlsx` 是**唯一主源且为二进制**；exporter 的 `init` 命令会按 schema 重建整个工作簿并丢弃已录入数据，**禁止使用**。`Config/Generated/**` 由工具生成，**禁止手改**。

1. 写一个一次性迁移脚本 `Tools/ConfigExporter/scripts/add_feel_dragpreview_columns.py`，**照抄 `add_feel_carddrag_columns.py` 的做法**：只文本级修改 `xl/sharedStrings.xml`（追加 6 条字符串 = 3 列名 + 3 说明）与 `xl/worksheets/sheetNN.xml`（表头行/说明行/数据行各追加 3 个单元格 + 同步 `spans` / `dimension` / `sst@count`/`uniqueCount`），其余条目按原字节复制。必须保持幂等、自动 `.bak`、检测到半量列时拒绝写入。
2. `Config/Schema/配置表结构.json`：在「表现配置」sheet 的列数组（第 561-592 行附近）末尾追加 3 个列定义，**列顺序必须与脚本中的 `NEW_COLUMNS` 一致**；同时补 seed 段的默认值。
3. `Assets/GameConfig/Runtime/Data/FeelConfigData.cs`：追加 3 个 `float` 字段。
4. `Assets/GameConfig/Editor/Import/GameConfigImporter.cs::BuildFeelConfig`（第 814-823 行附近）：追加 3 行 `GetFloat(v, col, "...", 默认值)`。
5. 运行迁移脚本，再重跑导出：
   ```
   dotnet run --project Tools/ConfigExporter/src/ConfigExporter -- \
       --input Config/Excel/游戏数值配置.xlsx \
       --schema Config/Schema/配置表结构.json \
       --output Config/Generated
   ```
   产出 `Config/Generated/Csv/表现配置.csv` 与 `Config/Generated/game-config.json`。
6. Unity 内触发 `GameConfigImporter`，刷新 `Assets/GameConfig/Generated/FeelConfigDatabase.asset`。
7. `Assets/Scripts/Core/Services/FeelConfigProvider.cs`：新增 3 个属性、删除 7 个 deprecated 属性。

---

## 八、旧方案下线

| 对象 | 处理 |
|---|---|
| `Assets\Scripts\UI\CardDragPreview\CardDragPreviewController.cs` | 删除（385 行：预览相机、RenderTexture、PreviewLight、RawImage、跨拖拽实例复用） |
| `GameInstaller.cs:355-357` 的绑定 | 替换为 `Container.BindInterfacesAndSelfTo<CardDragWorldPreviewController>().AsSingle();`，**去掉 `.WithArguments(_targetUICanvas)`** |
| `_targetUICanvas` 字段本身 | **保留**：仍被 `Container.Bind<Canvas>().WithId("TargetUICanvas")`（`GameInstaller.cs:337-340`）与 `AddMissing`（`:401`）使用，且 `GameInstallerTests` 断言其缺失时报错 |
| `CardPresenter.cs:30` 的 `[Inject(Optional = true)] CardDragPreviewController` | 改为注入新控制器，`Optional` 语义保留（缺失时静默降级为只缩卡） |
| `ProjectSettings/TagManager.asset` 的 User Layer 9 `CardPreview` | **保留占位不删**，仅确认代码中不再有任何引用（`LayerMask.NameToLayer("CardPreview")` / `PreviewLayerName`） |
| `CardDragPreviewUtils.PreviewLayerName` / `SetLayerRecursively` / `TryGetRenderBounds` / `ForceEvaluatePose` | 随 RT 方案一起删除（新方案不改 Layer、不需要取景包围盒与强制求值） |

---

## 九、边界与约束

- 实心模型，不做半透明、染色、材质克隆。
- 实例使用 prefab 自带 Layer（Default），不新增 Layer、不改主相机 cullingMask。
- 不做旋转 / bobbing，固定高度。
- 不做跨拖拽实例复用（旧 RT 控制器有此优化，新方案明确放弃）。
- 落位终点为 `TrySpawnCard` 落定后的实际位置：单位可能被 `TryClaimStandingUnit` 二次吸附到站位槽，不一定是 `RealCenterWorldCoordinate`。补间终点必须**在生成之后读取**，不能预先假定为格心。
- **`SpawnUnit` 用 `WorldToHexCoordinate(g.transform.position)` 反推格子**（`CardPresenter.cs:470`），因此调用它之前**必须已经清掉 hoverHeight**（把实例放到目标格心的地面高度），否则反推的格子可能不是目标格。
- 拖拽期的实例挂在独立拖拽根节点下，不在 `PlayerUnit` / `PlayerBuilding` 之下：
  - 场景雾（`m_Fog: 0`）是关的，实际生效的是自研后处理 `FogEnvironmentSelectiveEffect`，其 `fogRoots = {PlayerBuilding, EnemyBuilding}`、`eraseRoots = {PlayerUnit, EnemyUnit}`（`MapPresentationBootstrap.cs:444-446`）；
  - 因此拖拽期实例**完全不受雾化影响**；落地 `SetParent` 后由 `_mapVisualEvent.Raise()` → `RefreshRenderers()` 纳入对应组；
  - 部署只允许在己方势力范围内（已探索），故落地瞬间不会出现"从雾中显形"的跳变。
- 吸附窗口内目标格可能失效：本方案改为同步提交，不存在这个窗口。

---

## 十、验收标准

1. 拖拽普通卡，实心模型出现在指针处、悬浮跟随；建筑与单位的大小关系与落地后完全一致（建筑不被缩小）。
2. 模型悬停所在格 = 高亮格 = 实际落地格（三者始终一致，含屏幕上下缘、含最大缩放 75）。
3. 相机在拖拽中平移/缩放（键盘、边缘）时，模型跟着地形走，不停滞。
4. 松手到有效格：同一实例落到目标格，落地即成为真实单位/建筑；场景中只存在一个该模型对象，且只有一个 `UIController` / 一套血条。
5. 松手到无效格 / 拖出地图 / 取消 / 失焦 / 卡牌被 `SetActive(false)`：实例销毁，卡牌复位，无残留。
6. 扣费、腾槽、补牌正确；生成失败（缺 prefab / 缺父节点 / 缺血条 UI）时卡牌正确复位且未扣费。
7. 探索奖励入口（`ExplorationRewardSystem`）仍正常生成单位/建筑；AI（`AIEntityFactory`）与兵营（`BarracksSpawner`）不受影响。
8. **拖拽中途**点暂停、以及落位补间过程中点暂停：模型冻结，恢复后继续；补间结束后单位的 `IsPaused` 与 `GameLoop.IsPaused` 一致（暂停中落地的单位不会被解冻）。
9. 落位补间期间单位不会被自身 brain 拽走（位置只由补间驱动）。
10. 反复拖拽 20 次以上：无 GameObject / tween / 材质泄漏；场景切换后未落位实例被销毁。
11. 代码中不再有对 `CardPreview` 层、`RenderTexture`、预览相机的任何引用；控制台无相关报错。

---

## 附：本版相对初版的修订

- **提交时序**：初版的 `SnapTo(cellCenter, onLanded)` + 回调内生成与 `HandleCardDragEnd` 的同步 `bool` 契约冲突，「失败复位卡牌」不可实现 → 改为同步提交 + 纯视觉补间（§4.1）。
- **配置**：初版 §五 隐式删除了 7 个字段，其中 `cardDragStage2Ratio` 仍被 `CardController.cs:282` 读取，会编译失败 → 明确保留策略与同步改动点（§6、§7.1）。
- **模块表**：补入 `CardController.cs`、`ICardView.cs`、旧控制器删除项（§三）。
- **旧方案去留**：初版未交代 → 新增 §八。
- **屏幕坐标**：初版 `Follow(screenPos)` 未指明用哪个坐标，会导致模型与高亮差一格 → 强制 `GetCardDragLogicPosition`（§4.3）。
- **射线距离**：默认 `maxDistance = 100f` 在最大缩放下不够 → 显式 300f（§4.3）。
- **逐帧驱动**：初版依赖 `OnDrag` 触发，相机动而指针不动时模型停滞 → 改为 `ITickable`（§4.3）。
- **组件清单**：补齐 `TrailRenderer` / `Light` / `GraphicRaycaster` / `EventTrigger` / `ParticleSystemRenderer` / `applyRootMotion` / `updateWhenOffscreen`；「不碰 MonoBehaviour」改回禁用（§4.4）。
- **实例复用冲突**：双重注入、UI 重复接线、prefab 预检查语义（§4.2）。
- **暂停机制**：初版只说「暂停冻结」无方案；项目不用 `Time.timeScale` → 明确 `GameTime` 驱动写法（§五）。
- **事实更正**：scale 并非恒为 1（建筑 2.5 / 1.5）；场景雾是关的，实际是自研后处理；AI 与兵营不走 `SpawnUnit/SpawnBuilding`；「暂停中拖拽/松手」进不去，改为「拖拽中途暂停」。
