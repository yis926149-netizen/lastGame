# 卡牌拖拽增益映射 · 效果文档

> 版本：v1
> 范围：**只描述玩家能看到、能感觉到的东西**。不含实现方案、不含改动清单、不含参数落库方式。
> 参考对象：《英雄联盟手游》技能 / 插眼的摇杆式指向。

---

## 一、要解决的问题

在竖屏触屏上从手牌往地图上拖卡时，**手指和手掌盖住了目标地块**。玩家看不到自己正要放的那一格。

这是本文档要解决的全部问题。

---

## 二、设计前提

直接把落点从触点移开，就能让目标露出来。但这么做会立刻引出真正难的那一半：

**落点和手指一旦分开，玩家凭什么知道那个落点是自己在控制？**

如果分离量是恒定的——拎起卡牌的瞬间落点就已经在一段距离之外——那么玩家没有做任何动作，位移却凭空发生了。这段距离与手上的动作无关，也就无从建立因果。看到的会是两个各自独立的东西：手里的卡，和上面那个不知道为什么在那儿的落点。

所以本设计的立足点是：

> **分离必须由玩家自己的动作推出来，而且推的过程要看得见。**

分离量不是一个设定值，是玩家手指行程的函数。玩家不是被告知这个关系的，是在一次拖拽里做出来、从而知道的。

---

## 三、目标效果（一句话）

**手指在屏幕下方一小段范围内移动，落点在整个屏幕上移动。手指移得少，落点移得多。**

拎起卡牌的瞬间，落点就在卡牌底下，分离量为零。随着手指往上抬，落点比手指跑得更快，逐渐拉开。手指抬到某个高度就不再往上了——卡牌也停在那儿——而此时落点已经能够到屏幕顶端。

---

## 四、玩家视角的完整过程

### 4.1 起手

按住卡牌、开始拖动的第一刻，**卡牌就在手指下，落点就在卡牌下**。三者重合，没有跳变，没有凭空位移。

玩家在这一刻不需要理解任何规则。他做的就是最朴素的那件事：把卡拖到想放的地方。

### 4.2 上抬

手指开始往上走。落点走得比手指快，并且**越往上越快**。

玩家看到的是：我的手在动，上面那个目标也在动，方向一致、同时开始、同时停止，只是它跑得比我快。这是一种可以在一次拖拽内学会的关系——不需要教程，不需要提示，做一遍就懂了。

**这就是关联的全部来源。** 参考对象里手指和指示器之间没有任何连线，因为映射的连续性本身已经建立了关联。玩家不是靠一根线知道"那个是我控制的"，是靠它跟着自己的手同步动知道的。

### 4.3 封顶

手指抬到某个高度之后，**卡牌不再继续上升，落点也不再继续前进**。

此时手指大约停在屏幕下半部（不进入地图的主要区域），而落点已经可以到达屏幕顶端。继续往上推没有任何效果——就像摇杆推到了外圈。

> 这是本方案里**唯一一条必须严格保证的规则**：卡牌视觉和落点必须同时停住。
> 如果手指越过封顶后卡牌还在往上走而落点已经不动了，就会在行程顶端制造出一段脱节，
> 而且比任何固定分离都更刺眼。玩家可以接受"推到头了"，不能接受"我还在动它却不动了"。

### 4.4 横向

**横向不做增益，或只做很弱的增益。** 手指左右移动多少，落点就左右移动多少（或略多一点）。

原因：遮挡问题本质上是纵向的——手和手指从屏幕下方伸进来，挡住的是触点及其下方区域。横向放大只会损失精度，换不来任何遮挡收益。这是相对参考对象可以做的简化：技能射程是个圆，必须二维放大；这里不是。

### 4.5 松手

落点在哪就放在哪。

---

## 五、手感基线（供真机调参时对照）

以 1080×1920 为参照。这些数不是最终值，是"合理区间"的锚点。

| 项 | 参考值 | 说明 |
|---|---|---|
| 手指纵向行程上限 | 屏幕高度的 25%~30% | 手指全程停留在屏幕下半部 |
| 落点纵向可达范围 | 从手牌上方一直到屏幕顶端 | 整屏可达是硬要求 |
| 起手增益 | 1× | 拎起瞬间零分离，不可协商 |
| 峰值增益 | 5× 上下 | 行程上限取大一点，峰值就能压低一点 |
| 增益增长方式 | 平滑爬升，高段压平 | 不要线性到底，见 §6.1 |

**行程上限与峰值增益是此消彼长的。** 行程给得越短，遮挡解决得越彻底，但峰值增益越高、越难瞄准。这两者之间的取舍是本方案唯一需要在真机上反复试的东西，其余都是从它派生的。

---

## 六、已知的手感风险

### 6.1 屏幕顶部精度最差（必须正视）

相机是俯视带透视的，**屏幕越靠上，六边形在屏幕上占的像素越少**。而屏幕顶部又恰好是增益最高的位置。精度损失在这里是叠加的：格子最小 × 手指最灵敏。

处理方式：

- 增益曲线在高段压平，不要一路线性冲到峰值；
- **接受屏幕最顶端一小段（约 10%~15%）无法精确点选**。那一段的格子本来就远、本来就小，本方案不承诺在那里做到精确。

### 6.2 格边界上的反复跳格

高增益下手指的微小抖动会被放大。但因为落点最终是**量化到六边形**的，表现不会是"预览在抖"，而是"在两格之间反复横跳"。

处理方式：跨格时留一点滞回——落点要越过格边界一小段之后才真正换格。**离散输出在这里是帮忙的**，同一套映射如果输出是连续的会难用得多。

### 6.3 编辑器里试会比真机难用

用鼠标试这套映射的体感明显差于用手指：鼠标没有遮挡问题，光标和落点分家却看不出理由，纯粹是负收益。

**这是最容易导致误判的一点**——很可能在编辑器里觉得"这方案不行"，而它在真机上是成立的。凡是评价这套手感的结论，都必须来自真机。

---

## 七、明确不追求的效果

1. **不追求落点和触点重合。** 那等于回到遮挡问题本身。本方案是把两者的分离变成可理解、可控制的，不是把它消除。
2. **不追求分离量小。** 最终的分离量会相当大。可接受性来自"是玩家自己推出来的"，不来自"距离短"。
3. **不做连线。** 映射的连续性已经承担了关联的功能，连线在分离量可变且可能很长的情况下会变得又长又吵。
4. **不做手指行程的视觉提示**（摇杆圈、行程条等）。参考对象里那个圈是因为技能按钮位置固定、需要标出可操作区；这里起点跟着卡牌走，画圈只会增加噪声。第一版不做，真机上确认需要再说。
5. **不做相机在拖拽期间的自动位移**来腾出视野。那是另一条路线，会带来眩晕感，不在本方案内。
6. **不改变放置规则。** 哪些格能放、哪些不能，本方案不涉及。
7. **不改变松手之后的任何表现。** 落位、失败回弹等均不在本方案范围内。

---

## 八、验收（全部为玩家可观察项）

**核心**

1. 拎起卡牌的瞬间，卡牌在手指下、落点在卡牌下，三者重合，无凭空位移。
2. 手指上抬时落点同步上移，且移动量明显大于手指移动量。
3. 手指停止时落点立即停止，二者无延迟、无惯性、无回弹。
4. 手指行程到顶后，**卡牌与落点在同一帧一起停住**，继续上推无任何变化。
5. 手指行程到顶时，落点能够到达屏幕最顶端可见的地块。
6. 全程中手指不进入屏幕上半部，即可完成屏幕内任意位置的放置。
7. 松手落点与松手瞬间高亮的那一格严格一致。

**精度**

8. 在屏幕中部选中指定的某一格，不需要反复调整。
9. 手指静止时落点不跳格。
10. 在格边界附近缓慢移动时，换格是干脆的一次跳变，不反复横跳。

**完整性**

11. 卡牌自身的拖拽表现（缩小、淡出等）在手指行程到顶时恰好完成，不会因为行程被封顶而永远走不完。
12. 手指往下拖（低于起手位置）时行为明确且不诡异。
13. 不同分辨率 / 宽高比下，行程上限与可达范围按比例正确。
14. 所有卡牌类型表现一致。
15. 取消拖拽、暂停、切后台后再回来，均无残留、无状态错乱。

**学习成本**

16. 第一次接触的玩家，在一次拖拽之内能建立"手移得少、落点移得多"的认知，不需要任何文字或图形提示。

---

# 技术实现文档 · 第一版

> 以上为效果文档，以下为实现。
> **第一版只做一件事：把「触点 → 落点」的映射从恒等改成线性增益。** 其余一概不做。

---

## 九、第一版范围

### 9.1 做

- 落点屏幕坐标 = 以拖拽起点为原点，对纵向位移施加一个固定倍率。
- 起点在 `OnBeginDrag` 采样，在拖拽结束时清除。

### 9.2 不做（本版明确推迟）

| 效果文档条款 | 第一版状态 | 原因 |
|---|---|---|
| §4.3 手指行程封顶 | **不做** | 不规定触点移动范围 |
| §5 增益曲线（起手 1×、高段压平） | **不做**，全程恒定倍率 | 映射用最简单实现 |
| §6.2 跨格滞回 | **不做** | 属于映射体验优化 |
| §7.4 之外的任何指示 UI | **不做** | 本版不加任何 UI |
| 参数进 Excel 配置链路 | **不做**，用 `const` | 见 §12 |

推迟封顶带来一个直接后果：**§4.1「起手三者重合」在第一版不成立**。倍率是恒定的，所以手指一动，落点立刻以 N 倍速度分离。这是第一版与效果文档最大的偏差，见 §13。

---

## 十、唯一的接缝

全项目只有一个函数把触点换算成用于地图射线的屏幕坐标：

```csharp
// Assets/Scripts/UI/CardController.cs:60
public static Vector2 GetCardDragLogicPosition(Vector2 pointerPosition)
```

三个调用方全部走它，**一个都不用改**：

| 调用方 | 位置 | 用途 |
|---|---|---|
| `CardDragWorldPreviewController.TrySnapToTerrain` | `CardDragWorldPreviewController.cs:241` | 世界空间模型预览每帧吸附 |
| `PlayerInputHandler.HighlightGridOnMouseHover` | `PlayerInputHandler.cs:125` | 可放置格高亮 |
| `CardController.OnEndDrag` | `CardController.cs:428` | 松手落点判定 |

所以第一版的改动**集中在 `CardController.cs` 一个文件内**。

唯一的结构性变化是：这个函数从「触点的纯函数」变成了「需要拖拽起点的有状态函数」。起点必须存成静态字段，因为另外两个调用方都拿不到卡牌实例——`PlayerInputHandler` 是轮询式的（用 `IInputService.MousePosition`，不走 EventSystem 回调），世界空间预览控制器只持有一个 token。这和已有的 `_activeDraggingCard`（`CardController.cs:88`）是同一种做法，不引入新范式。

---

## 十一、改动点（`Assets/Scripts/UI/CardController.cs`）

### 11.1 删除固定偏移常量

删掉 `CardController.cs:46-49` 的 `CardDragLogicOffsetRatio`。**是替换不是叠加**——两者同时存在会得到「固定偏移 + 增益」的复合行为，起手就有凭空位移，正是要去掉的东西。

### 11.2 新增增益常量与起点状态

```csharp
/// <summary>纵向增益倍率：落点纵向位移 = 触点纵向位移 × 本值。
/// 1 = 恒等（落点跟手，即无遮挡缓解）。第一版恒定，不随行程变化。</summary>
private const float CardDragLogicGainY = 3.0f;

/// <summary>本次拖拽的增益映射起点（屏幕像素）。OnBeginDrag 采样，ReleaseDragCapture 清除。
/// 必须是静态的：PlayerInputHandler（轮询式）与 CardDragWorldPreviewController（只持 token）
/// 都拿不到卡牌实例，却都要调 GetCardDragLogicPosition。</summary>
private static Vector2 _dragOriginScreenPoint;
private static bool _hasDragOrigin;
```

横向不设增益常量。第一版 X 直通（等价于 1×），符合效果文档 §4.4；留常量只会诱使人去调它。

### 11.3 重写映射函数

```csharp
/// <summary>返回用于地图射线的逻辑位置（屏幕像素坐标）。
/// 以拖拽起点为原点，纵向位移放大 CardDragLogicGainY 倍；横向直通。
/// 起点未就绪时退化为恒等映射（见 §12.3）。</summary>
public static Vector2 GetCardDragLogicPosition(Vector2 pointerPosition)
{
    if (!_hasDragOrigin) return pointerPosition;

    float dy = pointerPosition.y - _dragOriginScreenPoint.y;
    return new Vector2(pointerPosition.x, _dragOriginScreenPoint.y + dy * CardDragLogicGainY);
}
```

对称放大（`dy < 0` 同样放大）。这是"最简单实现"的直接结果，也是效果文档验收 §8.12「往下拖行为明确」的一种合格答案：往下拖时落点以 N 倍速度下沉、很快出屏、射线不命中、无高亮、松手按失败回弹处理——行为单调且可预测。手牌下方本来就被手牌 UI 遮住、没有可放置格，所以实际上不损失可达范围。

> **⚠ 临时改动（偏离本文档 §4.4 与 §11.3，由需求方临时追加）**：横向也改为与纵向**完全相同的映射**——落点位移 = (触点 − 起点) × 倍率，纵向、横向同倍率，横向不再直通。
> 对应代码：常量由 `CardDragLogicGainY` 更名为 `CardDragLogicGain`（同时作用于纵向与横向），`GetCardDragLogicPosition` 改为
> `_dragOriginScreenPoint + (pointerPosition - _dragOriginScreenPoint) * CardDragLogicGain`。
> 这是为真机对照临时加的旋钮，原文档约定横向直通（1×）；待真机验证后决定保留或回退到直通。

### 11.4 `OnBeginDrag` 采样起点

位置很关键：必须在**早退守卫之后**、**`_dropHandler?.OnCardDragBegin(this)`（`CardController.cs:380`）之前**。

```csharp
public void OnBeginDrag(PointerEventData eventData)
{
    if (IsNextCard || !_isAffordable || (_gameLoop != null && _gameLoop.IsPaused))
    {
        eventData.pointerDrag = null;
        return;
    }

    // 增益映射起点。必须早于 OnCardDragBegin：世界空间预览的 Begin 内部会立即
    // Follow + TrySnapToTerrain 做一次初始吸附（CardDragWorldPreviewController.cs:91-93），
    // 那一刻就要读到起点。
    _dragOriginScreenPoint = eventData.position;
    _hasDragOrigin = true;

    _playerInputHandler.Value.ForceDeselectUnit();
    _isDragging = true;
    _activeDraggingCard = this;
    // ...以下不变
}
```

取 `eventData.position` 而不是 `eventData.pressPosition`。EventSystem 要越过拖拽阈值（默认 10px）才触发 `OnBeginDrag`，此刻手指已经离开按下点。取按下点会让落点在拖拽开始的那一帧瞬移 `10 × 增益` 像素；取当前点则起点与手指重合，无跳变——代价是最初那 10px 行程被吞掉，不可见。

### 11.5 `ReleaseDragCapture` 清除起点

```csharp
private void ReleaseDragCapture()
{
    if (_activeDraggingCard == this)
    {
        _activeDraggingCard = null;
        _hasDragOrigin = false;
    }
}
```

放在 `if` 内部：否则一张已经失效的卡（迟到的 `OnDisable`）会清掉当前活动卡的起点。

`ReleaseDragCapture` 被 `OnEndDrag`（:416）、`OnDisable`（:500）、`CancelDrag`（:513）三处调用，覆盖全部结束路径，是唯一需要挂清理的点。

### 11.6 `OnEndDrag` 必须调整顺序（**本版最容易漏的一处**）

当前实现的执行顺序是：

```
_isDragging = false;
ReleaseDragCapture();                                        // :416
ClearHighlights();                                           // :418
... 暂停分支 ...
Vector2 dragLogicPosition = GetCardDragLogicPosition(...);   // :428
```

第一版之前这没问题，因为映射是纯函数。加入起点之后，**`:416` 已经把起点清掉了，`:428` 会退化为恒等映射**——落点回到手指处，与松手瞬间的高亮格不一致。这直接违反效果文档验收 §8.7，而且症状是"看着放在 A 格，实际放在 B 格"，非常难从表象倒推。

改法：把逻辑位置的计算提到 `ReleaseDragCapture` 之前，删掉原 `:428` 那一行。

```csharp
public void OnEndDrag(PointerEventData eventData)
{
    if (IsNextCard || !_isDragging) return;

    // 必须在 ReleaseDragCapture 之前算：后者清除增益映射起点，
    // 之后再调 GetCardDragLogicPosition 会退化为恒等映射，
    // 落点将落回手指处、与松手瞬间的高亮格不一致。
    Vector2 dragLogicPosition = GetCardDragLogicPosition(eventData.position);

    transform.SetSiblingIndex(originalSiblingIndex);
    _isDragging = false;
    ReleaseDragCapture();
    // ...以下不变，删除原 :428 的重复计算
}
```

---

## 十二、不需要改动的文件，以及为什么

### 12.1 `PlayerInputHandler.cs` — 零改动

它在 `:125` 调 `GetCardDragLogicPosition(_input.MousePosition)`，映射换了它自动跟着换。射程常量、高亮缓存、清理路径全部无关。

### 12.2 `CardDragWorldPreviewController.cs` — 零改动

同上，`:241` 调同一个函数。

但有一处**表现变化需要真机确认**：`Begin` 在 `:91-93` 会用当前指针立即吸附一次，此时 `dy ≈ 0`，射线打在手指处（屏幕底部、手牌 UI 覆盖的区域）。此前是打在手指上方 192px 处。如果这条射线不命中地图，`_lastHoverPosition` 会保持初始的隐藏位 `(0, -500, 0)`，模型在手指抬起来之前不可见。

这不是错误——手指抬起后即恢复正常，且效果文档 §4.1 本来就要求"落点在卡牌下"。只是第一帧的模型位置由此改变，需要在真机上看一眼是"模型出现在手指处"还是"模型延迟出现"。两种都可接受，但要知道是哪种。

### 12.3 `OnDragUpdate` / 卡牌缩放淡出 — 零改动

因为第一版不做行程封顶。卡牌视觉仍然完整跟随手指，`_upwardDistance` 与 `_cardProgress`（`CardController.cs:289`、`:306`）的分母 `_canvasHeight × CardDragStage1Ratio` 仍然对应真实的手指行程，缩放淡出照常走完。

**这条依赖必须记住**：一旦后续版本加入行程封顶（效果文档 §4.3），手指行程被压缩，这个分母就不再匹配，卡牌会永远走不完缩小与淡出。届时必须同步重标定。

### 12.4 起点未就绪的窗口 — 不需要处理，但要知道它存在

`PlayerInputHandler` 在 `GetMouseButtonDown` 那一帧（`:92`）就置 `_isDraggingCard = true` 并开始每帧高亮；而 `CardController.OnBeginDrag` 要等 EventSystem 的拖拽阈值。中间若干帧 `_hasDragOrigin` 为 false。

映射此时退化为恒等，即高亮跟着手指走——而这几帧手指本来就还没移动多少，恒等就是正确答案。**不需要额外处理**，但 §11.3 那个 `if (!_hasDragOrigin)` 分支不能省，省掉会读到上一次拖拽的残留起点。

---

## 十三、参数

第一版用 `const`，不进 Excel。

理由：Excel 那条链路（结构 JSON → 注入脚本 → 数据类 → 导入器 → Provider → 重跑导出）改一个数要动五处，而设备上无论走哪条路都得重新出包才能看到效果——`const` 没有额外成本。等倍率在真机上定下来、且确认需要按机型/关卡区分时再迁移。

倍率取值（起点在手牌行、约屏幕高度 13% 处，1080×1920）：

| 倍率 | 落点到屏幕顶端所需手指行程 | 手指最终高度 |
|---|---|---|
| 2.5× | 660px | 屏幕 47% |
| **3.0×** | 550px | 屏幕 42% |
| 4.0× | 412px | 屏幕 34% |

**当前临时取 2.0×**（原建议初值 3.0×）。第一版没有封顶也没有曲线，倍率是唯一的旋钮，宁可保守：倍率过高在没有封顶的情况下会让屏幕上半部完全不可达（手指还没抬到那儿，落点已经出屏了）。

---

## 十四、第一版可验的验收项

效果文档 §8 的 16 条里，第一版只对下列各条负责：

| 效果文档条款 | 第一版 | 说明 |
|---|---|---|
| 8.2 落点移动量明显大于手指 | ✅ 必须成立 | 核心 |
| 8.3 手指停止时落点立即停止，无延迟惯性 | ✅ 必须成立 | 纯函数映射，天然满足 |
| 8.5 落点可达屏幕顶端 | ✅ 必须成立 | 靠倍率保证 |
| 8.6 手指不进入屏幕上半部即可完成放置 | ⚠️ 部分 | 无封顶，靠倍率争取，不保证 |
| 8.7 松手落点 = 松手瞬间高亮格 | ✅ **必须成立** | 见 §11.6，本版最大陷阱 |
| 8.9 手指静止时落点不跳格 | ✅ 必须成立 | |
| 8.11 缩放淡出正常走完 | ✅ 必须成立 | 见 §12.3 |
| 8.12 往下拖行为明确 | ✅ 必须成立 | 对称放大，见 §11.3 |
| 8.13 不同分辨率正确 | ✅ 必须成立 | 倍率无量纲，天然满足 |
| 8.14 所有卡牌类型一致 | ✅ 必须成立 | 普通卡与战术卡走同一函数 |
| 8.15 取消 / 暂停 / 切后台无残留 | ✅ 必须成立 | 见 §11.5 |
| 8.1 起手三者重合 | ❌ **不成立** | 恒定倍率，无 1× 起步 |
| 8.4 行程到顶时卡牌与落点同帧停住 | ❌ 不适用 | 无封顶 |
| 8.8 / 8.10 精度与边界不跳格 | ❌ 不保证 | 无曲线、无滞回 |
| 8.16 一次拖拽内建立认知 | ❓ 待观察 | 本版的主要观察目标，见 §15 |

补充两条第一版专有的：

17. 连续拖多张卡，第二张的起点正确采样，不残留第一张的起点（快速连拖十次，落点始终与高亮一致）。
18. 按下卡牌但未越过拖拽阈值就松手（点选行为），不产生任何异常高亮或落点。

---

## 十五、第一版要回答的问题

第一版不是为了好用，是为了拿到三个判断：

1. **2.0× 这个量级对不对。** 太小则遮挡没解决，太大则上半屏不可达。这决定后续封顶行程怎么定。
2. **没有 1× 起步，起手的凭空分离到底有多难受。** 效果文档 §2 断言这是"断裂"的根源；第一版恰好是这个断言的对照实验——如果恒定倍率下手感已经可接受，那么增益曲线（§5）就不必做。
3. **量化到六边形之后，跳格到底有多严重。** 决定滞回（§6.2）是必须项还是可选项。

三个问题都只能在真机上回答，见效果文档 §6.3。

---

# UI 修改计划 · UI-1.0

> 以上为增益映射第一版的效果与实现文档。本节记录落点图标语义的重新设计。
> **核心决策：图标跟随世界空间射线命中点（与模型预览一致），而非地块中心；命中地形即显示，不受「是否可放置」约束。连线放弃，槽位留空。**

---

## 十六、UI-1.0 范围

### 16.1 做

- 将落点图标改为跟随**射线命中点**（`hit.point`）而非地块中心。
- 图标在射线命中地形时即显示，不受「是否可放置」约束。
- 放弃连线（`CardDragLinkView`），`_cardDragLinkPrefab` 槽位留空，依赖现有 `_linkDegraded` 降级路径静默关闭。

### 16.2 不做（明确推迟）

| 效果条款 | 状态 | 原因 |
|---|---|---|
| 跟随槽位（单位在格内的实际占位点） | 推迟 | 需要知道当前格的槽位布局，超出本版范围 |
| 图标与模型预览的悬浮高度对齐 | 推迟 | 两者各用 FeelConfig 参数，真机验证后统一调参 |
| 任何其他指示 UI | 推迟 | 不在本版 |

**为什么图标不该绑定地块？** 地块高亮已经承担「指示目标格 / 显示可放置性」的职责，绑定地块会在功能上与它重复。图标的职责是指示逻辑位置在哪，而逻辑位置是世界空间中的连续坐标——单位在格内有槽位，槽位不在格中心；逻辑位置不在可放置格时图标同样应当显示。地块高亮做不到这两点，图标才能。

---

## 十七、不需要改动的文件

| 文件 | 原因 |
|---|---|
| `Assets/Scripts/UI/CardController.cs` | `GetCardDragLogicPosition` 与图标无关；`CardDragRaycastMaxDistance` 供射线服务共用，不变 |
| `Assets/Scripts/UI/CardDragPreview/CardDragWorldPreviewController.cs` | 已独立跟随 `hit.point`；是图标改动的对标参照，零改动 |
| `Assets/Scripts/Infrastructure/Installers/GameInstaller.cs` | DI 绑定不变；`_cardDragLinkPrefab` Inspector 槽位留空即生效降级路径 |
| `Assets/Scripts/UI/CardDragPreview/CardDragLinkView.cs` | 代码完整保留以备将来启用；不赋值 prefab 时控制器 `_linkDegraded = true`，连线整体静默关闭 |

---

## 十八、`CardDragTargetMarkerView.cs` 改动

### 18.1 `ShowAtCell` → `ShowAtPosition`

唯一改动：方法重命名 + 注释更新，内部逻辑（激活根节点、应用高度偏移、启用装饰）不变。

```csharp
// 删除：
/// <summary>显示于指定地块中心上方；worldCellCenter 取 cell.RealCenterWorldCoordinate。</summary>
public void ShowAtCell(Vector3 worldCellCenter)

// 替换为：
/// <summary>显示于指定世界坐标上方（通常为射线命中点 hit.point）。
/// 高度偏移由 FeelConfigProvider.CardDragTargetIconHeight 控制；
/// 与 CardDragWorldPreviewController 使用的 CardDragPreviewHoverHeight 语义对齐：
/// 两者均为"贴地后往上抬多少"，可调为相同值使图标恰好悬浮在模型正上方。</summary>
public void ShowAtPosition(Vector3 worldPos)
{
    gameObject.SetActive(true);
    if (!_iconBound) return;

    transform.position = worldPos
        + Vector3.up * Mathf.Max(0f, FeelConfigProvider.CardDragTargetIconHeight);

    SetDecorationsActive(true);
}
```

---

## 十九、`CardDragTargetMarkerController.cs` 改动

### 19.1 接口：`SetTargetCell` → `SetTarget`

```csharp
// 删除原方法 SetTargetCell(HexCellData cell)，替换为：

/// <summary>
/// 同步本帧落点提示（PlayerInputHandler.HighlightGridOnMouseHover 每帧无条件调用）。
/// terrainHit = false 时隐藏；为 true 时在 hitPoint 上方显示，
/// 不受「是否可放置」约束——与可放置格高亮的触发条件独立。
/// </summary>
public void SetTarget(bool terrainHit, Vector3 hitPoint)
{
    if (_isDisposed || _degraded) return;

    if (!terrainHit)
    {
        Hide();
        return;
    }

    if (Camera.main == null)
    {
        Hide();
        return;
    }

    if (_view == null)
    {
        _view = Object.Instantiate(_iconPrefab);
        _view.name = "CardDragTargetMarker";
    }

    _shown = true;
    _view.ShowAtPosition(hitPoint);

    if (!_linkDegraded)
    {
        if (_linkView == null)
        {
            _linkView = Object.Instantiate(_linkPrefab);
            _linkView.name = "CardDragLink";
            _linkView.SetMarkerAnchor(_view);
        }
        _linkView.Show();
    }
}
```

### 19.2 删除地块坐标判重字段与逻辑

删除字段：
- `private Vector3 _currentCellCoordinate;`
- `private bool _hasCurrentCell;`

删除原 `SetTargetCell` 中的判重分支（含连线补显逻辑）。

**原因**：图标跟随连续世界坐标，每帧都可能移动，按地块坐标判重无意义。逐帧调用 `ShowAtPosition` 的开销与 `CardDragWorldPreviewController.Tick` 逐帧更新模型位置相当，可接受。

### 19.3 `Hide()` 内部简化

```csharp
private void Hide()
{
    _shown = false;
    // 删除原 _hasCurrentCell = false; _currentCellCoordinate = default;

    if (_view != null) _view.Hide();
    if (_linkView != null) _linkView.Hide();
}
```

`Clear()` 公开接口保持不变，依然调 `Hide()`。

---

## 二十、`PlayerInputHandler.HighlightGridOnMouseHover()` 改动

### 20.1 改动位置

`Assets/Scripts/Core/Services/PlayerInputHandler.cs`，`HighlightGridOnMouseHover()` 内对 `_targetMarker` 的调用一处（`PlayerInputHandler.cs:138` 附近）。

### 20.2 改动内容

```csharp
// 当前（图标只在可放置格时显示）：
_targetMarker.SetTargetCell(placeableCell);   // placeableCell == null 即隐藏

// UI-1.0（图标与高亮触发条件解耦）：
// 射线命中地形即显示图标，与 placeableCell 无关
_targetMarker.SetTarget(isMapHit, hit.point);

// 可放置格高亮逻辑不变
_hexHighlightRenderer.SetHighlightedCells(
    placeableCell != null ? new[] { placeableCell } : System.Array.Empty<HexCellData>());
```

其中 `isMapHit` 与 `hit` 已是射线服务的现有局部变量，`placeableCell` 的推算逻辑不变。`ClearCardDragHighlight()`（`PlayerInputHandler.cs:111`）已调用 `_targetMarker.Clear()`，不需要改动。

---

## 二十一、UI-1.0 验收

**核心行为**

1. 拖拽期间落点图标始终出现在射线命中点上方，位置随手指连续移动。
2. 射线命中地形但目标格**不可放置**时，图标仍显示；地块高亮在此情况下不激活。
3. 射线未命中地形时，图标不显示。
4. 同一地块内移动手指，图标随之连续移动，不锁格中心。
5. 拖拽结束 / 取消 / 暂停：图标消失，无残留。

**连线**

6. `_cardDragLinkPrefab` Inspector 槽位未赋值；Editor 日志输出一条 `[CardDragTargetMarker] 未绑定连线 prefab` LogWarning，图标正常工作，无异常、无空引用。

**回归**

7. 地块高亮触发条件与之前一致：仅在可放置格高亮，与图标显隐无关。
8. 松手落点判定不受影响：`OnEndDrag` → `GetCardDragLogicPosition` → `_mapRaycastService.RaycastMap` 路径未改动。
9. 世界空间模型预览行为不变。
10. 叠加相机（`MarkerOverlayCamera`）、Marker 图层、呼吸动画均正常，图标不被地形或单位遮挡。

