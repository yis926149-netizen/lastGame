# 卡牌拖拽放置高亮 · 方案甲（轻量发光描边）

> 版本：v1
> 定位：把既有"纯色六边形高亮"升级为"自发光能量描边 + 半透明填充"，**不抬高围墙、不遮格**。
> 与《卡牌拖拽增益映射-效果文档.md》《卡牌拖拽落点图标与连线计划.md》配套：本文档基于**移除落点图标**的决策——地块高亮成为"哪格 + 能否放置"的唯一载体，因此它的质感值得单独成文。

---

## 一、要解决的问题

上一轮设计确认：**去掉落点图标，把"能否放置 + 哪格"两种语义合并进地块高亮**（可放置金光 / 不可放置红光）。合并之后，高亮不再只是配角，而是玩家拖牌全程唯一盯着的反馈。

但现在的实现是 `Unlit/Color` 的纯色六边形——**没有能量感、没有边缘光、没有流动**，作为唯一载体太"素"，撑不起"这格是我选中的、这格能放"这种语义强度。

本方案要做的全部事情就是：**在一个不遮挡目标格的约束下，给高亮加上自发光能量质感。**

---

## 二、技术前提（两方案共用，都是硬约束）

1. **内建渲染管线**。全仓 shader 走 `CGPROGRAM` / `UnityCG.cginc` / Standard surface（见 [ExplorationPillar.shader](../Assets/Shader/ExplorationPillar.shader)），无 URP / HDRP。做自定义 GLSL-CG shader 完全可行，且仓库已有大量先例。
2. **无 PostProcessing / blom**。工程未安装 PostProcessing 包（仅 Cinemachine 内部 meta），`GraphicsSettings` 无后处理配置。**"辉光"不能靠 bloom 扩散，必须用 additive 混合 + 明暗渐变伪装**。这是所有光效的前提——设计时按"伪辉光"做，别指望后期 bloom。
3. **微信小游戏 WebGL 端**（依据：`name7\minigame\webgl.wasm.symbols.unityweb`、`审计与规划\微信小游戏性能优化实施计划.md`）。性能预算明显紧于原生 Unity——**单 mesh + 单 shader 优先，避免逐格粒子实例**。
4. **高亮渲染器已是"代码构建"**。`HexHighlightRenderer` 逐格生成六边形 mesh（外圈 6 点 + 中心点 → 轮廓与顶面填充），每通道独立材质（`GetOrCreateChannel` → `Unlit/Color`），`SetHighlightedCells(channel, cells, color)` 支持任意颜色。**网格与换色机制全已就绪，本文档只改材质与可选顶点**。
5. **`IMeshGenerator.BuildGridVertices` 直接给出外圈 6 个角点**（前 6 点即外圈，见 `HexHighlightRenderer.RebuildChannel` 的用法），后续任何"抬升/描边"都从这 6 点出发，无需重新采样地形。
6. **每帧只高亮 1 格**（`PlayerInputHandler` 传 `new[] { placeableCell }`），而每通道只有一个共享材质 → **单通道单色足够**，金/红切换 = 改 `_Color`，零额外成本。

---

## 三、视觉目标

- 一个**贴地的六边形**：外围一圈明亮的能量描边，内部半透明填充，**不抬高、不遮挡**；俯视带透视下，六边形边缘在掠射角处自然泛光（fresnel），形成"边缘亮、中心透"的效果。
- 有**轻微的流动/呼吸**：描边亮度与填充透明度随时间缓缓波动，读起来是"活的能量"而不是"死的高亮贴片"。
- **颜色语义**：`可放置 = 金黄`，`不可放置 = 红`（建议用比 `Color.red` 柔和的暖红，如 `(1f, 0.34f, 0.32f)`，拖拽全程盯着不刺眼）。颜色由 `PlayerInputHandler` 在 `HighlightGridOnMouseHover` 里决定，与现在传 `Color.yellow` 的同一位置。

---

## 四、实现方案

### 4.1 新增 shader：`Assets/Shader/CardDragGlow.shader`

一个 additive 半透明、带 fresnel 描边与噪声流动的 shader，属性驱动颜色与强度，全部由 `_Color` / 时间派生，**不写死颜色**（金/红复用同一材质，只改颜色）。

核心 TAGS / 混合（沿用 `HexGridOverlay.shader` 的贴地纪律 + 改 additive）：

```
Tags { "RenderType"="Transparent" "Queue"="Transparent" "IgnoreProjector"="True" }
Blend SrcAlpha One        // additive 伪辉光（无 bloom 的唯一可行解）
ZWrite Off
ZTest LEqual              // 被山体/单位正确遮挡
Offset -1, -1             // 消除与地形共面 z-fighting
Cull Off
```

frag 思路（约 60 行，技法全在仓库）：

```
- 世界法线 = up（平铺六边形顶面）。fresnel：
    float rim = pow(1 - saturate(dot(viewDir, worldUp)), rimPower);
- 填充：
    float fill = baseAlpha * (fresnel 边缘增强 + 常数中心透) * 呼吸波动;
- 流动：用 _Time.y 对噪声贴图 UV 做一轮 scroll，调制边缘亮度（能量感）。
- 输出 Color = _Color; alpha = fill; （additive 下明暗即"辉光"）
```

`_Color` 直接来自渲染器 `state.Material.color`，金/红切换对 shader 透明。呼吸/流动由 `_Time.y` 驱动，无需每帧 C# 更新 → **CPU 零成本**。

### 4.2 改 `HexHighlightRenderer.cs`：只给 CardPlacement 通道换材质

该渲染器每通道自带一个材质与 mesh。改法限定在 `GetOrCreateChannel`：`CardPlacement` 通道用新 shader 建材质，其余通道（Reachable / AttackRange / Selection / DebugDirtyChunk）维持 `Unlit/Color`。**不动** `SetHighlightedCells` 的颜色语义——`Material.color = color` 照旧，shader 读 `_Color`。

```csharp
// GetOrCreateChannel 内，按通道择材质：
Shader glowShader = Shader.Find("Custom/CardDragGlow");
state.Material = channel == HexHighlightChannel.CardPlacement && glowShader != null
    ? new Material(glowShader)
    : new Material(Shader.Find("Unlit/Color") ?? Shader.Find("Sprites/Default"));
```

这样即便 shader 找不到，也安全回退到现有纯色路径，不炸。

### 4.3 可选：顶部轻微抬升描边（做与不做都成立）

如果不希望完全平铺（贴地在高差地形上会有轻微嵌地感），可在 `RebuildChannel` 里把外圈 6 点抬升一个很小的量（如 `+0.12f`），仅让**描边**浮起，填充仍贴地。默认建议**先不做**，看真机效果再决定——本方案的核心卖点就是"不遮格"，抬升越小越好。

### 4.4 颜色由谁决定

`PlayerInputHandler.HighlightGridOnMouseHover` 在算 `placeableCell` 的同一处决定颜色：

```csharp
Color highlightColor = CanHighlightCellForCard(cell)
    ? PlaceableGlowColor   // 金黄
    : UnplaceableGlowColor; // 柔和红
_hexHighlightRenderer.SetHighlightedCells(
    HexHighlightChannel.CardPlacement, new[] { cell }, highlightColor);
```

两个颜色常量放在 `HexHighlightRenderer` 或 `PlayerInputHandler`（建议渲染器，供其他通道复用约定）。具体取值上真机调。

---

## 五、文件清单

| 文件 | 动作 |
|---|---|
| `Assets/Shader/CardDragGlow.shader` | 新建：additive + fresnel + 噪声流动 |
| `Assets/Scripts/Managers/HexHighlightRenderer.cs` | 改 `GetOrCreateChannel`（仅 CardPlacement 通道换材质）；新增金/红颜色常量 |
| `Assets/Scripts/Core/Services/PlayerInputHandler.cs` | 改 `HighlightGridOnMouseHover`（颜色由可放置性决定金/红；沿用 方案：去掉图标后的高亮常亮逻辑） |
| `Assets/Scripts/Managers/HexHighlightRenderer.cs`（mesh） | 可选：外圈 6 点微抬升 |

**不动**：`IMeshGenerator`、其它高亮通道、地图 mesh、`CardDragWorldPreviewController`。

---

## 六、验收（玩家可观察）

1. 拖牌时目标格出现**贴地发光六边形**，可放置为金光、不可放置为红光。
2. 六边形**边缘亮、中心透**，有明显但克制的能量流动/呼吸。
3. **不遮挡目标格及其单位**——围墙不抬高，或只抬描边极小量。
4. 金↔红切换只改颜色，无纹理/形状突变。
5. 不做抬手特效，无粒子、无额外 instance；微信小游戏 WebGL 端帧率无可见回退。
6. 其它高亮通道（可到达、攻击范围、选中）保持原有纯色表现，不受影响。
7. 山格不显示任何高亮（`IsBlockedByMountainGate` 门禁，语义不变）。

---

## 七、边界与风险

- **山格吞红光**：`HexHighlightRenderer.IsBlockedByMountainGate` 过滤有效山格 → 指向山时**连格带光都不出**。山明显不能放，可接受；若想"山也给反馈"，需单独开一条不经过门禁的通道，本方案不承诺。
- **无 bloom 的伪装极限**：additive 只能做"亮"，做不出真扩散光晕；若真机觉得"不够发光"，可加一层渐变 sprite 残影，但幅度要克制，避免糊。
- **颜色需真机调**：金黄 / 柔性红的取值在编辑器里看和真机（WebGL 色彩空间 / 屏幕亮度）差别大，必须真机定，见效果文档 §6.3。
- **性能**：单 mesh + 单 additive shader，是全项目最轻的光效路径之一；呼吸由 `_Time.y` 驱动，CPU 每帧零更新。

---

## 八、与方案乙的取舍（本方案为什么"轻"）

图里那种"高能量围墙 + 中心光柱 + 粒子塔"是 hero 概念效果（参考对象《英雄联盟手游》：技能落点也有高亮，但**不抬高成塔**）。放置高亮是**给即将落地的单位占位**：
- 抬高城墙会挡格、挡单位，与"让玩家看清要放哪"的目标直接冲突；
- 每格粒子实例在微信小游戏 WebGL 上是叠加的 overdraw + 每帧更新成本；
- 而"能否放置"靠颜色、'哪格'靠格子本身即可表达，**不需要塔**。

若真机看完觉得"太素、缺 wow"，再走方案乙（能量围墙 + 光柱），但默认建议以本方案落地。
