# 卡牌拖拽逻辑落点上移-修改计划

> 版本：v3（新方案：只逻辑位置上移，视觉留在触点）
> 目标：拖动卡牌时，**卡牌与模型预览仍显示在手指 / 鼠标触点处不变**，只把**地块高亮与最终落点**上移到触点上方，使目标地块不被手指遮挡。

**方案演进记录**：

- v1 / v2：视觉（卡牌、模型）与逻辑（高亮、落点）**一起**上移；解决“手指挡卡牌”，但改动大、`OnDrag` 双坐标转换与进度起点同步偏移最易出错。
- **v3（本文件采用）**：只把**逻辑位置**上移，视觉留在触点。解决“手指挡目标地块”，改动面减半，`OnDrag` / `OnDragUpdate` 零改动，回归风险极低。代价是模型预览与高亮在视觉上分离，靠较小偏移量（6.5%）缓解。

---

## 一、最终设计

只定义一个用于**地图射线检测**的逻辑指针位置：

```text
逻辑位置 = 原始鼠标 / 触屏位置 + 固定向上偏移
```

分工明确：

| 用途 | 使用的坐标 |
|---|---|
| 卡牌 UI 位置 | 原始触点（不变） |
| 模型预览位置 | 原始触点（不变） |
| 可放置地块高亮 | **逻辑位置（上移）** |
| 松手落点判定 | **逻辑位置（上移）** |

```text
              可放置高亮 / 实际落点
                     ▲
                     │ 固定偏移（6.5% 屏高）
                     │
              卡牌 / 模型 / 手指
              （三者仍在触点，手指压卡牌属拖拽常态）
```

因此手指仍压在卡牌 / 模型上（拖拽常态，可接受），但**目标地块从手指下方移开**，玩家能清楚看到“要放到哪一格”。

鼠标和触屏不做区别，统一使用同一套偏移规则。

**已接受的视觉分离**：模型预览在触点处、高亮框在其上方一段距离，两者不重合。偏移量取较小值（6.5%）使其更像“指向提示”而非割裂。

---

## 二、偏移量定义

固定屏幕比例，不按卡牌尺寸动态计算：

```csharp
private const float CardDragLogicOffsetRatio = 0.065f;
```

实际偏移（屏幕像素坐标）：

```csharp
Vector2 offset = Vector2.up * (Screen.height * CardDragLogicOffsetRatio);
```

即高亮 / 落点相对触点向上 6.5% 屏高。如需微调只改这一个常量，建议范围 `0.05f ~ 0.08f`。

**语义说明**：v3 的偏移量是“高亮相对模型的领先距离”，不是“把视觉抬离手指”，因此取值比 v2 的 10% 小。

---

## 三、修改范围

只修改两个文件：

- `Assets\Scripts\UI\CardController.cs`
- `Assets\Scripts\Core\Services\PlayerInputHandler.cs`

**不需要修改**：`CardPresenter`、`CardDragPreviewController`、`MapRaycastService`、`IMapRaycastService`、`ICardDragVisualHandler`、`CameraController`、`MapHeightEditTestController`。

**v3 相对 v2 额外不动的部分**（新方案的核心优势）：

- `OnBeginDrag` 传给模型预览的 `screenPos` **不动**。
- `OnDrag` 移动卡牌 / 幽灵卡的坐标转换 **不动**（无需双坐标转换）。
- `OnDragUpdate` 的进度计算 **不动**（无需起点同步偏移）。

---

## 四、具体修改计划

### 4.1 `CardController.cs`

#### 1. 增加逻辑位置转换方法

在类字段区域增加：

```csharp
/// <summary>高亮 / 落点相对触点向上的屏幕高度比例（视觉不使用此偏移）。</summary>
private const float CardDragLogicOffsetRatio = 0.065f;

/// <summary>返回用于地图射线的逻辑位置（触点上方，屏幕像素坐标）。
/// 仅用于高亮与落点，卡牌 / 模型视觉仍使用原始触点。</summary>
public static Vector2 GetCardDragLogicPosition(Vector2 pointerPosition)
{
    return pointerPosition + Vector2.up * (Screen.height * CardDragLogicOffsetRatio);
}
```

命名刻意用 `Logic` 而非 `Pointer`，强调它只服务于逻辑落点，避免后续误用到视觉位置上。

#### 2. `OnBeginDrag` —— 不修改

首次视觉更新继续使用 `eventData.position`：

```csharp
_dragVisual?.OnCardDragUpdate(this, eventData.position, 0f, 0f, 0f);
```

模型预览应显示在触点处，保持原样。

#### 3. `OnDrag` —— 不修改

卡牌 / 幽灵卡的 `ScreenPointToLocalPointInRectangle` 输入继续用 `eventData.position`，模型预览的 `screenPos` 继续用 `eventData.position`。整个方法保持现状，无需双坐标转换。

#### 4. `OnDragUpdate` —— 不修改

进度计算 `localPoint.y - originPos.y` 的起点仍是真实触点局部坐标，天然不产生虚假初始位移。v2 中“起点同步偏移”的逻辑在 v3 中不需要。

#### 5. `OnEndDrag` —— 落点判定改用逻辑位置（含备用路径与相机空值守卫）

这是 `CardController` 中**唯一**需要改的方法。当前 `OnEndDrag` 在分支外无条件执行 `Camera.main.ScreenPointToRay(eventData.position)`（约 CardController.cs:398），需整段替换为：

```csharp
Vector2 dragLogicPosition = GetCardDragLogicPosition(eventData.position);

RaycastHit hit;
bool isMapHit;
if (_mapRaycastService != null)
{
    isMapHit = _mapRaycastService.RaycastMap(dragLogicPosition, out hit);
}
else
{
    Camera mainCamera = Camera.main;
    if (mainCamera == null)
    {
        EndDragVisual();
        _dropHandler?.OnCardDragCancel(this);
        ResetToOrigin();
        return;
    }

    Ray ray = mainCamera.ScreenPointToRay(dragLogicPosition);
    isMapHit = Physics.Raycast(ray, out hit)
        && hit.transform.gameObject == _mapDataService.MapGameObject;
}
```

约束：`OnEndDrag` 中从此之后不允许再出现 `eventData.position`；主路径与备用路径共用同一个 `dragLogicPosition`，保证最终落点与拖动时高亮一致。

---

### 4.2 `PlayerInputHandler.cs`

#### 1. 只修改 `HighlightGridOnMouseHover` 的地图射线坐标

将：

```csharp
if (_mapRaycastService.RaycastMap(_input.MousePosition, out RaycastHit hit))
```

改为：

```csharp
Vector2 dragLogicPosition = CardController.GetCardDragLogicPosition(_input.MousePosition);
if (_mapRaycastService.RaycastMap(dragLogicPosition, out RaycastHit hit))
```

高亮资格判断、颜色、清理和拖拽状态管理全部不变。

#### 2. 本文件其余坐标一律不动

必须继续使用**原始**触点坐标：

- `GetCardUnderMouse`（约 :236）：点选卡牌必须用实际按下位置。
- `IsPointerOverBlockingUI`（约 :154）：UI 阻挡检测必须基于真实触点。
- `HandleTileClickForExploration` 的 `_explorationPointerStart` / `releasePosition`（约 :281/:292）：探索点击用原始坐标。
- 按钮按下 / 抬起检测：与坐标无关，不动。

#### 3. 已知时序 / 输入源差异（不修改，仅记录）

- 触屏下 `Input.mousePosition` 与 `PointerEventData.position` 在目标设备上通常一致，代码层面不保证；纳入真机验收。
- `Tick` 轮询与 EventSystem 回调可能相差一帧，接受最多一帧的高亮滞后 / 提前。

---

## 五、明确不做的内容

1. 不区分鼠标与触屏，两者统一偏移。
2. 不做屏幕边缘动态翻转或 Clamp。
3. 不移动卡牌 / 模型的视觉位置（v3 与 v2 的根本区别）。
4. 不修改 `OnBeginDrag` / `OnDrag` / `OnDragUpdate`。
5. 不增加视觉偏移接口参数，不改模型预览控制器。
6. 不增加半透明、连接线等额外视觉效果。
7. 不按卡牌尺寸动态计算偏移。
8. 不改变卡牌可放置资格判断规则。
9. 不修改探索点击、UI 阻挡检测、卡牌点选（`GetCardUnderMouse`）的原始坐标。
10. 不修改 `CameraController`、`MapHeightEditTestController`。

---

## 六、验收标准

1. 拖动卡牌时，卡牌与模型预览仍显示在手指 / 鼠标正下方（位置相对修改前不变）。
2. 地块高亮出现在触点上方约 6.5% 屏高处，目标地块不被手指遮挡。
3. 松手落点与拖动时高亮地块一致。
4. 主路径（`_mapRaycastService` 存在）与备用路径（`Physics.Raycast`）落点一致。
5. `Camera.main` 缺失时备用路径按失败流程收尾（视觉关闭、取消回调、卡牌复位），不抛空引用。
6. 普通卡（有模型预览）与战术卡（幽灵卡，两个槽位）行为均正确。
7. 鼠标和触屏行为一致；真机确认 `Input.mousePosition` 与 `PointerEventData.position` 一致。
8. 拖动取消、暂停、失焦、失败落点、成功放置流程正常。
9. 拖动开始不产生任何异常初始位移（因视觉未偏移，天然满足）。
10. 探索点击、卡牌点选、UI 阻挡检测行为与修改前完全一致。
11. 不需要修改 `ICardDragVisualHandler` 接口及模型预览控制器。

---

## 七、已接受的第一版限制

- 手指仍压在卡牌 / 模型上（拖拽常态，v3 不解决“手指挡卡牌”，只解决“手指挡目标地块”）。
- 模型预览在触点处、高亮框在其上方分离显示，两者不重合（靠 6.5% 小偏移缓解）。
- 触点靠近屏幕顶部时，上移后的高亮 / 落点可能超出地图有效范围，此时射线不命中即无高亮（与现有“射线未命中”行为一致，不额外处理）。
- 偏移量按屏幕像素定义（`Screen.height`），高亮为地图射线检测，不涉及 Canvas 局部坐标转换，无 CanvasScaler 单位问题。
- 偏移量先固定为 6.5%，后续调整只改一个常量。
