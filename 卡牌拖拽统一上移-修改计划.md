# 卡牌拖拽统一上移-修改计划

> 版本：v2（修订版，基于 v1 代码审查修订）
> 目标：拖动卡牌时，将卡牌、模型预览、地块高亮和最终落点统一移动到鼠标 / 手指触点上方，避免触点遮挡目标。

**v2 修订记录**（相对 v1）：

1. 补全 `OnDrag` 坐标转换的完整代码（v1 §4.1-4 的片段缺少方法体上下文和相机参数说明）。
2. 明确 `OnEndDrag` 中备用 `Physics.Raycast` 路径的射线必须同样使用偏移后位置，并新增 `Camera.main == null` 的失败处理。
3. 明确 `PlayerInputHandler` 中只允许修改高亮射线一处坐标，其余原始坐标（选卡、UI 阻挡、探索点击）不得改动。
4. 新增两条已接受限制：触屏输入源差异、Tick 轮询与 EventSystem 回调的一帧时序差异。
5. 明确固定偏移按屏幕像素定义，与 Canvas 局部坐标的关系写清楚，禁止误改 `_canvasHeight`。

---

## 一、最终设计

不再分别维护「逻辑位置」和「视觉位置」两个坐标，也不修改现有拖拽视觉接口。

统一定义一个新的拖拽指针位置：

```text
拖拽位置 = 原始鼠标 / 触屏位置 + 固定向上偏移
```

之后所有拖拽相关逻辑都使用这个“拖拽位置”：

- 卡牌位置：移动到拖拽位置；
- 模型预览：跟随拖拽位置；
- 可放置地块高亮：检测拖拽位置；
- 松手放置判定：检测拖拽位置。

```text
                 卡牌 / 模型
                     ▲
                     │
              拖拽位置 / 实际目标
                     ▲
                     │ 固定偏移
                     │
                 鼠标 / 手指
```

因此，手指不再直接覆盖实际目标地块，且视觉对象、高亮和最终落点始终保持一致。

鼠标和触屏不做区别，统一使用同一套位置偏移规则。

---

## 二、偏移量定义

第一版使用固定屏幕比例，不根据卡牌尺寸动态计算：

```csharp
private const float CardDragPointerOffsetRatio = 0.10f;
```

实际偏移：

```csharp
Vector2 offset = Vector2.up * (Screen.height * CardDragPointerOffsetRatio);
```

即向屏幕上方偏移约屏幕高度的 10%。如果实际体验偏高或偏低，只调整 `0.10f`，建议范围为 `0.07f ~ 0.13f`。

**【v2 补充】坐标单位说明**：偏移量定义在**屏幕像素坐标系**中（`Screen.height`），用于 `eventData.position` 和地图射线输入是正确的。卡牌 UI 在 `OnDrag` 中会先把屏幕坐标转换为 Canvas 局部坐标，转换后的本地移动距离受 CanvasScaler / Canvas 缩放影响，与屏幕像素不等价——这是预期行为，不要为此把 `_canvasHeight`（缩放进度基准，见 §4.1-4）改成 `Screen.height`，两者职责不同。

---

## 三、修改范围

只修改两个文件：

- `Assets\Scripts\UI\CardController.cs`
- `Assets\Scripts\Core\Services\PlayerInputHandler.cs`

模型预览已经接收 `CardController` 传入的屏幕位置，因此只要传入偏移后的坐标即可；战术卡幽灵代理也通过现有 `CardController.OnDragUpdate` 移动。

**【v2 确认】以下文件不需要修改**：`CardPresenter`、`CardDragPreviewController`、`MapRaycastService`、`IMapRaycastService`、`ICardDragVisualHandler`、`CameraController`、`MapHeightEditTestController`。

---

## 四、具体修改计划

### 4.1 `CardController.cs`

#### 1. 增加统一拖拽位置方法

在类字段区域增加：

```csharp
/// <summary>拖拽位置相对鼠标 / 触点向上的屏幕高度比例。</summary>
private const float CardDragPointerOffsetRatio = 0.10f;

/// <summary>返回位于原始触点上方的统一拖拽位置（屏幕像素坐标）。</summary>
public static Vector2 GetCardDragPointerPosition(Vector2 pointerPosition)
{
    return pointerPosition + Vector2.up * (Screen.height * CardDragPointerOffsetRatio);
}
```

该方法作为全项目唯一的拖拽坐标转换入口，保证卡牌、模型、高亮和落点使用同一个偏移。

#### 2. `OnBeginDrag` 使用偏移后位置启动模型预览

将首次视觉更新：

```csharp
_dragVisual?.OnCardDragUpdate(this, eventData.position, 0f, 0f, 0f);
```

改为：

```csharp
Vector2 dragPointerPosition = GetCardDragPointerPosition(eventData.position);
_dragVisual?.OnCardDragUpdate(this, dragPointerPosition, 0f, 0f, 0f);
```

#### 3. `OnDrag` 使用偏移后位置移动卡牌 / 幽灵卡

将 `OnDrag` 方法体整体改为：

```csharp
public void OnDrag(PointerEventData eventData)
{
    if (IsNextCard || !_isDragging) return;

    transform.SetAsLastSibling();

    RectTransform handPanelRect = _rectTransform.parent as RectTransform;
    if (handPanelRect == null) return;

    Vector2 dragPointerPosition = GetCardDragPointerPosition(eventData.position);

    Vector2 pointerLocalPos;
    Vector2 dragLocalPos;

    // 两次转换必须使用同一个相机参数（当前项目手牌 Canvas 为
    // ScreenSpaceOverlay，传 null 正确；若未来改为 ScreenSpaceCamera，
    // 此处需改为 canvas.worldCamera）。
    bool ok1 = RectTransformUtility.ScreenPointToLocalPointInRectangle(
        handPanelRect, eventData.position, null, out pointerLocalPos);
    bool ok2 = RectTransformUtility.ScreenPointToLocalPointInRectangle(
        handPanelRect, dragPointerPosition, null, out dragLocalPos);

    if (!ok1 || !ok2) return;

    // 显示点 = 偏移后位置；进度起点 = 原点 + 本地偏移量，
    // 两者差值仍是用户实际拖动距离，固定偏移不会计入进度（见第 4 点）。
    Vector2 localDragOffset = dragLocalPos - pointerLocalPos;
    OnDragUpdate(dragLocalPos, (Vector2)_originPosition + localDragOffset);

    // 进度已在 OnDragUpdate 中算好，这里只做转发（每帧不重复计算）。
    _dragVisual?.OnCardDragUpdate(this, dragPointerPosition,
        _upwardDistance, _cardProgress, _modelProgress);
}
```

这样普通卡牌和战术卡幽灵代理都会显示在触点上方。

**【v2 修正】v1 第 4 点只给了片段，实施时必须按上面完整方法体替换。** 关键约束：

- 两次 `ScreenPointToLocalPointInRectangle` 的相机参数必须与现状一致（当前项目为 `null`）；若手牌 Canvas 未来改为 `ScreenSpaceCamera` / `WorldSpace`，必须改用对应 `worldCamera`，否则坐标错位。参考 `CardDragPreviewController.ApplyVisual` 中已有的 renderMode 分支写法。
- 任何一个转换失败（返回值 false）都必须跳过本帧，禁止只处理成功的那一个，否则进度基准与显示点会错位。

#### 4. 防止固定偏移导致拖拽刚开始就触发缩放

原理（已由第 3 点的完整代码实现，本节仅为说明）：

`OnDragUpdate(localPoint, originPos)` 的进度计算是 `localPoint.y - originPos.y`。若只把显示点上移而起点不动，拖拽开始瞬间就会产生 `屏幕高度 × 10%` 的虚假向上距离，卡牌立即进入缩放 / 淡出阶段。

因此起点必须同步偏移同一本地偏移量：

```csharp
Vector2 localDragOffset = dragLocalPos - pointerLocalPos;
OnDragUpdate(dragLocalPos, (Vector2)_originPosition + localDragOffset);
```

代入后可验证：

```text
dragLocalPos - (_originPosition + localDragOffset)
= dragLocalPos - dragLocalPos + pointerLocalPos - _originPosition
= pointerLocalPos - _originPosition
```

即卡牌虽然显示在触点上方，但卡牌缩小、淡出和模型淡入仍从实际拖动开始计算，不会因为固定偏移产生额外初始位移。

**注意**：`_canvasHeight`（`UIScreenHelper.CanvasHeight(this)`）是缩放进度的本地坐标基准，继续保留原值，不要改成 `Screen.height`。

#### 5. 模型预览继续使用同一个偏移后屏幕位置

已在第 3 点的完整方法体中一并完成：`_dragVisual?.OnCardDragUpdate` 的 `screenPos` 参数传入 `dragPointerPosition`。

不修改 `CardPresenter` 和 `CardDragPreviewController`。

#### 6. 松手落点判定使用偏移后位置（含备用路径与相机空值守卫）

**【v2 修正】v1 只要求替换服务调用和备用 `ScreenPointToRay`，但当前 `OnEndDrag` 在分支之外无条件执行 `Camera.main.ScreenPointToRay(eventData.position)`（约 CardController.cs:398），只改服务调用会留下两个 bug：备用路径仍用原始触点导致高亮与落点不一致；且 `_mapRaycastService` 非空时该行也会先执行，`Camera.main` 为空时直接空引用。**

将 `OnEndDrag` 中从射线生成到落点判定的段落整体改为：

```csharp
Vector2 dragPointerPosition = GetCardDragPointerPosition(eventData.position);

RaycastHit hit;
bool isMapHit;
if (_mapRaycastService != null)
{
    // 统一射线服务：命中 Chunk 的 MapChunkView 后代。
    isMapHit = _mapRaycastService.RaycastMap(dragPointerPosition, out hit);
}
else
{
    // 备用路径：必须使用同一个 dragPointerPosition，
    // 否则高亮与最终落点不一致；相机为空时按失败落点流程处理。
    Camera mainCamera = Camera.main;
    if (mainCamera == null)
    {
        EndDragVisual();
        _dropHandler?.OnCardDragCancel(this);
        ResetToOrigin();
        return;
    }

    Ray ray = mainCamera.ScreenPointToRay(dragPointerPosition);
    isMapHit = Physics.Raycast(ray, out hit)
        && hit.transform.gameObject == _mapDataService.MapGameObject;
}
```

即：**`OnEndDrag` 中从此之后不允许再出现 `eventData.position`**，主路径与备用路径使用同一个 `dragPointerPosition`，保证最终放置地块与拖动过程中显示的高亮地块一致。

---

### 4.2 `PlayerInputHandler.cs`

#### 1. 只修改 `HighlightGridOnMouseHover` 的地图射线坐标

将原始位置：

```csharp
_input.MousePosition
```

转换为统一拖拽位置：

```csharp
Vector2 dragPointerPosition = CardController.GetCardDragPointerPosition(
    _input.MousePosition);
```

再用于地图射线：

```csharp
if (_mapRaycastService.RaycastMap(dragPointerPosition, out RaycastHit hit))
```

高亮资格判断、颜色、清理和拖拽状态管理全部保持不变。

#### 2. 【v2 新增】本文件中其余坐标一律不得改动

以下位置必须使用**原始**触点坐标，禁止套用上移偏移：

- `GetCardUnderMouse`（约 :236）：用实际按下位置判断点中哪张卡；若偏移，点击卡牌下半部会漏选。
- `IsPointerOverBlockingUI`（约 :154）：UI 阻挡检测必须基于真实触点，否则手指上方偏移处被误判为阻挡。
- `HandleTileClickForExploration` 中的 `_explorationPointerStart` 与 `releasePosition`（约 :281/:292）：探索不是卡牌拖拽，释放判定与距离阈值都必须用原始坐标。
- `HandleCardDragging` 中的按钮按下 / 抬起检测（`GetMouseButtonDown/Up`）：输入状态与坐标无关，不动。

#### 3. 【v2 新增】已知时序差异（不修改，仅记录并纳入验收）

`PlayerInputHandler` 在 `Tick` 中轮询 `Input.mousePosition`，而 `CardController` 由 EventSystem 回调驱动且使用 `PointerEventData.position`。两者存在两个已接受差异：

- **输入源差异**：触屏下 `Input.mousePosition` 由 Unity 模拟为主触点位置，在目标设备上通常与 `eventData.position` 一致，但代码层面不保证。第一版接受该差异，需在真机验收中确认（见验收标准第 9 条）。
- **时序差异**：`Tick` 轮询与 `OnBeginDrag` / `OnDrag` 回调可能相差一帧。极端情况下 `HandleCardDragging` 可能在 `OnBeginDrag` 之前先置 `_isDraggingCard = true` 并开始高亮，或高亮与卡牌位置相差一帧。第一版接受最多一帧的滞后 / 提前，不引入跨类回调重连。

---

## 五、明确不做的内容

1. 不区分鼠标与触屏，两者统一上移。
2. 不做屏幕边缘动态翻转或 Clamp。
3. 不增加视觉偏移接口参数。
4. 不修改模型预览控制器内部逻辑。
5. 不增加半透明、幽灵卡或连接线效果。
6. 不按卡牌尺寸动态计算偏移。
7. 不调整现有卡牌缩放、淡出、模型淡入规则。
8. 不改变卡牌可放置资格判断规则。
9. 【v2 新增】不修改探索点击、UI 阻挡检测、卡牌点选（`GetCardUnderMouse`）的原始坐标。
10. 【v2 新增】不修改 `CameraController` 的相机拖动坐标（它已通过 `CardController.IsAnyCardDragging` 排除拖拽期间）。
11. 【v2 新增】不修改 `MapHeightEditTestController` 等调试工具的原始坐标。
12. 【v2 新增】不修改 `_canvasHeight` 的计算方式（它是进度基准，与屏幕像素偏移无关）。

---

## 六、验收标准

1. 鼠标或手指拖动卡牌时，卡牌显示在原始触点上方约 10% 屏幕高度的位置。
2. 普通卡模型预览显示在同一个位置。
3. 战术卡幽灵代理显示在同一个位置（两个战术卡槽位都要验证）。
4. 地块高亮位于偏移后的实际目标位置，不再被手指遮挡。
5. 松手时的实际放置地块与拖动时的高亮地块一致。
6. 鼠标和触屏行为一致。
7. 拖动开始时不会因为固定偏移而提前触发卡牌缩放或模型淡入。
8. 拖动取消、暂停、失焦、失败落点和成功放置流程正常。
9. 【v2 新增】在目标触屏设备上确认 `Input.mousePosition` 与拖拽事件的 `PointerEventData.position` 一致（卡牌位置与高亮位置同帧贴合，无持续可见偏差）。
10. 【v2 新增】高亮与最终落点在 `_mapRaycastService` 存在（主路径）与缺失（备用 `Physics.Raycast` 路径）两种情况下都使用偏移后位置，结果一致。
11. 【v2 新增】`Camera.main` 缺失时备用路径按失败落点流程收尾（视觉关闭、取消回调、卡牌复位），不抛空引用。
12. 【v2 新增】探索点击、`GetCardUnderMouse` 点选、UI 阻挡检测行为与修改前完全一致（回归确认未被偏移影响）。
13. 不需要修改 `ICardDragVisualHandler` 接口及模型预览控制器。

---

## 七、已接受的第一版限制

- 触点靠近屏幕顶部时，偏移后的卡牌 / 模型可能部分移出屏幕。
- 实际放置目标是触点上方的地块，而不是手指正下方的地块。
- 偏移量先固定为屏幕高度的 10%，后续如需调整只修改一个常量。
- 【v2 新增】`PlayerInputHandler` 使用 `Input.mousePosition` 轮询，而 `CardController` 使用 `PointerEventData.position`；在特殊输入设备上两者可能存在一帧或输入源差异，第一版不为此引入跨类状态共享。
- 【v2 新增】固定偏移按屏幕像素定义，转换为 Canvas 局部坐标后的本地单位受 CanvasScaler / Canvas 缩放影响，与“屏幕高度 10%”在本地坐标中数值不等价，属预期。
- 【v2 新增】UI 坐标转换沿用现有 `null` 相机参数，前提为手牌 Canvas 是 `ScreenSpaceOverlay`；若未来改为其他渲染模式，需同步调整转换代码。
