# 势力范围实体城墙改造方案

## 一、改造目标

将当前 `SphereOfInfluenceRenderer` 生成的势力范围描边面片替换为实体模型：

- 边界折线的节点使用“城墩”预制体；
- 边界线段使用“城墙”预制体；
- 保留玩家与敌方势力范围的颜色、迷雾可见性和刷新规则；
- 支持地块高度差、随机扰动以及矩形过渡区域上的边界墙段。

本方案只讨论几何数据和模型摆放，不涉及本次直接修改代码。

---

## 二、当前面片边界的组成

当前势力范围边界由 `MeshDataGenerator.GetOneSphereOfInfluenceVertices` 生成，主要包含两类四边形面片。

### 2.1 三-1：地块自身的边界边

每条真正的六边形边界边由同一地块的两个 `SolidAreaVertices` 角点构成：

```text
NE：点 1 → 点 2
E ：点 2 → 点 3
SE：点 3 → 点 4
SW：点 4 → 点 5
W ：点 5 → 点 6
NW：点 6 → 点 1
```

这一类线段对应标准城墙：

- 水平投影长度基本等于一个六边形地块边长；
- 两端来自同一地块，基础高度一致；
- 只存在顶点随机扰动带来的微小高度差。

### 2.2 三-2：相邻边缘地块之间的过渡边

代码会连接相邻边缘地块的对应角点。例如 NE 方向：

```text
当前格点 1 → NE 邻格点 5
当前格点 2 → NE 邻格点 4
```

这一类线段跨越矩形过渡区域，确实对应独立城墙，而不是城墩。

其特点是：

- 两端来自不同地块；
- 长度受地块位置、扰动及地形结构影响；
- 两端高度可能不同；
- 城墙可能需要适应任意坡度；
- 不能假设其长度恒等于标准六边形边长。

因此实体化后必须明确区分“标准边城墙”和“过渡边城墙”。

---

## 三、核心几何数据

建议先将原面片生成过程整理为统一的边界线段数据，而不是直接在原四边形顶点列表上生成模型。

```text
BoundarySegment
├── Vector3 Start
├── Vector3 End
├── BoundarySegmentType Type
│     ├── HexEdge       // 三-1：标准六边形边
│     └── Transition    // 三-2：矩形过渡区域边
├── int OwnerIndex
└── Color FactionColor
```

每条线段可直接计算：

```text
水平向量 = End.xz - Start.xz
水平长度 = |水平向量|
高度差   = End.y - Start.y
三维长度 = |End - Start|
坡度角   = atan2(高度差, 水平长度)
中点     = (Start + End) / 2
```

该数据层应与最终表现方式分离。以后无论使用预制体、运行时变形 Mesh 或对象池，都不需要重写边界判定。

---

## 四、标准边城墙

三-1 线段使用普通城墙预制体。

推荐约定：

- 预制体 Pivot 位于底部中心；
- 本地 `Z` 轴为城墙长度方向；
- 本地 `Y` 轴为竖直方向；
- 预制体原始长度等于一个标准六边形边长；
- 城墙模型两端允许略微插入城墩，以隐藏接缝。

摆放方式：

```text
位置：Start 与 End 的中点
水平朝向：沿 Start → End
高度：两端 Y 的平均值或过滤扰动后的统一高度
长度缩放：通常为 1；必要时按实际水平长度微调
```

同一地块边上的微小随机 Y 扰动不建议让整段墙倾斜。可以设置高度容差：

```text
abs(End.y - Start.y) < epsilon
→ 视为水平城墙
```

---

## 五、过渡边城墙

三-2 线段两端来自不同地块，高度、长度和坡度都可能不同，不能依赖预制的固定倾斜角。



### 5.2 方案：运行时 Mesh 变形

城墙保持世界竖直，只让底部和顶部沿长度方向逐渐变化高度。

对城墙 Mesh 中每个顶点，按其在本地长度轴上的比例 `t` 计算高度：

```text
t = 顶点沿长度轴的位置 / 模型总长度
目标底部高度 = Lerp(Start.y, End.y, t)
```

优点：

- 支持任意长度、坡度和随机扰动；
- 城墙的竖直方向仍然保持竖直；
- 不需要制作多种倾斜预制体；
- 视觉上比整体倾斜自然。

缺点：

- 砖块、门洞和垛口可能被拉伸或剪切；
- 预制体 Mesh 必须具有足够的长度方向分段；
- 每个实例需要独立 Mesh，必须管理 Mesh 生命周期；
- 不适合含复杂骨骼、动画或依赖固定 UV 的模型。

适合作为中等坡度的主方案。


---

## 六、推荐混合方案

不建议所有过渡边统一使用一种处理方式。推荐先过滤随机噪声，再根据真实高度差分类：

| 条件 | 处理方式 |
|---|---|
| 高度差小于容差 | 忽略 Y 差，使用水平标准墙 |
| 中等高度差 | 使用运行时 Mesh 变形墙 |
| 高度差较大 | 使用阶梯墙或专用垂直连接结构 |

推荐流程：

```text
1. 取得 Start / End 实际世界坐标
2. 将微小随机扰动从高度判断中剔除
3. 计算有效高度差
4.  变形墙
```

### 6.1 过滤随机扰动

不建议直接用最终扰动后的 Y 判断坡度，否则地表噪声会造成城墙轻微抖动。

可选方法：

1. 使用 `SolidAreaVerticesWithoutPerturb` 的基础 Y；
2. 使用 `HexCellData.Height * elevationStep` 作为高度等级；
3. 对实际 Y 设置阈值，小于阈值的差异归零；
4. XZ 使用最终扰动位置，Y 使用过滤后的地块高度。

推荐：

```text
XZ：使用实际边界顶点，保留地形横向扰动
Y ：使用地块基础高度或高度等级，忽略微小随机噪声
```

---

## 七、城墩设计

城墩只负责边界折线的节点，不代替三-2 过渡墙。

需要解决：

- 同一理论角点可能由多个地块给出不同扰动坐标；
- 相邻墙段高度可能不同；
- 多条墙段可能在同一点汇合；
- 同一城墩不能重复生成。

建议：

1. 对角点进行空间量化后去重，而不是直接用 `Vector3` 精确比较；
2. 城墩 Y 取连接墙段端点的最大值或平均值；
3. 城墩底部适当向下延伸，遮挡悬空和插地接缝；
4. 城墩直径应略大于墙厚，允许墙体插入城墩内部。

---

## 八、刷新与性能

当前 `SphereOfInfluenceRenderer` 每次刷新都会销毁旧 Mesh 和材质后重建。若改为实体模型，不建议每次直接 `Instantiate/Destroy` 大量对象。

推荐使用对象池：

```text
WallPool
├── StandardWall
├── TransitionWall
└── StepWallPart

TowerPool
└── Tower
```

刷新时：

1. 计算新的边界线段和角点集合；
2. 从对象池取所需数量；
3. 更新位置、旋转、缩放或 Mesh；
4. 多余对象回池并禁用；
5. 不在每次视野变化时反复销毁材质和 Mesh。

敌方势力仍需遵守当前迷雾规则：

- 玩家势力完整显示；
- 敌方势力仅显示当前可见部分；
- 不沿迷雾切断处生成虚假的封闭边界。

---

## 九、预制体制作约定

城墙预制体：

- Pivot：底部中心；
- 长度轴：本地 Z；
- 高度轴：本地 Y；
- 原始长度：标准六边形边长；
- 墙体两端允许被城墩覆盖；
- 若支持运行时变形，长度方向需要有足够顶点分段；
- 避免将碰撞体作为势力范围显示的必要组件。

城墩预制体：

- Pivot：底部中心；
- 外形尽量旋转对称；
- 底座向下留出埋地部分；
- 直径略大于墙厚；
- 能遮挡不同高度墙段之间的接缝。

---

## 十、推荐实施顺序

### 阶段 1：边界数据拆分

- 从 `GetOneSphereOfInfluenceVertices` 中提取统一 `BoundarySegment`；
- 明确标记 `HexEdge` 与 `Transition`；
- 输出唯一角点集合；
- 暂时保留原面片用于对照。

### 阶段 2：标准墙和城墩

- 暴露城墙和城墩预制体字段；
- 三-1 边生成标准城墙；
- 边界节点生成城墩；
- 加入对象池；
- 与原面片边界重合验证。

### 阶段 3：过渡墙

- 三-2 边生成过渡城墙；
- 增加高度差阈值；
- 实现水平墙和变形墙两种模式；
- 测试不同地形高度和随机种子。

### 阶段 4：陡坡与细节

- 对大高度差增加阶梯墙；
- 调整城墩接缝遮挡；
- 保留玩家/敌方颜色差异；
- 检查迷雾刷新、城池销毁、势力扩张时的对象池更新。

---

## 十一、结论

该设计可以实现，但不能把所有边界线段都当作固定长度、水平的六边形边。

必须区分：

```text
三-1 标准边 → 固定长度普通城墙
三-2 过渡边 → 动态长度、动态高度的过渡城墙
边界节点    → 城墩
```

最稳妥的最终方案是：

```text
统一 BoundarySegment 数据层
+ 标准墙预制体
+ 中坡运行时 Mesh 变形
+ 陡坡阶梯墙
+ 城墩遮挡接缝
+ 对象池管理实例
```
 
这样无需为随机坡度制作大量预制体，同时能够适应任意地块高度和随机扰动。

---

## 十二、编辑器操作步骤

以下操作全部在 Unity Editor 中完成。

### 12.1 挂载 SphereOfInfluenceRenderer

1. 打开 `GameScene.unity`
2. 在场景中找一个合适的空物体（或新建一个 `GameObject`），命名为 `SphereOfInfluenceRenderer`
3. 给它添加 `SphereOfInfluenceRenderer` 组件（`Add Component → Sphere Of Influence Renderer`）
4. 位置和父物体不限，组件本身只在初始化时生效

### 12.2 制作城墙预制体

1. 在 Project 窗口 `Assets/Prefabs`（或自选目录）创建一个新预制体，命名如 `CityWall`
2. 预制体结构要求：

   ```text
   CityWall（根 GameObject）
   └── MeshFilter + MeshRenderer（用于显示墙体）
       材质：建议 Lit/Standard，颜色按需
   ```

3. **Pivot 位置**：底部中心（Y=0 是地面，Z=0 是墙长度中点）
4. **朝向**：Z 轴为墙的长度方向（Z+ 和 Z- 各走一半）
5. **原始长度**：与标准六边形边长对齐。六边形边长查看 `MapGenerationConfigSO → OuterRadius`，边长 = `OuterRadius / tan(60°/2)` ≈ `OuterRadius × 0.577`。例如 `OuterRadius = 3` 时，边长 ≈ `1.732`
6. 墙体两端略超出标准半边长，以便插入城墩遮掩接缝
7. **如果要做变形墙**，Mesh 在 Z 方向需要**足够多的顶点分段**（建议每单位长度 2–3 段），否则坡度渐变不平滑

   > 简单验证：在 ProBuilder 或用自定义模型沿 Z 向拉出墙体，看 Vertices 数量。Z 向分段数 < 4 的做变形墙效果差。

### 12.3 制作城墩预制体

1. 创建新预制体，命名如 `CityTower`
2. 预制体结构：

   ```text
   CityTower（根 GameObject）
   └── MeshFilter + MeshRenderer
   ```

3. **Pivot**：底部中心
4. **造型**：尽量旋转对称（圆柱/方柱），直径略大于墙体厚度
5. 底部向下延伸一小段，在高差大的地方不会露出缝隙
6. 高度不少于标准墙高度

### 12.4 在 SphereOfInfluenceRenderer 上指定预制体

1. 选中场景中的 `SphereOfInfluenceRenderer` 物体
2. Inspector 中有两个序列化字段：
   - **WallPrefab** ：拖入 `CityWall` 预制体
   - **TowerPrefab** ：拖入 `CityTower` 预制体
3. `Height Tolerance` 保持默认值 `0.15`：高度差小于此值视为水平墙
4. 两个预制体**都**指定后，运行时自动使用实体模型渲染；任何一个是 `None` 则回退旧面片模式，方便逐步替换调试

### 12.5 预览与调试

1. 在 Unity Editor 中 `Play` 进入游戏
2. 按 `G` 显示六边形网格，观察城墙和城墩是否与网格边界对齐
3. 查看不同地块高度的边界是否正常：
   - 低差区域：标准水平墙
   - 高差过渡区：变形墙（墙底沿高度逐渐变化，但墙身保持竖直）
4. 如果变形墙视觉不理想：退回预制体增加 Z 向顶点分段，或暂时把预制体设为 `None` 先用面片模式
5. 播放时选中运行时生成的 `SphereOfInfluence_Models` 物体，可查看对象池内容

### 12.6 常见问题

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 运行时没有城墙/城墩 | 未指定预制体 | 在 Inspector 拖入 `WallPrefab` 和 `TowerPrefab` |
| 城墙和网格边界对不齐 | 预制体长度 ≠ 六边形边长 | 调整预制体模型 Z 向长度 |
| 变形墙有锯齿 | Mesh 的 Z 向顶点分段不足 | 增加墙模型 Z 向分段数 |
| 城墩和墙之间有明显缝隙 | 墙端不够长 / 城墩直径太小 | 给墙端加长，或增大城墩直径 |
| 同一角点出现多个城墩重叠 | 空间量化去重粒度过细 | 调整 `SphereOfInfluenceRenderer.ExtractSphereOfInfluenceBoundary` 中 `QuantizeKey` 的精度乘数（当前 ×100） |
| 敌人势力范围全部显示而非迷雾过滤 | 迷雾可见性依赖原面片逻辑 | 已有代码沿用 `membershipCells` 参数，无需额外处理 |
