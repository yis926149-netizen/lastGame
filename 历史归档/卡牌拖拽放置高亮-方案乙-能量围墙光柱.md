# 卡牌拖拽放置高亮 · 方案乙（能量围墙 + 光柱）

> 版本：v1
> 定位：接近参考概念图的 hero 质感——**抬起的能量围墙 + 顶部渐隐光柱 + 可选底部粒子**，视觉冲击强，但**遮格、更贵**。
> 与《卡牌拖拽放置高亮-方案甲-轻量发光描边.md》二选一：方案甲轻、不遮格；本方案重、更接近概念图。

---

## 一、要解决的问题

方案甲只解决"有能量感"，本方案解决"**像图里那样**"——图里是发光的六边形能量围墙，墙内顶面上浮一颗落点、墙外粒子飘散、整体像一座悬浮的魔法结界。

本方案在方案甲的基础上追加三样东西：

1. **抬起的能量围墙**：六边形的 6 条边从地面向上立起 0.5~1.0 世界单位，形成"容器感"，告诉玩家"这一整格被我框住了"。
2. **顶部渐隐 / 光柱**：围墙顶边与格中心有向上的辉光渐隐，或一根中心光柱，强化"能量封顶"。
3. **底部粒子（可选 garnish）**：围墙底部一圈飘散的 additive 粒子火花。

> **注意**：这三样都直接与"看清落点格"的目标冲突——围墙越高越挡格、粒子越多越挡视野。因此本方案默认**只做围墙 + 光柱，不做或少做粒子**，并克制高度。

---

## 二、技术前提（与方案甲完全一致，硬约束）

1. **内建渲染管线**，无 URP/HDRP；shader 走 `CGPROGRAM`/Standard（见 [ExplorationPillar.shader](../Assets/Shader/ExplorationPillar.shader)）。
2. **无 PostProcessing / bloom** → 一切"辉光"必须 additive 伪装；**本方案更依赖这点**，因为围墙+光柱的"光"全靠 additive 和明暗渐变撑。
3. **微信小游戏 WebGL 端**，性能紧 → 围墙/光柱做成**同一个 mesh + 1~2 个 shader**，粒子只用**单个** span 在格中心，不做逐墙粒子。
4. **`HexHighlightRenderer` 已是代码构建**，`BuildGridVertices` 直给外圈 6 点，每通道独立材质，`SetHighlightedCells(channel, cells, color)` 任意色。
5. **每帧只高亮 1 格**，单通道单色足够；金/红 = 改 `_Color`。

---

## 三、视觉目标

- 一个**立体六边形结界**：外围 6 面能量墙立起，墙内为半透明填充顶面，墙顶边缘有向上升起、逐渐变淡的辉光剪影（"光柱感"）。
- 围墙**底部亮、向上渐隐**（竖直梯度），墙壁表面有随时间向上的能量流动。
- 顶部中心可选一根**光柱**（更进一步的 wow，可后续再加）。
- **颜色语义不变**：可放置金光 / 不可放置红光，整座结界同色。

---

## 四、实现方案

### 4.1 新增 shader：`Assets/Shader/CardDragPillarGlow.shader`

一个 additive 半透明 shader，同时服务"顶面填充"与"围墙竖面"（用顶点色或语义区分），属性驱动颜色、墙高、流动。沿用 `HexGridOverlay.shader` 的贴地纪律：

```
Tags { "RenderType"="Transparent" "Queue"="Transparent" "IgnoreProjector"="True" }
Blend SrcAlpha One
ZWrite Off
ZTest LEqual          // 想"无视遮挡"再改成 Always（见 §七 取舍）
Offset -1, -1
Cull Off
```

frag 思路：

```
- 竖直梯度（围墙）：用 worldPos.y 相对墙底 _BaseY、墙高 _WallHeight 归一化，
    alpha = 底部亮 → 顶部渐隐（如 saturate(1 - normalizedY)）。
- 流动：噪声贴图 UV 随 _Time.y 向上滚动，调制边缘/表面亮度（能量上升感）。
- 顶面填充：与方案甲相同，fresnel 边缘增强。
- 输出 Color = _Color; alpha = 各项叠加。
```

### 4.2 改 `HexHighlightRenderer.cs`：CardPlacement 通道换材质 + 抬升围墙顶点

`GetOrCreateChannel` 里 `CardPlacement` 通道用 `Custom/CardDragPillarGlow` 建材质（找不到回退 `Unlit/Color`）。

`RebuildChannel` 里，对 `CardPlacement` 通道的每个格，在既有"外圈 6 点"基础上：

```
- 保留顶面填充（外圈 6 点 + 中心点，与现状一致）；
- 追加围墙：对每条边 (i, i+1)，在角点 P[i]、P[i+1] 处各加"底点 + 顶点"，
  生成一个竖面 quad，顶点抬升 _WallHeight；墙顶顶点 alpha 驱动到渐隐。
```

即每格从现在的 7 顶点（6 圈 + 1 心）增加到 7 + 6×4 = 31 顶点左右（6 个竖面 × 4 点 + 顶面 7 点）。单格单帧重建的顶点量级完全可接受，但 mesh 重建频率仍是"变化才重建"（现状缓存语义不变）。

### 4.3 可选 garnish：单个 additive ParticleSystem

格中心放**一个** `ParticleSystem`，材质 additive + 发光，短生命周期、弱强度，用来补一点"粒子塔"的 wow。**默认不做**——它只贡献观感，不贡献语义，且在微信小游戏 WebGL 上是每格实例化的成本。要加就加一个，别多。

### 4.4 颜色由谁决定

与方案甲相同，`PlayerInputHandler.HighlightGridOnMouseHover` 按 `CanHighlightCellForCard` 决定金/红，`SetHighlightedCells` 传色。围墙/光柱/顶面共用 `_Color`，整座结界同色。

---

## 五、文件清单

| 文件 | 动作 |
|---|---|
| `Assets/Shader/CardDragPillarGlow.shader` | 新建：additive + 围墙竖直梯度 + 流动 |
| `Assets/Scripts/Managers/HexHighlightRenderer.cs` | `GetOrCreateChannel` 仅 CardPlacement 换材质；`RebuildChannel` 追加围墙竖面；新增墙高/颜色常量 |
| `Assets/Scripts/Core/Services/PlayerInputHandler.cs` | 同方案甲：颜色金/红 |
| `Assets/Shader/CardDragPillarGlow.shader`（顶端光柱） | 可选：如需中心光柱，可同 shader 加一个竖直渐变 quad 或复用 `ExplorationPillar` 技法 |
| 单个 additive ParticleSystem | 可选 garnish，默认不做 |

**不动**：`IMeshGenerator`、其它高亮通道、地图 mesh、`CardDragWorldPreviewController`。

---

## 六、验收（玩家可观察）

1. 拖牌时目标格出现**立体能量围墙**：六面立起、底部亮、顶部渐隐，可放置金光 / 不可放置红光。
2. 围墙**高度克制**（≤1 世界单位，真机定），不遮蔽格内单位与落点。
3. 墙壁有向上的能量流动，但**不刺眼、不糊**。
4. 金↔红切换单色跳变，无纹理/形状突变。
5. （若做粒子）格中心有**单一个**粒子火花，强度弱、无性能回退。
6. 其它高亮通道（可达/攻击/选中）保持纯色，不受影响。
7. 山格不显示任何高亮（门禁不变）。

---

## 七、边界与风险（本方案比方案甲多出来的）

- **真 occlusion 取舍（最关键）**：围墙立起后，若 `ZTest LEqual`，格内单位会被走廊式遮挡（墙挡住部分视野）；若 `ZTest Always` 又会让墙无视地形/单位叠在最上，可能"穿模"到玩家单位头顶。**这一条必须在真机对上场景高度定**——它直接决定围墙是"结界"还是"挡视线的护栏"。
- **高度必须真机标定**：`_WallHeight`、`_BaseY` 与地图高差、相机俯角强相关，编辑器里看着合适，真机上可能过顶或嵌地。参考效果文档 §6.3 "编辑器判定不可信，唯一真机"。
- **mesh 顶点量**：每格约 31 顶点、每帧仅 1 格、变化才重建，量级安全；但不要把这套带到"多格同时高亮"的通道上去（Reachable 会差一个数量级，仍是单通道，但顶点数会随格子数线性涨）。
- **无 bloom 伪装极限**：围墙的"光"只有 additive 明暗渐变，做不出真扩散；想要更强光晕只能加伪 bloom 残影层，会叠加 overdraw。控制加层数。
- **性能**：比方案甲贵，主要是 6 个竖面带来的 overdraw（从"平铺1个六边形"变成"6 面 + 顶面"）。微信小游戏 WebGL 上要真机确认不掉帧；若掉，回退方案甲或降低墙高/撤粒子。

---

## 八、决策建议

- **默认不建议本方案作为首版**：它把"看清落点格"这个核心目标让给了"视觉冲击"，且 occlusion、高度、性能三处都要真机调，投入明显高于方案甲。
- **何时值得做**：方案甲真机看完觉得"太素、撑不起唯一载体的语义强度"，且确认围墙高度在真机俯角下不挡格，再上本方案。
- **若做，从"围墙 + 顶面 + 单色"起步**，光柱、粒子、伪 bloom 全部后置——先立起结界看 occlusion 与帧率，再决定堆不堆 garnish。
