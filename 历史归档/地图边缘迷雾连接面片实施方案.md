# 地图边缘迷雾连接面片实施方案

## 背景与问题

主体迷雾集成进地形 Shader 后（`Fog` GameObject 不再生成网格），暴露出一圈历史遗留缝隙：

- **地图边缘**是不规则六边形锯齿轮廓（边缘地块的真实顶点，含扰动、含高度）。
- **封皮（FogCover / FogCover_two）内边界**是矩形包围盒（外扩 1 单位）。
- 两者之间的环形区域**没有任何网格覆盖**，透出背景。

### 根因定位

`MeshDataGenerator.GetFogVertices`（约 `MeshDataGenerator.cs:5244` 附近）其实**已经算出了地图真实不规则轮廓** `outerBoundaryVector3`——遍历边缘地块的开放边、按序收集真实顶点。但随后（约 5283 行）把它**丢弃**，仅用其包围盒生成矩形 `RectPoints` 作为输出边界。

以前不露馅的原因：旧主体 Fog 网格 = 矩形减去已探索洞，恰好把"不规则边缘 ↔ 矩形"的缝隙全盖住了。主体迷雾移入地形 Shader 后，这圈缝隙裸露。

### 连带问题

- **外观断裂**：封皮用旧迷雾材质（`fogCoverMaterial`），与新版"纯色 + 整图图案"地形迷雾不一致，即使几何贴合，交界处颜色/图案也会断开。
- **高度**：真实轮廓顶点带地形高度，矩形在 `MaxY` 平面上，连接环带会形成斜面（俯视角通常无碍）。
- 场景里 `FogCover` 此前为 `m_Enabled: 0`（禁用），实施时需确认启用。

---

## 已确认的方案

**新建一个独立的"连接面片"网格**：内缘贴合地图真实不规则轮廓、外缘贴合封皮内边（矩形），且该面片**采样整图迷雾贴图**，让图案从未探索地形 → 连接面片 → 封皮一路无缝延续。

---

## 设计细节

### 1. 几何（带洞环形网格，复用现有管线）

```
外边界 = 现有封皮内边（矩形包围盒，GetFogVertices 现在的输出，+1 外扩）
洞     = 地图真实不规则轮廓（outerBoundaryVector3——已算出、目前被丢弃）
面片   = 矩形与真实轮廓之间的"锯齿环带"
```

- **数据来源零新算法**：新增输出方法（如 `GetFogConnectorBoundaries(out rect, out realOutline)`），把已算出的真实轮廓透出来；**不改动旧接口**，封皮逻辑完全不受影响。
- **三角剖分用现成的** `MeshGenerator.GenerateMesh(outerBoundary, holes, material)`——它本就是"外轮廓 + 洞"的剖分器（旧主体迷雾就是矩形 + 已探索洞）。这次只是洞恰好 = 整个地图轮廓。
- **归属**：作为第三个 FogCover 类物体（如 `FogConnector`），挂 `OnFogInit` 一起生成；一次性静态网格，探索状态变化**不需要重建**（它永远在地图外，恒为"未探索"）。

### 2. 贴图接入（关键：与整图迷雾同一套 UV）

给连接面片一个小 Shader（恒迷雾）：

```hlsl
// 与 FogBlend.cginc 完全同一公式
uv    = (worldPos.xz - _FogMapOrigin.xy) / _FogMapSize.xy;
color = _FogColor * lerp(1, tex2Dlod(_FogTex, uv), _FogTexAmount) * _FogEmission;
```

- 使用**同一组全局参数**（`_FogTex / _FogColor / _FogMapOrigin / _FogMapSize / _FogTexAmount / _FogEmission`），不新增任何 C# 设置代码——`MapRenderer.SetupFogGlobalShaderProperties` 已在设置。
- 面片在地图包围盒**之外**的部分 UV 会越界 [0,1]——迷雾贴图 wrap 已设 `Clamp`，越界自动取贴图边缘色、向外自然延伸不重复，正好覆盖"矩形四角比地图轮廓远"的区域。
- **恒未探索、纯自发光、不受光照、不投射阴影**——与地形迷雾未探索分支行为一致，交界处无明暗差。

### 3. 衔接质量两个细节

1. **内缘零缝隙**：洞的顶点就是边缘地块的 `SolidAreaVertices` 真实顶点（含扰动、含高度），与地形网格逐点重合。唯一需验证：轮廓点序/绕向符合剖分器对"洞"的约定（旧主体迷雾曾以已探索团块为洞剖分，支持任意六边形团块轮廓，风险低）。
2. **高度过渡**：环带从锯齿边真实高度斜向外缘 `MaxY` 统一平面。常规视角自然；若低角度斜面显眼，可后续把外缘统一抬到 `MaxY` 或压到边缘平均高。

### 4. 颜色连续性链条

完成后从内到外应为一体：

```
未探索地形（迷雾） → 连接面片 → FogCover → FogCover_two
   整图UV采样         同一UV公式    （建议材质也换同款Shader，
                      Clamp延伸      否则封皮仍是旧材质有色差）
```

> 建议：连接面片落地时，把 FogCover / FogCover_two 的材质也换成同款"恒迷雾"Shader，整条链条一个观感（作为可选第二步）。

---

## 涉及文件清单

| 文件 | 操作 |
|------|------|
| `Assets/Scripts/Core/Services/MeshDataGenerator.cs` | 修改：新增 `GetFogConnectorBoundaries`（输出矩形 + 真实轮廓，不动旧接口） |
| `Assets/Shader/FogCover.shader` | **新建**：恒迷雾 Shader（整图 UV + Clamp 延伸 + 纯自发光） |
| `Assets/Scripts/Managers/FogManager.cs`（或新建 FogConnector 逻辑） | 修改：`OnFogInit` 生成连接面片；封皮材质可选换新 Shader |
| `Assets/Scenes/GameScene.unity` | 修改：新增 `FogConnector` GameObject（或由代码动态创建） |

---

## 实施顺序

1. **第一步（核心）**：`GetFogConnectorBoundaries` + 恒迷雾 Shader + `FogConnector` 生成 —— 缝隙闭合、图案延续。
2. **第二步（可选）**：FogCover / FogCover_two 材质换同款 Shader —— 整条迷雾链条观感统一。

每步单独 git 提交，便于回滚。

---

## 验证清单

| 测试项 | 预期结果 |
|--------|----------|
| 地图边缘任意锯齿处 | 连接面片与地形网格逐点贴合，无缝隙、无背景透出 |
| 图案连续性 | 未探索地形 → 连接面片交界处图案/颜色无断裂 |
| 包围盒外区域 | Clamp 延伸取边缘色，无重复平铺痕迹 |
| 光照/阴影 | 连接面片不受光照、不投影、不被阴影压暗 |
| 探索推进后 | 连接面片不变（无需重建），地形侧正常揭开 |
| 低视角 | 高度斜面过渡可接受（如显眼，调外缘高度策略） |
| FogCover 启用状态 | 确认场景中 `m_Enabled` 已启用，封皮正常显示 |
