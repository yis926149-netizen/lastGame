# 战术卡影响范围遮罩 · 分步实施计划

> 目标：**拖拽战术卡时**，把「触点当前指向的地块 + 其 n 环」作为一个整体圈出来，
> 范围随触点指向的地块变化而实时改变。复用现有放置遮罩（红/绿）的整套技术路线
> （拓扑 → 拟合 → 投屏 → 填充 + 描边）。
>
> 关联文档：`不可放置区域红色遮罩实现方案.md`、`不可放置区域红色遮罩-实施问题记录.md`
> 关联代码：`Assets/Scripts/UI/PlacementMask/`

---

## 0. 先读：三条代码事实与已定处置

动手前必须知道，否则计划会建在错误前提上。三条都已由用户拍板处置方式。

### 事实 A：战术卡目前**没有**范围参数，范围是硬编码的 1 环

三处配置类都没有 range / radius / ringCount 字段：

| 文件 | 现有字段 |
| --- | --- |
| `Assets/Scripts/TacticalCard/Data/TacticalCardSO.cs:1-26` | `cardId / cardName / description / cardSprite / effectType / effect` |
| `Assets/Scripts/TacticalCard/Data/TacticalCardEffect.cs:5-21` | `healRatio, unitHealRatio, attackMultiplier, speedMultiplier, duration` |
| `Assets/GameConfig/Runtime/Data/TacticalCardBalanceData.cs:10-24`（Excel 主源，阶段6 唯一数值源） | 同上 + `cardId, legacyId, displayName, enabled, initialQuantity` |

实际范围写死在 `TacticalCardPresenter.cs:670-683`（`FindOwnBuildingsInOneRing`）
与 `:699-712`（`FindOwnUnitsInOneRing`）两份重复实现里：

```csharp
AddOwnBuildingOnCell(cell, result);
for (int d = 0; d < 6; d++)                     // ← 硬编码 1 环
{
    HexCellData neighbor = _mapDataService.GetNeighbor(cell, (Enums.HexDirection)d);
    if (neighbor == null) continue;
    AddOwnBuildingOnCell(neighbor, result);
}
```

> ✅ **处置（用户决策）：先将范围参数接入配置表。**
> 即把这份硬编码提升为 Excel 主源里的一个正式数值字段，走完整的配置链路
> （Excel → `TacticalCardBalanceData` → SO → Presenter）。见**步骤 2**。
>
> 由此确立一条硬约束：范围有了**唯一真源**，遮罩与结算必须读同一个值、
> 调同一个枚举函数（否则遮罩画的和实际生效的不一致 —— 这类欺骗性 bug 见 §5 R1）。

### 事实 B：悬停格只在**拖拽态**存在，且未对外暴露

- 悬停格 `hoveredCell` 是 `PlayerInputHandler.HighlightGridOnMouseHover()` 的**局部变量**
  （`PlayerInputHandler.cs:158`），算完即弃，未对外暴露。
- 但它每帧都被写进 private 缓存字段 `_lastDraggingHighlightCell`（`:35`、赋值在 `:184`）——
  **该字段就是「本帧触点指向的地块」，已经在维护了**。
- 该方法**只在拖拽态调用**：`Tick()` → `HandleCardDragging()` → `:128`
  `if (_isDraggingCard) HighlightGridOnMouseHover();`
- 提起态走的是**另一条与鼠标无关**的路径 `HandleRaisedCardPlacementPreview()`（`:218`），
  遍历全图做可放置二分；且 `_isDraggingCard` 为真时直接清空快照（`:222-226`）。

> ✅ **处置（用户决策）：本功能不在提起态，而是在拖拽态 —— 范围跟随触点指向的地块。**
>
> 这条把原计划**大幅简化**：所需数据在拖拽态**已经逐帧算好了**，
> 只需给 `_lastDraggingHighlightCell` 加一个只读属性暴露出来即可，
> **不需要改任何调用时机、不需要让它在提起态也跑**。见**步骤 3**（已从「改逻辑」降级为「纯提取」，
> R5 回归风险随之大降）。
>
> 连带影响：遮罩的生命周期锚定在 `_isDraggingCard` 上 —— 按下拖起出现、松手即消失，
> 与落点图标 / 单格高亮**共存亡**，语义天然一致。

### 事实 C：没有共享的"取 n 环格"与"六边形距离"工具

- `IMapDataService` 只有 `GetNeighbor(cell, dir)`（`:22`）和 `GetNeighbors(cell)`（`:23`）。
- 全仓库 grep `GetCellsInRange|GetNeighborsInRadius|GetCellsWithinDistance|GetRing|Spiral` → **零命中**。
- 唯一一份真正的 BFS n 环全收集：`ArrowTowerShooter.cs:248 CollectCellsInRange(center, maxRange)`（私有）。
- 六边形距离公式 `(|dx|+|dy|+|dz|)*0.5f` 被复制到**至少 9 处** private static
  （`UnitBrainBase.cs:615`、`UIController.cs:389`、`ArrowTowerShooter.cs:273`、
  `RangedStrategy.cs:192`、`MeleeStrategy.cs:186`、`UnitMovementSystem.cs:610`、
  `PublicBuildingGenerator.cs:429`、`ArenaEventManager.cs:609`、
  `MapVisualTransitionService.cs:500`、`RidgeGenerator.cs:570`）。
- 立方坐标字段：`HexCellData.HexCoordinate`（`HexCellData.cs:43-44`），类型是 `Vector3`，
  用 `.x/.y/.z`，满足 `x+y+z=0`。

> ✅ **处置（用户决策）：先简单实现一个，等未来重构再统一。**
> 本次只新建一个够用的 `HexRange`，**不**去动那 9 处已有副本（无关改动、扩大风险面）。
> 但要把它放在将来能被它们复用的位置（`Assets/Scripts/Utilities/`），
> 并在类注释里写明「已知有 N 处重复实现待统一」，给未来重构留线索。见**步骤 1**。

---

## 1. 需求与已定决策

**拖拽战术卡时**，圈出「触点当前指向的地块 + 其 n 环」，范围随触点指向的地块变化实时改变。
视觉上与放置遮罩（红/绿）同一套语言。

已确认的决策（用户拍板）：

1. **中心格不合法时照画**，但**描边与填充双双压暗**（表达「位置无效，但你能看到打出来会覆盖哪」）；
2. **只要中心格合法就画**（即：范围本身不受地形影响，纯坐标 n 环，山格/水格照进范围）；
3. **中心格自身不做额外强调**（不单独描圈）；
4. **范围参数接入配置表**（事实 A 的处置）；
5. **触发态是拖拽态**，不是提起态；范围跟随触点指向的地块（事实 B 的处置）；
6. **n 环工具先简单实现一个**，未来重构再统一（事实 C 的处置）。

由前几轮讨论确定的取舍：

7. **范围层开简化**（沿用 `SimplifyEpsilonInR = 0.6R`）。n 环是规则对称的「六边形的六边形」，
   DP 简化正好把 6 条直边的锯齿拟合成圆角大六边形 —— 这是对 n 环最忠实的视觉表达，
   玩家看的是「覆盖到哪」而非「有几格」。
8. **n = 0（仅中心格）不走遮罩**。实施记录 §5.1 已证明 0.6R 会让孤立单格塌成圆润小块。
   若出现 n=0 的战术卡，走现有 `HexHighlightRenderer` 单格高亮 —— 「单格指向」与
   「区域圈定」本就是两种语义。
9. **范围层独立成组件**，不塞进 `PlacementRangeMaskUI` 的 `Rebuild()`。理由见 §5 风险 R2。

> **与放置遮罩（红/绿）的关系**：红/绿是**提起态**的全图二分，本层是**拖拽态**的局部范围，
> 两者触发态**不同**。按现有代码，提起态与拖拽态互斥（`HandleRaisedCardPlacementPreview`
> 在 `_isDraggingCard` 为真时清空快照，`PlayerInputHandler.cs:222-226`），
> 故**红/绿与本层通常不会同屏**。这大幅缓解了原先担心的三层填充叠加问题（见 §5 R4）。

---

## 2. 现有可复用资产盘点

| 层 | 文件 | 复用度 | 说明 |
| --- | --- | --- | --- |
| 拓扑 | `PlacementMaskTopology.cs` | **100% 直接复用** | `Build(cells, outerRadius, elevationStep)` 对「这批格是什么语义」零认知 |
| 几何 | `PlacementMaskOutline.cs` | **100% 直接复用** | 纯几何（DP 简化 / 圆角 / 去重 / 缎带），连 `HexCellData` 都不认识 |
| 填充 | `PlacementMaskFill.cs` | **100% 直接复用** | 扫描线 + 偶奇规则，吃「处理后闭环列表」 |
| 绘制 | `PlacementRangeMaskGraphic.cs` | **100% 直接复用** | 吃 `(verts, tris, colors)` 的 `Graphic` |
| 参数 | `PlacementRangeMaskSettings.cs` | 模式复用 | 新建平行的 `TacticalRangeMaskSettings`，**不复用同一个实例** |
| 主控 | `PlacementRangeMaskUI.cs` | **0%，全是耦合** | 硬编码了快照来源、提起卡触发、4 个固定 graphic、红绿两组配色 |

结论：**技术路线本身几乎白拿**，要新写的只有「主控 + 参数 + 数据源」这一层。

---

## 3. 分步实施计划

每步独立可编译、可验证。建议按顺序做，每步做完即验证再进下一步。

### 步骤 1 · 新建 n 环枚举工具（先简单实现，不统一存量）

**新增**：`Assets/Scripts/Utilities/HexRange.cs`（静态类）

```csharp
public static class HexRange
{
    /// 立方距离（整数）。
    public static int Distance(Vector3 a, Vector3 b);

    /// 中心格 + 半径 n 内全部格（含中心），纯坐标、无地形过滤；
    /// 图外格由 mapData 自然裁掉。结果写进 result（复用缓冲，不分配）。
    public static void CollectInRange(
        IMapDataService mapData, HexCellData center, int radius, List<HexCellData> result);
}
```

要点：
- 实现用 **BFS 逐环扩张**（参照 `ArrowTowerShooter.cs:250-270` 的成熟结构），
  而非「遍历全图 + 距离判定」—— 后者在 598 格地图上每次触点移格都全扫，浪费。
- **`radius = 0` 返回仅中心格**；调用方自行决定 n=0 不画遮罩（决策 8）。
- 贴地图边缘时结果被自然裁成不规则形，格数 < `3n²+3n+1`，**这是正常的**。
- 带 `result` 出参而非返回新 List：本函数会在触点移格时高频调用。

**类注释里必须写明「待统一」线索**（决策 6 的配套）——否则这就成了第 11 处重复实现：

```
// ⚠️ 本类是「先简单实现、未来重构再统一」的产物（见 战术卡影响范围遮罩-分步实施计划.md 事实 C）。
// 仓库内已知的同类重复实现，将来统一时一并处理：
//   n 环收集：ArrowTowerShooter.cs:248 CollectCellsInRange
//   距离公式（(|dx|+|dy|+|dz|)*0.5f）至少 10 处 private static：
//     UnitBrainBase.cs:615 / UIController.cs:389 / ArrowTowerShooter.cs:273 /
//     RangedStrategy.cs:192 / MeleeStrategy.cs:186 / UnitMovementSystem.cs:610 /
//     PublicBuildingGenerator.cs:429 / ArenaEventManager.cs:609 /
//     MapVisualTransitionService.cs:500 / RidgeGenerator.cs:570
// 本次刻意不动它们：无关改动会无谓扩大回归面。
```

**验证**：新增单测 `Assets/Tests/HexRangeTests.cs`
- `n=0 → 1 格`；`n=1 → 7 格`；`n=2 → 19 格`；`n=3 → 37 格`（地图中心，不触边）
- 贴边缘时格数减少且不含 null
- 每格到中心的 `Distance ≤ n`，且恰好覆盖所有 `Distance ≤ n` 的格
- `Distance` 与现有副本公式结果一致（抽样对拍）

---

### 步骤 2 · 把范围参数接入配置表，并让结算改用共享枚举

这一步是**逻辑层**，与遮罩无关，但必须先做 —— 否则遮罩没有可读的 n，
且遮罩与结算会各算各的（见 §5 风险 R1）。

**这是决策 4 的落地**：范围从硬编码提升为走完整配置链路的正式数值字段。

**配置链路（四处需同步，缺一则值被冲掉）**：

```
Excel 表（数值主源，阶段6 唯一）
  → TacticalCardBalanceData.cs        新增字段 effectRadius
  → TacticalCardSO / TacticalCardEffect  新增字段 + 映射
  → TacticalCardPresenter             读取并传给 HexRange.CollectInRange
```

**改动**：
1. **Excel 表**新增 `effectRadius` 列（int，默认 1）。⚠️ 先确认本仓库的 Excel 列增改流程
   （表文件位置、是否需跑代码生成、生成物是否入库），照流程走，见 §5 R6
2. `Assets/GameConfig/Runtime/Data/TacticalCardBalanceData.cs` 增加对应字段
3. `Assets/Scripts/TacticalCard/Data/TacticalCardEffect.cs` 增加 `public int effectRadius;`
4. `TacticalCardSO` 侧按现有映射链路补齐（与其余数值字段同形）
5. `TacticalCardPresenter.cs:670-683 / :699-712` 两处 `FindOwn*InOneRing` 改为
   调用 `HexRange.CollectInRange(..., radius, ...)` 后再做目标层过滤
   —— **目标层过滤逻辑（Tag / IsDestroyed / PlayerIndex / hp）一律不动**，
   只替换「哪些格」这一层。方法名同步改掉 `InOneRing` 后缀（已不再是 1 环）

**默认值必须是 1**：现有全部战术卡的行为不能变（这是回归底线）。

**验证**：
- 默认 `effectRadius = 1` 时，Repair / BattleOrder 影响的建筑与单位集合**与改前完全一致**
  （建议临时打日志对拍一局）
- 把某张卡在 Excel 里调成 2 → 回灌 → 确认影响面扩大到 19 格口径
- **回灌一次后参数仍在**（专门验 R6）

> **关键**：从此刻起，「范围」有了唯一真源。遮罩层后面必须读同一个 `effectRadius`、
> 调同一个 `HexRange.CollectInRange`，绝不能自己再写一份 n 环。

---

### 步骤 3 · 暴露拖拽态悬停格（纯提取，零逻辑改动）

**改动**：`Assets/Scripts/Core/Services/PlayerInputHandler.cs`

由决策 5（触发态是拖拽态），本步骤**大幅简化**：所需数据已经在逐帧维护了。

`HighlightGridOnMouseHover()` 每帧把触点指向的地块写进 `_lastDraggingHighlightCell`
（`:184`），且该方法在拖拽态每帧调用（`:128`）。**这个字段就是我们要的数据**。

因此只需加一个只读属性：

```csharp
/// <summary>
/// 拖拽态下触点当前指向的地块（无命中 / 非拖拽态为 null）。纯数据，不产生任何视觉。
/// 复用 HighlightGridOnMouseHover 每帧维护的缓存，与落点图标 / 单格高亮同源同帧。
/// </summary>
public HexCellData DraggingHoveredCell => _isDraggingCard ? _lastDraggingHighlightCell : null;
```

**不需要做的事**（原计划里有，现已删除）：
- ~~把算悬停格与画高亮拆开~~ —— 不必，拖拽态本来就每帧在算
- ~~让它在提起态也更新~~ —— 不必，本功能不在提起态
- ~~改任何调用时机~~ —— 零改动

`_isDraggingCard` 的守卫是必要的：`_lastDraggingHighlightCell` 在
`ClearCardDragHighlight()`（`:137`）里已置 null，但加上守卫可防御任何遗漏路径，
让「非拖拽态恒为 null」成为属性自身的硬保证。

**验证**：
- 拖拽态下的落点高亮与图标行为**与改前逐帧一致**（只加属性、不动逻辑，理应完全不变）
- 松手后 `DraggingHoveredCell` 立即为 null
- 触点移出地图（射线不命中）时为 null，而非卡在上一格

---

### 步骤 4 · 新建范围遮罩参数类

**新增**：`Assets/Scripts/UI/PlacementMask/TacticalRangeMaskSettings.cs`

平行于 `PlacementRangeMaskSettings`，**不复用同一实例**（两者刷新频率、
视觉定位、标定口径都不同）。字段：

```
[显示开关]   ShowTacticalRangeMask = true

[配色·合法]  FillColor        低 alpha 底
             StrokeColor      高饱和描边

[配色·非法]  InvalidFillColor    ← 决策 1：中心格不合法时的压暗填充
             InvalidStrokeColor  ← 决策 1：中心格不合法时的压暗描边

[描边形状]   StrokeHalfWidth / StrokeCoreRatio /
             SimplifyEpsilonInR / CornerRadiusInR / CornerSegments / MergeEpsilonLocal

[立体感]     Shadow : PlacementMaskShadowSettings   ← 直接复用已有的 shadow 参数类

[重建阈值]   CamMoveSqrThreshold / CamRotThresholdDeg
```

**配色建议（初值，待真机标定）**：
- 色相取**红绿都撞不上**的 —— 白或亮青（cyan）最稳；黄色紧挨绿边会糊。
  （虽然按决策 5 红/绿与本层通常不同屏，但仍建议避开：一是防将来两态被打通，
  二是金黄/柔红已被拖拽落点单格高亮占用，见 `HexHighlightRenderer.PlaceableGlowColor`。）
- 描边**比红/绿略粗**（红是 12f，范围层建议 14~18f 起标）：范围是局部小区域，
  要在地形底上跳出来；但不必像原先设想的那样为压过红/绿满屏底而给到 20f。
- 非法态：同色相压暗 + 降饱和（不要换成红色 —— 会与单格高亮的柔红混淆）。

---

### 步骤 5 · 新建范围遮罩主控组件

**新增**：`Assets/Scripts/UI/PlacementMask/TacticalRangeMaskUI.cs`

结构照抄 `PlacementRangeMaskUI`（拓扑 → `PrepareLoops` → 填充 + 描边两层），
但有三处**必须不同**：

#### 5.1 脏检测 —— 这是全案最容易埋雷的地方

**绝对不能抄 `PlacementRangeMaskUI` 的「快照数量变化」脏标记**（`:148-150`）。

理由：n 固定时格数恒为 `3n²+3n+1`。触点在地图内部从任何一格移到任何另一格，
**格数一动不动** → 用数量当脏键会 100% 失效，遮罩完全粘死。
（贴地图边缘时被裁、格数才会变，于是症状是「地图中间不刷新、划到边上又突然跳一下」，
这种半灵不灵最难归因。）

正确脏键：

```
(拖拽卡引用, 触点指向格的 HexCoordinate, effectRadius, 中心格是否合法, 相机变换)
```

其中**触点指向格的坐标是核心**。用 `HexCoordinate`（值）而非格对象引用做键更稳。

> 可借鉴的现成写法：`HighlightGridOnMouseHover` 的
> `if (hoveredCell != _lastDraggingHighlightCell)`（`:170`）就是同一思路的最小版本 ——
> 本层只是把它扩成含 radius / 合法性 / 相机的复合键。

#### 5.2 触发条件与清空

```
非拖拽态（DraggingHoveredCell 为 null）→ 清空
拖拽的不是战术卡                      → 清空
effectRadius <= 0                     → 清空（决策 8：n=0 不走遮罩）
```

生命周期与落点图标 / 单格高亮**共存亡**：按下拖起出现、松手即消失。
清理点复用现有语义即可 —— `ClearCardDragHighlight()`（`:131-141`）已是
「松手 / 暂停 / `CardController.ClearHighlights`」全部收尾路径的唯一汇合点，
本层只要依赖 `DraggingHoveredCell` 返回 null 就自动跟着清空，无需额外挂钩。

「拖拽的是不是战术卡」的判定：拖拽态已缓存 `_draggingCardData` / `_draggingDropHandler`
（`:122-123`），据此判断即可（战术卡的 DropHandler 是 `TacticalCardPresenter`）。

#### 5.3 配色按中心格合法性切换（决策 1）

合法性判定**必须复用**放置资格的统一入口，不能自己写一份：
`ICardDropHandler.CanDeployTo(cardData, cell)`
（战术卡侧实现见 `TacticalCardPresenter.cs:481-485`，目前是 `return cell != null;`）。

```csharp
bool valid = dropHandler.CanDeployTo(cardData, hoveredCell);
Color fill   = valid ? _settings.FillColor   : _settings.InvalidFillColor;
Color stroke = valid ? _settings.StrokeColor : _settings.InvalidStrokeColor;
```

> 注：战术卡当前 `CanDeployTo` 恒为「非 null 即可」，故非法态实际只在
> 触点脱离地图时出现（而那时按 §5.2 已整体清空）。**压暗分支目前几乎跑不到**，
> 但仍要实现 —— 将来战术卡加了放置限制（如「只能落在己方领土」）时即刻生效，
> 且 `PlayerInputHandler` 侧的单格高亮已经在用同一套金/红二态（`:174-176`），
> 语义须保持一致。

#### 5.4 图层与渲染顺序

- 2 个 graphic：`TacticalRangeMask_Fill` / `TacticalRangeMask_Stroke`
- 建在**放置遮罩之后**（sibling 更靠后 = 渲染在上）。两者通常不同屏（决策 5），
  但顺序仍应确定，以防将来两态被打通
- `CreateGraphic` 里**必须显式 `AddComponent<CanvasRenderer>()`** —— 本工程已知坑，
  漏了会静默不出 mesh 且不报错（见 `UITrailRenderer.cs:269-276`）
- `raycastTarget = false` —— 拖拽中遮罩绝不能吞掉指针事件，否则松手放置直接失效

---

### 步骤 6 · Zenject 绑定

**改动**：`Assets/Scripts/Infrastructure/Installers/GameInstaller.cs`

照抄 `PlacementRangeMaskUI` 的绑定形态（`:216-227`）：

```csharp
[Header("战术卡影响范围遮罩")]
[SerializeField] private UI.PlacementMask.TacticalRangeMaskSettings _tacticalRangeMaskSettings
    = new UI.PlacementMask.TacticalRangeMaskSettings();

Container.Bind<UI.PlacementMask.TacticalRangeMaskUI>()
         .FromNewComponentOnNewGameObject()
         .WithGameObjectName("TacticalRangeMaskUI")
         .AsSingle()
         .OnInstantiated<UI.PlacementMask.TacticalRangeMaskUI>(
             (ctx, mask) => mask.Settings = _tacticalRangeMaskSettings)
         .NonLazy();
```

`NonLazy` 不能漏：没有其他消费者解析它，不立即构造就永远不会开始每帧驱动。
Settings 走 Installer 序列化字段的原因同放置遮罩：组件是运行时新建对象，
自身 Inspector 不随场景保存、无法在 Play 之前调。

---

### 步骤 7 · 验证与真机标定

**编译 / 单测**
```
dotnet build MainGame.csproj      # 期望 0 error
dotnet build Tests.csproj         # 期望 0 error
cd Temp/runner && dotnet run      # HexRangeTests + PlacementMaskTopologyTests 全 PASS
```
（反射宿主会连带跑整个 `Tests.dll`，其余测试类因缺 Unity 运行时/源码路径而失败，
属宿主环境限制，只看这两个类。）

**编辑器目验清单**
1. **拖拽**战术卡 → 触点在地图上移动 → 范围圈**实时跟随**（重点验 §5.1 的脏键）
2. 触点在地图**内部**连续移动多格 → 每一格都刷新（格数不变，专门验证不会粘死）
3. 圈的形状是圆角大六边形，n=1 → 7 格、n=2 → 19 格口径
4. 圈**贴到地图边缘**时被正确裁切，不越界、不闪烁
5. **松手** → 圈立即消失，与落点图标 / 单格高亮同时消失（不残留、不闪一帧）
6. 触点移出地图（射线不命中）→ 圈消失，而非卡在上一格
7. 拖拽**非战术卡**（普通单位/建筑卡）→ 圈不出现，红/绿放置遮罩行为不变
8. **提起态**（未拖拽）→ 圈不出现，红/绿遮罩行为与改前完全一致
9. 圈中心格与拖拽落点单格高亮**始终是同一格**（同源验证，见步骤 3）
10. 相机推拉旋转 → 圈跟随，描边粗细恒定
11. 拖拽中在圈上松手 → **能正常放置**（验 `raycastTarget = false`，见 §5.4）
12. Excel 把某卡 `effectRadius` 调成 2 → 回灌 → 圈变 19 格口径，**且结算影响面同步变化**
    （R1 的端到端验证：看到的 = 打出来的）

**真机标定**（外部，需实机）
- `FillColor` / `StrokeColor` / `InvalidFillColor` / `InvalidStrokeColor`
- `StrokeHalfWidth`（初值建议 14~18f）
- `Shadow.Offset`（小范围区域建议先给 0 或很小值）

> ⚠️ 标定完成后**直接改 Installer 上的序列化值并存盘**。参照调试手册
> 「文档/笔记里记录的『表现常量』可能是陈旧值」条 —— 代码/场景里的值是当前事实，
> 文档里的初值只是候选，冲突时以前者为准。

---

## 4. 改动文件汇总

| 文件 | 类型 | 步骤 | 备注 |
| --- | --- | --- | --- |
| Excel 战术卡数值表 | 改 | 2 | 新增 `effectRadius` 列，**流程待确认**（R6） |
| `Assets/Scripts/Utilities/HexRange.cs` | 新增 | 1 | |
| `Assets/Tests/HexRangeTests.cs` | 新增 | 1 | |
| `Assets/GameConfig/Runtime/Data/TacticalCardBalanceData.cs` | 改 | 2 | |
| `Assets/Scripts/TacticalCard/Data/TacticalCardEffect.cs` | 改 | 2 | |
| `Assets/Scripts/TacticalCard/Data/TacticalCardSO.cs`（及映射链路） | 改 | 2 | |
| `Assets/Scripts/TacticalCard/.../TacticalCardPresenter.cs` | 改 | 2 | 两处 `FindOwn*InOneRing` |
| `Assets/Scripts/Core/Services/PlayerInputHandler.cs` | 改 | 3 | **仅加一个只读属性** |
| `Assets/Scripts/UI/PlacementMask/TacticalRangeMaskSettings.cs` | 新增 | 4 | |
| `Assets/Scripts/UI/PlacementMask/TacticalRangeMaskUI.cs` | 新增 | 5 | |
| `Assets/Scripts/Infrastructure/Installers/GameInstaller.cs` | 改 | 6 | |

**不动**：`PlacementMaskTopology.cs` / `PlacementMaskOutline.cs` / `PlacementMaskFill.cs` /
`PlacementRangeMaskGraphic.cs` / `PlacementRangeMaskUI.cs` / `PlacementRangeMaskSettings.cs`。
四个底层文件直接复用，放置遮罩（红/绿）本身零改动。

---

## 5. 风险与对策

### R1 · 遮罩与结算口径不一致（**最高危**）
遮罩画 n 环、结算实际按另一套逻辑 → 玩家看到的和打出来的不符，是欺骗性 bug，
且测试很难发现（两边各自都"对"）。

**对策**：步骤 2 强制两边共用 `HexRange.CollectInRange` 与同一个 `effectRadius`。
遮罩层**绝不允许**自己再写一份 n 环枚举。若将来结算加了地形过滤，遮罩必须同步。
目验清单第 12 条做端到端验证。

### R2 · 刷新频率错配导致性能塌方
若把范围层塞进 `PlacementRangeMaskUI.Rebuild()`，则**触点每移一格都会连带把
598 格的红层整个重建一遍** —— 而红层压根没变。

两层的刷新频率差一个数量级：红/绿是「提起卡变化 + 相机移动」，本层是「触点移格」。
且两者触发态本就不同（提起 vs 拖拽），塞在一起还会让脏检测逻辑互相污染。

**对策**：独立组件（决策 9）。这是坚持拆开的**主要理由**，比代码整洁重要得多。

### R3 · 脏检测抄错 → 遮罩粘死
见 §5.1。格数在 n 固定时恒定，「数量变化」脏键必然失效。

**对策**：脏键必须含触点指向格的坐标。目验清单第 2 条专门测这个。

### R4 · 填充 alpha 叠加 → 脏色（**已因决策 5 大幅缓解**）
原先的担心是：红∪绿 = 全图、屏幕已被填充铺满，范围层再叠一层就是实施记录
问题 C 那个「深浅不均」的老毛病（而当年的 Stencil 方案已随路线三删除）。

**但决策 5 把触发态定在拖拽态后，红/绿与本层通常不同屏**
（`HandleRaisedCardPlacementPreview` 在 `_isDraggingCard` 为真时清空快照，
`PlayerInputHandler.cs:222-226`），这条风险从「必然发生」降为「仅在将来两态被打通时发生」。

**对策**：仍按「低底 + 强调边」口径给填充较低 alpha（与红/绿同口径），
真机标定时观察。退路不变：范围层彻底不出填充层（`BuildFillLayer` 整段跳过）。
另需注意本层填充会盖在**拖拽落点单格高亮**之上，标定时一并看这两者是否打架。

### R5 · 步骤 3 的回归风险（**已因决策 5 大幅降低**）
原计划要改 `HighlightGridOnMouseHover` 的调用时机（让它在提起态也跑），
那是现役拖拽落点高亮的代码路径，风险不小。

**决策 5 之后这一步降级为纯提取**：只加一个只读属性，
`_lastDraggingHighlightCell` 的赋值时机、缓存语义、高亮重建逻辑**一行不动**。

**对策**：坚持「只加属性、不动逻辑」的边界。目验以「拖拽高亮与图标行为逐帧一致」为底线。

### R6 · Excel 配置链路未走全 → 新参数被回灌冲掉
`TacticalCardBalanceData` 是阶段6 唯一数值主源。只改 SO 不改 Excel 列，
下次回灌会把 `effectRadius` 冲回默认值。

**对策**：步骤 2 的四处链路必须同步改完。**动手前先确认本仓库的 Excel 列增改流程**：
表文件在哪、是否需要跑代码生成、生成物是否入库。改完必须实际回灌一次验证参数仍在
（目验清单第 12 条）。

---

## 6. 建议的落地顺序

**先做步骤 3（最小、最独立）**：只加一个只读属性，几分钟完事，
且能立刻确认「拖拽落点高亮无回归」这条底线。先把它排掉，后面出问题时可以排除这一层。

**再做步骤 1 → 2（纯逻辑层）**：不碰任何视觉，可独立验证（单测 + 结算对拍）。
务必先确认默认 `effectRadius = 1` 时行为与改前完全一致，并**实际回灌一次 Excel**
验证参数不被冲掉（R6），再进视觉层。这一段是全案风险最集中的部分 ——
配置链路走不通、或结算行为变了，后面全白做。

**最后步骤 4~6（遮罩本体）**：此时所有依赖都已就位且各自验证过，
出问题能快速定位在哪一层。

> 步骤 3 与步骤 1→2 相互独立，也可并行；但都必须在步骤 5 之前完成。

---

## 7. 本次刻意**不做**的事

- **不**统一那 10 处重复的 `HexDistance` / `CubeDistance` 副本，也不动
  `ArrowTowerShooter.CollectCellsInRange`（决策 6：先简单实现，未来重构再统一；
  但步骤 1 的类注释里要留下线索）
- **不**抽象通用的 `HexRegionMaskRenderer`（等第二个用例真正落地、
  确认哪些参数需要 per-layer 之后再抽；此刻抽出的接口大概率缺字段或多字段，
  带着错的抽象去接第二个用例比写两遍再合并更贵）
- **不**改放置遮罩（红/绿）的任何现有行为与已标定参数
- **不**让本层在提起态出现（决策 5：只在拖拽态）
- **不**改 `HighlightGridOnMouseHover` 的逻辑与调用时机（决策 5 的连带收益）
- **不**给中心格加额外强调（决策 3）
- **不**给范围加地形过滤（决策 2：纯坐标 n 环）

---

## 8. 待确认事项

1. **Excel 战术卡数值表的列增改流程**（阻塞步骤 2）：表文件位置、是否需跑代码生成、
   生成物是否入库。这是唯一一个动手前必须先问清的外部依赖。
2. 是否存在（或计划存在）`effectRadius = 0` 的战术卡 —— 若有，按决策 8 走单格高亮而非遮罩。
