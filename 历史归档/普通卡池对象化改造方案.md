# 普通卡池对象化改造方案（方案 C，复核修订版）

> 目标：把普通卡（单位卡 + 建筑卡）的卡池从“硬编码 ID 数组 + 平行列表数据库”重构为与天赋卡相同方向的 ScriptableObject 对象引用模式。
>
> 本文档仅是实施计划，尚未修改运行时代码和 Unity 资产。

---

## 一、复核结论

方案 C 可实施，但原计划不能直接执行。复核当前代码、资产、场景和程序集后，已确认以下阻断点：

1. AI 手牌实际存放在 `AIPlayerState.AICardState` 的 `List<int> HandCardIds` 和 `int NextCardId` 中，原计划漏掉了该状态模型。
2. 玩家拖拽出牌只能从 `ICardView.CardID` 取回 int；如果卡牌对象化，必须同步修改 `ICardView` 和 `CardController`，否则配置对象在视图层丢失。
3. `CardData` 同时被普通卡和战术卡使用，战术卡还使用负数 `ID` 表示槽位。不能把 `CardData` 改成只支持普通卡配置。
4. 原计划中的 `NormalCardEntry` 一处写成顶层类型、一处写成 `NormalCardPoolSO.NormalCardEntry`，并且保底字段类型与返回类型不一致。
5. 普通卡当前是“逐张、有放回、使用命名 `System.Random` 随机流”；天赋卡是“一批候选无放回、使用 `UnityEngine.Random`”。不能直接复制 `TalentCardPoolResolver` 的抽法。
6. 当前初始手牌是严格的“3 张随机 + 1 张固定箭塔 + 1 张随机”。把箭塔放进随机池并不能等价保留该行为。
7. `BuildingDatabase.asset` 实际为：建筑模型 6 项、建筑卡面 6 项、基础 HP 12 项。原计划写成三个列表均为 6 项，会导致迁移静默丢数据或越界。
8. 迁移脚本若放在裸 `Assets/Editor` 下，缺少一个显式引用 `MainGame` 的 Editor-only asmdef。
9. 新增 `NormalCardPoolSO` 后必须把资产引用写入 `GameScene` 的 `GameInstaller`，否则 `ValidateRequiredReferences()` 会在启动时抛错。
10. `BarracksSpawner` 是运行时 `AddComponent` 创建的，不能靠 Inspector 给它拖入 `UnitConfigSO`。

修订后的核心原则：

- 配置层对象化，运行时稳定 ID 保留。
- 普通卡池直接引用单位/建筑配置 SO。
- 不再使用“单位数量偏移”拼接普通卡 ID。
- 不把天赋卡的无放回抽取语义强行复制给普通卡。
- 旧字段与新字段必须在过渡期并存，先迁移资产，再切换运行时，最后清理。

---

## 二、当前代码和资产基线

### 2.1 当前普通卡抽取链

```text
CardUnlockRuleProvider.GetUnlockedCardIds(techLevel, cultureLevel)
    -> List<int>（当前实际仅加入 6 张建筑卡）
CardGenerationRule.GenerateNextCardId(..., System.Random)
    -> 首张固定返回 0（移民），后续有放回随机取一个 int
CardService.GenerateNextCardID() / AICardBrain.GenerateCardId()
    -> 玩家 CardPresenter 或 AI AICardState
```

### 2.2 当前复合 Card ID 规则

- 单位卡：`cardId == unitId`。
- 建筑卡：`cardId == unitCount + buildingId`。
- 类型判断：`cardId < unitCount`。
- 建筑 ID 还原：`buildingId = cardId - unitCount`。

需要删除的是这套“普通卡复合 ID”规则，而不是删除全项目的单位 ID 和建筑 ID。

### 2.3 当前真实池内容

`CardUnlockRuleProvider` 当前代码把单位卡加入逻辑注释掉了，所以随机池实际上是：

```text
建筑 ID：3、4、5、2、0、1（即全部 6 个普通建筑）
首张保底：单位 ID 0（移民），不在随机池中
```

`CardUnlockRuleProviderTests` 仍然期待旧科技/文化累计解锁行为，已经与生产代码不一致。迁移时不能把该测试当成当前产品规则。

### 2.4 真实资产规模与异常

| 资产 | 当前内容 |
|---|---|
| `UnitDatabase.asset` | unitModels/unitDatas/Cards/unitIcons/skillIcons 各 12 项 |
| `BuildingDatabase.asset` | CityModel 1 项、buildingModels 6 项、buildingCards 6 项、buildingBaseHP **12 项** |
| `ExplorationRewardConfigSO.asset` | `rewardUnitIDs` 当前保存 2 和 5 |
| `GameScene.unity` | 已绑定 UnitDatabase、BuildingDatabase、TalentCardPool 等，但没有 NormalCardPool 字段 |

### 2.5 已确认的产品决策（2026-08-03）

| # | 决策 | 内容 |
|---|---|---|
| 1 | 建筑 HP 异常 | `buildingBaseHP` 后 6 项确认为垃圾数据，迁移时删除，只取前 6 项与模型/卡面对齐；迁移工具仍须在长度不一致时中止并报告，由脚本按决策显式截断 |
| 2 | 单位卡进池 | 随机池包含全部 12 张单位卡 + 全部 6 张建筑卡，共 18 张；移民卡（单位 ID 0）同时作为首张保底卡并保留在随机池中，开局后仍可能再次抽到 |
| 3 | 开局纯随机 | 删除"3 随机 + 1 固定箭塔 + 1 随机"的临时逻辑，开局 5 张手牌全部从随机池抽取（第一张仍触发移民保底），预告卡同样纯随机 |
| 4 | 普通卡无重复概念 | 普通卡不做"不重复"约束，保持有放回随机，手牌允许出现多张同配置卡；"重复/堆叠"概念只属于战术卡 |
| 5 | AttackInterval 保持原样 | 见 2.6，迁移时保持现有序列化值 0 原样复制，不做修复 |
| 6 | 探索奖励对象化 | `rewardUnitIDs` 改为 `UnitConfigSO[]` 引用，资产中的 ID 2、5 迁移为对应 config 引用，玩家/AI 两条调用链同步（见 7.5） |
| 7 | 攻击音效配置化 | `UnitMovementController` 的 UnitID 音效 switch 改为读取 `UnitConfigSO` 中的音效配置（见 7.7） |
| 8 | 迁移执行方式 | 迁移工具由开发助手（Kilo）在 Unity 编辑器中通过 MCP 执行 `Tools/Normal Card Pool/Migrate`，用户无需手动操作 |
| 9 | 提交方式 | 按实施阶段分次提交，每个阶段独立 commit |

### 2.6 `AttackInterval` 历史数据处置

`AttackInterval` 的用途：单位两次攻击之间的最小间隔（秒）。`UnitBrainBase.MarkAttacked()` 在每次攻击结算后用它设置攻速冷却，冷却结束前 `CanAttack` 被骨架层屏蔽（`UnitBrainBase.cs:50-54`）。

当前 `UnitDatabase.asset` 中 12 个单位的 `AttackInterval` 全部序列化为 0，而代码默认值为 1.5f。序列化的 0 会直接生效，即**所有单位无攻速冷却**（两次攻击间无间隔限制）。

决策：保持现有序列化值 0 原样迁移，不修改数值（无攻速冷却视为既有设计）。迁移报告须记录该字段保持 0，不通过重新调用构造函数改变数值。

### 2.7 运行时 ID 必须保留的原因

以下系统仍依赖稳定 int ID：

- `CharacterData.UnitID`；
- `UnitData.id`；
- 玩家、AI、探索奖励、兵营等非卡牌生成入口；
- 单位策略、信息面板等现有逻辑；
- `BuildingData` 和 `Enums.BulidingType` 的现有运行时状态。

本次改造让数据库、普通卡池、探索奖励和攻击音效配置对象化，但不要求所有外围系统立刻放弃 int ID；Provider 的按 ID 查询接口继续保留。

---

## 三、目标领域模型

### 3.1 统一普通卡配置基类

使用统一基类替代原计划中“单位引用 + 建筑引用二选一”的 `NormalCardEntry`，避免空引用组合和类型歧义。

```csharp
public abstract class NormalCardConfigSO : ScriptableObject
{
    public Sprite cardSprite;
}
```

`NormalCardPoolSO` 可以直接保存：

```csharp
public List<NormalCardConfigSO> cards = new();
```

Unity 对 `ScriptableObject` 派生类引用支持多态序列化，列表中可以同时放 `UnitConfigSO` 和 `BuildingConfigSO`。

### 3.2 `UnitConfigSO`

建议字段：

| 字段 | 来源/用途 |
|---|---|
| `UnitData unitData` | 旧 `unitDatas[i]`；`unitData.id` 作为单位 ID 的唯一来源 |
| `GameObject unitModel` | 旧 `unitModels[i]` |
| `Sprite cardSprite` | 旧 `Cards[i]`，继承自基类 |
| `Sprite unitIcon` | 旧 `unitIcons[i]` |
| `Sprite skillIcon` | 旧 `skillIcons[i]` |
| `UnitStrategyType strategyType` | Settler / Ranged / Melee，替代策略工厂中的 0/3/5/9 魔法数 |
| `AttackSfxConfig attackSfx` | 攻击音效配置（音效名 + 延迟回放节奏），替代 `UnitMovementController` 的 UnitID 音效 switch（见 7.7） |

不再额外添加一个可编辑的 `UnitConfigSO.id`，避免与 `UnitData.id` 形成两个事实来源。可提供只读属性：

```csharp
public int Id => unitData != null ? unitData.id : -1;
```

迁移和 `OnValidate` 必须校验 `unitData != null`、ID 非负且唯一。

按已确认决策 5，`AttackInterval` 保持现有序列化值 0 原样迁移（见 2.6）；迁移工具不能通过重新调用构造函数无意改变字段值。

### 3.3 `BuildingConfigSO`

建议字段：

| 字段 | 来源/用途 |
|---|---|
| `int buildingId` | 保留旧建筑 ID，必须唯一、非负 |
| `Enums.BulidingType buildingType` | 显式存储，消除 `(buildingId + 1)` |
| `GameObject buildingModel` | 旧 `buildingModels[i]` |
| `float baseHP` | 旧 `buildingBaseHP[i]`（待先处理 12 项异常） |
| `Sprite cardSprite` | 旧 `buildingCards[i]`，继承自基类 |
| `bool blocksMovement` | 替代 `buildingId == 0 || buildingId == 1` |

建筑迁移应使用显式 ID 到枚举映射表，而不是只写 `(Enums.BulidingType)(i + 1)`。当前映射为：

| buildingId | buildingType |
|---|---|
| 0 | AttackStatue |
| 1 | DefenseStatue |
| 2 | Altar |
| 3 | TechnologyAndCultural |
| 4 | Barracks |
| 5 | ArrowTower |

### 3.4 `UnitDatabaseSO` 和 `BuildingDatabaseSO`

最终结构：

```text
UnitDatabaseSO.units = List<UnitConfigSO>
BuildingDatabaseSO.buildings = List<BuildingConfigSO>
BuildingDatabaseSO.cityModel = GameObject
```

过渡期必须保留旧平行列表和新字段，待资产迁移与运行时切换全部完成后再删除旧字段。

两个现有数据库资产必须**原地修改并保留 `.meta` 和 GUID**，禁止删除后重建，否则 `GameScene` 中的引用会断开。

### 3.5 `NormalCardPoolSO`

建议字段：

```csharp
public class NormalCardPoolSO : ScriptableObject
{
    public List<NormalCardConfigSO> cards = new();
    public UnitConfigSO guaranteedFirstCard;
}
```

- `cards`：普通随机池，按已确认决策 2 为 12 张单位卡 + 6 张建筑卡（共 18 张）。
- `guaranteedFirstCard`：首张保底移民卡（单位 ID 0），替代 `return 0`；移民卡同时存在于随机池中，开局后仍可能再次抽到。
- 开局固定箭塔逻辑已按决策 3 删除，不再需要 `initialHandFixedCard` 字段。

本阶段不预留 `poolWeight`。若未来需要不同卡池使用不同权重，权重应属于“池条目”而不是全局卡配置，届时再增加专门的加权条目结构。

---

## 四、抽卡职责和随机语义

### 4.1 不直接复制 `TalentCardPoolResolver`

天赋卡一次抽多个候选且无放回；普通卡当前逐张抽取且允许重复（按决策 4，普通卡没有"不重复"概念）。普通卡继续保持：

- 有放回随机；
- 同一张卡可以连续出现，手牌允许存在多张同配置卡；
- 玩家继续使用 `SeedService.GetRandom("Card")`；
- AI 继续使用 `AIRandomProvider.Random`；
- 不切换到 Unity 全局 `Random`。

因此本次不新增与天赋卡完全同构的 `DrawRandom(pool, count)` resolver，避免职责重复和随机行为变化。

### 4.2 唯一职责链

```text
NormalCardPoolSO
    -> CardUnlockRuleProvider.GetUnlockedCards()
    -> CardGenerationRule.GenerateNextCard(..., System.Random)
    -> 玩家 CardService / AI AICardBrain
```

建议接口：

```csharp
public interface ICardUnlockRuleProvider
{
    IReadOnlyList<NormalCardConfigSO> GetUnlockedCards();
    UnitConfigSO GetGuaranteedFirstCard();
}
```

`CardUnlockRuleProvider` 注入 `NormalCardPoolSO`，不再依赖 `IUnitDataProvider`、`IBuildingDataProvider` 和科技/文化参数。

`CardGenerationRule` 接收已过滤卡列表、保底卡和 `System.Random`：

- 首次且保底卡有效：返回保底配置；
- 后续池非空：有放回随机返回一个配置；
- 后续池为空：为保持当前回退 0 的语义，可回退保底配置；
- 保底配置也为空：配置错误，初始化/校验阶段应直接失败，不能静默返回 null。

---

## 五、玩家卡牌完整数据流

### 5.1 `CardData` 保持通用视图模型

`CardData` 同时服务普通卡和战术卡，必须保留现有可写字段：

```csharp
public int ID { get; set; }
public Sprite CardSprite { get; set; }
public bool IsUnit { get; set; }
```

新增可选字段：

```csharp
public NormalCardConfigSO NormalCardConfig { get; set; }
```

规则：

- 普通卡：`NormalCardConfig != null`，普通卡业务不再依赖复合 `ID` 判断类型。
- 战术卡：`NormalCardConfig == null`，继续显式设置负数 `ID`、卡面和 `IsUnit=false`。
- `ID` 对普通卡仅保留兼容/调试用途：单位卡写 `UnitConfigSO.Id`，建筑卡写 `BuildingConfigSO.buildingId`；该值允许跨卡种重复，必须结合 `NormalCardConfig` 类型理解，不能再承担“单位数量偏移”的业务语义。

### 5.2 `ICardView` / `CardController`

`ICardView` 需要新增只读数据出口，例如：

```csharp
CardData Data { get; }
```

`CardController` 返回其 `_data`。现有 `CardID` 继续保留，确保战术卡负数 ID 和其他兼容逻辑不受影响。

### 5.3 玩家出牌链

```text
CardService.GenerateNextCard()
    -> NormalCardConfigSO
CardPresenter 构造独立 CardData
    -> ICardView.SetData()
CardController 保存 CardData
拖拽结束 -> CardPresenter 读取 view.Data.NormalCardConfig
    -> UnitConfigSO：SpawnUnit(config, position)
    -> BuildingConfigSO：SpawnBuilding(config, position)
```

每次抽卡仍创建独立 `CardData` 运行时对象。ScriptableObject 仅作为不可变配置引用，禁止把手牌数量、临时费用、冷却等运行时状态写入 SO。

### 5.4 玩家初始化发牌（按决策 3：纯随机）

按已确认决策 3，删除固定箭塔逻辑，开局改为纯随机：

```text
5 次 GenerateNextCard（第一次触发移民保底）
1 次 GenerateNextCard 生成预告卡
```

开局行为与改造前相比：不再固定包含一张箭塔卡。

---

## 六、AI 卡牌完整数据流

### 6.1 `AIPlayerState.AICardState` 必须修改

当前：

```csharp
List<int> HandCardIds;
int NextCardId = -1;
```

改为：

```csharp
List<NormalCardConfigSO> HandCards;
NormalCardConfigSO NextCard;
```

- `NextCard == null` 替代 `NextCardId < 0` 和 `-1` 哨兵。
- 手牌允许保存同一 SO 的多个引用，等价于当前有放回抽到重复卡。
- `List.Remove(config)` 每次只移除一个匹配引用，符合“打出一张”的语义。
- 如未来普通卡拥有逐张运行时状态，应把 AI 手牌改为独立 `NormalCardInstance`，本阶段无需提前引入。

### 6.2 AI 出牌链

```text
GenerateCard() -> NormalCardConfigSO
    -> AICardState.HandCards / NextCard
GetCardPriority(config)
TryPlaySingleCard(config)
    -> UnitConfigSO：AIEntityFactory.GenerateUnit(config, position)
    -> BuildingConfigSO：AIEntityFactory.GenerateBuilding(config, position)
```

优先级等价迁移：

- `UnitStrategyType.Settler`：100；
- `BuildingConfigSO.buildingType == TechnologyAndCultural`：90；
- 其他单位：70；
- 其他建筑：60。

---

## 七、数据库 Provider 和非卡牌生成入口

### 7.1 Provider 最终职责

`IUnitDataProvider` 保留现有按 ID 查询方法，内部改为按 `UnitConfigSO.Id` 查找，而不是 `units[unitId]`：

```text
GetUnitPrefab(int)
GetUnitData(int)
GetUnitIcon(int)
GetSkillIcon(int)
GetCard(int)
GetUnitConfig(int) / TryGetUnitConfig(int, out ...)
```

`IBuildingDataProvider` 同理：

```text
GetCityModel()
GetBuildingPrefab(int)
GetBuildingBaseHP(int)
GetBuildingCards(int)
GetBuildingConfig(int) / TryGetBuildingConfig(int, out ...)
```

强制查询不存在 ID 时应抛带 ID 和数据库名的明确异常；可选查询返回 false。配置列表顺序允许调整，不能重新依赖列表索引。

### 7.2 Count API 删除时机

`GetUnitIconCount()` 和 `GetBuildingCardsCount()` 只有在所有普通卡复合 ID 调用点清零后才能删除。不能在消费层尚未切换时提前删除，否则中间阶段无法编译。

### 7.3 单位策略

`UnitStrategyFactory.Create(int)` 最终改为 `Create(UnitStrategyType)` 或 `Create(UnitConfigSO)`。所有仍以 int ID 进入的生成路径先通过 Provider 解析为 config，再创建策略。

需要保持并测试现有映射：

- 0 -> Settler；
- 3、5、9 -> Ranged；
- 其他 -> Melee。

### 7.4 兵营

`BarracksSpawner` 由玩家和 AI 建筑生成器动态 `AddComponent`，因此本次不把 `_spawnUnitID` 改成“Inspector 引用”。可选方案：

- 最小改动：保留 `_spawnUnitID = 1`，生成时用 Provider 解析 `UnitConfigSO`；
- 完全配置化：在 `BuildingConfigSO` 增加 `producedUnit`，`SetupBarracksSpawner` 后显式 `Initialize(producedUnit)`。

本次推荐第二种，但必须增加初始化方法和 null 配置校验，不能依赖 Inspector。

### 7.5 探索奖励（按决策 6：对象化）

探索奖励虽不是普通卡池的一部分，但按已确认决策 6 一并对象化：

```csharp
// 现状
int[] rewardUnitIDs
int RollUnitID()

// 改为
UnitConfigSO[] rewardUnits
UnitConfigSO RollUnitConfig()
```

影响链（必须同步修改）：

- `ExplorationRewardConfigSO`：字段改型；空数组不再回退魔法 ID 2，返回 null 并让调用方安全失败；
- `ExplorationRewardSystem`：`SpawnUnitsWithOverflow` 改为取 config 并生成（`IPlayerUnitSpawnService` 若保留 int 签名则传 `config.Id`，若对象化则传 config）；
- `AIAutoExplorer`：`_aiFactory.GenerateUnit(config, spawnPos)`；
- `IPlayerUnitSpawnService`：确认最终签名（推荐保持 `SpawnPlayerUnit(int, Vector3)`，由奖励系统取 `config.Id`，减少接口波及）；
- 资产迁移：`ExplorationRewardConfigSO.asset` 中 `rewardUnitIDs` 的 2、5 必须显式映射为 `UnitConfigSO` 引用，Unity 不会自动完成 `int[] -> UnitConfigSO[]` 转换。

### 7.6 `BuildingData`

普通建筑生成拿到 `BuildingConfigSO` 后，应增加从 config 初始化 `BuildingData` 的路径，直接使用 `buildingType` 和 `baseHP`。主城仍可保留现有 `GetCityModel()` / Provider 路径。

### 7.7 攻击音效配置化（按决策 7）

`UnitMovementController` 中按 `characterData.UnitID` 的 switch 音效（`UnitMovementController.cs:461-506`）改为读取单位配置：

- `UnitConfigSO` 增加可序列化的 `AttackSfxConfig`：主音效名 + 延迟回放列表（次数/延迟秒），覆盖现有各单位的多次 `Invoke` 节奏；
- `UnitMovementController` 注入 `IUnitDataProvider` 或从 `CharacterData` 取得 `UnitConfigSO`，按配置播放音效；
- 迁移时把现有 1..11 号单位的音效映射逐项填入对应 `UnitConfigSO`，保证行为等价；
- 删除 switch 后，`UnitID` 不再承担音效分支职责。

---

## 八、资产迁移设计

> 执行方式（决策 8）：迁移工具由开发助手（Kilo）在 Unity 编辑器中通过 MCP 执行 `Tools/Normal Card Pool/Migrate` 菜单，用户无需手动操作。

### 8.1 Editor 程序集

新增专用目录和 asmdef：

```text
Assets/Editor/NormalCardMigration/NormalCardMigration.Editor.asmdef
Assets/Editor/NormalCardMigration/NormalCardPoolMigration.cs
```

asmdef 必须：

- 仅包含 Editor 平台；
- 显式引用 `MainGame`；
- 不复用 `ConsoleLogCopier.Editor` 程序集。

### 8.2 过渡字段顺序

迁移脚本可编译、可执行的正确顺序：

1. 新增配置 SO 类型。
2. 在 `UnitDatabaseSO` / `BuildingDatabaseSO` 中新增目标字段，同时保留全部旧字段。
3. 工程编译成功。
4. 运行迁移工具，读取旧字段并写入新字段。
5. 运行时切换到新字段。
6. 最终清理提交中同时删除旧字段和引用旧字段的迁移工具。

### 8.3 迁移前预检（全部通过才允许写入）

- 单位五个列表长度均为 12；
- 单位 ID 非负、唯一，且与预期旧索引一致；
- 建筑模型 6、卡面 6、HP 12：按决策 1 只取前 6 个 HP，脚本显式截断并输出"已删除后 6 项"报告；
- 所有源 prefab/sprite/data 非空或明确允许为空；
- 目标资产路径无冲突；
- 已有目标资产时采用“原地更新并保留 GUID”，禁止盲目删除重建；
- 按决策 5，12 个单位 `AttackInterval` 保持原序列化值 0；
- 探索奖励 `rewardUnitIDs` 中的 2、5 必须能映射到存在的 `UnitConfigSO`。

### 8.4 迁移写入

1. 为 12 个单位创建或幂等更新 `UnitConfigSO`（`AttackInterval` 保持原值 0，音效按现有 switch 映射填入 `attackSfx`）。
2. 为 6 个建筑创建或幂等更新 `BuildingConfigSO`（HP 取前 6 项）。
3. 原地更新 `UnitDatabase.asset` 和 `BuildingDatabase.asset` 的新列表，保留原 GUID。
4. 创建 `NormalCardPool.asset`：随机池放 12 张单位卡 + 6 张建筑卡（共 18 张），保底指向单位 0。
5. 迁移 `ExplorationRewardConfigSO.asset`：`rewardUnits` 指向单位 2、5 对应的 config。
6. 每个建筑输出“旧 ID / prefab / card sprite / buildingType / blocksMovement”映射报告。
7. 保存并刷新 AssetDatabase。

### 8.5 幂等性与失败恢复

- 写入前完整预检，避免半迁移。
- 重跑时更新已有 config 资产，不创建重复文件，不改变 GUID。
- 失败时输出已创建/已修改资产清单。
- 不自动覆盖用户手工修改过且与源数据不同的 config；应中止并报告冲突。

### 8.6 场景绑定

`GameInstaller` 增加 `_normalCardPoolSO` 后必须：

1. 给 `GameScene` 中的 `GameInstaller` 写入 `NormalCardPool.asset` 引用；
2. 保存场景；
3. 验证场景 YAML 中 `_normalCardPoolSO` GUID 非空；
4. 验证原 UnitDatabase / BuildingDatabase GUID 未改变。

该绑定必须在切换 `CardUnlockRuleProvider` 之前完成；不能把 Installer/场景迁移留到卡牌运行时代码切换后的下一阶段。

---

## 九、精确影响面

### 9.1 新增生产文件

| 文件 | 作用 |
|---|---|
| `NormalCardConfigSO.cs` | 普通卡统一基类 |
| `UnitConfigSO.cs` | 单位配置对象 |
| `BuildingConfigSO.cs` | 建筑配置对象 |
| `NormalCardPoolSO.cs` | 普通卡池 + 首张保底 + 初始固定卡 |
| `UnitStrategyType.cs` | Settler / Ranged / Melee（也可并入 UnitConfigSO） |

不新增 `NormalCardPoolResolver`，抽取继续由 `CardGenerationRule` 使用 `System.Random` 完成。

### 9.2 新增 Editor 文件

| 文件 | 作用 |
|---|---|
| `Assets/Editor/NormalCardMigration/NormalCardMigration.Editor.asmdef` | Editor-only，引用 MainGame |
| `Assets/Editor/NormalCardMigration/NormalCardPoolMigration.cs` | 一次性、幂等资产迁移工具 |

### 9.3 必须修改的生产文件

| 文件 | 修改内容 |
|---|---|
| `UnitDatabaseSO.cs` | 过渡期新增 `List<UnitConfigSO>`，最终删除旧平行列表 |
| `BuildingDatabaseSO.cs` | 过渡期新增 `List<BuildingConfigSO>` + `cityModel`，最终删除旧列表 |
| `UnitDataProvider.cs` | 按显式 ID 查 config，新增 config API，最终删 Count API |
| `BuildingDataProvider.cs` | 按显式 ID 查 config，新增 config API，最终删 Count API |
| `CardUnlockRuleProvider.cs` | 注入 NormalCardPoolSO，删除硬编码数组和 tech/culture 参数 |
| `CardGenerationRule.cs` | 返回 `NormalCardConfigSO`，保留有放回 `System.Random` 抽取 |
| `CardService.cs` | `GenerateNextCardID()` 改为返回配置对象 |
| `CardData.cs` | 增加可选 `NormalCardConfigSO`，保持战术卡旧字段兼容 |
| `ICardView.cs` | 暴露完整 `CardData`，继续保留 `CardID` |
| `CardController.cs` | 实现 `Data`，保持普通卡和战术卡共用 |
| `CardPresenter.cs` | 发牌、拖拽、单位/建筑生成改用 config；开局改为纯随机（决策 3） |
| `AIPlayerState.cs` | AI 手牌和预告牌从 int 改为配置引用 |
| `AICardBrain.cs` | 排序、出牌、类型判断、优先级改用 config |
| `AIEntityFactory.cs` | 单位/建筑生成内部解析或直接接收 config |
| `UnitStrategyFactory.cs` | 从配置策略类型创建策略 |
| `BarracksSpawner.cs` | 动态组件显式初始化产出配置，或至少通过 Provider 解析旧 ID |
| `BuildingData.cs` | 增加 BuildingConfigSO 初始化路径 |
| `UIController.cs` | 移民/远程判断改读单位 config，不再使用 0/3/5/9 |
| `ExplorationRewardConfigSO.cs` | `rewardUnitIDs` 改为 `UnitConfigSO[]`；`RollUnitID()` 改为 `RollUnitConfig()` |
| `ExplorationRewardSystem.cs` | 奖励生成改取 config（决策 6） |
| `AIAutoExplorer.cs` | `GenerateUnit` 调用点传 config |
| `IPlayerUnitSpawnService.cs` | 确认最终签名（默认保持 int，奖励系统取 `config.Id`） |
| `UnitMovementController.cs` | 攻击音效 switch 改为读取 `UnitConfigSO.attackSfx`（决策 7） |
| `GameInstaller.cs` | 序列化、绑定、校验 NormalCardPoolSO |

### 9.4 资产/场景修改

- `UnitDatabase.asset`（原地更新，GUID 不变）；
- `BuildingDatabase.asset`（原地更新，GUID 不变）；
- `ExplorationRewardConfigSO.asset`（`rewardUnitIDs` 2/5 → config 引用）；
- 新建 12 个 UnitConfig 资产；
- 新建 6 个 BuildingConfig 资产；
- 新建 `NormalCardPool.asset`；
- `GameScene.unity` 写入新池引用。

### 9.5 兼容回归但默认不改

| 文件/系统 | 验证内容 |
|---|---|
| `TacticalCardPresenter.cs` | 负数 CardData.ID、CardController 共用和拖拽仍正常 |
| `UIManager.cs` | Provider 改造后卡面、技能图标查询仍正常 |
| `GameFlowManager.cs` | 主城模型与主城 BuildingData 初始化正常 |

---

## 十、测试计划

### 10.1 修改现有测试

| 文件 | 修改内容 |
|---|---|
| `CardUnlockRuleProviderTests.cs` | 按真实池配置测试，不再测试已移除的科技/文化累计解锁 |
| `CardServiceTests.cs` | 去掉无关 Unit/Building Provider mock；测试首张保底、后续属于随机池、Reset 后再次保底 |
| `GameInstallerTests.cs` | 增加 `_normalCardPoolSO` 缺失引用断言 |

### 10.2 新增 EditMode 测试

- `CardGenerationRuleTests`：首张保底仅一次、池内有放回抽取（允许重复）、空池回退、固定随机源稳定。
- `UnitDataProviderTests`：列表顺序打乱后仍按显式 ID 查到正确 config；重复/缺失 ID 行为明确。
- `BuildingDataProviderTests`：同上，并验证 buildingType/baseHP。
- `NormalCardMigrationTests`：源字段逐项迁移（含 `AttackInterval` 保持 0）、HP 截断为前 6 项、幂等重跑、GUID 稳定。
- `ExplorationRewardMigrationTests`：`rewardUnitIDs` 2/5 正确映射为 config 引用；空数组不再回退魔法 ID 2。
- `CardDataCompatibilityTests`：单位卡、建筑卡、战术卡三种数据不会互相误判。
- `UnitStrategyFactoryTests`：0、3、5、9 迁移后策略等价。
- `AttackSfxTests`（可选）：各单位音效配置与旧 switch 等价。

### 10.3 PlayMode / 场景验证

当前 `Assets/Tests/Tests.asmdef` 仅包含 Editor 平台。若要自动化真实场景和动态组件测试，应新增独立 PlayMode 测试程序集。

至少执行以下人工或 PlayMode 冒烟：

1. `GameScene` 启动，Installer 无缺失引用异常。
2. 玩家初始手牌：5 张纯随机（第一张为移民保底），无固定箭塔。
3. 玩家后续抽卡允许重复，预告牌与补牌正常。
4. 单位卡和建筑卡拖拽后生成正确 prefab/config。
5. AI 初始手牌、预告牌、排序、出牌正常。
6. 兵营动态添加后能生成配置的单位。
7. 探索奖励按 `UnitConfigSO` 引用生成单位（原 ID 2/5 行为不变）。
8. 战术卡负 ID、堆叠数量、借出/归还和拖拽效果正常。
9. 主城、普通建筑血量和建筑类型映射与改造前一致。
10. 各单位攻击音效与改造前一致（决策 7）。

---

## 十一、分阶段实施顺序

> 提交方式（决策 9）：每个阶段独立 commit；迁移工具由开发助手在 Unity 编辑器中执行（决策 8）。

| 阶段 | 内容 | 必须保持 |
|---|---|---|
| 0. 决策定稿 | 本章决策表作为最终基线（决策 1-9） | 不修改运行时 |
| 1. 过渡模型 | 新增 5 个配置类型；数据库新增新字段但保留旧字段；新增 Editor asmdef 和迁移工具 | 工程可编译，运行时仍读旧字段 |
| 2. 资产与安装迁移 | 预检后创建/更新 config 和 pool；原地更新数据库新字段；迁移探索奖励资产；GameInstaller 增加池绑定并保存 GameScene 引用 | 旧资产 GUID 不变；迁移可幂等重跑；场景可启动 |
| 3. Provider 与实体生成 | Provider 切换 config；改策略、单位/建筑生成、兵营、探索奖励、UI 类型判断和攻击音效 | int 入口仍兼容；卡牌管线暂可继续旧 ID |
| 4. 卡牌状态与视图 | 同步修改 CardData、ICardView、CardController、CardService、CardPresenter、AIPlayerState、AICardBrain；开局改纯随机 | 战术卡兼容；玩家/AI 随机语义不变 |
| 5. 全量验证 | EditMode、PlayMode/人工冒烟、资产引用扫描 | 所有验收项通过 |
| 6. 清理 | 同一提交删除数据库旧字段和引用旧字段的迁移工具；删除 Count API 和复合 Card ID 残留 | 清理后仍可编译和启动 |

每个阶段都必须保持工程可编译。迁移工具和旧字段应在同一个清理提交中一起删除，避免工具引用不存在字段导致编译失败。

---

## 十二、完成标准

- 普通随机池内容完全由 `NormalCardPoolSO.cards` 决定（12 单位 + 6 建筑）。
- 首张保底由 `guaranteedFirstCard` 配置，不再硬编码 `return 0`。
- 开局为纯随机（第一张触发移民保底），不再硬编码固定箭塔。
- 玩家和 AI 普通卡状态都持有配置对象，不再依赖复合 Card ID。
- 普通卡保持有放回随机（无"不重复"约束），并继续使用玩家/AI各自的 `System.Random`。
- UnitDatabase/BuildingDatabase 不再使用平行列表作为运行时唯一数据源。
- Provider 按显式 ID 查找，配置列表排序不影响运行结果。
- 建筑类型不再通过 `buildingId + 1` 推导。
- 12 个单位 `AttackInterval` 保持原值 0（决策 5，无攻速冷却为既有设计），迁移报告已记录。
- 探索奖励使用 `UnitConfigSO[]` 引用（原 ID 2/5 行为不变）。
- 攻击音效由 `UnitConfigSO.attackSfx` 配置驱动，不再使用 UnitID switch。
- 普通卡对象化不破坏战术卡的 `CardData` 和 `CardController` 共用逻辑。
- 原 UnitDatabase/BuildingDatabase 资产 GUID 和场景引用保持稳定。
- 建筑 HP 后 6 项垃圾数据已删除，迁移报告有明确记录。
- 所有相关 EditMode 测试通过，玩家、AI、兵营、探索奖励、战术卡和主城完成冒烟验证。

---

## 十三、本次明确不做

- 不把普通卡改成天赋卡那样的单批无放回三选一。
- 不实现普通卡权重抽取。
- 不恢复已移除的科技/文化解锁系统。
- 不实现卡牌逐张运行时状态（`NormalCardInstance`）；手牌直接保存配置引用，未来需要费用修正/冷却等状态时再引入。
