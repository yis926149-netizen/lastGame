# 阶段 5 运行时接入-完成说明与验证清单

## 一、目标回顾

按 `Excel配置表混合架构修改计划.md` 阶段 5「迁移地图、经济、流程和AI硬编码」，本轮收口建议顺序的**前五项**：
1. **经济和探索费用**；
2. **游戏流程时间参数**；
3. **AI 节奏与出牌优先级**；
4. **地图生成与竞技场参数**；
5. **战斗公式共享系数**。

（剩余子项 6：动画/手感参数，见文末「已知缺口」。）

## 二、数据流

```
Excel（唯一数值源，新增 2 张全局表）
  → ConfigExporter 导出 CSV/JSON
  → Unity 导入器（Tools/游戏配置/导入并校验）
  → 只读 SO（EconomyConfigDatabaseSO / GameFlowConfigDatabaseSO）
  → Provider（Excel 优先，Legacy 默认值回退）
  → 运行时消费点
```

## 三、本次改动清单

### 1. Schema（`Config/Schema/配置表结构.json`）新增 5 张单行全局表

| 表 | 稳定 ID | 字段 |
|---|---|---|
| 经济配置 | economy.main | startingGold=100、baseIncomePerTick=2、aiIncomeBonusPerTick=6、incomeTickInterval=1.0、explorationCostFallback=50、cardCostFallback=10 |
| 游戏流程配置 | gameflow.main | gameDurationSeconds=300、dayNightCycleSeconds=300（与时长解耦）、noonLightIntensity=1.2、sunsetLightIntensity=0.4、countdownUrgentThreshold=60、settlementDelaySeconds=1.5、endGameUiDelaySeconds=6.5、annexationRecalcDepth=3、fogTransitionSpeed=0.5 |
| AI配置 | ai.main | cardPlayInterval=1.5、exploreInterval=1.5、globalActionMinInterval=1.0、settlerCardPriority=100、technologyCardPriority=90、unitCardPriority=70、buildingCardPriority=60、militaryRewardOverflowRings=5 |
| 战斗公式 | battle_formula.main | riverDefensePenalty=-0.5、attackStatueBonus=0.7、highGroundAttackBonus=0.5、highGroundRangeBonus=1、meleeAlertRange=3 |
| 地图生成参数 | map_gen.main | perlinFrequency=0.05、perlinOctaves=3、perlinPersistence=0.6、arenaRadius=3、arenaRiseDurationSeconds=1.2 |

### 2. Unity 数据类 + 只读 SO
- `Assets/GameConfig/Runtime/Data/EconomyConfigData.cs` / `.../Database/EconomyConfigDatabaseSO.cs`
- `Assets/GameConfig/Runtime/Data/GameFlowConfigData.cs` / `.../Database/GameFlowConfigDatabaseSO.cs`
- `Assets/GameConfig/Runtime/Data/AIConfigData.cs` / `.../Database/AIConfigDatabaseSO.cs`
- `Assets/GameConfig/Runtime/Data/BattleFormulaConfigData.cs` / `.../Database/BattleFormulaConfigDatabaseSO.cs`
- `Assets/GameConfig/Runtime/Data/MapGenConfigData.cs` / `.../Database/MapGenConfigDatabaseSO.cs`

### 3. 导入器扩展（`Assets/GameConfig/Editor/Import/GameConfigImporter.cs`）
- 新增 `EconomyConfigAssetPath` / `GameFlowConfigAssetPath` / `AIConfigAssetPath` / `BattleFormulaConfigAssetPath` / `MapGenConfigAssetPath`；
- `BuildEconomyConfig` / `BuildGameFlowConfig` / `BuildAIConfig` / `BuildBattleFormulaConfig` / `BuildMapGenConfig`（单行表解析，空表报错）；
- ImportAll 循环接入 + `CreateOrReplace` 生成 SO。

### 4. Provider
- `Assets/Scripts/Core/Services/Resource/EconomyConfigProvider.cs` — 起始金币/基础收入/AI 补贴/结算周期/费用兜底；
- `Assets/Scripts/Core/Services/GameFlowConfigProvider.cs` — 时长/昼夜周期/光照/紧急阈值/结算延迟/吞并深度/迷雾速度；
- `Assets/Scripts/AI/AIConfigProvider.cs` — AI 出牌/探索节奏、全局操作间隔、出牌优先级、军事奖励溢出搜索环数；
- `Assets/Scripts/Core/Services/BattleFormulaRule.cs`（静态 Configure）— 河流惩罚/雕像加成/高低地加成/高地射程/近战警戒范围；
- `Assets/Scripts/Core/Services/MapGenConfigProvider.cs` — Perlin 频率/八度/持续、竞技场半径/动画时长。

### 5. 消费点改造（删除旧 const / 默认值字段，改读 Provider）
| 文件 | 改动 |
|---|---|
| `Resource/GoldWallet.cs` | `StartingGold`/`PassiveIncomePerTick`/`ExplorationCost`/`CardCost` 由 `{get;set;}=默认值` 改为构造注入 `EconomyConfigProvider` 的只读属性 |
| `Resource/GoldIncomeService.cs` | `IncomeInterval`/`AIIncomeBonusPerTick` 改读 Provider |
| `Core/Services/GlobalTimerService.cs` | `const DefaultDurationSeconds` 改为注入 `GameFlowConfigProvider` 的 `DefaultDuration` 属性 |
| `TalentCard/TalentCardSelectionUI.cs` | `StartTimer(GlobalTimerService.DefaultDurationSeconds)` → `_timer.DefaultDuration` |
| `Controllers/SunCycleController.cs` | `CycleDuration`/`HalfCycle`/光照 1.2/0.4 改读 Provider（角度常量保留） |
| `UI/GlobalTimerUI.cs` | 紧急阈值 60 改读 Provider |
| `Turn/EndGame.cs` | 结算延迟 1.5s / UI 延迟 6.5s 改读 Provider |
| `Territory/TerritoryService.cs` | `LogisticsService.MaxAnnexationRecalcDepth` 改读 Provider |
| `Core/Services/FogTransitionManager.cs` | `TransitionSpeed` 由 public 字段改为静态 `Configure` + 只读属性 |
| `AI/AICardTicker.cs` | `CardInterval=1.5`、全局间隔 `1f` 改读 `AIConfigProvider` |
| `AI/AIAutoExplorer.cs` | `ExploreInterval=1.5`、全局间隔 `1f`、`maxRings=5` 改读 `AIConfigProvider` |
| `AI/AICardBrain.cs` | `GetCardPriority` 的 100/90/70/60 改读 `AIConfigProvider` |
| `Core/Services/Exploration/ExplorationRewardSystem.cs` | `maxRings=5` 改读 `AIConfigProvider` |
| `Core/Services/CombatResolver.cs` | 河流 -0.5 / 雕像 +0.7 / 高低地 ±0.5 改读 `BattleFormulaRule` |
| `Controllers/BuildingBase.cs` | 雕像 +0.7 改读 `BattleFormulaRule` |
| `Controllers/UnitMovementController.cs` | 河流 -0.5 / 雕像 +0.7 / 高低地 ±0.5 / 高地射程 +1 改读 `BattleFormulaRule` |
| `UI/UIController.cs` | 高地射程 +1 改读 `BattleFormulaRule` |
| `Controllers/ArrowTowerShooter.cs` | 高地射程 +1 改读 `BattleFormulaRule` |
| `Units/RangedStrategy.cs` | 高地射程 +1 改读 `BattleFormulaRule` |
| `Units/MeleeStrategy.cs` | `const AlertRange=3` 改读 `BattleFormulaRule` |
| `Managers/MapGenerator.cs` | Perlin 噪声 0.05/3/0.6 改读 `MapGenConfigProvider` |
| `Managers/ArenaEventManager.cs` | `const ArenaRadius=3` / `const ArenaRiseDurationSeconds=1.2` 改读 `MapGenConfigProvider` |

### 6. DI 绑定（`Infrastructure/Installers/GameInstaller.cs`）
- 新增 `_economyConfigSO` / `_gameFlowConfigSO` / `_aiConfigSO` / `_battleFormulaConfigSO` / `_mapGenConfigSO` 字段 + 条件绑定；
- 绑定 `EconomyConfigProvider` / `GameFlowConfigProvider` / `AIConfigProvider` / `MapGenConfigProvider`；
- `FogTransitionManager.Configure(...)` 静态注入迷雾速度；`BattleFormulaRule.Configure(...)` 静态注入战斗公式系数。

## 四、验证清单

1. **关闭占用 Excel 的进程**（WPS 中打开的 `游戏数值配置.xlsx`），然后执行：
   ```powershell
   dotnet Tools/ConfigExporter/src/ConfigExporter/bin/Debug/net8.0/ConfigExporter.dll init --schema Config/Schema/配置表结构.json --output Config/Excel/游戏数值配置.xlsx
   dotnet Tools/ConfigExporter/src/ConfigExporter/bin/Debug/net8.0/ConfigExporter.dll --input Config/Excel/游戏数值配置.xlsx --schema Config/Schema/配置表结构.json --output Config/Generated --strict
   ```
   确认 CSV 输出新增 `经济配置.csv` / `游戏流程配置.csv` / `AI配置.csv` / `战斗公式.csv` / `地图生成参数.csv`。
2. Unity 打开项目等待编译；菜单 `Tools/游戏配置/导入并校验`，确认 5 个 `*ConfigDatabase.asset` 生成、Console 无错误。
3. 打开 `GameInstaller`，填入 5 个新 SO 字段。
4. 运行游戏核对：
   - 开局金币 100、被动收入 2/s、AI 补贴 6/tick、结算周期 1s；
   - 倒计时 300s、昼夜周期 300s、剩余 ≤60s 触发紧急脉冲、结算/UI 延迟 1.5s/6.5s、吞并深度 3、迷雾过渡 0.5/s；
   - AI 出牌节奏 1.5s、探索间隔 1.5s、全局操作间隔 1s、出牌优先级（移民100/科文90/单位70/建筑60）、军事奖励溢出搜索 5 环；
   - 战斗：河流防御 -0.5、雕像 +0.7/座、高地攻 ±0.5、高地射程 +1 格、近战警戒 3 格；
   - 地图：Perlin 噪声频率 0.05/八度 3/持续 0.6、竞技场半径 3、突起动画 1.2s。
5. 回归：`Assets/Tests` 现有用例（`new GoldWallet()`/`new GlobalTimerService()`/`new LogisticsService(map)` 等无参/少参调用）应仍编译通过（新构造参数均带默认值）。

## 五、已知缺口（阶段 5 剩余子项 + 阶段 6 收口）

- **阶段 5 子项 6**：动画/手感参数（相机、地图变异动画时长表、探索/UI 特效）待确认是否配表；
- **AI 未配表项（架构性，暂缓）**：AI 阵营索引=1 多处写死（`AICardBrain`/`AIAutoExplorer`/`AIEntityFactory`/`EnemyModelManager`）、AI 选天赋/落点均匀随机（无权重策略）、AI 每回合补牌=1 张（逻辑而非数值）；
- **阶段 6**：删除 Legacy 默认值回退（各 Provider 的 `?? fallback`），并清理旧字段/const。
