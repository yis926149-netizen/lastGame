# 受击反馈事件层改造计划

## 背景与范围裁剪

讨论受击表现方案（伤害飘字 vs 受击闪红）后确认：**当前 `Assets/Model/` 下的角色与建筑均为 demo 资产，后续会整体更换**。

因此本计划**只做与模型资产无关的部分**，把依赖模型层级/材质的闪红实现推迟到换模型之后。

### 为什么闪红要推迟

前期调查得到的全部实现依据，都是当前 demo 资产的属性，换模型后逐条作废：

| 调查结论 | 换模型后 |
|---|---|
| `swordsman_blue` 等含 23 个 SkinnedMeshRenderer + 19 个 MeshRenderer（多为禁用的换装备选件） | 作废，新模型层级不同 |
| 7 个角色 prefab 共享同一材质 `WK_Standard_Units.mat`（guid `c805af27...`） | 作废，共享关系不复存在 |
| 建筑 prefab 自身 0 个 Renderer，模型是嵌套 PrefabInstance（如 `City_Red` → `building_castle_red.fbx`） | 作废 |
| 项目实际跑内置管线（`GraphicsSettings.m_CustomRenderPipeline: 0`，材质 shader `fileID: 10752/10755`），属性名为 `_Color` / `_EmissionColor` | 很可能变——新资产常带 URP 材质，届时若启用 URP，属性名变 `_BaseColor`，`_EMISSION` keyword 一整套坑随之更换 |

附带风险：若现在把闪红调到手感满意，选新模型时会不自觉偏向"能复用这套闪红"的资产（层级简单、材质独立）。**不应让临时资产的技术约束反向限制美术选型。**

### 本次范围

- ✅ **阶段一**：事件层扩展（纯逻辑，换模型零改动）
- ⬜ **阶段二**（可选）：Transform 形变打击感（只依赖 Transform，换模型零改动）
- ⏸ **阶段三**：受击闪红——**换模型后再做**，本计划不含

阶段一现在做的理由不是提前量，而是**三个伤害发布点此刻已全部摸清**。日后重找"哪些地方结算了伤害"，要重新趟一遍多格转发、建筑走 `BuildingAttacked` 而非 `Resolve` 等弯路。

---

## 阶段一：事件层扩展 ✅（已完成）

### 目标

`DamageEventBroker` 当前事件签名只携带世界坐标，表现层拿不到受击对象，无法驱动任何作用于对象自身的反馈。

```csharp
// Assets/Scripts/Core/Services/DamageEventBroker.cs:18
public event Action<Vector3, float, bool, int> DamageDealt;
```

而坐标**无法反查对象**——锚点取的是血条位置而非模型位置（`CombatResolver.cs:59-64`、`BuildingBase.cs:164-166`、`ArrowTowerShooter.cs:224-226`），射线或距离反查会错配。

### 设计：新增独立事件，不改现有签名

```csharp
/// <summary>
/// 受击表现事件。参数：受击对象根节点（单位为 model，建筑为自身 gameObject）。
/// 与 DamageDealt 分离：飘字与受击反馈的过滤规则（如迷雾）、生命周期、参数各不相同。
/// </summary>
public event Action<GameObject> UnitHit;

public void RaiseHit(GameObject target)
{
    if (target == null) return;
    UnitHit?.Invoke(target);
}
```

**为什么不扩 `DamageDealt` 的签名**：飘字与受击反馈在过滤规则（迷雾是否显示）、生命周期、参数上都不同，合并会让两者互相牵制；且改签名要同时改动订阅方 `DamageFloatTextRenderer`，扩大改动面。

### 三个发布点

均在现有 `RaiseDamage` 调用之后补一行，条件与之一致（`damage > 0f`）：

| 文件 | 位置 | 传入对象 |
|---|---|---|
| `CombatResolver.cs` | :47 单位互攻结算 | `target.model` |
| `BuildingBase.cs` | :162-168 建筑受击 | `gameObject` |
| `ArrowTowerShooter.cs` | :222-231 箭矢命中结算 | `target` |

注意：`ArrowTowerShooter` 与 `BuildingBase` 的 broker 是 `[Inject(Optional = true)]`，补的代码需沿用现有 `_damageEventBroker != null` 判空。

### 待定的设计问题（记录，不阻塞阶段一）

- **多格建筑**：`CombatResolver.GetBuildingTarget`（:91-109）会把子格攻击转发到根格。受击反馈应整体触发还是只触发被打格？倾向整体（与"多格共享一份 HP"一致），但属设计决定。
- **迷雾过滤**：飘字当前不做可见性过滤（`DamageFloatTextRenderer.cs:12` 注释），迷雾中敌打敌也出字。受击反馈是否过滤需另行决定。

### 验收

- 三处发布点均能触发 `UnitHit`，参数非空。
- 现有飘字行为完全不变（`DamageDealt` 未改动）。
- 无订阅者时不产生任何开销与报错。

---

## 阶段二（可选）：Transform 形变打击感

### 目标

在不碰 Renderer / 材质的前提下提供受击确认感，作为换模型前的过渡方案。手感上与闪红解决同一问题（"这一下打中了"），且在卡通 / 低多边形风格中本就常见——不少休闲策略游戏只做形变不做染色。

### 做法

新增 `HitFeedbackPresenter`，订阅 `UnitHit`，对受击对象根节点做 `DOPunchScale`（或沿受击方向 `DOPunchPosition`）。

- 只依赖 `Transform`，**换模型后零改动**。
- 时长控制在 0.2s 内，幅度宜小；过长会从"被打"变成"抖动"。
- 重复受击需 `DOTween.Kill(transform)` 后重开，避免叠加。
- 必须 `SetLink(go)`——死亡那一击也会触发反馈，而死亡流程（`UnitRemovalService.RemoveUnit`）可能同时销毁对象。此约定与飘字一致（`DamageFloatTextRenderer.cs:133`）。

### 接入

对齐飘字的既有模式：在 `MapPresentationBootstrap` 中创建（参照 `EnsureDamageFloatTextRenderer`，:478-494），broker 由 `GameInstaller` 注入。

### 风险

- 单位若有移动 / 攻击动画在跑，punch 可能与动画争夺 Transform，需实机验证。
- 多目标同时受击时辨识度不如闪红（形变幅度小，余光不易捕捉）。属过渡方案的已知代价。

---

## 阶段三：受击闪红（换模型后再做）

**不在本次范围**。以下为换模型后重新评估时的起点，届时须以新资产重新核实，勿直接沿用。

### 换模型后需重新确认

1. 渲染管线是否已真正启用 URP（决定属性名 `_Color` 还是 `_BaseColor`，以及能否用 Renderer Feature）。
2. 新模型的 Renderer 数量与层级深度（决定是否需要缓存 `Dictionary<GameObject, Renderer[]>`）。
3. 材质是否跨 prefab 共享（决定 `sharedMaterial` 的踩坑风险）。
4. 建筑模型是否仍为嵌套 prefab（决定能否在 prefab 上预挂引用）。

### 已形成的设计倾向（可迁移的判断）

- **改 `_EmissionColor` 而非 `_Color`**：`_Color` 是乘算，染红会让模型变暗发闷，卡通模型显脏；emission 是加算，呈现为发光高光，且不破坏阵营配色。
- **闪光用白色偏暖（约 `(1, 0.9, 0.8)`），不分阵营**：
  - 飘字已用红色表示"我方受击"（`DamageFloatTextRenderer.cs:115`），闪红会与之语义冲突（飘字红=我方吃亏，闪红红=通用受击）。
  - 红色闪光打在红方阵营模型上几乎不可见。
  - 让闪光只表达"这里被打了"，阵营信息交给飘字与血条，两个系统各管一件事。
- **动画曲线不对称**：约 0.08s 爆亮（`Ease.OutQuad`）+ 0.12s 消退（`Ease.InQuad`），总时长 ≤0.2s。
- **内置管线的 emission 坑**：需 `EnableKeyword("_EMISSION")`，且 `globalIlluminationFlags` 设为 `EmissiveIsBlack`，否则改了 `_EmissionColor` 完全无反应。（仅在仍为内置管线时适用）

---

## 执行顺序

1. 阶段一（建议现在做）：broker 加 `UnitHit` + 三处发布，约十余行。
2. 阶段二（可选）：接 `HitFeedbackPresenter`，先获得基础打击感，实机试手感后再决定去留。
3. 阶段三：换模型完成后重新评估。
