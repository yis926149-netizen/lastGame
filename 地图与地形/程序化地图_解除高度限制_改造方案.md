# 程序化地图 - 解除高度限制改造方案

## 一、目标与实施原则

### 改造目标

- 地图使用 N 层离散高度，最低和最高高度由配置决定
- 相邻地块允许任意整数高度差
- 矩形、三角过渡区域在任意高度组合下都能封闭核心面片之间的空隙
- 大落差的 Mesh 细分数有上限，避免顶点数随高度差无限增长
- 材质、海陆判断、通行性和河流不再依赖固定的 0/1/2 三层高度

### 本轮范围

- 第一阶段保持整数离散高度，不同时引入连续 `float` 高度
- 矩形和三角过渡面始终生成；高度差只决定形状和细分数
- 大落差先使用"有限细分的陡坡/阶梯"，瀑布作为后续视觉增强
- 每个阶段完成后，项目必须可编译、可生成地图、可独立验收
- 正式高度生成（柏林噪声）最后切换到 N 层，避免未改造模块提前收到不支持的数据

### 每阶段统一完成标准

每个阶段只有同时满足以下条件才进入下一阶段：

1. Unity 脚本编译无错误，现有 EditMode 测试通过
2. 本阶段新增的自动化测试通过
3. 固定种子的默认配置可以完整生成地图，无异常日志
4. 本阶段的受控高度样例通过场景检查
5. 默认配置仍保持当前 0/1/2 地图行为，除本阶段明确修改的视觉效果外无回归

### 总体策略：硬编码 3x3 地图先行，柏林噪声最后

核心思路：**先验证"任意高度差对应什么过渡面片"，再让柏林噪声生成这些高度。**

```
✅ 硬编码 3x3 高度地图（绕过噪声）— 第 0 步完成
  ✅ 矩形通用过渡面片
  ✅ 三角通用过渡面片
  ✅ 完整 3x3 拼接验证
  ✅ 材质和游戏逻辑（三层分桶动态化、水域集中化、战斗高低判定）
  ✅ N 层优化器（Dictionary 计数替代 count0/1/2 硬编码）
  ✅ 柏林噪声正式输出 N 层高度（minHeight/maxHeight 替代 T1/T2，floor(noise*layerCount) 映射）
```

`3x3` 地图足以覆盖本地拓扑验证：中心地块拥有全部 6 个邻居（NE、E、SE、SW、W、NW），可覆盖 3 个矩形方向和 2 个三角区域。多张 `3x3` 地图组合即可穷举所有高度组合情况，无需依赖柏林噪声。

**关于"增加类型"**：此处"类型"指几何输入类别，不是枚举。不按高度差 `n` 扩充 `RectType` / `TriType`，而是建立统一的通用算法：

- 矩形：统一输入 `(heightA, heightB)`，内部根据 `abs(hA - hB)` 计算细分数
- 三角：统一输入三条已有矩形边的实际状态（坡/梯 + 顶点序列 + 细分数），内部根据边界拓扑剖分。高度信息已编码在顶点 Y 坐标中，三角算法不接触高度值。

---

## 二、关键规则

### 1. 高度和细分规则

定义：

```text
heightDelta = abs(heightA - heightB)
subdivision = clamp(ceil(heightDelta / stepHeight), 0, maxStepSubdivision)
```

| 高度差 | 矩形过渡表现 |
|--------|--------------|
| `heightDelta = 0` | 生成扁平连接面，使用最小拓扑 |
| `0 < heightDelta <= stepHeight` | 生成一级普通过渡 |
| `heightDelta > stepHeight` | 按落差动态增加细分 |
| 超过上限 | 固定为 `maxStepSubdivision`，形成陡坡或悬崖效果 |

矩形面不能在高度差为 0 时跳过。相邻地块的核心面片本身不相连，扁平矩形面仍负责填补两者之间的空隙。

### 2. 细分数的归属

当前 `HexCellData.interpCount` 是地块级共享值，不适合任意高度差。同一地块的 NE、E、SE 三条边可能具有不同落差，因此细分数必须按边计算：

```text
GetSubdivision(heightA, heightB, stepHeight, maxStepSubdivision)
```

- 不把动态结果写回 `HexCellData.interpCount`
- 矩形生成器显式接收或内部计算当前边的细分数
- 三角生成器对三条边分别计算细分数
- 同一条公共边必须调用同一套采样函数，保证矩形与三角边界顶点完全一致

### 3. 三角过渡规则

三角过渡面始终生成，用于填补三个核心面片之间的空隙。一个三角区域由三个地块围成：

```text
        A
       / \
    边0   边1
     /     \
  Self ---- B
       边2

边0：Self ↔ A   (Self.NE 方向)
边1：A ↔ B      (邻居之间的边)
边2：Self ↔ B   (Self.E 方向)
```

三角生成器**不通过高度判断类型**，而是直接接收三条矩形边已经生成的顶点数据：

- 每条边携带：坡/梯类型 + 细分数 + 顶点序列
- 三角算法只负责对这个闭合边界内部进行剖分

矩形与三角的公共边顶点完全一致，因为三角直接复用矩形边已生成的边界点，不做重复采样。

#### 按三条边类型分类

每条边有 **坡** / **梯** 两种状态，共 `2³ = 8` 种组合。按拓扑等价（不考虑旋转），归为 4 类：

| 三条边类型 | 拓扑形态 | 当前 TriType 对应 |
|-----------|---------|:---:|
| 坡坡坡 | 简单三角形（无阶梯边） | `one` |
| 梯坡坡 / 坡梯坡 / 坡坡梯 | 一条阶梯边 + 两条坡边 | `two`（未实现） |
| 梯梯坡 / 梯坡梯 / 坡梯梯 | 两条阶梯边 + 一条坡边 | `three` |
| 梯梯梯 | 三条阶梯边 | 新增 |

`four`（两梯一平）是上述分类的特例：当某条坡边的两端高度相同时，不需要在该边上做任何插值，直接简单三角形即可。

#### 新三角算法的输入输出

```text
输入：
  Edge0: 坡/梯 + 顶点序列 (从 Self 到 A)
  Edge1: 坡/梯 + 顶点序列 (从 A 到 B)
  Edge2: 坡/梯 + 顶点序列 (从 B 到 Self)

输出：
  三角形内部剖分的 vertices、UV、DrawOrder
```

三条边的顶点序列形成一个闭合多边形边界，三角算法在这个边界内部进行剖分。高度信息已经完全编码在三条边的顶点 Y 坐标中，三角算法不再读取高度值。

边界组装规则：

```text
Boundary =
    Edge0(Self → A)
  + Edge1(A → B)，去掉重复的 A
  + Reverse(Edge2)(B → Self)，去掉重复的 B 和最终重复的 Self
```

Edge2 定义上是 Self ↔ B 方向，组装时需要反转顶点序列使其方向为 B → Self。

#### 高度不限后的完整分类

高度不限后，三角类型数量**不会随高度层数增加**，因为坡/梯已与高度解耦。分类只看三条边的实际类型，共 `2³ = 8` 种有方向组合：

```text
        A
       / \
    边0   边1
     /     \
  Self ---- B
       边2
```

| 边0 | 边1 | 边2 | 拓扑类 | 说明 |
|:---:|:---:|:---:|:------:|------|
| 坡 | 坡 | 坡 | 三坡 | 边界只有 3 个角点，简单三角形 |
| 梯 | 坡 | 坡 | 一梯两坡 | 梯边在 Self↔A |
| 坡 | 梯 | 坡 | 一梯两坡 | 梯边在 A↔B |
| 坡 | 坡 | 梯 | 一梯两坡 | 梯边在 B↔Self |
| 梯 | 梯 | 坡 | 两梯一坡 | 坡边在 B↔Self |
| 梯 | 坡 | 梯 | 两梯一坡 | 坡边在 A↔B |
| 坡 | 梯 | 梯 | 两梯一坡 | 坡边在 Self↔A |
| 梯 | 梯 | 梯 | 三梯 | 三条边全细分 |

忽略旋转后为 4 类，需要保留哪条边是梯的信息以确定正确的顶点绕序。

**高度不限带来的变化**：不是类型增加，而是每条边携带的细分数可以不同。同一拓扑类下的不同实例仅在边界顶点数量上有差异：

```text
例：同样是"梯梯坡"（边0梯、边1梯、边2坡）

实例 A：n0=2, n1=7, n2=0
实例 B：n0=10, n1=1, n2=0
```

它们属于同一种拓扑分类，通用边界剖分算法同时处理两者。

**最终分类模型**：

```text
TriTopology = 三条边的坡/梯组合 (8 种，固定)
TriGeometry = 三条边的有序顶点序列 (细分数不限)
Height      = 只决定顶点坐标和细分数，不参与分类
```

**语义保留规则**：

- 高度差为 0 但游戏规则指定为梯：仍记为梯，几何上呈扁平细分面
- 高度差很大但游戏规则指定为坡：仍记为坡，形成陡坡不开梯
- 三条梯边细分数不同：仍属三梯，通用边界剖分处理

---

## 三、3x3 硬编码测试地图设计

### 设计目的

`3x3` 硬编码地图是整套改造方案的核心验证装置。它直接提供高度数组，完全绕过柏林噪声，但复用从 `int[,] terrainMap` 之后的地图数据构建和 Mesh 渲染完整流程，因此验证的不仅是孤立函数，还包括邻居查询、坐标方向和面片拼接。

### 为什么 3x3 足够

中心地块 `(1,1)` 拥有全部 6 个邻居：

```
SW  W   NW
  \ | /
SE - C - NE
  / | \
S   E   N
```

其中 NE、E、SE 方向（中心地块的半边邻居）覆盖 3 个矩形过渡面；(中心, NE, E) 和 (中心, E, SE) 覆盖 2 个三角过渡区域。通过切换中心地块和邻居的高度，可以用少量 `3x3` 地图穷举所有高度组合。

### 测试地图结构

每个测试案例是一张 `H` 矩阵：

```csharp
int[,] H = new int[3, 3]
{
    { h00, h01, h02 },
    { h10, h11, h12 },
    { h20, h21, h22 }
};
```

其中 `h11` 是中心地块。

### 覆盖的高度组合

```text
矩形边方向（观察 NE / E / SE 三个方向各自的两个端点高度差）：
  0-0   两块同高，扁平连接
  0-1   一级落差
  0-2   两级落差
  0-5   大落差
  5-0   大落差，方向相反

三角区域方向（观察中心、NE、E 和 中心、E、SE 三组）：
  0-0-0   三块同高
  0-0-1   两同一异（一个邻居高）
  5-0-0   两同一异（中心高）
  0-1-1   两同一异（两个邻居同高）
  0-1-2   两同一异（中落差）
  0-2-5   三大落差三不同
  5-2-0   三大落差三不同，方向相反
```

### 调试开关

每张 `3x3` 地图需要支持渲染模式切换，用于分步验证：

| 模式 | 渲染内容 |
|------|----------|
| `RectOnly` | 仅核心面片 + 目标矩形过渡区域 |
| `Full` | 全部面片（用于最终拼接验证） |

例如第 2 步（矩形改造）期间，用 `RectOnly` 模式渲染 `3x3` 地图，确保大落差高度不会同时调用尚未改造的旧三角算法。

---

## 四、分阶段实施计划

### 第 0 步：锁定基线和 3x3 硬编码测试地图基础设施 ✅

**目的**：后续每个 Mesh 模块都能通过硬编码 `3x3` 地图独立验证，不依赖柏林噪声。

**改动**：

- ✔ 保留已完成的 `SeedService`、稳定派生、`useFixedSeed` 和 `generatorVersion`
- ✔ 记录一个固定种子和默认 0/1/2 配置作为回归基线
- ✔ 增加 `3x3` 硬编码测试地图入口，直接提供 `int[3,3]` 高度数组，绕过噪声
- ✔ 测试入口复用"高度数组之后"的地图数据构建和 Mesh 渲染流程
- ✔ 增加渲染模式开关（`RectOnly` / `Full`）
- ✔ 非目标模块（河流、湖海、单位）在测试模式下可单独关闭
- □ 增加 Mesh 通用断言：索引不越界、三角形面积非零、顶点为有限数、UV 数量等于顶点数量（后续第 1 步配合做）

**首批硬编码测试地图**：见第三节覆盖列表，已实现 11 组预设 + 自定义数组入口。

**涉及文件**：
| 文件 | 改动 |
|------|------|
| `Enums.cs` | +`HeightSourceMode`、+`TransitionDebugMode`、+`HardcodedPreset` |
| `HardcodedHeightPresets.cs` | 新文件，11 组预设 + `FromArray()` |
| `MapGenerationConfigSO.cs` | +`heightSourceMode`、+`hardcodedPreset`、+`hardcodedCustomHeights`、+`transitionDebugMode` |
| `MapGenerator.cs` | 硬编码模式强制 3x3、跳过噪声/优化器/河流/地貌/资源 |
| `MapRenderer.cs` | `MainMapMeshCreat()` 中矩形/三角回路增加 `if (mode != ...)` 条件 |

**验证**：

- ✔ 默认固定种子连续生成两次，高度数组和 Mesh 统计一致
- ✔ `3x3` 硬编码地图能够在 Scene/Game 视图中渲染
- ✔ 当前 0/1/2 正式地图生成结果保持不变

**完成标志**：`3x3` 硬编码测试基础设施就绪，不改变正式地图行为。

### 第 1 步：增加兼容配置和公共边采样器

**目的**：先建立矩形、三角都会使用的高度差和边界采样基础，不切换现有 Mesh 路径。

**配置新增**：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `minHeight` | `0` | 最低离散高度 |
| `maxHeight` | `2` | 最高离散高度，默认保持三层 |
| `stepHeight` | `1` | 每多少高度单位增加一级细分 |
| `maxStepSubdivision` | 待性能测试确定 | 最大细分数 |
| `seaLevel` | `0` | 海域高度阈值 |

**改动**：

- 增加纯函数 `GetSubdivision(heightA, heightB, ...)`
- 提取公共边采样函数，输入两个端点和细分数，输出顺序稳定的边界点
- 保留旧 `RectType`、`TriType` 和旧渲染调用，暂不改变正式结果
- 新配置默认值必须与当前行为兼容

**自动验证**：

- `0-0` 返回最小细分，`0-1`、`0-2`、`0-5` 单调增加并受上限约束
- 交换端点后，采样点数量相同、顺序相反、坐标逐点对应
- 相同输入每次生成完全相同的采样结果

**场景验证**：默认地图与第 0 步基线一致。

**完成标志**：公共算法已经可测试，但尚未接管任何正式面片。

### 第 2 步：矩形过渡 Mesh 通用化

**目的**：只替换矩形过渡面，通过 `3x3` 硬编码地图验证，三角面和正式高度生成保持原样。

**改动**：

- 将矩形判断从 `TerrainGenerator.IsType` 中解耦
- 只要目标方向存在邻居，就生成矩形过渡面
- 统一矩形顶点、UV 和 DrawOrder 生成路径
- 当前边的细分数由两端高度即时计算，不再读取地块级 `interpCount`
- `heightDelta = 0` 生成扁平连接面，不得跳过
- 河流矩形先保持原路径；本阶段测试地图关闭河流
- 在 `3x3` 地图上使用 `RectOnly` 模式验证矩形，避免旧三角参与渲染
- 暂时保留三角所需的旧 `RectType` 分类，等三角替换后再删除

**3x3 验证矩阵**：

```text
0-0 / 0-1 / 0-2 / 0-5 / 5-0
在 NE / E / SE 三个方向各自验证
```

**自动验证**：

- 对每个高度组合分别检查顶点、UV、索引合法性
- `0-0` 面片覆盖两个核心面片之间的空隙
- `0-5` 和 `5-0` 的几何位置一致，仅绕序/朝向符合预期
- 超大落差的顶点数不超过由 `maxStepSubdivision` 推导出的上限
- 每个三角形面积大于容差，法线方向一致

**场景验证**：

- 在 `RectOnly` 模式下逐一渲染各高度组合的 `3x3` 地图
- 开启线框检查矩形内部无破面、翻面和重叠
- 默认 0/1/2 正式地图可完整生成

**完成标志**：矩形过渡面已经支持任意落差，在 `3x3` 地图上全部通过。

### 第 3 步：三角过渡 Mesh 通用化（Full 测试路径已接入）

**目的**：替换三角分类和剖分逻辑。新算法不依赖高度，只接收三条矩形边的实际状态（坡/梯 + 顶点序列），在闭合边界内部剖分。

**实现顺序**：

1. 先实现三条坡边（坡坡坡）：边界由 3 个角点组成，简单三角形
2. 再实现一条梯边 + 两条坡边（梯坡坡 / 坡梯坡 / 坡坡梯）：补充当前未实现的 `two` 分支
3. 再实现两条梯边 + 一条坡边（梯梯坡 / 梯坡梯 / 坡梯梯）：替代当前的 `three`
4. 再实现三条梯边（梯梯梯）：全新的情况
5. 各类通过后切到 `Full` 模式验证矩形+三角拼接，最后接入正式渲染

**当前进度**：

- ✔ 新增无状态 `TransitionEdgeProfile`，边携带坡/梯类型和有序顶点序列
- ✔ 边采样职责归属 `RectangleTransitionMesh`，现有矩形 `SimpleInterpolate` 已委托同一公式
- ✔ 新增中心扇形剖分，统一支持 8 种坡梯组合和三边不同细分数
- ✔ 新增 `Legacy / GenericFan` 切换，默认保留旧路径
- ✔ 新增 8 种组合的几何测试，覆盖索引、UV 和零面积三角形检查
- ✔ 新增无状态通用矩形 Mesh；矩形生成并暴露四条 profile，三角没有边配置或采样入口
- ✔ `Full / RectOnly` 均已接入 Generic 路径；三角只在 Full 中消费已生成的矩形 profile
- ✔ 反向三角边直接倒序矩形 profile，不再重新插值或扰动
- ✔ 矩形和三角索引均按 XZ 有向面积统一为朝上绕序
- ✔ 取消三角独立配置和 `TriOnly`：三角不再采样边界，只消费本次矩形循环缓存的 `one/four` profile
- ✔ 新增 `Configured / Random` 测试数据模式；Random 每次 Play 随机 3x3 高度和每条实际矩形的坡梯类型
- ✔ 同步双梯边逐层梯田剖分（`TryBuildStriped` / `BuildStrips`），替代适用场景中的中心扇形
- ✔ 通用阶梯角剖分（`BuildTerrace`）：含梯边的三角从最低角→最高角按高度层逐级拉链，踏面水平、踢面斜向，无内凹
- ✔ 移除 `AddUpwardTriangle` 的纯 XZ 面积退化判断，改为只跳过真正重复顶点索引，消除竖立面被误跳造成的镂空
- ✔ 矩形过渡材质融合方向修正：UV 改为沿 profile 方向参数化（`BuildProfileUV`），融合方向恒为 self→neighbor（边1→边3），不再随世界朝向旋转；`ToFlatShaded` 沿用源 UV
- ✔ `MapController` 矩形混合材质 A/B 参数恢复为 `_MainTexA=self, _MainTexB=neighbor`
- ✔ 三角过渡材质三边融合修正：`BuildFan` 统一后处理重心坐标 UV（`ComputeBarycentricUVs`），运行时生成 RGB 重心遮罩贴图（`GetOrCreateBarycentricMask`），`ToFlatShaded`（三角版）沿用源重心 UV
- ✔ `MapController` 三角混合材质的 `_BlendContrast/_GlobalSmoothness` 修正为 shader 实际声明的 `_BlendSmooth`
- □ 尚需在 Unity Test Runner 中实际运行 EditMode 测试并逐组目视检查

当前三组配置的语义是规范矩形所有者的 `NE / E / SE` 物理方向槽，不再是三角局部 `edge0 / edge1 / edge2`：

```text
triangleEdge0* = 所有 NE 物理边
triangleEdge1* = 所有 E 物理边
triangleEdge2* = 所有 SE 物理边
```

因此当前测试路径支持三个方向使用不同坡梯类型和细分数，但同一方向的全图边仍共享配置。任意单条物理边覆盖需要后续增加 `(owner, direction)` 配置表。

`GenericFan` 目前只在 `HeightSourceMode.Hardcoded` 下启用，用于无河流 `3x3` 验证；Noise 正式地图继续回退 Legacy 路径，等河流矩形和正式高度策略接入后再开放。

**核心设计**：三角面片不需要知道高度。三条矩形边的顶点序列构成闭合多边形：

```text
Boundary = self→A 边顶点
         + A→B 边顶点（去除重复起点）
         + B→self 边顶点（去除重复起点）
```

剖分算法只在一个 `List<Vector3>` 边界环上工作。

现有 `HexCellData.NERectVertices / ERectVertices / SERectVertices` 不能直接作为三角边界输入：这些列表会追加河流顶点，且重复渲染时未清空、会累积旧数据。因此 `RectangleTransitionMesh` 从端点生成无状态矩形数据，`MapRenderer` 按 `(owner.GenerateOrder, direction)` 缓存本次渲染的矩形及四条 profile。`RectangleDrivenTriangleMesh` 只选择并按需倒序这些 profile，然后进行内部剖分。

**改动**：

- 新算法直接接收三条边的顶点序列和边类型
- 三角内部剖分必须消费完整边界，不能产生 T 型接缝
- 删除 `Mathf.Abs(...) == 2` 等固定高度差判断
- 新路径稳定后删除 `TriType.one/two/three/four` 分发和无实现的 `two` 路径
- 矩形、三角都不再依赖后，再删除旧 `RectType` 和 `HexCellData.interpCount/isSlope`

**自动验证**：

- 覆盖 `0-0-0、0-0-1、0-1-1、0-1-2、0-2-5、5-2-0`，分别在 NE_E 和 E_SE 两组三角区域验证
- 对三角三个角进行循环置换，结果覆盖区域和拓扑统计保持一致
- 每条三角边的采样点与相邻矩形边逐点一致（`Full` 模式下对比）
- 不存在索引越界、零面积三角形、重复三角形和未使用边界点
- 细分达到上限后，顶点数保持有界

**场景验证**：

- 先在 `RectOnly` 模式检查矩形，再在 `Full` 模式检查由矩形边界生成的三角
- 在 `Full` 模式下用线框从正反两侧检查公共边和矩形-三角交界处
- 默认地图完整生成，原先会进入 `TriType.two` 的组合不再打印"无此方法"

**完成标志**：矩形和三角过渡系统均不再依赖 0/1/2 高度枚举，`3x3` 全高度组合通过。

### 第 4 步：材质和子 Mesh 支持任意高度

**目的**：在正式生成 N 层高度前，先解除材质数组和三个固定 subList 的限制。

**改动**：

- ✔ 移除核心面片按高度 0/1/2 写入三个固定 subList 的逻辑；改为 `WaterLevelConfig.ClassifyHeight()` 按水线/中点动态分桶
- ✔ 移除 `HexController.SetHexMaterial` 对 0/1/2 的固定 `switch`
- ✔ 采用统一地形材质，通过高度区间分桶选材，不在 Shader 中做额外映射
- ✔ 矩形和三角过渡面沿用同一高度材质规则
- ✔ 保持水面、河流等独立材质不变
- ✔ 水域判定集中到 `WaterLevelConfig`（`WaterLevel=1`、`IsWater()`），全部硬编码替换
- ✔ 战斗高地加成从 ±1 限制改为高于/低于二元判定

**3x3 验证**：在 `Full` 模式下渲染 `0、1、2、3、5` 高度带的 `3x3` 地图。

**自动验证**：

- 高度 `0..N` 均能得到有效材质数据，不越界、不返回空材质
- Mesh subMesh 数量不随 `maxHeight` 线性增长
- 受控高度 5 不再回退到硬编码的高度 2 材质分支

**场景验证**：

- 使用已接通矩形和三角的 `3x3` 地图检查材质连续性
- 检查矩形、三角过渡区域与核心面片之间无明显材质断裂
- 默认 0/1/2 地图的颜色语义保持可接受

**完成标志**：渲染层可以显示任意高度，但正式生成器仍只输出 0/1/2。

### 第 5 步：高度语义和游戏逻辑配置化

**目的**：先让所有业务逻辑认识任意高度，再扩大正式高度范围。

**改动**：

- ✔ `Height == 0` 的通行判断改为基于 `WaterLevelConfig.IsWater()`
- ✔ `isLakeOrSea`、海岸判断和水位规则统一使用 `WaterLevelConfig` 常量
- ✔ 战斗高地加成从 ±1 限制改为高于/低于二元判定
- □ 审查寻路、建筑放置、出生点和视野中所有固定高度比较（状态：均通过 HexType.LakeOrSea 间接依赖 WaterLevelConfig，无直接硬编码残留）
- □ 明确负高度是否允许；若允许，海陆判断仍只依赖 `WaterLevel`
- □ 河流流向继续遵循邻居高度不高于当前地块，大落差暂按普通下落处理

**3x3 验证**：在 `Full` 模式下使用 `3x3` 地图，验证不同 `seaLevel` 下高度 3、5 的海陆分类和路径。

**自动验证**：

- 在不同 `seaLevel` 下验证海陆分类和 movementCost
- 高度 3、5 的陆地可以参与寻路和放置规则，不因缺少分支失效
- 河流高度检查覆盖相等、小落差和大落差

**场景验证**：

- 在 `3x3` 地图上执行选格、寻路、单位移动和建筑放置
- 开启河流后 `3x3` 地图和默认地图均可完成生成

**完成标志**：游戏逻辑已不假设地图只有 0/1/2 三层。

### 第 6 步：地形优化器支持 N 层

**目的**：先替换 `OptimizeTerrain` 中的三分类多数投票，仍让正式生成器输出 0/1/2，单独验证优化器。

**改动**：

- ✔ 用 Dictionary 统计邻居高度频数，按出现次数最多者替换
- ✔ 最高频高度唯一时才替换，平票保持原高度
- ✔ 不再把所有"大于 1"的高度错误归入高度 2

**自动验证**：

- 分别覆盖 0/1/2 和包含 3/5/负值的邻域
- 覆盖唯一多数、平票、边界格和单格地图
- 默认 0/1/2 输入与旧实现输出一致

**场景验证**：默认地图仍完整生成，并与基线比较高度分布。

**完成标志**：优化器能够接受 N 层输入，正式高度范围尚未扩大。

### 第 7 步：正式高度生成切换为 N 层（柏林噪声）

**目的**：Mesh、材质、游戏逻辑和优化器全部就绪后，最后让柏林噪声输出 N 层高度。这是整个改造的最后一步。

**改动**：

- ✔ 将双阈值 `{0,1,2}` 映射替换为 `minHeight + min(floor(noise * layerCount), layerCount-1)` 的离散映射
- ✔ TerrainHeights 结构替换 `T1/T2` 为 `minHeight/maxHeight`
- ✔ MapGenerationConfigSO 新增 `minHeight`(0)/`maxHeight`(2)/`generatorVersion`(2) 字段
- ✔ `generatorVersion` 递增至 2，避免同种子在算法变更后被误认为同一地图

建议映射：

```text
layerCount = maxHeight - minHeight + 1
height = minHeight + min(floor(noise * layerCount), layerCount - 1)
```

**自动验证**：

- 所有输出都位于 `[minHeight,maxHeight]`
- 固定种子结果可复现
- `minHeight == maxHeight` 时输出单一高度
- 小地图、大地图和不同高度范围均无越界
- 优化前后都不会产生配置范围外的高度

**场景验证**：

- 依次使用 `0..2、0..5、0..10` 生成完整地图
- 检查核心面、矩形、三角、材质、碰撞、寻路和水体
- 记录不同 `maxStepSubdivision` 下的顶点数、生成耗时和内存占用

**完成标志**：正式地图已使用 N 层高度，完整主流程可运行。

### 第 8 步：河流大落差视觉增强（可选）

**目的**：在功能正确后处理大落差河流的视觉表现，不阻塞 N 层高度主改造。

**改动候选**：

- 高度差小于阈值时沿普通过渡面流动
- 高度差超过阈值时生成瀑布面
- 瀑布底部增加水花或独立材质，仅作为表现层功能

**验证**：

- 覆盖平流、一级下落、多级下落和无合法下游
- 瀑布面与河道连接，无悬空、反面和穿透
- 不改变河流路径选择的确定性

---

## 五、阶段依赖与可运行状态

| 阶段 | 测试数据源 | 正式高度输出 | 已切换模块 | 本阶段可独立观察 |
|------|-----------|-------------|------------|------------------|
| ✅ 0 | - | 0/1/2 | 验证入口 | 基线与 3x3 基础设施 |
| 1 | - | 0/1/2 | 公共边采样器（未接管渲染） | 采样算法测试 |
| 2 | 3x3 硬编码 | 0/1/2 | 通用矩形 | RectOnly 任意落差矩形面 |
| 3 | 3x3 硬编码 | 0/1/2 | 通用矩形 + 通用三角 | RectOnly → Full 拼接 |
| 4 | 3x3 硬编码 | 0/1/2 | 通用高度材质 | Full 模式材质连续性 |
| 5 | 3x3 硬编码 | 0/1/2 | 通用高度业务逻辑 | Full 模式寻路和水体 |
| 6 | 正式 0/1/2 | 0/1/2 | N 层优化器 | 独立优化器结果 |
| 7 | 正式噪声 | N 层 | 全主流程 | 完整 N 层随机地图 |
| 8 | 正式噪声 | N 层 | 河流增强 | 瀑布和大落差河流 |

核心顺序约束：

```text
✅ 3x3 硬编码测试基础设施 — 第 0 步完成
  → 公共边采样
  → 矩形通用化（3x3 RectOnly 验证）
  → 三角通用化（消费矩形 profile → 3x3 Full 拼接验证）
  → 材质通用化（3x3 Full 验证）
  → 游戏逻辑通用化（3x3 Full 验证）
  → 优化器通用化
  → 柏林噪声正式输出 N 层（最末）
```

任何阶段失败时，正式高度生成仍保持 0/1/2，可以在孤立环境中定位问题。

---

## 六、待实现时确认的参数

- [ ] `maxStepSubdivision` 的默认值：根据 0..10 高度压力测试确定
- [ ] 大落差表现采用连续陡坡还是保留明显阶梯
- [ ] 通用三角内部剖分的具体算法及顶点预算
- [ ] Shader 使用世界高度还是归一化高度写入顶点数据
- [ ] 是否在后续版本支持负高度
- [ ] 瀑布效果是否进入本轮范围

不再作为本轮未决项：

- 高度采用 N 层离散整数，连续高度留到后续版本
- 高度差为 0 的矩形和三角过渡面仍然生成
- `RectType` / `TriType` 在通用算法接管后删除，不按高度差 n 扩充枚举
- 柏林噪声 N 层生成在所有 Mesh 和逻辑改造完成后执行
