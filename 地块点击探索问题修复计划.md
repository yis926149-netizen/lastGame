# 地块点击探索问题修复计划

> 针对玩家反馈"点击地块时有时探索不到"的问题，对 `Assets/Scenes/GameScene.unity` 场景 Canvas 层级与探索点击脚本进行排查后，形成的修复计划。

---

## 一、问题背景

玩家在点击未探索地块时，偶发"点击不生效、探索不到"的情况。经排查，根因并非地图数据问题，而是 **UI 元素的视觉范围与实际可交互（Raycast）范围不一致**，以及**探索失败时缺乏反馈**所致。

---

## 二、Canvas 层级结构（现状）

```
Canvas (Screen Space - Overlay, CanvasScaler 1080×1920)
├── CostLabelContainer (直接子级，⚠️ LocalScale: 10.685, 19.092, 1)
└── SafeArea (拉伸填满)
    ├── GlobalTimerUI     ✅ 活跃 - 右上角 206×213
    ├── UnitInfoPanel     ❌ 非活跃
    ├── TimeStop          ✅ 活跃 - 中心偏右
    ├── Tech&CulturePoints ❌ 非活跃
    ├── TechTree          ❌ 非活跃
    ├── CultureTree       ❌ 非活跃
    ├── Block             ❌ 非活跃 - 全屏 1920×1080 透明遮罩 ⚠️
    ├── VictoryVideo      ❌ 非活跃
    ├── DefeatVideo       ❌ 非活跃
    ├── WidthScaleGroup   ✅ 活跃 - 底部居中 100×100
    ├── VictoryUI         ❌ 非活跃
    ├── DefeatUI          ❌ 非活跃
    └── CardDeckPanel     ✅ 活跃 - Image 组件已禁用
```

---

## 三、问题清单与修复方案

### P0-1：CostLabelContainer 缩放异常（根因之一）

**场景数据：**
- `CostLabelContainer` 的 `m_LocalScale = {10.685, 19.092, 1}`
- 该容器拉伸到全屏（AnchorMin: 0,0 → AnchorMax: 1,1）

**问题影响：**
容器内动态创建的探索费用标签会被放大约 **10-19 倍**，导致：
- 标签的**视觉位置**与**实际 RectTransform 碰撞范围**严重错位
- GraphicRaycaster 命中的可点击区域远大于标签的视觉大小
- 本应点击地图地块的操作被标签的"隐形大范围"拦截
- 标签显示在地块 A 上方，但实际可点击区域覆盖到地块 B

**修复方案：**
1. 在 Unity 编辑器中选中 `Canvas/CostLabelContainer`，将 `RectTransform` 的 `LocalScale` 改为 `(1, 1, 1)`。
2. 确认代码侧 `CostLabelRenderer.cs:44-58` 创建容器时未额外设置 scale（当前代码未设置，保持即可）。
3. 验证：运行游戏后，探索费用标签尺寸应与设计稿一致，且可点击范围与视觉范围重合。

---

### P0-2：触屏设备 IsPointerOverUI 检测不准确（根因之一）

**代码位置：** `Assets/Scripts/Core/Services/InputService.cs:58-62`

```csharp
public bool IsPointerOverUI()
{
    // -1 = 鼠标指针 fingerId
    return _eventSystem != null && _eventSystem.IsPointerOverGameObject(-1);
}
```

**问题影响：**
项目为微信小游戏（触屏设备）。`IsPointerOverGameObject(-1)` 使用鼠标的 fingerId，在触屏上触摸事件使用 `touch.fingerId`。Unity 官方文档明确：触屏设备应传入实际 touch fingerId，否则该方法可能**始终返回 false**，导致：
- `PlayerInputHandler.HandleTileClickForExploration()` 中 `_explorationPointerDown = !_input.IsPointerOverUI(null)` 恒为 true
- 手指按在 UI 元素上时仍发起地图射线检测，命中错误的物体/坐标
- 点击行为随机性大，表现为"有时探索不到"

**修复方案：**

```csharp
public bool IsPointerOverUI()
{
    if (_eventSystem == null) return false;
    if (Input.touchCount > 0)
        return _eventSystem.IsPointerOverGameObject(Input.GetTouch(0).fingerId);
    return _eventSystem.IsPointerOverGameObject(-1);
}
```

**验证：**
- 真机/模拟器触屏环境：手指按在 UI（如计时器、卡牌）上时，不应触发地块探索。
- 手指按在地图空白区域时，应正常发起探索射线。

---

### P1-3：TryExplore 返回值被静默丢弃

**代码位置：** `Assets/Scripts/Core/Services/PlayerInputHandler.cs:259`

```csharp
_explorationService.TryExplore(cell, 0);  // 返回值未检查
```

**问题影响：**
探索可能因以下原因失败且玩家无任何反馈，表现为"点击没反应"：
- 金币不足
- 不是邻接已探索区域
- InteractionGate 锁定中（地图动画/地形变更期间）
- 已被 AI 占领

**修复方案：**

```csharp
var result = _explorationService.TryExplore(cell, 0);
if (result != ExploreResult.Success)
{
    // 输出日志便于排查
    Debug.Log($"[探索] 地块 {cell.HexCoordinate} 探索失败: {result}");
    // 可选：触发轻量 UI 提示（金币不足 / 无法探索该地块 等）
}
```

**验证：** 在金币不足时点击可探索地块，应出现明确的失败提示，而非无响应。

---

### P1-4：Dimmed CostLabel 双重拦截

**触发场景：**
当 CostLabel 可见但玩家金币不足时（`CostLabelRenderer.cs:106-108`）：
- `button.interactable = false` → Button.onClick 不触发
- 但 `IsPointerOverUI(null)` 仍返回 true → 地图射线检测被阻止

**结果：** 点击"灰色标签"时，两条路径（Button 点击 / 地图射线）均不生效，玩家看到标签却点击无任何反应。

**修复方案（二选一，推荐 A）：**

- **方案 A：** 在 `HandleTileClickForExploration()` 中，点击被 UI 拦截但该 UI 不可交互时，向玩家提示"金币不足"。
- **方案 B：** 将不可负担的 CostLabel 完全隐藏（不生成 Button），仅保留可负担标签，避免"看得见点不了"的困惑。

---

### P2-5：地图射线检测缺少 MapChunkView 验证

**代码位置：** `Assets/Scripts/Core/Services/PlayerInputHandler.cs:253`

```csharp
_input.RaycastFromScreen(releasePosition, out RaycastHit hit, 100f, LayerMask.GetMask("Map"))
```

**问题影响：**
`PlayerInputHandler` 使用原始 `Physics.Raycast`，仅过滤 "Map" 层。而 `CardController` 使用的 `MapRaycastService` 会额外验证命中物体是 `MapChunkView` 的后代。若 "Map" 层上存在其他碰撞体（误放置），射线可能命中错误物体，导致 `GetCellByWorldPosition` 返回错误格或 null。

**修复方案：**
1. 让 `PlayerInputHandler` 注入 `IMapRaycastService`（DI 容器中已绑定，见 `GameInstaller.cs:193`）。
2. 将 `_input.RaycastFromScreen(...)` 替换为 `_mapRaycastService.RaycastMap(...)`。

```csharp
if (!_mapRaycastService.RaycastMap(releasePosition, out RaycastHit hit))
    return;
```

**验证：** 地图上存在非地形碰撞体时，点击应被正确过滤，不影响真实地块的探索。

---

### P3-6：Camera 平滑延迟导致坐标偏移

**代码位置：** `Assets/Scripts/Controllers/CameraController.cs`（`SmoothDamp`，平滑时间约 0.1s）

**问题影响：**
刚结束拖拽后立即点击时，`Camera.main.transform.position` 与玩家视觉所见位置存在轻微偏差，射线解析可能命中相邻格子。

**修复方案（可选）：**
- 在探索射线发射前，使用相机目标位置（`_targetCameraPosition`）代替实际位置计算射线，或
- 在拖拽松手后短暂延迟（如 100ms）内忽略探索点击。

---

## 四、优先级与验收标准

| 优先级 | 问题编号 | 问题名称 | 验收标准 | 状态 |
|--------|----------|----------|----------|------|
| P0 | P0-1 | CostLabelContainer 缩放异常 | 标签视觉大小与实际可点击范围一致 | ✅ |
| P0 | P0-2 | 触屏 IsPointerOverUI 检测失效 | 触屏点 UI 不触发探索，点地图正常探索 | ✅ |
| P1 | P1-3 | TryExplore 无失败反馈 | 探索失败时有明确提示/日志 | ✅ |
| P1 | P1-4 | Dimmed CostLabel 双重拦截 | 不可负担标签保持变暗且不阻断地图点击 | ✅ |
| P2 | P2-5 | 射线检测缺少地图验证 | 非地图碰撞体不干扰探索 | ✅ |
| P3 | P3-6 | 相机平滑偏移 | 拖拽后快速点击命中正确格 | ✅ |

---

## 五、涉及文件清单

| 文件 | 改动类型 | 状态 |
|------|----------|------|
| `Assets/Scenes/GameScene.unity` | 修改 `CostLabelContainer` 的 `LocalScale` 为 (1,1,1)，关闭底部纯视觉 slot 的 RaycastTarget | ✅ |
| `Assets/Scripts/Core/Services/InputService.cs` | 修改 `IsPointerOverUI()` 支持触屏 fingerId | ✅ |
| `Assets/Scripts/Core/Services/PlayerInputHandler.cs` | 检查 TryExplore 返回值、注入 IMapRaycastService、处理 Dimmed CostLabel | ✅ |
| `Assets/Scripts/Managers/CostLabelRenderer.cs` | 不可负担标签保持变暗并禁用射线拦截 | ✅ |
| `Assets/Scripts/Controllers/CameraController.cs` | 探索射线使用目标相机位置 | ✅ |

---

## 六、风险与注意事项

1. **修改场景文件前**务必先备份或用 Git 提交，避免误改其他节点。
2. `IsPointerOverUI` 改动需在**编辑器鼠标环境**与**真机触屏环境**分别回归，避免影响 PC 端表现。
3. 探索失败提示需要与现有 UI 框架（VContainer 管理）风格保持一致，建议复用已有 Toast/提示组件。
4. `IMapRaycastService` 注入需确认 `PlayerInputHandler` 由 Zenject 容器构造，绑定关系在 `GameInstaller.cs` 中验证。
