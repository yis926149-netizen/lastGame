# 地图地形细化 - 任务列表

目标风格：文明6 —— 地表靠"terrain 色块 + 边缘混合"，细节靠 feature 摆件；几何不再细分。

## 阶段一：terrain 数据层配置化

- [ ] `Enums.TerrainType` 由高度派生改为配置化事实源（参照 `MapLandFormSO` 做成 SO）
- [ ] terrain 种类扩展到文明6量级：草原 / 平原 / 苔原 / 沙漠 / 雪地 / 海床
- [ ] 每种 terrain 配置 albedo、移动消耗、产出等字段
- [ ] 地图生成阶段写入 `HexCellData.terrainType`，渲染层不再自行按高度判材质
- [ ] 坐实 terrain 与 landForm 的正交关系（森林可长在草原/苔原上）

## 阶段二：splat 权重混合

- [ ] 地形 ID / 权重写入顶点属性通道（顶点色或空闲 UV）
- [ ] `TerrainBase_Fog` 改为多层权重混采样
- [ ] 同步改造 `TerrainBase_Fog_Transition` 变体
- [ ] 移除 `_rectMaterialCache` / `_triMaterialCache` 组合材质缓存
- [ ] 验证 rect / tri 过渡带获得软过渡（替代当前硬边界）

## 阶段三：feature 摆件加密

- [ ] 扩充 landForm 种类（森林 / 雨林 / 沼泽 / 绿洲等）
- [ ] 利用 `LandFormClusterSpawnRule` 做簇状摆放，提高密度
- [ ] 确认摆件不影响地表材质

## 可选

- [ ] 山体顶点动画由 CPU 逐帧写回迁移至 vertex shader（start/target 已在 UV2/UV3）
- [ ] 水岸 shader：浅深渐变 + 岸线泡沫（已有 `isCoast` / `waterLevel` 数据）
- [ ] 格边悬崖装饰几何

## 明确不做

- 不再细分网格 —— 现有 solid / rect / tri 过渡带已超出文明6所需精度
- 不为纯视觉差异新增 `TerrainType` 独立材质（组合缓存 n³ 增长）
- 不扩展程序化山脉系统（其复杂度由高度玩法买单，非外观）
