# 功能偏差检查报告

对照策划案.txt，检查代码实现的功能完成度和偏差。

> **最后校准日期**：2026-07-30
> 本轮校准内容：领地连通性、敌方地块占领、兵营连通、卡牌部署连通、开发连通 — 均已在 `ILogisticsService`/`LogisticsService` 中实现并接入。修正了报告中"无实现"的错判。

## 本轮低侵入修改

- [x] 暂停期间禁止玩家部署、开发，以及 AI 出牌和自动开发
- [x] 所有常规牌均禁止部署到已有单位的地块
- [x] 箭塔锁定当前目标，目标存活且在射程内时不重新选敌，离开射程后解除锁定
- [x] 兵营生产完成但无出生位置时保持 100%，出现空位后立即生成
- [x] 开发服务增加空值保护，并禁止开发存在敌方单位的地块
- [x] 开发邻接规则统一为邻接玩家已占领且已探索地块
- [x] 初始主城生命值降至 0 时立即结束对局
- [x] 兵营增加独立生产进度条，不复用建筑血条
- [x] 迷雾过渡动画：探索时逐渐消散（2秒），开局主城范围瞬间清晰
- [x] 天赋卡"防御"改为"建筑HP+25%"（`buildingHp`，Enum 值 3，乘数 1.25），接入 `BuildingBase.Start()`
- [x] 箭塔高地射程+1：`IsHighGround()` 判断箭塔格高度是否大于全部 6 个邻居，首次锁敌和锁定维持均使用 `AttackRange + 1`
- [x] 全局倒计时服务：`GlobalTimerService`（300 秒倒数、暂停感知）、`GlobalTimerUI`（纯秒数显示、最后 60 秒变红呼吸）、天赋选卡结束后启动，倒计时归零时统计双方占领地块数判定胜负
- [x] 远程单位高地射程+1：`RangedStrategy.GetEffectiveRange()` 判断单位格高度分类为高地时 `BasicAttackRange + 1`，`CanAttack`/`DoCombat`/`ChooseNextPath` 三处统一使用；`UIController.EnterRangedAttackMode` 和 `UnitMovementController.TryStartRangedAttack` 同步使用有效射程
- [x] 步兵 3 格警戒范围：警戒内有敌方单位才追击，无近敌时朝敌方主城行军，锁定后不切换

> 余留接入项：领地亮暗色表现、宝箱/金矿部署限制

---

## 一、缺失的核心系统

| 序号 | 功能 | 策划案要求 | 代码现状 | 涉及文件 |
|------|------|-----------|---------|---------|
| 1 | **战术牌** | 维修（恢复建筑HP）、战斗号令（提升步兵攻速移速），不占常规手牌，不自动补充，使用不消耗金币，同名牌叠放 | ☑️"坚固"已改为建筑HP+25%。维修和战斗号令仍无实现 | `TalentCardConfigSO.cs`, `TalentCardSO-1.asset`, `BuildingBase.cs` |
| 2 | ~~领地连通判断~~ | ✅ BFS 连通性已实现，`IsLogisticsConnected` 已接入部署/开发/兵营/AI；`IsVisibleToFaction` 通过迷雾过渡实现连通=清晰、断连=遮挡，亮暗色视觉区分已包含 | `TerritoryService.cs:60-181`, `CardPresenter.cs:273`, `BarracksSpawner.cs:50`, `MapRenderer.cs:1203` |
| 3 | **中央宝箱** | 一个中央宝箱，90秒后激活显示血条，步兵可攻击，箭塔不攻击，摧毁后最后一击所属阵营获海克斯选择，选择时暂停对局 | 无宝箱实体、无激活时间、无海克斯选择暂停。`TalentCardBootstrap` 是公共建筑天赋卡，不是宝箱替代 | 无 |
| 4 | **金矿系统** | 经济奖励可生成永久金矿，不可被攻击，不阻挡士兵移动，所在格不能部署卡牌，连接主城时产金，未连接时暂停 | 无任何金矿代码 | 无 |
| 5 | ~~敌方地块占领~~ | ✅ 已实现。`UnitMovementSystem.TryCaptureEnemyCell`（第247行）移动完成后检查归属→`TransferOwner`；`BuildingController.TryCaptureAfterBuildingDestroyed` 处理建筑被毁后占领。不消耗金币，不触发探索奖励 | `UnitMovementSystem.cs:247-274`, `BuildingController.cs:323-341` |
| 6 | **探索奖励分类** | 4种奖励倾向：普通(50%)、经济(30%/即时金币或金矿)、军事(10%/弱化援军 50HP/10ATK)、战术(10%/战术牌)。对局开始时所有奖励预先确定 | 待复查：需对照 `ExplorationRewardSystem.cs` 最新代码确认是否有分类和预生成 | `ExplorationRewardSystem.cs` |
| 7 | **对局倒计时与超时结算** | 300秒时间限制，结束时比较双方占领地块数量 | ☑️ 倒计时、UI、超时判胜负均已实现（`HandleTimeout` → 统计 `Player_City_Index.Key` → `BeginEndGame`）| `GlobalTimerService.cs`, `GlobalTimerUI.cs`, `EndGame.cs:39-65` |

---

## 二、重要行为偏差

| 序号 | 系统 | 策划案要求 | 代码现状 | 涉及文件 |
|------|------|-----------|---------|---------|
| 9 | **结束条件** | 任意一方主城生命值降至0时对方获胜 | ✅ 已注册双方初始主城并按HP立即判胜负；原城市数量逻辑仅保留为初始化异常兜底 | `EndGame.cs`, `GameFlowManager.cs`, `AIEntityFactory.cs` |
| 10 | **步兵寻路** | 无目标时向敌方主城前进，警戒范围3格，锁定目标后不切换，目标死亡或失效才解除锁定 | ☑️ 3格警戒范围已加入（`AlertRange=3`），范围内有敌方单位才追击；无近敌时朝最近敌方建筑行军；锁定后不切换已实现 | `MeleeStrategy.cs` |
| 11 | **箭塔锁定** | 锁定射程内第一支敌方步兵，不切换目标；现修正规则为目标离开射程后解除锁定；高地首次锁定射程+1格 | ☑️ 射程内锁定和离开解除已实现；高地射程+1 已实现（`IsHighGround()` → `GetEffectiveRange()`） | `ArrowTowerShooter.cs` |
| 12 | **兵营生产** | 连接主城时生产进度增长，未连接暂停保留进度，100%时检查相邻出生位置，无位置保持100%等待，显示进度条 | ☑️ 连通检查已实现（`BarracksSpawner.cs:50`），100%等待和独立进度条已完成；尚缺高地/特殊地形出生限制 | `BarracksSpawner.cs` |
| 13 | **卡牌部署检查** | 只能部署在归属己方且连接己方主城的格子，地形允许部署，不能部署在宝箱格/金矿格/暗色领地 | ☑️ 连通检查已实现（`CardPresenter.cs:273`），单位占用检查已补齐；尚缺宝箱/金矿/暗色领地过滤 | `CardPresenter.cs` |
| 14 | **中立地块开发条件** | 需至少与一块己方**连通**领地相邻、无敌方单位、不处于交战状态 | ☑️ 连通检查已实现（`ExplorationService.cs:140` `AdjacencyExplorationRule`），敌方单位检查已补齐；尚缺交战状态检查 | `ExplorationService.cs` |
| 15 | **AI决策间隔** | 1.5秒一次 | ✅ 已改为 1.5 秒 | `AICardTicker.cs:17` |
| 16 | **箭塔属性配置** | 攻击间隔1秒，攻击力15，射程2格 | ✅ 攻击间隔已改为 1 秒 | `ArrowTowerShooter.cs:14` |
| 17 | **兵营生产周期** | 15秒 | ✅ 已改为 15 秒 | `BarracksSpawner.cs:23` |

---

## 三、优先级建议（校准后）

### P0 - 核心玩法缺失
1. **战术牌系统**（维修/战斗号令）—— "坚固"已改为建筑HP+25%。维修和战斗号令仍空白，依赖探索奖励分类中的"战术"奖励
2. **中央宝箱机制** —— 90秒激活/海克斯选择/对局暂停均未实现

### P1 - 关键行为偏差
3. ~~步兵锁定+警戒~~ ✅ 已完成（3格警戒、无近敌朝主城行军、锁定不切换）
4. **探索奖励分类 + 预生成** —— 影响战术牌、金矿的来源合法性

### P2 - 参数/细节偏差
7. ~~对局超时结算~~ ✅ 已完成（统计占领地块数，多者胜，相同平局）
8. **金矿系统** —— 需经济奖励分类 #6 和连通表现 #3 先就位
9. **开发交战状态检查**（`ExplorationService.cs` `AdjacencyExplorationRule`）
