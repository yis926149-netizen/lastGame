# 微信小游戏性能优化实施计划

> 依据：微信开发者工具「优化建议」弹窗（未使用 wasm 代码分包 / 未使用预下载 / 未命中 CDN 缓存 / 长耗时帧 cost=1588ms、runtime=31697ms、总卡顿 13265ms）
> 编写日期：2026-08-24
> 适用构建：`name7/`（AppID `wxdb5246452802e6e0`）

---

## 一、现状实测基线

从构建产物与配置文件读出的硬数据，作为优化前基线：

| 项目 | 实测值 | 来源 |
|---|---|---|
| 首资源包大小 | **27.1 MB（未压缩）** | `name7/minigame/game.js` → `DATA_FILE_SIZE: 28417622` |
| 首资源包压缩开关 | **关闭** | `MiniGameConfig.asset` → `compressDataPackage: 0` |
| 资源加载方式 | CDN（腾讯云 COS 直链） | `assetLoadType: 0`，`CDN: https://7-2026824-...cos.ap-guangzhou.myqcloud.com/` |
| wasm 代码包 | 7.20 MB（brotli 后） | `name7/minigame/wasmcode/a28d8176b006e016.webgl.wasm.code.unityweb.wasm.br` |
| wasm 原始大小 | 31.95 MB | `name7/webgl/Build/webgl.wasm` |
| brotli 多线程压缩 | **开启**（降低压缩率） | `CompileOptions.brotliMT: 1` |
| 首启总下载量 | **≈ 34.3 MB / 仅 2 个请求** | 27.1 MB 资源 + 7.2 MB wasm |
| 预下载文件列表 | 空 | `preloadFiles: `（空）、`game.js` 的 `preloadDataList: []` |
| StreamingAssets | 构建时清空 | `DeleteStreamingAssets: 1` |
| 地图规模 | 20 × 30 = **600 格** | `MapGenerationConfig.asset` → `xNumber: 20`, `zNumber: 30` |
| 最长单帧 | **1588 ms** | 微信性能监控 |
| 累计卡顿 | 13265 ms / 31697 ms = **41.9%** | 微信性能监控 |

### 工程侧资源体量（`Assets/`，已排除 `_ImportArchive` 与 SDK）

| 类型 | 数量 | 合计 |
|---|---|---|
| `.png` | 405 | 174.35 MB |
| `.tga` | 14 | 75.38 MB |
| `.fbx` | 561 | 75.34 MB |
| `.psd` | 2 | 46.81 MB |
| `.prefab` | 513 | 30.70 MB |
| `.wav` | 26 | 22.52 MB |
| `.unitypackage` | 5 | 20.03 MB |

---

## 二、优先级总览

> 状态图例：✅ 已完成　🟡 部分完成　⬜ 未开始　— 不适用

| 优先级 | 优化项 | 预期收益 | 工作量 | 风险 | 状态 |
|---|---|---|---|---|---|
| **P0** | 地图初始化分帧（消除 1588ms 单帧） | 最长单帧 1588ms → < 100ms | 中（1～2 天） | 中（涉及初始化时序） | 🟡 代码完成，待真机测量与调参 |
| **P0** | Shader 变体预热 | 消除运行期编译 hitch，累计卡顿显著下降 | 小（半天） | 低 | 🟡 代码完成，待真机验证 |
| **P1** | 开启首包 brotli 压缩 / CDN 传输压缩 | 首包 27.1 MB → 约 10～15 MB | 小（1 小时） | 低 | 🟡 配置已改，待重导出+重传 CDN+真机验证 |
| **P1** | COS 设置 `Cache-Control` | 二次启动命中缓存 | 极小（5 分钟） | 无 | 🟡 仓库侧完成，待云端执行一次上传/控制台设置 |
| **P1** | 关闭 `brotliMT` | wasm 包缩小数百 KB～1 MB | 极小（1 分钟） | 无 | ✅ 配置已改，下次导出生效 |
| **P2** | 纹理 / 音频导入设置瘦身 | 首包再降 30%+ | 大（2～3 天） | 中（画质权衡） | ⬜ |
| **P3** | 实例化对象池 + GPU Instancing | 降低运行期卡顿与 GC | 中 | 低 | 🟡 对象池已完成，GPU Instancing 工具就绪待编辑器执行 |
| **不做** | wasm 代码分包 | 收益低（包体量级不匹配） | 大 | 中 | — |
| **不做** | 预下载 / AssetBundle 拆分 | 需先重构资源管线 | 很大 | 中 | — |

---

## 三、P0-1：地图初始化分帧（核心项）🟡 代码完成，待真机测量与调参

### 3.1 问题定位

`1588ms` 长耗时帧的成因已定位到确定位置：

`Assets/Scripts/Managers/MapPresentationBootstrap.cs:51-61` 的 `InitializeMapPresentation()` 是**完全同步的单帧操作**，由 `Assets/Scripts/Managers/GameFlowManager.cs:49` 在开局一次性调用，一帧内完成以下全部工作：

1. `SetupFogGlobalShaderProperties()` — 遍历全部 600 格算包围盒 + 创建雾遮罩纹理
2. `ChunkMapRender()` — **一次性构建全部 Chunk** 的 terrain / river / water / grid 四套 mesh
   （`ChunkMapRenderer.cs:229-235` 的 `foreach` 内串行执行 `BuildChunkStaging` + `CommitChunkStaging`）
3. `MergeChunkBoundaryNormals()` — 全量跨 Chunk 边界法线合并（`ChunkMapRenderer.cs:238-239`）
4. `InstantiateLandForms()` — 逐格 `Instantiate` 地貌 prefab，最多 600 次（`MapPresentationBootstrap.cs:87-97`）
5. `InstantiateResources()` — 逐格 `Instantiate` 资源 prefab，最多 600 次（`MapPresentationBootstrap.cs:106-116`）
6. `SetupEnvironmentFogEffect()` / `FogInit()` / `EnsureCostLabelRenderer()`

### 3.2 可复用的既有基础设施

**关键优势：分帧构建的能力已经存在**，只是当前仅用于运行期改地图，未用于开局全量构建。

| 已有能力 | 位置 |
|---|---|
| `IReadOnlyList<ChunkIndex> ComputeDirtyChunkIndices(...)` | `Core/Interfaces/IMapRenderBackend.cs:49`，实现在 `ChunkMapRenderer.cs:281` |
| `PreparedChunkGeometry PrepareChunkGeometrySlice(IReadOnlyList<ChunkIndex>)` — 每帧只构建指定 Chunk | `IMapRenderBackend.cs:55`，实现在 `ChunkMapRenderer.cs:288` |
| `void CommitChunkGeometry(PreparedChunkGeometry)` | `ChunkMapRenderer.cs:263` |
| 分帧驱动器（`ITickable`，每帧推进一批） | `Core/Services/MapMutation/MapSlicedCommitExecutor.cs` |
| `maxChunksPerFrame` 分帧参数（默认 2） | `MapMutationService.cs:399` `CommitSliced(..., int maxChunksPerFrame = 2)` |
| 逐格交互闸门（可在分帧期间锁全图） | `Core/Services/MapMutation/MapInteractionGate.cs` |
| Chunk 构建耗时埋点（默认关闭、零开销） | `ChunkMapRenderer.cs:43` `public static bool EnableChunkBuildTiming` |

### 3.3 必须尊重的约束（关键风险点）

`GameFlowManager` 是 `MonoBehaviour, IInitializable`，`Initialize()` 是 **Zenject 的同步初始化入口**，且注册了固定执行顺序：

```
GameInstaller.cs:364 → Container.BindInitializableExecutionOrder<GameFlowManager>(-30);
```

`Initialize()` 中 `InitializeMapPresentation()` 之后的每一步都依赖地图已就绪：

```
mapGenerator.Generate()                      // 1.  地图数据
_mapPresentationBootstrap.InitializeMapPresentation()  // 1.1 ← 待分帧
_arenaEventManager.OnMapInitialized()        // 1.2 依赖地图数据
_landFormMarkerManager.CreateAllMarkers()    // 1.5 依赖地貌模型已实例化
_publicBuildingGenerator.GenerateAll(...)    // 4.  依赖地图数据 + 场景根节点
mapGenerator.GenerateExplorationRewards()    // 4.1
PlayerInit()                                 // 5.  依赖 "PlayerBuilding" 根节点，末尾 _mapVisualEvent.Raise()
_logisticsService.RecalculateAll()
_endGame.MarkInitializationComplete()
```

**因此不能简单把 `Initialize()` 改成协程** —— 会破坏与其他 `IInitializable` 的顺序契约，且下游代码假定地图节点已存在。

### 3.4 实施方案（推荐：数据原子 + 表现分帧）

将 `InitializeMapPresentation()` 拆成「同帧必须完成的骨架」与「可分帧的重活」两段。

#### ⬜ 步骤 1：先量化，再切分（必做的前置步骤）

打开 `ChunkMapRenderer.EnableChunkBuildTiming`，在真机跑一次开局，记录以下四项各自耗时占比，用来决定切分粒度：

- `BuildChunkStaging`（terrain / river / water / grid 分别多少）
- `CommitChunkStaging`（mesh 上传 + 碰撞体）
- `MergeChunkBoundaryNormals`
- 两个 `Instantiate` 循环

> 不要跳过这一步。如果 1588ms 里 `Instantiate` 占了 1200ms，那么只分帧 mesh 构建等于白做。

> **未完成（需真机）**。代码实施已先行，并用两项设计对冲了跳过本步骤的风险：
> ① 两个阶段都分帧（mesh 与 `Instantiate`），不赌哪一项占比大；
> ② 每帧预算 `_chunksPerFrame` / `_instantiatesPerFrame` 是 `[SerializeField]`，可在 Inspector 直接调；
> ③ 新增 `MapPresentationBootstrap.EnableSlicedInitTiming` 埋点，分别统计骨架帧 / Chunk / 地貌 / 资源 / 收尾耗时与 `MaxSliceMs`，真机跑一次即可反推该调哪个预算。

#### ✅ 步骤 2：扩展接口

在 `IMapPresentationBootstrap` 增加分帧入口：

```csharp
public interface IMapPresentationBootstrap
{
    void InitializeMapPresentation();                 // 保留：编辑器/测试同步路径
    void BeginInitializeMapPresentation();            // 新增：建骨架（同帧）
    bool TickInitializeMapPresentation();             // 新增：推进一批，返回 true 表示全部完成
    bool IsPresentationReady { get; }
}
```

在 `ChunkMapRenderer` 增加两个方法（复用现有私有实现，不重写构建逻辑）：

```csharp
/// 只创建 Chunk 宿主 GameObject 与分组，不构建 mesh。返回全部 Chunk 索引。
public IReadOnlyList<ChunkIndex> PrepareChunkHosts(Vector3[] hexVertices);
```

`PrepareChunkHosts` 从现有 `ChunkMapRender()`（`ChunkMapRenderer.cs:202-243`）中抽出前半段：
`NormalizeWaterState` → 按 `ChunkIndex.Of` 分组 → `GetOrCreateChunk`（`ChunkMapRenderer.cs:853`），到此停止，不调用 `BuildChunkStaging`。

> **实施差异（均为实施中发现的必要修正）**：
> - `BeginInitializeMapPresentation(Action onReady = null)` 带了完成回调参数——收尾时需要重放资源可见性（见步骤 4 下方）。
> - `ChunkMapRender()` 反过来**复用** `PrepareChunkHosts`（而非各自实现前半段），保证同步/分帧两条路径的 Chunk 划分、水面归一化、雾初值逐位一致。
> - `PrepareChunkHosts` 里额外为**全图**预置 `cell.RealCenterWorldCoordinate`。该字段原本只在逐 Chunk 的 `PreBuildRectProfiles` 里被顺手写出，分帧后 `GameFlowManager` 同帧就要读它定位主城/AI 主城/公共建筑/地貌浮标。为此新增纯函数 `IMeshGenerator.ComputeSolidAreaCenter`（2 次噪声采样、不分配 44 点数组），并让 44 点数组的 0 号点直接调用它，杜绝两处公式漂移。详见 `.claude/debugging-playbook.md`。
> - 边界法线合并另拆出 `FinishInitialChunkBuild(...)`，逐帧提交走不合并的 `CommitChunkGeometrySlice(...)`。

#### ✅ 步骤 3：`MapPresentationBootstrap` 改造

```
BeginInitializeMapPresentation():
    hexVertices = GetHexVertices()
    SetupFogGlobalShaderProperties(hexVertices)      // 含 CreateFogMask，保持同帧
    chunkIndices = _chunkMapRenderer.PrepareChunkHosts(hexVertices)   // 只建宿主
    创建 LandForm / Resource 根节点（空节点，供下游 GameObject.Find 使用）
    初始化分帧队列：chunkIndices / 待实例化地貌格列表 / 待实例化资源格列表

TickInitializeMapPresentation():   // 每帧调一次
    阶段 A：从 chunkIndices 取 N 个 → PrepareChunkGeometrySlice(...) → CommitChunkGeometry(...)
            同时把 staging 累积进 _builtStaging（600 格量级，内存可接受）
    阶段 B：从地貌队列取 M 个 → Instantiate
    阶段 C：从资源队列取 M 个 → Instantiate
    全部空后收尾（仅一次）：
            MergeChunkBoundaryNormals(_builtStaging)   // 必须等全部 Chunk 建完
            SetupEnvironmentFogEffect()
            _mapVisualEvent.FogInit()
            EnsureCostLabelRenderer()
            IsPresentationReady = true; return true
```

分帧参数建议起始值：`N = 2`（对齐 `maxChunksPerFrame` 默认值），`M = 30`。按步骤 1 的实测数据调整，目标是把单帧压到 **< 16ms**（60fps 预算）或至少 **< 33ms**。

> **已按此实现**（`_chunksPerFrame = 2` / `_instantiatesPerFrame = 30`，两者均为 `[SerializeField]`）。两点实施差异：
> - 阶段 A 用 `CommitChunkGeometrySlice(...)`（不含法线合并），收尾统一调一次 `FinishInitialChunkBuild(_builtStaging)`；`MergeChunkBoundaryNormals` 读的是**已提交**的 mesh，逐帧合并既产生错误的中间法线又白做功。当前配置 `shadingStyle: FlatAll`，该收尾实为 no-op，故收尾帧很轻。
> - 阶段 B/C 的队列存**格子**而非 prefab，实例化时重新读 `cell.resource`；若骨架帧之后资源被消费，分帧路径自然跳过，最终状态与同步路径一致。

#### ✅ 步骤 4：初始化时序改造

`GameFlowManager.Initialize()` 改为：

```
mapGenerator.Generate()
_mapPresentationBootstrap.BeginInitializeMapPresentation()   // 骨架就绪，节点可 Find
... 后续全部数据层步骤保持原样、保持同帧原子 ...
_endGame.MarkInitializationComplete()
// 表现层剩余重活交给新增的 ITickable 分帧完成
```

新增 `MapPresentationSlicedInitExecutor : ITickable`，仿照 `MapSlicedCommitExecutor` 写法（`Core/Services/MapMutation/MapSlicedCommitExecutor.cs`），每帧调 `TickInitializeMapPresentation()`，完成后自行停止。在 `GameInstaller.cs` 用 `BindInterfacesTo` 注册。

**需要核对的一点**：`_landFormMarkerManager.CreateAllMarkers()`（`GameFlowManager.cs` 步骤 1.5）是否依赖 `cell.landFormModel` 已实例化。如果依赖，有两个选择：
- (a) 把地貌实例化提前到 `BeginInitializeMapPresentation()` 内同帧完成（只分帧 mesh 与资源模型）；
- (b) 把 `CreateAllMarkers()` 也挪到分帧收尾阶段。
优先选 (b)，(a) 会把 600 次 `Instantiate` 留在同帧。

> **✅ 已核对：两个方案都不需要。** `CreateAllMarkers()` 只读 `cell.landForm.markerPrefab`、`cell.RealCenterWorldCoordinate`、`cell.IsExploredBy`，不碰 `cell.landFormModel`，因此保持同帧原样即可（`RealCenterWorldCoordinate` 已由步骤 2 的全图预置保证正确）。
>
> **✅ 但核出了另一处真依赖**：`_publicBuildingGenerator.GenerateAll(...)` 内的 `MarkUnexplorableArea()` 会在骨架帧执行 `hex.resourceModel.SetActive(false)`，而分帧路径下 `resourceModel` 尚未创建。修法：由该生成器自己记录隐藏意图（`HashSet<HexCellData> _resourceHiddenHexes`）并暴露幂等的 `ApplyResourceVisibility()`，在 `BeginInitializeMapPresentation` 的完成回调 `OnMapPresentationReady` 里重放一次。
> 注意**不能**改成实例化时按 `SetActive(!cell.IsUnexplorable)` 推断——`ArenaEventManager` 也对 37 个竞技场预留格置 `IsUnexplorable = true` 但故意不隐藏资源，按标记反推会多藏一批。详见 `.claude/debugging-playbook.md`。

#### 🟡 步骤 5：分帧期间的用户可见行为

分帧期间地图是逐步出现的。两种处理：

- ⬜ **推荐**：保持微信 SDK 的加载遮罩/进度条到 `IsPresentationReady == true` 再隐藏。当前 `game.js` 配置 `hideAfterCallmain: true` + `HideAfterCallCallMain: 1`，会在 callmain 后立刻隐藏封面；需改为由 C# 侧主动通知隐藏（SDK 提供相应接口）。
  > 属导出配置改动，不在 C# 范围内，未做。
- ✅ **兜底**：分帧期间调用 `MapInteractionGate.LockCells(全部格)`，完成后 ~~`UnlockAll()`~~ **`UnlockCells(_lockedCells)`**，防止玩家在半成品地图上点击。
  > 改用 `UnlockCells` 而非 `UnlockAll`：`MapInteractionGate` 是无引用计数的 `HashSet`，`UnlockAll()` 会顺手清掉动画/事务持有的锁。已核对分帧期间无人消费该闸门（仅 `CardPresenter.CanDeploy` / `ExplorationService` / `UnitMovementSystem` 读它，三者都不在 `Initialize()` 期间运行），锁全图是安全的。

### 3.5 验证方式

- ⬜ 微信开发者工具「性能面板」重跑，看最长单帧是否 < 100ms
- ⬜ 打开 `EnableChunkBuildTiming` 对比改造前后各阶段耗时
- 🟡 跑 `Assets/Tests/MapMutationStage5Tests.cs`，确认既有分帧提交测试未被破坏
  > 编译已过（`dotnet build Tests.csproj` → 0 error），但**未实际执行**：本机 Unity batchmode 跑测试会在许可证握手处失败（见 `.claude/debugging-playbook.md` §Unity batchmode test execution），需在 Unity Test Runner 窗口手动跑 EditMode。
- ✅ 新增测试：全量分帧初始化完成后，Chunk 数量、mesh 顶点数、地貌/资源实例数与同步路径 `InitializeMapPresentation()` 完全一致
  > 落在 `Assets/Tests/MapPresentationSlicedInitTests.cs`。实现策略是**让两条路径共用同一段代码**（`PrepareChunkHosts` / `BuildChunkStaging` / `SpawnLandFormModel` / `SpawnResourceModel`），三项计数因此天然相同；测试守的是这份共用关系不被后续重构拆开（10 条源码契约），外加一条真数值测试 `ComputeSolidAreaCenter == BuildSolidArea().Center` 守住步骤 2 那枚防漂移的钉子。
  > 完整 A/B 数值对账需 Zenject 场景 + 渲染上下文（PlayMode），归入上面两条真机验证。

---

## 四、P0-2：Shader 变体预热 🟡 代码完成，待真机验证

### 4.1 问题

WebGL 平台 shader 编译是**同步阻塞**的：每个材质/变体第一次进入画面就是一次卡顿。这是 `13265ms` 累计卡顿里除长帧之外的另一大来源（长帧只占 1588ms，剩下约 11.7s 分散在运行期）。

### 4.2 实施

1. Project Settings → Graphics，用 **Shader Variant Collection** 录制一遍完整开局流程（含地图、单位、建筑、UI、雾效、VFX）
2. 保存为 `Assets/Resources/PreloadedShaders.shadervariants`
3. 在 loading 阶段（`InitializeMapPresentation` 之前，或 StartScene 内）调用 `ShaderVariantCollection.WarmUp()`
4. 清理 Graphics 设置里的 **Always Included Shaders**，未用到的移除（每个都会全变体编译，同时增大包体）
5. 检查 `Assets/Materials/Fog.mat` 等雾效材质的变体数量——全局 shader 属性多的 shader 变体容易爆炸

> **🟡 已按「编辑器自动收集 + 运行时预热」实现**，与计划「手动录制」路径不同（录制需在编辑器跑一遍完整开局，本轮无法执行）：
>
> - **变体收集改用编辑器工具**：`Assets/Editor/ShaderVariantPreloadTool.cs`，菜单 `Tools/微信小游戏性能优化/生成 Shader 变体预热集`。扫描 `Assets/Shader/`（24 个自有 shader）+ `Assets/Materials/` + TextMesh Pro 材质引用的 shader；优先用内部 API `ShaderUtil.GetShaderVariantEntries` 做**精确枚举**（含 surface shader 隐式生成的 fog/lightmap/shadow 变体），失败则回退关键字组合枚举。
> - **产物**：`Assets/Resources/PreloadedShaders.shadervariants`，并注册进 Graphics Settings → **Preloaded Shaders** 列表。这一步是关键：仅把 `.shadervariants` 放进 Resources 不会被 Shader Stripping 视为引用，变体仍可能被裁掉、运行时 `WarmUp()` 退化成 no-op；注册进 Preloaded Shaders 列表才保证变体保留进包。
> - **运行时预热**：`Assets/Scripts/Infrastructure/ShaderPreloader.cs`，`[RuntimeInitializeOnLoadMethod(AfterSceneLoad)]` 在首场景（StartScene）加载后同步 `WarmUp()`，等价于计划的「StartScene 内」预热。StartScene 只有一枚延迟 3s 的开始按钮，预热发生在玩家可操作之前。
> - **Always Included 清理与变体量核对**：同工具的第二菜单 `分析 Shader 变体与 Always Included` 输出——逐个列出 Always Included shader 是否被材质引用（候选移除项；prefab/代码直接引用不在扫描内，需人工确认后再删），并按 shader 打印变体数（对应计划第 5 条 Fog.mat 变体爆炸排查）。
> - **与 Always Included 的现状关系**：GraphicsSettings 里已有 **17 个自有 shader** 在 Always Included（全变体编译），另有 **7 个未在列表内**（`RealMaterialMaskBlend_Transition` / `TerrainBase_Fog_Transition` / `ThreeMaterialBlend_Land_Transition` / `MountainLowPoly_Fog_Transition` / `MountainLowPoly_Fog` / `FogConnector` / `ExplorationPillar`）——这 7 个才是运行期首次编译 hitch 的主要来源，正是变体集要覆盖的对象。
> - **待真机**：微信性能面板「总卡顿时长」对照（预期 13265ms → 3000ms 以内，配合 P0-1）。如需更早预热（藏进微信 loading 遮罩），把 `ShaderPreloader` 的 `AfterSceneLoad` 改为 `BeforeSceneLoad` 即可。

### 4.3 验证

对比预热前后微信性能面板的「总卡顿时长」；预期从 13265ms 降到 3000ms 以内（配合 P0-1）。

---

## 五、P1 组：配置级优化 🟡（brotliMT ✅ / Cache-Control 🟡 待云端 / 首包压缩 ⬜）

### 5.1 开启首包压缩

**现状**：`compressDataPackage: 0`，上传到 COS 的 `994d88a6233c8c42.webgl.data.unityweb.bin.txt` 是 27.1 MB **未压缩**文件，而 COS 直链默认不做传输层 gzip/brotli。

**二选一**：

- **方案 A（工程内，推荐先试）**：在微信小游戏导出面板勾选「压缩首包资源」（`compressDataPackage`）。SDK 会 brotli 压缩为 `.br`，客户端解压。预计 27.1 MB → 10～15 MB。代价：首次启动多约 200ms 解压耗时（SDK 官方说明）。
  > 注意：SDK 面板提示该选项「仅推荐使用小游戏分包加载时使用」。本项目走 CDN 加载，需实测确认插件在 CDN 模式下正确处理 `.br`（`game.js` 中 `compressDataPackage` 字段会同步为 `true`），若异常则回退方案 B。
- **方案 B（服务端）**：给 COS 桶挂腾讯云 CDN，开启「智能压缩」。工程零改动，效果等价且无客户端解压开销。

> **🟡 实施状态（配置已改，待重导出验证）**：已采用方案 A，`MiniGameConfig.asset` 的 `compressDataPackage: 0 → 1`。
> 已核对 `WXConvertCore.cs:1466-1541` 确认该选项在 **CDN 模式（`assetLoadType: 0`）下工作正常**：
> `compressDataPackage` 为真时对数据包执行 `Brotlib` 压成 `.br`（`dataFilename = *.br`），
> `convertDataPackageJS()` 会把 `game.js` 的 `$COMPRESS_DATA_PACKAGE` 占位符替换为 `"true"`，
> 客户端据此解压加载（首次启动多约 200ms 解压耗时，SDK 官方说明）。
> 剩下两步需在导出后执行：① 重新导出，确认 `name7/minigame/game.js` 出现 `compressDataPackage: true`、
> 且 CDN 产物由 `*.bin.txt` 变为 `*.bin.br`（体积预期 10～15 MB）；② 用带缓存头的 `上传CDN.bat`
> 重传 `.br` 文件到 COS（文件名带内容 hash，会变成新文件名）。

### 5.2 设置 CDN 缓存头

**现状**：COS 直链默认 `Cache-Control` 过弱，导致「未命中 CDN 缓存」。

**操作**：COS 控制台 → 桶 → 「文件默认配置 / 存储桶设置」，为资源目录设置：

```
Cache-Control: public, max-age=31536000
```

文件名已带内容 hash（`994d88a6233c8c42.` / `a28d8176b006e016.`），可安全长缓存。

> 说明：工具是在冷启动下测的，首次运行必然 miss，这一条建议本身有虚报成分。但设置缓存头零成本零风险，仍应执行。

> **🟡 实施状态（仓库侧已完成）**：本机未配置 COS 密钥（`coscmd` 未安装、`上传CDN.bat` 里仍是占位符），云端操作无法在仓库内直接执行。已完成能做的全部改动：
> - `上传CDN.bat` 的上传命令改为 `coscmd upload -r -f -H {"Cache-Control":"public,max-age=31536000"} ...`——之后每次打包上传，全部文件都会写入该缓存头；同名覆盖同时给**存量文件**补上头（覆盖即更新 HTTP 头）。
> - 剩余步骤任选其一即完成本项：① 把真实 SecretId/SecretKey 填进 `上传CDN.bat` 第 14 行，双击跑一次（27 MB 重新上传，覆盖存量并补缓存头）；② 控制台 → 桶 `7-2026824-1412305634` → 「文件默认配置」设置 `Cache-Control: public, max-age=31536000`（只对未来上传生效），再对存量文件批量「设置 HTTP 头」。
> - 验证：`curl -I https://7-2026824-1412305634.cos.ap-guangzhou.myqcloud.com/994d88a6233c8c42.webgl.data.unityweb.bin.txt`，响应应含 `Cache-Control: public, max-age=31536000`。

### 5.3 关闭 brotli 多线程压缩

**现状**：`CompileOptions.brotliMT: 1`。

SDK 面板原文提示（`Assets/WX-WASM-SDK-V2/Editor/WXEditorSettingHelper.cs:407`）：

> 开启多线程压缩可以提高出包速度，但会降低压缩率。**如若不使用 wasm 代码分包请勿用多线程出包上线**

多线程实现（`WXConvertCore.cs:1815` `MultiThreadBrotliCompress`）分块独立压缩，压缩率低于单线程（`WXConvertCore.cs:1805` `UnityUtil.brotli`）。本项目**既未使用代码分包又开了 MT**，属于纯损失。

**操作**：导出面板取消勾选「brotli 多线程压缩」，重新导出。预计 wasm 包 7.20 MB → 6.3～7.0 MB。

> 注意：`WXConvertCore.cs:1782` 有压缩缓存，切换 `brotliMT` 会触发重新压缩，出包时间会变长（单线程压 32MB wasm 可能数分钟）。

> **✅ 已执行**：`Assets/WX-WASM-SDK-V2/Editor/MiniGameConfig.asset` 第 71 行 `brotliMT: 1 → 0`。下次 Unity 导出时，压缩缓存因 `lastBrotliType` 改变而失效，自动以单线程重新压缩 wasm（出包变慢属预期）。导出后对比 `name7/minigame/wasmcode/` 下 `.br` 体积（基线 7.20 MB，预期 6.3～7.0 MB），并记得用带缓存头的 `上传CDN.bat` 重新上传。

### 5.4 复核 `profilingFuncs`

`CompileOptions.profilingFuncs: 1` → 给 emscripten 加 `--profiling-funcs`（`WXConvertCore.cs:1193-1196`），保留函数名以便读栈。symbols 文件已通过 `project.config.json` 的 `packOptions.ignore` 排除出包体，对包体影响有限。

**结论：保留**。可读的崩溃栈对毕设调试价值大于这点体积。（另注：`WXConvertCore.cs:1647` 注明「代码分包需要 symbol 文件以进行增量更新」，若将来做代码分包则必须保留。）

---

## 六、P2：资源瘦身（收益最大但耗时）⬜

治本项。目标是把 27.1 MB 首包源头压下来。

1. **纹理**（405 张 png / 174 MB + 14 张 tga / 75 MB + 2 个 psd / 47 MB）
   - WebGL 平台覆写：Max Size 降至 512 或 1024（UI 图标 256）
   - 强制压缩格式（DXT1/DXT5），关闭无必要的 Read/Write Enabled
   - 关闭无必要的 Mipmap（UI、图标）
   - `MiniGameConfig.asset` 里 `CompressTexture.halfSize: 0` / `useDXT5: 0` 可评估开启
   - `.psd` 源文件（47 MB）应移出 `Assets/` 或改 `.png` 导出
2. **音频**（26 个 wav / 22.5 MB）
   - 全部转 Vorbis，Quality 50～70
   - BGM：Streaming + Load In Background
   - SFX：Compressed In Memory
3. **未使用资源**
   - 5 个 `.unitypackage`（20 MB）应从 `Assets/` 移除（`fbslim: 1` 已开启，会清理未引用资源，但源文件仍占仓库）
   - `Assets/Toon_RTS`（186 MB）、`Assets/KayKit`（41 MB）、`Assets/VFXPACK_...`（27 MB）按实际引用裁剪
   - 已有 `Assets/_ImportArchive/` 归档机制，延续使用
4. **模型**（561 个 fbx / 75 MB）
   - 关闭未使用的 Import Animation / Import Materials
   - Mesh Compression 设为 Medium，关闭 Read/Write

**验证**：每轮导出后对比 `game.js` 的 `DATA_FILE_SIZE`。

---

## 七、P3：运行期卡顿的其余来源 🟡

- **对象池** ✅：`Managers/ExplorationPillarPool.cs` / `ExplorationCoinPresenter` / `CostLabelRenderer` 已有池化范例。
  本轮新增通用池 `Assets/Scripts/Utilities/PrefabPool.cs`（按 prefab 分桶，Queue + HashSet 防重复回收），
  并把 `UIController` 敌方单位指示器的「每次刷新 Instantiate/Destroy」改为池化（`ClearEnemyIndicators` 回收、
  `ShowEnemyIndicators` 取用、`OnDestroy` 清池）。
  > 说明：`MapPresentationBootstrap` 两个 `Instantiate` 循环（最多 1200 次）经核对是**开局一次性**实例化，
  > 不是运行期反复 spawn/destroy，对象池不减少首次 1200 次实例化，故未对其池化；真正受益的是运行期反复创建销毁的瞬态对象。
  > 其余池化候选（资源拾取特效 reapEffect、战术卡飞牌 flyGO）因涉及粒子 Play-on-Awake / DOTween 状态复位，
  > 池化需额外处理重播逻辑，本轮暂缓，避免引入不可验证的视觉回归。
- **GPU Instancing** 🟡：新增编辑器工具 `Assets/Editor/GPUInstancingTool.cs`，两个菜单：
  `批量开启材质 GPU Instancing`（对 `Assets/Materials` + `Assets/KayKit` + `Assets/Toon_RTS` 的材质置
  `enableInstancing = true` 并保存）与 `检查材质 Shader 引用`（统计 shader 使用、标记引用缺失 shader 的材质）。
  > 待编辑器执行；另需注意两点局限：① Chunk 地形是逐 Chunk 唯一 mesh，不受 Instancing 影响；
  > ② 实施中发现 `KayKit` 模型包材质引用的 shader（guid `933532a4fcc9baf4fa0491de14d08ed7`）
  > 与地貌/资源 SO 的 `modelPrefab`（如 Forest/Stone/Animals）均不在当前 `Assets/` 的 .meta 中、
  > 且 `MapLandFormDatabase`/`MapResourceDatabase` 的列表为空——地貌/资源模型管线当前处于重配中间态，
  > 需先修好 prefab/材质引用，Instancing 才能落到实际 DrawCall 上。用「检查材质 Shader 引用」菜单可一键列出缺失项。
- **GC**（暂缓）：`ChunkMapRenderer` 构建路径大量 `new List<>`；分帧后单帧 GC 压力已降低，如仍有尖刺再考虑缓冲区复用。
- **`MemorySize: 256`**（待真机）：确认真机峰值 UnityHeap 不超过 256MB，避免运行期扩容尖刺。

---

## 八、明确不做的项及理由

### 8.1 wasm 代码分包 —— 不做

- `game.json` 已有 `wasmcode` 分包 + `parallelPreloadSubpackages`，但那只是「把 wasm 放进小游戏分包」，不是微信所指的 code split（切块、边下边编译、版本间增量更新）
- 真做需要微信开发者工具的 Unity 分包工具插件 + 改造导出流程
- 该能力主要服务 20～30 MB 量级的重度包（参见 `WXConvertCore.cs:1428` 关于 30MB 代码分包总大小限制的注释）；本项目 wasm 仅 7.2 MB，收益远小于工程成本
- **真正的「首资源包较大」元凶是 27.1 MB 未压缩资源包，由 §5.1 解决**

### 8.2 预下载能力 —— 当前无从下手

- `preloadFiles` 空、`preloadDataList` 空
- `DeleteStreamingAssets: 1`，工程无 AssetBundle
- **所有资源都在唯一的首包里，不存在"可以延后下载的内容"可供预下载**
- 前置条件：先把资源拆成 AssetBundle 走 CDN。属资源管线重构，工作量与毕设收益不匹配

### 8.3 「可缓存文件过少 / 网络并发过少」—— 与 8.2 同源

- SDK 只自动缓存 URL 含 `CDN + StreamingAssets` 的文件（`bundlePathIdentifier: StreamingAssets;`），而 StreamingAssets 已被构建清空 → 除首包外无可缓存文件
- 全程仅 2 个下载请求，并发无从谈起
- 这两条与「未使用预下载」本质是同一个诉求：**把资源拆成多个可缓存文件**
- 其中 §5.2 的 `Cache-Control` 是唯一可独立完成的部分，已单列为 P1

以上三项建议写入论文「未来工作」章节，说明已识别、已评估、因工程成本与收益比而暂缓。

---

## 九、验收指标

| 指标 | 优化前 | 目标 |
|---|---|---|
| 最长单帧 | 1588 ms | < 100 ms |
| 累计卡顿 / 运行时长 | 13265 / 31697 = 41.9% | < 10% |
| 首包下载量 | 27.1 MB | < 15 MB（P1）/ < 10 MB（P2） |
| wasm 包 | 7.20 MB | < 7.0 MB |
| 二次启动 | 未命中缓存 | 命中缓存 |

---

## 十、执行顺序

```
⬜ 第 1 步  打开 EnableChunkBuildTiming，真机量化 1588ms 的构成      ← 决定后续切分粒度
🟡 第 2 步  P1 三项配置改动（brotliMT ✅ / Cache-Control 🟡 待云端执行 / 首包压缩 ⬜）    ← 当天完成，先拿确定收益
🟡 第 3 步  P0-2 Shader 预热（代码完成，待真机验证）
🟡 第 4 步  P0-1 地图初始化分帧（本计划核心，含时序核对与回归测试）   ← 代码与测试已完成，待真机调参
⬜ 第 5 步  重跑微信性能监控，对照第九节验收指标
⬜ 第 6 步  P2 资源瘦身（迭代式，每轮对比 DATA_FILE_SIZE）
⬜ 第 7 步  P3 按剩余卡顿情况选做
```

> **实际执行顺序与计划不同**：先做了第 4 步（P0-1 代码），第 1 步的真机量化未做。
> 原因是量化需要真机跑一次开局，本轮无法进行；改为「两个阶段都分帧 + 预算可在 Inspector 调 + 加 `EnableSlicedInitTiming` 埋点」来对冲，把「量化」推迟到与第 5 步一起做：真机跑一次即可同时拿到验收数据和调参依据。

### 下一步（按当前状态）

1. Unity Test Runner 跑一遍 EditMode（`MapPresentationSlicedInitTests` + `MapMutationStage5Tests` + `ChunkRendererTests`）——本机 batchmode 因许可证跑不了。
2. 在 Unity Editor 跑 `Tools/微信小游戏性能优化/生成 Shader 变体预热集`，生成 `Assets/Resources/PreloadedShaders.shadervariants` 并注册进 Preloaded Shaders；再跑 `分析 Shader 变体与 Always Included` 核对 7 个未入列表的 shader 已覆盖、并按变体数排查雾效材质（P0-2）。
3. 真机跑开局，打开 `MapPresentationBootstrap.EnableSlicedInitTiming` 与 `ChunkMapRenderer.EnableChunkBuildTiming`，按 `MaxSliceMs` 调 `_chunksPerFrame`（当前 2）/ `_instantiatesPerFrame`（当前 30），目标最长单帧 < 100ms。
4. P1 三项状态：`brotliMT` 已关闭（下次导出生效）；`Cache-Control` 仓库侧已改好（`上传CDN.bat` 带缓存头），待填密钥跑一次上传或控制台设置即完成；首包压缩（§5.1）已改 `compressDataPackage: 1`，待重新导出并重传 `.br`。三项齐了再重跑性能面板对照第九节。
5. P3 对象池 + GPU Instancing：对象池代码已完成（`PrefabPool` + 敌方指示器池化）；GPU Instancing 需在 Unity Editor 跑 `Tools/微信小游戏性能优化/批量开启材质 GPU Instancing`，并跑 `检查材质 Shader 引用` 排查 §7 提到的缺失 shader / 空数据库问题，再回 DrawCall 对照验证。

---

## 附录：关键文件索引

| 文件 | 作用 |
|---|---|
| `Assets/WX-WASM-SDK-V2/Editor/MiniGameConfig.asset` | 小游戏导出全部配置 |
| `Assets/WX-WASM-SDK-V2/Editor/WXConvertCore.cs` | 导出/压缩核心逻辑 |
| `Assets/WX-WASM-SDK-V2/Editor/WXEditorSettingHelper.cs` | 导出面板与选项说明 |
| `Assets/Scripts/Managers/MapPresentationBootstrap.cs` | 地图表现层初始化（P0-1 主战场） |
| `Assets/Scripts/Managers/ChunkMapRenderer.cs` | Chunk 几何构建（3333 行，含既有分帧接口） |
| `Assets/Scripts/Managers/GameFlowManager.cs` | 开局时序（P0-1 需改造调用点） |
| `Assets/Scripts/Core/Interfaces/IMapRenderBackend.cs` | 分帧构建接口定义 |
| `Assets/Scripts/Core/Services/MapMutation/MapSlicedCommitExecutor.cs` | 分帧驱动器范例 |
| `Assets/Scripts/Core/Services/MapMutation/MapMutationService.cs` | `CommitSliced` / `TickSlicedCommit` 实现 |
| `Assets/Scripts/Infrastructure/Installers/GameInstaller.cs` | Zenject 绑定与初始化顺序 |
| `Assets/Scripts/Core/Services/MapMutation/MapPresentationSlicedInitExecutor.cs` | **P0-1 新增**：分帧初始化驱动器（`ITickable`，就绪后自停） |
| `Assets/Tests/MapPresentationSlicedInitTests.cs` | **P0-1 新增**：中心点数值一致性 + 10 条分帧源码契约 |
| `Assets/Scripts/Infrastructure/ShaderPreloader.cs` | **P0-2 新增**：运行时加载并 `WarmUp()` 变体集（`AfterSceneLoad` 启动钩子） |
| `Assets/Editor/ShaderVariantPreloadTool.cs` | **P0-2 新增**：编辑器工具，生成变体集 + 注册 Preloaded Shaders + 分析 Always Included/变体数 |
| `Assets/Scripts/Utilities/PrefabPool.cs` | **P3 新增**：通用预制体对象池（按 prefab 分桶，防重复回收） |
| `Assets/Editor/GPUInstancingTool.cs` | **P3 新增**：编辑器工具，批量开启材质 GPU Instancing + 检查材质 shader 引用 |
| `.claude/debugging-playbook.md` | P0-1 踩到的两个非平凡坑（隐式写出的逻辑字段 / 标记不等于副作用） |
| `name7/minigame/game.js` | 运行期配置（`DATA_FILE_SIZE` 等） |
| `name7/minigame/game.json` | 分包与预加载声明 |
| `name7/minigame/project.config.json` | `packOptions.ignore` 出包排除规则 |
