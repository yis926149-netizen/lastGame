# 弓箭手箭矢飞行效果实施计划

> 目标：让弓箭手（archer）攻击时像箭塔一样，真正射出一支会飞的箭，而不是只有"瞬间扣血 + 原地播放动画"。
> 范围：本文件仅记录实施方案，不做任何代码/预制体改动。

---

## 一、目标

| 现状 | 目标 |
| --- | --- |
| 弓箭手攻击 = `CombatResolver` 瞬间结算伤害 + `PlayRangedAttackAnim` 原地播放动画/音效，**无可见飞行箭矢** | 弓箭手每次远程攻击都实例化一支箭，沿弧线从弓/手上飞到目标，视觉上"真的射出一支箭" |

---

## 二、现状分析（已核实的事实）

### 1. 箭塔的射箭实现（我们要复用的效果）

文件：`Assets/Scripts/Controllers/ArrowTowerShooter.cs`

- `_arrowPrefab`：箭矢预制体，指向 `Assets/Model/BuildingModel/arrow.prefab`（guid `2b4b9deedcf97024a9824a94da40115d`）。
- `_shootPoint`：发射点，箭塔预制体 `arrow_tower_a_red.prefab` 内的子节点 `shoot-point`（本地坐标 `(0, 1.49, 0)`，scale 0.4）。
- `FireArrow()`（第 170~221 行）核心逻辑：
  1. `Object.Instantiate(_arrowPrefab)`；
  2. 起点 = `_shootPoint.position`，终点 = 目标位置 + `Vector3.up * 1f`；
  3. 用 DOTween 构造 3 个控制点：`起点 → 中点(抬高 _arcHeight) → 终点`，`DOPath(PathType.CatmullRom)` 沿弧线飞行；
  4. `Join(DOScale(0.5f))` 让箭飞行中缩小，`OnUpdate` 里持续 `LookAt(endPos)`；
  5. `OnComplete` 里销毁箭，并在命中时扣血（`targetData.currentHp -= _damage`）→ 更新血条 → 死亡时 `UnitRemovalService.RemoveUnit`。
- 箭塔数值（射程/间隔/伤害/弧高/飞行时长）全部来自 Excel，经 `Assets/Scripts/Core/Services/CoreGameplayConfigProvider.cs` 暴露：`ArrowTowerArcHeight`、`ArrowTowerFlightDuration` 等。

### 2. 弓箭手的远程攻击实现（要改造的地方）

文件：`Assets/Scripts/Units/RangedStrategy.cs`

- `DoCombat()`（第 102~170 行）：
  1. 选目标（单位或建筑，射程内最近者）；
  2. `brain.Combat.Resolve(...)` / `ResolveBuilding(...)` —— **瞬间扣血**（见 `Assets/Scripts/Core/Services/CombatResolver.cs`，纯数据层，调用即结算，含派系 buff、地形、雕像、河流、地貌防御等完整公式）；
  3. `umc.PlayRangedAttackAnim(bestTarget)` —— 原地朝向 + 攻击动画 + 音效（`Assets/Scripts/Controllers/UnitMovementController.cs` 第 770 行，注释明确"伤害已瞬间结算，此处仅负责表现"）；
  4. `brain.MarkAttacked()` —— 启动攻速冷却。

- 结论：**全程没有 `Instantiate` 任何箭矢，也没有飞行弹道。**

### 3. 弓箭手配置与模型

- 配置：`Assets/Scripts/ScriptableObjects/UnitConfigS/archer.asset`
  - `id: 12`，`unitName: 弓兵`
  - `strategyType: 2`（`UnitStrategyType.Ranged`，见 `Assets/Scripts/ScriptableObjects/UnitStrategyType.cs`）
  - `BasicAttackRange: 2`，`BasicAttackValue: 15`，`hp: 20`，`AttackInterval: 0`
  - `unitModel` → `Assets/Model/CharacterModel/archer_blue.prefab`（玩家）
  - `enemyUnitModel` → `Assets/Model/CharacterModel/archer_red.prefab`（敌方）
- 模型：两个弓箭手 prefab 内都有弓 `WK_weapon_Bow`（例如 `archer_red.prefab` 第 5074 行），另有 `Bip001 R Hand` / `R_hand_container` / `Quiver_container` 等可用作发射点的骨骼节点。

### 4. 单位生成入口（决定新组件在哪挂载）

玩家侧：
- `Assets/Scripts/Core/Services/CardPresenter.cs` `SpawnUnit()`（第 359 行起）：`Instantiate(prefab)` → `AddComponent<UnitMovementController>()` → `InjectGameObject` → `AddComponent<PlayerUnitBrain>()`。
- `Assets/Scripts/Controllers/BarracksSpawner.cs` `TrySpawnUnit()`：同样的 `Instantiate` + `AddComponent<UnitMovementController>()` 流程。

敌方（AI）侧：
- `Assets/Scripts/AI/AIEntityFactory.cs` `GenerateUnit()`（第 145 行起）：`Instantiate(_unitDataProvider.GetEnemyUnitPrefab(...))` → `AddComponent<UnitMovementController>()` → `AddComponent<AIUnitBrain>()`。

> 参考：箭塔的 `ArrowTowerShooter` 是直接挂在箭塔预制体上、并在 `CardPresenter.SetupArrowTowerShooter()` / `AIEntityFactory` 里 `GetComponent ?? AddComponent` 的。弓箭手可以照搬这一模式。

---

## 三、总体方案（推荐）

**核心思路：新增一个只负责"射箭表现"的组件 `UnitRangedShooter`，挂到弓箭手模型预制体上（复用箭塔的 `arrow.prefab` 与弓作为发射点），由 `RangedStrategy.DoCombat` 在结算伤害后触发一次射箭。伤害仍由 `CombatResolver` 瞬间结算，箭矢仅作视觉表现（最低风险，不改变数值平衡）。**

关键点：

1. **箭矢飞行逻辑抽取为共享工具**（避免和箭塔重复代码）：
   - 新增静态工具类 `ArrowFlight`（或 `ArrowProjectile` 组件），封装 `Play(prefab, from, to, arcHeight, duration, onArrive)`。
   - 箭塔与弓箭手都调用它。箭塔改动属低风险重构，需回归验证箭塔仍正常。

2. **新增 `UnitRangedShooter` MonoBehaviour**（镜像 `ArrowTowerShooter` 的射箭部分）：
   - 字段：`_arrowPrefab`、`_shootPoint`（可空，空则回退 `transform.position + up*偏移`）。
   - 方法：`Shoot(GameObject target)` —— 只负责生成箭并飞过去（纯表现，不扣血）。

3. **发射点**：优先用弓 `WK_weapon_Bow`（或右手 `Bip001 R Hand`）作为 `_shootPoint`；若想更可控，可在弓下新增子节点 `shoot-point`（同箭塔做法）。

4. **触发点**：`RangedStrategy.DoCombat` 中，在 `brain.Combat.Resolve(...)` 之后、`PlayRangedAttackAnim` 前后，调用
   `brain.Owner.unitMovementController.GetComponent<UnitRangedShooter>()?.Shoot(bestTarget)`。

5. **参数来源**（弧高/飞行时长）：二选一，见"待定决策"。

---

## 四、详细改动清单（文件级）

### 新增

| 文件 | 说明 |
| --- | --- |
| `Assets/Scripts/Combat/ArrowFlight.cs`（或 `Assets/Scripts/Utilities/ArrowFlight.cs`） | 静态工具：封装 DOTween 弧线飞行（起点/终点/弧高/时长/到达回调），箭塔与弓箭手共用 |
| `Assets/Scripts/Units/UnitRangedShooter.cs` | 挂在弓箭手模型上的 MonoBehaviour，暴露 `_arrowPrefab`/`_shootPoint`，提供 `Shoot(target)` |

### 修改（代码）

| 文件 | 改动 |
| --- | --- |
| `Assets/Scripts/Units/RangedStrategy.cs` | `DoCombat` 内结算伤害后，取 `UnitRangedShooter` 组件并调用 `Shoot(bestTarget)` |
| `Assets/Scripts/Controllers/ArrowTowerShooter.cs` | `FireArrow` 改为调用共享 `ArrowFlight`（**可选**，用于消除重复；不做也能跑） |

### 修改（预制体/配置，建议在 Unity 编辑器操作，不手改 YAML）

| 文件 | 改动 |
| --- | --- |
| `Assets/Model/CharacterModel/archer_blue.prefab` | 挂 `UnitRangedShooter`，`_arrowPrefab` 指向 `arrow.prefab`，`_shootPoint` 指向 `WK_weapon_Bow`（或新增 `shoot-point` 子节点） |
| `Assets/Model/CharacterModel/archer_red.prefab` | 同上（敌方模型） |

### 可选（参数/音效打磨）

| 文件 | 改动 |
| --- | --- |
| Excel 核心玩法配置 + `Assets/Scripts/Core/Services/CoreGameplayConfigProvider.cs` | 新增单位远程箭矢的弧高/飞行时长（若走"Excel 唯一主源"路线） |
| `Assets/Scripts/ScriptableObjects/UnitConfigS/archer.asset` | 把 `attackSfx` 里的刀剑音效（`Short_Sword_Hit`）换成弓箭音效（可选，当前用剑砍音效不搭） |
| `Assets/Scripts/Utilities/PrefabPool.cs` | 若大量弓箭手同时开火，可把箭矢改为对象池复用（可选优化） |

---

## 五、实施步骤（建议顺序）

1. **抽共享工具**：新增 `ArrowFlight.cs`，把 `ArrowTowerShooter.FireArrow` 的 DOTween 逻辑搬进去，先不改箭塔调用方（或同步改造并回归）。
2. **新增组件**：`UnitRangedShooter.cs`，内部调用 `ArrowFlight.Play`。
3. **编辑器挂组件**：给 `archer_blue.prefab`、`archer_red.prefab` 挂 `UnitRangedShooter`，拖好 `_arrowPrefab` 与 `_shootPoint`（弓节点）。
4. **接触发器**：`RangedStrategy.DoCombat` 增加射箭调用。
5. **参数接入**：决定弧高/时长的来源（复用箭塔值 or 新增单位专用值），写入 `UnitRangedShooter` 的取值处。
6. **联调验证**：见下一节。

---

## 六、测试与验证

- [ ] 玩家弓箭手攻击敌方单位：有可见箭矢从弓飞向目标，命中后目标扣血。
- [ ] 玩家弓箭手攻击敌方建筑/宝箱（`NeutralBuilding`）：箭矢同样飞出，建筑正常受伤。
- [ ] 敌方（AI）弓箭手攻击玩家单位：同样有箭矢表现。
- [ ] 兵营（`BarracksSpawner`）产出的弓箭手：组件与箭矢正常（兵营是第三条生成链路，容易漏）。
- [ ] 目标在箭飞行途中死亡/被移除：箭矢能正常销毁、无 NullReference（纯表现方案下伤害已瞬间结算，此场景应天然安全）。
- [ ] 箭塔回归：若重构了 `FireArrow`，确认箭塔射箭、扣血、锁敌、断供停火均无变化。
- [ ] 性能：一波多弓箭手齐射不出现明显卡顿（必要时上对象池）。

---

## 七、风险与回滚

| 风险 | 说明 | 应对 |
| --- | --- | --- |
| 伤害时机变化 | 若改成"箭命中才扣血"，会牵动 `CombatResolver` 完整公式与目标死亡/移位的判空 | 推荐方案保持"瞬间结算 + 箭矢纯表现"，不碰结算时序 |
| 预制体手工改 YAML 出错 | 两个弓箭手 prefab 结构复杂、fileID 多 | 强烈建议在 Unity 编辑器里挂组件拖引用，不手改 YAML |
| 箭塔回归 | 共享 `ArrowFlight` 会改动箭塔路径 | 重构后必做箭塔回归；或先独立复制、后续再合并 |
| 发射点为空 | 运行时找不到 `_shootPoint` | `UnitRangedShooter` 提供 `transform.position + up*偏移` 兜底 |

---

## 八、待定决策（实施前需确认）

1. **伤害时机**：
   - 方案 A（推荐）：伤害仍由 `CombatResolver` 瞬间结算，箭矢纯视觉表现。改动最小、无平衡变化。
   - 方案 B：对齐箭塔，改成"箭命中时才扣血"。更真实，但需延迟结算、处理目标中途死亡/移出、并把完整伤害公式搬到命中回调，改动与风险更大。

2. **箭塔是否同步复用共享工具**：
   - 复用（推荐，长期整洁，需回归）；或独立复制（短期快，代码重复）。

3. **弧高/飞行时长参数来源**：
   - 复用箭塔 Excel 值 `ArrowTowerArcHeight` / `ArrowTowerFlightDuration`（最快）；
   - 或新增"单位远程箭矢"专用参数（更符合"Excel 唯一主源"的项目约定，但要多改 Excel 与配置生成链路）。

---

## 九、涉及文件速查

- 箭塔效果参考：`Assets/Scripts/Controllers/ArrowTowerShooter.cs`
- 弓箭手策略：`Assets/Scripts/Units/RangedStrategy.cs`
- 伤害结算：`Assets/Scripts/Core/Services/CombatResolver.cs`
- 表现动画：`Assets/Scripts/Controllers/UnitMovementController.cs`（`PlayRangedAttackAnim`）
- 弓箭手配置：`Assets/Scripts/ScriptableObjects/UnitConfigS/archer.asset`
- 弓箭手模型：`Assets/Model/CharacterModel/archer_blue.prefab`、`archer_red.prefab`（弓节点 `WK_weapon_Bow`）
- 箭矢预制体：`Assets/Model/BuildingModel/arrow.prefab`
- 单位生成链路：`Assets/Scripts/Core/Services/CardPresenter.cs`、`Assets/Scripts/Controllers/BarracksSpawner.cs`、`Assets/Scripts/AI/AIEntityFactory.cs`
