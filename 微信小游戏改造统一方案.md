# 微信小游戏改造统一方案

> 目标：将本 Unity 项目构建为微信小游戏。
>
> 技术路线：Unity 2022.3.62f3c1 -> WebGL/IL2CPP -> `minigame-unity-webgl-transform` -> 微信开发者工具/真机。
>
> 本文是统一后的唯一工作文档，只保留已核对事实、明确决策和可执行步骤。`H5小游戏改造难点分析报告.md` 属于裸 WebGL/H5 路线，不在本次合并范围内。

## 0. 执行进度

- 阶段 0 完成：4 个无用 `using Unity.VisualScripting;` 已删除；`MainGame.asmdef` 已移除 UnityChan GUID。
- 阶段 A 进行中：
  - 已删除 `com.unity.visualscripting`、`com.unity.postprocessing`、`com.unity.shadergraph`。
  - 已清除全部平台 `UNITY_POST_PROCESSING_STACK_V2` 宏，`WeixinMiniGame` 节点保留为空值。
  - 已清空 QualitySettings Ultra 档与 GraphicsSettings 的 URP 引用（`m_SRPDefaultSettings: {}`）。
  - 已删除 `com.coplaydev.coplay`；`com.unity.inputsystem: 1.14.0` 已显式声明为直接依赖；glTFast 无项目引用，随 Coplay 移除。
- 阶段 B 进行中：`Assets/unity-chan!` 已整目录移出至 `_RemovedAssets/阶段B/unity-chan!`（1166 文件），根目录 `UnityChan.csproj` 已删除。第二批已移出 Model 孤儿链 92 个、UnitConfigS 12 个、BuildingConfigS 4 个（共 108 文件 + 108 meta，与清单一致，源目录保留文件与保留清单吻合）。**瘦身已按用户决定停止**：Scripts 下 19 个孤儿 SO、Toon_RTS 195 个未用变体、`_ImportArchive` 整体均列为暂不处理，不追求极致包体。
- 阶段 C 完成：Development Build 成功（183s）。反射烘焙已生效：`Added reflection baking to '510' types in assembly 'Zenject.dll'`；`Could not find actual type ... skipping` 均为编译器生成类型（lambda/迭代器状态机），属预期跳过。`MainGame` 经 `Assets/link.xml`（`preserve="all"`）保护，不经烘焙（MainGame 未引用 `Zenject-Usage` 且不在 `Zenject` 命名空间，符合“最小 link.xml 起步”设计）。阶段 C 体积代价：wasm.gz +129 KB、data.gz +55 KB。
- 阶段 D 完成（内存）：固定内存 128/256 MB 均 OOM（`Could not allocate memory` / `memory access out of bounds`，崩溃于启动 `Serialization` 阶段）。已核实非资源问题：data 28 MB、场景 0.3 MB、构建内贴图全 ≤1024、音频 CompressedInMemory。规律为“已用量 = 上限 − 41 MB”（128→87、256→215），即启动反序列化存在与堆同量级的瞬时内存需求，固定内存无法容纳。**已定案**：Memory Growth Mode=Geometric + Initial 128 + Max 2048，实测正常运行。固定内存的 iOS 优化推迟到装 SDK 后用 Memory Profiler 定位瞬时峰值。另发现 `Assets/UI/map/` 下 6 张 9449×14173 超大地图图（test-1/map-1..5）为无引用孤儿（不在构建），列为待清理。
- 卡牌拖拽误触相机平移已修复（`CardController` 拖拽状态 + `CameraController` 阻断）。
- 阶段 E 进行中（SDK）：已安装 `Assets/WX-WASM-SDK-V2`（v0.1.33，2026-08-18，双引擎统一 SDK，代码含 `EngineVersion.Unity`/`EngineVersion.Tuanjie` 双分支，标准 Unity 2022.3 兼容）。入口为菜单「微信小游戏 → 转换小游戏」，不注册独立构建平台。首次「生成并转换」成功（曾报 `Incompatible color space with graphics API`，因面板 `Webgl2` 未勾选导致 SDK 切到 OpenGLES2；勾选 WebGL2 后解决）。产物已生成于 `C:\Users\Administrator\Desktop\name7\minigame`（game.json/game.js/project.config.json/webgl.wasm.framework.unityweb.js，wasmcode+data-package 分包）。待办：①`game.json` 的 `deviceOrientation` 当前为 portrait（面板「游戏方向」=Portrait），游戏为横屏需改 Landscape；②`MemorySize` 当前 256 MB 固定内存，浏览器固定 256 曾 OOM，微信真机需评估是否上调；③微信开发者工具导入验证。

## 1. 当前基线

### 已确认事实

| 项目 | 当前状态 | 证据 |
|---|---|---|
| Unity | 2022.3.62f3c1 | `ProjectSettings/ProjectVersion.txt` |
| 构建场景 | `StartScene`、`GameScene` | `EditorBuildSettings.asset` |
| 渲染管线 | 实际使用内置管线 | `GraphicsSettings.asset`；WebGL 默认 High 档 |
| 迷雾 | 依赖内置管线 | `FogEnvironmentSelectiveEffect.cs` 使用 `OnRenderImage`、Blit、CommandBuffer、整屏 RT |
| 地图 | 20 x 24，共 480 格 | `MapGenerationConfig.asset` |
| DI | Zenject，大量场景和动态对象注入 | `GameInstaller.cs`、`ProjectContext.prefab` |
| WebGL 裁剪 | High（`managedStrippingLevel: 3`） | `ProjectSettings.asset` |
| 输入 | `activeInputHandler: 0`，旧 Input API | `ProjectSettings.asset`、`InputService.cs` |
| 微信 SDK | 当前未接入 | `Assets`、`Packages` 检索 |

### 历史构建基线

| 文件 | gzip | 解压后 |
|---|---:|---:|
| wasm | 8.37 MB | 32.43 MB |
| data | 19.82 MB | 26.95 MB |
| framework.js | 0.08 MB | 0.41 MB |

这些值只用于前后对比。A-D 完成后必须重新构建，不能直接用于最终分包，也不能把 wasm 8.37 MB 当作绝对验收上限。

### 已完成的输入改造

以下代码已落地，尚待 Unity、开发者工具和真机验证：

- `IInputService` 增加 `PinchRatio`、`IsMultiTouch`（捏合采用相对起点的间距比值，不依赖 deltaPosition，避免微信 WebGL 触摸符号反转）。
- `CameraController.HandlePinchZoom` 接入双指捏合缩放：张开=拉近（地图放大）、合拢=拉远；滚轮路径保持不变。
- 探索改为抬起确认 tap：位移不超过 20 px、时间不超过 250 ms。
- 相机拖拽增加 10 px 启动阈值，双指时禁用单指拖拽。
- 卡牌落点改用 `eventData.position`。
- 删除相机旋转和 Ctrl+A/D 路径。
- 删除远程攻击右键残留，保留自动索敌。
- V、R、F 调试键限制在 Editor/Development Build。
- 微信 WebGL 下退出按钮不调用无效的 `Application.Quit`。
- `activeInputHandler` 从 2 改为 0。

待验证：tap 手感、捏合倍率、卡牌上浮到拖拽的衔接、InputField 软键盘。

## 2. 已确认决策

1. **使用内置渲染管线。** 不切 URP，不重写迷雾；RT 降级放到真机 Profiling 后。
2. **保留键鼠路径。** 移动端只增加触摸能力。
3. **第一阶段只支持单指主操作 + 双指捏合。** 不做并行 pointerId 语义层。
4. **暂不引入 AssetBundle/Addressables。** DataCDN 方案以 SDK 转换结果、实际包体和加载方式为准。
5. **不做联机。**
6. **登录、分享、存档后置。** 优先保证启动、切场景、地图、战斗、迷雾和触摸操作。

## 3. 当前阻断项

### Visual Scripting 依赖清理（已完成）

4 个无用 `using Unity.VisualScripting;` 已删除，`com.unity.visualscripting` 包已移除。删除前已确认无真实 API/扩展方法调用。

### UnityChan 程序集引用已解除，资源待移出

`Assets/Scripts/MainGame.asmdef` 中 UnityChan 的 GUID（`fd4d9a3eff7aeca4991717e449ed6304`）已删除，主程序无 UnityChan 类型使用。`Assets/unity-chan!` 目录本身仍在，属于阶段 B 的移出对象。

### Coplay 依赖整理（已完成）

`com.coplaydev.coplay` 已从 `manifest.json` 删除；`com.unity.inputsystem: 1.14.0` 已显式声明。`Assets/_ImportArchive/HighlightPlus` 仍使用 Input System，因此 Input System 按方案 A 保留为直接依赖；glTFast 无项目代码引用，随 Coplay 离开。Unity 重新解析后会自行移除 `Packages/Coplay/` 克隆目录并刷新 `packages-lock.json`。

### 第三方 link.xml 已存在

已有：

- `Assets/Plugins/Zenject/Source/Usage/link.xml`，保留 `Zenject-usage`；
- `Assets/Plugins/Sirenix/Assemblies/link.xml`，保留 Odin 程序集。

不要把它们写成“致命白名单缺失”。新增主 `link.xml` 从最小范围开始。

### UniRx 和 GameConfig 风险尚未证实

业务代码中未发现 UniRx 调用，也未发现 `GameConfig.Runtime` 的运行时反射加载。Editor 导入器中的 `JsonUtility` 不属于 WebGL Player。因此不默认全量保留 UniRx、Sirenix、Zenject、DOTween、GameConfig 等完整程序集；仅在构建日志或运行异常证明需要时增加保护。

## 4. 执行顺序

### 阶段 0：编译依赖清点

1. 删除 4 个无用的 Visual Scripting using，确认无真实 API 使用。
2. 盘点 `_ImportArchive` 下参与编译的 `.asmdef`。
3. 决定 HighlightPlus、CFXR Demo 等归档运行时代码是否保留。
4. 决定 Input System 是否保留为直接依赖。
5. 处理 `MainGame.asmdef` 的 UnityChan GUID。
6. 重新编译，Console 0 编译错误。

本阶段不做大规模资源移动。

### 阶段 A：包与管线清理

1. 阶段 0 通过后，再从 `manifest.json` 移除 Coplay、PostProcessing、ShaderGraph、Visual Scripting。
2. 让 Unity 重新生成并检查 `packages-lock.json`。
3. 删除各平台的 `UNITY_POST_PROCESSING_STACK_V2`。
4. SDK 未安装前不要盲目删除 `WeixinMiniGame` 节点；先清旧宏值。
5. 清理 Ultra 质量档和 `GraphicsSettings` 的 SRP 引用。
6. 检查 `_ImportArchive` 中 shadergraph、URP Demo 和运行时 asmdef。
7. 重启 Unity，验证包解析、编译、两个场景、Timeline 和迷雾。
8. 记录新 WebGL 构建体积。

### 阶段 B：UnityChan 与孤儿资源清退

1. 删除 `MainGame.asmdef` 的 UnityChan GUID。
2. 用 GUID 依赖闭包生成最终移动清单，覆盖 prefab、controller、FBX、材质、贴图、SO 和 `.meta`。
3. 移出 `Assets/unity-chan!`。
4. 移出确认不在场景、数据库、Resources 和运行时加载路径中的单位孤儿链。
5. 移出确认无引用的旧建筑配置、prefab 和 Knight Statue 资源链。
6. 检查 `UnitDatabase`、`BuildingDatabase`、`GameInstaller`、两个场景和字符串加载路径。
7. 重启 Unity 并重新生成项目文件。

验收：无 UnityChan 程序集、无失效 asmdef、无 Missing 引用；Build Report 中 UnityChan 代码消失。

### 阶段 C：IL2CPP/AOT

1. 创建 `ZenjectReflectionBakingSettings.asset`。
2. 保持 `Is Enabled In Builds = true`、`All Generated Assemblies = true`。
3. 做干净的 WebGL Development Build。
4. 检查日志：`Added reflection baking to 'N' types in assembly 'MainGame'`。
5. 没有日志时再强制重编译；不要默认开启 `Is Enabled In Editor`。
6. 新 `Assets/link.xml` 先保护经证实的高风险程序集，不做“大而全”白名单。
7. 保留 Zenject/Sirenix 自带 link.xml。
8. 暂不设为 `NoCheckAssumeFullCoverage`；必要时用警告模式查漏烘焙类型。
9. 回归场景、地图、战斗、卡牌、迷雾和 UI 注入。

验收：无 `MissingMethodException`、`ExecutionEngineException`、`TypeLoadException`；功能与体积分别验收。

### 阶段 D：WebGL 与真机基线

1. 明确 WebGL2 策略，检查 Graphics API `Automatic` 是否符合 SDK 和目标设备。
2. 不预设 `webGLInitialMemorySize = 256`。
3. 对照测试 64 MB、128 MB、256 MB。
4. 记录冷启动、进 GameScene、地图生成、战斗和迷雾峰值。
5. Android/iOS 真机验证 WebGL context、`OnRenderImage`、Blit、CommandBuffer、ARGBHalf RT 和 shader。
6. 根据实测选择内存参数。

### 阶段 E：微信 SDK、转换和分包

1. A-D 通过后安装确定版本的 `minigame-unity-webgl-transform`。
2. 由 SDK 生成微信配置和平台登记。
3. 重新构建并在微信开发者工具转换。
4. 重新记录 framework、wasm、data 尺寸。
5. 根据当前 SDK 文档和实际产物配置 WasmSplit、首包和 DataCDN。
6. 验证首包、分包、CDN、缓存、版本更新和断网。
7. 后置接入 `wx.setStorage`、登录、分享。
8. 真机回归全流程、迷雾、触摸、卡牌和软键盘。

## 5. 验收标准

### 编辑器与依赖

- 0 编译错误；
- 无失效 `.asmdef` GUID；
- `manifest.json`、`packages-lock.json` 和实际程序集一致；
- `_ImportArchive` 中参与 Player 编译的内容已明确处理；
- 无启用的 URP/PPv2 设置。

### 功能、渲染与 AOT

- StartScene -> GameScene 全流程正常；
- Timeline、地图、战斗、卡牌、UI 注入和迷雾正常；
- WebGL2 真机上的 RT、CommandBuffer 和 shader 正常；
- Reflection Baking 日志可验证；
- 无 AOT/类型加载异常。

### 包体与移动端

- A-D 后重建 framework/wasm/data 基线；
- 不以历史 8.37 MB 为绝对阈值；
- Build Report 确认 UnityChan、无用 Demo、视频等未进入构建；
- DataCDN 和代码分包以当前 SDK 转换结果为准；
- tap、捏合、卡牌拖拽和 InputField 经过真机验证；
- 至少完成 Android、iOS 各一台设备的核心流程回归。

## 6. 暂不处理

- 不切 URP，不重写迷雾；
- 不在缺少实测数据时引入 AssetBundle/Addressables；
- 不做第一阶段并行多指；
- 不做联机；
- 不在 A-D 阶段接入登录、支付、分享、云存档；
- 不在真机 Profiling 前确定迷雾 RT 降级、MeshCollider 简化和音频按需加载；
- **不做极致瘦身**：Scripts 下 19 个孤儿 SO、Toon_RTS 未用动画变体、`_ImportArchive` 整体移出均暂缓（不影响功能与发布，只影响包体/磁盘）。

## 7. 参考资料

- [微信 Unity WebGL 适配方案](https://github.com/wechat-miniprogram/minigame-unity-webgl-transform)
- [微信小游戏 WebGL2](https://developers.weixin.qq.com/minigame/dev/guide/game-engine/unity-webgl-transform/Design/WebGL2.html)
- [微信小游戏 DataCDN](https://developers.weixin.qq.com/minigame/dev/guide/game-engine/unity-webgl-transform/Design/DataCDN.html)
- [微信小游戏内存优化](https://developers.weixin.qq.com/minigame/dev/guide/game-engine/unity-webgl-transform/Design/OptimizationMemory.html)
- [微信小游戏启动流程](https://developers.weixin.qq.com/minigame/dev/guide/game-engine/unity-webgl-transform/Design/Startup.html)

## 8. 文档维护规则

- 本文是微信小游戏改造的唯一工作文档。
- 每完成一个阶段，只回填实际状态、构建日志、体积和真机数据。
- 未验证推测不写成确定结论。
- H5/裸 WebGL 路线继续维护在 `H5小游戏改造难点分析报告.md`。
