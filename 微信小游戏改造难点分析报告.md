# 本项目转微信小游戏难点分析报告

> 分析对象：`E:\BaiduNetdiskDownload\毕设\My project - new`
> 分析方式：仅阅读源码与工程配置，**未改动任何代码/资源**
> 结论口径：微信小游戏 = Unity WebGL(IL2CPP) 构建 + 微信 `minigame-unity-webgl-transform` 适配层，运行于微信客户端 WebView（WebGL2/GLES3）

---

## 0. 一句话结论

本项目是一个 **Unity 2022.3 + 六边形地块的回合制/实时化策略游戏（480 格）**，代码架构本身较干净（无文件 IO、无线程、无网络），但被 **约 4.4 GB 的第三方资源包**、**渲染管线不确定（内置管线 + URP + 后处理栈 v2 三者并存）**、**纯鼠标/键盘输入**、以及 **若干平台专用插件（IAP/Ads/Analytics/Coplay）** 四座大山卡住。**它无法“一键转换”，需要先做资源瘦身、渲染管线统一、输入触摸化、插件剥离四轮结构性改造，才能真正跑在微信小游戏上。**

---

## 1. 项目技术栈画像（据实提取）

| 维度 | 实际情况 | 证据 |
|---|---|---|
| 引擎版本 | Unity **2022.3.62f3c1** | `ProjectSettings/ProjectVersion.txt` |
| 游戏类型 | 六边形地块策略（探索/占地/卡牌/战斗/公共建筑/迷雾） | `Assets/Scripts/` 目录结构 |
| 地图规模 | **20 × 24 = 480 格**（体量不大，属有利因素） | `Assets/Scripts/ScriptableObjects/MapGenerationConfig.asset` L27-28 |
| DI 框架 | **Zenject**（MonoInstaller + 场景强绑定） | `Assets/Scripts/Infrastructure/Installers/GameInstaller.cs` |
| 响应式 | **UniRx**（大量 Observable/Subject） | `Assets/Plugins/UniRx` |
| 动画/缓动 | **DOTween** | `Assets/DOTween_1_2_765` |
| 相机 | **Cinemachine 2.10.7** | `Packages/manifest.json` |
| 文本 | **TextMesh Pro 3.0.9**（含 EmojiOne 字体） | `Packages/manifest.json` |
| 几何 | **LibTessDotNet**（多边形三角化） | `Assets/Plugins/LibTessDotNet` |
| 编辑器工具 | **Sirenix Odin Inspector**（付费，Editor 为主） | `Assets/Plugins/Sirenix` |
| 测试 | **NSubstitute**（仅测试程序集） | `Assets/Tests/Tests.asmdef` |
| 渲染 | **内置管线 + URP 14 + Post-Processing Stack v2 三态并存** | 见 §2.2 |
| 平台插件 | Unity IAP / Ads / Analytics / **Coplay** | `Packages/manifest.json` L3-5 |
| 资源总量 | **Assets 约 4.42 GB（14,662 文件）**，其中 `Assets/Resources` 独占 **3.67 GB** | 实测统计 |

关键程序集结构：`Assets/Scripts/MainGame.asmdef`（主逻辑）、`Assets/GameConfig/Runtime/GameConfig.Runtime.asmdef`（Excel 生成数值库）、`Assets/GameConfig/Editor`（Excel 工具）、`Assets/Tests`（测试）。

---

## 2. 难点分级与逐条剖析

按“能否不改架构直接过去”的严重程度分为四级：**致命 / 高 / 中 / 低**。

---

### 2.1 【致命】包体与资源架构：4.4 GB 资产 vs 微信 4 MB 主包 / 20 MB 总包

这是**第一道、也是最硬的一道坎**。

实测数据：

```
Assets              4,424.6 MB   14,662 个文件
Assets/Resources    3,669.9 MB    7,058 个文件   ← 全部位于 Assets/Resources/Import
按扩展名：
  .png   1,936.8 MB  (852)
  .tga   1,062.8 MB  ( 87)
  .fbx     429.4 MB  (1683)
  .exr     411.3 MB  ( 18)
  .psd     129.3 MB  ( 14)
  .wav     125.6 MB  (421)
```

微信小游戏对**代码包**的硬限制是：**主包 ≤ 4 MB，主包 + 分包合计 ≤ 20 MB**（[CSDN 实测记录](https://blog.csdn.net/qq_33789001/article/details/136233557)、[微信官方适配方案 DataCDN](https://developers.weixin.qq.com/minigame/dev/guide/game-engine/unity-webgl-transform/Design/DataCDN.html)）。资源应通过 CDN 远程加载（AssetBundle / StreamingAssets 改造），而不是打进包内。

对本项目的三重打击：

1. **`Assets/Resources` 目录是“必打包”语义**。Unity 会把所有 `Resources` 文件夹下的资源全部打进构建，且**无法被引用裁剪剥离**、也无法方便地做分包/远程化。本项目的 3.67 GB 几乎全堆在 `Assets/Resources/Import/*` 下（Toon RTS、RPG Monster DUO、Understone、WizardPolyArt、Polytope、unity-chan、Magician、Cowboy、Knight Statue、Altar Ruins、Toon Crystals、HighlightPlus、Hyper Casual FX 等十几个 Demo 资源包），这些绝大多数是**从未被场景/代码引用的 demo 素材**，却因为身处 `Resources` 而全部进入构建。**这是体积爆炸的根因。**

2. **大量“源格式”资产未压缩/未转换**：`.exr`(411MB)、`.psd`(129MB)、`.tga`(1.06GB) 都是设计源图，不适合直接进游戏包；421 个 `.wav`(125MB) 应转码为 mp3/ogg 并走 CDN。纹理也未见统一的 ASTC/ETC2 压缩策略（`ProjectSettings.asset` 中 `m_TextureMipmapLimitGroupNames: []`，未配置移动端压缩分组）。

3. **资源与代码耦合方式**：`GameInstaller.cs` 通过 `[SerializeField]` 直接序列化了 **30+ 个 ScriptableObject 与场景对象引用**（L9-50），这些 SO 又各自引用 Prefab/材质/纹理。这种“场景内直接拖引用”的方式天然与“AssetBundle + CDN 分包”冲突——要远程化就必须把这一整条引用链改成可寻址加载（Addressables 或自定义 AB 管理）。

> 改造代价：**必须引入 AssetBundle/Addressables + CDN 资源服务器，并做一次大规模资源去重/降级/压缩**，这属于“重做资源管线”级别的工作，不是改几个开关。

---

### 2.2 【致命】渲染管线三态并存，后处理栈 v2 与 URP 互斥

这是**最隐蔽的雷**。本项目同时存在三套渲染相关的东西，且互相矛盾：

1. **URP 包已安装**：`manifest.json` 中 `com.unity.shadergraph: 14.1.0`（对应 URP 14），且 `GraphicsSettings.asset` L81 的 `m_SRPDefaultSettings` 指向一个 URP 资源。
2. **Post-Processing Stack v2 已安装并被编译宏引用**：`manifest.json` 中 `com.unity.postprocessing: 3.4.0`，且 `ProjectSettings.asset` L754-776 大量平台都定义了 `UNITY_POST_PROCESSING_STACK_V2`。**PPv2 是内置管线的后处理方案（基于 `OnRenderImage`），与 URP 完全不兼容。**
3. **实际运行管线是内置管线**：`QualitySettings.asset` 中只有 `Ultra` 档挂了 URP 资产（L292），其余六档（含 WebGL 默认的 `High` 档、Android/iPhone 默认的 `Medium` 档）`customRenderPipeline: {fileID: 0}` 全是内置管线。

**最关键的证据——迷雾系统写死了内置管线的图像特效 API**：

`Assets/Scripts/Managers/FogEnvironmentSelectiveEffect.cs`
- L114 `OnRenderImage(...)` + L126 `Graphics.Blit(...)`：这是**内置管线专用**的全屏后处理入口，URP 下根本不会被调用；
- L286 创建整屏 `RenderTexture`（`ARGBHalf`，L283-285）；
- L303/L350-351 用 `CommandBuffer` + `CameraEvent.BeforeImageEffects` 做对象遮罩绘制。

这套“选择性迷雾”是玩法核心视觉（单位/建筑不被雾盖住，靠对象遮罩 + 擦除 pass 实现），深度依赖：**相机深度纹理 + 多材质命令缓冲 + 全屏 RenderTexture**。

带来的三个问题：

- **切到 URP**：`OnRenderImage`/`Graphics.Blit`/`CameraEvent.BeforeImageEffects` 全部失效，迷雾系统必须用 URP 的 `ScriptableRendererFeature`/`RenderPass` 重写（工作量 + 风险都不小）。
- **留在内置管线**：微信侧虽然支持 WebGL2（[官方 WebGL2 说明](https://developers.weixin.qq.com/minigame/dev/guide/game-engine/unity-webgl-transform/Design/WebGL2.html)），但 **PPv2 + 整屏 ARGBHalf RenderTexture + CommandBuffer 在低端手机上内存与带宽开销极高**，且 PPv2 在微信适配器下本就属于“已知兼容雷区”。
- **URP 在微信上的坑**：社区大量反馈“Unity 转微信小游戏后 URP shader 报错 / 黑屏”（[URP shader 报错](https://developer.unity.cn/ask/question/664c43f0edbc2a001de2eb0f)、[开 URP 发布报错](https://developer.unity.cn/ask/question/66e3aad7edbc2a001e09e00b)）。当前项目“双管线并存 + 迷雾依赖内置图像特效”的状态，会让这条链路变得极难验证。

> 改造代价：**先拍板“最终用哪条管线”，再把迷雾/后处理重写到该管线**；否则任何一次构建都会踩到两套管线互相打架。

---

### 2.3 【致命】平台专用插件必须剥离/替换

`Packages/manifest.json` 里挂了四类与微信小游戏不直接兼容的依赖：

| 包 | 问题 | 处理 |
|---|---|---|
| `com.coplaydev.coplay`（Git 依赖） | CoplayDev 的 **Unity AI 开发辅助插件**（[GitHub](https://github.com/CoplayDev/coplay-unity-plugin)），属开发期工具，与游戏运行无关，却作为运行时依赖存在 | 发布前从 manifest 移除，确认无运行时残留 |
| `com.unity.purchasing: 4.11.0` | Unity IAP 在微信小游戏上需接微信支付专用适配，标准 IAP 商店（App Store/Google）不可用 | 移除或换微信支付 SDK |
| `com.unity.ads: 4.4.2` | Unity Ads 在微信小游戏无官方直连，需换微信流量主广告 | 移除或换微信广告 SDK |
| `com.unity.analytics: 3.8.1` | 微信侧数据上报应走微信统计/自有埋点 | 移除或换 |

另注意 `net.tnrd.nsubstitute`（测试库）、`com.unity.visualscripting`（Bolt）、`com.unity.timeline`、`com.unity.cinemachine` 等若未实际使用，也应在小游戏构建中裁剪，减少 IL2CPP 包体与 AOT 面。

> 改造代价：中低（删依赖 + 接微信 SDK），但**属于硬前置条件**，否则连包都打不出来或审核不通过。

---

### 2.4 【致命】IL2CPP + AOT 限制（WebGL 强制 IL2CPP）

Unity WebGL 只支持 **IL2CPP**（无 Mono），而 IL2CPP 是 AOT 编译，对**运行时反射、泛型动态实例化、`System.Reflection.Emit`、`dynamic`** 等有严格限制。

本项目的风险点：

- **Zenject**：依赖反射做注入解析。Zenject 自带 `ReflectionBaking`（`Assets/Plugins/Zenject/OptionalExtras/ReflectionBaking`）可在编辑器期把反射“烘焙”成静态代码以通过 AOT，**必须启用并烘焙**，否则运行时 `GetComponent`/构造注入会因 AOT 缺失而崩。
- **UniRx**：部分操作符（尤其涉及泛型 `IObservable<T>` 的 `Subscribe` 包装）在 IL2CPP 下需要 AOT 注册或裁剪白名单。
- **Odin Inspector**：`Assets/Plugins/Sirenix/Assemblies/link.xml` 已有 AOT 白名单，但它是 Editor 为主，运行时属性若被裁剪会报缺失。
- 项目当前 `managedStrippingLevel: WebGL = 1`（`ProjectSettings.asset` L792）且**没有针对主程序的 `link.xml`**（仅 Zenject/Sirenix 自带了）。主程序大量依赖“接口 + 具体实现”的 DI 绑定（如 `IMapDataService → HexMapService`），裁剪后极易出现 `AOT/Strip 导致类型丢失`。

**有利面**：主程序游戏代码经全文检索，**没有 `dynamic`、没有 `MakeGenericType`、没有实际的 `System.Reflection` 调用**（`RiverGenerator.cs` L5/L7 的 `using System.Reflection; using UnityEngine.UIElements;` 是**未使用的多余 using**，可清理），这大幅降低了 AOT 风险。真正的反射风险集中在第三方插件内部。

> 改造代价：中——启用 Zenject ReflectionBaking、补一份主程序 `link.xml`、提高/调整裁剪级别并真机回归。

---

### 2.5 【高】输入系统全面鼠标/键盘化，需触摸化改造

微信小游戏跑在手机上，**没有鼠标滚轮、没有键盘 WASD、没有右键**，而本项目的输入层是纯桌面式：

`Assets/Scripts/Core/Services/InputService.cs`（唯一输入抽象）
- L17 `MousePosition => Input.mousePosition`
- L19-21 `GetMouseButtonDown/Up` → `Input.GetMouseButtonDown/Up`
- L23-25 `GetKey/GetKeyDown/GetKeyUp` → `Input.GetKey*`
- L27 `GetAxis` → `Input.GetAxis`
- L28 `MouseScrollDelta => Input.mouseScrollDelta.y`  ← 滚轮缩放

`Assets/Scripts/Controllers/CameraController.cs`
- L235-236 用 `GetAxis("Horizontal"/"Vertical")` 做**键盘平移**；
- L252-261 用 `MouseScrollDelta` 做**滚轮缩放**；
- L407-423 用鼠标左键拖拽做旋转。

`Assets/Scripts/UI/CardController.cs`
- L306、L312 直接 `Input.mousePosition` 做卡牌拖拽/地图射线。

全文检索结果：**主程序没有任何 `Input.touchCount` / `Input.GetTouch`**。WebGL 上 Unity 只会把**单指触摸模拟成鼠标**，因此：

- 单指点击/拖拽“勉强能跑”；
- **双指捏合缩放、双指旋转、多指手势完全缺失**；
- 键盘平移在手机上不可用，需要改成“边缘拖动/双指平移”。

`Assets/Scripts/Scenes/StartScene/UIControl.cs` L523 `Application.Quit()` 在 WebGL/微信上是 **no-op**（无法真正退出小程序），需要改提示逻辑。

> 改造代价：中高——好在有 `IInputService` 这一层抽象，触摸改造可收敛在 `InputService` + `CameraController` + 卡牌拖拽三处，不必散改全工程。

---

### 2.6 【高】运行时程序化 Mesh 生成 + MeshCollider cooking

本项目地图是**全程序化**生成，几何相关代码密集：

- `ChunkMapRenderer.cs`：唯一渲染后端，**8×8 分块 + 每块 Terrain/Water/River active/staging 双缓冲 Mesh + 每块一个 `MeshCollider`**（L8、L30、L870 `AddComponent<MeshCollider>`、L2005-2022 双缓冲碰撞提交）。
- `MeshDataGenerator.cs`（4,000+ 行）、`MeshGenerator.cs`、`Triangulator.cs`、`UVGenerator.cs`、`WallMeshDeformer.cs`、`RiverGenerator.cs`、`RidgeGenerator.cs`、`MountainGeometryBuilder.cs` 等，全在运行时用 CPU 构建顶点/法线/UV/三角面。
- 高度扰动还依赖 `HexMetrics.SampleNoise` 的 `Texture2D.GetPixelBilinear`（`HexMetrics.cs` L26）——**运行时读纹理像素要求纹理可读（`isReadable`），在 IL2CPP/WebGL 上既占内存又是性能坑**。

WebGL 是**单线程**（`ProjectSettings.asset` L744 `webGLThreadsSupport: 0`，且 WebGL 下 `m_BuildTargetGraphicsJobs: 0`），`MeshCollider` 的 cooking 与程序化建网格都在主线程串行，480 格 + 分块双缓冲 + 动态地图脏区重建（`MapSlicedCommitExecutor` 分帧提交）在手机上会造成明显卡顿与首屏耗时。

> 改造代价：中高——好在已有分帧提交（`MapSlicedCommitExecutor`）与分块（Chunk）设计，方向是对的；但需要**调低地形细分、简化 MeshCollider（或用盒/胶囊近似）、把可读纹理改为预计算噪声**，并做真机 Profiling。

---

### 2.7 【高】内存预算紧张

- Unity WebGL 侧：`webGLInitialMemorySize: 32`（MB）、`webGLMaximumMemorySize: 2048`（MB）、`webGLMemoryGrowthMode: 2`（`ProjectSettings.asset` L746-748）；当前 `webGLMemorySize: 16`（L730）偏小，几乎必涨。
- 微信侧：iOS 端对小程序/小游戏进程内存上限比 Android 更严，低端机可用内存远低于 2GB；WASM 堆 + 显存（WebGL 的纹理/缓冲也吃同一块内存池）叠加后很容易超限被杀。
- 本项目吃内存大户：**整屏 ARGBHalf RenderTexture（迷雾）+ 480 格双缓冲 Mesh + 大量未压缩纹理 + 421 个全量常驻的 wav 音频 Clip（`AudioManager.cs` 把 `bgmClips/sfxClips` 全部直接序列化引用）**。

> 改造代价：中——与 §2.2/§2.6 强耦合，迷雾 RenderTexture 降级、音频转码 + 按需加载、纹理压缩是必须动作。

---

### 2.8 【中】字体 / 中文渲染

- 使用 TMP 3.0.9 + EmojiOne（`Assets/TextMesh Pro/Resources`）。中文若走 TMP 动态图集，**中文字形量大，动态字体图集（atlas）在手机上会非常大**；需要预生成静态中文字体资产并裁剪字符集，否则首次渲染中文会触发图集重建卡顿。
- 项目 UI 文案硬编码中文（如 `UIControl.cs`、`gameOptionController.cs` 里大量 `"未命名"`、`"请输入文本"` 等），需确认 TMP 字体资产覆盖全部用字。

---

### 2.9 【中】场景与分包结构

- `EditorBuildSettings.asset` 只有两个场景：`StartScene.unity`、`GameScene.unity`。
- 启动流程靠 `GameInstaller`（Zenject）**强绑定场景对象**（`FromComponentInHierarchy` 的 `MapGenerator`、`PlayerModelManager`、`EnemyModelManager`、`AIManager`、`CameraController`、`EndGame` 等，`GameInstaller.cs` L392-401 还会做缺失校验直接抛异常）。
- 若要微信分包（首包 ≤ 4MB），需要把“启动/主菜单”与“对战场景 + 重型资源”拆开，并让 Zenject 的强绑定适配分包加载时序。

> 改造代价：中——取决于分包诉求；若整体走 CDN 远程资源，场景本身的代码包很小，此项可降级。

---

### 2.10 【低/观察项】

- **存档/持久化**：主程序**没有 `PlayerPrefs`、没有 `System.IO` 文件读写**（全文检索未命中），这点非常干净；但小游戏侧存档应接 `wx.setStorage`/云存档，需自行加一层存档服务。
- **网络**：主程序无 `Thread/Task/async/WebSocket/UnityWebRequest` 使用；唯一“Network”字样是 `AnnexationService` 里的方法名 `AdjacentToNetwork`（连通性判断，非网络）。所以**联机不是当前需求**，无网络改造负担。
- **确定性**：`SeedService.cs` 用 `System.Random(derivedSeed)` 做确定性种子，地图/AI 可复现。`System.Random` 在 IL2CPP/WebGL 上行为一致，利于联机校验——属有利因素。
- **`WeixinMiniGame` 平台符号已存在**：`ProjectSettings.asset` L772 `WeixinMiniGame: UNITY_POST_PROCESSING_STACK_V2`，说明**已有过微信小游戏转换尝试的痕迹**，但当前 checkout 中未见微信适配器 SDK 本体（`Assets` 下无 WX 插件）。意味着：要么 SDK 曾被移除，要么只加了目标平台但未真正接入。这既是“路线已探过”的线索，也说明之前可能卡在某个环节。

---

## 3. 难点总览矩阵

| # | 难点 | 级别 | 根因（具体代码/配置） | 是否硬前置 |
|---|---|---|---|---|
| 2.1 | 包体 4.4GB vs 微信 4MB/20MB | 致命 | `Assets/Resources` 3.67GB + `GameInstaller` 强引用链 | 是 |
| 2.2 | 渲染管线三态并存 + 迷雾依赖内置图像特效 | 致命 | `FogEnvironmentSelectiveEffect` 的 `OnRenderImage/Blit/CommandBuffer/RenderTexture`；`QualitySettings` 双管线 | 是 |
| 2.3 | 平台插件 IAP/Ads/Analytics/Coplay | 致命 | `Packages/manifest.json` | 是 |
| 2.4 | IL2CPP AOT / 反射裁剪 | 致命 | Zenject/UniRx 反射 + 无主程序 link.xml | 是 |
| 2.5 | 输入纯鼠标/键盘 | 高 | `InputService`/`CameraController`/`CardController` | 是（触摸必需） |
| 2.6 | 程序化 Mesh + MeshCollider 性能 | 高 | `ChunkMapRenderer` 双缓冲 + MeshCollider；WebGL 单线程 | 否（优化项） |
| 2.7 | 内存预算 | 高 | ARGBHalf RT + 大纹理 + 全量音频常驻 | 否（优化项） |
| 2.8 | 中文字体图集 | 中 | TMP 动态图集 | 否 |
| 2.9 | 场景/分包结构 | 中 | 两场景 + Zenject 强绑定 | 视分包而定 |
| 2.10 | 存档/网络/确定性 | 低 | 主程序干净 | 否 |

---

## 4. 有利因素（降低了整体难度）

1. **主程序无文件 IO / 无线程 / 无网络 / 无 `dynamic` / 无反射 Emit**：WebGL 最常见的四类硬坑，本项目一个都没踩。
2. **地图规模小（480 格）**：不是万人同屏，性能压力有限。
3. **已有关键抽象层**：`IInputService`（输入）、`IMapRenderBackend`（渲染后端，`ChunkMapRenderer` 是唯一实现，替换/裁剪有入口）、`IMapRaycastService`（拾取）——为改造保留了收敛点。
4. **确定性种子**（`SeedService`）：利于后续联机/回放校验。
5. **已有分块 + 分帧提交**（`ChunkMapRenderer` + `MapSlicedCommitExecutor`）：说明作者已有“为性能分帧”的意识，路径可继续沿用。
6. **`WeixinMiniGame` 平台符号已存在**：说明转换方向已被确认过，只是尚未真正落地。

---

## 5. 建议的改造路线（仅建议，未执行）

按依赖顺序，分六阶段推进：

- **阶段 0 — 定管线**：在“内置管线 + 重写迷雾为 shader/RT 兼容版”与“URP + 用 RendererFeature 重写迷雾”之间二选一并冻结；删除 `com.unity.postprocessing`（PPv2）与 `UNITY_POST_PROCESSING_STACK_V2` 宏，消除双管线。
- **阶段 1 — 瘦身 + 资源远程化**：把 `Assets/Resources/Import` 下未引用 demo 包移出 `Resources`（或删），建 AssetBundle/Addressables + CDN，纹理统一 ASTC/ETC2，`.exr/.psd/.tga/.wav` 转码。这是工作量最大的一步。
- **阶段 2 — 插件清理**：移除 Coplay / IAP / Ads / Analytics（或换微信 SDK），裁剪 Visual Scripting / Timeline 等未用包。
- **阶段 3 — 输入触摸化**：在 `InputService` 增加 `Input.GetTouch` 分支，`CameraController` 增加双指缩放/平移，卡牌拖拽改触摸。
- **阶段 4 — IL2CPP AOT**：启用 Zenject ReflectionBaking，补主程序 `link.xml`，调整 WebGL 裁剪级别并真机回归。
- **阶段 5 — 微信接入**：登录/支付/分享/云存档/分包 + 适配器真机联调，重点回归迷雾（RenderTexture）与 URP shader 兼容。
- **阶段 6 — 性能**：迷雾 RT 降级、MeshCollider 简化、音频按需加载、可读噪声纹理改预计算、真机 Profiling。

---

## 6. 关键证据索引

- 引擎版本：`ProjectSettings/ProjectVersion.txt`
- 地图尺寸：`Assets/Scripts/ScriptableObjects/MapGenerationConfig.asset`（xNumber=20, zNumber=24）
- 资源体积：实测 `Assets` 4,424.6 MB / `Assets/Resources` 3,669.9 MB
- 包依赖：`Packages/manifest.json`（shadergraph=URP14、postprocessing=PPv2、purchasing/ads/analytics/coplay）
- 渲染管线：`ProjectSettings/GraphicsSettings.asset` L81、`ProjectSettings/QualitySettings.asset` L292
- 迷雾后处理：`Assets/Scripts/Managers/FogEnvironmentSelectiveEffect.cs` L114/L126/L283-286/L303/L350
- 输入：`Assets/Scripts/Core/Services/InputService.cs` L17-28；`Assets/Scripts/Controllers/CameraController.cs` L235-261/L407-423；`Assets/Scripts/UI/CardController.cs` L306/L312
- 程序化网格：`Assets/Scripts/Managers/ChunkMapRenderer.cs` L8/L30/L870/L2005-2022；`Assets/Scripts/Utilities/HexMetrics.cs` L26
- DI 强绑定：`Assets/Scripts/Infrastructure/Installers/GameInstaller.cs` L9-50/L392-401
- WebGL/微信设置：`ProjectSettings/ProjectSettings.asset` L730/L744-748/L772/L792
- 音频常驻：`Assets/Scripts/Managers/AudioManager.cs` L7-13
- 退出 no-op：`Assets/Scripts/Scenes/StartScene/UIControl.cs` L523

---

*本报告基于对源码与工程配置的静态阅读得出，未改动任何文件；资源体积为本地文件系统实测。*
