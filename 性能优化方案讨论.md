# 性能优化方案讨论

> 目标规模：支持 100×100（约一万格）地图
> 当前状态：全项目性能优化盘点，仅讨论不做代码修改

---

## 一、渲染层优化（最致命，一万格下必崩）

### 1. ⭐️ 致命：过渡面"每面一 submesh + 一材质实例"

**现状**：`MapController.cs:253-291` + `MapRenderer.cs` 每格生成约 5 个过渡面（3 矩形 + 2 三角），每个过渡面都：
- `mesh.SetTriangles(indices[i], i)` 占一个独立 submesh
- `ConfigureBlendMaterial` 每次 `new Material` 创建独立材质实例

**问题**：一万格 → 约 **5 万 submesh / 5 万材质 / 5 万+ draw call**。这是第一号杀手，光这一条就能让一万格地图直接卡死。

**方向**：地形混合从"每面独立材质"改成**单一混合 Shader + TextureArray**，用顶点色 / UV2 / UV3 携带"混合哪几种地形 + 混合权重"。整块地形压到 **1~3 个材质、几个 draw call**。

**成本**：高（需重写混合 Shader），但收益最大，一万格下几乎必做。

---

### 2. ⭐️ 地图分区（Chunk）

**现状**：整张地图所有地块拼成一个巨型 Mesh（`_terrainMesh`），一个 `_waterMesh`，一个 `_riverMesh`。

**方案**：将地图切成矩形的 Chunk（建议每块 8×8 或 10×10 地块），每块自成一个 GameObject + 独立合并 Mesh。

**收益**：
- 视锥剔除：镜头只看一小片时，屏幕外整块不进渲染管线
- 未探索 / 记忆区整块隐藏（极端优化可关闭完全不显示的 Chunk）
- 迷雾 `mesh.colors` 回传从"整图全量"变成"只更新有变化的 Chunk"（配合脏标记）

**难点——过渡面跨 Chunk 边界**：
- 过渡面按 **owner 地块所在 Chunk 归属**（当前代码以 `hexCellData` 为主，朝 NE/E/SE 方向朝正向邻居生成，天然单向），跨界面片数量可控
- Chunk 之间 Mesh 各自闭合，过渡面顶点即使跨界也整段放进 owner 的 Chunk，不切割单个三角形

**成本**：中高。一万格必做。

---

### 3. 网格线：每格一个隐藏 GameObject

**现状**：`MapRenderer.cs:668-681` 每个非水格 new 一个 `SubGridLine_j` GameObject + Mesh + Material。一万格 = 上万个 GameObject + 上万材质实例。即使 `SetActive(false)`，场景挂这么多对象也有开销（加载、GC、Hierarchy 遍历）。

**方向**：
- 合并成每 Chunk 一条整合网格线
- 或直接在 Shader 中画网格（世界坐标 / 屏幕坐标算六边形边），一个 draw call 搞定

**成本**：中。

---

### 4. 每格独立 MeshCollider（拾取优化）

**现状**：主地形 / 水面 / 网格线都 `AddComponent<MeshCollider>`。一万格的巨型 MeshCollider 构建慢、内存大，且每帧射线检测遍历大量三角面效率低。

**方向**：拾取用**六边形坐标数学反算**（世界坐标 → hex 坐标），完全去掉地形 collider；或退一步每 Chunk 一个简化平面 collider。

**成本**：中等（需改 `InputService` 的射线逻辑 + `HexMapService` 能否 O(1) 反算）。

---

### 5. 附属物体渲染优化（landform / resource / building / unit）

**现状**：一万格每格可能挂地貌模型 + 资源模型 + 建筑 + 单位 = 数万 GameObject/Renderer。

**方向**：
- **GPU Instancing / `DrawMeshInstanced`**：同种地貌合批，大幅减少 draw call
- 结合 Chunk 做距离/迷雾剔除，远处 / 未探索不实例化
- 静态地物使用 `StaticBatchingUtility.Combine`
- 单位可考虑 LOD + 视距裁剪

**成本**：中。

---

## 二、逻辑热路径优化（操作卡顿 lag spike）

### 6. `OnMapVisualChanged` 全量重算

**现状**：`MapRenderer.cs:1107` 每次单位移动 / 回合结算调用，连做四件全图级操作：

#### 6.1 FieldOfViewService.Recompute（`FieldOfViewService.cs:35`）

**问题**：每个玩家单位 `new HashSet` + `new List` 做 BFS；`RevealRing` 里又 `new HashSet`。频繁分配 → GC 压力。

**方向**：复用缓冲 List/HashSet；用数组 + `bool[]` 标记 visited 替代 HashSet；避免每次 new。

#### 6.2 UpdateExplorationVisuals（`:1248`）

**问题**：
- `Color[] colors = mesh.colors`（整块拷出）
- 遍历所有格改色
- `mesh.colors = colors`（整块传回 GPU）
- 水面、河流同样各来一遍
- 一万格 → 每次操作回传数十万顶点色

**方向**：结合 Chunk 分区 + 脏标记（只有视野变化的格所在的 Chunk 才回传 vertex colors）。这是治"一动就卡"的关键。

#### 6.3 UpdateFogMask（`:1354`）

**问题**：每次**整张遮罩清 G 通道 + 重盖所有可见格 + 整图 `SetPixels32`/`Apply`**。遮罩虽小（≤1024px 封顶），但每回合全量重建有 GC 开销。

**方向**：G 通道用"上次可见集合 vs 本次可见集合"的差集，只改变化像素；R 通道已用 `_fogMaskStamped` 做增量，没问题。

#### 6.4 SyncCellObjectVisibility（`:1124`）

**问题**：
- 遍历所有格检查 landFormModel / resourceModel / building
- 每个敌方单位 `GetComponentsInChildren<Renderer>(true)`（每次都重新查子物体）

**方向**：
- 缓存每个敌方单位的 Renderer[] / Canvas[] 引用，不让每帧重新查
- 只处理状态发生变化的格（视野变化列表）

---

### 7. `GetCellByWorldPosition` / `GetCell` 查询效率

**现状**：`Recompute`、`SyncCellObjectVisibility` 里频繁按世界坐标查格。

**确认项**：需确认 `HexMapService` 内部是否是 O(1) 坐标反算（hex 坐标直接数组索引），如果不是则是一万格下遍历的隐藏性能杀手。

**方向**：如果当前是字典/遍历查找，应改为六边形坐标反算公式直接数组索引（O(1)）。

---

## 三、生成期优化（一次性，影响加载时间）

### 8. 网格生成大量临时对象

**现状**：`MeshDataGenerator.cs` / `MapRenderer.cs` 里遍地 `new List`、`AddRange`、`.ToList()`、`.ToArray()`、每顶点 `HexMetrics.Perturb` 调用。一万格生成会分配海量临时对象 → 加载慢 + 大 GC。

**方向**：
- 预分配 List 容量（`new List<T>(capacity)`）
- 用数组 + 索引代替频繁 AddRange/ToArray
- **Job System + Burst Compiler** 并行生成网格（六边形顶点计算高度可并行，收益极大）

**成本**：高，但能把一万格生成从"卡几秒"降到可接受。

---

### 9. `RecalculateNormals` / `RecalculateTangents` / `SanitizeNormalsAndTangents`

**现状**：`MapController.cs:227-233` 对巨型 Mesh 调用 RecalculateNormals + RecalculateTangents，再逐顶点 SanitizeNormalsAndTangents。

**方向**：
- 分 Chunk 后每个 Mesh 变小，天然减轻
- 生成时直接计算正确法线，跳过 Recalculate（已知非零面积三角形法线公式）
- Sanitize 是兜底，优化前面的 UV/几何后可能不再需要

---

### 10. AI 回合优化

**现状**：README 提到 `AIFogService` 为 AI 现算可见集（不缓存）、`AITacticalBrain` 每单位决策。一万格 + 多 AI 单位时，AI 回合也可能是 lag 源。

**方向**：
- AI 视野计算复用玩家 FoV 的 `CollectVisibleCells` 缓冲
- AI 决策可分散到多帧（Coroutine yield），避免一帧跑完所有 AI
- 没在战斗的 AI 单位可简化决策（只做"朝前沿移动"）

---

## 四、GC / 内存优化

### 11. 缓冲复用与对象池

**问题汇总**：
- BFS 缓冲（`new HashSet`/`new List`）每次分配
- `mesh.colors` 每次整块拷贝分配
- 生成期大量临时 List/Array
- 敌方单位 `GetComponentsInChildren` 每次分配数组

**方向**：
- FoV BFS：类级别缓存 `Queue<HexCellData>` + `bool[]` visited
- Mesh colors：Chunk 级别缓存 `Color[]` 数组，不改动大小只改值
- Enemy renderer 缓存：在敌人生成/位置变化时重建，不每帧查

---

## 五、Shader 复杂度优化

### 12. 片元效率

**现状**：迷雾集成进所有地形/水/河流 Shader（`FogBlend.cginc`），过渡面还有 RealMaterialMaskBlend（双材质采样）/ ThreeMaterialBlend_Land（三材质采样）。一万格全屏时每个像素的 Shader 计算量可观。

**方向**：
- 过渡面改成 TextureArray 方案后，一次采样 (vs 当前 2-3 次采样 + 混合计算)
- 远处 / 小视野下可切换简化 Shader（LOD）
- 未探索地块可直接 early-out（整片 `clip`），跳过光照/法线计算

---

## 六、总结：优先级排序（针对一万格）

| 优先级 | 方向 | 治什么问题 | 改造成本 |
|---|---|---|---|
| **P0** | 过渡面材质合并（TextureArray + 单 Shader） | 稳态帧率 / draw call 崩溃 | 高 |
| **P0** | Chunk 分区 | 稳态帧率 + 迷雾增量更新 | 中高 |
| **P1** | 迷雾更新脏标记增量 | 操作 lag spike | 中 |
| **P1** | 网格线合批 / Shader 化 | draw call + GameObject 数 | 中 |
| **P1** | 去掉每格 MeshCollider，坐标反算拾取 | 内存 + 射线拾取性能 | 中 |
| **P2** | FoV / SyncVisibility 缓冲复用、缓存 Renderer | GC spike | 低中 |
| **P2** | 附属物体 GPU Instancing | draw call 数 | 中 |
| **P2** | Shader LOD / 未探索片元 early-out | 片元负载 | 低中 |
| **P3** | 生成期 Job + Burst 并行 | 加载卡顿 | 高 |
| **P3** | AI 多帧决策 | AI 回合 lag | 中 |

### 核心判断

**P0 两项（材质合并 + Chunk 分区）在一万格规模下是必做的**，否则连启动都可能卡死。且这两项相互配合——分区提供了"局部更新"的边界，材质合并解决了"draw call 线性膨胀"的根本原因，缺一个则另一个的收益会大打折扣。

### 依赖关系

```
纹理合并（P0-1）
    ↓ 提供了材质基础
Chunk 分区（P0-2）
    ↓ 提供了局部更新边界
迷雾增量更新（P1-1）
    ↓ 依赖分区 + 脏标记
网格线合批（P1-2）
    可独立并行
坐标拾取（P1-3）
    可独立并行
缓冲复用 / Instancing / Shader LOD（P2）
    可独立并行
 生成期 Job+Burst（P3）
     可独立并行
```

---

## 七、纯函数内局部优化（不动架构、不重构数据结构，可独立落地）

以下优化均在现有函数内完成，不需要改动类间接口、Shader、数据组织或整体架构。改动量小、风险低、可逐条独立实施。

### A. 缓存敌方单位的 Renderer/Canvas 引用

**位置**：`MapRenderer.cs:1168-1171`，`SyncCellObjectVisibility`

**现状**：每回合对每个敌方单位调用 `GetComponentsInChildren<Renderer>(true)` + `GetComponentsInChildren<Canvas>(true)`。这两个调用每次都会遍历子树 + 分配新数组。

**函数内改法**：
- 敌方单位生成/位置发生变化时，在单位 MonoBehaviour 上缓存 `Renderer[]` 和 `Canvas[]`，或维护 `Dictionary<GameObject, Renderer[]>` 在 MapRenderer 中。
- `SyncCellObjectVisibility` 直接读缓存数组遍历，无需每次 `GetComponentsInChildren`。

**收益**：消除每回合的子树遍历 + 数组分配。**性价比最高的一条。**

---

### B. FoV BFS 缓冲复用，避免每次 new

**位置**：`FieldOfViewService.cs:82`（`RevealRing`）、`:96-97,102`（`CollectVisibleCells`）

**现状**：每次 BFS 都 `new HashSet<HexCellData>` 做 visited、`new List` 做 frontier/next、`RevealRing` 里额外 `new HashSet` 做 into。

**函数内改法**：
- `visited` 改用**复用的 `bool[]`**（按 cell 索引下标），BFS 结束时批量或迭代清标记，不分配集合对象。
- `frontier`/`next`/`into` 使用类级别的复用 `List<HexCellData>`，每次 `Clear()`。
- `RevealRing` 里不需要临时 `HashSet`——可直接在 `CollectVisibleCells` 的遍历中对每格调 `Reveal`，省去中间集合。
- 不改 BFS 算法本身，仅换容器分配方式。

**收益**：消除视野计算的 GC spike（每次移动/回合触发）。

---

### C. 缓存 `_terrainMesh.colors` 数组字段，避免 Getter 拷贝

**位置**：`MapRenderer.cs:1252,1273`（`UpdateExplorationVisuals`）

**现状**：
```csharp
Color[] colors = _terrainMesh.colors; // getter → 整块数组拷贝
// ...修改...
_terrainMesh.colors = colors;         // 传回 GPU
```
`mesh.colors` 的 getter 每次都会分配一个新数组拷贝全部顶点色。一万格 → 每次几十万元素的数组分配。

**函数内改法**：
- 在 MapRenderer 中维护私有字段 `private Color[] _cachedTerrainColors`。
- 初始化时 `_cachedTerrainColors = _terrainMesh.colors` 缓存。
- 更新时直接改 `_cachedTerrainColors` 里的值，再 `_terrainMesh.colors = _cachedTerrainColors` 写回。
- 同样方式优化 `_riverMesh` 和 `_waterMesh`。
- **注意**：Chunk 分区后会天然解决此问题（每 Chunk Mesh 小得多），但在此之前的局部优化仍有价值。

**收益**：每回合省掉一次几千~几万元素的数组分配 + 拷贝。

---

### D. FogMask G 通道增量清空，替代全量遍历

**位置**：`MapRenderer.cs:1368-1373`（`UpdateFogMask`）

**现状**：每回合 `for (int k = 0; k < _fogMaskData.Length; k++)` 遍历全部像素清零 G 通道。

**函数内改法**：
- 维护一个 `List<int>` 记录"上一轮盖过 G 通道的像素索引"。
- 本回合先在上一轮的像素列表中逐个清零 G，再盖新的可见格像素，记录本轮索引。
- 不动遮罩结构和 Shader，只改清空方式。

**收益**：从 O(遮罩全像素数) 降到 O(可见格像素数)。100×100 地图下遮罩可能数千像素，可见格通常远小于全图。

---

### E. 生成期 List 预分配容量

**位置**：`MapRenderer.cs:73-77` 等多处 `new List<Vector3>()`、`new List<Color>()` 等

**现状**：生成 Mesh 时大量 List 无初始容量，Add 触发内部数组多次扩容（翻倍 + 拷贝）。

**函数内改法**：
- 根据 `hexVertices.Length` 估算容量：`new List<Vector3>(hexVertices.Length * 44)`（每格实心区 44 顶点）、`new List<Color>(hexVertices.Length * 44)` 等。
- 过渡面的 List 同理估算。
- 所有 `new List<T>()` 后面马上 `AddRange` 的地方都预设容量。

**收益**：减少生成期的内存扩容拷贝和 GC。改动纯参数，零风险。

---

### F. 顶点色填充：Color → Color32

**位置**：`MapRenderer.cs:145-146` 等多处向 List 逐元素 Add 顶点色

**现状**：使用 `Color`（4 个 float = 16 字节 / 顶点），且 `for c<44` 循环逐元素 Add。

**函数内改法**：
- 改用 `Color32`（4 个 byte = 4 字节 / 顶点），内存减为 1/4。
- `FogVertexColor` 返回 `Color32`，直接置 `.r`/`.g` 为 0 或 255。
- Mesh 的顶点色用 `mesh.SetColors(List<Color32>)`（Unity 2019+ 支持）。
- **注意**：需确认 Shader 是否以 0-1 范围读顶点色；是则需 Shader 端做 `/255.0` 或不改格式。

**收益**：顶点色内存减 75%，GPU 上传带宽减 75%。

---

### G. SanitizeNormalsAndTangents：无坏值免写回

**位置**：`MapController.cs:485-545`（`SanitizeNormalsAndTangents`）

**现状**：逐顶点检查 NaN/Inf/退化法线切线。即使全正常也会触发 `mesh.normals = normals` / `mesh.tangents = tangents` 赋值。

**函数内改法**：
- 已用 `normalsChanged` / `tangentsChanged` 标志跟踪。如果全程无坏值，直接 return 不调赋值。
- 当前逻辑已是条件赋值，但如果早期就遇到坏值设为 dirty、后续仍然遍历完才写——可加 early-out：全是好的则不写。
- 确认当前代码是否已经正确利用 dirty 标志；若是则跳过。

**收益**：生成期小幅加速，一万格 Mesh 无坏值时省 O(顶点数) 的数组赋值。

---

### H. `SyncCellObjectVisibility` 遍历：可先排除变化无关的格

**位置**：`MapRenderer.cs:1129-1149`

**现状**：遍历所有格检查 landFormModel / resourceModel / building 的 activeSelf。

**函数内改法（仅当前函数范围内）**：
- 遍历中 `activeSelf != IsExplored` 的判断已经避免无谓的 SetActive 调用（好的）。
- 可进一步：维护一个"上次视野状态缓存"（如 `private Dictionary<int, bool> _lastVisibleState`），只在状态翻转时才 SetActive，减少 Unity 原生方法调用次数。
- **注意**：这条需要额外字段，属函数级小改动；若不想加字段，当前遍历已有条件判断跳过不变格，不是最大瓶颈。

**收益**：中等。

---

### I. `CollectVisibleCells` 的 BFS 分配路径优化

**位置**：`FieldOfViewService.cs:100-117`

**现状**：每层 BFS 循环都 `new List<HexCellData>()` 创建 next。

**函数内改法**：
- 用两个复用的 `List<HexCellData>`（`_frontierA`、`_frontierB`）交替使用，`Clear()` 而非 new。
- 代码模式：
  ```
  var cur = _frontierA; cur.Clear(); cur.Add(center);
  var next = _frontierB;
  for ring...
      next.Clear();
      foreach cell in cur...
      swap(cur, next);
  ```

**收益**：BFS 过程中每层循环不再分配 List，与 B 条配合效果叠加。

---

## 七节总结：函数内优化的优先级排序

| 序 | 优化点 | 位置 | 主要收益 | 改动量 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 缓存敌方单位 Renderer/Canvas | `MapRenderer:1168` | 每回合消除子树遍历 + 数组分配 | 小 | ☑️ |
| 2 | FoV BFS 缓冲复用 + bool[] visited | `FieldOfViewService:82,96` | 每回合消除 GC spike | 小中 | ☑️ |
| 3 | 缓存 `mesh.colors` 数组字段 | `MapRenderer:1252` | 每回合消除大数组分配 | 小 | ☑️ |
| 4 | FogMask G 通道增量清空 | `MapRenderer:1368` | 从 O(全像素) 降到 O(可见像素) | 小中 | ☑️ |
| 5 | 生成期 List 预分配容量 | `MapRenderer:73+` | 减少加载期扩容 + GC | 极小 | ☑️ |
| 6 | Color → Color32 | 多处顶点色填充 | 内存 / GPU 带宽减 75% | 中 | ⏭ |
| 7 | BFS 双缓冲交替 Clear | `FieldOfViewService:100` | 消除 BFS 每层 List 分配 | 小 | ☑️ |
| 8 | Sanitize 无坏值免写回 | `MapController:485` | 生成期小幅 | 极小 | ☑️ |
| 9 | SyncVisibility 状态翻转缓存 | `MapRenderer:1129` | 减少 SetActive 调用 | 小 | ⏭ |

**推荐先做 1、2、3、5**：改动小、纯局部、无副作用，能立刻缓解"每回合卡一下"和加载 GC，且完全不挡后续的分区/材质合并大改造。

---

## 八、实施状态（2026-07-22）

已落地 6 条，3 条确认无需改动或跳过。

| 序 | 优化点 | 状态 | 涉及文件 |
|---|---|---|---|
| 1 | 缓存敌方单位 Renderer/Canvas | ✅ 已实施 | `MapRenderer.cs` |
| 2 | FoV BFS 缓冲复用 + `int[] visitedGen` | ✅ 已实施 | `FieldOfViewService.cs` |
| 3 | 缓存 `mesh.colors` 数组字段 | ✅ 已实施 | `MapRenderer.cs` |
| 4 | FogMask G 通道增量清空 | ✅ 已实施 | `MapRenderer.cs` |
| 5 | 生成期 List 预分配容量 | ✅ 已实施 | `MapRenderer.cs` |
| 7 | BFS 双缓冲交替 Clear | ✅ 已合并到 #2 | `FieldOfViewService.cs` |
| 8 | Sanitize 无坏值免写回 | ✅ 已有 `normalsChanged`/`tangentsChanged` 标志 | `MapController.cs:485` |
| 6 | Color → Color32 | ⏭️ 跳过（需改 MapController 接口签名） | — |
| 9 | SyncVisibility 状态缓存 | ⏭️ 跳过（`activeSelf != x` 已避免无谓 SetActive） | — |

### 改动要点

**FieldOfViewService.cs**：
- 新增 `_visitedGen`（`int[]`）替代每 BFS `new HashSet`，用 generation 计数避免每次清零
- 新增 `_frontierA`/`_frontierB` 双缓冲交替 + `_visibleBuffer` 复用
- `Recompute()` 走实例方法 `CollectVisibleCellsBuffered`（零分配）
- 保留 `public static CollectVisibleCells` 供 AI 模块兼容

**MapRenderer.cs**：
- 新增 6 个私有缓存字段（地形/水/河 color 数组、敌方 Renderer/Canvas 缓存字典、FogMask G 索引列表）
- `MainMapMeshCreat`/`RiverMeshCreat`/`LakeOrSeaMeshCreat` 的 List 均按 `cellCount * 44` 预分配容量
- 三块 Mesh 的 color 数组在生成时即缓存，运行时直接改值写回，不再 `mesh.colors` getter 拷贝
- `Subscribe`/`OnDisable` 注册 `IUnitRepository` 事件，敌人生成时缓存其 Renderer/Canvas
- `SyncCellObjectVisibility` 优先从缓存读取敌方 Renderer/Canvas
- `UpdateFogMask` G 通道从全量清零改为仅清零上一轮盖章的像素
- `StampCellToFogMask` 新增可选参数 `List<int> collectIndices`
