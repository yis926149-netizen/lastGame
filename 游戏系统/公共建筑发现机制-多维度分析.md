# 公共建筑发现机制——多维度分析

> 基于实际代码讨论。公共建筑开局隐藏，仅浮标示意；单位进入周围一环触发发现。

## 实施状态（2026-07-29）

阶段1至阶段4已完成代码落地，以下内容优先于本文早期讨论中的伪代码：

| 阶段 | 状态 | 实际实现 |
|---|---|---|
| 阶段1 基础发现机制 | **已完成** | 公共建筑生成后整体隐藏；`GameLoop` 每帧调用 `PublicBuildingBase.TickDiscovery()`；任意单位进入建筑外一环后全局发现；建筑占位格及外一环自动探索但不改变归属 |
| 阶段1 战斗隔离 | **已完成** | 玩家/AI 的 `FindNearestEnemyBuilding`、近战 `TargetInCell`、远程 `CanAttack/DoCombat` 均过滤隐藏公共建筑 |
| 阶段1 奖励与路径 | **已完成** | 发现前隐藏区域资源模型；发现时恢复；占领时通过 `_pendingCaptureRewards` 保留原有每格奖励；`GameLoop.InvalidateAllBrainPaths()` 清除旧方向路径 |
| 阶段2 浮标系统 | **已完成** | `PublicBuildingMarkerManager` 为普通 Zenject 单例；运行时创建 World Space Canvas（金色问号 + “公共建筑”）；呼吸动画并始终面向相机；发现后销毁 |
| 阶段3 单位趋向 | **已完成** | 玩家/AI 共用 `UnitBrainBase.FindApproximateDirectionToHiddenBuilding()`；近战/远程每次只向浮标方向移动一步，然后重新决策；浮标位置随机选自建筑外一环，不是准确占位格 |
| 阶段4 发现特效 | **已完成** | 复用 `ExplorationPillarPool` 当前 Inspector 选中的石柱/飞盘方案；新增 `PlayRevealEffect()`，只播放表现，不调用探索服务的占领/收割流程 |

### 实际代码位置

- `PublicBuildingBase.cs`：发现状态、检测区域缓存、发现流程、奖励延迟、资源恢复
- `PublicBuildingGenerator.cs`：初始隐藏、资源隐藏、浮标创建与 `PublicBuildingMarkerManager`
- `GameLoop.cs`：发现检测驱动与全单位路径失效
- `UnitBrainBase.cs`：双方共用的近似方向查询
- `MeleeStrategy.cs` / `RangedStrategy.cs`：方向单步移动和隐藏建筑攻击过滤
- `AIUnitBrain.cs` / `PlayerUnitBrain.cs`：隐藏建筑索敌过滤
- `AIEntityFactory.cs` / `CardPresenter.cs`：向双方 Brain 注入标记管理器
- `ExplorationPillarPool.cs`：无业务副作用的公共建筑发现特效入口
- `GameInstaller.cs`：标记管理器与探索特效池绑定

### 与早期方案的差异

- 没有新建场景 MonoBehaviour 版 MarkerManager，也不要求手动配置标记 Prefab；当前标记完全由代码创建。
- 浮标不再偏移到任意 1.5~2.5 格，而是随机落在建筑占位格的外一环。这样既不暴露准确占位格，也保证单位到达提示区域时一定触发发现。
- 建筑发现时立即激活，不在 inactive GameObject 上启动延迟协程；后续若需要模型渐显，应由独立且始终激活的表现控制器负责。
- 路径失效直接调用 `GameLoop.InvalidateAllBrainPaths()`，没有引入静态事件及订阅生命周期。
- `PublicBuildingMarkerManager` 暂与 `PublicBuildingGenerator` 同文件，避免运行中的 Unity 编辑器尚未刷新项目文件时漏编译新脚本；职责仍保持独立类型。

### 验证

- `dotnet build Assembly-CSharp.csproj --no-restore`：通过，0 Error。
- `git diff --check`：通过，仅有仓库现有 LF/CRLF 提示。
- 尚需在 Unity Play Mode 验收：标记尺寸、中文字体显示、石柱/飞盘批量播放帧率、双方单位实际行进方向和发现触发边界。

---

## 一、视觉效果层面

### 1.1 建筑模型的显隐控制

**当前实现**：
- `PublicBuildingGenerator.cs:153-156`：建筑实例化后直接 `SetActive(true)`，无显隐控制
- `PublicBuildingBase.cs:334-358`（`UpdateVisual`）：只更新血条颜色和 tag，模型始终可见

**需要新增**：
- `PublicBuildingBase` 新增 `DiscoveryState` 枚举 `{ Hidden, Revealed }`，默认 `Hidden`
- `Initialize()` 末尾当 `Hidden` 时 `gameObject.SetActive(false)`
- 隐藏后 `hex.BulidingTypeOnHex_Building.Value` 仍指向该 GameObject —— 需要在 `AIUnitBrain.FindNearestEnemyBuilding` 中过滤未发现建筑
- **Canvas/血条**：`SpawnUIWiring.WireBuildingCanvas`（`PublicBuildingGenerator.cs:180`）把 Canvas 作为建筑 prefab 的子对象挂载，`SetActive(false)` 会一并隐藏血条。Canvas 在 `uiConfig.AddRuntimeCanvas` 中注册的是组件引用，`CameraController` 恢复 Canvas 时（`CameraController.cs:296-305`，已改为无条件恢复）赋值 `canvas.enabled=true`，效果等到 `SetActive(true)` 后自然生效——无需额外处理。

### 1.2 浮标图标系统（全新）

**参考现有系统**：
- `PublicBuildingGenerator.cs:180` 调用 `SpawnUIWiring.WireBuildingCanvas` 挂血条（World Space Canvas）
- 浮标应为独立 World Space Canvas，不依附血条（血条随建筑隐藏，浮标始终显示）

**推荐方案B（偏移浮标）**：
- 浮标位置 = 建筑根格真实坐标 + 随机偏移 1.5~2.5 格半径
- 使用 `SeedService.GetRandom("MarkerOffset")` 保证确定性
- 浮标样式：半透明 + 呼吸灯 Alpha 脉冲，金色/暖色系（image组件）

**实现**：
```csharp
// 新建 PublicBuildingMarkerManager
public class PublicBuildingMarkerManager : MonoBehaviour
{
    public GameObject markerPrefab; // World Space Canvas 预制件
    private Dictionary<PublicBuildingBase, GameObject> _markers = new();

    public void CreateMarker(PublicBuildingBase building, System.Random rand)
    {
        float angle = (float)(rand.NextDouble() * Mathf.PI * 2);
        float dist = HexMetrics.outerRadius * (1.5f + (float)rand.NextDouble());
        Vector3 offset = new Vector3(Mathf.Cos(angle), 0, Mathf.Sin(angle)) * dist;
        Vector3 pos = building.RootHex.RealCenterWorldCoordinate + offset + Vector3.up * 5f;
        var marker = Instantiate(markerPrefab, pos, Quaternion.identity);
        _markers[building] = marker;
    }

    public void RemoveMarker(PublicBuildingBase building)
    {
        if (_markers.TryGetValue(building, out var marker))
        {
            Destroy(marker);
            _markers.Remove(building);
        }
    }
}
```

### 1.3 发现时的视觉过渡

**顺序**：
1. 浮标消失（粒子消散效果）
2. 建筑模型渐显（延迟 0.5 秒，使用类似石柱升起特效）
3. 建筑4格 + 周围一环的 Shader 从"去饱和+薄雾"过渡到正常渲染
4. 归属颜色逐步显示

- 特效复用 `地块探索特效-六边形石柱升起方案.md` 已有 `HexPillarEffect` 预制件
- 4 格 + 周围一环全部同时实例化特效

---

## 二、逻辑实现层面

### 2.1 发现触发机制

**触发条件**：单位进入公共建筑"周围一环"（4 格各自外圈的并集，HashSet 去重）

**实现方案（推荐事件驱动）**：
```csharp
// UnitMovementController.cs —— 单位到达新格子时触发
public static event Action<HexCellData> OnUnitEnteredHex;

// PublicBuildingBase 订阅此事件
private void CheckForDiscovery(HexCellData enteredHex)
{
    if (CurrentDiscoveryState == DiscoveryState.Revealed) return;
    if (GetInfluenceRingHexes().Contains(enteredHex))
    {
        Reveal();
    }
}
```

**注意**：`HexCellData` 单位存储有两套系统并存 —— 旧的 `HaveUnit`（`line 236`）和新的 `_occupant`（`line 241`），触发事件时需确保两者都会触发。

**备选方案（每帧检查，无需改 UnitMovementController）**：

> ⚠️ `GetInfluenceRingHexes` 当前是 `private`（`PublicBuildingBase.cs:287`），`GameLoop` 无法直接访问。建议不把检测逻辑放在 GameLoop，而是在 `PublicBuildingBase` 内新增 `TickDiscovery()` 方法，由 GameLoop 调用。同时首次调用后缓存检测环结果，避免每帧重算 4×6 邻格。

```csharp
// PublicBuildingBase.cs 新增字段 + 方法
private HashSet<HexCellData> _cachedDetectionRing;  // 缓存检测环

public void TickDiscovery()
{
    if (CurrentDiscoveryState == DiscoveryState.Revealed) return;

    if (_cachedDetectionRing == null)
    {
        _cachedDetectionRing = GetInfluenceRingHexes(); // private，内部可访问
    }

    foreach (var hex in _cachedDetectionRing)
    {
        if (hex.HasOccupant() || hex.IsHaveUnit())
        {
            Reveal();
            return;
        }
    }
}
```

```csharp
// GameLoop.cs:66 之后
TickPublicBuildingDiscovery();

private void TickPublicBuildingDiscovery()
{
    for (int i = _publicBuildings.Count - 1; i >= 0; i--)
    {
        _publicBuildings[i]?.TickDiscovery();
    }
}
```

### 2.2 Reveal() 实现

```csharp
public void Reveal()
{
    if (CurrentDiscoveryState == DiscoveryState.Revealed) return;
    CurrentDiscoveryState = DiscoveryState.Revealed;

    // 1. 播放特效（4格 + 周围一环）
    foreach (var hex in OccupiedHexes)
        InstantiateHexPillarEffect(hex.RealCenterWorldCoordinate);
    foreach (var hex in GetInfluenceRingHexes())
        InstantiateHexPillarEffect(hex.RealCenterWorldCoordinate);

    // 2. 自动探索所有相关地块（方案A：发现 = 探索）
    foreach (var hex in OccupiedHexes.Concat(GetInfluenceRingHexes()))
    {
        hex.IsUnexplorable = false;     // 开放给后续探索
        hex.ExploreThisHexCell();       // IsExplored = true
    }

    // 3. 延迟显示建筑模型
    StartCoroutine(DelayedShowBuilding(0.5f));

    // 4. 移除浮标
    _markerManager.RemoveMarker(this);

    // 5. 使所有单位的缓存路径失效（原来指向浮标的路径已作废）
    UnitBrainBase.OnForceInvalidatePaths?.Invoke();

    // 6. 触发 Shader 刷新
    _mapVisualEvent.Raise();
}
```

> **路径缓存为何需要失效**：单位开始朝浮标方向走后，`UnitBrainBase._cachedPath`（`UnitBrainBase.cs:125`）已缓存了指向浮标的完整路径。建筑被发现后浮标消失，但单位会把旧缓存路径走完才重新寻路——可能仍在走向已消失的浮标位置。因此需要广播失效事件。

**实现路径失效**——在 `UnitBrainBase.cs` 和 `GameLoop.cs` 中：

```csharp
// UnitBrainBase.cs 新增静态事件（文件顶部，class 内部）
public static event System.Action OnForceInvalidatePaths;

// 每个 UnitBrainBase 在 OnEnable / Start 中订阅，OnDisable / OnDestroy 中取消：
//   OnForceInvalidatePaths += InvalidatePath;
//   或由 GameLoop 统一管理

// GameLoop.cs 新增方法（由 Reveal() 调用或广播给所有 Brain）
public void InvalidateAllBrainPaths()
{
    for (int i = _brains.Count - 1; i >= 0; i--)
        _brains[i]?.InvalidatePath();
}
```

### 2.3 发现 vs 探索 vs 占领（新的三层语义）

| 概念 | 触发方式 | 结果 |
|---|---|---|
| **发现**（Discovery） | 单位进入周围一环（被动） | 模型显示 + `IsExplored = true` + `IsUnexplorable = false`。**不改变归属** |
| **探索**（Exploration） | 玩家主动花费资源 | 圈入势力范围 + 收割资源 |
| **占领**（Capture） | 击破公共建筑血量 | 建筑易主 + 周围强制覆盖势力范围 + 收割资源 |

**三者关系**：
```
Hidden Building（隐藏 + 浮标显示）
  ↓ 单位靠近触发发现（被动）
Revealed Building（可见 + IsUnexplorable=false + IsExplored=true）
  ↓ 玩家主动探索 / 占领
Explored / Captured Building（势力范围归属 + 收割资源）
```

### 2.4 发现 ≠ 占领

**关键区别**：
- 发现后地块 `IsExplored = true`，但 `Player_City_Index` 仍是 `(-1, -1)`（无主）
- 建筑仍是中立 `tag=NeutralBuilding`，`captureHp` 不变
- 任何一方都需要**击破血量**才能占领建筑

### 2.5 资源收割时机

**当前逻辑**：`PublicBuildingBase.cs:273-276` 占领时对 `!hex.IsExplored` 的格才收割

**问题**：发现时 `IsExplored = true`，占领时 `hex.IsExplored` 已为 `true`，跳过收割

**修复**：占领时不再检查 `IsExplored`，改为检查"资源是否已被收割"：
```csharp
// ExpandSphereOfInfluence:273 改为
if (hex.GetResource() != Enums.ResourceType.None)
{
    HarvestAndReward(hex, newOwnerPlayerIndex);
}
```
或引入新标记 `IsDiscoveredButUnclaimed`。

### 2.6 资源模型的显隐（发现前）

**问题**：资源模型存在 `HexCellData.resourceModel`，是独立于建筑 GameObject 的场景对象。建筑 `SetActive(false)` 不影�声音资源。若资源在雾中可见，玩家能通过"某片区域有异常密集的资源模型"反推建筑位置。

**两种选择**：

- **选项A（不隐藏）**：新 Shader 下 `isUnexplorable` 的格处于"去饱和+薄雾"状态，资源模型虽然可见但颜色灰蓝压暗，视觉上不易引起注意。实现零成本。
- **选项B（隐藏）**：在 `MarkUnexplorableArea` 末尾额外隐藏资源模型，发现时恢复：

```csharp
// PublicBuildingGenerator.MarkUnexplorableArea:224 之前
foreach (var hex in unexplorableArea)
{
    if (hex.resourceModel != null)
        hex.resourceModel.SetActive(false);
}

// PublicBuildingBase.Reveal() 中恢复
foreach (var hex in OccupiedHexes.Concat(GetInfluenceRingHexes()))
{
    if (hex.resourceModel != null)
        hex.resourceModel.SetActive(true);
}
```

**推荐选项B**，实现不算复杂且彻底消除定位漏洞，与"待确认问题#5"一致。

---

## 三、单位行为层面（玩家与 AI 完全一致）

> **前提确认**：玩家单位本来就是自主移动的。`PlayerUnitBrain` 在 `CardPresenter.cs:335-350` 生成后同样注册到 `GameLoop`，与 AI 单位一样由 `UnitBrainBase.OnStepFinished`（`UnitBrainBase.cs:146`）每帧驱动。策略层（`MeleeStrategy`/`RangedStrategy`）**不区分玩家/AI**，两者共用同一套 `ChooseNextPath` 决策链。因此"趋向公共建筑"的行为对玩家和 AI 一视同仁，改动集中在两个共用点。

### 3.1 "趋向方向"的实现

**当前单位移动决策链**（玩家/AI 共用）：
- `FindNearestEnemy()` → `FindNearestEnemyBuilding()`
- `MeleeStrategy.cs:26` / `RangedStrategy.cs:28`：先找敌人，再找建筑
- 没有"方向偏置"概念，只有精确坐标寻路

**问题**：`FindNearestEnemyBuilding` 能检测到隐藏的公共建筑（`cell.BulidingTypeOnHex_Building.Value` 在 `SetActive(false)` 后仍指向该 GameObject），单位会精准锁定隐藏建筑坐标。两个 Brain 都存在此问题：
- `AIUnitBrain.cs:91-96`
- `PlayerUnitBrain.cs:108-114`

**方案1：两个 Brain 都过滤未发现建筑**
```csharp
// AIUnitBrain.cs:96 之后 / PlayerUnitBrain.cs:114 之后（同样改动）
var pb = building.GetComponent<PublicBuildingBase>();
if (pb != null && pb.CurrentDiscoveryState == DiscoveryState.Hidden) continue;
```

**方案2：`FindApproximateDirectionToHiddenBuilding` 提升到基类**

因 Strategy 通过 `brain.` 调用，玩家/AI 两个 Brain 都要能访问，方法应声明在 `UnitBrainBase`：
```csharp
// UnitBrainBase.cs:208（FindNearestEnemyBuilding 声明）之后新增虚方法
public virtual Vector3? FindApproximateDirectionToHiddenBuilding() => null;
```

`AIUnitBrain` 和 `PlayerUnitBrain` 各自覆写，注入同一个 `PublicBuildingMarkerManager`，实现相同：
```csharp
public override Vector3? FindApproximateDirectionToHiddenBuilding()
{
    if (_markerManager == null || Owner?.model == null || MapData == null) return null;

    Vector3 selfHex = Owner.unitMovementController?.CurrentHexCoordinate
                      ?? MapData.WorldToHexCoordinate(Owner.model.transform.position);

    float closestDist = float.MaxValue;
    Vector3? closestMarkerPos = null;

    foreach (var marker in _markerManager.GetAllMarkers())
    {
        Vector3 markerHex = MapData.WorldToHexCoordinate(marker.transform.position);
        float dist = HexDistance(selfHex, markerHex);
        if (dist < closestDist && dist > 0.1f)
        {
            closestDist = dist;
            closestMarkerPos = markerHex;
        }
    }
    return closestMarkerPos;
}
```

### 3.2 优先级调整（玩家/AI 统一）

**修改 Strategy.ChooseNextPath**（因玩家/AI 共用，一处改动同时影响双方）：
```csharp
// MeleeStrategy.cs:26 / RangedStrategy.cs:28 改为
Vector3? targetHex = brain.FindNearestEnemy()
                     ?? brain.FindNearestEnemyBuilding()
                     ?? brain.FindApproximateDirectionToHiddenBuilding(); // 新增最低优先级
```

**开局特殊处理（可选）**：
- 开局 2 分钟内隐藏建筑优先级高于敌人，玩家和 AI 一样：
```csharp
if (gameLoop.GameTime < 120f)
{
    targetHex = brain.FindApproximateDirectionToHiddenBuilding()
                ?? brain.FindNearestEnemyBuilding()
                ?? brain.FindNearestEnemy();
}
```

> **注意**：玩家单位始终自主移动，没有"玩家手动控制 vs AI 自动"的区分。玩家不需要额外的 UI 引导或强制移动指令——单位会像 AI 一样自动趋向浮标方向。玩家的操控体现在部署单位、选择卡牌等决策上，而非逐格移动。

### 3.3 两个 Brain 的依赖注入

**AIUnitBrain.cs**：
```csharp
// L16 之后
private PublicBuildingMarkerManager _markerManager;
// Initialize:25 之后新增参数
PublicBuildingMarkerManager markerManager = null
```

**PlayerUnitBrain.cs**：
```csharp
// L16 之后
private PublicBuildingMarkerManager _markerManager;
// Initialize:37 之后新增参数
PublicBuildingMarkerManager markerManager = null
```

- `AIEntityFactory.GenerateUnit`（`AIEntityFactory.cs:180-190`）注入 AI 单位的 markerManager
- `CardPresenter` 生成玩家单位（`CardPresenter.cs:336-349`）注入玩家单位的 markerManager

### 3.4 `CanAttack` / `DoCombat` 也需要过滤隐藏建筑

**问题**：文档 §3.1 只讨论了 `FindNearestEnemyBuilding` 的过滤，但策略层还有两处直接检测 `NeutralBuilding` tag 的路由，也会命中隐藏建筑：

- `MeleeStrategy.cs:126`（`TargetInCell` 内）：`buildingInCell.CompareTag("NeutralBuilding")`
- `RangedStrategy.cs:112`（`CanAttack` 内）：`b.CompareTag("NeutralBuilding")`
- `RangedStrategy.cs:152`（`DoCombat` 内）：同上

若单位恰好走到隐藏建筑占位格旁边（可能发生，因为路径不按探索状态限制），`CanAttack` 会返回 `true`，`DoCombat` 会向隐藏建筑打出伤害。

**修复**：在三个方法各自的建筑检测代码块中，取到 `building` 后增加：

```csharp
// MeleeStrategy.cs:124-126（TargetInCell 返回之前）
GameObject buildingInCell = cell.BulidingTypeOnHex_Building.Value;
if (buildingInCell != null &&
    (buildingInCell.CompareTag(enemyBuildingTag) || buildingInCell.CompareTag("NeutralBuilding")))
{
    // 新增：跳过未发现的公共建筑
    var pb = buildingInCell.GetComponent<PublicBuildingBase>();
    if (pb != null && pb.CurrentDiscoveryState == DiscoveryState.Hidden)
        return (null, null);  // 或 continue（取决于上下文）
    return (buildingInCell, buildingInCell.tag);
}

// RangedStrategy.cs:111-112（CanAttack / DoCombat 各自）同理
var pb2 = b.GetComponent<PublicBuildingBase>();
if (pb2 != null && pb2.CurrentDiscoveryState == DiscoveryState.Hidden) continue;
```

> **受影响范围**：`MeleeStrategy.CanAttack`→`TargetInCell`（1处）、`MeleeStrategy.DoCombat`→`TargetInCell`（1处）、`RangedStrategy.CanAttack`（1处）、`RangedStrategy.DoCombat`（1处），共4处需要加同一种过滤。

---

## 四、具体代码改动清单

### 4.1 PublicBuildingBase.cs

| 位置 | 改动 |
|---|---|
| L14 类声明之后 | 新增 `public DiscoveryState CurrentDiscoveryState { get; private set; } = DiscoveryState.Hidden;` |
| 类外 | 新增 `public enum DiscoveryState { Hidden, Revealed }` |
| L39 之后 | 新增字段 `private PublicBuildingMarkerManager _markerManager;` + `private HashSet<HexCellData> _cachedDetectionRing;` |
| L46 `Initialize` 参数 | 新增 `PublicBuildingMarkerManager markerManager` |
| L97 之后 | `if (CurrentDiscoveryState == DiscoveryState.Hidden) gameObject.SetActive(false);` |
| 新增方法 | `public void Reveal()`（见 §2.2 实现，含路径失效 + 资源模型恢复） |
| 新增方法 | `public void TickDiscovery()`（见 §2.1，检测单位进入周围一环） |
| 新增协程 | `IEnumerator DelayedShowBuilding(float delay)` |
| `OnDestroy():367` | 添加 `_markerManager.RemoveMarker(this);`

### 4.2 AIUnitBrain.cs / PlayerUnitBrain.cs

| 文件 | 位置 | 改动 |
|---|---|---|
| AIUnitBrain.cs | L16 之后 | 新增 `private PublicBuildingMarkerManager _markerManager;` |
| AIUnitBrain.cs | L18 `Initialize` | 新增参数 `PublicBuildingMarkerManager markerManager = null` |
| AIUnitBrain.cs | L96 之后 | `var pb = building.GetComponent<PublicBuildingBase>();`<br>`if (pb != null && pb.CurrentDiscoveryState == DiscoveryState.Hidden) continue;` |
| PlayerUnitBrain.cs | L16 之后 | 同上，新增 `_markerManager` 字段 |
| PlayerUnitBrain.cs | L27 `Initialize` | 同上，新增 `markerManager` 参数 |
| PlayerUnitBrain.cs | L114 之后 | 同上，`FindNearestEnemyBuilding` 过滤隐藏建筑 |
| 两者（基类声明） | UnitBrainBase.cs:208 之后 | 新增 `public virtual Vector3? FindApproximateDirectionToHiddenBuilding() => null;` |
| 两者（各自覆写） | 新增方法 | `public override Vector3? FindApproximateDirectionToHiddenBuilding()` |

### 4.3 PublicBuildingGenerator.cs

| 位置 | 改动 |
|---|---|
| L22 `构造函数` | 新增参数 `PublicBuildingMarkerManager markerManager` |
| L187 之后 | `_markerManager.CreateMarker(pb, random)` |
| L188 之后 | `pb.gameObject.SetActive(false)`（隐藏建筑模型） |
| L224 之前（`MarkUnexplorableArea`末尾） | 对 `unexplorableArea` 中 `hex.resourceModel != null` 的格 `SetActive(false)`（详见 §2.6） |

### 4.4 MeleeStrategy.cs / RangedStrategy.cs

| 位置 | 改动 |
|---|---|
| L26 / L28 (`ChooseNextPath`) | 追加 `?? brain.FindApproximateDirectionToHiddenBuilding()` 作为 fallback |
| L124-126 (`TargetInCell`，Melee) | 建筑匹配后新增 `var pb = building.GetComponent<PublicBuildingBase>(); if (pb != null && pb.CurrentDiscoveryState == Hidden) return (null, null);` |
| L78-80 (`DoCombat`→`TargetInCell`，Melee) | 同上，`TargetInCell` 的过滤同时覆盖 `CanAttack` 和 `DoCombat` |
| L111-112 (`CanAttack`，Ranged) | 匹配 `NeutralBuilding` 后新增过滤隐藏建筑 |
| L150-152 (`DoCombat`，Ranged) | 同上 |

### 4.5 UnitBrainBase.cs / GameLoop.cs（路径失效）

| 文件 | 改动 |
|---|---|
| `UnitBrainBase.cs` | 新增 `public static event System.Action OnForceInvalidatePaths;` |
| `UnitBrainBase.cs`（各 Brain `Start`/`OnDisable`） | 订阅/取消 `OnForceInvalidatePaths += InvalidatePath` |
| `GameLoop.cs` | 新增 `public void InvalidateAllBrainPaths() { /* 遍历 _brains 调 InvalidatePath() */ }` |

### 4.6 HexCellData.cs（可选）

| 位置 | 改动 |
|---|---|
| L227 之后 | 新增 `public bool IsDiscoveredButUnclaimed { get; set; }` |

### 4.7 新建文件

| 文件 | 职责 |
|---|---|
| `PublicBuildingMarkerManager.cs` | 管理浮标实例的创建（含随机偏移）/销毁、`GetAllMarkers()` |
| 浮标 Canvas 预制件 | World Space Canvas + 呼吸灯动画（Unity Editor 创建） |

### 4.8 GameInstaller.cs

`PublicBuildingMarkerManager` 是 **MonoBehaviour**（挂载 World Space Canvas 预制件），不能通过 `Container.Bind<>().AsSingle()` 直接创建。正确方式：

1. 在场景 Hierarchy 中提前放一个空 GameObject，命名为 `PublicBuildingMarkerManager`，挂上该脚本
2. 在 `GameInstaller.cs` 中：
```csharp
Container.Bind<PublicBuildingMarkerManager>()
    .FromComponentInHierarchy()
    .AsSingle();
```
3. Zenject 自动找到场景实例并注入到 `PublicBuildingGenerator`（构造函数参数）、`AIEntityFactory`（`[Inject]` 字段）、`CardPresenter`（`[Inject]` 字段）

> ⚠️ `PublicBuildingGenerator` 是纯 C# 类（非 MonoBehaviour），通过构造函数接收 `MarkerManager`，Zenject 可正常注入 `FromComponentInHierarchy` 绑定的实例。`AIEntityFactory` 和 `CardPresenter` 各加一个 `[Inject] private PublicBuildingMarkerManager _markerManager;` 字段即可。

---

## 五、与现有设计的冲突与解决

### 5.1 与"地图全可见"原则的冲突

**探索重构核心**（`03-视觉表现.md`）：
- 地图全可见，迷雾只是去饱和+半透明雾
- `06-公共建筑与探索系统对接.md:27`：公共建筑始终显示

**冲突**：建筑模型彻底隐藏违背此原则

**解决**：公共建筑作为**唯一例外**——它是"待探索目标"，不是"地形的一部分"。在 `06-公共建筑与探索系统对接.md` 新增决策：
```
| #43 | 可见性（发现机制） | 开局隐藏，仅浮标示意；单位进入周围一环触发发现 |
```

### 5.2 与 `isUnexplorable` 的语义调整

**当前**（`05-迁移阶段计划.md:L42`）：`isUnexplorable` 是**永久标记**，不随易主变化

**调整为**：发现后 `IsUnexplorable = false`，开放给后续正常探索扩张

### 5.3 与 `01-本身逻辑.md §2` 的探索语义兼容

**当前**：探索 = 占领 + 收割（复合动作一次性完成）

**发现**：增加一个新概念，发现 ≠ 占领，只是让地块可见

**兼容**：
- 发现的地块 `IsExplored = true`，但不写入 `Player_City_Index`
- 后续占领建筑时通过 `ExpandSphereOfInfluence` 的 `!hex.IsExplored` 判定收割 → 改为按 `hex.GetResource() != None` 判定（详见 §2.5）
- 资源模型在发现前被隐藏，发现时恢复（详见 §2.6）

---

## 六、待确认产品问题

| # | 问题 | 推荐答案 |
|---|---|---|
| 1 | 浮标位置精度：精确 / 偏移 / 方向指南针 | **偏移**（1.5~2.5 格随机） |
| 2 | 发现时是否自动探索地块（IsExplored=true） | **是**（方案A，立即看清地形） |
| 3 | 多公共建筑时浮标数量 | **全部显示**（3 个建筑 = 3 个浮标） |
| 4 | 玩家初始单位行为：自动移动 / 仅 UI 引导 | **自主移动，与 AI 同一优先级链**（见 §3 前提确认） |
| 5 | 发现前资源模型是否也隐藏 | **隐藏**（否则能通过资源推断位置） |
| 6 | 发现触发者：仅己方 / 任意单位触发后全局可见 | **任意单位触发后全局可见**（避免信息不对称） |

---

## 七、实现优先级

### 阶段1：基础发现机制（最小可验证）
1. `PublicBuildingBase` 新增 `DiscoveryState` + `Reveal()` + `TickDiscovery()`
2. `PublicBuildingGenerator` 生成后 `SetActive(false)` + 隐藏 `isUnexplorable` 区域资源模型
3. `GameLoop.Tick` 新增 `TickPublicBuildingDiscovery()`
4. `AIUnitBrain` / `PlayerUnitBrain` 过滤未发现建筑（`FindNearestEnemyBuilding`）
5. `MeleeStrategy` / `RangedStrategy` 的 `CanAttack` / `DoCombat` 过滤隐藏建筑
6. `Reveal()` 中通过 `OnForceInvalidatePaths` 使所有单位路径缓存失效
7. 测试：开局看不到建筑，单位走近后建筑显示

### 阶段2：浮标系统
1. 新建 `PublicBuildingMarkerManager` + World Space Canvas 预制件
2. `PublicBuildingGenerator` 调用 `CreateMarker` with random offset
3. `Reveal()` 调用 `RemoveMarker`
4. 测试：开局看到浮标，走近后浮标消失、建筑显示

### 阶段3：AI 趋向行为
1. `AIUnitBrain` 新增 `FindApproximateDirectionToHiddenBuilding()`
2. `MeleeStrategy/RangedStrategy` 调整 fallback 优先级
3. 测试：AI 单位开局朝浮标方向移动

### 阶段4：视觉特效与抛光
1. `Reveal()` 中实例化 `HexPillarEffect`
2. 延迟显示建筑（配合石柱升起）
3. 浮标呼吸灯动画 + 粒子消失
4. 测试：发现时有戏剧性视觉反馈

### 阶段5：文档更新
- `06-公共建筑与探索系统对接.md` 新增决策 #43
- `03-视觉表现.md` 补充公共建筑隐藏特例说明

---

> **最后更新**：2026-07-29
