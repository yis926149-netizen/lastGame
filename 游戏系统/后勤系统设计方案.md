# 后勤系统设计方案

## 一、概念定义

后勤系统回答一个问题：

> 某阵营的某块领地，能否通过连续的己方地块连接到该阵营主城？

- **后勤畅通**：能连回主城，归属方的 `IsSupplied = true`；与归属方的 `IsExplored` 同为 `true` 时，该领地对双方都清除迷雾，并支持部署卡牌、开发相邻地块、兵营生产、金矿产金。
- **后勤中断**：不能连回主城，归属方的 `IsSupplied = false`，该领地对双方都覆盖迷雾，但士兵仍可通行和战斗，箭塔仍正常攻击，地块仍可被敌方占领。

策划案依据：[策划案.txt](策划案.txt:66)（第 3.4 节 连通）、[策划案.txt](策划案.txt:109)（第 7 节 领地颜色与功能）。

---

## 二、核心算法

### 2.1 连通判断

每个阵营独立 BFS/DFS：

1. 从该阵营主城格入队。
2. 遍历每个格子的六个共边邻居。
3. 邻居 `Player_City_Index.Key == 本阵营编号` 时入队。
4. 能访问到的所有格子 → `IsSupplied = true`（后勤畅通）。
5. 属于本阵营但未访问到的格子 → `IsSupplied = false`（后勤中断）。

复杂度：`O(阵营数 × 地块总数)`，当前地图规模完全可接受。

双方各自独立计算。由于 `Player_City_Index.Key` 是排他的单阵营归属，一块普通地在同一时刻最多对其当前所属阵营后勤畅通，不会同时属于玩家与 AI 的后勤网络。

### 2.2 重算策略

- **全量重算**：每次归属变化后，双方阵营全部重新计算。
- **时机**：
  - 对局初始化完成（双方主城及初始领地建立后）
  - 玩家开发中立地块成功
  - AI 开发中立地块成功
  - 敌方地块被占领
  - 已占领地块再次易主
  - 公共建筑导致归属变化

策划案依据：[策划案.txt](策划案.txt:128)（第 8 节 重新检查领地的时机）。

### 2.3 数据模型

`HexCellData.Player_City_Index.Key` 作为唯一阵营归属来源：

| 值 | 含义 |
|----|------|
| -1 | 中立 |
| 0 | 玩家 |
| 1 | AI |
| ≥2 | 公共建筑伪阵营（保留现有用途） |

`Value`（城市编号）仅兼容旧代码，不参与后勤计算。

`IsExplored` 不再作为迷雾的单一判断来源。已归属地块需要当前归属方的 `IsExplored` 和 `IsSupplied` 同时为 `true` 才清除迷雾。

- **`IsExplored(factionId)`**：指定阵营是否已发现该地块；属于永久历史状态，发现后不因断供清除。
- **`IsSupplied(factionId)`**：由指定阵营的后勤 BFS 结果决定，表示当前能否通过连续己方地块连接到该阵营主城；属于可逆当前状态。
- **已归属地块迷雾规则**：`visible = IsExplored(ownerFactionId) && IsSupplied(ownerFactionId)`；对所有观察方结果相同。
- **中立地块迷雾规则**：按观察方的永久发现状态判断；单位经过中立地块不写入发现状态，因此不会揭雾。
- 两种状态都必须按阵营存储或查询，不能各使用一个全局 `bool`。可使用阵营位掩码、`HashSet<int>` 或服务内 `Dictionary<int, HashSet<Vector3>>`。
- AI 开发取得归属且后勤畅通后，该领地对玩家可见；AI 领地断供后，对玩家和 AI 都重新覆盖迷雾。玩家领地同理。

---

## 三、系统架构

### 3.1 LogisticsService 接口

```text
// 主城注册（开局时调用）
RegisterMainCity(int factionId, HexCellData rootCell)

// 开发时调用（中立 → 己方）
SetOwner(HexCellData cell, int factionId)

// 占领/易主时调用（敌方 → 己方）
TransferOwner(HexCellData cell, int factionId)

// 清空归属（建筑摧毁等场景）
ClearOwner(HexCellData cell)

// 重算双方全部后勤（内部更新各方供应缓存）
RecalculateAll()

// 查询

// 后勤畅通查询（BFS 结果）
bool IsLogisticsConnected(HexCellData cell, int factionId)

// 指定阵营是否已经永久发现该地块
bool IsExploredByFaction(HexCellData cell, int factionId)

// 迷雾可见性：已归属地块按归属方探索+供应判断；中立格按观察方发现状态判断
bool IsVisibleToFaction(HexCellData cell, int viewerFactionId)

// 事件（Drives 迷雾刷新、兵营/金矿暂停）
event Action LogisticsChanged
```

### 3.2 内部数据结构

```text
Dictionary<int, HexCellData> mainCityRoots       // 阵营 → 主城格
Dictionary<int, HashSet<Vector3>> connectedCells  // 阵营 → 后勤畅通格坐标集合
Dictionary<int, HashSet<Vector3>> exploredCells   // 阵营 → 已发现格坐标集合
int version                                       // 递增版本号
```

### 3.3 现有文件改造

| 文件 | 改造内容 |
|------|---------|
| `TerritoryService.cs` / `ITerritoryService.cs` | 扩展为阵营通用服务，承担归属和后勤双重职责；或拆分为 `TerritoryService` + `LogisticsService` |
| `HexCellData.cs:216` | `Player_City_Index.Key` 作为唯一阵营归属真相源 |
| `HexCellData.cs` / 探索服务 | 将现有全局 `IsExplored` 改为阵营化查询或阵营位掩码，保留永久发现语义 |
| `LogisticsService` | 使用按阵营供应缓存表达 `IsSupplied(factionId)`；不能只在地块上新增一个全局 `bool` |

---

## 四、必须先修的数据前置

实现后勤系统之前，必须先统一以下数据问题：

### 4.1 归属数据一致化

当前问题：

- `Player_City_Index`（地块自身归属）、总领地字典、单城领地字典三份数据可能相互漂移。
- [AIAutoExplorer.cs](Assets/Scripts/AI/AIAutoExplorer.cs:84) AI 开发固定写 `(1, 0)`，不随主城变化。
- [PlayerModelManager.cs](Assets/Scripts/Managers/PlayerModelManager.cs:74) `RebuildSphereOfInfluence` 从单城字典反向覆盖总领地和 `Player_City_Index`。
- [EnemyModelManager.cs](Assets/Scripts/Managers/EnemyModelManager.cs:54) 同上。

修复原则：

> 地块自身的阵营归属字段是唯一真相。领地字典只能根据地块归属重建，不能反向覆盖地块。

### 4.2 主城索引冲突

- [GameFlowManager.cs](Assets/Scripts/Managers/GameFlowManager.cs:131) 玩家主城硬写城市编号 `0`，但没有调用 `AllocateCityIndex()`。
- [BuildingController.cs](Assets/Scripts/Controllers/BuildingController.cs:227) 首次城市接管仍会分配 `0` 并覆盖主城单城字典。

修复：玩家 `_nextCityIndex` 初始化为 `1`，或主城也统一走分配；P0 只有唯一主城，最好禁止普通城市易主逻辑。

### 4.3 公共建筑索引冲突

- [PublicBuildingBase.cs](Assets/Scripts/Controllers/PublicBuildingBase.cs:253) 公共建筑使用 `(阵营编号, 0)`，与 AI 主城的 `(1, 0)` 冲突。
- [PublicBuildingBase.cs](Assets/Scripts/Controllers/PublicBuildingBase.cs:276) 公共建筑失守时会删除整个单城键，可能连同 AI 主城记录一起删除。

修复：公共建筑不伪装为城市字典条目。

### 4.4 阵营化探索状态

当前问题：

- [HexCellData.cs](Assets/Scripts/Core/Models/HexCellData.cs:219) 只有一个全局 `IsExplored`。
- [AIAutoExplorer.cs](Assets/Scripts/AI/AIAutoExplorer.cs:87) AI 与玩家修改同一个探索字段，AI 探索会直接影响玩家迷雾。

修复：永久发现状态必须按阵营存储或查询。业务代码不得再直接读取全局 `cell.IsExplored` 判断某个阵营的视野。

### 4.5 探索结算原子性

当前问题：

- [ExplorationService.cs](Assets/Scripts/Core/Services/Exploration/ExplorationService.cs:61) 先扣费并标记已探索，领地、奖励和视觉刷新等待特效完成回调。
- [ExplorationPillarPool.cs](Assets/Scripts/Managers/ExplorationPillarPool.cs:46) 注入或 Prefab 缺失时不会订阅回调，地块会停留在“已扣费、已探索、未归属、无奖励”的半完成状态。

修复：扣费、发现、归属、奖励和后勤重算作为一次原子领域操作同步提交；特效只消费成功事件，不负责完成领域逻辑。

### 4.6 地形归属过滤

- [GameFlowManager.cs](Assets/Scripts/Managers/GameFlowManager.cs:155) 和 [PlayerModelManager.cs](Assets/Scripts/Managers/PlayerModelManager.cs:173) 扩展初始领地时缺少完整地形过滤，水域可能被写入阵营归属并参与 BFS。

修复：所有归属写入口统一拒绝不可取得归属的地形，BFS 也应防御性跳过这类地块。

---

## 五、重算入口

| 阶段 | 入口位置 | 触发条件 |
|------|---------|---------|
| 初始化 | `GameFlowManager.Initialize` (`GameFlowManager.cs:37`) | 双方主城和初始领地建立后，在 `MarkInitializationComplete` 前重算一次 |
| 玩家开发 | `ExplorationService.CompleteExploration` (`ExplorationService.cs:73`) | 归属写入后重算 |
| AI 开发 | `AIAutoExplorer.TryAutoExplore` (`AIAutoExplorer.cs:51`) | 归属写入后重算（不再直接写字段，由归属服务统一提交） |
| 地块占领 | 未来实现的占领服务 | 归属变更后重算 |
| 公共建筑易主 | `PublicBuildingBase.OnCaptured` | 所有格归属变更后重算（一次，不逐格重算） |

---

## 六、业务接入点

### 6.1 卡牌部署

| 文件 | 行号 | 改造内容 |
|------|------|---------|
| `CardPresenter.cs` | 264 | `IsReleaseValid` 改为检查"己方归属且后勤畅通"，移除建筑部署对单城字典键的检查 |
| `CardPresenter.cs` | 397 | 建筑生成不依赖单城字典 |

### 6.2 玩家开发

| 文件 | 行号 | 改造内容 |
|------|------|---------|
| `AdjacencyExplorationRule` (`ExplorationService.cs`) | 143 | 改为邻接"玩家后勤畅通"格 |
| `PlayerInputHandler.cs` | 218 | `HasExploredPlayerNeighbor` 改为后勤畅通 |
| `CostLabelRenderer.cs` | 228 | `HasExploredNeighbor` 改为后勤畅通 |

### 6.3 AI 行为

| 文件 | 行号 | 改造内容 |
|------|------|---------|
| `AIAutoExplorer.cs` | 51 | 候选格只能从 AI 后勤畅通边界生成 |
| `AICardBrain.cs` | 153 | 部署候选格改为 AI 后勤畅通 |

### 6.4 兵营

| 文件 | 行号 | 改造内容 |
|------|------|---------|
| `BarracksSpawner.cs` | 34 | 兵营格断供时不推进 `_timer` |
| `BarracksSpawner.cs` | 66 | 出生格必须属于同阵营且后勤畅通 |

### 6.5 金矿（未来）

- 保存所在 `HexCellData`。
- 每次产金前查询"当前归属方 + 当前后勤畅通"。
- 易主不销毁矿，断供暂停。

### 6.6 不受后勤影响的系统

- 箭塔攻击 (`ArrowTowerShooter.cs`)
- 士兵移动、寻路、战斗 (`MeleeStrategy.cs`, `UnitBrainBase.cs`)
- 建筑生命值和死亡逻辑

### 6.7 已发现的关联缺陷

这些问题不是后勤规则本身，但会绕过或破坏后勤门控，需一并修复：

- [AICardBrain.cs](Assets/Scripts/AI/AICardBrain.cs:195) AI 建筑放置未检查 `IsHaveUnit()`，可与现有单位重叠。
- [BarracksSpawner.cs](Assets/Scripts/Controllers/BarracksSpawner.cs:82) 出生格未检查兵营所属阵营和后勤，可跨边界向敌方地块产兵。
- [ExplorationService.cs](Assets/Scripts/Core/Services/Exploration/ExplorationService.cs:144)、[PlayerInputHandler.cs](Assets/Scripts/Core/Services/PlayerInputHandler.cs:228)、[CostLabelRenderer.cs](Assets/Scripts/Managers/CostLabelRenderer.cs:228) 复制了三套开发邻接规则，UI 与执行层容易漂移。应统一调用无副作用的开发合法性查询。
- [UnitMovementSystem.cs](Assets/Scripts/Core/Services/UnitMovementSystem.cs:226) 与 [BuildingController.cs](Assets/Scripts/Controllers/BuildingController.cs:296) 均没有普通地块占领入口。
- 地块单位占用同时存在 `HaveUnit` 与 `Occupant` 两套状态，生成、移动和死亡路径清理不一致；占领判定前应先统一占用查询接口。

---

## 七、表现层

### 7.1 可视规则

已归属地块按当前归属方状态统一判断，双方观察结果一致：

| 条件（当前归属方状态） | 视觉效果 | 说明 |
|------|---------|------|
| `IsExplored && IsSupplied` | 迷雾清除，地块正常显示 | 已发现且后勤畅通 |
| `IsExplored && !IsSupplied` | 覆盖迷雾 | 已发现但后勤中断 |
| `!IsExplored` | 覆盖迷雾 | 未发现 |

AI 开发的连通领地会为玩家揭雾；AI 领地断供后会对双方重新覆盖迷雾。中立格仍按观察方发现状态区分，单位路过不揭雾。

### 7.2 实现方案

当前项目已有迷雾系统（`FogManager` + `FogBlend.cginc`），核心原理是：

- `MapRenderer` 创建并更新全局 `_FogMaskTex`，Shader 最终根据该遮罩决定迷雾。
- [FogBlend.cginc](Assets/Shader/FogBlend.cginc:169) 虽接收顶点色中的 `exploration` / `visibility` 参数，但当前最终混合未使用它们，只采样 `_FogMaskTex`。
- [MapRenderer.cs](Assets/Scripts/Managers/MapRenderer.cs:1324) 当前遮罩采用“只增不减”的增量盖章，已经揭开的区域不能重新覆盖迷雾。
- `UpdateExplorationVisuals()` 当前没有动态调用入口，单独修改顶点色不足以实现后勤迷雾。

改造方案：

- 后勤重算后更新各阵营的供应缓存，并触发带版本号的 `LogisticsChanged`。
- `MapRenderer` 全量重建 FogMask：先清空 R 通道；已归属格只盖章 `IsExploredByFaction(cell, ownerFactionId) && IsLogisticsConnected(cell, ownerFactionId)`，中立格按玩家发现状态判断。
- 不再使用 `_fogMaskStamped` 维持永久单调状态；每次探索、归属或后勤变化均允许地块由清晰变迷雾，也允许恢复后重新揭雾。
- 玩家渲染使用玩家状态。AI 不需要实际渲染画面，但其决策必须查询 AI 自己的探索和供应状态。
- 保持现有 Shader 采样 `_FogMaskTex` 的方案时无需修改 `FogBlend.cginc`；但必须修改 FogMask 的生成和刷新方式，不能只改顶点色。

### 7.3 改造范围

| 文件 | 改造内容 |
|------|---------|
| `MapRenderer.cs` | FogMask 改为按归属方探索与供应状态全量可逆重建；中立格再按观察方发现状态判断 |
| `MapVisualEventSO` / `LogisticsChanged` | 增加明确的后勤变更刷新入口，避免只有无参数视觉事件且无法区分状态版本 |
| `FogBlend.cginc` | 保持 `_FogMaskTex` 为权威时无需改动；不得依赖当前未使用的顶点参数 |

---

## 八、敌方地块占领

### 8.1 触发条件

当士兵进入/停留在敌方地块时触发占领判定，策划案 [策划案.txt](策划案.txt:55)（第 3.3 节）和 [策划案.txt](策划案.txt:163)（第 10 节）：

**入口一：我方士兵进入敌方地块**
- 士兵从一个格子移动到相邻格子。
- 移动完成后，检查目标格是否属于敌方。

**入口二：敌方最后一个单位或建筑被清除**
- 我方士兵已经站在敌方地块上。
- 敌方单位死亡或敌方建筑被摧毁后，该格无敌方存在。

### 8.2 判定条件

必须同时满足（全部由程序判断，不消耗金币）：

- 地块 `Player_City_Index.Key` 等于敌方阵营编号。
- 地块上存在我方士兵。
- 地块上不存在敌方单位。
- 地块上不存在敌方建筑。
- 该格不是敌方主城格。

### 8.3 成功结果

1. 地块 `Player_City_Index.Key` 改为我方阵营编号。
2. 不消耗金币。
3. 不重复触发探索奖励（奖励只在首次开发时结算一次）。
4. 原有金矿继续保留。
5. 调用 `LogisticsService.TransferOwner` 并重算双方后勤。
6. 占领后即使士兵离开或死亡，地块仍保持当前归属。

### 8.4 不触发占领的情况

- 士兵经过中立地块：不改变归属（策划案第 3.2 节）。
- 目标格是敌方主城格：不占领（主城只能被摧毁，不能占领）。
- 目标格仍有敌方单位或建筑：不满足条件。

### 8.5 实现要点

占领判定可嵌入现有移动完成流程：

- 士兵移动完成（`UnitMovementController.OnMoveFinished` / `UnitMovementSystem.CommitDestination`）后。
- 检查目标格 `Player_City_Index.Key` 是否等于敌方阵营。
- 满足条件则调用占领逻辑。

建筑摧毁后亦需触发：

- `BuildingController.BuildingDestroyed` 清理建筑占用后。
- 检查该格是否有我方士兵站立。
- 有则触发占领判定。

### 8.6 与后勤系统的关系

占领不要求提前连通主城（策划案第 3.3 节），但占领后的后勤状态取决于地块和主城之间是否有连续己方领地：

**场景一：夺回被切断的己方领地**

```text
主城 - 蓝 - 蓝 - 红 - 蓝 - 蓝
          ↑后勤畅通↑   ↑断供↑
```

踩上中间红格占回后：

```text
主城 - 蓝 - 蓝 - 蓝 - 蓝 - 蓝
          ↑全链恢复畅通↑
```

直接打通，后方断供领地恢复后勤。

**场景二：跨中立地块占领敌方领地**

```text
主城 - 蓝 - 蓝 - 中立 - 中立 - 红
```

士兵穿过中立地踩上红格后，红变蓝：

```text
主城 - 蓝 - 蓝 - 中立 - 中立 - 蓝
    ↑后勤畅通↑              ↑断供↑
```

新占的蓝地与主城之间隔着两块中立地，后勤中断。中立地不会自动变成蓝地——只能逐格付费开发才能打通。占领前该红地对敌方也是断供的——敌方主城在另一端，中间也可能隔着中立。

**结论**：每次占领后 BFS 重算双方后勤。能否恢复畅通，取决于新占格在 BFS 图中是否恰好连接了断供区域与主城。

---

## 九、实施顺序

| 阶段 | 内容 | 依赖 |
|------|------|------|
| 1 | 统一归属写入口，修复主城/公共建筑索引冲突，禁止领地字典反向覆盖地块 | 无 |
| 2 | 将探索改为原子结算，并建立按阵营的永久发现状态 | 阶段 1 |
| 3 | 实现 `LogisticsService`（主城注册 + 按阵营 BFS 缓存 + 变更事件） | 阶段 1 |
| 4 | 将 FogMask 改为按观察阵营全量可逆重建 | 阶段 2、3 |
| 5 | 统一开发合法性查询，接玩家/AI 开发和部署门控 | 阶段 3 |
| 6 | 统一单位占用查询并实现敌方普通地块占领 | 阶段 1、3 |
| 7 | 接兵营断供暂停和连通出生格，修复跨边界产兵 | 阶段 3 |
| 8 | 接未来金矿 | 阶段 3 |
| 9 | 清理旧单城字典对业务规则的剩余影响 | 阶段 1-8 |

---

## 十、测试范围

### 10.1 算法测试

- 连续领地全部后勤畅通
- 中间一格被敌方占领后，后方断供
- 中间格夺回后恢复
- 环形领地存在替代路径时不断供
- 地图边缘、空根、根节点归属错误

### 10.2 双方测试

- 一次易主同时切断旧方、连接新方
- 双方缓存无残留
- `RecalculateAll` 后双方正确

### 10.3 业务规则测试

- `IsSupplied = false` 时迷雾覆盖（即使 `IsExplored = true`）
- 断供领地不可部署
- 断供领地不可作为开发邻接源
- 士兵可移动、战斗和占领
- 箭塔不受影响

### 10.4 兵营测试

- 断供暂停且保留进度
- 恢复后继续
- 100% 无出生格时等待
- 出生格必须连通

### 10.5 边界测试

- 中立格、公共建筑格、不可探索格、水域、河流、金矿格

### 10.6 现有缺陷回归测试

- 公共建筑被 AI 占领再失守时，AI 主城 `(1, 0)` 记录不被删除。
- 玩家主城占用城市编号 `0` 后，任何分配器都不会再次返回 `0`。
- AI 探索不会改变玩家的永久发现状态或玩家 FogMask。
- 探索特效缺失、禁用或中断时，扣费、归属和奖励仍严格完成一次。
- 断供后已揭开地块重新覆盖迷雾，恢复供应后再次清晰。
- AI 建筑不能与单位重叠。
- 兵营不能向敌方、中立、断供或已预留地块产兵。
- 移动完成和建筑清除后的占领判定不会占领主城，也不会重复结算探索奖励。

---

## 十一、已确认事项

1. 公共建筑占领后自动取得周围整片领地，后勤重算以整片领地归属变更后的全部地块为准。
2. 石头桥面设定暂时取消，相关地形类型和业务规则移出 P0 范围。
3. 新增阵营化的供应状态，迷雾由指定阵营的 `IsExplored(factionId)` 和 `IsSupplied(factionId)` 共同决定：两者均为 `true` 时清除迷雾，缺一不可。
4. 已归属领地的迷雾由当前归属方的永久发现状态和供应状态共同决定，对双方观察者结果相同；中立地块仍按观察方发现状态区分，单位经过不揭雾。
5. 地块占领不指定具体占领者，只需满足"地块上存在我方士兵"即可触发占领判定。

---

## 十二、暂不需要

- 按城市分别计算后勤（P0 双方各只有一个主城）
- 增量连通算法、并查集等复杂数据
- 四色领地染色和 Shader 修改（改用迷雾表现）
- 箭塔、移动、寻路、战斗依赖后勤

---

## 十三、代码静态审查结论（2026-07-29）

当前代码尚未实现后勤系统，不能直接补充 BFS 后投入使用。以下是实施前必须处理的主要风险：

| 严重程度 | 问题 | 代码位置 | 影响 |
|---------|------|---------|------|
| 阻断 | 缺少 `LogisticsService`、阵营化探索/供应状态和重算事件 | `TerritoryService.cs`、`HexCellData.cs` | 无法执行任何后勤规则 |
| 阻断 | FogMask 只增不减，Shader 最终只读取 FogMask | `MapRenderer.cs:1324`、`FogBlend.cginc:169` | 断供后无法重新起雾 |
| 严重 | 公共建筑和 AI 主城共用 `(1, 0)`，失守时删除整个键 | `PublicBuildingBase.cs:253`、`:276` | AI 主城和领地记录被破坏 |
| 严重 | 总领地、单城领地和地块归属相互漂移 | `TerritoryService.cs`、`PlayerModelManager.cs:74`、`EnemyModelManager.cs:54` | BFS 输入不稳定，易主结果可被旧字典覆盖 |
| 严重 | 玩家主城占用 `0`，分配器仍从 `0` 开始 | `GameFlowManager.cs:131`、`PlayerModelManager.cs:69` | 首次城市接管复用主城索引 |
| 严重 | 普通地块占领的移动完成和建筑清除入口均不存在 | `UnitMovementSystem.cs`、`BuildingController.cs:296` | 无法发生切断、夺回和飞地占领 |
| 严重 | 开发、部署、AI 和兵营均未接供应门控 | 第六节所列文件 | 断供领地仍可继续发挥功能 |
| 高 | 探索领域结算依赖视觉特效回调 | `ExplorationService.cs:61`、`ExplorationPillarPool.cs:46` | 特效异常时扣费后不归属、不奖励 |
| 高 | AI 建筑放置漏检单位 | `AICardBrain.cs:195` | 单位与建筑可重叠 |
| 高 | 兵营出生格不检查归属和后勤 | `BarracksSpawner.cs:82` | 可跨边界或向断供地块产兵 |

本结论来自静态代码审查，尚未运行 Unity 场景测试。实施时应按第九节顺序逐项修复，并在每阶段完成对应自动化或场景回归测试。
