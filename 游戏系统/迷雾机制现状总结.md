# 迷雾机制现状总结

> 整理时间：2026-07-28 | 当前为阶段7后的两态系统（已探索 / 未探索），三态迷雾与独立AI迷雾均已移除

---

## 1. 数据结构：`IsExplored` 是唯一真相源

**`Assets/Scripts/Core/Models/HexCellData.cs:220`**

```csharp
public bool IsExplored { get; private set; }
```

`IsVisible` 已在阶段6删除（第222行注释）。`IsUnexplorable`（第226行）标记公共建筑周边不可探索的地块。探索标记通过 `ExploreThisHexCell()`（第312行）内部写入，**仅能由 `ExplorationService` 调用**。

---

## 2. 探索触发：玩家点击

**`Assets/Scripts/Core/Services/PlayerInputHandler.cs:189-227`**

`HandleTileClickForExploration()` 在左键点击未探索地块时调用 `IExplorationService.TryExplore()`，前置条件：

- 未拖拽卡牌
- 点击未命中UI
- Raycast 命中 `Map` 层
- 该格未被探索
- 存在玩家已探索的相邻地块（第190-227行）

高亮逻辑（第87行 `HighlightGridOnMouseHover()`）仅在 `IsExplored == true` 的地块上显示网格线。

---

## 3. 探索验证与执行

**`Assets/Scripts/Core/Services/Exploration/ExplorationService.cs`**

**`TryExplore()`（第39-70行）** 五层验证：

| 条件 | 失败结果 |
|---|---|
| 水域（LakeOrSea） | `AlreadyExplored` |
| 已探索 | `AlreadyExplored` |
| `IsUnexplorable`（公共建筑周边） | `Unexplorable` |
| 无相邻已探索地块 | `NotAdjacent`（`AdjacencyExplorationRule`，第131行） |
| 金币不足 | `InsufficientResources` |

通过后调用 `ExploreThisHexCell()` 并触发 `CellExplored` 事件。

**`CompleteExploration()`（第73行）** 执行三件事：

1. `ITerritoryService.Claim(cell)` → 设置 `Player_City_Index`，加入势力范围（**`Assets/Scripts/Core/Services/Territory/TerritoryService.cs:32`**）
2. `HarvestAndReward()` → 资源转金币（基础5 + 资源奖励）
3. `_mapVisualEvent.Raise()` → 触发全部视觉订阅者

---

## 4. AI 自动探索

**`Assets/Scripts/AI/AIAutoExplorer.cs:39-51`**

每3秒 Tick 的 `TryAutoExplore()`：

1. 检查 AI 是否有足够金币
2. 从 `EnemyModelManager.Enemy_SphereOfInfluence_HexC_HexCellData` 收集 AI 拥有的所有地块
3. 找出这些地块的**未探索 + 非水**邻居
4. 随机选一个候选
5. 扣除金币、标记已探索、分配 AI 归属
6. 收割资源、触发 `_mapVisualEvent.Raise()`

---

## 5. 迷雾视觉效果：Shader 层

### 5.1 Shader 全局属性配置

**`Assets/Scripts/Managers/MapRenderer.cs:1106`** — `SetupFogGlobalShaderProperties()`

| 属性 | 值 | 作用 |
|---|---|---|
| `_FogTex` | 程序化噪点纹理 | 雾面纹理 |
| `_FogMapOrigin` / `_FogMapSize` | 地图世界空间包围盒 | 单张纹理覆盖全图 |
| `_FogPixelSize` | >0 有锯齿，0=平滑 | 锯齿边界控制 |
| `_FogJaggedAmount` | — | 锯齿扰动幅度 |
| `_FogUnexploredDesaturate` | 0.5 | 未探索区域去饱和强度 |
| `_FogUnexploredBlend` | 0.7 | 雾色叠加强度 |
| `_FogScrollSpeed` | — | 雾纹理滚动动画速度 |

### 5.2 FogMask 纹理

**`MapRenderer.cs:1222`** — `CreateFogMask()`：世界对齐的 RGBA32 纹理，R 通道 = 探索掩码（0或255）。

**`MapRenderer.cs`** — `StampCellToFogMask()`：将六边形足迹戳入 R 通道。`UpdateFogMask()` 在新地块被探索时增量更新。

### 5.3 FogBlend.cginc 核心逻辑

**`Assets/Shader/FogBlend.cginc`**

- **`FogBlend_vert()`**（第27行）：世界坐标 XZ → 归一化 UV [0,1]
- **`FogBlend_warpUV()`**（第57行）：锯齿边界核心——可选像素量化 + 低频连续值噪声扰动 UV
- **`FogBlend_resolveEdge()`**（第79行）：像素模式 `step(0.5, v)` / 平滑模式 `smoothstep`
- **`FogBlend_final()`**（第110行）：`finalcolor` 阶段——对未探索像素去饱和为灰度 + 冷蓝色调 + 半透明滚动雾覆盖，通过 `exploredJagged` 在已探索/未探索边界做混合
- **`FogBlend_surf()`**（第91行）/**`FogBlend_shadowClip()`**（第103行）：当前为空函数（不压暗、不裁阴影）

### 5.4 地形 Shader 接入

**`Assets/Shader/TerrainBase_Fog.shader`**

- `#pragma surface surf Standard fullforwardshadows vertex:vert addshadow finalcolor:fogFinal`
- 引入 `FogBlend.cginc`
- `vert()`（第46行）提取顶点色 `.r`（= IsExplored），调用 `FogBlend_vert()`
- `fogFinal()`（第75行）调用 `FogBlend_final()`

### 5.5 水面 Shader

**`Assets/Shader/River.shader`（第44行）** 和 **`Assets/Shader/LakeorSea.shader`（第144行）**：已简化——"始终显示正常水面，不再按探索状态覆盖迷雾或压暗"。

### 5.6 顶点色编码

**`MapRenderer.cs:1212`** — `FogVertexColor()`

```csharp
float r = cell.IsExplored ? 1f : 0f;
return new Color(r, 0f, 0f, 1f);
```

同时用于地形网格（`UpdateExplorationVisuals`，第1182行）、水面网格（`UpdateWaterExplorationVisuals`，第1295行）、河流网格（`UpdateRiverExplorationVisuals`，第1330行）。

---

## 6. 迷雾遮蔽网格（封皮）

**`Assets/Scripts/Managers/FogManager.cs`**

- **`GenerateFog()`**（第133行）：调用 `MeshDataGenerator.GetFogVertices()` 获取外部边界 + 已探索空洞，生成带洞雾面
- **`GenerateFogCover()`**（第144行）：矩形边界 + 探索空洞裁剪
- **`GenerateFogConnector()`**（第91行）：矩形封皮与不规则地图边缘之间的连接面片

**`Assets/Scripts/Core/Services/MeshDataGenerator.cs:5576`** — `GetFogVertices()`：用 DFS 聚类所有 `IsExplored` 地块为空洞区域，移除内部地块保留边缘，逆时针遍历边缘生成顶点列表。

---

## 7. 已彻底移除的旧机制

| 组件 | 原作用 | 移除位置 |
|---|---|---|
| `FieldOfViewService` | 每帧计算玩家视野 | `GameInstaller.cs:83` 注释 |
| `AIFogService` | 独立 AI 迷雾 | `GameInstaller.cs:83` 注释 |
| AI 实体 `SetActive(false)` | 基于迷雾隐藏 AI 单位/建筑/城市 | `AIEntityFactory.cs:25` 注释 |
| 敌方势力范围 `IsVisible` 裁切 | 按可见性裁剪势力范围显示 | `SphereOfInfluenceRenderer.cs:106` |
| `SyncCellObjectVisibility` | 按探索状态 Active 切换地块对象 | `HexCellData.cs:321` 注释引用 |

---

## 总体流程

```
玩家点击 / AI Tick
  │
  ▼
ExplorationService.TryExplore(cell)
  ├─ 验证（邻接、费用、不可探索）
  ├─ GoldWallet 扣费
  └─ cell.ExploreThisHexCell() → IsExplored = true
      │
      ▼ CellExplored 事件
      │
      ├─ ExplorationPillarPool → 柱体动画
      │   └─ dissolve 回调 → ExplorationService.CompleteExploration(cell)
      │       ├─ TerritoryService.Claim(cell) → Player_City_Index + 势力范围
      │       ├─ HarvestAndReward() → 资源 → 金币
      │       ├─ _mapVisualEvent.Raise()
      │       │   ├─ MapRenderer.UpdateFogMask()       → 戳入 _FogMaskTex
      │       │   ├─ MapRenderer.UpdateExplorationVisuals() → 顶点色更新
      │       │   ├─ SphereOfInfluenceRenderer.RefreshAllSpheres()
      │       │   ├─ FogManager.OnMapVisualChanged()
      │       │   └─ CostLabelRenderer.RefreshLabels()
      │       └─ ExplorationRewardTriggered → ExplorationRewardSystem
      │
      └─ CostLabelRenderer.OnCellExplored() → RefreshLabels()
```
