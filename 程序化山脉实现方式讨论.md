# 程序化山脉实现方式讨论

> 定位：**初步构思，未实现，仅供未来探讨方向**。讨论"地块之上的程序化突起山脉"如何融入现有地图系统。
> 日期：2026-08-05（同日多轮讨论后修订：路线 A 结论反转；文档按"作用 / 生成规律 / 网格构造"三大主题重构；补充"固定锚点"与"Low-poly 风格"硬约束）。
> 更新：2026-08-06（六项关键决策确认：①山格不可通行；②山形参数生成时固化；③山与河不共格、先生成山再生成河；④固定锚点 = 山体覆盖区外边界；⑤solid/rect/tri 全部替换式拓扑；⑥离散色阶走山体专属 shader。详见文末"已确认决策"。）
> 更新续：2026-08-06 第二轮（主题一玩法决策 ⑦~⑭；主题二生成规律决策 ⑮~㉕——含"山脉以一条脊线为生成单位、不做单格山"的粒度变更、山脊线综合评分、山格上不生成其他地貌等，详见文末"已确认决策"。）
> 更新续：2026-08-06 第三轮（主题三网格构造决策 ㉖~㉛：法线方案、UV 策略、Low-poly 参数、顶点拆分顺序、碰撞策略、验收标准确认；路线 C 升级条件暂不讨论。详见文末"已确认决策"。）
> 源码审计：2026-08-06（逐项复核生成、动态地图、Chunk 网格、碰撞、迷雾、探索、高亮与镜头调用链；修正“当前事实/未来方案”混写、碰撞矛盾和若干遗漏。新增“源码审计修正”章节；已确认决策仍代表目标行为，不代表当前代码已实现。）
> 实施进度：2026-08-06（第一批实现完成：阶段 1 数据模型与玩法规则、阶段 2 核心生成管线已落地；新增 `MountainRidgeData`、`MountainConfigSO`、`MountainCellRule`、`RidgeGenerator`，生成顺序/多簇互斥/河流避让/水陆与清除规则已接入。新增山脉规则、脊线生成、动态清除测试并全部通过；阶段 3 及之后尚未开始。）
> 实施进度续：2026-08-06（阶段 3.1 完成：`MountainHash` 确定性 hash、`MountainGeometryBuilder` 高度场与 `HasVisibleMountain`、快照字段补齐、`MountainGeometryTests`；阶段 3.2 完成：`TerrainGeometry` 增 `MountainIndices`/`CollisionIndices`/`HasMountain`、`ChunkRenderData` 增碰撞双缓冲、`ValidateTerrain`/`ResolveTransitionMaterials` 扩展、`CommitChunkStaging` 碰撞回落分支，MainGame/Tests 编译 0 错误。）
> 实施进度续2：2026-08-06（阶段 3 完成 3.3~3.7：`MountainGeometryBuilder` 增 solid 山体三角扇/山-山与山-普通 rect/3 山格 tri/诊断工具（`CellGeometry`、`MountainRectBuild`、退化三角、非流形边、几何 hash）；`ChunkMapRenderer` 集成替换式拓扑（solid/rect/tri 分槽、MountainIndices/CollisionIndices 双轨、山体 rect 独立缓存、山体槽稳定材质回落、碰撞终态高度恢复、OnDestroy 碰撞缓冲销毁）；`MountainGeometryTests` 增 3.3~3.8 单测。MainGame/Tests 编译 0 错误；Unity EditMode 回归与场景截图验收待项目锁释放。）
> 实施进度续3：2026-08-06（阶段 4.1 完成：新增 `MountainMaterialContract`（UV0 契约权威定义：ridgeKey01 / faceTier 编解码 / `IsValid` 边界校验 / Shader 回退路径常量）；`MountainConfigSO` 增"表现（阶段 4）"参数块（3 档色阶颜色、可选 1 张岩石纹理 × 染色、Triplanar world scale、blend sharpness、粗糙度/金属度/阴影强度）；`MountainGeometryBuilder` 三处 UV0 编码改走契约（行为零变化）；新增 `MountainMaterialContractTests` 与几何 UV0 契约集成测试。MainGame/Tests 编译与 EditMode 验证待项目锁释放。）
> 实施进度续4：2026-08-06（阶段 4.2 完成：新增 `Custom/MountainLowPoly_Fog` 稳定 Shader（Cull Off + FogBlend 契约 + UV0 tier 数据通道 + 4.2 纯色诊断输出，头部注释规划 4.3~4.6 演进）与 `MountainLowPoly_Fog.mat` 材质资产（Shader/材质 .meta 手写固定 GUID）；`ChunkMapRenderer` 新增 `GetOrCreateMountainMaterial`（克隆 config.stableMaterial 资产或 `Shader.Find` 一次，失败回落 `_terrainBaseMaterial0` 且只记录一次错误，OnDestroy 显式销毁）；`MountainConfigSO` 增 `stableMaterial` 共享资产引用；`MountainAssetSetup` 自动补齐材质资产与引用（幂等）。MainGame/Tests 编译与场景验收待项目锁释放。）
> 实施进度续5：2026-08-06（阶段 4.3~4.8 完成代码/资产落地：Shader 升级为稳定终态——4.3 Triplanar 世界空间三轴采样（`_ROCK_TEXTURE` 关键字，纯色模式零采样成本，abs() 防镜像反转、分母兜底）；4.4 `round/saturate` 解码 faceTier + 3 档色阶 + ridgeKey01 极轻亮度偏移；4.5 片元导数法线（插值几何法线校准朝向 + 退化面兜底；光照走逐面平面顶点法线——Surface shader 切空间约束的落地细化）；4.6 手动 ShadowCaster pass（Cull Off、无 clip）替代 addshadow；`GetOrCreateMountainMaterial` 增 `_ROCK_TEXTURE` 关键字/纹理接线；`MountainGeometryTests` 增 rect/tri UV0 契约测试；`MountainAssetSetup` 增诊断菜单 `Tools/程序化山脉/诊断山脉材质与配置`。Unity 侧编译、场景截图与雾化/阴影/生命周期/性能验收待项目锁释放。）
> 实施进度续6：2026-08-06（阶段 5.1 完成：新增 `MountainVertexAnimSource`（等权归一化 + `IsValid` 诊断，`CellGeometry.AnimSources` 与 `Weights` 平行填充）、`MountainTopologySignature`（HasMountain/顶点与索引数量/subMesh 布局 + indices 内容 FNV 摘要/有效山格集合摘要，无任何纯 Y 值；无山恒 `Empty` 零分配）、`ChunkRenderData.LastMountainTopology` 与 `CommitChunkStaging` 提交后写入；新增 `MountainTopologyTests`。MainGame/Tests 编译 0 错误；签名/来源纯函数 27 项独立校验全过；含格依赖与几何夹具用例待 Unity EditMode 运行。）
> 实施进度续7：2026-08-06（阶段 5.2~5.9 代码落地：5.2 规范化共享顶点动画来源（solid 环点/主峰 `[本格,1]`、rect profile `[owner:1-u,neighbor:u]`（格界锚点 0.5/0.5）、tri 从角 profile 按位置继承）；5.3 `AppendMountainAnimUV` 真实 UV2/UV3 通道替换恒等通道 + collision plain 顶点改走基础地形通道规则 + validator 有限值/delayEnd≥delayStart；5.4 `Custom/MountainLowPoly_Fog_Transition`（稳定契约 + keep-below clip，手动 ShadowCaster 同平面）+ `GetOrCreateMountainTransitionMaterial` 每 Renderer 单例/OnDestroy 销毁；5.5 `MapMutationService.Commit` 写前快照/写后比较 `VisibleCellSetHash`，拓扑变化整笔事务降级同步提交（日志单次、无锁/无 Ghost 残留）+ `MountainTopologyRouteTests`；5.6 同步提交 `ClearAnimationState`（缓存/clip MPB）+ `CommitSliced` 补 `ForceCompleteConflicting`；5.7 `ComputeConservativeAnimBounds` 动画保守 bounds（`TerrainGeometry.ConservativeBounds` + FillMeshData 覆盖）；5.8 `MountainVertexRanges` 碰撞索引回归校验。MainGame/Tests 编译 0 错误；独立校验 53 项全过；阶段 5 Unity EditMode 全量回归与场景截图验收待项目锁释放。）
> 实施进度续8：2026-08-06（阶段 6.1 完成：新增 `MountainVisibilityRule`（唯一入口 `IsPermanentlyVisible(cell)`，直接委托 `HasVisibleMountain`，与几何贡献同一有效性口径零漂移；类契约注明只参与视觉可见性合成——不调用 `ExploreBy`、不写 `IsExplored`/归属/探索费用、不伪造 `TemporaryVisibilityService` lease、结果与阵营无关、不改变玩法资格）；新增 `MountainVisibilityRuleTests`（普通格/有效山格/水淹/永久清除/恢复/无 ridge 数据/最小可见高度阈值边界（≥ 判定）/三类归属数据层不变/资格不变/纯函数无副作用/不伪造 lease，共 11 项）。MainGame/Tests 编译 0 错误；Unity EditMode 运行与阶段 6.2 resolver 接入待项目锁释放。）
> 实施进度续9：2026-08-06（阶段 6.2 完成：审计确认 `IMapVisibilityResolver` 唯一实现 = `TemporaryVisibilityService`（GameInstaller.cs:137 绑定），ChunkMapRenderer 全部雾目标刷新（初始生成 :222、地图变化 :171、后勤变化 :176、ArenaEventManager lease 获取/释放经 `ForceRefreshFogVisuals` :244/:375）均汇聚到 `UpdateFogTransitionTargets` → `IsVisibleToFaction`；在 `TemporaryVisibilityService.IsVisibleToFaction` 开头（lease 检查前）接入 `MountainVisibilityRule.IsPermanentlyVisible` 短路——单一合并点、每次刷新稳定生效、不写回探索状态、不创建 lease；清除/水淹自动回落，水→陆恢复重新生效；新增 `MountainVisibilityResolverTests`（9 项：强制可见短路后勤/双阵营视角/lease 获取释放无关/非山零回归/清除回落/水淹回落/恢复重生效/刷新幂等/不写探索位与归属）。MainGame/Tests 编译 0 错误；EditMode 回归与场景截图待项目锁释放。）
> 实施进度续10：2026-08-06（阶段 6.3 代码审计完成：①稳定/Transition 山体 Shader 均只用 `FogBlend.cginc` 世界 XZ mask——`FogBlend_vert`（MountainLowPoly_Fog.shader:84 / MountainLowPoly_Fog_Transition.shader:74）与 `fogFinal→FogBlend_final`（:139/:134），与 TerrainBase_Fog.shader:50/:70 同一契约，无其他雾采样分支；②不注册 `FogAffectedEnvironment`——`FogEnvironmentSelectiveEffect` 按 `Initialize` 传入 root 收集 Renderer（:82-85，root=地貌/资源/建筑/单位），Chunk 宿主只设 `Map` Layer（ChunkMapRenderer.cs:537-538/:832-837），山体为 Chunk mesh 的 subMesh 无独立 Renderer；山体材质（:2117-2157）不设置任何选择性雾化属性；③同一坐标/扰动契约——mask UV = `(worldPos.xz - origin)/size`，扰动 `FogBlend_warpUV` 由全局属性 `_FogPixelSize/_FogNoiseWavelength/_FogJaggedAmount` 驱动、逐片元世界位置决定，与 solid/rect/tri 几何类型无关；Transition 主 pass 同公式、ShadowCaster pass 无雾采样（阴影不雾化）且与 surf 同 clip 平面保证几何一致；④v1 限制确认：两 Shader 均无 worldY 高度感知分支，XZ 边界扰动为既有表现。固定 seed 雾边界截图验收待项目锁释放。）
> 实施进度续11：2026-08-06（阶段 6.4~6.9 代码落地：6.4 `HexHighlightRenderer` 门禁——新增纯函数 `IsBlockedByMountainGate`（玩家可见通道过滤 `IsEffectiveMountainCell`，DebugDirtyChunk 豁免）+ 显式诊断豁免入口 `SetHighlightedCellsDiagnostic`，整体替换语义覆盖动态恢复清理；6.5 调用方收口——`PlayerInputHandler` 拖牌预览走 `CanSpawnUnitOnCell`、`UIController.ShowReachableHexes` 攻击范围过滤有效山格、`MapHeightEditTestController` 改走诊断豁免；6.6 点击反查链路审计（MapRaycastService 先命中 Map 层 collision 再 `HexMapService.GetCellByWorldPosition` XZ 反查含半径上限，山体不进碰撞由阶段 3.7 保证，无代码改动）；6.7 镜头预算——`CameraController` 审计（bounds 按格中心 XZ + 最低 Y 平面、旋转中心射线 = Map 层（场景 m_Bits:64，InputCameraConfigurationTests 已断言）、H_max 不进预算为已知限制），`MountainAssetSetup.Diagnose` 增 H_max 超预算编辑器警告（maxHeight > minZoomDistance(20) - near clip(0.3)）；6.8 `CostLabelRenderer.RefreshLabels` 过滤有效山格（山格不显示探索费用标签）；6.9 测试聚合——新增 `MountainHighlightGateTests`（7 项：玩家通道全拦截/诊断豁免/普通格/水淹清除放行/水→陆重拦截/混合集合/多通道互不污染/null）与 `MountainStage6SourceContractTests`（6 项源码契约：门禁/豁免/资格函数/诊断入口/标签过滤/resolver 合成防回滚）。MainGame/Tests 编译 0 错误；PlayMode 运行时射线、镜头、截图验收与 EditMode 全量回归待项目锁释放。）
> 实施进度续12：2026-08-06（阶段 7.1 完成：Unity 编辑器侧编译基线建立——修复两处 Unity 编译错误：`MountainAssetSetup.cs` 61/69 行字符串插值内条件表达式缺括号（CS8076/CS8361，C# 全版本限制，dotnet 与 Unity 同报）与 `MountainVisibilityResolverTests.cs` 漏 `using System.Linq`（`IEnumerable<ICall>.Count()`，同文件其他测试均有该 using）；修复后 Unity 自动重编译全绿（MainGame/Assembly-CSharp-Editor/Tests 程序集成功重建、`LogAssemblyErrors (0ms)`、域重载成功、此后 0 error 0 warning）。Shader 导入检查通过：两个山脉 Shader 导入无 error/unsupported pass（日志无任何 shader 编译错误）。资产完整性验证：`Mountain.asset`（mountainForm=true、无模型/浮标/效果）、`MountainConfig.asset`（域重载后 `InitializeOnLoadMethod` 自动补齐 `stableMaterial` 引用，GUID 链 MapGenerationConfig→MountainConfig→Mountain.asset/MountainLowPoly_Fog.mat→MountainLowPoly_Fog.shader 全部闭环）、`MountainLowPoly_Fog.mat`（Shader 引用正确）。固定 seed（MapGenerationConfig：useFixedSeed=1、randomSeed=20260729）场景 PlayMode 验收与诊断菜单人工复核待用户在编辑器内执行。）
> 实施进度续13：2026-08-06（阶段 7.2/7.3 交织推进：①修复 `MountainGeometryTests` 夹具缺陷——`CreateFixtureCell` 的 `CenterWorldCoordinate.y=0` 与注释/断言基准"基础地形 Y=2"不一致（该几何夹具单测自 3.3 起从未在 Unity 运行，属首次运行即暴露的潜伏断言错误）；修复后 `SolidMountain_*`（世界高 = 2+隆起）、`MountainRect_MountainPlain_*`（格界锚点世界 Y=2）、`TriangleMountain_ThreeMountain_*`（交汇角点世界 Y=4=2+max(2,1.6,1)）、主峰高度（2+hA）断言口径统一；②阶段 7.3 新增 4 项确定性回归单测：`RidgeGeneratorTests.Generate_SameSeed_RepeatedRuns_IdenticalRidgePaths`（N=3 次脊线路径逐格/山格集合/固化参数一致）、`MountainGeometryTests.Determinism_RebuildAllGeometry_IdenticalHash_AndCollisionTrackUnchanged`（solid/rect/tri 全量 7 件重建 hash 一致 + 原始 solid 数组不被改写=collision 双轨逐位稳定）、`MountainTopologyTests.DynamicHeightChange_AnimatedEndState_EqualsDirectBuild`（拓扑不变 Height 变化：UV2.x=旧构建、UV2.y=直接构建，动画提交终态=直接构建）、`MountainTopologyTests.CrossChunkSharedEdge_RebuildSide_Uv0AndChannelsMatchExistingSide`（同脊线变体：共享位置/UV0.x/UV2/UV3 与另一侧一致 <1e-4）。MainGame/Tests dotnet 编译 0 错误；沙箱 harness 无法运行含 ScriptableObject 原生判定的用例（ECall 墙，RidgeGenerator/几何夹具均属此类），Unity EditMode 全量回归与固定 seed 场景验收待用户在编辑器内执行。）
> 实施进度续14：2026-08-06（阶段 7.4 代码落地：①新增 `MountainGeometryTests.FullMapTopologyScan_RenderAndCollisionTracks_NoDegenerateNoNonManifold`——18×18 网格 + 固定 seed 4 条脊线，复刻 Chunk 共享顶点数组 + 替换式双轨装配（plain solid/rect/tri + 山体 solid/rect/tri 全部归槽），渲染槽与 collision 槽分别扫描：无退化三角/无非流形边/索引不越界；collision 索引不得落入山体顶点区间（阶段 5.8 MountainVertexRanges 语义复刻）、山体渲染索引必须落在山体区间内；山-普通格界锚点 = 两端原始 solid 中点（决策 ④）；山-山/山-普通公共边数量前置断言；②新增 `OneOrTwoMountainJunction_CornerLiftIsZero_PlainTriFlatClosed`——1/2 山格三格交界角点隆起恒 0、plain tri 保持普通构建（闭合/平坦/无洞），补齐阶段 3.5 缺失的 1/2 山格单测；③新增 `MountainStage7SourceContractTests`（6 项源码契约：双轨分离/无山回落/MountainVertexRanges 校验/末尾山体槽/替换式路由条件/诊断工具保留/边闭合容差）；④沙箱独立校验（8 项）：plain 全图装配（18×18、578 个交界 tri、两种 apex 路由 + 去重）无退化/非流形/越界，NEE（apex A）与 ESE（apex B）角点逐一吻合——期间修复测试自身两处路由错误：(E,SE) 中间 rect 应取 (c+SE, NE)（连接两邻居的边归 SE 侧邻居的 NE rect），先误写为 (c+E, NE)；sandbox 先误用 GetHashCode 作字典键（向量哈希碰撞致三份 rect 同源）。MainGame/Tests dotnet 编译 0 错误；Unity EditMode 全量回归与 Chunk 边界截图验收待用户在编辑器内执行。）
> 实施进度续15：2026-08-06（阶段 7.5/7.6 交织推进：①7.6 部署入口收口审计——发现并修复 5 处"山格可部署单位"缺口：`CardPresenter.IsReleaseValid`（玩家卡牌确认路径，放置预览之外的第二道闸，补 `CanSpawnUnitOnCell`/`CanBuildOnCell`）、`AICardBrain.IsValidSpawnCellForUnit/ForBuilding`（AI 卡牌，单位分支此前只查水/建筑/单位/归属，不查山格）、`BarracksSpawner.FindAdjacentEmptyHex`（兵营生产出口格，玩家 + AI）、`AIAutoExplorer.SpawnRewardUnits/FindOverflowCell`（AI 探索奖励）、`ExplorationRewardSystem.SpawnUnitsWithOverflow/FindOverflowCell`（玩家探索奖励）；寻路侧审计确认天然安全（`UnitMovementSystem.CanEnterCell` 拒绝 `movementCost == MaxValue`，路径不穿山、山格终点不可达）；②新增测试：`MountainTopologyRouteTests.Commit_AnimatedHeightOnMountain_ThenClear_ForceCompletesAndSyncs`（7.5 动画中冲突提交——纯 Height 动画进行中清除山格 ⇒ ForceCompleteConflicting 收尾旧动画 + 整笔事务同步降级 + 无锁残留 + movementCost 回落 1 + 无 TransitionStarted）、`UnitMovementSystemTests.Pathfinding_MountainCells_BlockRouteAndDestination`（7.6 寻路——山墙封死 3 邻 ⇒ 目标不可达；山格作为终点 ⇒ 拒绝）、`MountainStage7SourceContractTests` 增 5 项部署入口源码契约（防回滚）。MainGame/Tests dotnet 编译 0 错误；7.5/7.6 的 Unity 运行期项（动画视觉/ghost/点击/镜头）待用户在编辑器内执行。）
> 实施进度续16：2026-08-06（PlayMode 首跑即捕获真实 bug 并修复——`MountainVertexRanges` validator 首次在运行时生效：初始生成 Chunk(0,0)/(0,1)/(0,2) 与竞技场清除 8 格重建均报 "Terrain collision index 引用了仅山体渲染顶点"。根因 = `ChunkMapRenderer.BuildChunkTerrain` 3 山格 tri 分支（:1259 原）的 plain 封口 collision 索引用 `i + IndexOffset`——`IndexOffset` 在 :1229 捕获于山体 tri 追加之前，而 plain tri 顶点在 :1255 追加于山体 tri 之后，collision 索引打入山体顶点区间；7.4 复刻测试因装配顺序与真实代码不同（复刻先加 plain 后加山体）而掩盖了该 bug。修复：plain 封口改用 `int plainOffset = verticesList.Count`（山体 tri 追加后捕获）再偏移；逐一审计全部 7 处 `collisionIndices.Add`（solid/河 rect/普通 rect/贴山 rect/3 山格 tri/普通 tri 其余 6 处偏移均正确）；7.4 复刻测试改为与真实代码相同装配顺序（山体 tri 先、plain 后）；新增源码契约 `ChunkMapRenderer_CollisionPlainTri_UsesFreshOffset` 锁定修复。validator 本身工作正确（正是设计用途），该 bug 暴露了"复刻测试装配顺序 ≠ 真实装配顺序"的盲区。MainGame/Tests 编译 0 错误；待用户重新进入 PlayMode 复验初始地图与竞技场流程。）
> 实施进度续17：2026-08-06（场景截图视觉验收**不通过**并修复——山体呈平顶台地（"平顶峰"）且平面形态呈圆形"酱饼"，两处根因与决策修订：**①角点规则 max → 均值（决策 ④ 细化规则修订）**。原规则"3 格全有效山 ⇒ 角点取 max(hA,hB,hC)"会把覆盖区内部每个交汇角点抬到局部最大山高，而 solid 扇面主峰 = 本格山高恒 ≤ 环角点 ⇒ 任何格都无法形成局部高点，§2.2 设计的"脊高坡低"高度场在几何阶段被完全抹平 → 整条山脉变成平顶台地。均值同样满足规范化共享（对称/确定/跨 Chunk 一致），且保留高度场梯度：脊线格主峰重新成脊、坡面格成坡。`MountainGeometryBuilder.CornerHeight` 改均值，`MountainGeometryTests` 三处数值断言同步（交汇角点 2+4.6/3、镜像边点 lerp(0,4.6/3,u)、tri 顶点 2+4.6/3），新增 `CornerHeight_ThreeVisibleMountains_ReturnsMean_PeakStandsAboveRim` 防平顶峰回归。**②平坦区游走加转向权重（决策 ⑰ 修订）**。原 `PickNext` 平坦分支为均匀随机游走（无任何转向惩罚），平地脊线蜷缩成团，再经宽度化膨胀成圆形酱饼；改为按 `exp(-scoreTurnPenalty × 转向量)` 加权（优先直行/缓弯，纯函数 `ComputeFlatWalkWeight` 暴露供单测），转向惩罚 = 0 时退化为均匀（兼容旧行为）。新增 `FlatWalkWeight_PrefersStraight_AndDecreasesWithTurn` 与行为级 `Generate_FlatTerrain_RidgeStaysElongated`（端点距离 ≥ 路径长度/2）。MainGame/Tests dotnet 编译 0 错误；EditMode 回归与重新截图验收待执行。）
> 实施进度续18：2026-08-06（山脉高度暴露到地图设置 SO——视觉验收续：修复平顶峰/酱饼后用户反馈"高度还不够高"。新增 `MapGenerationConfigSO.mountainHeightScale`（程序化山脉块，默认 1，Min 0.01）：`RidgeGenerator.Generate` 增可选参数 `heightScale`，`WriteRidgeData` 中 `hMax = clamp(baseH + k·(len−minLen), minH, maxH) × heightScale`（clamp 后缩放，允许突破 MountainConfig.maxHeight）；缩放不消耗随机流 ⇒ 同 seed 脊线路径/宽度化/山格集合不变（决策 ㉓ 保持）；只影响视觉隆起，不改格级 Height 与玩法规则。`MapGenerator` 注入 `_config.mountainHeightScale`；`MapGenerationConfig.asset` 初值设为 2（有效 H_max 上限 = 2.5×2 = 5，仍远低于镜头预算 19.7）；`MountainAssetSetup.Diagnose` 镜头预算诊断改按"有效 H_max 上限 = maxHeight × scale"判定与提示；新增 `RidgeGeneratorTests.Generate_HeightScale_MultipliesHMax_PathUnchanged`（hMax×2、路径/长度/seed 不变）。MainGame/Tests dotnet 编译 0 错误；EditMode 回归与重新截图验收待执行。注意：maxSlope 快照字段不随缩放（它是"防陡薄片"约束目标而非生成输入；缩放过大时用户需自行权衡坡度观感）。）
> 实施进度续19：2026-08-06（阶段 7.7/7.8/7.9 代码侧落地：①7.7 新增 `MountainStage7VisualContractTests`（12 项视觉契约：双 Shader 可查找/稳定 Shader 色阶与 Triplanar 属性声明/Transition keep-below clip 与 ShadowCaster 同平面/稳定态无 clip/单一 FogBlend 无选择性雾化/Triplanar 世界坐标与 NaN 兜底/手动 ShadowCaster 双面/材质参数推送与 `_ROCK_TEXTURE` 关键字/Transition 克隆后关键字恢复/每 Renderer 单实例与 OnDestroy 销毁/配置资产三档色阶互异不透明/稳定材质资产 Shader 引用与默认纯色模式）；`MountainAssetSetup.Diagnose` 增 Transition Shader 存在性与视觉契约摘要（三档色阶/纯色 vs 纹理模式）；②7.8 新增 `ChunkMapRenderer` 静态性能钩子（`EnableChunkBuildTiming` 默认关闭零开销：Chunk 构建耗时按含山/无山分类、`CollisionCommitCount` cooking 次数、`AnimProgressFrameMsTotal/Max` CPU 逐顶点写入帧耗时）+ `DebugChunks` 只读诊断枚举 + `MountainGeometryBuilder.SolidMountainFanVertexCount=54` 预算常量 + 新增 `MountainPerformanceBaseline` 编辑器工具（`Tools/程序化山脉/性能基线（自动进入 PlayMode）`：自动进 PlayMode 等地图生成稳定后采集构建耗时/cooking/动画帧/mesh 面数/材质实例并写 `Temp/mountain_perf_report.txt` 后自动退出）+ `MountainStage7PerformanceContractTests`（5 项源码契约）；③7.9 文档侧验收报告（决策 ㉛ 清单状态表 + 已知限制 + 放行决策）见文末"阶段 7 验收记录"。MainGame/Tests/Assembly-CSharp-Editor dotnet 编译 0 错误；Unity EditMode 运行与 7.7/7.8 运行期采集待用户在编辑器内执行。）
> 实施进度续20：2026-08-06（角点规则第 3 次修订：均值 → **脊线连续**——场景截图验收反馈"不应是几个尖顶山峰，而应是一条山脊"。根因：纯均值把脊线格之间的公共角点压到"含坡面格的三格均值"（如 (2+1.6+1)/3），相邻主峰之间形成深谷，截面锯齿化为"每格一个独立尖峰"。修订：3 格全有效山且存在**脊线相邻对**（同一脊线的两个脊线格且路径相邻，|ΔmountainPosAlongRidge|=1，新增 `MountainGeometryBuilder.IsRidgeConsecutive` 规范化判定，ridgeId+整数索引，与遍历顺序无关）时，角点 = 各相邻对两格山高均值的最大值；无相邻对回落三格均值；含普通格仍恒 0（决策 ④ 固定锚点不变）。效果：脊线格公共边点/rect 保持脊线高度，各主峰由折线状连续山脊连接；角点介于两峰之间（垭口单调缓降、最高峰仍突出，续17 防平顶性质保持）；拐弯交汇多对取 max（绕弯不降）；急转弯自触（|Δs|≥2 空间相邻）不架脊桥。`MountainGeometryTests` 增 3 项（`CornerHeight_RidgeConsecutivePair_ReturnsCrestPairMean`——含脊线边带镜像、`CornerHeight_RidgeBendJunction_TakesMaxPairMean`、`CornerHeight_NonConsecutiveSameRidge_FallsBackToMean`）；`BuildTriangleMountain` 注释同步；既有夹具测试（A/B/C 故意不同 ridgeId）全部走均值路径不受影响。MainGame/Tests dotnet 编译 0 错误；沙箱独立校验 15 项全过（真实 `IsRidgeConsecutive` 全边界 + 三种交汇情形复算：相邻对 1.8 > 旧均值 1.533、介于两峰之间、拐弯 max、自触回落均值）；EditMode 全量回归与重新截图验收待用户在编辑器内执行——`Tools/程序化山脉/运行山脉测试（EditMode，结果写 Temp）` 已补批处理入口 `ExecuteForBatch` 与续19 两个新测试类。）
> 实施进度续21：2026-08-07（调试对照生成模式——需求："绕过已有生成规律（不删除相关代码），同图生成 1 个山脉地块 + 1 条直的山脊供对比"。新增 `MountainConfigSO.debugSingleCellAndStraightRidge`（默认 false）与 `debugStraightRidgeLength`（默认 8）；`RidgeGenerator.Generate` 在空值检查后分流到 `GenerateDebugComparison`（ridgeCount/起点禁区/评分行走全部不生效，正常代码路径一行未删、取消勾选即恢复）：A = 单格脊线（无坡面格的孤立山脉地块），B = 固定方向直线行走 length 格；**2026-08-07 后续语义修正**：调试直脊线同样跳过 `WidenMountain`，因此 `Debug Straight Ridge Length=n` 时直脊线严格只占 n 个山格（总山格数 = 单格对照 1 + n），不再附加两侧坡面格；正常生成模式仍保留宽度化。B 起点距 A ≥ length+3 格六边形距离保证同图分离；锚点/方向扫描均消耗传入随机流（同 seed 可复现），数据固化/几何/玩法规则继续复用 `WriteRidgeData`。`RidgeGeneratorTests.Generate_DebugComparison_SingleCellPlusStraightRidge` 锁定：恰好 2 条记录/单格无坡面方向字段空/直脊线方向恒定/`mountainCellCount==length==n`/无 SlopeCell/总山格数 `n+1`/两山分离/同 seed 一致。`MountainConfig.asset` 已勾选开关（length=8）供 PlayMode 直接对比。MainGame/Tests dotnet 编译 0 错误；EditMode 运行与对照截图验收待用户在编辑器内执行。）
> 实施进度续22：2026-08-07（**山-普通 rect 格界劈半**（决策 ④ 细化）——场景对照验收发现"每个山脉地块连面带料占据全部相邻 rect"：rect 是横跨两格的整体网格件，edgeMountain 任一侧是山即整条替换进山体槽，普通半边虽高度不变但披山体材质（岩褐色阶、无地形纹理/格线），视觉上每个山格向外多吃一圈（0.3×内切圆），是"山脊扩大一圈"的最直接来源。修复：`BuildMountainRectData` 山-普通分支在格界劈成两件——**山侧半边**（山格环点 → 格界锚点，带坡度折面，进山体槽；UV.y ∈ [0,0.5] 或 [0.5,1] 按山格是 owner/neighbor 取向）与**普通半边**（格界 → 普通环点，恒 0 隆起，`MountainRectBuild.PlainRect` 产出）；`ChunkMapRenderer` 把 PlainRect 按 RectFlat  shadingStyle 走 `ToFlatShaded` 后归入 rectGroups (matA,matB) 地形材质分组 + `AppendRectAnimUV` 普通通道——山体视觉边界收回到格界线，两件在格界点严格闭合（同一 boundary 点位），动画权重/材质混合坐标与原整面 rect 半段逐点一致（v 区间保留）。山-山 rect 不变（整面山体，PlainRect=null）；collision 仍用原始整条 rect（双轨不变）；MountainVertexRanges 校验只查 collision 索引，不受影响。测试：`MountainRect_MountainPlain_SplitAtBoundary_PlainHalfReturned`（重写原 ProfilesLength3：两件长度 2、锚点 0 隆起、格界闭合、UV 区间、各 6 三角无退化）、`MountainRect_NeighborMountain_SplitPicksNeighborHalf`（neighbor 为山的取向：Rect=neighbor 半边/lifts [0,endLift]/UV 区间）；`FullMapTopologyScan` 复刻装配同步（取向感知锚点断言 + 普通半边进渲染轨）；`MountainStage7SourceContractTests` 增 `ChunkMapRenderer_MountainPlainRectSplit_TerrainHalfRouting`（5 项源码契约防回滚）。MainGame/Tests dotnet 编译 0 错误；EditMode 全量回归与对照截图验收（山体收回格界后的山脚观感）待用户在编辑器内执行。）
> 实施进度续23：2026-08-07（**无宽度化直脊线的 rect 脊桥**——续21 将调试直脊线改为严格占 n 格后，场景验收暴露相邻脊线格仍呈独立尖峰：两格公共边的两个 corner 都各自与第三个普通/越界格交汇，按决策 ④ 恒 0；原 `EdgePointLift` 对所有山-山边均从两端 corner 插值，故两个内部 edge points 也为 0，mountain-mountain rect 整面贴地，无法连接峰体。修复：`EdgePointLift` 对 `IsRidgeConsecutive(cell, neighbor)` 的连续脊线对特判——两个内部边点直接取 `(hCell+hNeighbor)/2`；两端 corner 继续保持 0（不抬侧邻普通格），形成“中央高、两端落地”的窄脊桥，既连接相邻峰体又不新增山格/不向两侧扩张。`BuildSolidMountain` 环边点统一委托 `EdgePointLift`，消除 builder 内重复逻辑与规则漂移；规则对两格对称、跨 Chunk 一致。更新既有 `CornerHeight_RidgeConsecutivePair_ReturnsCrestPairMean` 边带断言（两个 edge points 均 = pair mean），新增 `RidgeConsecutiveEdge_NoFlankMountains_RectStillFormsCrestBridge`：移除全部侧邻山格后断言两端 corner=0、内部边点=pair mean、A/B 两侧 4 条 rect profiles 镜像一致（角 profile 0/边 profile crest）、整面无退化三角。MainGame/Tests 编译 0 错误；PlayMode 重新截图验收待执行。）
> 实施进度续24：2026-08-07（**连续脊边的 tri 视觉肩部**——用户澄清：不是把 tri 变成山格/山脉地块，而是把相邻 tri 靠脊边的一小部分作为脊边视觉延伸，第三格保持普通。实现：①连续脊线 rect（`IsRidgeConsecutive(owner,neighbor)`）4 条 profile 升级为 3 点线；两个边 profile 中点保持 pair mean 主脊高，两个角 profile 在原 corner=0 时仅把中点抬至 `pairMean×0.42`，提供与 tri 共享的肩点；端点仍为 0，不改变普通格边界。②新增 `BuildRidgeEdgeTriangleShoulder(owner,A,B,plainTri)`：仅当三格中恰有一对同 ridgeId/|Δs|=1 的连续 RidgeCell 且第三格非有效山时返回山体 wedge；从 plain tri 的重心 UV 识别脊边两端/第三格角，脊边中点高度复用 rect 角 profile 的 `pairMean×0.42`，向第三格方向内缩 `0.38` 后回落至原 terrain，3 个 flat 三角（9 顶点）进山体槽；原 terrain tri 完整保留在地形槽/collision，第三格不写 landForm/ridgeStatus/movementCost，`Debug Straight Ridge Length=n` 仍严格占 n 格。③动画来源：脊边端点分别跟随两脊线格，中点 0.5/0.5；肩部外沿分别按 ridge0/third、ridge1/third 的 0.38 插值，静态/动画都与 rect/terrain 连续。④`ChunkMapRenderer` 在非 allMountain tri 分支保留原 tri 装配后叠加 shoulder 到 `mountainIndices`/`mountainRanges`，绝不进 collision。测试：新增 `RidgeEdgeTriangleShoulder_UsesConsecutiveEdge_ThirdCellRemainsPlain`（9 顶点/无退化/来源合法/第三格数据不变/shoulder 高点逐点等于 rect 相邻角 profile 中点/低于主峰）、`RidgeEdgeTriangleShoulder_NonConsecutiveOrThirdMountain_ReturnsNull`；FullMapTopologyScan 复刻 shoulder 路由；源码契约 `ChunkMapRenderer_RidgeEdgeTriangleShoulder_VisualOnlyRouting` 锁定连续脊边触发/第三格普通/只进山体槽不进 collision。MainGame/Tests 编译 0 错误；Unity EditMode 与 PlayMode 截图验收待执行。）
> 实施进度续25：2026-08-07（**山-普通 rect 山脚材质融合**——用户确认“爪子”不仅是几何楔形，更是山体 Shader 到地形 Shader 在格界处硬切。实现：山侧半 rect 按普通侧地形材质分组进入独立 boundary subMesh；新增 `TerrainGeometry.MountainBoundaryMaterials` 与 `UV4s`，UV4=`(原 rect UV.x, 原 rect UV.y, mountainBlend, 0)`，山侧环点 blend=1、格界 blend=0，普通半 rect继续使用原 Terrain shader。`MountainLowPoly_Fog`/Transition 增 `_MOUNTAIN_TERRAIN_BLEND` 变体：保留岩石 Triplanar，采样邻接地形 `_TerrainTex` 并在同一 Opaque pass 内 `lerp(terrain, mountain, blend)`；应用邻接地形 tiling/offset、颜色与光滑度，雾和动画契约不变。主山体槽仍统一使用原材质；山脚槽按普通侧材质实例缓存，collision 仍原始整条 rect；`HasMountain` 扩为主山体或 boundary 任一存在，`HasMainMountainSlot` 单独控制末尾主山体槽，避免 Chunk 边界槽位漂移。`MountainGeometryBuilder.RectToTerrainBlendRender` 与 UV4 通道校验/`MountainRect_TerrainBlendData_MountainSideOneBoundaryZero_AndUvPreserved` 锁定山侧1→格界0及取向；源码契约锁定 boundary 分组、UV4、shader 变体和材质接线。MainGame/Tests 编译 0 错误；Unity Shader d3d11 变体导入、EditMode 与 PlayMode 截图验收待执行。）
> 实施规划续：2026-08-06（阶段 6 按交互调用链细化为 6.1~6.9：可见性契约、resolver 接入、FogBlend 边界、高亮统一门禁、高亮调用方收口、点击反查、镜头预算、标签/Overlay、诊断验收；每步均补充独立完成标准。）
> 实施规划续2：2026-08-06（阶段 7 按验收闭环细化为 7.1~7.9：编译/资产基线 → EditMode 全量回归 → 固定 seed 确定性重建 → 拓扑验收 → 生命周期验收 → 玩法与交互回归 → 表现与视觉验收 → 性能与资源生命周期 → 已知限制与放行；每步均补充独立完成标准，最终 v1 放行 = 决策 ㉛ 全过 + 各步完成标准聚合。）
> 实施规划：2026-08-06（阶段 3 按源码审计事实细化为 3.1~3.8 八个子步骤；补充：固定锚点的网格学解释（真正格界在 rect 区域中点）、tri 角点"3 格全山才隆起、否则恒 0"规则、`MountainRidgeData` 快照字段缺口（ridgeNoiseAmplitude/cellNoiseScale）、碰撞网格共享顶点数组双轨方案、贴山 rect 恒非河的简化依据。）
> 关联：`动态地图/动态地图变化系统-使用报告.md`（动态地图管线）、`资源与地貌选择性雾化方案.md`（地貌雾化）、`Assets/Scripts/Managers/MapPresentationBootstrap.cs`（地貌实例化）、`Assets/Scripts/Core/Services/MapMutation/MapMutationService.cs`（水陆双向重置）、`Assets/Scripts/Managers/RiverGenerator.cs`（山脊走向算法借鉴）、`Assets/Scripts/Managers/ChunkMapRenderer.cs`（路线 A 改造点）。
> 补充关联（2026-08-06 讨论补充）：`Assets/Scripts/Controllers/BuildingController.cs`（放置写回 movementCost=1）、`Assets/Scripts/Managers/HexHighlightRenderer.cs`（高亮遮挡）、`Assets/Scripts/Core/Services/LandFormClusterSpawnRule.cs`（簇机制单簇限制）、`Assets/Scripts/Core/Services/HexMapService.cs`（点击反查格）、`Assets/Scripts/Controllers/CameraController.cs`（镜头边界预算）。
> 前提澄清：**山脉不是"高度很高的地块"**（那只是调噪声参数就有的高海拔），而是叠加在地块之上的程序化隆起——**山脉是跨格的连续隆起高度场**，不改变格级 `Height`。
> 风格硬约束：**山脉必须是 Low-poly 风格**（稀疏控制点、大块平面三角、平面法线、离散色阶），构造时**保留山体覆盖区外边界（山-非山公共边）的原始地块点不变**作为固定锚点（2026-08-06 决策 ④，见 §3.3.1），保证与相邻正常地块无缝衔接。

---

# 现状盘点（三主题共用的事实基础）

| 环节 | 现状 | 归属主题 |
|---|---|---|
| 数据 | `HexCellData.landForm`（SO 引用，MapLandFormSO.cs） | 生成规律 |
| 模型实例化 | `MapPresentationBootstrap.InstantiateLandForms`（:80-97）逐格 `Instantiate(modelPrefab)`，挂 `ModelController`，**Layer=`FogAffectedEnvironment`** | 网格构造（路线 B） |
| 簇生成 | `LandFormClusterSpawnRule`（金矿扎堆 BFS 生长；**当前只处理数据库中第一个 `clusterSpawn` 地貌，且资格判断不排除已有 `landForm`**） | 生成规律 |
| 生命周期 | `RefreshCellObjects`（ChunkMapRenderer.cs:2131-2158）：变化格模型移除/归位 | 作用（动态地图交互） |
| 动画跟随 | `MapVisualTransitionService.cs:359-360`：`RegisterVisualFollower(landFormModel.transform, cell)` | 网格构造（动画通道） |
| 雾化 | `FogAffectedEnvironment` 层 + `FogEnvironmentSelectiveEffect` 选择性雾化 | 作用（探索表现） |
| 规则 | `blockBuildingSpawn`（防建筑）、`movementCost`（寻路消耗） | 作用（玩法语义） |

---

# 源码审计修正（2026-08-06）

本节区分三种状态：**当前事实**（源码已经如此）、**目标决策**（已确认但未实现）、**实施契约**（若不补齐会产生错误行为）。后文“已确认”均指目标决策，不等于现有代码天然支持。

## A. 必须修正的现状判断

1. **当前生成顺序仍是地形 → 河 → 地貌 → 资源**（`MapGenerator.cs:115-121`），不是目标顺序。`RiverGenerator` 的源头与流向只排除水域/已有河流，不检查 `landForm`（`:99-108`、`:270-319`）。目标顺序落地时必须同时修改河源和每一步流向资格，单纯移动调用顺序不够。
2. **当前簇规则不是“零侵入多簇机制”**。`FindClusterForm` 返回第一个 `clusterSpawn` 地貌（`LandFormClusterSpawnRule.cs:23-33`），`MapGenerator` 也只执行一次（`:253-263`）；`SelectCenters/GrowCluster` 不排除已有 `landForm`。山与金矿并存前必须升级为有确定顺序、共享占用集合的多簇遍历。
3. **当前散落地貌不会跳过已生成山格**。`LandFormDataGeneration` 会直接覆盖 `cell.landForm`（`MapGenerator.cs:239-251`）；只有资源生成已有 `landForm != null` 跳过（`:266-280`）。山先生成后，其他地貌 pass 必须显式跳过已有山格。
4. **`movementCost = float.MaxValue` 只保证寻路不可进入，不保证不可部署单位**。玩家卡牌、AI 卡牌、兵营等部署入口并不统一检查 `movementCost`。应新增统一的格级资格函数（如 `CanSpawnUnitOnCell/CanBuildOnCell/CanEnterCell`）并接入所有入口；`blockBuildingSpawn` 只阻止建筑。
5. **`ClearLandForm` 缺少 `Terrain` 脏位不是当前“不重建 Chunk”的直接原因**。当前 `Commit/CommitSliced` 对 changed cells 始终准备并提交 Chunk 几何（`MapMutationService.cs:191-202`、`:345-403`），未按 dirty flag 跳过 Terrain。仍应把程序化山体清除标为 `Terrain | Objects`，用于事件语义和未来按脏位分流，但不能描述成当前必现的山体残留 bug。
6. **现有 Terrain builder 不会跳过水格**，它仍为水域生成海床 solid/rect/tri；未来只能跳过“山体贡献”，不能跳过基础地形。有效山体判定应统一为 `isMountain && !IsWater && visibleHeightPassed`，并用于 solid、rect、tri 三层。
7. **`IsExplored` 不是视觉免雾开关**，而是阵营探索/领地流程的一部分（`HexCellData` 位掩码；`ExplorationService` 会据此判定 AlreadyExplored 并取得归属）。把山格预置为玩家已探索会阻止玩家正常开发，却不能阻止 AI 探索；归属后又受后勤连通性影响。山格永久免雾必须在 visibility resolver/FogAlpha 目标计算中增加独立规则，不能复用 `ExploreBy(0)` 或直接写一次 `FogAlphaTarget=1`（后者会被刷新覆盖）。
8. **高亮不会天然排除山格**。`HexHighlightRenderer` 接受任意格并在基础地表重建高亮；放置预览和攻击范围的调用方也不都检查山体语义。v1 若不做山顶高亮，必须在各高亮来源统一过滤山格，否则高亮会埋在山体内或被遮挡。
9. **地图点击仍依赖物理射线**。`MapRaycastService` 先射线命中 `Map` 层碰撞体，再用命中点 XZ 反查最近中心；XZ 反查还有格半径上限（`HexMapService.cs:85-107`）。因此“不让山体参与碰撞”不等于“不需要碰撞网格”，且侧坡归属只能视为近似策略。

## B. 路线 A 新增的强制实施契约

1. **独立碰撞网格**：当前 `TerrainFilter.sharedMesh` 与 `TerrainCollider.sharedMesh` 指向同一个 mesh（`ChunkMapRenderer.cs:1510-1517`），Unity 不能按 subMesh 排除山体。决策 ㉚要求新增一份保留原始基础 solid/rect/tri 的 collision mesh；渲染 mesh 使用山体替换拓扑，碰撞 mesh 保持无洞基础表面。碰撞仍供点击、拖放和镜头旋转枢轴射线使用。
2. **山体槽不止三个改点**：还需扩展 `TerrainGeometry`、山体 index 构建与槽存在标记、稳定/Transition 材质缓存及销毁、校验器、可选颜色通道、碰撞 DTO 和测试。`hasMountain` 应以“该 Chunk 实际产出非空山体 indices”为准，不能只看残留 `landForm`。
3. **rect profile 是 tri 的输入**：当前 tri 由三条 rect profile 派生，profile 又在 Chunk + halo 阶段预构建（`PreBuildRectProfiles` / `RectangleDrivenTriangleMesh`）。山体 rect 边界必须先进入 profile/halo 契约，再由其生成 tri 封口；不能在最后独立抬高 tri 顶点。
4. **共享边动画也必须确定性**：静态位置一致不足以防动画裂缝。重复/拆分顶点还必须得到相同的 UV2 起终高度和 UV3 delay window；规范化共享边函数需同时返回格权重或规范化 owner，用同一规则生成位置与动画通道。
5. **拓扑增删不是免费高度动画**：UV2/UV3 只适用于山形不变、基础 `Height` 改变。清除山体或水淹会删除拓扑；上升路径的 `TerrainGhost` 会让旧山体保留到结束后突消，下降路径可能提交时立即消失。需要明确采用“拓扑变化禁用动画”、山体专用淡出/clip，或独立山体过渡方案。
6. **预扩 bounds**：当前 mesh 只在提交时 `RecalculateBounds`，CPU 动画逐帧改 vertices 不更新 bounds。高山和抬升动画需按 start/target/最大山高预设保守 bounds，避免视锥与阴影剔除山峰。
7. **导数法线不是零改造替换**：专属 shader 要正确处理 world/tangent 空间转换、`Cull Off` 背面朝向、ShadowCaster normal offset、Triplanar 权重和动画期间法线。按 `worldY` 分段还可能在单个三角面内切出色带；若要求“一面一色阶”，应存 face tier（拆点后三顶点同值）或仅按面法线离散分档。
8. **验收工具需新增**：现有 staging 校验只覆盖通道长度、有限数值、索引范围和三角索引计数，不检查共享边误差、退化三角、非流形或固定 seed hash。决策 ㉛需对应编辑器测试/运行时诊断实现。

## C. 交互与表现遗漏

- **水→陆恢复**：残留山 `landForm` 会使几何重新出现，但 `MapMutationService` 默认把 `movementCost` 重置为 1；必须在状态变化后重新派生山格规则，或把移动力改为基础地形与覆盖规则的组合计算。
- **永久免雾**：增加山格专属 visibility 规则，并明确它是否只免视觉雾、是否允许探索/占领、是否参与后勤。当前后勤遍历只看归属，不看 `movementCost`，山格若可被占领会成为跨山后勤链路。
- **单位部署**：除寻路外，覆盖玩家/AI 卡牌、兵营、AI 工厂及其他直接生成入口；动态变成山格时继续使用现有取消路径与弹射流程。
- **镜头与遮挡**：镜头边界按基础格中心 XZ 与最低 Y 平面计算，山高不会进入预算；旋转中心又取 Map 层射线命中点。需验收近裁剪、峰顶遮挡、边缘山峰、旋转枢轴，以及费用标签、Overlay marker、选择反馈、单位/建筑/河流/格线遮挡。
- **路线 B 生命周期**：仅设置 `FogAffectedEnvironment` Layer 不会注册选择性雾化；该后处理按已注册 root 收集 Renderer。簇级程序对象还需要创建/替换/销毁、root 注册、`RefreshRenderers` 和多格动画跟随，现有逐格 `landFormModel` follower 不足。

---

# 主题一：山脉在游戏中的作用

## 1.1 概念定位

| 项 | 定义 |
|---|---|
| 山脉 | 基础地形之上叠加的隆起高度场（`mountainHeight`），覆盖 solid 顶面 / rect 过渡面 / 必要的 tri 角点，外缘回落至地形表面；`finalY = terrainY + mountainHeight` |
| 粒度 | 首版直接为脊线山脉（决策 ⑯）：一条山脉 = 脊线 + 宽度化格，不做 v1 单格山 |
| 山脉地块 | 山脉的基本管理单元（一个六边形格），负责本格 solid 顶面 + 相邻 rect 过渡面的隆起（决策 ⑯，见 §3.4） |
| 性质 | 纯视觉构造物 + 可选的规则语义（防建筑/移动惩罚/可交互），不改变格级 `Height`；**Low-poly 风格**（见主题三 §3.3） |
| 定位 | 地貌体系（landForm）的程序化扩展，或独立体系（`terrainType`，已空置） |

## 1.2 玩法规则语义

- 山与金矿/森林同属"地貌型"载体，但**网格运行时生成而非预制体**——规则语义（防建筑、寻路消耗、可交互）挂在格级数据上，与渲染路线无关
- **已确认（2026-08-06 决策 ①）：山格不可通行、不可建造、不可部署单位**——`blockBuildingSpawn = true` 只负责建筑；`movementCost = float.MaxValue` 负责寻路。单位部署入口需要统一资格校验，不能依赖这两个字段自动覆盖全部入口。水→陆重置会把 `movementCost` 改回 1（MapMutationService.cs:491-492），山格规则需在状态变化后重新派生
- **已确认（2026-08-06）：山格不在水域格上生成**——初始山生成资格排除水域；运行期水淹时，未来 mountain builder 必须在 solid/rect/tri 贡献判定中显式忽略水格山体，基础海床仍保留。若 `landForm` 残留，则恢复陆地后山体会重新出现，必须同时恢复不可通行规则
- **已确认（2026-08-06）：点击归属维持“物理射线 + XZ 最近中心”近似反查**——不做三角面格 ID；但不能保证所有侧坡命中均归山格，且 XZ 反查有半径上限。决策 ㉚采用无山体独立 collision mesh 后，射线命中山下基础面，再按 XZ 反查
- **已确认（2026-08-06）：v1 不做山顶专属高亮**——现有高亮来源必须过滤山格；`HexHighlightRenderer` 本身不会按 `movementCost/blockBuildingSpawn` 过滤，否则基础地表高亮可能埋入山体或被遮挡
- **已确认（2026-08-06）：不参与探索提示**——山格无 `markerPrefab`，`LandFormMarkerManager` 跳过（:121），无浮标
- **已确认（2026-08-06）：整个山格地块不被迷雾遮盖**——通过 visibility resolver 增加“有效山格永久可见”的专属规则实现，只改变视觉雾，不写 `IsExplored`、不改变归属/探索成本/后勤。路线 A 山体仍按世界 XZ 采样迷雾，rect/tri 边缘受邻格 mask 与扰动影响，需截图验证覆盖边界
- **已确认（2026-08-06）：山格优先生成，山格上不生成其他地貌**——目标顺序为 地形 → 山 → 其他地貌 → 河 → 资源；实现时新增其他地貌 `landForm != null` 跳过。当前代码尚未跳过，会覆盖已有山格；资源 pass 已有该跳过逻辑
- **已确认（2026-08-06）：山与地貌权重互斥——山走簇专属，不参与散落权重池**——与金矿相同策略（`clusterSpawn = true`、`RemoveScatteredForm` 清散落命中格）；其他地貌权重不受山的存在影响
- **已确认（2026-08-06）：战斗语义暂不扩展**——山格不提供高地防御/射程/视野加成；`LandFormEffectRule` 不为山配置效果（effectType = None）

## 1.3 探索与雾化表现

**现状核实：两条雾化路线的"山尖破雾"目前都做不到。**
- `FogEnvironmentObjectMask.shader:43`：遮罩存像素**世界 XZ**（`fogUV = (worldPosition.xz - origin) / size`）
- `FogEnvironmentSelective.shader:34-45`：后处理同样采样 `_FogMaskTex`（与 `FogBlend_final` 同款边界解析）——**选择性雾化也是 XZ 掩码驱动、高度盲**，山在未探索格上时尖与底座一起被雾化
- 若做高度感知雾（山尖探出迷雾可见）：路线 A 更干净——山体槽用专属 shader（按 worldY 衰减迷雾强度），不碰 `TerrainBase_Fog` 全局地形 shader；路线 B 需改后处理，影响所有地貌
- **已确认（决策 ⑥，2026-08-06）：走山体专属 shader 路线**（山体槽专属 shader 保留按 worldY 衰减迷雾强度的扩展能力，不碰 `TerrainBase_Fog`）；**v1 不启用高度感知雾**——山格由 visibility resolver 专属规则保持可见，不写探索位；高度感知雾作为后续视觉增强保留

## 1.4 与动态地图的交互

1. **山被 W/S（R/F）抬升/沉降**
   - 路线 A：山体拓扑不变时，Chunk 重建和 UV2/UV3 可复用现有高度动画（主题三 §3.6）；另需共享边通道一致性和保守 bounds
   - 路线 B：可复用部分对象刷新思路，但簇级大山还需创建/替换/销毁、root/雾化注册和多格实时 Y 聚合，不只是新增一个 follower
2. **山被 F 按成水域（陆→水双向重置）**
   - 路线 A：mountain builder 按有效山体判定删除山体贡献；这属于**拓扑删除**，不是天然的“沉没动画”。数据残留会导致水→陆后山体恢复，届时必须重新派生 `movementCost = MaxValue`
   - 路线 B：需在双向重置补规则（"动态地图管线反哺点"）
3. **不可通行**：寻路只看 `movementCost`；山格置 `float.MaxValue`（见 1.2，决策 ①）
4. **清除语义**：山格被 `ClearLandForm` 清除后不恢复山体（决策 ㉕）。应把 dirty flag 标为 `Terrain | Objects`；当前提交路径即使只有 `Objects` 也会重建 changed Chunk，但补齐语义可防未来按脏位分流时回归。动画提交还需处理旧山体 ghost 突消问题

## 1.5 与地貌体系的生态

- **已确认（2026-08-06）：山走簇专属，不参与散落权重池**——与金矿相同策略，`clusterSpawn = true`，`RemoveScatteredForm` 清散落命中格；其他地貌权重不受山的存在影响
- **已确认（2026-08-06）：山格优先生成，目标顺序 地形 → 山 → 其他地貌 → 河 → 资源**（见 §2.1）；资源已有 `landForm != null` 跳过（MapGenerator.cs:276），其他地貌 pass 需新增跳过，当前 :250 会直接覆盖
- 山不参与 `InstantiateLandForms` 逐格实例化（程序生成不占预制体池）
- 与另两个扩展方向的关系：山的基座与地形衔接 = "圆滑衔接"方向的子问题；山邻水 = "海岸"方向的联动——三者可统一规划

---

# 主题二：山脉生成规律

## 2.1 数据层：从单簇机制升级为脊线山脉生成

现有 `LandFormClusterSpawnRule`（:64-126）从堆心 BFS 生长并逐格写 `landForm`，但只支持数据库中第一个簇地貌；山脉采用决策 ⑯ 的 RidgeGenerator 后，不能直接把该 BFS 当作脊线生成器。可复用的是“格级 `landForm` 占用”与通用配置入口；脊线路径、宽度化、参数固化和多簇互斥均为新增逻辑。

**已确认（决策 ③，2026-08-06 扩展）：生成顺序改为 地形 → 山 → 其他地貌 → 河流 → 资源**——现行管线为 地形 → 河 → 地貌 → 资源（MapGenerator.cs:119-121）。需拆分山/其他地貌 pass；散落地貌跳过已有山格；RiverGenerator 的源头与每步流向均显式排除山格。初始地图不存在“山间河”（§3.4 相关分支降级为防御性代码）。

**已确认（决策 ②）：山形参数生成时固化**——脊线路径/seed/γ/宽度化半径等参数在生成时写入格级数据（如 `mountainRidgeId` + 参数快照），Chunk 重建按固化参数派生，不随当前连通簇实时重算；清除单格不重算整条山脉山形（山脉分裂按各自固化参数派生）。

## 2.2 高度规律（v2：距脊线衰减 + 沿脊线起伏）

高度规律决定**山脉地块控制点的高度**（Low-poly 控制点布局见主题三 §3.3）。决策 ⑯ 后山脉以"一条"脊线为生成单位，高度改为"距脊线的距离"决定（替代原簇心衰减方案）：

```
对一条山脉（脊线 S + 宽度化格集合）：
  对每个山脉地块 i：
    d_i  = |格中心 - 脊线最近投影|（XZ 距离）
    t_i  = 1 - d_i/R                  // R = 宽度化半径 ≈ 1.5 格距；0(边缘) ~ 1(脊线)
    h_i  = H_ridge(s_i) * t_i^γ       // γ≈1.2；H_ridge 沿脊线位置 s 起伏（峰/垭口）
         + noise * t_i                // hash(格坐标, seed) → [-0.3, 0.3]*H_max
    H_ridge(s) = H_max * (0.6 + 0.4 * ridgeNoise(s))   // 沿脊线确定性噪声起伏
```

- **确定性保证**：全部输入 = cell 数据 + seed，任意 Chunk 重建生成相同几何，跨 Chunk 天然一致——路线 A 成立的前提
- **蘑菇感缓解**：垂直脊线衰减（脊线高、两侧矮）+ 沿脊线峰/垭口起伏，相邻山脉地块公共边共享高度 → 视觉连片；**脊线连续角点规则（续20）**：脊线相邻格的公共角点/公共边保持脊线高度（相邻对两格山高均值），各主峰由折线状连续山脊连接而非断成"每格一个尖峰"
- **距离定义（决策 ㉑）**：`d` 用 XZ 平面距离，**禁用含 Y 的三维距离**（基础地形高度会扭曲山形）
- **高度范围约束（决策 ⑳）**：`mountainHeight >= 0`（噪声后钳制）；最小可见高度 0.15（以下不生成山体几何，防微隆起噪点）；最大坡度 = 相邻控制点高差 ≤ 0.8 × 格距（防陡薄片）；最大绝对高度 = `terrainY + H_max` 受配置上限约束
- **沿脊线起伏（决策 ⑱/㉔）**：脊线格高度随位置起伏（峰/垭口）；`H_max` 随脊线长度缩放

## 2.3 走向规律：山脊线（河流算法借鉴；决策 ⑯ 后为主路线）

山脊与河流是**同构问题：在六边形网格上生成一条有向路径**。

### 河流算法/数据结构盘点（已核实）
- 数据（HexCellData）：`hasRiver` + `RiverIncoming/OutgoingDirection`（有向：1 入 1 出）+ `HexType`（Source/Midstream/RiverEnd）
- 行走框架（RiverGenerator.cs:111-163）：源头 → 长度随机 → 逐格剔方向 → 随机选向
- 剔方向（:270-319）：高度条件 `邻居Height <= 自身`（只下坡，:304-310）；交叉条件 `邻居无河`（:313-319）

### 映射表

| 河流 | 山脊 |
|---|---|
| `hasRiver` + Incoming/Outgoing（有向） | `hasRidge` + `RidgeDirectionA/B`（**无向**：两端通过方向） |
| `HexType`：Source / Midstream / RiverEnd | 山脊端点 / 中段 / 山尾 |
| 行走框架：源头 → 长度 → 剔方向 → 随机选向 | **同一框架复刻** |
| 高度条件：只下坡（:304-310） | **反写为综合评分（决策 ⑰）**：高度排名 + 两侧落差 − 转向惩罚，禁回访；平坦区高度差不足时噪声随机游走（2026-08-06 修订：平坦游走按转向惩罚加权，优先直行，防蜷缩成酱饼） |
| 交叉条件：邻居无河（:313-319） | 同（保单脊线；分岔 = 允许 1 入 2 出——决策 ⑱：v2 首版不分岔，留 v3） |

### 关键差异（不能照抄的部分）
1. **方向语义相反**：河流有向（水必须往下），山脊无向（不区分上下游）——数据改名复用即可；"1 入 1 出"天然禁分岔（决策 ⑱：v2 首版维持不分岔，Y 型分岔 = 主脊带支脊，留 v3 放宽）
2. **高度约束"贴地形"而非"叠加在高度上"**：山脊走向锚定现有 `Height` 场——沿高处走，山从"地形本来就隆起的地带"冒出来，与"山脉不改 Height"的前提兼容；平坦区需要高度差阈值，不足时噪声驱动
3. **河流挖槽、山脊加料**：几何侧比河流简单（但 rect 要隆起，见主题三 §3.4）

### 落地组合（v1 首版，决策 ⑯）
```
1. RidgeGenerator（复刻 RiverGenerator 框架）：在 Height 场走脊线 → 写 hasRidge + RidgeDirectionA/B
2. 宽度化：脊线格 + 距脊线 1 格的邻居 = 山格（低矮），形成山体宽度 → "连片"而非"一团"
3. 几何：脊格之间三角带（山脊）+ 两侧山体（山坡）+ 离散色阶（§3.3）
```
方向字段（`RidgeDirectionA/B`）一套数据两个用途：决定哪里隆起，同时决定哪些 rect 边属于山体（§3.4）。

**山脉地块（决策 ⑯）**：每条山脉由"山脉地块"组成（脊线格 + 宽度化格）；每个山脉地块负责本格 solid 顶面 + 相邻 rect 过渡面的隆起（§3.4）；高度沿脊线起伏、垂直脊线衰减（§2.2）。

---

# 主题三：山脉网格构造

## 3.1 路线对比与选择

| 路线 | 做法 | 结论（2026-08-05 复核后） |
|---|---|---|
| A | 并入 Chunk 地形 mesh：隆起高度场写入 solid/rect/tri 顶点（末尾加 subMesh 槽） | **推荐为 v1 主路线**（§3.2~3.7） |
| B | 地貌模型：空 GO + 程序 mesh，挂 LandForm 根下，layer=FogAffectedEnvironment | 保留为参照系：单格/独立山体、纯视觉需求；构建器草稿见 §3.8 |
| C | `terrainType` 独立体系（已空置） | 未来升级触发条件见"待确认问题" |

## 3.2 路线 A 可行性复核（代码核实）

### subMesh 槽布局：动态布局 + 需要扩展完整几何契约
- 地形槽 = `3 基础 + N rect组合 + M tri组合`，按 Chunk 内材质组合键**动态变化**（ChunkMapRenderer.cs:1169-1192 建槽、1754-1793 解析材质）。不同 Chunk 槽数本来就不同，加山脉只需在**末尾追加 1 槽**
- 除 `ResolveTerrainMaterials`、`ResolveTransitionMaterials` 和 :1820 布局校验外，还需扩展 `TerrainGeometry`、山体 indices/槽存在标记、材质缓存与销毁、validator、碰撞 geometry 和测试；不是只有 3 个改点
- :1820 校验漂移时会降级为稳定材质，但 **CPU 顶点动画仍然执行**；丢失的是 Transition shader 的 keep-below clip/ShadowCaster 契约，可能出现 ghost 重叠或拓扑突变。现有注释“无顶点变形”本身已与 CPU 动画实现不一致
- MPB 作用于整个 TerrainRenderer，不按 subMesh 隔离。山体稳定材质可保持静态参数，但山体 Transition shader 仍必须兼容同一 Renderer 的 `_ChunkProgress/_ChunkAnimBaseY/_ChunkAnimRiseHeight` 属性块

### 顶点动画免费（确认）
动画是 CPU 逐顶点插值：`SetChunkAnimationProgress` 每帧写 `mesh.vertices`，基线取 UV2.x 旧高度、窗口内插值 UV2.y（:423-432 缓存，规则③）。山体顶点只需写入 UV2/UV3 通道（复用 `AppendRectAnimUV` 模式），天然随地形升降。

### 生命周期/水淹：高度变化可复用，拓扑变化需新增策略
Chunk 重建会让路线 A 与地形同步；仅基础 `Height` 变化且山体拓扑不变时可复用 UV2/UV3。水淹/清除会删除山体拓扑，不是免费沉没动画，需禁用该次动画或新增山体淡出/clip。`ClearLandForm` 应补 `Terrain` dirty 语义，但当前 changed Chunk 本来就会重建。路线 B 的簇级跟随新机制在 A 中消失。

### 碰撞：渲染/碰撞网格必须分离
决策 ㉚要求山体不参与碰撞，但当前 Renderer/Collider 共用同一个 Terrain mesh。路线 A 必须新增无山体的基础 collision mesh，保留被渲染山体替换掉的原始 solid/rect/tri，避免点击射线空洞；不能依赖 subMesh 排除碰撞。

## 3.3 山体几何构造（Low-poly 硬约束）

### 3.3.1 固定锚点规则：覆盖区外边界固定（2026-08-06 决策 ④）

**2026-08-06 修订**：原"保留原始地块边界点不变"与"山格 ↔ 山格公共边允许隆起"矛盾（若每个六边形边界都固定，两格之间只能回落 0，高度场无法跨格连续）。修订为：**固定锚点 = 山体覆盖区外边界（山-非山公共边）；覆盖区内部公共边允许隆起，但必须由规范化共享边函数产生完全相同的数值与细分控制点**（输入 = 两端格数据 + seed，与遍历顺序/Chunk 无关，防重建裂缝）。

**山体顶点由原始地形顶点派生，但原始地形数据（`Height`、`_solidVertices`、rect/tri 顶点）绝不修改**——山体构造只读取它们并生成派生顶点：

```
mountainVertex = terrainVertex + Vector3.up * mountainHeight
```

| 位置 | 规则 |
|---|---|
| 山格内部 | `mountainHeight > 0`，顶点向上隆起 |
| **山体覆盖区外边界（山 ↔ 普通格公共边）** | **`mountainHeight = 0`，公共边顶点保持原始位置**——与普通格 solid/rect/tri 无缝衔接 |
| 山格 ↔ 山格公共边 | 允许隆起，但**必须由规范化共享边函数计算**（两侧高度与细分布局完全相同；随机量以规范化边键 `(min/max GenerateOrder, seed)` 派生，不依赖遍历顺序） |
| 山体外缘向内 | 从公共边 0 高度逐级折面抬升到山体高度（山脚坡面） |
| 顶点索引 | 位置数值必须一致，**但可复制顶点索引**（山体用独立材质/法线） |

截面示意：

```
普通格          山脉覆盖区
______________/\____________
              ↑
       公共边保持原始位置
```

### 3.3.2 稀疏控制点布局（不做平滑圆锥）

每个山格**不**生成 6~12 段圆锥，只布置少量控制点：

- 山体覆盖区外边界上的原始锚点（固定）；山-山内部边不固定为 0
- 1 个主峰点
- 0~2 个偏心次峰点
- 沿 `RidgeDirectionA/B` 放置少量山脊控制点
- rect 区 1~2 个隆起点（§3.4）

一个山格通常**十几个顶点**，而非几百个。

### 3.3.3 不规则三角剖分

```
        主峰
       / | \
  次峰───┼──山脊点
    / \  |  / \
固定边界锚点
```

- 峰顶**故意偏离格中心**（XZ 带确定性扰动）
- 控制点 XZ 加确定性的轻微扰动，三角面大小不完全一致
- **避免每格形成规则六棱锥**（Low-poly 的"自然感"来源）
- 相邻山格共享相同的公共边高度与山脊控制点位置

### 3.3.4 平面法线（Low-poly 外观的关键）

Low-poly 观感不靠低面数本身，靠**面与面之间不平滑法线**：

- 每个三角面使用独立顶点，或在生成后做 flat-shaded 顶点拆分
- 不允许山体相邻面共享平滑法线
- 可参考项目现有 `FlatAll` / `ToFlatShaded` 处理方式（ChunkMapRenderer.cs:1109-1110 先例）
- `RecalculateNormals()` 前确保不同三角面的顶点已拆分，否则 Unity 会把法线平均成圆滑山体
- **注意：顶点拆分后，拆分出的每个顶点必须完整保留来源映射并写齐 UV0/UV2/UV3 与 face tier**——否则动画插值/着色数据丢失（§3.6）
- **已确认（决策 ㉖）跨 Chunk 法线合并冲突**：采用**专属 shader 内片元导数（dFdx/dFdy）重算法线**；`MergeChunkBoundaryNormals` 可不修改，但 shader 仍需正确处理法线空间、双面朝向、Triplanar 权重和 ShadowCaster，不能视为“零改造”

### 3.3.5 离散色阶（不做连续渐变）

- 按面法线 / 高度**分段**着色：低处深灰褐岩石 → 中部灰岩 → 高处浅灰 → 雪顶离散白色面片
- 同一三角面的三个顶点使用**相同色阶**（保持块面感）
- 实现：优先为每个面生成离散 `faceTier`（拆分后三顶点同值）并由山体专属 shader 着色。仅按 `worldY` 阈值会在跨阈值的大三角面内部形成连续切带，不满足“同面同色”

## 3.4 rect 过渡面的隆起（关键修正）

### 河流的模式（已核实）
`MainMeshRectFunction`（ChunkMapRenderer.cs:1954-1964）按格数据输出 `isRiver`；rect 构建分支（:1080-1117）在坡面 rect 的 8 顶点基础上**追加 4 个河道点**（`+RiverDepth`，MeshDataGenerator.cs:1315-1342），draw order 同时含坡面与河道壁三角（:1479-1528）——**河流是就地改造 rect 几何（挖槽）**，不是叠加装饰。

### 山脉的正确模型：过渡区是山体的一部分，且要隆起
**关键修正：山-山之间的 rect 过渡面不是"山鞍"（两山之间独立的低洼连接件），也不是"原地形 rect 换山体材质"——它本身就是山体，必须随山体一起隆起。**

```
A 地块山体              B 地块山体
        ┌────────────┐
       /              \
      /                \
_____/   隆起的 rect    \_____
```

- 山体 = **跨格的连续隆起高度场**：覆盖区内的 solid 顶面、rect 过渡面、必要的 tri 角点，全部叠加 `mountainHeight`
- 最终几何：`finalY = terrainY + mountainHeight`（固定锚点规则 §3.3.1 同样适用）
- rect 拓扑（slope / step 由 `RectType` 决定）保留，但**顶点 Y 整体加隆起**；若 A/B 山高不同，rect 顶部在两者之间平滑倾斜
- 山体内部 rect 高度连续；**相邻山格共享边缘高度**（否则跨 Chunk 重建出裂缝）
- **侧坡只生成在山体覆盖区外缘**（山-非山边）：隆起从山体高度衰减到 0（"衰减隆起"而非突变），山脚自然落地

### 折面化（Low-poly 要求）
rect 隆起不做高密度平滑插值：保留原始边界锚点，用 1~2 个隆起点把过渡区切成 **2~6 个大三角面**（折面而非弧面）：

```
普通格固定边             山脉内部
─────────────╲_______╱────────
              \  ▲  /
               隆起点
```

- 山-山 rect：整体处于较高位置，形成连续山脊平台
- 山-普通 rect：从固定原始边（偏移 0）开始，一至两级折面抬升到山体高度

### z-fighting 处理
**已确认（决策 ⑤）：solid/rect/tri 全部替换式拓扑**——被山体覆盖的原始三角面（solid 顶面、rect 过渡面、tri 角点面）一律从地形 subMesh 中移除，隆起面归山体 subMesh；不能叠加绘制，否则 z-fighting、内部重叠面与重复碰撞。

### 判定规则
- 不能只判"两格皆山"：山-非山边也可能需要衰减隆起
- 正确判定：**该 rect 边上是否存在山体贡献**（`hasMountainContributionOnEdge`）——两格皆山 ⇒ 完整隆起；山邻普通格 ⇒ 衰减隆起（幅度取决于该侧山体外缘高度）
- ~~山间河让位~~：**已确认（决策 ③）山与河不共格**，初始地图不存在"山间河"；此分支降级为防御性代码（若未来数据被动态改出共格，`isRiver` 分支优先、山体该侧内收）
- tri 角点：**已确认（决策 ⑤）三格交界 tri 封口进 v1**——1/2/3 个山格三种情形都要替换式隆起（3 格全山时三面抬升；1~2 格时外边界固定封口），否则出现洞/竖裂缝/薄片

### 与河流的真正对应
```
河流：原始 rect ──下沉──> 挖出河槽（地形材质 + 独立河面 mesh）
山脉：原始 rect ──抬升──> 隆起为山体坡面（山体材质，离散色阶）
```
两者都是**改变 rect 过渡区域的几何高度**，而非在 rect 上叠装饰模型——这是路线 A 与路线 B（模型叠加）的本质区别。

## 3.5 subMesh 槽位与材质

- 山体 subMesh = 末尾追加 1 槽：山体三角（solid 隆起面 + rect 隆起面 + tri 封口）同槽
- 完整改造范围见 §3.2 与“源码审计修正 B”：DTO、indices、材质稳定/Transition 路径、缓存销毁、validator、collision geometry 与测试需同步扩展
- 山体稳定材质不持有逐 Chunk 私有状态；但 MPB 作用于整个 TerrainRenderer，山体稳定/Transition shader 都必须容忍同一 Renderer 的属性块，Transition 版还必须消费 clip 所需参数
- **已确认（决策 ⑥）：离散色阶走山体专属 shader**（以离散 face tier/面法线为主，避免 worldY 阈值在单面内部切带；不做连续渐变）——顺带保留高度感知雾扩展能力（§1.3）
- **专属 shader 的配套契约**：山体 Transition 变体需完整复制稳定版山体契约——Triplanar、face tier、导数法线的空间转换/双面处理、keep-below clip、手动 ShadowCaster pass（与 surf 同 clip）、FogBlend、CPU 顶点动画——不是“换个 shader 名”就算变体
- **已确认（决策 ㉗）UV 策略：世界空间三平面映射（Triplanar）**——不规则三角随顶点扰动/脊线走向不同每次都要重算展开，Triplanar 直接用 worldPos 采样，确定性、零展开成本，Low-poly 大块面配合离散色阶效果好

## 3.6 顶点动画通道

- 山体顶点（含隆起 rect 顶点）照写 UV2/UV3：UV2.x=旧高、UV2.y=目标高、UV3=错峰窗口（与地形顶点同一套生成逻辑，复用 `AppendRectAnimUV` 模式）
- `SetChunkAnimationProgress` 每帧逐顶点插值，山体随地形升降免费
- 隆起 rect 顶点与普通 rect 顶点同坐标来源（`ctx.Solid` 边界顶点），动画行为一致
- **flat-shaded 顶点拆分与动画通道必须保持来源映射**：当前 rect/tri 流程是先 flat 拆分、再按最终顶点写 UV2/UV3；若坚持决策 ㉙“先写通道再拆分”，必须新增能复制 UV2/UV3/faceTier/source-index 的山体 DTO/拆分工具。无论顺序如何，共享边重复顶点的起终高度和 delay window 必须完全一致

## 3.7 生命周期与碰撞

- Chunk 重建自动按当前数据再生山体；基础高度动画可复用，水淹/清除等拓扑变化需单独过渡策略
- MeshRenderer 使用含山体替换拓扑的渲染 mesh；MeshCollider 使用保留原始基础地表、无山体且无洞的独立 collision mesh
- collision mesh 不逐帧 cooking；提交时切到终态基础地表。地图点击和镜头旋转枢轴继续依赖该物理射线

## 3.8 路线 B 构建器草稿（保留作参照）

```
MountainMeshBuilder.Generate(cells: 簇内格集合, seed)
 1. 基座：取簇包围盒，底部六边形顶点吸附各格 RealCenterWorldCoordinate.y（贴地防 z-fighting）
 2. 主体：Low-poly 尖顶（稀疏控制点 + 不规则三角剖分，§3.3），非平滑锥体
 3. 山脊：跨格簇沿主轴生成 2-3 个锥体叠加/连接面，避免"每格一个独立锥"的蘑菇感
 4. 材质：单材质岩石色 + 离散色阶（顶点色或面法线分段）
 5. 挂 LandForm 根下，layer=FogAffectedEnvironment，加 ModelController
```

- 面数可控（每山几百~几千三角）；576 格地图几十座山预算内

## 3.9 风险与护栏

1. **渲染核心的护栏**：路线 A 触碰完整 TerrainGeometry/材质/碰撞契约（§3.2）；:1820 布局校验兜底但降级时 CPU 动画仍执行。山体 shader 必须兼容 Renderer 级 MPB
2. **跨 Chunk 一致性**：per-cell 确定性生成天然保证；脊线骨架同确定性；相邻山格共享边缘高度（§3.3.1），防重建裂缝
3. **flat 拆分 × 动画通道**：拆分顶点必须有来源映射并写齐 UV0/UV2/UV3/faceTier；共享边动画窗口也必须一致（§3.6）
4. **地貌语义占用**：同池权重 vs 簇专属（金矿先例）
5. **水陆/清除语义**：有效山体贡献会在重建时消失，但拓扑变化动画需处理；水→陆需恢复山格规则；`ClearLandForm` 补 `Terrain | Objects` dirty 语义
6. **高山 bounds 与遮挡**：按动画起终点和 `H_max` 预扩 bounds；验证近裁剪、阴影剔除、镜头旋转枢轴、标签/Overlay marker/单位/建筑遮挡

---

# 待确认问题（按主题归类）

## 已确认决策（2026-08-06）

| # | 决策 | 推论与未决细节 |
|---|---|---|
| ① | 山格不可通行、不可建造（`movementCost = MaxValue`；`blockBuildingSpawn = true`） | `blockBuildingSpawn` 天然阻止建筑放置，不会触发 BuildingController 写回；水→陆重置后山格规则需重新生效（MapMutationService.cs:491-492）；山格规则挂格级数据独立于 `Height` |
| ② | 山形参数生成时固化（脊线路径/seed/γ/宽度化半径等生成时写入格级数据） | Chunk 重建按固化参数派生，不随当前连通簇实时重算；`HexCellData` 新增字段结构待定（如 `mountainRidgeId` + 参数快照） |
| ③ | 山与河不共格；生成顺序 地形 → 山 → 其他地貌 → 河 → 资源 | 河生成跳过山格；其他地貌/资源掷点跳过山格；§3.4"山间河让位"降级为防御性代码 |
| ④ | 固定锚点 = 山体覆盖区外边界（山-非山公共边）；内部山-山公共边由规范化共享边函数隆起 | §3.3.1 已按此修订 |
| ⑤ | solid/rect/tri 全部替换式拓扑（被覆盖原始三角从地形 subMesh 移除） | 三格交界 tri 封口进 v1（1/2/3 个山格三种情形）；§3.4 已按此修订 |
| ⑥ | 离散色阶走山体专属 shader | 顺带支持高度感知雾；山体 Transition 变体需完整复制 clip/阴影/雾/CPU 动画契约；§3.5 已按此修订 |
| ⑦ | 山格不在水域格上生成；运行期水淹时移除山体贡献，保留基础海床；允许 `landForm` 残留 | 水淹是拓扑删除而非天然沉没动画；水→陆后山体恢复并必须重新派生不可通行规则 |
| ⑧ | 点击归属维持“基础 collision mesh 物理射线 + XZ 最近中心”近似反查，不做三角面格 ID | 侧坡可能归邻格或超出反查半径；山格不可部署使误差影响可接受，但必须做边界点击回归测试 |
| ⑨ | v1 不做山顶专属高亮 | 各高亮来源显式过滤山格；现有 `HexHighlightRenderer` 不会自动过滤，不能原样依赖 |
| ⑩ | 不参与探索提示（无 `markerPrefab`，`LandFormMarkerManager` 跳过） | 山不需要"发现奖励"，与金矿区别对待 |
| ⑪ | 山格整体不被迷雾遮盖，但不写 `IsExplored`；通过 visibility resolver 的山格永久可见规则实现；v1 不启用高度感知雾 | 视觉免雾与探索/归属/后勤解耦；rect/tri 的 XZ mask 边界需实测 |
| ⑫ | 山格优先生成，山格上不生成其他地貌和资源 | 资源当前已跳过；其他地貌 pass 必须新增 `landForm != null` 跳过，当前实现尚无此条件 |
| ⑬ | 山不参与散落权重池，以 RidgeGenerator 专属 pass 生成；金矿仍走簇规则 | 山与散落随机流解耦；其他地貌权重不变，多簇占用顺序由决策 ⑮ 统一约束 |
| ⑭ | 战斗语义暂不扩展（effectType = None，不提供高地防御/射程/视野） | `LandFormEffectRule` 不为山配置效果 |
| ⑮ | 多种簇地貌：升级为多簇遍历 + 互斥/顺序（山先于金矿等其他簇） | 当前仅支持第一个簇地貌；新增共享占用集合与独立确定性随机流。山脉脊线由 RidgeGenerator 生成，不直接套用金矿 BFS |
| ⑯ | 粒度直接上 v2：山脉以"一条"（脊线）为生成单位，不做 v1 单格山 | 山脉 = 一条脊线 + 宽度化格；每格 = "山脉地块"，作为管理单元，负责本格 solid 顶面 + 相邻 rect 过渡面的隆起（与路线 A 按格构建一致；§1.1/§2.3 已按此修订） |
| ⑰ | 山脊线评分 = 综合评分：高度排名 + 两侧落差 − 转向惩罚，禁回访 | "只上坡"会停在局部峰顶；平坦区高度差不足时噪声随机游走——**2026-08-06 修订：平坦区游走同样按转向惩罚加权（优先直行/缓弯，`exp(-w3×转向量)`），均匀随机会让平地脊线蜷缩成圆形酱饼**；评分 = w1·高度排名 + w2·两侧落差 − w3·转向惩罚，全部确定性（§2.3 映射表已按此修订） |
| ⑱ | 山脊线参数：长度 5~12 格；陡坡阈值 = 1 级 `Height`；v2 首版不分岔（1 入 1 出，同河流）；脊线替代簇（无"簇内取脊"双轨） | Y 型分岔留 v3；宽度化 = 脊线格 + 两侧 1 格（低矮坡面，决策 ⑯） |
| ⑲ | `hasMountainContributionOnEdge`：边两端至少一端为山脉地块且该侧隆起 > 0；衰减 = 1~2 级折面（非弧面插值） | 山-山边完整隆起（规范化共享边函数）；山-非山边从 0 折面抬升到边缘高度；单级最大高差 ≈ 1.5 × elevationStep（防陡薄片） |
| ⑳ | 高度范围约束：`mountainHeight >= 0`（噪声钳制）；最小可见高度 0.15；最大坡度 = 相邻控制点高差 ≤ 0.8 × 格距；最大绝对高度受 `H_max` 配置上限约束 | 防负隆起/陡薄片/微隆起噪点；§2.2 已按此修订 |
| ㉑ | 簇心距离 `d` 用 XZ 平面距离（禁用含 Y 三维距离） | 六边形图距离作为备选，v2 不启用；§2.2 已按此修订 |
| ㉒ | 不做雪顶/季节变化 | 离散色阶固定 3~4 段（岩石灰褐→灰→浅灰），不随季节/高度气候带扩展 |
| ㉓ | 跨 Chunk 确定性：共享边随机量用规范化边键 `(min/max GenerateOrder, seed)` | 所有随机量用 hash(键 + seed) 确定性函数，不连续消耗 `System.Random`、不依赖遍历顺序 |
| ㉔ | 山形参数随脊线长度缩放：`H_max = clamp(baseH + k·(len − minLen), minH, maxH)`；不做双峰项 | 山专属参数块（长度/陡坡/宽度/衰减/高度）入 `MapLandFormSO` 或独立 `MountainConfigSO`；**2026-08-06 增补：地图设置 SO 暴露全局高度缩放 `MapGenerationConfigSO.mountainHeightScale`（clamp 后相乘，只影响视觉隆起，不消耗随机流）** |
| ㉕ | 山被动态清除后不恢复（首版暂不考虑重生成） | 数据层语义：清除 = 永久移除，重建时跳过；`ClearLandForm` 补 `Terrain` 脏位后几何同步消失 |
| ㉖ | 跨 Chunk 法线合并：专属 shader 内片元导数（dFdx/dFdy）重算法线 | 主颜色法线可不依赖 CPU 合并，但 shader 需处理法线空间、双面和 ShadowCaster；`MergeChunkBoundaryNormals` 可不改，阴影边界必须验收 |
| ㉗ | 山体 UV 策略：世界空间三平面映射（Triplanar） | 不规则三角随顶点扰动/脊线走向不同难以手工展开；Triplanar 用 worldPos 采样，确定性、零展开成本；§3.5 已按此修订 |
| ㉘ | Low-poly 参数起步值：XZ 扰动幅度 ±0.15×格内切圆半径；峰顶偏心量 [0.05, 0.20]×格内切圆半径（hash(cellId,seed)）；色阶 3 段起步（岩褐/灰岩/浅灰） | 参数入 `MountainConfigSO`；后续需要时加第 4 段（雪顶） |
| ㉙ | flat 拆分与动画通道保持完整来源映射；目标顺序为控制点通道 → flat 拆分复制 → 提交 | 当前通用工具不复制 UV2/UV3，山体需新增 DTO/拆分工具；也可沿用现有“先拆分、后按最终顶点写通道”，但共享边通道一致性是硬约束 |
| ㉚ | 山体不参与 MeshCollider；渲染 mesh 与 collision mesh 分离 | collision mesh 保留山下原始 solid/rect/tri，不能按 subMesh 排除；物理射线仍必需，随后才做 XZ 反查；仍有基础地形 cooking 成本 |
| ㉛ | 验收标准（v1 清单）：共享边位置及 UV2/UV3 误差 < 1e-4；无非流形边；无退化三角；动画通道长度一致；固定种子重建 hash 一致；清除/水淹无残留；Chunk 边界截图无裂缝 | 新增 topology/determinism 测试与 bounds、碰撞无洞、拓扑变化动画、阴影边界验收；现有 validator 不覆盖这些项目 |

## 主题一（作用）
- [x] 建立统一山格规则入口：已新增 `IsMountainCell`、`IsEffectiveMountainCell`、`CanEnterCell`、`CanSpawnUnitOnCell`、`CanBuildOnCell`；水→陆后会重新派生不可通行规则
- [ ] 玩家与 AI 卡牌、兵营、AI 工厂等所有单位部署入口接入 `CanSpawnUnitOnCell`（统一函数已存在，调用方尚未全部改造）
- [ ] 永久免雾规则：visibility resolver 独立处理山格，不写 `IsExplored`；明确山格能否探索/占领及是否参与后勤（建议均否）
- [x] 山被动态清除的数据/脏位语义：`ClearLandForm` 永久清除山脉数据并补 `Terrain | Objects`；对应单元测试已通过
- [ ] 山被动态清除的视觉过渡：采用无动画提交或山体专用拓扑淡出，避免 TerrainGhost 突消
- [ ] 可读性/遮挡预算：高山遮挡单位/建筑/河流/格线/费用标签/Overlay marker；验证 `H_max`、近裁剪、bounds、镜头边界、旋转枢轴和阴影距离

## 主题二（生成规律）
- 全部 11 项已于 2026-08-06 决策完毕（决策 ⑮~㉕，见上表），无剩余待确认

## 主题三（网格构造）
- 全部 7 项已于 2026-08-06 决策完毕（决策 ㉖~㉛，见上表），无剩余待确认
- 路线 C 升级触发条件暂不讨论，v1 按路线 A 推进
- 实施前置虽已定方向但尚需编码：mountain-aware rect profile/tri、独立 collision mesh、拓扑变化动画策略、共享边动画通道、保守 bounds、山体材质生命周期和 topology/determinism 测试

---

# 分阶段实施修改计划

> 目标：在不破坏现有地形、河流、动态地图、迷雾和点击链路的前提下，按“数据契约 → 生成规则 → 几何基础 → 碰撞/动画 → Shader/表现 → 全量验收”的顺序实施路线 A。
>
> 原则：每阶段完成后都应能独立编译、运行或通过对应测试；阶段之间不跨越未满足的前置条件。以下计划解决的是前文源码审计发现的主要错误和遗漏，但**计划本身不代表代码已经修改完成**。

## 阶段 0：建立基线与回归入口（部分完成）

**目的**：在修改前固定当前行为，避免山脉改造与工作区已有改动混淆。

**工作项**：

- [x] 确认当前 Unity 项目可打开并记录编译基线；第一批实现后 MainGame/Tests 程序集编译无错误。
- [x] 单元测试固定地图尺寸、水位和随机种子，形成脊线生成确定性回归入口。
- [ ] 记录现有地形的 Chunk 边界、河流、海床、点击、镜头旋转、迷雾和动态抬升/沉降截图。
- [ ] 增加或整理最小回归场景：普通地形、河流、水域、Chunk 边界、动态 Height 修改。
- [x] 实施前确认工作区干净，山脉修改范围可与原有改动区分。

**完成标准（部分达成）**：编译无新增错误；固定种子脊线数据与山格集合可重复。基线截图与专用回归场景尚未补齐。

## 阶段 1：数据模型与玩法契约（已完成当前阶段范围）

**目的**：先解决“山是什么、占哪些格、规则如何恢复”的问题，不生成山体几何。

**主要文件/模块**：

- `HexCellData.cs`
- `MapLandFormSO.cs` 或新增 `MountainConfigSO.cs`
- 地图保存/加载或种子初始化相关代码
- 新增 `MountainCellRule` / `MountainData` 等纯数据服务

**工作项**：

- [x] 定义脊线级数据：`ridgeId`、脊线顺序/端点信息、生成 seed、长度、宽度化半径、衰减参数、`H_max` 快照等（`MountainRidgeData`）。
- [x] 定义格级数据：脊线格/宽度化坡面格、所属 ridge、固化参数引用、距脊线距离、沿脊线位置、永久清除状态（`HexCellData`）。
- [x] 使用 `landForm` 作为占用标记；已自动创建 `Mountain.asset`，配置为 `modelPrefab = null`、`markerPrefab = null`、`effectType = None`、`blockBuildingSpawn = true`、`spawnWeight = 0`。
- [x] 提供统一的 `IsMountainCell`、`IsEffectiveMountainCell`、`CanEnterCell`、`CanSpawnUnitOnCell`、`CanBuildOnCell` 判断，统一处理水域、山体和永久清除状态（`MountainCellRule`）。
- [x] 水→陆、陆→水、`ClearLandForm` 后重新派生移动规则；山体规则不会再被默认 `movementCost = 1` 覆盖。
- [ ] 山格免雾的运行时代码：只影响视觉、不写 `IsExplored`、不改变归属/探索/AI/后勤（设计已确认，留阶段 6 实现）。

**完成标准（已通过）**：`MountainCellRuleTests` 与 `MapMutationMountainTests` 全部通过；山格不可通行/不可部署/不可建造，水陆切换和清除状态符合决策 ①、⑦、㉕；尚未接入山体几何。

## 阶段 2：生成管线与确定性脊线（核心已完成，hash 细化待补）

**目的**：解决当前生成顺序、多簇互斥、河流避让和山形参数固化问题。

**主要文件/模块**：

- `MapGenerator.cs`
- `LandFormClusterSpawnRule.cs`
- 新增 `RidgeGenerator.cs`
- `RiverGenerator.cs`
- `LandFormSpawnRule.cs`
- `ResourceSpawnRule` 调用处

**工作项**：

- [x] 将初始生成顺序改为：地形 → 山脉 → 其他簇地貌/散落地貌 → 河流 → 资源。
- [x] 实现 `RidgeGenerator`：使用山脉独立确定性 seed 流选择起点，以高度排名、两侧落差、转向惩罚综合评分行走，长度 5~12 格，禁回访，不分岔。
- [x] 将脊线格宽度化为两侧低矮坡面格，并写回 ridge 固化数据与格级 `d/s` 派生输入。
- [x] 将 `LandFormClusterSpawnRule` 从“只找第一个簇地貌”升级为按配置顺序遍历；堆心、生长和散落拦截使用共享占用集合；原金矿簇测试全部通过。
- [x] 其他地貌掷点前跳过已有山格；资源沿用 `landForm != null` 跳过，山格不会被资源覆盖。
- [x] 修改 `RiverGenerator` 的源头候选、每步流向和交叉检查，统一排除山格，并保留空邻居防御性过滤。
- [ ] 将全部山脉随机量进一步改为 `hash(mapSeed, ridgeId, GenerateOrder/规范化边键)`；当前脊线生成结果同 seed 可复现，但行走仍连续消耗山脉专属 `System.Random` 流，尚未满足决策 ㉓ 的最终遍历顺序无关要求。

**完成标准（部分达成）**：固定 seed 多次生成的 ridge 数据和山格集合一致（`RidgeGeneratorTests` 全部通过）；山格生成、水域排除、宽度化、脊线互斥、地貌/资源互斥和河流避让代码已接入。规范化 hash、跨 Chunk 几何归属及完整运行时地图回归留待后续阶段。

## 阶段 3：路线 A 几何基础与基础碰撞网格（2026-08-06 细化为 3.1~3.8）

**目的**：先实现静态、无动画的替换式 solid/rect/tri 拓扑，优先解决裂缝、洞、重复面和碰撞冲突。

**主要文件/模块**：

- `ChunkMapRenderer.cs`
- `MeshDataGenerator.cs`
- `TriangleTransitionMesh.cs`
- `PreparedRenderGeometry.cs` / `PreparedChunkGeometry.cs`
- `MapChunkView.cs`
- 新增 `MountainHash.cs`（确定性 hash）、`MountainGeometryBuilder.cs`（山体几何纯函数）、`MountainGeometryTests.cs`（测试）

**源码审计关键事实（细化依据，2026-08-06 复核）**：

1. solid 顶面 = 44 点布局（0 中心 + 1~6 角点 + 7~18 边点 + 19~43 河道点），顶面扇形只用 0~18；山格永不为河格（决策 ③）⇒ 山体 solid 恒用无河扇形（DrawOrder1），无需河道变体。
2. solid 扇形边位于 **0.7×内切圆半径** 处（`SolidAreaRatio`）；**真正的格界（山-非山公共边）在 rect 区域中点**（profile start→end 的 u=0.5 恰好过格界）。因此"固定锚点 = 公共边原始不动"（决策 ④）在网格上的准确解释是：**rect 在格界线处高度 = 0、普通格的 solid/rect/tri 全部保持原始**；山格自己的 solid 环属于覆盖区内部，允许隆起。
3. rect = 4 条 profile（角0/边1/边2/角3）×（start→end 插值）；tri 由相邻 rect 的**角 profile** 派生（`RectangleDrivenTriangleMesh`）⇒ 山体 rect 必须输出同样的 profiles 契约，且**折面只加在中间边 profile，角 profile 恒为无折点直线**，否则 tri 与 rect 之间出裂缝。
4. solid 角点 = 3 格交界点。规则细化为：**3 格全为有效山 ⇒ 隆起；否则恒 = 0**。推论：山-非山边上的角点恒 0 ⇒ 含普通格的 tri 恒为平面，与角 profile 端点天然一致，1/2 山格 tri 不需要任何山体改造（只有 3 山格 tri 进山体槽）。（**2026-08-06 修订链：max → 均值 → 脊线连续**——max 把覆盖区内部角点全部抬到局部最大山高，扇面主峰恒 ≤ 环角点，任何格都无法成峰，整条山脉被抹平成平顶台地（续17）；纯均值又把脊线格间公共角点压到含坡面格的均值，相邻主峰间成深谷，截面锯齿化为"每格一个尖峰"（续20）。现行规则 = 脊线连续：存在脊线相邻对时角点 = 相邻对两格山高均值的最大值，否则三格均值——脊线成连续山脊、坡面成坡，见续20。）
5. 河流 rect 要求双边 `hasRiver`，山格无河 ⇒ **任何贴山 rect 恒非河**，mountain rect 无需处理 isRiver 分支（决策 ③ 的防御性共格分支仅需保留 solid 层）。
6. `TerrainFilter.sharedMesh` 与 `TerrainCollider.sharedMesh` 当前指向同一 mesh（:1516）⇒ 碰撞网格必须独立成 mesh；采用**共享顶点数组 + 独立 collision indices** 双轨：被替换面的原始版本进 collision indices，山体面只进渲染槽。

### 阶段 3.1：确定性 hash 与高度场（纯函数先行）

**目的**：把"每个山格山高是多少"收敛为不依赖遍历顺序的纯函数，供 solid/rect/tri 与后续所有几何复用。

**工作项**：

- [✔] 新增 `MountainHash`：整数混合 hash（FNV 风格），`Hash01(key…, seed)` / `HashSigned(key…, seed)`；所有阶段 3.1 高度场随机量走规范化键 hash，不连续消耗 `System.Random` 流（决策 ㉓ 的最终形态）。
- [✔] `MountainRidgeData` 补 `ridgeNoiseAmplitude`、`cellNoiseScale` 快照字段（决策 ② 要求快照自足），`RidgeGenerator` 生成时写回。
- [✔] 实现高度场 `MountainGeometryBuilder.ComputeMountainHeight(cell)`：`t = clamp01(1 − d/R)`；`H_ridge(s) = hMax × (0.6 + ridgeNoiseAmplitude × ridgeNoise(s))`，ridgeNoise 使用沿脊线 1D 平滑 hash 值噪声；加入格级 hash 噪声并将结果钳制到 `[0, hMax]`（决策 ⑳）。
- [✔] 统一有效山体判定 `HasVisibleMountain(cell)` = `IsEffectiveMountainCell && ComputeMountainHeight ≥ minVisibleHeight`，供后续 solid/rect/tri 三层与山体槽存在标记共用（审计修正 A-6）。
- [✔] 提供规范化键：边键 = `(min/max GenerateOrder, seed)`；角点键 = 排序后 3 个 `GenerateOrder`；峰点键 = `(GenerateOrder, seed)`。
- [✔] 补阶段 3.1 单测：同输入同输出；边/角点键遍历顺序无关；`t=1` / `t→0` 边界；噪声钳制；默认快照坡度回归断言。

**完成标准**：高度场纯函数 + 快照字段补齐 + hash 单测全过（Unity EditMode 测试待当前 Unity 实例释放项目锁后执行）。

### 阶段 3.2：数据契约扩展（TerrainGeometry/DTO/双缓冲）

**目的**：只扩展 DTO 与渲染数据结构，不改变任何输出行为，保证可编译、可回归的中间态。

**工作项**：

- [✔] `TerrainGeometry` 增加 `MountainIndices`（int[]，可空）、`HasMountain`（bool 派生属性）、`CollisionIndices`（int[]，可空；无山 Chunk 为 null，Commit 时碰撞回落渲染 mesh，零额外内存）。
- [✔] `ChunkRenderData` 增加 `ActiveCollision`/`StagingCollision` 双缓冲 Mesh（延迟创建：仅 HasMountain 时分配，见 `CommitChunkStaging` 碰撞分支）。
- [✔] `ValidateTerrain` 扩展：MountainIndices/CollisionIndices 索引范围校验；`HasMountain ⇔ MountainIndices 非空` 由派生属性保证一致（槽存在条件以最终非空 indices 为准，不看残留 landForm）。
- [✔] `ResolveTransitionMaterials` 布局校验计入山体槽（`3 + rect + tri + (HasMountain ? 1 : 0)`）；山体槽在阶段 4 专属 shader 就位前回落稳定材质，保证动画路径不报错。
- [✔] 编译通过（MainGame/Tests 均 0 错误）；现有测试无回归（行为零变化已达成——当前无任何代码产出 MountainIndices/CollisionIndices，新分支全部不可达）。

**完成标准**：契约就位，行为不变，编译与测试全绿（已完成：MainGame/Tests 编译 0 错误；阶段 3.6 之前 MountainIndices/CollisionIndices 恒为 null，无行为变化）。

### 阶段 3.3：MountainGeometryBuilder——solid 山体

**目的**：生成单格 Low-poly 山体顶面（替换式），边界环与 rect 共享同一套规范化高度规则。

**工作项**：

- [✔] 边界环高度规则（v1 细化）：角点 = 3 格全有效山 ? **脊线连续规则**（存在脊线相邻对 ⇒ 各相邻对两格山高均值的最大值，脊线格间连成连续山脊；否则 avg(hA,hB,hC)，坡面成坡。2026-08-06 修订链：max → 均值（续17，max 抹平成平顶）→ 脊线连续（续20，纯均值把脊线格间角点压成深谷、呈"每格一个尖峰"锯齿））: 0；边点：山-山边 = lerp(两端角点高度, u∈{1/3, 2/3})；山-非山边 = hA × 0.5（boundaryEdgeRatio，v1 常量 0.5，后续可入配置）。环点 XZ 恒取原 solid 点位，仅 Y = terrainY + 边高。
- [✔] 顶点布局：18 环点 + 1 主峰（19 顶点三角扇）。主峰 XZ 偏心：幅度 hash ∈ [peakEccentricMin, peakEccentricMax]（决策 ㉘）；**脊线格偏心方向沿 RidgeDirectionA/B 轴 ±hash**；v1 不做独立次峰/脊线点（3.3.2 允许 0 个次峰）。
- [✔] 三角扇 18 面 → flat 拆分 54 顶点；每面同 faceTier：`tier = clamp(floor(面均高 / hMax × 3), 0, 2)`（决策 ㉘ 色阶 3 段起步），写 UV0.y = (tier + 0.5)/3；UV0.x = ridgeKey01（ridgeId 散列，阶段 4 备用）。UV 占位从本步骤写入，避免阶段 4 返工。
- [✔] 输出 `CellGeometry { Vertices, UVs, Indices, Weights }`：Weights = 每顶点所属格权重（环边点 [self, neighbor]、角点 [3 格]、峰点 [self]），供阶段 5 动画通道插值（审计修正 B-4）。
- [✔] 单测：山-山共享交汇/镜像边点由两格分别构建的环点数值一致（< 1e-4）；山-非山边环点高度规则正确；峰点偏心幅度不越界；无退化三角；单格顶点预算 = 54。

**完成标准**：单格山体几何纯函数稳定，共享边一致性单测通过（MainGame/Tests 编译 0 错误；测试待 Unity 运行）。

### 阶段 3.4：mountain-aware rect

**目的**：rect 过渡区成为山体一部分（§3.4），同时保留 profiles 契约供 tri 派生（审计修正 B-3）。

**工作项**：

- [✔] 山-山 rect：starts/ends = 两侧环边 4 点（角点按角点规则、边点按边规则），Slope 直纹面（profile 长度 2，8 顶点 6 三角，flat 拆分）；v1 不加中间折线（决策 ⑲ 折面要求只针对衰减边）。
- [✔] 山-普通 rect：**格界劈半（2026-08-07 续22 修订，决策 ④ 细化）**——山侧半边 profile 长度 2：[角点 0 / 边点 E=hA×0.5, 0@格界锚点]（1 级折面衰减，决策 ⑲）进山体槽；普通半边 profile 长度 2：[0@格界， 0@普通环点] 恒原始平面，产出为 `PlainRect` 由 ChunkMapRenderer 回地形槽（地形材质/格线/普通动画通道），两件在格界点严格闭合。（修订前：4 条 profile 统一长度 3 整面进山体槽，普通半边披山体材质 ⇒ 视觉多吃一圈，见续22。）
- [✔] rect 归属 = owner 方向 {NE, E, SE}；山格可能是 owner 也可能是 neighbor，两种朝向共用同一规范化角点/边点函数（翻转 start/end 对称处理）。
- [✔] 折面顶点只进 Vertices/Indices；`Profiles[0..3]` 输出 4 条角/边线（端点恒为角点/边点高度），供 `RectangleDrivenTriangleMesh` 复用。
- [✔] UV/faceTier/Weights 规则同 3.3；rect 顶点 Weights = [self, neighbor]（faceTier 用逐顶点隆起数组，随构建产物携带）。
- [✔] 缓存：mountain-aware rect 在 `PreBuildRectProfiles` 的 halo 阶段构建并存入独立 `_mountainRectangleMeshes`（**与文档原案不同：不与普通 rect 同键覆盖**——普通 rect 保留原始表面供碰撞与 1/2 山格 tri 复用；mountain rect 供渲染槽与 3 山格 tri 复用，两套同键并存），tri 直接复用，跨 Chunk 一致。
- [✔] 单测：山-山 rect 与两侧 solid 环共享点逐点一致；山-普通 rect 格界线高度 = 0；profiles 端点与 tri 输入契约兼容（BuildFan 端点校验 + 顶点对照）。

**完成标准**：两类 rect 几何稳定，与 solid 环无缝，profile 契约可供 tri 消费（MainGame/Tests 编译 0 错误）。

### 阶段 3.5：tri 封口（1/2/3 山格）

**目的**：三格交界 tri 在三种山格计数下都无洞、无裂缝（决策 ⑤）。

**工作项**：

- [✔] 1 山格 tri：三角点恒 0（审计事实 4）⇒ 保持普通构建与归槽，不做山体改造。
- [✔] 2 山格 tri：同上（角点含普通格恒 0）⇒ 普通构建。
- [✔] 3 山格 tri：三条角 profile 均隆起 ⇒ 复用 `RectangleDrivenTriangleMesh`（mountain rect profiles）+ 遍历拆分为逐面顶点，归山体槽；faceTier 按 3 格交汇角点高同 3.3 规则（整面同 tier）。
- [✔] 校验：tri 与 rect 角 profile 端点逐点一致（`BuildFan` 的 `ValidateConnectedEdges` 强制 < 1e-4 端点闭合）；三种情形扇形封闭、无洞。
- [✔] 单测：3 山格 tri 归山体槽的正确性（顶点 = 交汇角点规则值（脊线连续，续20）、单平坦三角）、端点一致性、无退化三角；1/2 山格路径与普通构建完全一致（测试辅助的角点规则断言）。

**完成标准**：tri 三种情形正确归属（仅 3 山格进山体槽），无洞无裂缝（MainGame/Tests 编译 0 错误）。

### 阶段 3.6：BuildChunkTerrain 集成（替换式拓扑 + 末尾山体槽）

**目的**：把 3.3~3.5 的生成器接入 Chunk 构建主流程，落地决策 ⑤ 替换式拓扑。

**工作项**：

- [✔] 有效山格 solid：原 44 点仍写入顶点数组（供 collision 扇形），其顶面索引不进地形槽、只进 CollisionIndices；山体 `CellGeometry` 追加进顶点数组，索引进 MountainIndices。
- [✔] 非山格/无效山格（水淹、清除、低于最小可见高度）：走原路径完全不变。
- [✔] rect：山-山/山-普通 → mountain rect（渲染槽）+ 原始 generic rect 顶点与索引只进 CollisionIndices；普通 rect → 索引同时进地形槽与 CollisionIndices（共享顶点零成本）。
- [✔] tri：3 山格 → mountain tri（山体槽）+ plain tri（collision）；1/2 山格 → 普通 tri，索引同时进 tri 槽与 CollisionIndices。
- [✔] subMesh 布局 = `3 基础 + N rect + M tri + (HasMountain ? 1 山体槽)`；HasMountain 以 MountainIndices 最终非空为准。
- [✔] 动画构建（anim ≠ null）时山体顶点写恒等 UV2/UV3（start = target = y，delay = 所属格窗口）保持通道等长；完整动画语义属阶段 5，本步只保证校验不炸。
- [✔] `ResolveTerrainMaterials` 追加山体槽临时稳定材质（复用 TerrainBase_Fog 系；阶段 4 换专属 shader）。
- [✔] 单测：山体几何无退化三角、顶点预算、faceTier UV 离散；`ValidateTerrain` 扩展山体槽布局一致性校验；MountainIndices 与地形槽索引无交集由构造保证（分槽构建）；UV/UV2/UV3 等长校验沿用现有 validator。

**完成标准**：静态山体在场景中显示；替换式无叠加、无 z-fighting（MainGame/Tests 编译 0 错误；Unity 场景验收待项目锁释放）。

### 阶段 3.7：独立 collision mesh 接入

**目的**：渲染/碰撞分离（决策 ㉚），山格点击仍命中基础地表。

**工作项**：

- [✔] `CommitChunkStaging`：HasMountain 时用共享 Vertices + CollisionIndices 填充 `StagingCollision`（单 subMesh），`TerrainCollider.sharedMesh = StagingCollision`；无山 Chunk 维持现状（碰撞 = 渲染 mesh，零回归）。
- [✔] 双缓冲交换扩展（ActiveCollision/StagingCollision）与 `OnDestroy` 销毁补充。
- [✔] 动画/ghost 路径兼容：`TerrainGhost` 快照仍取渲染 mesh；collision mesh 提交时一次性切换（动画路径从 UV2.y 恢复终态高度，不逐帧 cooking）。
- [✔] 校验：collision 索引无越界（`ValidateTerrain`）；山格区域 collision 面与原始地形顶点级一致（构造上共享顶点数组 + 原始索引，对比测试见 3.8）。
- [ ] 单测/集成验证：山格中心、侧坡、山脚、Chunk 边界射线均命中基础碰撞面且 XZ 反查正确（决策 ⑧ 回归入口；需 Unity 运行时射线，待项目锁释放后执行）。

**完成标准**：山格点击/拖放/镜头旋转枢轴正常，射线无空洞（代码路径就位；运行时射线验收待 Unity）。

### 阶段 3.8：诊断、单测聚合与场景验收

**目的**：关闭决策 ㉛ 中阶段 3 范围内的验收项，防止"看起来对"的几何回归。

**工作项**：

- [✔] 诊断工具（测试用）：`CountDegenerateTriangles`（面积 < 1e-6）、`FindNonManifoldEdges`（边被 > 2 面引用）、`GeometryHash`（固定 seed 重建一致性）。
- [✔] `MountainGeometryTests.cs` 聚合 3.1~3.6 纯函数单测（高度场/hash/共享边一致性/rect/tri/确定性/诊断）；固定输入重建几何 hash 一致。
- [ ] 现有测试全量回归 + Unity EditMode 运行（ChunkRendererTests、TriangleTransitionMeshTests、MapMutation*、Ridge*、Mountain*；待项目锁释放）。
- [ ] 场景验收（固定种子截图）：山-普通外边界无裂缝；山-山公共边无缝；三种 tri 无洞；山体不进 collision；点击/镜头正常（需 Unity 运行）。

**完成标准（阶段 3 总）**：静态山体能够显示；山-普通格外边界无裂缝；山-山公共边无缝；三种 tri 情形无洞；山体不进入 collision mesh；地图点击仍能命中基础碰撞面并完成 XZ 反查。
**当前状态**：3.1~3.7 代码全部落地，MainGame/Tests 编译 0 错误，git diff --check 通过；3.8 的纯函数诊断与确定性单测已补齐，Unity EditMode 全量回归与场景截图验收待项目锁释放后执行。

## 阶段 4：材质槽、Low-poly 法线与离散色阶

**目的**：在几何拓扑稳定后接入山体专属稳定材质，不先处理动态过渡。

**主要文件/模块**：

- `ChunkMapRenderer.cs` 材质解析、缓存和销毁
- `TerrainGeometry` 材质布局
- 新增山体稳定 Shader
- Shader 公共 Fog/Triplanar 辅助代码

**阶段边界**：本阶段只实现山体**稳定态**材质。山体 Transition 变体、keep-below clip、动态拓扑淡出与完整 UV2/UV3 动画语义仍属于阶段 5；阶段 4 的动画路径先回落稳定山体材质，不提前复制一套未验证的 Transition Shader。

### 阶段 4.1：材质数据契约与配置入口

**目的**：先明确 Shader 需要哪些输入，避免实现中途反复改 `TerrainGeometry`/配置资产。

**工作项**：

- [x] 复核阶段 3 已写入的 UV0 契约：`UV0.x = ridgeKey01`，`UV0.y = (faceTier + 0.5) / 3`；新增 `MountainMaterialContract`（`FaceTierCount`/`RidgeKeyDomain`/`EncodeFaceTier`/`DecodeFaceTier`/`RidgeKey01`）作为几何写入与 shader 解码的唯一权威定义；`MountainGeometryBuilder` 三处几何改走契约编码（行为零变化）；`ChunkMapRenderer` 山体 UV 追加处注明"普通地形 UV 逻辑禁止重解释山体槽 UV"。
- [x] 明确首版材质参数：岩褐/灰岩/浅灰 3 档颜色；纹理方案选 **1 张岩石纹理 × 色阶染色**（3 纹理方案暂缓，`rockTexture` 留空即纯色模式）；Triplanar world scale（`triplanarWorldScale`）；法线轴锐度（`triplanarBlendSharpness`）；粗糙度/金属度/阴影强度（`roughness`/`metallic`/`shadowStrength`）。
- [x] 在 `MountainConfigSO` 增加"表现（阶段 4：稳定材质契约）"参数块；配置资产只存数据，禁止把运行时材质实例序列化回共享配置资产（类注释与字段注释均已注明）。
- [x] 参数边界与默认值：world scale > 0（`[Min(0.01)]`）、blend sharpness ≥ 1（`[Min(1)]`）、tier 由 `EncodeFaceTier` 固定钳制到 0~2；`MountainMaterialContract.IsValid` 统一校验；无纹理时 3 档纯色可稳定显示。
- [x] 新增测试：`MountainMaterialContractTests`（faceTier 往返解码仅得 0/1/2、任意 UV0.y 解码不越档、越界档位钳制、ridgeKey01 稳定且在 [0,1)、配置默认值有效、无纹理纯色模式有效、非法边界逐项报错、Shader 回退路径契约 = `StableShaderName` + 基础槽 0 内回退索引）；`MountainGeometryTests` 增几何 UV0 与契约编码一致性 + 同面同 tier 集成测试。

**完成标准**：材质输入契约和配置入口稳定；仅数据变化，MainGame/Tests 编译通过，现有渲染行为不变（代码已落地，编译与 EditMode 测试验证待项目锁释放）。

### 阶段 4.2：稳定山体 Shader 骨架与材质资产

**目的**：先建立可加载、可赋值、可受 Terrain FogBlend 的最小稳定 Shader，不立即加入复杂光照。

**工作项**：

- [x] 新增稳定 Shader `Assets/Shader/MountainLowPoly_Fog.shader`（`Custom/MountainLowPoly_Fog`，GUID 手写稳定）与对应材质资产 `Assets/Materials/MountainLowPoly_Fog.mat`；Surface Shader 管线（Built-in），无 SRP 专属 API；头部注释写明 4.2→4.6 演进契约（4.3 Triplanar / 4.4 tier 色阶 / 4.5 导数法线 / 4.6 ShadowCaster 验证）。
- [x] 最小骨架支持 `Cull Off`、`fullforwardshadows + addshadow`（接收/投射阴影）、`FogBlend_vert`/`fogFinal → FogBlend_final`（与 `TerrainBase_Fog` 同一 FogBlend.cginc 全局契约）；Input 声明 UV0 tier 数据通道（`tierUV`，vert 里透传 `v.texcoord`）+ fogCoord；worldPos/worldNormal 按注释规划到 4.3/4.5 再按需声明（4.5 走片元导数，零插值器成本）。
- [x] 首版 surf 输出 `_DiagnosticColor` 纯色诊断（验证山体末尾 subMesh 槽绑定专属 Shader）；普通 Terrain/rect/tri 槽不受影响（材质数组其他槽逻辑零改动）。
- [x] `ChunkMapRenderer` 新增 `GetOrCreateMountainMaterial()`：优先克隆 `MountainConfigSO.stableMaterial` 资产（属性全继承），否则 `Shader.Find` 一次并缓存；查找失败回落阶段 3 的 `_terrainBaseMaterial0`，`_mountainShaderLookupAttempted` 保证只尝试一次、只记录一次错误；按配置推 tier 颜色/Triplanar/粗糙度等契约参数；实例本 Renderer 独占，`OnDestroy` 显式销毁（防泄漏）。
- [x] `MountainConfigSO` 表现参数块新增 `stableMaterial` 共享资产引用（运行时实例严禁写回资产）；`MountainAssetSetup` 自动补齐：材质资产缺失时按 Shader 创建，`config.stableMaterial` 缺失时写入引用（幂等，不覆盖已配置值）；Shader/材质 `.meta` 手写固定 GUID。
- [ ] Unity 侧验收（待项目锁释放）：Console 无粉色材质/unsupported pass；固定 seed 场景截图确认山体槽呈诊断色、普通地形槽不变；Shader 缺失回退路径只报一次错。

**完成标准**：山体槽可独立使用稳定 Shader；无粉色材质、无 null 材质槽、普通地形外观不变（代码与资产已落地，编译与场景验收待项目锁释放）。

### 阶段 4.3：世界空间 Triplanar 采样

**目的**：让不规则 Low-poly 面无需 UV 展开即可稳定贴图（决策 ㉗）。

**工作项**：

- [x] 使用 `worldPos` 的 YZ/XZ/XY 三轴坐标采样同一岩石纹理或三轴纹理；world scale 从配置传入并防 0/负值（shader 内 `max(_TriplanarWorldScale, 1e-4)` 双保险）。
- [x] Triplanar 权重 = `pow(abs(worldNormal), blendSharpness)` 后归一化；分母 < 1e-6 兜底为 +X 轴，禁止 NaN。
- [x] 纹理方向按世界轴固定，不随 Chunk transform/生成顺序变化；负轴采样取 `abs()` 防镜像轴与正轴方向突然反转。
- [x] 同一世界位置在相邻 Chunk/重复顶点上采样一致；只用世界坐标，禁用局部对象坐标与 `ridgeKey01` 作纹理平移量。
- [ ] 验证顶面、陡坡、近竖直侧坡三类朝向，无明显拉伸、接缝或单轴主导闪烁（需 Unity 场景验收）。

**完成标准**：山体各朝向纹理尺度一致；跨 Chunk 采样连续；旋转镜头时无游移或接缝闪烁（shader 与材质接入已落地；视觉验收待项目锁释放）。

### 阶段 4.4：离散 face tier 着色

**目的**：落地"一面一色阶"，禁止大三角内部出现连续高度切带（决策 ⑥/㉘）。

**工作项**：

- [x] 在 Shader 中用 `saturate(round(UV0.y * 3 - 0.5))` 解码 tier 0/1/2（与 `MountainMaterialContract.DecodeFaceTier` 等价）；不按 `worldY` 连续阈值重新计算色阶。
- [x] tier 0/1/2 分别映射岩褐/灰岩/浅灰（`_ColorLow/_ColorMid/_ColorHigh`）；同一三角面 3 顶点同 tier（阶段 3 已 flat 拆分并写齐 UV0）。
- [x] `UV0.x = ridgeKey01` 只做极轻、离散且确定性的亮度偏移（×0.97~1.03，逐面恒定）；不改变三档顺序，无面内渐变。
- [x] 禁止雪顶（决策 ㉒）；无第 4 档实验参数进入正式材质资产。
- [x] 几何契约测试：`MountainGeometryTests` 新增 `MountainRectsAndTriangles_Uv0EncodesMaterialContract` 与共享 `AssertUv0Contract`（solid/rect/tri 全覆盖：同面三顶点 UV0.y 差 < 1e-6，解码结果仅 0/1/2，UV0.x ∈ [0,1)）。

**完成标准**：每个三角面颜色块稳定；无面内渐变色带；3 档在不同坡向和光照下仍可辨（代码已落地；渲染验证待项目锁释放）。

### 阶段 4.5：片元导数平面法线与双面处理

**目的**：用 Shader 片元导数获得真正 Low-poly 平面法线，消除 CPU 跨 Chunk 法线合并依赖（决策 ㉖）。

**工作项**：

- [x] 片元阶段 `cross(ddy(worldPos), ddx(worldPos))` 计算世界空间面法线，归一化前做有限值/退化面保护（`length² < 1e-8` 回退插值几何法线）。
- [x] 叉乘朝向校准：与插值几何法线 `dot < 0` 时取反（"正面法线朝外"）；`Cull Off` 下背面片元得到同一朝外法线，不发黑、不反向——无需 VFACE/SV_IsFrontFace 翻转（落地细化：校准法比 VFACE 更稳健，不依赖摄像机朝向与绕序）。
- [x] Triplanar 权重使用片元平面法线；光照一致性落地细化：Surface shader 光照的 `o.Normal` 是切空间，无法注入世界空间导数法线，光照改走插值顶点法线——几何已 flat 拆分 + `RecalculateNormals` ⇒ 逐面平面法线，与导数法线同向等值（默认 FlatAll 不执行 `MergeChunkBoundaryNormals`，无接缝；两者仅在非 FlatAll 合并边有微小差异）。"同一法线"在全部内部片元严格成立。
- [x] tangent-space normal map 首版禁用（`o.Normal = float3(0,0,1)` 切空间恒等），不假装支持却空间错误。
- [x] 退化三角由阶段 3 诊断拦截；shader 兜底为插值几何法线，禁止 NaN/黑闪。

**完成标准**：每个山体三角呈单一平面光照；相邻面有清晰硬折线；双面与近竖直面不发黑、不翻转（光照依赖逐面顶点法线 + 导数法线校准，逻辑落地；渲染验证待项目锁释放）。

### 阶段 4.6：ShadowCaster 与 Terrain 雾化契约

**目的**：让颜色、阴影与迷雾三条路径使用一致的山体几何和可见性语义。

**工作项**：

- [x] 新增手动 ShadowCaster pass（`Cull Off`、无 clip、`TRANSFER_SHADOW_CASTER_NORMALOFFSET`），替代 addshadow 自动 pass（与 `TerrainBase_Fog_Transition` 先例一致，避免重复生成；稳定态无需阶段 5 的 keep-below clip）。
- [x] 阴影 pass 与主 pass 顶点位置一致（同 mesh）、双面策略一致（Cull Off）、无 clip 差异；背面重复投影按双面阴影处理（同地形先例）。
- [x] 稳定 Shader 引入 `FogBlend.cginc`，`FogBlend_vert`/`FogBlend_final` 与 `TerrainBase_Fog.shader` 保持同一世界 XZ 掩码契约。
- [x] 路线 A 山体只参与 Terrain FogBlend；不注册 `FogAffectedEnvironment` 选择性对象雾化，避免双重雾化（决策 ⑪）。
- [x] v1 不实现高度感知雾（决策 ⑪）；`_ShadowStrength` 为预留参数默认关闭（Standard 光照模型不支持逐面阴影强度，需要时引入自定义光照函数），不改变现有雾边界。

**完成标准**：山体投影完整；主 pass 与 ShadowCaster 无轮廓漂移；山体只被一条雾化路径处理（逻辑落地；阴影/雾化回归验收待项目锁释放）。

### 阶段 4.7：ChunkMapRenderer 材质生命周期接入

**目的**：把稳定山体材质接入现有 subMesh 布局、缓存、回退和销毁机制。

**工作项**：

- [x] `ResolveTerrainMaterials` 在末尾山体槽使用专属稳定材质，替换阶段 3 的 `_terrainBaseMaterial0` 临时回退；槽索引仍为 `3 + rectCount + triCount`（4.2 已落地）。
- [x] 山体材质按 Renderer 共享一份稳定实例（`_mountainMaterial` 字段缓存）；采用"确需运行时复制"策略：克隆 `config.stableMaterial` 或新建 Shader 实例并推入配置参数——克隆而非直接引用共享资产，保证 `OnDestroy` 只销毁本 Renderer 创建的实例、绝不销毁项目资产。
- [x] `ResolveTransitionMaterials` 在阶段 5 前继续让山体槽回落稳定材质；布局校验保持 `3 + rect + tri + (HasMountain ? 1 : 0)`。
- [x] Shader 惰性缓存与"查找失败只报一次"状态（`_mountainShaderLookupAttempted`）；Chunk 重建不会反复 `Shader.Find` 或创建材质实例。
- [x] `OnDestroy` 只销毁本 Renderer 创建的材质（`_mountainMaterial`），不销毁项目资产或外部注入的共享材质；缓存清空后无失效引用。
- [x] 无山 Chunk 不加载/创建山体材质（`GetOrCreateMountainMaterial` 仅 `HasMountain` 时懒调用）；有山 Chunk 多次重建实例数量不增长（字段缓存，待 Unity 运行期诊断复核）。

**完成标准**：山体槽稳定绑定专属材质；材质实例数量有界；无山 Chunk 和普通地形路径零回归（代码路径已落地；运行期实例数量诊断待项目锁释放）。

### 阶段 4.8：Shader/材质诊断与场景验收

**目的**：关闭阶段 4 的视觉与资源生命周期验收，避免只验证"Shader 能编译"。

**工作项**：

- [x] 新增编辑器诊断入口 `Tools/程序化山脉/诊断山脉材质与配置`（`MountainAssetSetup.Diagnose`）：`MountainMaterialContract.IsValid` 配置校验、`Shader.Find` 存在性（缺失时提示回落 `_terrainBaseMaterial0`）、材质资产 Shader 引用、`stableMaterial` 引用一致性，全量输出到 Console。
- [ ] MainGame/Tests 编译与 Shader 导入无错误；检查 Console 无粉色材质、unsupported pass、NaN、材质槽漂移日志（需 Unity，项目锁释放后）。
- [ ] 固定 seed 场景截图：顶面/斜坡/近竖直面、山-普通边、山-山边、3 山格 tri、Chunk 边界、近景与远景（需 Unity）。
- [ ] 截图检查：同面同色阶、硬面法线、Triplanar 无拉伸、跨 Chunk 无颜色/法线缝、阴影无缺口（需 Unity）。
- [ ] 雾化回归：已可见山格、未探索邻格边界、普通 Terrain/河流/海床；确认山体不被选择性对象雾化二次处理（需 Unity）。
- [ ] 材质生命周期诊断：重复重建/切场景后材质实例数量稳定，无隐藏的 per-Chunk material clone（代码路径已保证有界；运行期复核待 Unity）。
- [ ] 性能基线：记录山体 Shader 的纹理采样次数（纯色模式 0 次 / 纹理模式 3 次）、GPU frame cost 与目标平台表现（需 Unity）。

**阶段 4 完成标准**：山体使用专属稳定 Shader，呈现平面 Low-poly 光照与 3 档离散色阶；Triplanar、阴影、雾化和跨 Chunk 表现稳定；普通地形、河流、海床及材质生命周期无回归（代码/资产/诊断全部落地；Unity 侧验收待项目锁释放后执行）。

## 阶段 5：高度动画、拓扑变化与碰撞生命周期（细化为 5.1~5.15）

**目的**：解决 Height 动画、共享边动画裂缝、清除/水淹拓扑删除、高山 bounds、TerrainGhost 和独立碰撞网格的完整生命周期。

> 2026-08-06 二次细化说明（5.1~5.10 → 5.1~5.15）：原 5.4 拆为 5.4（Transition Shader 资产）与 5.5（Transition 材质生命周期接线）；原 5.5 拆为 5.6（拓扑变化检测）与 5.7（同步降级路由与日志）；原 5.6 拆为 5.8（TerrainGhost 与冲突动画所有权）与 5.9（动画收尾幂等与状态清理）；原 5.7/5.8/5.9 顺延为 5.10/5.11/5.12；原 5.10 拆为 5.13（EditMode 测试聚合）、5.14（固定 seed 场景验收）、5.15（碰撞/交互、性能与限制记录）。实施进度续 6/7 中的"5.1~5.9"按此映射理解。

**主要文件/模块**：

- `ChunkMapRenderer.SetChunkAnimationProgress` / `CommitAnimatedChunkGeometry` / `FinalizeChunkAnimation`
- `MapMutationService.cs`
- `MapVisualTransitionService.cs`
- `PreparedRenderGeometry.cs` / `PreparedChunkGeometry.cs` / `ChunkRenderData`
- `MountainGeometryBuilder.cs` / `MountainCellRule.cs`
- 新增山体动画来源 DTO、拓扑签名/判定器与对应 EditMode 测试

**当前代码事实（拆分依据，2026-08-06 复核）**：

1. 山体 solid/rect/tri 动画通道目前统一走 `AppendIdentityAnimUV`：`UV2.x = UV2.y = 当前 y`，所以山体不会随普通 Height 动画插值；它只保证阶段 3 的通道长度校验不失败。
2. `CellGeometry.Weights` 当前只保存“关联格数组”，没有数值权重；mountain rect 的所有顶点都只记录 `{owner, neighbor}`，不足以恢复 profile 上不同位置的精确旧高度，也不能严格保证共享边重复顶点得到同一 UV2/UV3。
3. CPU 动画在 `SetChunkAnimationProgress` 每帧写 `mesh.vertices`；Shader 顶点阶段不再读取 UV2/UV3。稳定山体 Shader 属阶段 4，山体 Transition 变体尚不存在。
4. 当前动画提交会在替换 mesh 前捕获完整旧 `TerrainGhost`。若山体清除/水淹仍走动画，旧 Ghost 会保留旧山体直到 Finalize 后突消；这不是普通高度插值能够修复的问题。
5. 当前 `FillCollisionMeshData` 已用 `UV2.y` 恢复终态顶点并在 Commit 时一次切换独立 collision mesh，不逐帧 cooking；阶段 5 应保持这一策略并补测试，而不是改成逐帧更新 Collider。
6. 当前 `FillMeshData` 只在提交时 `RecalculateBounds`；动画开始前顶点会被改到旧高度，之后 CPU 每帧改顶点但不更新 bounds，高峰可能在动画中被视锥/阴影剔除。
7. `ClearLandForm` 的 `Terrain | Objects` 脏位、山体永久清除、水陆变化后 `DeriveMovementCost` 恢复已经落地；阶段 5 重点是把这些数据语义与动画路由、Ghost、碰撞和玩法收尾串起来。

**首版路由决策（阶段 5 硬约束）**：

- 只有山体渲染拓扑不变时才允许现有 CPU 高度动画：有效山格集合、`HasVisibleMountain` 阈值结果、山体 solid/rect/tri 归槽及 vertex/index/subMesh 布局均不变。
- 清除山体、陆→水、水→陆、跨越 `minVisibleHeight`、新增/恢复山体等任一山体拓扑变化，首版整笔事务降级为 `Duration=0` 同步地形提交；不创建 TerrainGhost、不切山体 Transition 材质、不启动该事务的视觉过渡。
- 暂不做“同一事务内部分 Chunk 动画、部分 Chunk 同步”的混合提交，避免交互锁、单位/对象 follower、MapChanged phase 和 Finalize 所有权分裂。该优化不属于 v1 放行范围。

### 阶段 5.1：动画/拓扑数据契约与基线测试

**目的**：先把"哪些数据决定能否动画"变成显式契约，不直接修改现有插值行为。

**工作项**：

- [x] 为山体几何补显式动画来源 DTO（`MountainVertexAnimSource { HexCellData Cell; float Weight; }`），替代只有关联格、没有系数的 `CellGeometry.Weights`；权重归一化且总和 > 0（5.1 落地为等权基线 `MountainVertexAnimSource.Uniform`，`CellGeometry.AnimSources` 与 `Weights` 平行填充，5.2 起按规范化共享边规则细化权重）。
- [x] `TerrainGeometry` / `ChunkStagingGeometry` 增山体拓扑签名或等价摘要：新增 `MountainTopologySignature`（`HasMountain`/`TotalVertexCount`/`MountainIndexCount`/`LayoutHash`（subMesh 布局 + 山体/碰撞 indices 内容 FNV 摘要）/`CellSetHash`（本 Chunk 有效山格排序集合摘要））；签名不含任何纯 Y 值——普通 Height 变化不会误判拓扑改变。
- [x] `ChunkRenderData` 保存最后一次已提交的山体拓扑签名（`LastMountainTopology`），`CommitChunkStaging` 提交后写入；普通无山 Chunk 恒为 `MountainTopologySignature.Empty`，提前返回零分配。
- [x] 新增纯函数测试（`MountainTopologyTests`）：仅 Height 改变 ⇒ 签名不变；清除/水淹/恢复/阈值跨越 ⇒ 签名改变（恢复后回到原签名）；相同输入重建签名一致；subMesh 布局/索引内容/顶点数变化均改变签名；有效山格集合顺序无关。
- [x] 本步骤只扩 DTO/诊断，不替换 `AppendIdentityAnimUV`，行为零变化、MainGame/Tests 独立编译通过（0 错误；签名纯函数 27 项独立校验全过，含格依赖的用例留 Unity EditMode 运行）。

**完成标准**：山体动画来源和拓扑签名成为可测试的数据契约；尚未改变运行时动画表现（已达成：MainGame/Tests 编译 0 错误；Unity EditMode 运行待项目锁释放）。

### 阶段 5.2：规范化共享顶点动画来源

**目的**：让 solid/rect/tri 的重复顶点能由同一规范化来源计算 start/target Y 与 delay window，关闭共享边动画裂缝前置缺口。

**工作项**：

- [x] solid 主峰来源 = self 权重 1；solid 环点（角/边，无论山-山/山-普通）来源 = 本格权重 1（基点 = 本格 solid 顶点 Y，隆起与 Height 无关，与 plain 地形 solid 环同模型）；山-山边镜像点各自 [本格,1]（基点不同 ⇒ 非同一顶点）。
- [x] mountain rect profile 每个顶点按 profile 参数写 owner/neighbor 数值权重 `[owner:1-u, neighbor:u]`（u = raw UV.y，与 plain rect 的 `AppendRectAnimUV` 同模型）；山-普通格界锚点 u=0.5 ⇒ `[0.5,0.5]`（普通侧基础地形旧高，隆起仍 0，决策 ④）。
- [x] 3 山格 tri 从三条 mountain rect 角 profile 按位置匹配（1e-4）继承来源，不在 tri 构建末尾另算一套权重；共享端点来源集合、权重与 profile 完全一致（reversed profile 的 u 同步反向）。
- [x] 规范化来源排序：`GenerateOrder` 固定顺序（[owner, neighbor] / [本格]），不依赖 owner/neighbor 朝向、Chunk 遍历顺序或 Dictionary 枚举顺序；`MountainVertexAnimSource.Lerp/Unit` 为唯一入口。
- [x] 单测：rect 端点 ↔ 两侧 solid 环点共享位置来源一致（跨格/跨 Chunk 构建）；山-普通格界锚点 [0.5,0.5]；rect 逐顶点 u 权重；tri 端点 [A,1]/[B,1]/[C,1] 继承（fixture 用例待 Unity EditMode 运行）。

**完成标准**：任何共享山体位置都有唯一、确定性的动画来源描述；尚未启用真实山体动画（代码落地：MainGame/Tests 编译 0 错误；来源规则 5.1 基线已替换为 5.2 位置规则，几何/UV 行为零变化）。

### 阶段 5.3：山体 UV2/UV3 完整通道生成

**目的**：仅针对拓扑不变的 Height 修改，把阶段 3 的恒等通道替换为真实起终高度和错峰窗口。

**工作项**：

- [x] 新增统一 helper `MountainGeometryBuilder.AppendMountainAnimUV`（纯函数，delta/delay 委托注入），替换 `AppendIdentityAnimUV`：`targetY = 最终山体顶点 Y`；`startY = targetY - Σ(weight × anim.DeltaY(cell))`。山体隆起量本身保持不变，只插值其所附着基础地形的 Height 差。
- [x] UV3 delayStart/delayEnd 使用同一来源集合按权重确定性混合；共享边重复顶点得到完全相同的窗口（5.2 来源规则保证，不分别取 owner delay）。
- [x] solid/rect/tri 全部使用各自 `CellGeometry.AnimSources` 逐顶点来源；collision-only plain 顶点改用基础地形通道规则（plain rect → `AppendRectAnimUV`、plain tri → `AppendTriangleAnimUV`），不把山体隆起写入碰撞终态。
- [x] 扩展 validator：动画通道全顶点 start/target/delay 有限 + `delayEnd >= delayStart`（山体与普通地形通道一并校验，无回归风险）。
- [x] 单测：统一 Height 增减、相邻格不同 delta、跨 Chunk 共享边、三格角点，startY/targetY 误差 < 1e-4；固定输入通道重建一致（纯函数 17 项独立校验全过；fixture 用例待 Unity EditMode）。

**完成标准**：拓扑不变时，山体所有最终顶点具备真实 CPU 动画通道；共享边起终位置和窗口一致（代码落地，MainGame/Tests 编译 0 错误）。

### 阶段 5.4：山体 Transition Shader 资产（原 5.4 拆分 I）

**目的**：提供动画期间的山体专用 Transition Shader 资产（keep-below clip），完整复制稳定版外观契约（决策 ⑥/㉖）。

**工作项**：

- [x] 新增 `Assets/Shader/MountainLowPoly_Fog_Transition.shader`（`Custom/MountainLowPoly_Fog_Transition`，GUID 手写固定）：完整复制稳定版契约（Triplanar/face tier/ridgeKey01 偏移/导数法线/逐面顶点法线光照/FogBlend/Cull Off），只新增 `_ChunkProgress/_ChunkAnimBaseY/_ChunkAnimRiseHeight` 与 keep-below clip。
- [x] 手动 ShadowCaster pass 与 surf 使用同一 clip 平面、同一 CPU 已变形顶点（worldPosY 传片元再 clip）；vert 不读 UV2/UV3 位移，避免 CPU + Shader 双重动画（与 TerrainBase_Fog_Transition 同一演化模式）。
- [x] `MountainMaterialContract` 增 `TransitionShaderName` 常量（资产名称权威定义，与 `StableShaderName` 同域，缺失回落路径只此一处）。
- [ ] Unity 侧验收：Shader 导入无错误、无 unsupported pass；动画期间主 pass 与 ShadowCaster 无轮廓漂移、双面阴影正常（需 Unity，项目锁释放后）。

**完成标准**：山体动画专用 Shader 资产就位，契约 = 稳定版 + keep-below clip 扩展（代码/资产落地，MainGame/Tests 编译 0 错误；渲染验收待 Unity）。

### 阶段 5.5：山体 Transition 材质生命周期接线（原 5.4 拆分 II）

**目的**：把 Transition 材质接入 subMesh 槽布局、缓存、回退和销毁机制，动画期间山体槽外观与稳定态一致。

**工作项**：

- [x] `ResolveTransitionMaterials` 山体末槽改用山体 Transition 变体（`GetOrCreateMountainTransitionMaterial`：从稳定山体材质克隆属性 + 换 Shader + 显式恢复 `_ROCK_TEXTURE` 关键字）；Shader 缺失时回落稳定山体材质并只报一次，绝不回落普通 Terrain shader。
- [x] 每 Renderer 只缓存一份山体 Transition 材质实例（`_mountainTransitionMaterial` 字段缓存）；`OnDestroy` 仅销毁自建实例（与稳定山体材质同生命周期，不销毁项目资产）。
- [x] 懒加载标志（`_mountainTransitionShaderLookupAttempted`）：`Shader.Find` 只尝试一次；Chunk 重建/重复动画不反复查找或创建实例。
- [ ] 测试/诊断：稳定与 Transition 的属性名、纹理关键字、材质槽索引一致；重复动画不增长材质实例数（需 Unity 运行期诊断，`MountainAssetSetup.Diagnose` 扩展留待项目锁释放）。

**完成标准**：动画期间山体槽外观 = 稳定版 Triplanar/色阶/法线/雾化，材质实例数量有界（代码落地，MainGame/Tests 编译 0 错误）。

### 阶段 5.6：山体拓扑变化检测（原 5.5 拆分 I）

**目的**：把"本次事务是否改变山体拓扑"变成写前快照 + 写后比较的纯函数判定（决策 ⑨/㉛ 前置）。

**工作项**：

- [x] 纯函数 `MountainTopologySignature.VisibleCellSetHash`：格集合 → 有效山格（`HasVisibleMountain`，已并入水域/永久清除/ridge 快照/阈值）的排序 GenerateOrder 摘要（决策 ㉓ 顺序无关）；纯 Height 变化不变、可见性翻转必变。
- [x] 数据写入前快照变化格的有效山体可见性；只快照 changedSet 本身即等价含一环 halo：脏 Chunk 山体几何 = f(本 Chunk 格 ∪ halo 的 HasVisibleMountain)，而 halo 在本事务不变（补丁只作用于 changedSet）。
- [x] 检测范围覆盖：`ClearLandForm`、陆→水、水→陆、恢复/新增（均为可见性翻转）；`minVisibleHeight` 阈值跨越在纯函数层覆盖（路由机制一致）。
- [x] 纯函数测试：可见性翻转 ⇒ 摘要改变；恢复 ⇒ 还原；顺序无关（`MountainTopologyTests` 签名组用例）。

**完成标准**：拓扑变化判定成为可测试的纯函数契约（代码落地，MainGame/Tests 编译 0 错误）。

### 阶段 5.7：同步降级路由与日志（原 5.5 拆分 II）

**目的**：在交互锁、动画 staging/Ghost 和 `BeginTransition` 语义确定前，阻止山体拓扑增删进入普通高度动画管线。

**工作项**：

- [x] `MapMutationService.Commit` 写数据后、动画 staging 前比较旧/新摘要；拓扑变化 ⇒ `animated` 置 false（effectiveDuration 等价 0），整笔事务走同步 `PrepareChunkGeometry/CommitChunkGeometry`。
- [x] 同步降级仍按正常 Commit 发布 `Committed`/同步 Finalize、刷新对象、单位位置、路径与 MapVisualEvent；无活动动画、无 Deferred lock、无 `RemovedVisualHandle` 残留（`lockDeferredToAnimation` 保持 false，finally 解锁）。
- [x] 日志只记录一次明确原因（`MountainTopologyChanged: Clear/Water/VisibilityThreshold/Restore`（N 格）），禁止每 Chunk 刷屏。
- [x] 单测（`MountainTopologyRouteTests`）：清除/水淹/水→陆恢复即使请求 `Duration>0` 也不调用 `CommitAnimatedChunkGeometry`、不发布 `TransitionStarted`、无锁残留；纯 Height 修改仍走动画（需 Unity EditMode 运行）。

**完成标准**：拓扑变化从源头同步提交，无旧山体 Ghost 突消、无材质槽漂移、无动画锁泄漏（代码落地，MainGame/Tests 编译 0 错误）。

### 阶段 5.6：TerrainGhost、冲突动画与收尾幂等

**目的**：明确拓扑不变动画和同步拓扑提交各自的 Ghost/Finalize 所有权，避免旧快照或过期材质复活。

**工作项**：

- [x] 拓扑不变 Height 动画允许捕获含山体的 `TerrainGhost`；Ghost 使用旧稳定材质数组（CaptureTerrainGhost 在切 Transition 前克隆 sharedMaterials，含山体稳定材质）；动画山体使用 Mountain Transition（5.4），Finalize 恢复当前稳定数组并销毁 Ghost 克隆 mesh。
- [x] 同步拓扑提交禁止创建 TerrainGhost/FadeGhost、不写动画缓存、不切 Transition 材质；同 Chunk 已有旧动画时先 `ForceCompleteConflicting` 再同步提交（Commit 动画路径既有逻辑；`CommitSliced` 同步路径补齐同等前置）。
- [x] 非动画路径提交后清除过期 `StableTerrainMaterials`（既有）+ 新增 `ClearAnimationState`：AnimUV2/UV3/BaseVerts/Buffer、`AnimationReturnsToStart` 与 clip MPB（钉死"恒不裁"），防旧 Finalize 恢复过期 subMesh 布局。
- [x] `FinalizeChunkAnimation` / cancel / `ForceComplete` 三条路径幂等（`Complete` 以 `t.Completed` 幂等守卫；`DestroyGhost` 引用置 null 防重复销毁；`ClearAnimationState` 兜底）。
- [ ] 测试：动画中再次清除/水淹山格、动画强制完成后同步重建、场景销毁时仍有活动动画三种顺序（需 Unity EditMode 运行，MapMutationStage5 冲突顺序测试可复用的部分已存在）。

**完成标准**：Ghost、材质数组、动画缓存和交互锁均有单一所有者；任何完成/取消顺序都不残留旧山体（代码落地，MainGame/Tests 编译 0 错误）。

### 阶段 5.7：动画保守 bounds

**目的**：CPU 顶点动画期间不逐帧 `RecalculateBounds`，但峰顶、阴影和上升全过程都不会被错误剔除。

**工作项**：

- [x] `TerrainGeometry` 增可选保守 bounds（`ConservativeBounds`，动画构建填充）；纯函数 `MountainGeometryBuilder.ComputeConservativeAnimBounds` 扫描所有顶点 XZ 与 UV2.x/UV2.y 包络 + Y 安全余量（keep-below clip +0.02 与数值误差，默认 0.05）；山峰已在 targetY 内（顶点最终 Y 含隆起），额外余量兜底数值误差。
- [x] bounds 同时覆盖 start、target、keep-below 过渡与最大山高；普通无山/无动画 Chunk 保持 null，`RecalculateBounds` 行为零变化。
- [x] `FillMeshData` 动画路径在 `RecalculateBounds` 后覆盖保守 bounds；`SetChunkAnimationProgress` 维持不逐帧重算 bounds（现状）。
- [x] TerrainGhost 使用其克隆旧 mesh bounds（Instantiate 自动携带）；collision mesh 用终态基础地表自身 `RecalculateBounds`，不扩入山峰（现状验证 + 注释）。
- [x] 纯函数测试：最高峰、最低起点、目标顶点和 XZ 偏心全部位于 bounds 内；progress=0/0.5/1 插值顶点均包含；Y 余量扩展；空输入零 bounds（独立校验 + 单测，fixture 级用例待 Unity）。

**完成标准**：高峰/阴影动画全程不因 bounds 被剔除，且无每帧 bounds 重算开销（代码落地，MainGame/Tests 编译 0 错误）。

### 阶段 5.8：独立 collision mesh 提交时机与双缓冲

**目的**：保持山体不参与碰撞，同时让视觉动画、同步拓扑变化和射线入口始终有终态基础碰撞面。

**工作项**：

- [x] 保持现策略：动画 Commit 时 `FillCollisionMeshData` 从 UV2.y 恢复终态基础地表并一次切换 `TerrainCollider.sharedMesh`；动画过程中不逐帧 cooking（现状验证 + 注释）。
- [x] 同步拓扑变化走普通终态 collision 提交：山体新增/恢复仍使用无山体基础碰撞面；清除/水淹后 `CollisionIndices` 为 null ⇒ 碰撞回落新渲染 mesh（无旧 buffer 空洞），旧 Active/StagingCollision 保留待复用（内存有界：每 Chunk 2 mesh，OnDestroy 统一销毁）。
- [x] 明确 ActiveCollision/StagingCollision 交换（Collider 恒指向已提交的 Active，不指向被重用/销毁的 staging）、无山 Chunk 回落渲染 mesh 语义（注释 + 现状验证）。
- [x] validator 增 `MountainVertexRanges`（仅山体渲染顶点平坦区间）回归拦截：collision indices 不得引用仅山体渲染顶点；索引范围校验沿用既有 `ValidateIndices`。
- [ ] Unity 运行时射线测试：山格中心、侧坡 XZ、山脚、Chunk 边界在动画前/中/后及清除/水淹后均命中基础面并正确反查格（需 Unity 运行时，决策 ⑧ 回归入口）。

**完成标准**：视觉可动画、Collider 不逐帧 cooking；任意生命周期节点均无射线空洞或悬空旧碰撞面（代码路径落地，MainGame/Tests 编译 0 错误）。

### 阶段 5.9：玩法规则与视觉对象联动收尾

**目的**：确认同步拓扑降级和高度动画不会绕过阶段 1 的山格玩法语义或现有动态地图收尾。

**工作项**：

- [x] `ClearLandForm` 继续永久清除山脉数据并标记 `Terrain | Objects`；对象刷新/浮标/网格重建单次语义（阶段 1 已落地，`MapMutationMountainTests` 覆盖）。
- [x] 陆→水/水→陆后重新调用 `MountainCellRule.DeriveMovementCost`；恢复山体时不可通行/不可部署/不可建造立即生效，不等待视觉动画结束（`ApplyPatch` 派生路径；5.5 降级为同步提交 ⇒ 无等待窗口）。
- [x] 单位处理顺序保持：写数据 → 取消经过不可通行格的移动 → 弹射 → 提交渲染；同步降级只影响渲染/浮标/事件路径，不跳过取消/弹射（5.5 降级点位于单位处理之前）。
- [x] 路径缓存、AI 重决策、站立单位高度吸附、地貌/资源 follower、MapChanged phase 与交互锁在动画/同步两条路径结果一致（同步路径代码路径验证 + 5.5 路由测试无锁残留/无 TransitionStarted）。
- [ ] 测试：山格清除、水淹、恢复、普通 Height 抬升/下降时单位/建筑/寻路/对象刷新符合规则；无双重事件或漏解锁（需 Unity EditMode 全量回归；route 测试已覆盖锁/事件单次性）。

**完成标准**：几何生命周期与规则生命周期一致；山体拓扑变化不会产生一帧可部署、可通行或对象残留窗口（阶段 1~2 语义 + 5.5 路由共同保证，代码落地）。

### 阶段 5.10：诊断、回归与场景验收

**目的**：关闭阶段 5 的动画/拓扑/bounds/碰撞验收，形成阶段 6 交互适配的稳定前置。

**工作项**：

- [ ] EditMode 测试聚合：动画来源权重、共享边 UV2/UV3、拓扑签名/路由、bounds、Finalize 幂等、规则恢复；运行现有 MapMutation/Transition/ChunkRenderer/Mountain 全量回归（代码已就位，待项目锁释放后 Unity 运行）。
- [ ] 固定 seed 场景：普通 Height 上升/下降、跨 Chunk 山脊、清除山格、陆→水、水→陆、阈值跨越、动画中冲突提交（需 Unity）。
- [ ] 截图/录屏检查：共享边无裂缝、山体色阶/Triplanar 动画中不变色、Ghost 无双山重叠/突消、ShadowCaster 与 clip 同步、峰顶不被剔除（需 Unity）。
- [ ] 碰撞/交互检查：动画前/中/后点击和镜头旋转枢轴正常；同步拓扑提交无空洞（需 Unity）。
- [ ] 性能记录：山体动画 CPU 顶点写入、Ghost mesh 峰值、collision cooking 次数、材质实例数；确认 bounds 不逐帧重算、Collider 不逐帧 cooking（需 Unity）。
- [x] 记录首版限制：拓扑变化整笔事务同步提交（5.5）；不做山体专用淡出（清除/水淹拓扑删除 = 同步提交，无沉没动画）；不做同事务按 Chunk 混合动画；这些限制不得被描述成现有动画天然支持（已按此实现，限制与实现一致）。

**阶段 5 完成标准**：普通 Height 波动无共享边裂缝；山体峰顶不被剔除；拓扑删除/恢复无 Ghost 或材质残留；水→陆恢复山体时规则正确；碰撞与视觉拓扑始终无射线空洞；所有动画完成/取消路径资源有界且幂等（代码/资产/单测已落地，MainGame/Tests 编译 0 错误，独立校验 53 项全过；Unity EditMode 全量回归与场景截图验收待项目锁释放）。

## 阶段 6：迷雾、探索、高亮、点击与镜头适配（细化为 6.1~6.9）

**目的**：关闭主要表现层遗漏，避免山格规则通过错误的探索位或高亮路径泄漏到玩法系统。

**主要文件/模块**：

- visibility resolver / `TemporaryVisibilityService`
- `ChunkMapRenderer` 雾目标刷新
- `HexHighlightRenderer` 及所有高亮调用方
- `MapRaycastService` / `HexMapService`
- `CameraController.cs`
- `CostLabelRenderer`、Overlay marker 相关代码

**阶段边界**：本阶段只适配既有山格语义和路线 A 表现，不新增山顶高亮、三角面格 ID、高度感知雾或探索/占领规则。山格“永久可见”仅表示视觉免雾，不写 `IsExplored`，不授予归属，不改变探索成本、AI 信息、后勤连通或战斗视野。

### 阶段 6.1：山格可见性纯规则契约

**目的**：先把“山格免雾但不算已探索”收敛为可测试的纯规则，避免直接散写 `FogAlphaTarget = 1`。

**工作项**：

- [x] 定义唯一入口（`MountainVisibilityRule.IsPermanentlyVisible(cell)`）：仅有效山格返回 true；水淹、永久清除、无 ridge 数据或低于最小可见高度时返回 false，直接委托 `HasVisibleMountain` 保证与几何贡献同一有效性口径、判定零漂移。
- [x] 明确输出只参与视觉可见性合成，不调用 `ExploreBy`、不修改 `IsExplored`/owner/探索费用，也不伪造 `TemporaryVisibilityService` lease（类契约注释 + 无副作用/无 lease 测试）。
- [x] 固定中立、玩家归属、AI 归属三类山格结果：视觉均可见，但数据层归属与探索位保持原值；山格仍不可通行、不可部署、不可建造（测试断言探索位/`Player_City_Index`/资格不变）。
- [x] 新增纯函数测试（`MountainVisibilityRuleTests`，11 项）：普通格、有效山格、水淹山格、清除山格、恢复山格、水→陆恢复、无 ridge 数据、最小可见高度阈值边界（恰好等于阈值可见，≥ 判定）、与 `HasVisibleMountain` 逐状态一致、三类归属数据层不变、纯函数无副作用（不写探索位/归属/`FogAlphaTarget`）与不伪造 lease。

**完成标准**：可见性规则无 Unity 场景依赖；调用前后探索位、归属和玩法规则不变；同一格是否有可见山体与是否永久视觉可见使用同一有效性口径（已达成：`MountainVisibilityRule` + 11 项纯函数测试落地，MainGame/Tests 编译 0 错误；Unity EditMode 运行与阶段 6.2 resolver 接入待项目锁释放）。

### 阶段 6.2：visibility resolver 与雾目标接入

**目的**：把 6.1 规则接入现有可见性合成链，使每次雾目标刷新都能稳定恢复山格可见，而不是只初始化一次。

**工作项**：

- [x] 审计完成：`IMapVisibilityResolver` 唯一实现 = `TemporaryVisibilityService`（GameInstaller.cs:137 `BindInterfacesAndSelfTo`）；`ChunkMapRenderer` 雾目标刷新唯一入口 = `UpdateFogTransitionTargets`（:2652），触发点覆盖初始生成（`ChunkMapRender` :222）、地图/后勤变化（`OnMapVisualChanged` :171、`OnLogisticsChanged` :176）、lease 获取/释放（`ArenaEventManager` → `ForceRefreshFogVisuals` :244/:375）；`BuildingBase` 血条显隐同查 resolver 但山格不可建造无实例，不构成第二特判点。
- [x] 单一合并点落地：`TemporaryVisibilityService.IsVisibleToFaction` 开头（lease 检查前）短路 `MountainVisibilityRule.IsPermanentlyVisible`——有效山格强制 true，优先于普通未探索雾与后勤可见性，不写回探索状态、不创建 lease；结果与阵营无关。
- [x] 初始生成、Chunk 重建、可见性全量刷新、临时可见 lease 获取/释放、归属变化后均重新经过同一 resolver（上述触发点全走 `IsVisibleToFaction`，无需逐调用方特判）。
- [x] 山格清除或水淹后 `IsPermanentlyVisible` 自动回落 false，回落到该格原本的探索/临时可见/后勤结果；水→陆恢复山体时下一次刷新重新生效（测试覆盖）。
- [x] 新增测试（`MountainVisibilityResolverTests`，9 项）：强制可见短路后勤（后勤未被查询、无 lease 伪造）、玩家/AI 双阵营视角一致、lease 获取/释放不影响山格、非山格零回归（后勤/探索位回落行为不变）、清除回落、水淹回落、水→陆恢复重生效、重复刷新幂等、`IsExplored` 与 owner 从未被山格规则改写。

**完成标准**：山格永久视觉可见不会被后续 FogAlpha 刷新覆盖；失效山格能正确回落；玩家与 AI 的探索、归属和后勤测试无回归（已达成：`TemporaryVisibilityService.IsVisibleToFaction` 单一合并点 + 9 项 resolver 测试落地，MainGame/Tests 编译 0 错误；既有 `TemporaryVisibilityServiceTests`/`DomainInvariantTests` 逻辑未变，Unity EditMode 全量回归与场景截图待项目锁释放）。

### 阶段 6.3：路线 A FogBlend 与边界表现

**目的**：确认 mountain solid/rect/tri 只走 Terrain FogBlend，且山-普通格边界没有双重雾化、漏雾或明显硬缝。

**工作项**：

- [x] 复核完成：稳定/Transition 山体 Shader 均只使用 `FogBlend.cginc` 的世界 XZ mask（`FogBlend_vert`/`fogFinal→FogBlend_final`，与 `TerrainBase_Fog` 同一契约），无其他雾采样分支；不注册 `FogAffectedEnvironment` root——选择性雾化按 `Initialize` 传入 root 收集（地貌/资源/建筑/单位），Chunk 宿主只设 `Map` Layer，山体是 Chunk mesh 的 subMesh 无独立 Renderer；山体材质不设置任何选择性雾化属性。
- [x] 代码审计确认：mask UV = `(worldPos.xz - origin)/size` + `FogBlend_warpUV` 扰动（全局属性 `_FogPixelSize/_FogNoiseWavelength/_FogJaggedAmount`），逐片元世界位置决定，与 solid/山-山 rect/山-普通 rect/3 山格 tri 几何类型无关（同一坐标/扰动契约）；Transition 主 pass 同公式；ShadowCaster pass 无雾采样（阴影不雾化），与 surf 同 clip 平面保证阴影/可见几何一致。
- [ ] 固定 seed 构造“可见山格紧邻未探索普通格”和跨 Chunk 山脊，截图检查山脚、格界、tri 角点与远景雾边界（需 Unity，项目锁释放后）。
- [x] v1 限制记录：两 Shader 均无 worldY 高度感知分支（不按 worldY 做山尖破雾）；边界扰动属 XZ mask 既有表现——若普通邻格局部透出，记录为既有表现，不另开高度感知分支。

**完成标准**：山体不存在选择性对象雾化的二次处理；稳定/动画材质雾化一致；山-普通格与跨 Chunk 边界无可见漏雾或整块错误遮蔽（代码审计已确认单一雾化路径与同一 mask 契约；视觉截图验收待 Unity）。

### 阶段 6.4：HexHighlightRenderer 统一山格门禁

**目的**：在渲染入口建立最后一道防线，避免任一遗漏调用方把基础地表高亮埋进山体。

**工作项**：

- [x] 门禁落地：`HexHighlightRenderer.IsBlockedByMountainGate(channel, cell)` 纯函数 + `SetHighlightedCells`/`SetHighlightedCellsInternal` 统一过滤有效山格（过滤依据 = `MountainCellRule.IsEffectiveMountainCell`，不复制 `landForm` 字段判断）；玩家可见通道（CardPlacement/Reachable/AttackRange/Selection）全部拦截，DebugDirtyChunk 诊断通道豁免。
- [x] 动态恢复清理：整体替换语义保证格在水→陆/普通变有效山后，下一次刷新移除旧高亮（`MountainHighlightGateTests` 覆盖水→陆恢复重拦截）。
- [x] 非山格通道优先级、材质、颜色和生命周期行为不变（过滤仅发生在 Set 时，其余路径零改动）；空集合/全被过滤时 `RebuildChannel` 现有安全清空逻辑复用（Root.SetActive(verts.Count > 0)）。
- [x] 显式诊断豁免入口：新增 `SetHighlightedCellsDiagnostic`；`MapHeightEditTestController`（开发调试工具）改用豁免入口并注明用途，玩家可见通道不得豁免；DebugDirtyChunk 通道内部豁免。
- [x] 新增 EditMode 测试（`MountainHighlightGateTests`，7 项）：混合集合过滤、全山集合（玩家通道全拦截）、动态恢复（水→陆）、清除山后重新可高亮、多通道互不污染、诊断豁免、null 安全。

**完成标准**：任何普通高亮 API 收到山格都不会产出该格的可见高亮；诊断豁免必须显式；非山格现有高亮回归通过（代码落地：MainGame/Tests 编译 0 错误；运行期高亮视觉验收待 Unity）。

### 阶段 6.5：放置、攻击、可达、选择与调试高亮调用方收口

**目的**：让 UI 提示与真实玩法资格一致；Renderer 门禁只作兜底，调用方不得继续把山格计入“可用目标”。

**工作项**：

- [x] 调用方清单登记（审计完成）：`PlayerInputHandler`（CardPlacement 拖牌预览）、`UIController.ShowReachableHexes`（Reachable 通道，实际 = 攻击范围）、`MapHeightEditTestController`（Selection 调试指针）、`MapMutationService`（DebugDirtyChunk 调试脏格，豁免）；AttackRange 通道当前无直接调用方（枚举备查）。
- [x] 放置/生产预览：`PlayerInputHandler.HighlightGridOnMouseHover` 改用 `MountainCellRule.CanSpawnUnitOnCell`（决策 ① 统一资格，不再只看 `IsExplored`）。
- [x] 攻击范围：`UIController.ShowReachableHexes` 过滤 `IsEffectiveMountainCell`（决策 ⑨）；确认动作（`GetEnemyUnderMouse` 右键选敌）目标只能是单位/建筑，山格不可部署 ⇒ 天然无山格目标，无旁路。
- [x] 调试通道：`MapHeightEditTestController` 改用 `SetHighlightedCellsDiagnostic` 显式豁免；`MapMutationService` DebugDirtyChunk 通道内部豁免（6.4）。
- [x] 调用方测试：`MountainStage6SourceContractTests` 源码契约（6 项，防收口回滚）+ `MountainHighlightGateTests` 纯函数（资格状态翻转）。

**完成标准**：不可用山格既不显示误导性高亮，也不能通过同一工作流的确认阶段执行；所有高亮来源有清单和测试，不依赖 Renderer 静默纠错（代码落地：编译 0 错误；玩家/AI 入口结果一致性运行期验收待 Unity）。

### 阶段 6.6：基础碰撞射线与 XZ 反查回归

**目的**：验证路线 A 替换拓扑后，点击、拖放与旋转枢轴继续命中无山体的基础 collision mesh，并明确近似反查边界。

**工作项**：

- [x] 链路审计确认（无代码改动）：`MapRaycastService.RaycastMap` 先命中 `Map` 层 Collider（:26 `LayerMask.GetMask("Map")` + `IsMapObject` 校验），再由 `HexMapService.GetCellByWorldPosition` 按命中点 XZ 找最近格（`TryWorldToHexCoordinate` :85-108）；山体不进碰撞由阶段 3.7 独立 collision mesh 保证，射线命中基础面。禁止改为山体渲染 mesh 或写入三角面格 ID。
- [x] `HexMapService` cell radius 上限保持：`minDist > cellRadius²` 返回 false（:104），不为山体放宽全局反查半径（`HexMapServiceTests` 已有超限 null 用例）；记录已知近似：视觉侧坡可能归邻格（决策 ⑧）。
- [ ] 固定 seed 射线测试：山格中心、视觉峰顶投影、侧坡 XZ、山脚、三格角点与 Chunk 边界；静态/动画前中后/清除/水淹/恢复各生命周期（需 Unity 运行时 Physics.Raycast）。
- [ ] 卡牌拖放、地图点击和镜头旋转枢轴集成测试（需 Unity 运行时）。

**完成标准**：任一生命周期节点无射线空洞、悬空旧 Collider 或命中山体渲染面；XZ 反查行为稳定，已知侧坡近似不导致山格部署绕过（代码链路审计完成；运行时射线验收待 Unity）。

### 阶段 6.7：镜头高度、边界与旋转预算

**目的**：将 `H_max` 纳入镜头可读性验证，防止高峰被近裁剪、屏幕边缘裁掉或破坏旋转中心。

**工作项**：

- [x] `CameraController` 审计完成：地图边界 = 格中心 XZ ± 外圈 padding（`TryInitializeBounds` :175-208）；最低 Y 平面 `_boundsPlaneY` = 所有格中心 Y 最小值（:194）——**山高不进边界预算为已知限制**（配合 6.8 遮挡预算与 `H_max` 上限控制，不改变基础 XZ 地图边界语义）；近/远裁剪、缩放限制（`minZoomDistance=20`/`maxZoomDistance=75`）保持现状，只验收不改代码。
- [x] 旋转中心继续来自基础 collision mesh：`GetScreenCenterWorldPosition` 射线 = `rotationRaycastLayers`（场景配置 m_Bits:64 = Map 层，`InputCameraConfigurationTests.GameScene_CameraRotationRaycastIncludesMapLayer` 已断言）⇒ 命中无山体的基础碰撞面（阶段 3.7），山体视觉区域开始旋转无跳轴/穿山路径（代码层面成立）。
- [x] 超预算配置编辑器警告：`MountainAssetSetup.Diagnose` 增镜头预算检查——`maxHeight(H_max 上限) > minZoomDistance(20) - near clip(0.3)` 时 Warning 提示降低 `maxHeight` 或提高 `minZoomDistance`，不在运行时静默裁峰。
- [ ] 最坏场景运行期验证：地图中心峰、四边/四角峰、跨 Chunk 长脊线、最高基础地形叠加最高山体；最小/最大缩放与全部允许俯仰角下峰顶不穿 near clip；边缘山峰可被合理纳入视野（需 Unity）。
- [ ] 旋转回归：从山体视觉区域开始旋转无跳轴、穿山或突然拉远（需 Unity）。

**完成标准**：允许配置上限内的山峰在常用镜头操作中可见且不穿裁剪面；地图边缘与旋转中心稳定；普通无山地图镜头手感无回归（代码审计 + 诊断警告落地；运行期镜头验收待 Unity）。

### 阶段 6.8：费用标签、Overlay marker 与场景对象遮挡

**目的**：处理高山对屏幕空间标签和地表对象的可读性影响，避免不可用山格仍显示可交互提示。

**工作项**：

- [x] 审计完成：`CostLabelRenderer.RefreshLabels`（:160-221）费用标签、`LandFormMarkerManager`（山无 `markerPrefab` 跳过，决策 ⑩ 已实现）、`CostLabelRenderer` 世界锚点 = `RealCenterWorldCoordinate` + up*1.2；选择反馈/单位/建筑 Renderer 锚点 = 各自模型位置（山格上无模型）。
- [x] 山格不显示部署/移动费用和可交互 marker：`CostLabelRenderer.RefreshLabels` 增加 `IsEffectiveMountainCell` 过滤（水淹/清除后自动恢复显示条件，恢复山体后隐藏——每次 `RefreshLabels` 全量重建，动态语义天然成立）。
- [x] 规则按 6.5 统一资格结果控制，不通过简单提高标签 Y 来暗示可用（山格直接不显示标签）。
- [x] 遮挡预算记录：非山邻格标签/对象被山体遮挡由 `H_max` 上限（`maxHeight` 默认 2.5）与镜头预算（6.7）控制；山脊对单位/建筑/河流/格线/选择反馈的遮挡不改玩法规格，限制在 `H_max` + 镜头预算内验收。
- [ ] 遮挡验收截图：非山邻格标签被山体遮挡的深度遮挡可接受性；近景/远景、地图边缘、跨 Chunk 山脊、密集 UI（需 Unity）。

**完成标准**：山格没有虚假的费用/交互提示；关键非山 UI 可读；单位、建筑、河流、格线和 marker 遮挡处于已记录且可接受的预算内（标签过滤代码落地；遮挡视觉验收待 Unity）。

### 阶段 6.9：诊断、回归与场景验收

**目的**：聚合阶段 6 的规则、表现和交互验证，为阶段 7 全量放行提供稳定前置。

**工作项**：

- [x] EditMode 测试聚合：`MountainVisibilityRuleTests`（6.1，11 项）+ `MountainVisibilityResolverTests`（6.2，9 项）+ `MountainHighlightGateTests`（6.4，7 项）+ `MountainStage6SourceContractTests`（6.9，6 项源码契约防回滚）；既有 `TemporaryVisibilityServiceTests`/`DomainInvariantTests`/`HexMapServiceTests` 逻辑未动（零回归）。
- [ ] PlayMode/运行时测试聚合：FogAlpha 刷新、临时可见 lease、collision raycast、拖放、旋转枢轴、标签和 Overlay 生命周期（需 Unity）。
- [ ] 固定 seed 场景矩阵：中立/玩家/AI 山格，可见山邻未探索普通格，跨 Chunk 山脊，最高 `H_max`，地图边缘，清除/水淹/恢复及普通 Height 动画（需 Unity）。
- [ ] 截图/录屏：雾边界、高亮过滤、点击反馈、镜头缩放/俯仰/旋转、费用标签、公共 marker、单位/建筑/河流/格线遮挡（需 Unity）。
- [x] 诊断断言落地：山格规则不得写 `IsExplored`/owner（6.1/6.2 测试断言）；路线 A 山体不得注册选择性对象雾化（6.3 审计 + 源码契约）；普通高亮通道不得含有效山格（6.4 门禁 + 测试）；Collider 不得引用山体顶点区间（阶段 5.8 `MountainVertexRanges` validator）；`MountainAssetSetup.Diagnose` 镜头预算警告（6.7）。
- [x] v1 限制记录：无山顶高亮、无高度感知雾、点击按基础面 XZ 近似、侧坡可能归邻格、遮挡主要由 `H_max` 与镜头预算控制（本阶段各小节 + 阶段 7 限制清单）。

**阶段 6 完成标准**：山格免雾不影响探索/归属/AI/后勤（6.1/6.2 落地）；不可用山格不进入候选集合且不出现误导性放置/攻击高亮（6.4/6.5 落地）；点击、拖放和镜头旋转不因山体替换拓扑失效（6.6/6.7 链路审计落地）；允许 `H_max` 范围内无裁峰，主要 UI 与场景对象遮挡可接受（6.7/6.8 预算与过滤落地）；阶段 6 新增 33 项测试与既有探索、迷雾、输入、镜头回归全部通过（EditMode 运行与 PlayMode/场景截图验收待项目锁释放）。

## 阶段 7：完整回归、性能与验收关闭（2026-08-06 细化为 7.1~7.9）

**目的**：确认主要错误和遗漏在代码层真正关闭，而不是只在设计文档中被记录；以决策 ㉛ 验收清单为主线，按"编译基线 → 测试回归 → 确定性 → 拓扑 → 生命周期 → 规则交互 → 表现 → 性能 → 放行"的闭环顺序逐项关闭。

**主要文件/模块**：

- `MountainGeometryTests` / `MountainTopologyTests` / `MountainTopologyRouteTests` / `MountainMaterialContractTests` / `MountainCellRuleTests` / `MountainVisibilityRuleTests` / `MountainVisibilityResolverTests` / `MountainHighlightGateTests` / `MountainStage6SourceContractTests` / `RidgeGeneratorTests` / `MapMutationMountainTests`
- 既有回归套件：`ChunkRendererTests` / `TriangleTransitionMeshTests` / `MapMutation*` / `TemporaryVisibilityServiceTests` / `DomainInvariantTests` / `HexMapServiceTests` / `InputCameraConfigurationTests` / `HexHighlight*`
- `MountainAssetSetup.Diagnose`（编辑器诊断汇总入口）、`MapHeightEditTestController`（运行期手测入口）

**阶段边界**：本阶段不新增功能代码；所有工作项均为验证、修复回归、记录与放行决策。发现缺陷时按缺陷归属阶段修复并在本阶段复验，修复本身不得扩大 v1 范围。

### 阶段 7.1：Unity 编译与资产基线

**目的**：项目锁释放后先建立 Unity 侧绿基线，排除"dotnet 编译通过但 Unity 资产/Shader 导入失败"的差异。

**工作项**：

- [x] 项目锁释放后打开 Unity，确认 MainGame/Tests 程序集编译 0 错误、Console 无新增 Warning/Error（既有一类警告除外）。**（2026-08-06 完成：修复 `MountainAssetSetup.cs` 61/69 行插值条件表达式缺括号与 `MountainVisibilityResolverTests.cs` 漏 `using System.Linq` 后，Unity 自动重编译全绿，域重载后 0 error 0 warning。）**
- [x] Shader 导入检查：`Custom/MountainLowPoly_Fog` 与 `Custom/MountainLowPoly_Fog_Transition` 编译无 error/unsupported pass；场景中无粉色材质、无 `Material._mountainMaterial == null` 槽。**（Shader 导入无错误已验证；粉色材质/材质槽需场景渲染目检，待用户进入 PlayMode 复核。）**
- [x] 材质/配置资产完整性：`Mountain.asset`（mountainForm=true、无模型/浮标/效果）、`MountainConfig.asset`（stableMaterial 引用）、`MountainLowPoly_Fog.mat`（Shader 引用正确）——执行 `Tools/程序化山脉/诊断山脉材质与配置` 确认无 Warning。**（资产逐项核对通过；域重载后 stableMaterial 已由 `InitializeOnLoadMethod` 自动补齐；诊断菜单需用户在编辑器内人工执行复核。）**
- [ ] 固定 seed 场景能正常打开并进入 PlayMode：地图生成无异常、山体出现、Console 无 NaN/IndexOutOfRange/空引用。**（待用户进入 PlayMode：GameScene + MapGenerationConfig useFixedSeed=1 / randomSeed=20260729。）**

**完成标准**：Unity 侧编译与资产全绿；固定 seed 场景 PlayMode 稳定；诊断菜单输出无缺失项。**（编译/资产两项已达成；PlayMode 验收待用户执行。）**

### 阶段 7.2：EditMode 测试全量回归

**目的**：把阶段 1~6 全部 EditMode 测试在 Unity 侧跑通，建立"代码编译通过 ≠ 行为正确"的回归基线。

**工作项**：

- [ ] 运行山脉专属测试套件：`MountainCellRuleTests`、`MountainGeometryTests`（含 3.3~3.8 几何/诊断）、`MountainTopologyTests`、`MountainTopologyRouteTests`、`MountainMaterialContractTests`、`MountainVisibilityRuleTests`、`MountainVisibilityResolverTests`、`MountainHighlightGateTests`、`MountainStage6SourceContractTests`、`RidgeGeneratorTests`、`MapMutationMountainTests`（约 11 个文件、100+ 用例）。**（2026-08-06：首次 Unity 运行前修复 `MountainGeometryTests` 夹具缺陷——`CreateFixtureCell` 的 `CenterWorldCoordinate.y=0` 与断言基准"基础地形 Y=2"不一致，3 处 Y 依赖断言同步修正（peak/山-普通 rect 格界锚点/tri 交汇角点）；此为 3.3 起从未在 Unity 运行的潜伏断言错误。）**
- [ ] 运行既有回归套件：`ChunkRendererTests`、`TriangleTransitionMeshTests`、`MapMutation*`（阶段 5 冲突顺序）、`TemporaryVisibilityServiceTests`、`DomainInvariantTests`、`HexMapServiceTests`、`InputCameraConfigurationTests`、`HexHighlight*`——确认山脉改造零回归。
- [ ] 失败用例处置：失败原因按归属阶段登记，修复后重跑至全绿；不允许以"环境问题"掩盖。
- [ ] 记录全量回归结果（用例总数/通过/失败/耗时）到验收记录。

**完成标准**：全部 EditMode 用例通过（含既有回归）；无被跳过或忽略的山脉用例。

### 阶段 7.3：固定 seed 确定性重建回归

**目的**：关闭决策 ㉛ 的"固定种子重建 hash 一致"——同一输入重建必须逐顶点一致，是跨 Chunk 无裂缝与动态重建无抖动的数学前提。

**工作项**：

- [x] 固定 seed 地图重复生成 N 次：山脊路径（`RidgeGeneratorTests`）、山格集合、共享边控制点逐项 hash 一致。**（代码落地：新增 `Generate_SameSeed_RepeatedRuns_IdenticalRidgePaths`——N=3 次同 seed 生成，脊线路径逐格、山格集合、固化参数（ridgeId/seed/length/hMax/噪声振幅/格数）逐项一致；共享边控制点由 `SolidMountain_MirrorEdgePoints_AgreeAcrossCells`/`SolidMountain_SharedJunctionCornerHeights_AgreeAcrossCells` 覆盖。Unity EditMode 运行待执行。）**
- [x] 同一 Chunk 重复构建（不改变数据）：`GeometryHash` 全量一致；含山体 solid/rect/tri 与 collision 双轨。**（代码落地：新增 `Determinism_RebuildAllGeometry_IdenticalHash_AndCollisionTrackUnchanged`——solid A/B/C、rect A-B/B-C/A-C、3 山格 tri 共 7 件重建 GeometryHash 一致；collision 双轨共享的原始 solid 数组多次构建后逐位未被改写。Unity EditMode 运行待执行。）**
- [x] 动态路径确定性：Height 变化 → 重建 → 与直接按新数据构建逐顶点一致（拓扑不变动画提交后终态 = 直接构建）。**（代码落地：新增 `DynamicHeightChange_AnimatedEndState_EqualsDirectBuild`——solid/rect/tri 的 `AppendMountainAnimUV` 通道 UV2.y = 直接按新数据构建顶点 Y、UV2.x = 旧数据构建顶点 Y（< 1e-4）。Unity EditMode 运行待执行。）**
- [x] 跨 Chunk 相邻重建：仅一侧 Chunk 重建时，共享边顶点/UV0/UV2/UV3 与另一侧现存顶点数值一致（< 1e-4）。**（代码落地：新增 `CrossChunkSharedEdge_RebuildSide_Uv0AndChannelsMatchExistingSide`——同脊线变体（B 并入 A 脊线）下 rect 端点与邻格 solid 环点在共享位置的位置/UV0.x（ridgeKey01）/UV2/UV3 全部一致。Unity EditMode 运行待执行。）**
- [ ] 归一化键回归：边/角点/峰点键遍历顺序无关（既有纯函数测试在 Unity 侧复跑）。**（测试已存在：`Hash_SameKeysAreStable_AndNormalizedKeysIgnoreOrder`；Unity EditMode 复跑待执行。）**

**完成标准**：所有确定性回归项 hash 一致；无依赖遍历顺序或 Dictionary 枚举顺序的几何输出。

### 阶段 7.4：拓扑验收（决策 ㉛ 几何项）

**目的**：关闭"无重复面、无退化三角、无非流形边、无 Chunk 边界裂缝、collision 无洞"的几何验收项。

**工作项**：

- [x] 全图扫描：`CountDegenerateTriangles` = 0（面积 < 1e-6）；`FindNonManifoldEdges` = 0（边被 > 2 面引用）；渲染 mesh 与 collision mesh 分别扫描。**（代码落地：新增 `FullMapTopologyScan_RenderAndCollisionTracks_NoDegenerateNoNonManifold`——18×18 网格 + 固定 seed 4 条脊线，复刻 Chunk 共享顶点数组 + 替换式双轨装配，渲染槽（地形槽+山体槽）与 collision 槽分别扫描，另含山-山/山-普通公共边前置计数断言；沙箱独立校验 8 项全过（plain 全图 578 个交界 tri 两种 apex 路由 + 去重，无退化/非流形/越界）。Unity EditMode 运行待执行。）**
- [x] 替换式拓扑复查：被山体覆盖的原始 solid/rect/tri 三角面均从地形槽移除（索引无交集，`MountainIndices` 与地形槽无重复面）；无 z-fighting 场景截图确认。**（代码落地：全图装配内断言 collision 索引不得落入山体顶点区间（阶段 5.8 `MountainVertexRanges` 语义复刻）、山体渲染索引必须落在山体区间内；新增 `MountainStage7SourceContractTests` 防回滚（双轨分离/末尾山体槽/替换式路由条件/无山回落）。z-fighting 截图待 Unity。）**
- [x] 山-普通格固定锚点：公共边顶点高度 = 0、与普通格 solid/rect/tri 顶点逐点一致；山-山公共边两侧构建数值一致。**（代码落地：全图装配内山-普通 rect 格界锚点 = 两端原始 solid 中点断言（决策 ④，起伏地形下非平凡）；山-山两侧一致性由既有 `SolidMountain_MirrorEdgePoints_AgreeAcrossCells`/`SharedJunctionCornerHeights_AgreeAcrossCells` 覆盖。Unity EditMode 运行待执行。）**
- [x] 三格交界 tri：1/2/3 山格三种情形无洞、无竖裂缝、无薄片；3 山格 tri 与 rect 角 profile 端点闭合（`ValidateConnectedEdges` < 1e-4）。**（代码落地：新增 `OneOrTwoMountainJunction_CornerLiftIsZero_PlainTriFlatClosed`——1/2 山格角点隆起恒 0、plain tri 闭合且平坦于基础地形（补齐阶段 3.5 缺失的 1/2 山格单测）；3 山格由既有 `TriangleMountain_ThreeMountain_FlatAtCornerHeight_NoHole` 覆盖；闭合容差由 `MountainStage7SourceContractTests` 锁定 `EndpointTolerance`/`ValidateConnectedEdges`。Unity EditMode 运行待执行。）**
- [x] collision mesh：无山体顶点引用（`MountainVertexRanges` validator 通过）；无洞（山格区域射线可穿透到基础面）；碰撞索引无越界。**（代码落地：`MountainVertexRanges` validator 已随阶段 5.8 落地（ChunkMapRenderer.cs:1950-1966），全图装配测试以纯函数复刻其语义 + 索引越界断言；射线无洞需 Unity 运行时。Unity EditMode/运行期验收待执行。）**
- [ ] Chunk 边界截图：固定 seed 场景逐 Chunk 边界无裂缝、无颜色/法线接缝（需 Unity 场景截图，决策 ㉛ 目检项）。

**完成标准**：几何诊断全零；碰撞双轨校验通过；Chunk 边界截图无可见缝。

### 阶段 7.5：生命周期验收（动画、水陆、清除与 Ghost）

**目的**：关闭动态地图全生命周期路径——普通 Height 动画、拓扑变化同步降级、Ghost/Finalize 幂等与碰撞切换。

**工作项**：

- [ ] 普通 Height 抬升/沉降（R/F）：山体随地形动画插值，共享边无裂缝、无抖动；峰顶全程不被视锥/阴影剔除（保守 bounds 生效）。**（纯函数层已覆盖：保守 bounds 由 `MountainTopologyTests` 断言；动画通道共享边一致性由 7.3 四项断言；动画插值/剔除需 Unity 运行期目检。）**
- [x] 拓扑变化路由：清除山格、陆→水、水→陆、恢复山格、阈值跨越——均整笔事务同步提交（无 `TransitionStarted`、无锁残留、无旧山体 Ghost 突消）。**（`MountainTopologyRouteTests` 4 项覆盖清除/水淹/水→陆恢复/纯 Height 保动画；阈值跨越在 `MountainTopologyTests.Signature_ClearWaterThreshold_ChangeSignature` 纯函数层覆盖；新增动画中冲突提交用例见下。）**
- [x] 动画中冲突提交：动画进行中再次清除/水淹/高度修改 → `ForceCompleteConflicting` 后正确重建，无双山重叠、无过期材质槽。**（代码落地：新增 `MountainTopologyRouteTests.Commit_AnimatedHeightOnMountain_ThenClear_ForceCompletesAndSyncs`——纯 Height 动画进行中清除山格 ⇒ 旧动画被 ForceCompleteConflicting 收尾（IsAnimating 立即 false）、整笔事务同步降级、无锁残留、movementCost 回落 1、无 TransitionStarted；既有 `MapMutationStage5Tests` 覆盖通用冲突/ForceComplete/Finalize 幂等路径。运行期目检待 Unity。）**
- [x] 水→陆恢复：山体几何重现 + 不可通行/不可部署/不可建造立即生效 + 免雾规则重新生效（6.2）。**（路由测试 `Commit_WaterToLandRestore_WithDuration_DowngradesToSync` 断言恢复后 movementCost = MaxValue 立即生效；免雾重生效由 6.2 `MountainVisibilityResolverTests` 覆盖。几何重现需 Unity 运行期目检。）**
- [x] 清除语义：`ClearLandForm` 永久清除（`Terrain | Objects`），重建后无山体、movementCost 回落 1、费用标签/高亮/反查按普通格处理。**（`MapMutationMountainTests` 3 项覆盖永久清除/脏位；`MountainTopologyRouteTests` 冲突用例断言 movementCost 回落 1；标签/高亮过滤由 6.4/6.8 测试覆盖。）**
- [ ] Finalize/cancel/ForceComplete 三路径幂等：无重复销毁、无 null 引用、材质实例数不增长。**（`MapMutationStage5Tests` 覆盖 Complete/ForceComplete/Sliced 路径；材质实例数需 Unity 运行期诊断。）**

**完成标准**：动画与同步两条路径在任何完成/取消顺序下无残留；生命终点状态 = 数据终态。

### 阶段 7.6：玩法规则与交互回归（玩家 + AI）

**目的**：关闭规则与输入回归——山格语义不得泄漏到寻路、部署、建造、探索、后勤与点击。

**工作项**：

- [x] 寻路：山格不可通行（`CanEnterCell` false）；无路径穿过山格；山格作为起点/终点被正确拒绝。**（代码落地：`UnitMovementSystem.CanEnterCell` 拒绝 `movementCost == MaxValue` ⇒ 路径不穿山（GetAllNeighborsAndCosts 跳过）、山格终点不可达；新增 `UnitMovementSystemTests.Pathfinding_MountainCells_BlockRouteAndDestination`——山墙封死目标格全部界内邻居 ⇒ 不可达、山格作为终点 ⇒ 拒绝。Unity EditMode 运行待执行。）**
- [x] 部署入口：玩家卡牌、AI 卡牌、兵营生产、AI 工厂——全部入口对山格不可部署（`CanSpawnUnitOnCell`）；水→陆恢复后立即恢复拒绝。**（代码落地 + 修复 5 处缺口：`CardPresenter.IsReleaseValid`（玩家卡牌确认路径补统一资格，与 6.5 放置预览同口径，关闭"无高亮但可执行"窗口）、`AICardBrain.IsValidSpawnCellForUnit/ForBuilding`（AI 卡牌）、`BarracksSpawner.FindAdjacentEmptyHex`（兵营生产）、`AIAutoExplorer.SpawnRewardUnits/FindOverflowCell`（AI 探索奖励）、`ExplorationRewardSystem.SpawnUnitsWithOverflow/FindOverflowCell`（玩家探索奖励）；水→陆恢复拒绝由 `DeriveMovementCost` 统一派生立即生效（阶段 1 测试覆盖）。源码契约 5 项防回滚（`MountainStage7SourceContractTests`）。）**
- [x] 建筑放置：`blockBuildingSpawn` 阻止山格建造；放置预览不高亮山格（6.4/6.5）。**（`Mountain.asset` blockBuildingSpawn=true（阶段 1）；玩家/AI 建筑入口统一走 `CanBuildOnCell`（本阶段收口）；预览/攻击高亮过滤由 6.4/6.5 测试覆盖。）**
- [x] 资源与地貌：初始生成不覆盖山格；河流不经过山格；山格不参与散落权重池。**（`RidgeGeneratorTests` 覆盖山格生成/水域排除/脊线互斥；河流避让由阶段 2 `RiverGenerator` 修改 + Ridge/河流测试覆盖；散落互斥由 `LandFormClusterSpawnRuleTests`/阶段 2 测试覆盖。运行期复查待 Unity。）**
- [x] 探索与后勤：山格免雾不写 `IsExplored`；归属/探索/后勤语义与无山地图一致（6.1/6.2 测试 + 运行期复查）；山格不显示费用标签（6.8）。**（6.1/6.2 共 20 项测试覆盖免雾不写探索位/归属不变/后勤零回归；标签过滤由 6.8 源码契约覆盖。）**
- [ ] 点击/拖放/旋转：山格中心、侧坡 XZ、山脚、Chunk 边界均命中基础 collision 并正确 XZ 反查（6.6）；卡牌拖放到山格无部署、无高亮。**（链路审计在 6.6 完成；需 Unity 运行期射线/拖放验收。）**
- [ ] 高亮与镜头：攻击范围/可达不含山格；调试豁免仅调试工具可用；镜头旋转中心 = 基础面，无跳轴/穿山（6.7）。**（高亮过滤 6.4/6.5 测试覆盖；镜头旋转需 Unity 运行期验收。）**

**完成标准**：玩家与 AI 入口结果一致；任一交互路径不出现"无高亮但可执行"或"山格可通行/可部署"窗口。

### 阶段 7.7：表现与视觉验收

**目的**：关闭 Low-poly 视觉项——色阶、阴影、Triplanar、雾边界与镜头预算的可视验收。

**工作项**：

- [x] 视觉契约代码侧防线：新增 `MountainStage7VisualContractTests`（12 项源码/资产契约，见实施进度续19 ①）与 `MountainAssetSetup.Diagnose` 扩展（Transition Shader 存在性 + 三档色阶互异不透明 + 纯色/纹理模式摘要）。**（2026-08-06 完成：MainGame/Tests/Assembly-CSharp-Editor dotnet 编译 0 错误；Unity EditMode 运行待执行。）**
- [ ] 固定 seed 截图矩阵：近景/远景、地图边缘、跨 Chunk 山脊、最高 `H_max` 叠加最高地形、地图中心峰。
- [ ] 色阶：同面同色阶、3 档可辨、无面内渐变色带、无 worldY 连续切带；ridgeKey01 偏移不改变档序。
- [ ] 阴影：主 pass 与 ShadowCaster 无轮廓漂移；双面阴影正常；峰顶阴影不缺失；动画期间 clip 与阴影同步。
- [ ] Triplanar：三轴采样尺度一致、无拉伸/接缝/单轴主导闪烁；跨 Chunk 采样连续；纯色模式（无纹理）稳定。
- [ ] 雾边界：可见山格邻未探索普通格——山脚/格界/tri 角点无漏雾、无整块错误遮蔽；山体不被选择性对象雾化二次处理（6.3）；未探索山格视觉可见（6.2）。
- [ ] 镜头预算：允许 `H_max` 内无裁峰；边缘山峰可纳入视野；旋转无穿山；遮挡（单位/建筑/河流/格线/标签/Overlay marker）可接受（6.7/6.8）。

**完成标准**：截图矩阵全部通过目检；无粉色材质/NaN 闪黑/接缝裂缝；记录最终 `H_max`、clip、缩放与俯仰约束。（代码侧视觉契约已落地；目检项待 Unity。）

### 阶段 7.8：性能与资源生命周期

**目的**：确认 v1 面数、材质实例、碰撞 cooking 与动画 CPU 成本在预算内，无隐藏的 per-Chunk 泄漏。

**工作项**：

- [x] 性能采集工具链：新增 `MountainPerformanceBaseline`（`Tools/程序化山脉/性能基线（自动进入 PlayMode）`——自动进 PlayMode 等地图生成稳定后采集并写 `Temp/mountain_perf_report.txt` 后自动退出）、`ChunkMapRenderer` 静态计时钩子（`EnableChunkBuildTiming` 默认关闭零开销，构建耗时按含山/无山分类、`CollisionCommitCount`、动画帧耗时采样）与 `DebugChunks` 只读诊断枚举。**（2026-08-06 完成：编译 0 错误；运行期采集待用户执行菜单。）**
- [x] 顶点/三角预算代码侧：`MountainGeometryBuilder.SolidMountainFanVertexCount = 54` 预算常量（18 面 × 3，阶段 3.3 断言权威化）+ `MountainStage7PerformanceContractTests`（5 项源码契约：计时钩子默认关闭/碰撞计数条件/DebugChunks 只读/预算常量/碰撞双缓冲销毁）。**（2026-08-06 完成。）**
- [ ] Chunk 构建耗时：无山 Chunk 与含山 Chunk 分别记录（山体替换式构建不显著拖慢初始生成）——由性能基线工具采集。
- [ ] 顶点/三角预算：576 格地图全图顶点/三角总量记录并纳入预算表——由性能基线工具采集。
- [ ] 材质实例数量：稳定 + Transition 各每 Renderer 1 份；重复重建/切场景后实例数不增长（字段缓存生效）——由性能基线工具采集实例数，稳定性需人工复核。
- [ ] collision mesh：cooking 次数 = 提交次数（不逐帧 cooking）；每 Chunk 2 mesh 上限（Active/Staging 双缓冲），OnDestroy 无泄漏——cooking 次数由性能基线工具采集，双缓冲由 7.8 源码契约锁定。
- [ ] 动画成本：CPU 逐顶点写入帧耗时（进入 PlayMode 按 R/F 抬升触发动画后再采集）；保守 bounds 不逐帧重算；Shader 纹理采样（纯色 0 次 / 纹理 3 次）符合 4.3 契约。
- [ ] GC/内存：雾遮罩、诊断数据、几何临时数组无每帧分配热点（粗查）。

**完成标准**：性能数据记录成表，无超预算项；材质/碰撞/mesh 生命周期无泄漏。（工具链与代码侧契约已落地；运行期数据待采集。）

### 阶段 7.9：已知限制、验收报告与 v1 放行

**目的**：聚合 7.1~7.8 结果形成放行决策，明确"已实现 vs 未实现"边界，防止以"当前代码天然保证"掩盖未实现契约。

**工作项**：

- [ ] 记录已知限制：v1 无分岔（决策 ⑱）、无雪顶（决策 ㉒）、无高度感知雾（决策 ⑪）、无山顶高亮（决策 ⑨）、无动态山脊重算（决策 ㉕）、拓扑变化整笔事务同步提交（5.5 限制）、点击按基础面 XZ 近似（决策 ⑧）、侧坡可能归邻格、遮挡由 `H_max` 与镜头预算控制。**（限制清单已写入文末"阶段 7 验收记录"，随 7.7/7.8 结果确认后勾选。）**
- [x] 决策 ㉛ 验收清单逐项核对：共享边位置及 UV2/UV3 误差 < 1e-4（7.3）；无非流形边/无退化三角（7.4）；动画通道长度一致（5.3 validator）；固定种子重建 hash 一致（7.3）；清除/水淹无残留（7.5）；Chunk 边界截图无裂缝（7.4/7.7）；碰撞无洞（7.4）；拓扑变化动画与阴影边界验收（7.5/7.7）。**（2026-08-06：状态表落地于文末"阶段 7 验收记录"——代码侧项已全部落地，Unity 运行期项待执行后勾选。）**
- [ ] 生成验收报告：各阶段完成标准勾选汇总、测试统计、性能表、截图清单、遗留问题清单。**（2026-08-06：报告框架落地于文末"阶段 7 验收记录"，测试统计/性能表/截图清单待 Unity 侧数据回填。）**
- [ ] 放行决策：满足"Unity 编译无错误 + 固定种子结果稳定 + 主要动态地图与交互回归通过 + 决策 ㉛ 全过"即 v1 放行；不满足项按 7.1~7.8 回退修复复验。**（结论待 7.2 EditMode 全量回归与 7.4/7.7/7.8 运行期验收后给出。）**

**完成标准**：验收报告成文；v1 限制与实现一致（无"文档承诺但代码未实现"条目）；放行/不放行结论明确并有依据。

**最终 v1 放行标准**：决策 ㉛全部通过；Unity 编译无错误；固定种子结果稳定；主要动态地图与交互回归通过；没有以“当前代码天然保证”掩盖的未实现契约（= 7.1~7.9 完成标准聚合）。

## 阶段 7 验收记录（2026-08-06 追加：7.7~7.9 代码侧落地与放行状态）

> 本记录是阶段 7.9 的"验收报告"框架：代码侧项（测试/契约/工具）已全部落地并标记 ✅；Unity 运行期项（EditMode 运行、PlayMode 采集、场景截图目检）待用户在编辑器内执行后回填状态。每个代码侧条目后标注对应验收工具，杜绝"看起来对"。

### A. 决策 ㉛ 验收清单状态表

| # | 验收项（决策 ㉛） | 代码侧状态 | 运行期状态 |
|---|---|---|---|
| 1 | 共享边位置及 UV2/UV3 误差 < 1e-4 | ✅ 7.3 四项确定性单测（跨 Chunk/动画终态/镜像边/角点） | ⏳ EditMode 运行 |
| 2 | 无非流形边 | ✅ 7.4 全图双轨扫描 + 沙箱 8 项 | ⏳ EditMode 运行 |
| 3 | 无退化三角 | ✅ 7.4 全图双轨扫描 + `CountDegenerateTriangles` | ⏳ EditMode 运行 |
| 4 | 动画通道长度一致 | ✅ 5.3 validator 扩展（有限值/delayEnd≥delayStart） | ⏳ EditMode 运行 |
| 5 | 固定种子重建 hash 一致 | ✅ 7.3 `GeometryHash` 7 件 + 脊线 N=3 次 | ⏳ EditMode 运行 |
| 6 | 清除/水淹无残留 | ✅ 7.5 路由测试（含动画中冲突提交） | ⏳ EditMode 运行 |
| 7 | Chunk 边界截图无裂缝 | — | ⏳ 场景截图目检（7.4/7.7） |
| 8 | 碰撞无洞 | ✅ 7.4 双轨装配 + `MountainVertexRanges` validator | ⏳ 运行期射线（6.6） |
| 9 | 拓扑变化动画与阴影边界 | ✅ 5.4~5.9 路由/幂等 + 7.7 源码契约（ShadowCaster 同 clip 平面） | ⏳ 运行期目检（7.5/7.7） |
| 10 | 视觉表现（色阶/Triplanar/雾边界/镜头） | ✅ 7.7 视觉契约 12 项 + Diagnose 扩展 | ⏳ 场景截图目检（7.7） |
| 11 | 性能与资源生命周期 | ✅ 7.8 钩子 + 基线工具 + 契约测试 5 项 | ⏳ 性能基线采集（7.8） |

### B. 测试统计（代码侧已落地的山脉测试文件）

`MountainCellRuleTests` / `MountainGeometryTests` / `MountainTopologyTests` / `MountainTopologyRouteTests` / `MountainMaterialContractTests` / `MountainVisibilityRuleTests` / `MountainVisibilityResolverTests` / `MountainHighlightGateTests` / `MountainStage6SourceContractTests` / `MountainStage7SourceContractTests` / `MountainStage7VisualContractTests`（新）/ `MountainStage7PerformanceContractTests`（新）/ `RidgeGeneratorTests` / `MapMutationMountainTests`——共 14 个文件；既有回归套件（ChunkRenderer/TriangleTransitionMesh/MapMutation*/TemporaryVisibilityService/DomainInvariant/HexMapService/InputCameraConfiguration/HexHighlight*）零改动。**全量用例数/通过数待 Unity EditMode 运行后回填。**

### C. 性能表（7.8 采集项，待 `Tools/程序化山脉/性能基线（自动进入 PlayMode）` 回填）

| 指标 | 期望 | 实测 |
|---|---|---|
| Chunk 构建平均耗时（含山/无山） | 无山不显著拖慢；比值记录 | ⏳ |
| 全图渲染 mesh 顶点/三角 | 记录入预算表；单格 solid 扇 = 54（✅ 常量断言） | ⏳ |
| 山体稳定材质实例 | 每 Renderer 1 份 = 含山 Renderer 数 | ⏳ |
| Transition 材质实例 | 动画期间才创建，每 Renderer ≤ 1 | ⏳ |
| collision cooking 次数 | = 含山 Chunk 提交次数（不逐帧 cooking） | ⏳ |
| CPU 顶点动画帧耗时 | R/F 抬升触发后采样平均/最大 | ⏳ |

### D. 已知限制（v1，均与实现一致）

1. 山脊线不分岔（决策 ⑱，Y 型留 v3）；无雪顶/第 4 档色阶（决策 ㉒）。
2. 无高度感知雾（决策 ⑪）：山格免雾靠 visibility resolver 专属规则，山尖破雾为后续增强。
3. 无山顶专属高亮（决策 ⑨）：所有高亮来源显式过滤山格。
4. 山被动态清除后不恢复、无动态山脊重算（决策 ㉕）。
5. 拓扑变化（清除/水淹/恢复/阈值跨越）整笔事务同步提交，无山体专用淡出动画（5.5 限制）。
6. 点击按基础 collision mesh 射线 + XZ 最近中心近似反查（决策 ⑧）；视觉侧坡可能归邻格。
7. 遮挡由有效 `H_max` 上限（maxHeight × mountainHeightScale）与镜头预算控制（6.7/6.8）；`maxSlope` 快照不随高度缩放（续18 注意项）。
8. 同一事务内不做"部分 Chunk 动画、部分同步"的混合提交（5.5 优化留后）。

### E. 放行决策

**当前结论：暂缓放行。** 代码侧 7.1~7.9 全部落地（MainGame/Tests/Assembly-CSharp-Editor dotnet 编译 0 错误；7.1 Unity 编译/资产基线已在编辑器侧全绿），剩余待用户在编辑器内执行：
1. 7.2 EditMode 全量回归（14 个山脉测试文件 + 既有回归套件）；
2. 7.4/7.5/7.6 运行期项（射线/拖放/旋转/动画/ghost）；
3. 7.7 固定 seed 截图矩阵目检（色阶/阴影/Triplanar/雾边界/镜头）；
4. 7.8 性能基线菜单采集并回填 C 表；
5. 以上全过后按"最终 v1 放行标准"勾选放行，并回填 B 表测试统计。

## 实施顺序总表

```text
阶段 0  基线与回归入口
  ↓
阶段 1  数据模型与玩法契约
  ↓
阶段 2  生成顺序、多簇互斥、脊线与河流避让
  ↓
阶段 3  静态路线 A 几何 + 独立 collision mesh
        （3.1 hash/高度场 → 3.2 数据契约 → 3.3 solid 山体 → 3.4 mountain rect
         → 3.5 tri 封口 → 3.6 Chunk 集成替换拓扑 → 3.7 独立碰撞 → 3.8 诊断验收）
  ↓
阶段 4  山体稳定材质、Triplanar、Low-poly 法线与色阶
        （4.1 材质契约 → 4.2 Shader 骨架 → 4.3 Triplanar → 4.4 face tier
         → 4.5 导数平面法线 → 4.6 阴影/雾化 → 4.7 材质生命周期 → 4.8 诊断验收）
  ↓
阶段 5  高度动画、拓扑变化策略、bounds 与碰撞生命周期
        （5.1 动画/拓扑契约 → 5.2 共享顶点来源 → 5.3 UV2/UV3 通道
         → 5.4 山体 Transition Shader → 5.5 拓扑同步降级路由 → 5.6 Ghost/Finalize
         → 5.7 保守 bounds → 5.8 collision 生命周期 → 5.9 玩法/对象收尾
         → 5.10 诊断验收）
  ↓
阶段 6  迷雾、探索、高亮、点击、镜头与遮挡
        （6.1 可见性契约 → 6.2 resolver 接入 → 6.3 FogBlend 边界
         → 6.4 高亮统一门禁 → 6.5 高亮调用方收口 → 6.6 点击反查
         → 6.7 镜头预算 → 6.8 标签/Overlay → 6.9 诊断验收）
  ↓
阶段 7  完整回归、性能与 v1 验收
        （7.1 编译/资产基线 → 7.2 EditMode 全量回归 → 7.3 固定 seed 确定性重建
         → 7.4 拓扑验收 → 7.5 生命周期验收 → 7.6 玩法规则与交互回归
         → 7.7 表现与视觉验收 → 7.8 性能与资源生命周期 → 7.9 限制记录与放行）
```
