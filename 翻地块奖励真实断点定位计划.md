# 翻地块奖励真实断点定位计划（以现有设计为准）

> 本计划只依据当前 Excel、配置 Schema、生成资产、场景绑定、调用链和 Unity 运行日志制定。
>
> **当前阶段不修改代码、不修改 Excel、不修改生成资产。先把真实断点定位到具体函数和字段，再决定修复方式。**

## 一、现有设计约束（不得擅自改变）

1. 探索奖励类型权重：`None / Gold / MilitaryUnit / TacticalCard / Building` 各 20%。
2. `unitCountTiers` 允许 `0`，Schema 已说明 0 表示空奖励；因此军事奖励抽到 0 个单位不应被直接判为配置错误。
3. `building.gold_mine` 允许作为建筑奖励；不能因为旧脚本注释与当前 Schema 不一致就删除金矿奖励。
4. 建筑奖励无法放置时可以按照现有设计降级为金币。
5. 战术卡奖励可以增加已有战术卡数量，不要求一定出现新的卡牌类型。
6. 地图生成阶段预生成奖励快照，探索阶段只消费快照，不重新随机。
7. 地块资源收割金币与探索随机奖励是两条独立链路，不能把基础金币误判为随机金币奖励。

## 二、配置核查结论

### 2.1 Excel、CSV、Unity 资产已同步

原始配置：`Config/Excel/游戏数值配置.xlsx`

导出配置：

- `Config/Generated/Csv/探索奖励配置.csv`
- `Config/Generated/Csv/探索奖励池.csv`

运行时资产：

- `Assets/GameConfig/Generated/ExplorationRewardConfigDatabase.asset`
- `Assets/GameConfig/Generated/ExplorationRewardPoolDatabase.asset`

已核对：

- 生成 CSV 的 SHA-256 与 `Config/Generated/game-config.manifest.json` 一致；
- 生成资产内容与 CSV 一致；
- `Assets/Scenes/GameScene.unity` 已绑定两个 Excel 探索奖励数据库；
- Excel 的奖励池中已配置弓兵、剑客、两张战术卡和金矿。

因此当前问题不是“Excel 没写奖励”，也不是“生成资产未更新”。

### 2.2 当前有效配置

探索奖励配置：

```text
noneWeight=20
goldWeight=20
militaryWeight=20
tacticalWeight=20
buildingWeight=20
goldTiers=5,5,10,15,20,25
unitCountTiers=0,0,0,1,1,2
```

探索奖励池：

```text
unit.swordsman       -> MilitaryUnit
unit.archer          -> MilitaryUnit
tactical.battle_order -> TacticalCard
tactical.repair       -> TacticalCard
building.gold_mine    -> Building
```

单位映射已存在：

```text
unit.archer    -> legacyId 12 -> UnitConfigSO.Id 12
unit.swordsman -> legacyId 13 -> UnitConfigSO.Id 13
```

建筑映射已存在：

```text
building.gold_mine -> legacyId 6 -> BuildingConfigSO.buildingId 6
```

战术卡资源和 Excel cardId 也均存在：

```text
tactical.battle_order
tactical.repair
```

## 三、奖励完整调用链

### 3.1 预生成

```text
GameFlowManager.Initialize()
  -> PublicBuildingGenerator.GenerateAll()
  -> MapGenerator.GenerateExplorationRewards()
  -> ExplorationRewardProvider.GenerateReward(random)
  -> HexCellData.SetExplorationReward(reward)
```

对应脚本：

- `Assets/Scripts/Managers/GameFlowManager.cs` 74-79 行；
- `Assets/Scripts/Managers/MapGenerator.cs` 348-361 行；
- `Assets/Scripts/Core/Services/Exploration/ExplorationRewardProvider.cs` 47-155 行。

### 3.2 探索与快照消费

```text
ExplorationService.TryExplore()
  -> GoldWallet.TrySpendGold()
  -> 标记探索/取得归属
  -> MapResourceCollectionService.HarvestForGold()
  -> HexCellData.TakeExplorationReward()
  -> Broadcast(Explored)
```

对应脚本：`Assets/Scripts/Core/Services/Exploration/ExplorationService.cs` 100-151 行。

### 3.3 玩家结算

```text
ExplorationRewardSystem.OnBroadcast(Explored)
  -> Settle()
  -> Gold / Unit / TacticalCard / Building 分支
  -> Broadcast(Settled)
```

对应脚本：`Assets/Scripts/Core/Services/Exploration/ExplorationRewardSystem.cs` 48-123 行。

### 3.4 表现

```text
ExplorationService.SignalRewardPoint()
  -> Broadcast(RewardPoint)
  -> ExplorationCoinPresenter 仅处理 SettledRewardType=Gold
```

对应脚本：

- `Assets/Scripts/Core/Services/Exploration/ExplorationService.cs` 177-233 行；
- `Assets/Scripts/Managers/ExplorationCoinPresenter.cs` 75-85 行。

所以金币表现层本来就只表现金币；单位、卡牌、建筑必须分别从场景对象、卡槽数量和建筑对象验证，不能期待它们出现在金币特效里。

## 四、目前真正可确认的断点

### 断点 1：军事 x0 是设计允许的空奖励，不是配置链断裂

当前配置中 3/6 的单位数量档位为 0，代码会执行：

```text
RewardType=MilitaryUnit
UnitConfigs.Length=0
ExplorationRewardSystem 跳过 SpawnUnitsWithOverflow
仍发布 Settled(MilitaryUnit)
```

运行日志中的：

```text
军事单位奖励 x0
```

与当前 Schema 一致。真正的问题是**结算状态和玩家可见反馈没有区分“军事类型空奖励”**，不是 Excel 缺单位配置。

验证要求：把该情况从“链路断点”单独归类，不要修改 `unitCountTiers`。

### 断点 2：探索基础收割会稳定产生金币，造成视觉误判

`ExplorationService.TryExplore()` 127 行无论随机奖励类型是什么都会调用：

```csharp
_collectionService.HarvestForGold(targetCell, factionId);
```

因此探索 `None`、MilitaryUnit x0、战术卡或建筑降级时，玩家仍可能获得基础资源金币。日志中的：

```text
[Exploration] 阵营 ... 探索 ...，+5 Gold
```

只能证明资源收割成功，不能证明随机奖励是金币。

验证要求：同一地块必须同时记录 `harvestReward` 和 `OriginalRewardType`，避免把两种金币合并为“随机奖励金币”。

### 断点 3：旧日志字段名不能作为新 Provider 断点证据

当前运行日志有：

```text
探索奖励无可用单位配置（rewardUnits 为空）
建筑奖励但 rewardBuildings 为空
```

但当前生产预生成路径已经是：

```text
ExplorationRewardProvider -> Excel DB + RewardPoolDatabaseSO
```

而 `rewardUnits`、`rewardBuildings` 是旧 `ExplorationRewardConfigSO` 字段。当前代码仍在 `ExplorationRewardSystem.cs` 和 `AIAutoExplorer.cs` 使用旧字段名打印日志，但并不代表 Excel 奖励池为空。

这是一个**诊断断点/旧文案断点**，不能直接当作资源映射失败。

### 断点 4：战术卡最需要先验证初始化时序

配置和资源均存在，但：

- `TacticalCardPresenter.Initialize()` 在 44-97 行初始化 `_cardPrefab`、`_cardViews` 和初始卡牌；
- `AddCardWithFly()` 在 156 行开始发放；
- `GameInstaller.cs` 只明确设置了 `GameFlowManager` 为 -30、`CardPresenter` 为 -20，没有明确设置 `TacticalCardPresenter` 的初始化顺序；
- `GameInstaller.cs` 221 行把 `ExplorationRewardSystem` 以 `NonLazy` 创建并订阅广播。

必须确认同一局第一次奖励的顺序：

```text
TacticalCardPresenter.Initialize()
GameFlowManager.Initialize()
ExplorationRewardSystem.OnBroadcast(Explored)
TacticalCardPresenter.AddCardWithFly()
TacticalCardPresenter.AddCard()
```

特别注意：`AddCardWithFly()` 在 173-179 行找不到目标槽位、Prefab、锚点或主相机时会直接 `FinishFly()`；`FinishFly()` 在 321-325 行调用 `AddCard()`，而 `AddCard()` 在 107-111 行如果尚未初始化会返回 -1。若奖励发生在 Presenter 完成初始化前，卡牌就会被静默丢失。

这比“Excel 没有战术卡”更符合当前配置状态，应作为首要真实断点验证项。

### 断点 5：建筑必须逐节点确认，不能先判定金矿配置错误

当前 Excel 和运行库都能解析 `building.gold_mine`。建筑奖励链路必须按以下节点逐一确认：

```text
RollPoolEntry("Building")
  -> configId=building.gold_mine
  -> BuildingBalanceDatabaseSO.TryGetBuilding()
  -> BuildingBalance.legacyId=6
  -> BuildingDataProvider.TryGetBuildingConfig(6)
  -> BuildingDatabaseSO 中的 gold_mine.asset
  -> ExplorationRewardData.BuildingConfig
  -> ExplorationAcquisition.BuildingConfig
  -> RewardBuildingRule.CanPlace(cell)
  -> CardPresenter.SpawnPlayerBuilding(6, position)
```

当前运行日志的“rewardBuildings 为空”只说明 `acquisition.BuildingConfig == null` 或旧文案误导，不能证明 Excel 池为空。

必须记录：

1. Provider 生成的 `configId` 和 `BuildingConfigSO.name`；
2. `ExplorationAcquisition.Explored()` 收到的 `BuildingConfig`；
3. `SettleBuilding()` 收到的 `buildingId`；
4. `RewardBuildingRule.CanPlace()` 的返回值；
5. `CardPresenter.SpawnBuilding()` 中的 `h`、Prefab、parent、Canvas/UI 校验和最终返回值。

只有当第 1-3 步正常而第 4-5 步失败时，才能确认是建筑放置/生成断点。金矿地貌的 `blockBuildingSpawn=true` 只会影响金矿地貌上的具体地块，不能推出所有建筑奖励都必然失败。

### 断点 6：单位也必须区分“数量为 0”和“生成失败”

单位奖励链路：

```text
unitCount=0 -> 合法空奖励
unitCount>0 -> UnitConfigs[i] -> SpawnPlayerUnit()
```

`UnitDatabase.asset` 已含弓兵与剑客资源，且 `UnitBalanceDatabase.asset` 映射存在。当前 `SpawnUnitsWithOverflow()` 198 行仍打印旧的 `rewardUnits 为空`，所以必须进一步确认：

- `acquisition.UnitConfigs.Count` 是否为 0；
- 每个 `UnitConfigs[i]` 是否为 null；
- `unitConfig.Id` 是否为 12/13；
- `CardPresenter.SpawnPlayerUnit()` 是否返回 null；
- 是否是目标格满导致 BFS 无可用格；
- 是否是 Prefab/UI 层级检查失败（`CardPresenter.cs` 411-420 行）。

## 五、断点定位实施顺序（不改代码阶段）

### 第一步：使用一次固定种子局作为样本

固定 `SeedService` 产生的地图，不反复使用随机局。选取至少三类地块：

- 一块 `MilitaryUnit` 且数量 0；
- 一块 `MilitaryUnit` 且数量 >0；
- 一块 `TacticalCard`；
- 一块 `Building`。

记录每块坐标、原始类型和预生成字段。

### 第二步：为单次链路建立日志检查表

日志字段必须按以下顺序出现：

```text
[RewardTrace] Generate cell/type/config
[RewardTrace] TakeSnapshot cell/type/fields
[RewardTrace] Explored payload type/fields
[RewardTrace] Settlement entered type/fields
[RewardTrace] Spawn/Add operation result
[RewardTrace] Settled actualType/amount/status
```

当前阶段只允许增加 Development Build 诊断，不改变结果、不增加补偿、不修改配置。

### 第三步：优先验证战术卡生命周期

在不改变奖励逻辑的前提下确认：

- Presenter 是否在第一次 `Explored` 前完成 `Initialize()`；
- `_database.cards` 是否包含两张卡；
- `_cardPrefab`、锚点、`_cardViews` 是否有效；
- `FindTargetSlot(config)` 是否返回有效槽位；
- `FinishFly()` 的 `AddCard(config)` 返回值是否为 -1；
- 若卡牌数量已从 1 变 2，只是表现层不明显，不应重复发卡。

### 第四步：定位建筑 null 的具体发生层

依次观测 Provider、Acquisition、Rule、Spawn 四层。任何层出现 null 或 false，立即停止向后推断，记录该层为断点。

### 第五步：定位单位生成失败的具体原因

用数量大于 0 的军事样本，排除 x0 语义后再检查资源、站位、Prefab 和 UI 层级。

## 六、确认断点后的修复方向（暂不执行）

- 若只是军事 x0：不改 Excel；补充“空军事奖励”的结算和表现说明。
- 若基础金币造成误判：分离日志和奖励 UI 文案，分别展示资源收割与随机奖励。
- 若战术卡初始化早于发放：调整 `TacticalCardPresenter` 初始化顺序或建立待发队列。
- 若战术卡已入账但无感知：加强数量徽标/卡槽反馈，不改变奖励语义。
- 若建筑在 Provider/Acquisition 丢失：修复快照字段传递，不改金矿配置。
- 若建筑在 `CanPlace` 失败：按现有设计保留金币降级，但显示具体原因。
- 若建筑在 `SpawnBuilding` 失败：只修复真实失败的 Prefab/UI/提交节点，不先替换奖励池。
- 若单位 `SpawnPlayerUnit` 失败：修复具体资源、Prefab、站位或 UI 校验，不把失败误判为 Excel 空池。
- 若发现旧 SO 仍被实际调用：再单独处理双事实源；目前静态调用链显示地图预生成使用的是 Excel Provider。

## 七、当前结论

已经确认：

1. Excel 奖励内容已经写好，并且生成链路同步；
2. 当前设计允许军事奖励数量为 0，不能把它当成配置错误；
3. 金矿作为建筑奖励是 Schema 明确允许的设计，不能先删；
4. 基础资源收割金币会掩盖随机奖励结果；
5. 当前日志中的 `rewardUnits/rewardBuildings` 是旧架构文案，证据等级不足；
6. 最值得优先验证的真实代码断点是战术卡 Presenter 初始化时序，其次是 BuildingConfig 在 Provider→Acquisition→Spawn 哪一层丢失，以及 UnitConfig 是否在生成入口失败。

## 八、本次实机运行后的新增证据

本次运行日志确认：随机奖励类型实际上已经抽取到了非金币类型，问题发生在“类型抽取之后”。日志中出现了：

```text
军事单位奖励 x1
军事单位奖励 x2
军事单位奖励 x0
战术奖励但无法发牌（配置数据库或持有者为空）
建筑奖励但 rewardBuildings 为空，降级为金币
无奖励
金币奖励 +5/+10/+15/+20/+25
```

因此可以排除“随机类型永远是 Gold”。

### 8.1 战术卡：Provider 已抽到 TacticalCard，但结算条件失败

`ExplorationRewardSystem.cs` 95-105 行的警告只会在以下条件之一成立时出现：

```csharp
acquisition.TacticalCard == null || _tacticalCardPresenter == null
```

本次运行同时有多次：

```text
[TacticalCardPresenter] Created: BattleOrder ...
[TacticalCardPresenter] Created: Repair ...
```

说明 Presenter 至少执行过初始化，并不是简单的“Excel 没有战术卡”。下一步必须把两个条件拆开记录：

- `acquisition.TacticalCard == null`；
- `_tacticalCardPresenter == null`；
- `TacticalCardPresenter.AddCardWithFly()` 是否进入；
- `AddCard()` 是否实际增加数量。

在这四项未拆分前，不能再把问题归因于初始化时序；当前更强的候选是 `ExplorationRewardProvider.RollTacticalCard()` 返回 null，或 `ExplorationAcquisition` 传递时丢失卡对象。

### 8.2 建筑：当前日志已明确进入 Building 结算，但字段结果仍未拆分

建筑警告位于 `ExplorationRewardSystem.SettleBuilding()` 132-137 行，只能证明：

```csharp
acquisition.BuildingConfig == null
```

不能证明奖励池为空，也不能证明 `RewardBuildingRule.CanPlace()` 失败，因为当前代码在 config null 时立即 return，后面的放置规则不会执行。

因此建筑真实断点应优先检查：

```text
ExplorationRewardProvider.GenerateReward()
  -> reward.BuildingConfig
ExplorationAcquisition.Explored()
  -> acquisition.BuildingConfig
ExplorationRewardSystem.SettleBuilding()
  -> acquisition.BuildingConfig
```

如果三处都非 null，才继续检查金矿地貌禁建、Prefab/UI 层级和 `SpawnPlayerBuilding()` 返回值。

### 8.3 单位：当前日志同时出现 x1/x2 和“配置为空”，说明不能用 x0 解释全部问题

`军事单位奖励 x1/x2` 说明 Provider 有时确实生成了正数量快照；但随后出现旧文案“rewardUnits 为空”，说明某些 `UnitConfigs[i]` 可能为 null，或者日志来自不同运行阶段/不同版本输出。

下一步必须逐条确认：

```text
UnitConfigs.Count
UnitConfigs[i] 是否为 null
UnitConfigs[i].Id
SpawnPlayerUnit 返回的 GameObject
```

不能把所有单位问题都归因于合法的 x0 空奖励。

## 九、当前最明确的判断

本次实机运行已经把问题范围缩小为：

```text
奖励类型抽取正常
  ↓
非金币奖励字段在 Provider → Acquisition → Settlement 之间
出现 null，或结算入口返回失败
```

其中：

- 战术卡：优先确认 `TacticalCard` 字段是否为 null；
- 建筑：优先确认 `BuildingConfig` 字段是否为 null；
- 单位：优先确认 `UnitConfigs` 元素是否为 null以及生成入口返回值；
- x0：仍按现有设计视为合法空奖励；
- 基础金币：仍来自 `HarvestForGold()`，不是随机奖励类型。

## 十、下一步（仍不改代码）

现有日志已经无法继续把 Provider、Acquisition 和 Settlement 拆开。若要锁定“真正断点”，下一次必须允许增加**只读诊断日志**，但诊断日志不能改变随机数、奖励字段、结算结果或玩法规则。

需要增加的最小观测点：

1. `ExplorationRewardProvider.GenerateReward()`：输出奖励类型和具体资源 ID；
2. `ExplorationAcquisition.Explored()`：输出各字段是否为空；
3. `ExplorationRewardSystem` 三个非金币分支：输出进入条件和资源 ID；
4. `TacticalCardPresenter.AddCardWithFly()` / `AddCard()`：输出发放结果；
5. `CardPresenter.SpawnPlayerUnit()` / `SpawnPlayerBuilding()`：输出返回值和失败校验点。

在没有这些字段级日志前，不应继续修改 Excel，也不应直接重写奖励逻辑。

**当前结论：本次运行确认了“不是只抽到金币”，但还没有足够字段级证据确认 Provider、Acquisition 还是生成入口是最终断点。**

