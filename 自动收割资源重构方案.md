# 自动收割资源重构方案

## 背景

当前资源系统存在两条并存的收割路径：

1. **探索自动收割**（探索未探索地块时 → 换金币）
2. **手动收割按钮**（选单位点按钮 → 战斗 Buff）

手动收割按钮与自动化移动/战斗的核心规则矛盾，需要取消，改为单位经过地块时自动触发。

---

## 改造目标

1. 取消手动收割按钮（UI + 代码）
2. 单位走到有资源模型的地块时自动触发收割
3. 效果改为永久实时 Buff，去除"下一次""持续N秒"等旧语义

---

## 一、触发点

**位置：** `UnitMovementController` — 单位逐格移动到达新 Hex 坐标时

**逻辑：**
```
到达新格 → 读 currentCell.GetResource()
         → 非 None → 触发收割效果 → 销毁 resourceModel → 不修改金币（金币由探索系统发放）
```

- 玩家单位、AI 单位统一入口，物理位移即触发
- 不需要在 `UnitBrainBase` 额外加判断

---

## 二、效果重设计（方案 B — 永久版）

所有 Buff 作用于该单位永久生效，不设时间限制、不消费清零。

| 资源类型 | 新效果 | 实现方式 |
|----------|--------|----------|
| 动物 Animals | 该单位攻击力永久 ×1.7 | `CharacterData.Resource_Animals = 0.7f`（持久，不消耗） |
| 植物 Plants | 该单位立即回血 25% 最大 HP | `CharacterData.Heal(0.25 * maxHP)` |
| 矿物 Minerals | 该单位防御力永久 ×1.25 | `CharacterData.Resource_Minerals = 0.25f`（持久，不消耗） |
| 宝箱 Chest | 玩家阵营金币 +50 | 通过 GoldWallet 加金，需确保只对玩家单位生效 |

### 关于永久 Buff 的说明

- `Resource_Animals` / `Resource_Minerals` 目前是两个浮点字段，存储在 `CharacterData` 中
- 战斗中 `AttackGain = 1 + attacker.Resource_Animals + ...`、`DefenseGain = 1 + defender.Resource_Minerals + ...`
- 只要不再将其清零，这个单位永久享有该加成
- 每种资源每个单位最多触发一次（收割后 `ReapResource()` 清掉 resourceType，不会再重复触发）

---

## 三、现有战斗代码修改

### 删除"用后清零"（两处）

| 文件 | 行 | 当前代码 | 操作 |
|------|-----|----------|------|
| `UnitMovementController.cs` | 387 | `attacker.Resource_Animals = 0;` | 删除此行 |
| `UnitMovementController.cs` | 389-390 | `theAttacked.Resource_Minerals = 0;` | 删除此行 |
| `CombatResolver.cs` | 134-136 | `if (AttackGain > 1) attacker.Resource_Animals = 0;` | 删除此块 |
| `CombatResolver.cs` | 137-139 | `if (DefenseGain > 1) theAttacked.Resource_Minerals = 0;` | 删除此块 |

### 保留的代码（不变）

- `AttackGain = 1 + Resource_Animals + ...` 的乘法逻辑不变，只是字段不再被清零
- `DefenseGain = 1 + Resource_Minerals + ...` 同理

---

## 四、手动收割代码清理

### 必须删除

| 文件 | 行/位置 | 内容 |
|------|---------|------|
| `UIController.cs` | 97-100 | ReapButton UI 类型绑定 |
| `UIController.cs` | 443-487 | `UnitInfoPanelReapButton()` 方法 |
| `UIController.cs` | 489-530 | `ReapResource_Animals/Plants/Minerals/Chest()` + `ReapResource(GameObject)` |
| 单位信息面板 UI Prefab | — | ReapButton GameObject（Scene 里操作） |

### 可选的连带清理（如果没有其他地方调用）

| 文件 | 内容 |
|------|------|
| `EnvironmentModelsProvider.cs:7-10` | `GetReapAnimalsEffect()` 等四个接口声明 |
| `EnvironmentModelsProvider.cs:38-41` | 四个接口实现 |
| `EnvironmentModelsSO.cs:9-12` | `reapAnimalsEffect` 等四个字段 |
| 对应的特效 Prefab | `.asset` 中的引用（不删 Prefab 本身也安全） |

- 搜索确认无其他调用后再删，否则保留也不影响运行

---

## 五、不变的部分

以下代码**不修改**——探索和公共建筑占领仍然正常调用：

| 文件 | 原因 |
|------|------|
| `HexCellData.ReapResource()` | 探索收割、公共建筑收割、自动拾取共用 |
| `HexCellData.resourceModel` | 探索收割和自动拾取都需 `Destroy` |
| `ExplorationService.HarvestAndReward()` | 探索即收割金币，核心流程不动 |
| `AIAutoExplorer` | AI 探索收割金币逻辑 |
| `PublicBuildingBase` 收割部分 | 占领后扩展地块收割资源 |

---

## 六、逻辑闭环说明

整体收割流转：

1. **探索未探索地块** → `ExplorationService.CompeleteExploration()` → `HarvestAndReward()` → `cell.ReapResource()` → 销毁模型 → 加金币
2. **占领公共建筑扩张** → `PublicBuildingBase` → 遍历子格 → `cell.ReapResource()` → 销毁模型 → 加金币
3. **初始主城周边环** → 这些格已领土但没走过收割流程，上面可能有资源模型 → 单位走到该格时 → **新增的自动拾取** → 给 Buff / 金币 → `cell.ReapResource()` → 销毁模型
4. **单位走到未被探索过、但被其他方式标为已领土的格** → 同上自动拾取

四个入口互不冲突——因为 `ReapResource()` 有幂等性（resourceType 变为 None 后不会重复触发）。

---

## 七、实施步骤

1. 在 `UnitMovementController` 中新增自动收割逻辑（到达新 Hex 时检测资源）
2. 删除 `UnitMovementController` 和 `CombatResolver` 中的"用后清零"行
3. 删除 `UIController` 中的手动收割方法链
4. 删除单位信息面板 Prefab 中的 ReapButton
5. 检查 `EnvironmentModelsProvider` / `EnvironmentModelsSO` 中 reap 相关字段是否已无调用，酌情清理
6. 编辑器 Play 测试：
   - 单位走到有动物/植物/矿物/宝箱模型的格上应触发对应效果
   - 动物/矿物 Buff 永久生效（攻击/防御一直有加成）
   - 宝箱给金币只对玩家单位
   - 同一个格不会重复触发
