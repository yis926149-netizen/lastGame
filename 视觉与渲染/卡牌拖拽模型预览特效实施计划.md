# 卡牌拖拽「卡牌→模型」预览特效实施计划

> 状态：方案已确认，已完成代码级审查并修订，待实施（尚未改动具体代码）。
> 本计划只覆盖「拖拽普通卡时，卡牌缩小并反向浮现对应单位/建筑 3D 模型」这一项特效。
> 审查基线：2026-08-25 当前代码。实施时以本计划中的生命周期、接口和配置链路为准。

---

## 0. 需求确认（已由策划/作者拍板）

1. **渲染方案**：方案 A —— RenderTexture 预览（专用预览相机 + 隔离 Layer + UI 上的 RawImage）。
2. **模型出现位置**：跟随被拖的卡牌 / 指针移动。
3. **两段距离阈值**：`D1 = 0.2 × CanvasReferenceHeight`，`D2 = 0.4 × CanvasReferenceHeight`；当前主 Canvas 基准高度为 1920。
4. **松手衔接**：预览模型直接消失（真实单位/建筑仍由现有部署流程在落点生成）。
5. **触发方向**：仅向上拖拽触发两段变化；向下拖拽的进度按 0 处理，不使用绝对值。

其余实现细节由开发者自行决定，遵循项目既有风格（数值尽量迁入 `FeelConfigData`、逐帧驱动而非 Tween、尊重 `GameLoop.IsPaused`）。

---

## 1. 现状回顾（基线）

### 1.1 拖拽事件链路
- `Assets\Scripts\UI\CardController.cs` 实现 `IBeginDragHandler / IDragHandler / IEndDragHandler`。
  - `OnBeginDrag` → `_dropHandler?.OnCardDragBegin(this)`（第 272 行）。
  - `OnDrag` → `OnDragUpdate(handPanelLocalPos, _originPosition)`（第 290 行）。
  - `OnEndDrag` → 命中地图格走 `HandleCardDragEnd`，否则 `OnCardDragCancel` + `ResetToOrigin`（第 294–337 行）。
- 接口 `ICardDropHandler`（`Assets\Scripts\Core\Interfaces\ICardView.cs:3`）目前只有：
  - `HandleCardDragEnd` / `OnCardDragBegin` / `OnCardDragCancel` / `CanDeployTo`。
  - **缺少向 Presenter 发送逐帧拖拽视觉进度的回调**——这是本特效必须新增的通道。
- `ICardView` 已有 `OnDragUpdate(Vector2 localPoint, Vector2 originPos)`；它是 View 自身的移动/缩放入口，不能误认为属于 `ICardDropHandler`。
- 成功释放路径当前不会调用 `OnCardDragCancel`：`HandleCardDragEnd` 返回 true 后直接结束，普通卡还会在 `ConsumePlayedCard` 中先失活再销毁。因此必须新增明确的成功结束清理，不能依赖 `OnDisable`。

### 1.2 现有拖拽缩放逻辑（待改造）
`CardController.OnDragUpdate`（第 222–231 行）：
```csharp
float distanceY = Mathf.Abs(localPoint.y - originPos.y);          // 只取垂直位移，取绝对值
float maxDistance = UIScreenHelper.ReferenceHeight * 0.37f;      // 1920 × 0.37 = 710.4
float minScale = 0.6f;
float scaleRatio = Mathf.Lerp(1f, minScale, Mathf.Clamp01(distanceY / maxDistance));
target.localScale = _uiConfig.CardSize * scaleRatio;
```

### 1.3 模型数据源（现成，无需新映射）
- 普通卡的 `CardData.NormalCardConfig`：
  - 单位卡 → `(config as UnitConfigSO).unitModel`（`UnitConfigSO.cs:42`）。
  - 建筑卡 → `(config as BuildingConfigSO).buildingModel`（`BuildingConfigSO.cs:16`）。
- 部署时 `CardPresenter.SpawnUnit / SpawnBuilding` 已复用这两个字段（`CardPresenter.cs:361`、`472`）。

---

## 2. 方案概览

拖拽普通卡时：

1. 阶段一（`0 ≤ upwardDistance ≤ D1`）：卡牌 `localScale` 从 `100% → 10%` 线性缩小，并在缩小后期同步淡出。
2. 阶段二（`D1 < upwardDistance ≤ D2`）：模型预览窗口从 `10% → 100%` 线性放大，并淡入；模型内部可缓慢自转增加观感。
3. 预览窗口是一个挂在主 UI Canvas 上的 `RawImage`，跟随指针移动（屏幕坐标换算到自身父节点本地坐标）。
4. 松手：先关闭预览，再执行现有 `CardPresenter.HandleCardDragEnd → SpawnUnit/SpawnBuilding`；成功、失败、暂停、失焦都必须进入幂等清理。

视觉本质：卡牌「蜕皮」成模型，两者在同一 RectTransform 坐标系内反向缩放。

---

## 3. 参数设计

比例基准优先读取卡牌所在 Canvas 的 `UIScreenHelper.CanvasHeight(this)`；找不到 CanvasScaler 时才回退 `UIScreenHelper.ReferenceHeight = 1920`。

| 参数 | 取值 | 说明 |
|---|---|---|
| `D1`（卡牌缩完） | `0.2 × CanvasReferenceHeight`（当前 384） | 阶段一满程距离 |
| `D2`（模型长完） | `0.4 × CanvasReferenceHeight`（当前 768） | 阶段二满程距离 |
| `CardMinScale` | `0.1` | 卡牌最小缩放 |
| `ModelMinScale` | `0.1` | 模型起始缩放 |
| `CardFadeStart` | `cardProgress ≥ 0.6` | 卡牌开始淡出 |
| `ModelFadeIn` | `modelProgress ∈ [0, 0.2]` | 模型淡入区间（交叉淡化） |

推导公式：

```csharp
float upwardDistance = Mathf.Max(0f, localPoint.y - originPos.y);
float cardProgress   = Mathf.Clamp01(upwardDistance / D1);
float modelProgress  = Mathf.Clamp01((upwardDistance - D1) / (D2 - D1));

// 阶段一：卡牌
float cardScale = Mathf.Lerp(1f, CardMinScale, cardProgress);
float cardAlpha = Mathf.Lerp(1f, 0f, Mathf.Clamp01((cardProgress - 0.6f) / 0.4f));

// 阶段二：模型
float modelScale = Mathf.Lerp(ModelMinScale, 1f, modelProgress);
float modelAlpha = Mathf.Clamp01(modelProgress / 0.2f);   // 前 20% 淡入
```

> 卡牌 `alpha` 说明：缩到 10% 时卡面（费用文字等）已糊成一团，必须同步淡出；`CardFadeStart = 0.6` 保证缩到一半后才开始消失。

> 数值必须走完整 Excel 唯一主源链路，不能只修改 `FeelConfigData.cs`。同步修改：`Config/Schema/配置表结构.json`、`Config/Csv/表现配置.csv`、生成的 `game-config.json`、`GameConfigImporter.BuildFeelConfig`、`FeelConfigData`、`FeelConfigProvider`，然后重新导入生成 `FeelConfigDatabase.asset`。

---

## 4. 新增资源与层级（场景/资产改动）

### 4.1 隔离 Layer
- 新增 User Layer：`CardPreview`。
- 主相机 Culling Mask 排除该 Layer（避免预览模型出现在主画面）。
- 预览相机 Culling Mask **只**渲染该 Layer。
- `ProjectSettings/TagManager.asset` 当前尚未存在 `CardPreview`；必须实际新增并递归设置模型根及全部子物体的 Layer。
- 还需检查 Marker/叠加相机等其他相机，不能只修改主相机。

### 4.2 预览相机（Prefab 或场景物体）
- 正交相机（`orthographic`），`targetTexture` 指向全局 RT。
- `clearFlags = SolidColor`，背景色 `alpha = 0`（保证 RT 透明，RawImage 才能透出卡牌/地图）。
- 默认 `enabled = false`，仅拖拽期间开启。

### 4.3 全局 RenderTexture
- 尺寸建议 `512×512`（卡牌大小足够；微信小游戏预算紧张时可降 `256×256`）。
- `depth = 0`、支持 alpha 通道。
- **全局单张复用**，不随每次拖拽新建。
- 明确由预览控制器创建/释放，设置 `ARGB32`、关闭 mipmap，并在场景销毁时 `Release` 后销毁；平台不支持目标格式时提供回退。
- RawImage 必须使用能正确采样 alpha 的 UI 材质；透明背景不能只依赖相机背景色 alpha=0。

### 4.4 RawImage 预览窗口
- 放在主 UI Canvas 顶层子节点（确保盖在卡牌之上）。
- 显示 RT；初始 `alpha = 0`、`raycastTarget = false`（不拦截射线）。
- 当前主 Canvas 为 `ScreenSpaceOverlay`，屏幕坐标转父节点本地坐标时 camera 参数传 `null`；若改 Canvas 模式必须同步修改。
- 定义 RawImage 固定尺寸、pivot/anchor、屏幕边缘 Clamp 和安全区规则，防止窗口跟随指针时被裁剪。

### 4.5 预览模型挂载点
- 一个位于“预览工作室”位置的 `Transform`（地图外、不可见，仅被预览相机看到）。
- 每次拖拽 `Instantiate` 模型到该挂载点，调整相机取景使其居中并适配 bounds。
- 当前场景已有 `UIModel`，但它在 Default Layer 且带 `BoxCollider`，不能直接视为预览工作室；建议新增独立 `CardPreviewRoot/Anchor/Camera` 层级。
- 预览相机必须明确位置、朝向、near/far clip、光源、模型朝向和正交尺寸；bounds 只在 Begin 时计算一次。

---

## 5. 模块拆分与改动清单

### 5.1 新增 `CardDragPreviewController`
职责：预览相机的开关、RT/RawImage 生命周期、模型实例的创建/销毁、每帧更新缩放与透明度。

建议成员：
- `Camera _previewCamera`、`RenderTexture _previewRT`、`RawImage _previewImage`（Inspector/注入绑定）。
- `Transform _previewAnchor`（预览模型挂载点）。
- `GameObject _currentModel`（当前预览实例，单实例复用）。
- `ICardView _currentView` 或等价拖拽 token，用于拒绝旧卡的迟到回调。
- 若控制器是普通 C# 单例，需在 `GameInstaller` 绑定并通过 Zenject 注入；若是 MonoBehaviour，需场景挂载并由 `FromComponentInHierarchy` 绑定，二者只能选一种。
- 方法：
  - `void Begin(GameObject modelPrefab)`：实例化并“纯展示化”，配置相机取景，显示 RawImage。
  - `void UpdateProgress(Vector2 screenPos, float modelProgress)`：跟随指针 + 缩放 + 淡入。
  - `void End()`：立即销毁模型、隐藏 RawImage、关闭相机。
  - `void Cancel()`：同 `End()`（本方案两者一致，直接消失）。

### 5.2 扩展拖拽视觉协议（`ICardView.cs`）
新增逐帧进度回调：
```csharp
/// <summary>拖拽逐帧更新：upwardDistance 为向上位移，进度定义见计划 §3。</summary>
void OnCardDragUpdate(ICardView view, Vector2 screenPos, float upwardDistance, float cardProgress, float modelProgress);
```
- 可将该方法加入 `ICardDropHandler`，但要明确这是新增的视觉通知；更推荐独立 `ICardDragVisualHandler`，避免把放置资格/部署接口和视觉状态强绑定。
- 必须新增成功释放通知 `OnCardDragEnd(ICardView view)`，因为当前成功路径不会调用 `OnCardDragCancel`，且随后会销毁卡牌。
- `CardPresenter`：普通卡驱动预览，并记录当前预览 View，防止旧卡通知清理新卡预览。
- `TacticalCardPresenter`：不创建模型预览，但必须保持现有 Ghost Proxy 的移动、缩放、借出、归还和销毁行为。

### 5.3 改造 `CardController.OnDragUpdate` / `OnDrag`
- 按 §3 公式计算 `cardScale`、`cardAlpha`。
- 卡牌 `alpha` 必须作用于整卡（含子级）。当前 `CaptureBaseColors/SetAffordable` 已按 Graphic 颜色处理金币压暗；不能直接把 CanvasGroup 与其叠加后再各自恢复。实现时采用统一的 `baseAlpha × affordableAlpha × dragAlpha` 刷新，或明确 CanvasGroup 的唯一所有者和恢复时序。
- 计算 `cardProgress / modelProgress / screenPos`，调用 `_dropHandler?.OnCardDragUpdate(...)`。
- 幽灵卡（战术卡）路径保持不变：`target = _dragProxy != null ? _dragProxy : _rectTransform`。
- 向下拖拽使用 `upwardDistance = Mathf.Max(0f, localPoint.y - originPos.y)`；不能继续使用 `Mathf.Abs`，否则向下拖也会触发模型预览。
- `ResetToOrigin` 必须同时恢复本体和代理的缩放/透明度；代理存在时不能只恢复本体。

### 5.4 实现 `CardPresenter.OnCardDragUpdate`
- `OnCardDragBegin`：从统一的玩家模型来源取得 Prefab，非空且为普通单位/建筑卡才 `_preview.Begin(modelPrefab)`。
- `OnCardDragUpdate`：`_preview.UpdateProgress(screenPos, modelProgress)`。
- `OnCardDragEnd`：在有效部署成功路径中先 `_preview.End()`，再或在明确的提交边界内执行正式生成；该通知不可依赖卡牌 `OnDisable`。
- `OnCardDragCancel`：`_preview.End()`；`End` 必须幂等。

### 5.5 模型「纯展示化」工具
新增静态方法（如 `CardDragPreviewUtils.StripToVisual(GameObject)`）：
- 递归禁用/移除 `Collider`、`Rigidbody`。
- 禁用子物体中的 world-space Canvas（血条）。
- 移除/禁用运行时控制器（`UnitMovementController`、`BuildingController` 等，正常预览实例不会自动带上，但防御性处理）。
- 保留 `Renderer` / `Animator`（待机动画）。
- 递归 `gameObject.layer = CardPreview`。
- 优先禁用而非从实例中移除组件，避免破坏 Prefab 生命周期；至少处理 `Collider/Rigidbody/NavMeshAgent/AudioSource/ParticleSystem/TrailRenderer/VisualEffect/Light`，并禁用 world-space Canvas、运行时控制器和交互事件。
- Animator 仅在确认默认待机动画安全时保留；明确 Root Motion、动画事件和暂停时更新策略。

### 5.6 类型化预览尺寸（可选增强）
- `UnitConfigSO` / `BuildingConfigSO` 增加 `previewScale`（或 `previewCameraSize`），用于标定不同体积单位/建筑在 100% 时的大小；缺省时用渲染 bounds 自动适配。

---

## 6. 动画时间线（每帧状态）

| upwardDistance | 卡牌 | 模型 |
|---|---|---|
| `0` | scale 100%、alpha 1 | 未创建 / alpha 0 |
| `0 ~ 230`（cardProgress 0~0.6） | 缩小中，alpha 保持 1 | 未创建 |
| `230 ~ 384`（cardProgress 0.6~1） | 缩小至 10%，alpha → 0 | 未显示；达到 D1 时创建，初始 alpha 0 |
| `384 ~ 768`（modelProgress 0~1） | scale 10%、alpha 0 | scale 10%→100%，alpha 0→1 |
| `> 768` | 维持 10%、alpha 0 | 维持 100%、alpha 1（封顶） |

> 模型在 `upwardDistance >= D1` 时创建；同一拖拽周期只允许一次实例化。若用户从模型阶段反向拖回 D1 以下，模型保持实例但隐藏 RawImage 或将 alpha 置 0，不重复创建；具体行为必须固定后再实现。
> 若一次 OnDrag 从 0 跳到超过 D2，必须在同一帧创建模型并直接应用当前 `modelProgress/modelAlpha`，不能依赖逐帧经过 D1。

---

## 7. 释放与取消衔接

- **落点有效**：当前 `CardController.OnEndDrag` 在 `HandleCardDragEnd` 返回 true 后直接 return，且普通卡随后会被 `ConsumePlayedCard` 失活并销毁；因此必须增加 `OnCardDragEnd` 成功通知，或在明确的部署提交边界中调用 `_preview.End()`。不能只依赖 `OnCardDragCancel`/`OnDisable`。
- **成功清理顺序**：推荐 `OnCardDragEnd` 先关闭预览，再执行正式生成；若为避免业务耦合而在 `HandleCardDragEnd` 内清理，也必须保证失败返回不会误清理下一张卡，并使 `End()` 幂等。
- **落空 / 暂停 / 失焦**：`OnCardDragCancel` → `_preview.End()`；卡牌 `ResetToOrigin()` 弹回（已实现）。
- **暂停**：`GameLoop.IsPaused` 时 `OnEndDrag` 已有取消路径；预览相机/RT 也随 `End()` 关闭。
- **场景切换/卡牌销毁**：预览控制器自身必须有 `OnDestroy`/场景清理路径，释放 RT、隐藏 RawImage、关闭相机并销毁当前模型；不能只依赖被拖卡牌的生命周期。

---

## 8. 边界情况

| 场景 | 处理 |
|---|---|
| 战术卡（`NormalCardConfig == null`） | 不创建预览，维持现有幽灵卡缩放（minScale 0.6 不变） |
| 普通卡但 `unitModel/buildingModel` 为 null | 只缩卡，不出模型（fallback） |
| 拖拽中变为买不起 / 金币变化 | 沿用现有 `SetAffordable` → `CancelDrag` 路径，预览随之 `End()` |
| 拖拽中卡被销毁（场景切换） | `OnDisable` 已处理取消；预览 `End()` 需防御 `_currentModel == null` |
| 释放到不能放的格 | 高亮本就不亮，`HandleCardDragEnd` 返回 false → 走取消 → `End()` |
| 向下拖拽 | `upwardDistance=0`，不触发模型阶段，卡牌保持 100% 缩放与 alpha 1 |
| 反向跨过 D1 | 不重复 Instantiate；模型按当前进度隐藏/显示，卡牌 alpha 与 scale 连续恢复 |
| 有效释放后回调迟到 | 用当前 View/token 校验，旧卡不能清理新拖拽卡的预览 |
| 模型包含血条/VFX/音频/导航组件 | 预览实例禁用这些运行时组件，仅保留已确认安全的 Renderer/Animator |

---

## 9. 性能与微信小游戏

- **单实例复用**：全局一张 RT、一台预览相机；同一拖拽周期只有一个模型实例。跨拖拽是否保留模型实例需根据模型 Prefab 是否变化决定，不能把每次 `Instantiate/Destroy` 误称为零 alloc。
- **按需渲染**：预览相机仅拖拽期间 `enabled = true`，平时关闭。
- **RT 尺寸**：默认 512，可配置降 256。
- **模型加载**：`unitModel/buildingModel` 已随配置加载，`Instantiate` 成本一次；如需极致优化可对象池化常用预览模型（列为后续优化项）。
- **每帧约束**：禁止每帧 `GetComponentsInChildren`、bounds 全量扫描、材质实例化、集合分配和 RT 创建；模型 bounds、相机取景、Layer 列表在 Begin 阶段缓存。
- **暂停约束**：预览是位移驱动；暂停发生时沿用现有取消路径。Animator/自转如果保留，必须显式检查 `GameLoop.IsPaused`。

---

## 10. 验收标准

1. 拖拽普通单位/建筑卡向上，卡牌随位移缩小至 10% 并淡出，模型从 10% 反向放大至 100% 并淡入。
2. 预览窗口始终跟随指针；`D1/D2` 处缩放/透明度连续无跳变。
3. 松手到有效格：预览模型立即消失，真实单位/建筑正常出现在落点，卡牌正常消耗扣费。
4. 松手到无效格/取消/暂停：预览模型消失，卡牌弹回原位。
5. 战术卡拖拽行为与改造前一致。
6. 主画面不出现预览模型残影（Layer/Culling 正确）；预览不影响任何射线/碰撞/交互。
7. 微信小游戏 / 低配设备下拖拽期间无明显掉帧、无每帧 GC 峰值。
8. 卡牌买不起时的 `CaptureBaseColors/SetAffordable` 压暗，与拖拽淡出叠加后可恢复；取消、失焦、场景切换后卡牌和预览透明度均正确。
9. 快速跨越 D1/D2、反向拖回、屏幕边缘拖拽、模型无 Animator/含子级 Canvas/VFX 等情况无黑底、裁剪、重复实例或残留。
10. 有效释放一定先清理预览，再生成真实实体并消费卡牌；无效释放、暂停和失焦均弹回卡牌。

---

## 11. 实施顺序（里程碑）

1. **基础设施**：加 `CardPreview` Layer → 预览相机 + RT + RawImage + 挂载点（场景/资产）。
2. **预览控制器**：新增 `CardDragPreviewController`，先用手动调用验证“显示任意 prefab 到 RawImage”。
3. **纯展示化工具**：`StripToVisual` + bounds 取景适配。
4. **拖拽生命周期协议**：新增逐帧视觉更新和成功结束通知；先打通 Begin/Update/End/Cancel 四条路径，再接模型。
5. **CardController 视觉状态**：改造向上距离、两段缩放、统一 alpha 合成、代理恢复；验证战术卡 Ghost Proxy 不变。
6. **普通卡接线**：`CardPresenter` 驱动预览并按当前 View 校验；`TacticalCardPresenter` 不创建模型但回归原有借出/幽灵流程。
7. **数值收口**：`D1/D2/minScale/淡出阈值` 及新增旋转/RT/窗口参数同步迁入完整 `FeelConfig` 导入链路。
8. **联调与验收**：按 §10 逐项验证，回归战术卡拖拽、部署扣费、暂停和场景切换。

---

## 12. 风险与待定

- **RT 透明背景**：预览相机背景 alpha=0 与 RawImage 材质需搭配正确，否则出现黑底/白底方块（联调重点）。
- **取景适配**：单位与建筑体积差异大，`previewScale` 缺失时依赖 bounds 自动适配，需在阶段 3 统一验证。
- **卡牌 alpha 与压暗冲突**：优先统一计算 `baseAlpha × affordableAlpha × dragAlpha`；若最终改用 `CanvasGroup`，必须由单一组件负责设置和复位，避免与 `SetAffordable/CaptureBaseColors` 各自覆盖。
- **微信小游戏 RT 兼容性**：需在真机验证 RT + 透明背景 + 每帧关闭/开启的稳定性。
- **成功路径残留**：`HandleCardDragEnd` 成功不会调用取消回调，必须用显式 `OnCardDragEnd` 或明确提交边界清理，否则 RawImage/模型会残留。
- **Canvas 坐标与裁剪**：主 Canvas 为 Overlay，RawImage 位置换算、pivot、边缘 Clamp 和安全区必须固定，否则预览会偏移或出屏。
- **模型 Prefab 生命周期**：禁止直接调用正式 `SpawnUnit/SpawnBuilding` 生成预览；预览实例不能注入完整运行时控制器，避免注册单位、建筑、血条和交互。
