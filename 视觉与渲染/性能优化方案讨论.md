# 性能优化方案讨论

> 目标规模：优先保证 **20×30（600 格）** 地图；当前配置仍为 11×11
> 当前状态：性能方案讨论，部分已实施
> 核心原则：先消除高频热路径中的 O(地图格数) 与重复寻路，再根据 Unity Profiler 数据决定是否重写渲染架构

---

## 一、当前架构前提

- 游戏由 `GameLoop.Tick` 实时驱动，空闲单位持续执行自动决策。
- 地图全图始终可见，只保留 `IsExplored`；探索由玩家点击、AI 定时探索、主城初始化和公共建筑占领改变。
- 单位移动不再改变探索状态。

### 20×30 下的判断标准

- **必须优先处理**：每单位每帧或每次逐格移动都会触发的全图扫描、重复 Dijkstra、全图 Mesh 更新。
- **需要测量后处理**：约 2800 级过渡 submesh/material、逐格地貌资源 Renderer。
- **暂不优先**：Chunk、TextureArray、Job/Burst、Shader LOD 等高成本架构改造。

---

## 二、先建立性能基线

先增加 20×30 测试配置并用 Development Build + Unity Profiler 建立基线，否则无法判断渲染与 AI 各自占比。

### 测试矩阵

| 地图 | 单位总数 | 测试场景 |
|---|---:|---|
| 20×30 | 10 | 基础运行、探索、拖牌、战斗 |
| 20×30 | 30 | 中期持续移动与索敌 |
| 20×30 | 60 | 压力场景，多单位同时空闲/重新决策 |

### 必看指标

- CPU Main Thread：`GameLoop.Tick`、单位 Brain、寻路、`MapRenderer.OnMapVisualChanged`
- GC Alloc：稳定运行目标为常态接近 0 B/frame；记录决策帧和探索帧峰值
- Rendering：Batches、SetPass Calls、Triangles、主线程 RenderLoop
- 场景对象：GameObject、Renderer、Material、Mesh、Collider 数量
- 加载：地图生成耗时、峰值托管内存、首次 Shader/材质卡顿

给以下路径添加 `ProfilerMarker`：单位决策、目标选择、单次寻路、世界坐标转格子、视觉事件、费用标签刷新。

---

## 三、P0：20×30 必须优先解决

### 1. 世界坐标查格从 O(N) 改为 O(1) ✔

**现状**：`HexMapService.TryWorldToHexCoordinate` 遍历全部格子中心找最近点，随后 `GetCellRadius` 又遍历一次。`UnitBrainBase.Update` 中每个单位每帧会为农田/祭坛回血检查多次调用，整体代价按"单位数 × 帧数 × 600"增长。

**已实施**：

- `cellRadius` 在 `HexMapService.Initialize` 中计算一次并缓存；`GetCellRadius` 改为读缓存。✔
- `UnitMovementController.CurrentHexCoordinate` 改为懒缓存 + `OnMoveFinished` 更新，不再每次从世界坐标重算。✔
- `UnitBrainBase` 新增 `GetCurrentCell()` 辅助方法，用 `MapData.GetCell(hexCoord)`（O(1) 字典查找）替代 `GetCellByWorldPosition`（O(600) 扫描）。✔
- `TryStartRangedAttack` 中计算当前格距离也改为使用缓存的 `CurrentHexCoordinate`。✔

**剩余**：鼠标射线命中位置的 hex round 反算暂未改（`PlayerInputHandler` 中偶发调用，非每帧热路径）。

### 2. 降低实时 AI 的重复 Dijkstra（部分完成）

**现状**：空闲 Brain 在 `GameLoop.Tick` 中调用 `OnStepFinished`。当前索敌可能对每个敌方单位/建筑分别寻路，选定目标后 Strategy 再寻路，`RequestMove` 还会重算路径。现有 Dijkstra 每次创建 Dictionary、List、HashSet 和优先队列，并在扩展邻居时执行 `allPoints.Contains` 线性查询。

**已实施**：

- `UnitMovementSystem.CalculateMinMovementCostBetweenTwoHexes` 和 `GetAllReachableHexesFromStartHex` 入口将 `List<Vector3> allPoints` 转为 `HashSet<Vector3>`，邻居有效性判断从 O(600) 降为 O(1)。每次 Dijkstra 最坏情况从 200 万次比较降为约 3600 次。✔
- `RangedStrategy.ChooseNextStep` 删除冗余的 `CanAttack` 调用（`OnStepFinished` 已检查）。✔
- `PlayerUnitBrain` / `AIUnitBrain` 的 `FindNearestEnemy` 和 `FindNearestEnemyBuilding` 使用缓存的 `CurrentHexCoordinate`，去掉 `GetCellByWorldPosition`（O(600)）和 `WorldToHexCoordinate`（O(600)）调用。✔
- 六边形距离预筛：Dijkstra 前先用 `HexDistance(start, end)` 与 `bestCost` 比较，跳过距离已超过当前最优的候选。✔
- `HexDistance` 提取为 `UnitBrainBase` 的 `protected static` 方法。✔

**剩余**：目标缓存与决策节流（`UnitBrainBase` 每 20 帧搜索一次空目标，`OnMoveFinished` 强制触发完整搜索）；一次寻路结果贯穿整个决策链；Dijkstra 内部字典按 `GenerateOrder` 复用数组。

### 3. 拆分并收紧地图视觉事件（部分完成）

**现状**：`UnitMovementController.OnMoveFinished` 调用 `_mapVisualEvent.Raise()`，但移动已经不会改变 `IsExplored`。一次 Raise 触发：

- 地形、水面、河流顶点色全量改写并上传；
- FogMask 更新；
- `SyncCellObjectVisibility` 遍历所有格和敌方单位；
- 探索费用标签与势力边界等订阅者刷新。

**已实施**：

- 删除单位普通移动结束时的地图视觉事件。✔
- `OnMapVisualChanged` 精简为仅 `UpdateFogMask()`：FogBlend.cginc 确认地形/水/河的顶点探索色（R 通道）已不被 Shader 读取，`UpdateExplorationVisuals` 为死代码，顶点色全量上传已移除。✔
- `SyncCellObjectVisibility` 整条方法和调用已删除（全图始终可见后无业务价值）。✔
- 敌方 Renderer/Canvas 缓存（`_enemyRendererCache`/`_enemyCanvasCache`）、`OnEnemyUnitAdded`/`OnEnemyUnitRemoved` 事件订阅及 `_unitRepository` 注入已移除。✔

**验收**：单位逐格移动不触发 Mesh colors、FogMask、费用标签或势力范围重建；主动探索和公共建筑占领仍正确更新。

### 4. 重做逐格网格线存储 ✔

**现状**：每个非水格创建一个隐藏 `SubGridLine_j` GameObject、Mesh、Material 和默认 MeshCollider。每个子 Mesh 都传入整张地图的 `verticesGridLine.ToArray()` / `uvGridLine.ToArray()`，只使用自己的 indices。

**已实施**：

- 每个 `SubGridLine` 的 Mesh 改为只包含自身 12 个顶点 + 局部索引（不再拷贝整图顶点缓冲）。✔
- `MapController.CreatMesh` 增加 `addCollider: false`，去掉每格的 MeshCollider。✔
- 保留逐 GameObject 结构以支持逐格 `SetActive` 高亮和材质颜色控制（`UIController` / `PlayerInputHandler` 依赖）。

**收益**：顶点内存从 12×N×N 降为 12×N，消除约 600 个无用 Collider，逐格高亮和 G 键开关不受影响。

---

## 四、P1：完成 P0 后按 Profiler 决定

### 5. 合并过渡 submesh/material ✔

**现状**：地形仍为单个 Mesh，每条矩形过渡和三角过渡各占一个 submesh，并 `new Material`。20×30 下实际约为 2800 级。

**已实施**：按材质组合签名归组（3 类基础地形 → 矩形最多 9 组、三角最多 27 组），相同签名共享一个 submesh。submesh 从约 2800 级降到最多 36 级。✔

### 6. 删除探索视觉中的失效工作 ✔

需在实现前用 Frame Debugger/Shader 检查确认：

- 地形探索视觉当前主要读取 FogMask R 通道，顶点色若已不参与结果，应停止 `UpdateExplorationVisuals` 的全量上传。✔
- FogMask G 通道仍被维护，但当前 Shader 只使用 R 时，应删除 G 通道清理、盖章与旧缓存。✔
- 敌方 Renderer/Canvas 缓存服务于战争迷雾显隐；全图可见后应移除。✔

### 7. 地图集合与寻路容器减分配 ✔

**现状**：`GetAllCells`、`GetAllHexCoordinates` 和 `AllEnemyUnitGroups` 的部分访问会通过 LINQ 创建新 List；`GetNeighbors` 也会创建 List。实时决策中反复调用会产生 GC。

**已实施**：

- `HexMapService.GetAllCells()` 和 `GetAllHexCoordinates()` 改为返回缓存列表，首次访问时从字典 Values/Keys 生成，之后直接返回。✔
- `UnitMovementSystem.AllHexCoordinates` 统一使用缓存列表。✔

### 8. 探索费用标签事件化 ✔

**现状**：`CostLabelRenderer` 每秒及每次地图视觉事件扫描全部格子；标签已有对象池，`LateUpdate` 只处理当前边界标签。

**已实施**：

- 移除每秒定时全图扫描 (`Update` 中的 `_refreshTimer`)。✔
- 订阅 `GoldWallet.OnGoldChanged`，金币变化时只遍历现有标签更新可支付状态，不重新扫描全图。✔
- `OnDestroy` 合并，确保注销所有事件订阅。✔

---

## 五、P2：暂缓或仅在数据证明后实施

### 9. 地貌、资源、建筑和单位渲染 ✔

600 格仍可能产生较多 Renderer，但先看 Batches 与 SetPass：

- 同种静态地貌/资源可用 GPU Instancing 或静态合批。✔ (`StaticBatchingUtility.Combine` 应用于 LandForm 和 Resource 父节点)
- 单位数量较高时再做动画 LOD、血条可见性和距离裁剪。
- 普通建筑若大量使用各自 `Update`，再考虑集中 Tick。

### 10. 本阶段不建议优先做的项目

| 项目 | 暂缓原因 | 重新考虑条件 |
|---|---|---|
| Chunk 分区 | 600 格单 Mesh 的剔除与局部更新收益有限，改造边界复杂 | 主 Mesh 上传或剔除被 Profiler 证明为瓶颈 |
| TextureArray + 全新混合 Shader | 开发与美术验证成本高 | 材质组合归组后 SetPass 仍不达标 |
| Job System + Burst | 当前更大的问题是重复算法与分配 | 单次地图生成 CPU 仍明显超预算 |
| Shader LOD / 未探索 early-out | 全图可见且 600 格，收益不确定 | GPU frame time 明确受地形片元限制 |
| 完全删除主地图 MeshCollider | 当前只有一个主 Collider，点击拾取频率不高 | 拖牌期间 Physics.Raycast 成为实测热点 |
| AI 分帧 Coroutine | 可能掩盖重复寻路并引入行为延迟 | 完成目标缓存/单次寻路后仍有不可接受峰值 |

---

## 六、实施顺序

```text
20×30 Profiler 基线
    ├── 坐标查询 O(1) + 单位持有当前格 ✔
    ├── AI 目标缓存、候选过滤、单次寻路 ✔
    ├── 移除移动触发的全图视觉事件 ✔
    └── 网格线 vertex 瘦身 / 无 Collider ✔
            ↓
第二轮 Profiler 对比
    ├── OnMapVisualChanged 精简为仅 UpdateFogMask ✔
    ├── 删除 SyncCellObjectVisibility / 敌方缓存 / 死顶点色 ✔
    ├── FogMask G 通道删除 ✔
    ├── 地图集合缓存 + 费用标签事件化 ✔
    ├── 过渡材质归组 ✔
    └── 地貌资源静态合批 + Brain 决策节流 ✔

---

## 七、验收标准

### 功能回归

- 20×30 地图生成、河流/水面、过渡材质和网格线显示正确。
- 世界坐标反算在中心、六边形边缘、地图边缘均正确；拖牌与点击探索不选错格。
- 玩家/AI 单位仍能自动索敌、寻路、攻击，不进入每帧重复决策或卡死。
- 玩家探索、AI 探索、主城初始化、公共建筑占领均正确更新探索遮罩、势力边界和费用标签。
- `G` 网格开关、暂停/继续、天赋选卡暂停语义不变。

### 性能验收

最终阈值需按目标机器确定，至少记录并对比：

- 20×30、30/60 单位时平均帧时间与 99th percentile 主线程帧时间；
- 稳态 GC Alloc/frame、单位集中决策峰值、单次探索峰值；
- Batches、SetPass Calls、Material/Mesh/GameObject/Collider 数；
- 地图生成耗时与首次进入游戏峰值。

### 核心判断

对 20×30 地图，P0 各项已实施：

1. 消除单位高频世界坐标 O(600) 扫描 ✔
2. 将自动战斗中的"每帧、每目标、多次 Dijkstra"收敛为受控频率的一次有效寻路 ✔
3. 删除单位移动触发的探索全图刷新 ✔
4. 把逐格且重复整图顶点的网格线改为 vertex 瘦身 ✔
5. 删除死代码（顶点探索色全量上传、SyncCellObjectVisibility、敌方缓存）✔
