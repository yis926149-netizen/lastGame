# 迷雾边缘柔化方案

> 讨论时间：2026-07-28 | 目标：让迷雾与已探索地块的边界不那么清晰分明
> 
> 实现方式：沿用现有 `ShadingStyle` 的下拉框模式，不删除任何旧代码

---

## 一、设计思路

当前边界清晰的原因是：`FogMask` 中每个像素只有 `0/255` 二值，`FogBlend_resolveEdge()` 围绕 `0.5` 做极窄的 `smoothstep`。过渡带仅由 `fwidth` 抗锯齿产生，通常只有屏幕上一两个像素宽。

解决方案：在 Shader 中新增多种边缘柔化风格，通过下拉框切换对比。旧代码完全保留，`Original` 风格与现状 100% 一致。

---

## 二、需要修改的文件（4个）

### 1. `Assets/Scripts/Utilities/Enums.cs`

在 `ShadingStyle`（第69行）下方新增枚举：

```csharp
// 迷雾边缘风格（下拉框对比用，不删除旧逻辑）
public enum FogEdgeStyle
{
    Original,       // 现状：二值遮罩 + 窄 smoothstep（清晰边界）
    BlurMask9,      // 9点世界空间模糊遮罩（柔和过渡带）
    WideSmooth,     // 更宽的 smoothstep 拉软过渡（最轻量）
    DitheredEdge,   // 边界带内做噪声抖动（雾气消散感）
    SoftPlusFogBand // 组合：模糊 + 边缘动态薄雾
}
```

### 2. `Assets/Scripts/ScriptableObjects/MapGenerationConfigSO.cs`

在第125行"迷雾锯齿边界" header 附近追加：

```csharp
[Header("迷雾边缘风格（下拉框对比）")]
[Tooltip("切换后重新进入 Play 模式对比不同边界柔化方案。Original=现状")]
public Enums.FogEdgeStyle fogEdgeStyle = Enums.FogEdgeStyle.Original;
[Tooltip("边缘柔化宽度（世界单位），仅非 Original 风格生效。建议 0.6~1.2")]
[Range(0f, 3f)]
public float fogEdgeSoftness = 0.8f;
```

注意：`fogEdgeSoftness` 用世界单位，与现有 `fogMaskTexelSize`（同时控制分辨率）独立。

### 3. `Assets/Scripts/Managers/MapRenderer.cs`

在 `SetupFogGlobalShaderProperties()` 第1163行（`_FogNoiseWavelength` 之后）加两行：

```csharp
Shader.SetGlobalFloat("_FogEdgeStyle", _config != null ? (float)(int)_config.fogEdgeStyle : 0f);
Shader.SetGlobalFloat("_FogEdgeSoftness", _config != null ? _config.fogEdgeSoftness : 0.8f);
```

切换方式：重新进入 Play 生效（与 `ShadingStyle` 一致）。

### 4. `Assets/Shader/FogBlend.cginc`

#### 4.1 新增全局变量声明

在 `_FogMaskTex` 声明（第14行）下方添加：

```hlsl
float _FogEdgeStyle;    // 0=Original,1=BlurMask9,2=WideSmooth,3=Dithered,4=SoftPlusFogBand
float _FogEdgeSoftness; // 边缘柔化宽度（世界单位）
```

#### 4.2 修改 `FogBlend_final()` 中的遮罩采样逻辑

将原来的两行：

```hlsl
float r = tex2Dlod(_FogMaskTex, float4(uvW, 0, 0)).r;
float exploredJagged = FogBlend_resolveEdge(r);
```

改为按 style 分支（旧路径保留在 `Original` 分支中）：

```hlsl
float exploredJagged;
if (_FogEdgeStyle < 0.5) {
    // Original：保持现状
    float r = tex2Dlod(_FogMaskTex, float4(uvW, 0, 0)).r;
    exploredJagged = FogBlend_resolveEdge(r);
} else {
    // 新风格：先得到软遮罩，再按具体 style 处理
    float soft = FogBlend_sampleSoftMask(uvW);
    exploredJagged = ...; // 按 style 细分
}
```

#### 4.3 新增辅助函数

在 `FogBlend_resolveEdge` 下方新增（不改动现有函数）：

```hlsl
// 在世界空间对周围 3x3=9 点采样，返回 0~1 的软遮罩
float FogBlend_sampleSoftMask(float2 uvCenter)
{
    float2 step = (_FogEdgeSoftness / _FogMapSize.xy);
    float sum = 0;
    for (int dy = -1; dy <= 1; dy++)
        for (int dx = -1; dx <= 1; dx++)
            sum += tex2Dlod(_FogMaskTex, float4(uvCenter + step * float2(dx, dy), 0, 0)).r;
    return sum / 9.0;
}
```

#### 4.4 各风格的具体差异

仅在 `else` 块内区分：

| 风格 | 处理方式 |
|------|----------|
| `BlurMask9` | `soft` 直接做 `smoothstep(0.3, 0.7, soft)` |
| `WideSmooth` | 单次采样 + `smoothstep(0.5-w, 0.5+w, r)`，w 由 `_FogEdgeSoftness` 算 |
| `DitheredEdge` | `soft` + 用 `FogBlend_valueNoise`（已有函数）对阈值加抖动 |
| `SoftPlusFogBand` | `soft` + 在边界带（`soft*(1-soft)` 大的区域）叠加额外滚动雾 |

---

## 三、为什么这样设计

| 考量 | 说明 |
|------|------|
| **旧代码不动** | 选 `Original` 即与现状完全一致，零风险 |
| **改动最小** | 仅加 1 个枚举、Config 2 字段、MapRenderer 2 行、cginc 1 分支 + 1 函数 |
| **不碰游戏逻辑** | 探索/AI/领土/存档不受任何影响 |
| **全局生效** | `FogBlend_final` 被 3 个地形 Shader 共同调用，改 cginc 一处全部生效 |
| **世界空间模糊** | 柔化宽度不随遮罩分辨率变化，各风格可共用同一套采样函数 |

---

## 四、切换方式

在 `MapGenerationConfigSO` Inspector 中修改 `FogEdgeStyle` 下拉框 → 重新进入 Play 模式即可对比效果。与现有 `ShadingStyle` 的切换方式一致。
