# 资源精确审计报告

> 审计对象：`E:\BaiduNetdiskDownload\毕设\My project - new`
> 方法：静态解析 Unity 序列化引用链（场景 → GameInstaller 的 11 个 SO 数据库 → Config SO → Prefab → 网格/材质/贴图），用 7812 个 `.meta` 建立 GUID→路径 映射后做**传递闭包（BFS）**，逐文件实测体积。
> 性质：只读分析，未改动任何游戏文件。

---

## 0. 一句话结论

游戏**实际被引用**的资产共 **458 个文件、1,272 MB（1.27 GB）**，其中 **约 1.16 GB 是未压缩的源格式贴图（TGA/EXR/8K PNG）**，真正无法压缩的模型/动画/UI 只有 **约 95 MB**。

> 这比"资产总量 4.4 GB"精确得多：4.4 GB 里约 2.48 GB 是**完全无引用的 demo 垃圾**（但因位于 `Resources` 目录仍会被强制打包），而真正被游戏引用的 1.27 GB 又几乎全是**贴图源文件**。

---

## 1. 核心数据

| 指标 | 数值 |
|---|---|
| 实际可达（被游戏引用）资产 | **458 文件 / 1,272.20 MB** |
| 其中纹理（tga+png+exr+jpg） | **1,176.8 MB（占 92.5%）** |
| 其中模型/动画/UI/材质等 | **95.4 MB（占 7.5%）** |
| 被引用的资产里位于 `Resources/Import` | **≈ 1,186 MB** |
| `Resources/Import` 总量 | 3,669.9 MB |
| `Resources/Import` 中**无引用的垃圾** | **≈ 2,484 MB（2.48 GB）** |
| 非 Resources 目录被引用 | ≈ 86 MB（Model / Toon_RTS / UI / Texture / unity-chan 等） |

---

## 2. 按资源包分桶（被引用体积，降序）

| 资源包 | 体积 | 文件数 | 被谁引用 |
|---|---:|---:|---|
| **Import\Horseman** | **558.09 MB** | 25 | **Melee_3/Ranged_3 近战·远程单位的人形 Base mesh**（8K PBR 源图） |
| **Import\Altar_Ruins_FREE** | **267.25 MB** | 36 | **Altar 建筑**（含 1 张 129.7MB 的 `.exr` 置换贴图） |
| Import\Mini Legion Grunt PBR HP Polyart | 83.73 MB | 12 | Grunt_Melee 单位 |
| Import\FourEvilDragonsPBR | 55.47 MB | 22 | DragonSoulEater / DragonTerrorBringer 单位 |
| Import\SciFiWarriorPBRHPPolyart | 55.08 MB | 11 | Ranged_3 单位 |
| Import\Knight Statue | 48.28 MB | 7 | 某建筑/雕像 |
| Import\RPGHero | 35.05 MB | 13 | RPGHero_Melee 单位 |
| **Model**（游戏自己的 prefab 副本） | 26.99 MB | 58 | 全部单位/建筑 prefab |
| Import\LowPolyEnviroPack_LITE_builtIn | 23.27 MB | 9 | 地图/环境 |
| Toon_RTS | 16.52 MB | 15 | **archer / swordsman 单位**（WK_Standard_Units 材质+动画） |
| Import\Toon Crystals pack | 15.15 MB | 11 | 地图晶体/石头 |
| UI | 14.86 MB | 37 | 卡牌/图标/血条/指示器 |
| Import\Mini Legion Lich PBR HP Polyart | 13.97 MB | 11 | FreeLich_Ranged 单位 |
| Import\Assets_SheepRun | 13.20 MB | 16 | 羊/草（某资源/地貌） |
| Texture | 12.89 MB | 5 | 地图材质贴图（含 10.3MB highLand.jpg） |
| unity-chan! | 6.86 MB | 9 | **CityBuilder 单位** |
| Import\RPGMonsterPartnersPBRPolyart | 5.57 MB | 15 | Beholder / ChestMonster 单位 |
| Import\Magician_RIO | 3.47 MB | 4 | 法师（某单位） |
| TextMesh Pro | 3.02 MB | 4 | UI 文本 |
| Import\RPGMonsterBuddiesPBRPA | 2.81 MB | 15 | Cactus / Mushroom 单位 |
| Particles | 2.27 MB | 4 | 采集/探索特效 |
| Import\Understone | 1.97 MB | 27 | 公共建筑/轮城 |
| 其余（noise / KayKit / Gridness / JMO / 材质等） | ≈ 20 MB | 100+ | 地图/特效/材质 |

---

## 3. 按格式分桶（被引用体积）

| 格式 | 体积 | 文件数 | 说明 |
|---|---:|---:|---|
| **.tga** | **635.31 MB** | 23 | 未压缩源图（Horseman 身体/武器 64MB/张） |
| **.png** | **400.19 MB** | 121 | 含大量 4K/8K PBR 贴图 |
| **.exr** | **129.71 MB** | 1 | Altar 的置换贴图（纯浪费） |
| .jpg | 11.85 MB | 2 | — |
| **纹理小计** | **≈ 1,177 MB** | 147 | **占总量 92.5%** |
| .fbx | 63.88 MB | 137 | 模型（体量合理） |
| .prefab | 21.96 MB | 46 | — |
| .anim / .controller | 5.88 MB | 27 | — |
| .asset / .mat / .ttf / shader / cs | ≈ 3.4 MB | 101 | — |

---

## 4. 三大关键发现

### 4.1 头号元凶：Horseman 包 558 MB，被近战/远程单位拖入

追踪 `Melee_3.prefab`（UnitConfig-6 的单位模型）发现，它引用：
- 网格 `Base mesh.fbx`（Horseman 的人形基础网格，GUID `59d8c5f8…`）
- Horseman 的 4 个材质：`4021754b…`(身体)、`68a2d706…`(武器)、`181d2c95…`(头)、`2dcfadef…`(头盔)

这 4 个材质引用 **8K TGA 源图**：`T_Body_Albedo.tga`(64MB)、`T_Weapon_*.tga`(64MB)、`T_Body_*.tga`(48MB)、`T_Head/Helmet_*.tga`(12–16MB) 等，**25 个文件合计 558 MB**。

> 仅这一个单位（近战/远程系列）就贡献了全项目被引用体积的 44%。把它从 8K TGA 降到 1K 贴图，可一次性省下 ~500 MB。

### 4.2 `Resources` 目录是"强制打包放大器"

Unity 会**无条件把任何 `Resources` 文件夹下的资产打进构建**（即使零引用）。本项目的现状：

```
Assets/Resources/Import  3,669.9 MB
├── 被游戏引用  ≈ 1,186 MB   ← 真正需要（但也大多是源图）
└── 零引用垃圾  ≈ 2,484 MB   ← 会被强制打包，游戏却完全用不到
```

所以即使不做任何"压缩优化"，只要**把 2.48 GB 无引用 demo 素材移出 `Resources`**，构建体积就能立刻从 4 GB 级降到 1.2 GB 级。

### 4.3 孤儿资产：地图资源/地貌的单个 SO 未被接线

审计发现 `MapResourceDatabase.asset` 与 `MapLandFormDatabase.asset` 的列表均为**空**（`resources: []` / `landForms: []`），而独立的 `Animals/Chest/Minerals/Plants/Forest/GoldMine/Stone/BigBones/FromLand` 等 SO **在场景和代码中均无任何引用**（grep 场景 GUID 零命中）。

结论：这些地图资源/地貌模型（约 2 MB prefab + 关联贴图）当前是**死资产**——它们引用的 `Assets\Model\ResourceModel`、`Assets\Model\LandFormModel` 等 prefab 不会随当前玩法实际实例化。由于它们**不在 `Resources` 目录、也不在场景引用链上**，Unity 构建时会自动裁剪（**不打进包**）；删除只是清理工程卫生，**对包体无影响**。

---

## 5. 单位 → 资源包 映射表（谁拖进了哪个包）

| 单位（UnitConfig） | 实际美术来源 | 引入体积 |
|---|---|---|
| archer / swordsman（初始卡） | **Toon_RTS**（WK 材质+动画） | ≈ 16.5 MB |
| Melee_3 / Ranged_3 | **Horseman 人形 Base mesh + SciFiWarrior** | **≈ 558 + 55 MB** |
| CityBuilder | **unity-chan!** | ≈ 6.9 MB |
| Grunt_Melee | Mini Legion Grunt | ≈ 83.7 MB |
| Cactus / Mushroom | RPGMonsterBuddiesPBRPA | ≈ 2.8 MB |
| Beholder / ChestMonster | RPGMonsterPartnersPBRPolyart | ≈ 5.6 MB |
| FreeLich_Ranged | Mini Legion Lich | ≈ 14 MB |
| DragonSoulEater / DragonTerrorBringer | FourEvilDragonsPBR | ≈ 55.5 MB |
| RPGHero_Melee | RPGHero | ≈ 35 MB |
| 建筑（兵营/箭塔/金矿/主城） | **KayKit + Knight Statue + Altar_Ruins** | ≈ 0.97 + 48 + 267 MB |

> 特点：**单位美术是"拼盘"**——从十几个不同的 Asset Store 包里各取一个模型，导致风格不统一，也导致每个单位都拖进一大包高分辨率 PBR 源图。这是包体失控的结构性原因。

---

## 6. 修正后的合理包体预估（基于实测）

### 代码包（不变，与之前一致）
引擎 wasm + 逻辑 + shader：**压缩后 8–14 MB**，主包 ≤ 4MB + 分包。

### 资源包（CDN，实测修正后）

| 项目 | 现状（实测） | 压缩/降级后 |
|---|---:|---:|
| 纹理 1,177 MB（源图） | tga 635 + png 400 + exr 130 | **ASTC/ETC2 压缩 + 8K/4K→1K/2K：≈ 80–150 MB** |
| 模型/动画/UI 等 95 MB | fbx 64 + prefab 22 + anim 6 + 其他 | 基本不动 ≈ 90 MB |
| **全量资源合计** | **1,272 MB** | **≈ 170–240 MB（保守）/ ≈ 100 MB（激进换低模）** |

**合理目标（修正版）**：
- 全量 CDN 资源：**≤ 150 MB**（需纹理压缩 + 删 Horseman 8K + 删 .exr + 精简单位阵容）
- 首屏必加载：**≤ 30–40 MB**
- 若只做"移出 Resources 垃圾"这一步：可先把构建从 ~4GB 压到 ~1.2GB，再做纹理压缩压到 ~150MB。

---

## 7. 行动清单（按性价比排序）

1. **【零成本·立竿见影】把 `Resources/Import` 下 2.48 GB 无引用 demo 素材移出 `Resources`**（或删除）。构建体积 4GB→1.2GB。
2. **【最高收益】处理 Horseman 558 MB**：近战/远程单位换用 1K 贴图（或换 KayKit/Toon RTS 低模角色），单点省 ~500MB。
3. **【删 1 文件省 130MB】删 `Altar_Ruins` 的 `Trim_Sheet_Displacement.exr`**（置换贴图对低模风格无意义）。
4. **【全量压缩】纹理统一 ASTC/ETC2 + 8K/4K→1K/2K**：1.18GB → 100MB 级。
5. **【风格统一】把"拼盘单位"收敛到 1–2 套低模资源包**（如 Toon RTS + KayKit），既省体积又统一美术。
6. **【清理】移除孤儿 SO**（MapResource/MapLandForm 的 Animals/Chest/Forest 等 9 个 SO）及对应的 ResourceModel/LandFormModel prefab。

---

## 8. 审计产物说明

- 本审计由两个临时 PowerShell 脚本完成（`audit_reachable.ps1`、`audit_summary.ps1`），分析结束后已删除，未残留于项目。
- 体积均为**本地文件系统实测**（未考虑 Unity 导入后的 ASTC/ETC2 压缩收益，实际构建体积会小于源文件体积）。
- 本报告结论基于静态引用链解析，Unity 的最终打包清单以 Editor 的 `Editor.log` / Build Report 为准；如需 100% 精确到"哪些资源进入首包"，建议在 Unity 里用 `Build Report` 或 Addressables `Analyze` 复核一次。
